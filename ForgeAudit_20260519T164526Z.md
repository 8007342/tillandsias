# Forge Audit — 2026-05-19T16:45:26Z

## Container Identity

| Field | Value |
|---|---|
| Container name | `forge-tillandsias` |
| Image | `tillandsias-forge` |
| Hostname | `forge-tillandsias` |
| IP | `10.0.42.76` |
| DNS | `10.0.42.1` (podman bridge) |
| OS | Fedora Linux 44 (Container Image) |
| Kernel | `7.0.8-200.fc44.x86_64` (PREEMPT_DYNAMIC) |
| Variant | Container Image (`VARIANT_ID=container`) |
| Shell | bash 5.3.9 |
| Zsh | 5.9 |
| Fish | 4.6.0 |
| OpenCode | 1.15.5 |

## Hardware (Host)

| Resource | Value |
|---|---|
| CPU | Intel Core Ultra 7 265KF |
| Cores | 20 |
| RAM | 62 GiB total / 7.6 GiB used / 54 GiB available |
| Swap | 8.0 GiB total / 2.5 GiB used |
| Disk (overlay) | 952 GiB total / 354 GiB used (38%) |

## Security Posture

| Check | Status |
|---|---|
| Capabilities (Eff) | `0000000000000000` — NONE |
| Capabilities (Bnd) | `0000000000000000` — NONE |
| Seccomp | mode 2 (strict), 1 filter |
| No-new-privileges | Implied by zero capabilities |
| User namespace | `keep-id` (uid=1000 forge) |
| Secrets at `/run/secrets/` | Empty directory — no credentials mounted |
| CA bundle | `/tmp/tillandsias-combined-ca.crt` (225 KB, 108 certs) |

**Assessment**: Fully locked down. Zero capabilities, strict seccomp, no credentials exposed to forge. Matches the Tillandsias enclave security model.

## Network

| Check | Detail |
|---|---|
| Internal IP | `10.0.42.76/16` (enclave bridge) |
| Default gateway | `10.0.42.1` (DNS) |
| External ICMP | ❌ Blocked (no `ping` binary, network filtered) |
| GitHub HTTPS | ✅ 200 OK (via proxy) |
| Proxy | External traffic routed through enclave proxy |
| External exposure | None — no listening ports |

## Runtimes & Languages

| Tool | Version |
|---|---|
| Node.js | v22.22.2 |
| npm | (workspaces/false) |
| Python 3 | 3.14.4 |
| Go | Not installed |
| Rust/Cargo | Not installed |
| gcc / g++ | Not installed |
| Nix | Not installed |
| direnv | Not installed |
| Podman/Docker | Not installed (expected — forge is offline by design) |

## Git State

| Field | Value |
|---|---|
| Branch | `linux-next` (tracking `origin/linux-next`) |
| Working tree | Clean |
| Remote | `origin` → `https://github.com/8007342/tillandsias.git` |
| Recent commits | `fc6e8cd3` fix(tray): detect ptyxis + other modern terminals |
| Version file | `0.2.260519.2` |
| User name | **NOT SET** |
| User email | **NOT SET** |

## Tillandsias Environment

| Variable | Value |
|---|---|
| `PROJECT` | `tillandsias` |
| `TILLANDSIAS_PROJECT_PATH` | `/home/forge/src/tillandsias/` |
| `TILLANDSIAS_WORKSPACE` | `/home/forge/src` |
| `TILLANDSIAS_EPHEMERAL` | `/tmp` |
| `TILLANDSIAS_EXTERNAL_LOGS` | `/var/log/tillandsias/external` |
| `TILLANDSIAS_PROJECT_CACHE` | `/home/forge/.cache/tillandsias-project` |
| `TILLANDSIAS_PULL_CACHE` | `/home/forge/.cache/tillandsias/cheatsheets-pulled/tillandsias` |
| `TILLANDSIAS_SHARED_CACHE` | `/nix/store` (empty — no Nix in forge) |
| `SSL_CERT_FILE` | `/tmp/tillandsias-combined-ca.crt` |
| `OPENCODE` | `1` |
| `container` | `oci` |

## Project Cache State

| Cache | Path | Status |
|---|---|---|
| npm | `~/.cache/tillandsias-project/npm` | Empty (cold start) |
| cargo | `~/.cache/tillandsias-project/cargo` | Not populated |
| go | `~/.cache/tillandsias-project/go` | Not populated |
| pip | `~/.cache/tillandsias-project/pip` | Not populated |
| uv | `~/.cache/tillandsias-project/uv` | Not populated |

## Notable Observations

1. **Fresh forge session**: The cache directories are mostly empty — this is a fresh/fast forge spawn with minimal pre-warming.
2. **No cheatsheets**: `/opt/cheatsheets/` does not exist. Agent-facing cheatsheets were not baked into this image or not mounted.
3. **Git identity unset**: No `user.name` or `user.email` configured — commits from this forge will fail without setting them first.
4. **No Rust toolchain**: This forge image does not include `rustup`, `cargo`, or `rustc`. The Tillandsias Rust project cannot be compiled inside the forge (compilation happens on the host).
5. **Network audit**: HTTPS to GitHub succeeds (via proxy), confirming outbound proxy path works. No direct external network access.
6. **Secure by design**: Zero capabilities, strict seccomp, no credentials, no podman socket — exactly as specified in the Tillandsias architecture.
7. **OpenCode version**: 1.15.5 — recent, major.minor.patch healthy.

## Summary

This forge container is correctly provisioned per the Tillandsias enclave model:
- ✅ Fully offline with proxy-mediated outbound access
- ✅ Zero capabilities, strict seccomp, no-new-privileges
- ✅ No credentials exposed to forge (empty `/run/secrets/`)
- ✅ Language runtimes (Node, Python) available for dev work
- ⚠️ No Rust/Cargo toolchain — expected, build happens on host
- ⚠️ Git identity unset — needs config before any commit
- ⚠️ Cache cold — first operations may pull dependencies
