-- init.lua — Expert system Lua runtime initialization.
-- Hot-reloadable: re-execution is idempotent.
-- All functions are registered as globals by their respective modules.
-- @trace order:902-5bf9

expert.log_info("expert system lua runtime initialized")
