# Forge Build Telemetry Dashboard

Auto-generated metrics tracking the build performance and size of the forge image.

## Build Duration Over Time

```mermaid
xychart-beta
    title "Forge Build Duration (seconds)"
    x-axis "Builds" 1 -> 1
    y-axis "Seconds"
    line []
```

## Image Size Over Time

```mermaid
xychart-beta
    title "Forge Image Size (MB)"
    x-axis "Builds" 1 -> 1
    y-axis "MB"
    bar [0]
```

*Metrics are extracted from `/tmp/tmp.C0MmxsSnBQ/home/.cache/tillandsias/telemetry/build-metrics.jsonl` via semantic distillation.*
