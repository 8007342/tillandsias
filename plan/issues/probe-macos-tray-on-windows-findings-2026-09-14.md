# /probe-macos-tray-on-windows findings — 2026-09-14

Generated by `/probe-macos-tray-on-windows` (the windows host's daily
cross-platform health probe of the macos-tray crate). One section per
build run. A build SUCCESS = healthy cross-platform discipline; a
FAILURE means something broke in the shared modules
(`menu_disabled_v2`, `terminal_attach`) or shared crates
(`tillandsias-host-shell`, `tillandsias-control-wire`).

trace: skills/probe-macos-tray-on-windows/SKILL.md
       skills/build-macos-tray/SKILL.md (the macos host's authoritative
       full-build skill; do NOT invoke from windows)

---

### 20260914T170914Z — ok

- agent_id: windows-esmeraldinha-claude (esmeraldinha, Windows 11, N100 — floor tier)
- head_sha: e6f675d18
- cargo exit: 0
- first error line: none
- classification: expected (cfg-gated stub)
- shared-crate impact: none

**Findings**: green. The stub built cleanly on a head carrying a `--no-ff`
merge of `origin/linux-next` plus this host's own `1155-jurn` work, so the
shared portion of `tillandsias-macos-tray` survives today's trunk content —
`menu_disabled_v2`, `terminal_attach`, `tillandsias-host-shell` and
`tillandsias-control-wire` all compile from a non-macOS host at this head.

Regime: Windows-native NTFS checkout (not drvfs, not inside WSL), cargo
invoked from Git Bash on the Windows side so no `with-wsl2-builder` re-exec
occurred, `--release`, `CARGO_BUILD_JOBS=1`, target directory **cold for this
crate** — 70s.

**That number is not comparable to yesterday's 27s**, and the reason is worth
stating rather than leaving to inference: this run followed a full
`./build.sh --check` on the same host, which rebuilt the workspace into the
*redirected* `CARGO_TARGET_DIR` under the distro. The Windows-side
`./target/release` that this probe uses was therefore untouched by that gate
and cold for this crate, where yesterday's 27s ran against a warm one. Two
runs, two target-directory states, one number each — and neither is a
measurement of the other.

**Sequencing note, and it is the operational half of this entry**: the daily
probe cron fired while the `1155-jurn` gate was mid-run (`cargo test
--workspace` active). The probe was HELD until the gate finished rather than
run concurrently, per this file's own 2026-09-13 entry — the two contend for
the build-directory lock, and two builds were reaped for memory on this host
during the preceding session. The gate is the blocking deliverable and had
already survived one reap; restarting it to make room for a 70-second probe
would have traded a 70-minute gate for nothing. Holding cost the probe about
an hour of latency and cost the gate nothing, which is the right side of that
trade on a single-builder host.

**Cross-host visibility note**: N/A — nothing for the macos host to act on.

**Next iteration ask**: unchanged from 2026-09-13 and now with a second
instance behind it — the skill's Step 1 still reads
`git merge --ff-only origin/linux-next`, which aborts whenever `windows-next`
carries any commit of its own, and which fast-forwards (rather than merging)
when the platform branch is strictly behind trunk, putting trunk's commits on
the first-parent line and causing the pre-push plan-only lane to refuse. The
working form is `git merge --no-ff --no-edit origin/linux-next`, and the one
command that predicts the verdict before pushing is
`git log --first-parent --no-merges --oneline origin/windows-next..HEAD` —
it must list only your own commits. This run used the `--no-ff` form.

Adding one ask this cycle: the skill has no concurrency clause. It should say
that this probe must not run while another cargo build is active on the host —
not as hygiene but because the wall-clock number it records is the only thing
the entry carries, and a contended number is not a measurement. The 2026-09-13
entry recorded the lock contention as an observation; a reader following the
skill top-down has no way to learn it.

**Instrument note, added after the push that carried this file**: the push
log is not a progress indicator, and I read it as one. Both pushes of this
entry produced a log of exactly three lines — `ok:no-stale-base-revert`,
`ok:main-branch-affordance`, `ok:linux-next-merged:1` — and no verdict line.
The first had genuinely not landed; the second HAD, `origin/windows-next`
moving to the expected SHA with the file readable at that ref. So the missing
verdict distinguishes nothing: the same three lines accompany a refusal and a
success. The hook chain's later output does not reach the redirected file on
this host, which makes "the log stopped early" a statement about the log.

The only thing that settled it either time was reading the remote ref and then
the file's content AT that ref. Recorded because the wrong inference is the
cheap one — a short log looks like an interrupted run, and here it is the
normal shape of a completed one.
