# macOS tray e2e findings — 2026-05-30

Generated by `/test-e2e-macos-tray`. One section per run. Complements
`macos-build-findings-2026-05-30.md`: the build file says "bytes are
green," this file says "the user-facing app is green." `SECTION_KIND` is
greppable so sibling hosts can audit macOS UX readiness without running on
macOS.

trace: .claude/skills/test-e2e-macos-tray/SKILL.md (the skill that wrote this)
       openspec/specs/macos-native-tray/spec.md (the UX contract)
       plan/issues/macos-tray-ux-gaps-2026-05-29.md (the durable gap list this asserts against)
       plan/steps/20-macos-tray-v0_0_1.md

---

### 20260530T121550Z — new-gap-surfaced

- agent_id: macos-Tlatoani-MacBook-Air-claude-opus-20260530T121550Z
- head_sha: b4a45622
- installed_version: 0.1.0 (Cargo crate version per --diagnose --json; build VERSION was 0.2.260528.1 — schema mismatch worth raising separately)
- bundle: ~/Applications/Tillandsias.app
- evidence_dir: /tmp/macos-e2e-20260530T121550Z
- ux_gaps_file: plan/issues/macos-tray-ux-gaps-2026-05-29.md

**Launch**:
- launch_status: **died-within-30s** (NEW behavior — bundle survives the 3s autonomous-smoke but exits sometime before 30s)
- quit_status: already-dead (it died on its own; no SIGTERM needed)
- screencapture: not-attempted-this-run (skill §3a was skipped because the process was already dead when §3 began; first-run scaffolding hit a launch-already-died path the skill didn't anticipate)
- ax_dump: not-attempted (AX dump requires live process)

**Gap assertions**:
- gap-1 (status-item-shows-T): **unverified** — could not capture menu-bar evidence; process died before §3a screencapture could run
- gap-2 (wrong-menus): **unverified** — could not AX-dump dead process
- gap-3 (failed-to-fetch-recipe): **fetch-path-succeeds-autonomously** — stderr shows `Start VM: rootfs.img fetched successfully` (line 3 of evidence); rootfs.img persisted at `/Users/tlatoani/Library/Application Support/tillandsias/rootfs.img` as 8 GiB file post-run. User's original "failed to fetch recipe" observation was against the 5e331872 bundle; the current code (b4a45622) appears to have the fetch path working. NOT marking as fully `resolved` because that needs user-attended verification — autonomous evidence is necessary but not sufficient.

**Stderr flagged lines** (full stderr — only 3 lines total, no error patterns):
```
[tillandsias-tray] Auto-boot: spawning worker (image_root=/Users/tlatoani/Library/Application Support/tillandsias)
[tillandsias-tray] Start VM: rootfs.img missing at /Users/tlatoani/Library/Application Support/tillandsias/rootfs.img; attempting recipe-artifact fetch
[tillandsias-tray] Start VM: rootfs.img fetched successfully
```

**Diagnose snapshot** (post-run, on the persisted image_root):
- provisioned: false
- release_tag: v0.2.260526.1
- manifest_pin_aarch64_img: 6859a7bcc4a9
- rootfs_present: true, rootfs_bytes: 8589934592 (8 GiB)
- kernel_present: false (this is what blocks provisioned=true)
- initrd_present: false (same)
- An additional `rootfs.img.xz.partial` (76 MB) was also observed on disk — suggests a second fetch attempt got partway, or a separate decompress staging file lingered.

**Findings** (free-form):
- **NEW GAP**: The tray dies between t=3s (alive in autonomous-smoke) and t=30s (dead in e2e). Post-recipe-fetch path is the suspect — the last stderr line is `rootfs.img fetched successfully`, then silence, then process exit. Possibilities: (a) the auto-boot worker exits cleanly after fetch completes because the rest of the provisioning (kernel/initrd fetch + VZ guest boot) isn't wired yet, taking the parent down with it; (b) a panic in the post-fetch path that doesn't make it to stderr (could be SIGABRT or similar); (c) intentional behavior in dev mode. Filing as **gap-4** in `plan/issues/macos-tray-ux-gaps-2026-05-29.md`.
- **Surprise**: gap-3 ("failed to fetch recipe") as written in the gap file reflects an older observation. Current code's fetch path SUCCEEDS — confirmed by both stderr trace AND the persisted 8 GiB rootfs.img on disk. The gap shouldn't be considered open for the fetch step itself; what may still be broken (and what the user saw) is the *user-visible surfacing* of fetch status — i.e., the tray menu/UI might still show "failed to fetch recipe" even when the fetch succeeded at the worker level. Need user-attended re-verification on the new bundle.
- **Skill self-refinement (§9 candidate)**: this run hit a path the skill scaffolding didn't anticipate — the bundle dies between the launch and the §3 observation phase. The skill should: (i) check liveness BEFORE §3a screencapture and skip the live-process checks gracefully, (ii) still attempt §3b/§3d/§3e (bundle resource scan, post-run stderr scan, post-run diagnose) since those don't need a live process. Will roll this into a skill-iteration commit.

**Cross-host visibility note**: N/A. UX bugs are macOS-local. The recipe fetch path success is good news for any sibling-host that was assuming the macOS fetch was a blocker.

**Next iteration ask**: scaffold the §3a-fallback-when-already-dead path; user-attended verification of gap-1/gap-2 against the fresh bundle (in `~/Applications/Tillandsias.app`) is the real signal we still need.
