# /probe-macos-tray-on-windows findings — 2026-09-12

Generated by `/probe-macos-tray-on-windows` (the windows host's daily
cross-platform health probe of the macos-tray crate). One section per
build run. A build SUCCESS = healthy cross-platform discipline; a
FAILURE means something broke in the shared modules
(`menu_disabled_v2`, `terminal_attach`) or shared crates
(`tillandsias-host-shell`, `tillandsias-control-wire`).

trace: skills/probe-macos-tray-on-windows/SKILL.md
       skills/build-macos-tray/SKILL.md (the macos host's authoritative
       full-build skill; do NOT invoke from windows)

---

### 20260912T023115Z — ok

- agent_id: esme-windows (ESMERALDINHA, N100/16GB, Windows 11 + Git Bash, drvfs checkout)
- head_sha: 81820c05f
- cargo exit: 0
- first error line: none
- classification: expected (cfg-gated stub)
- shared-crate impact: none

**Findings**: `cargo build -p tillandsias-macos-tray --release` succeeded in
1m53s with `CARGO_BUILD_JOBS=2` (this host's floor setting). The cfg-gating is
intact: no Apple-only dependency was pulled on Windows. Every crate this probe
transitively pins compiled clean —

- `tillandsias-host-shell` v0.1.0
- `tillandsias-control-wire` v0.1.170
- `tillandsias-core` v56.9.11
- `tillandsias-scanner` v56.9.11
- `tillandsias-secure-channel` v0.1.0
- `tillandsias-vm-layer` v0.1.0
- `windows` v0.58.0

No warnings surfaced in the tail of the build log, so this is `ok` rather than
`degraded`. This is the healthy steady state the skill describes; no escalation
and no entry appended to `plan/issues/tray-convergence-coordination.md`.

Recorded at head 81820c05f, which is windows-next fast-forwarded onto the
then-current origin/linux-next. Trunk moved during this cycle (linux-next
reached d2f550ed3); the probe verdict is pinned to the SHA above, not to trunk
head.

**Cross-host visibility note**: nothing for the macos host to act on. The
shared portion of macos-tray builds from a non-macOS host today, so a macOS
build failure observed in `/build-macos-tray` on the same head would be
platform-specific rather than shared-module breakage — this run narrows that
search space.

**Next iteration ask**: three defects in the skill file itself, none affecting
today's verdict, all worth fixing before the next daily —

1. **Step 2 contradicts Step 3.** Step 2 says "The build is EXPECTED to fail",
   but the Step 3 table and the skill's own prose both say exit 0 is the
   expected steady state and a failure is the escalation case. The prose is
   right; the Step 2 line is stale from before the cfg-gating landed and
   inverts the probe's meaning for anyone reading top-down. It should read
   "The build is EXPECTED to succeed as a stub."
2. **Wrong working dir.** The skill states `C:/Users/bullo/src/tillandsias`.
   This host's checkout is `C:/Users/bullo/claudia/tillandsias`. A literal
   follow of the skill fails at step 1 on this host.
3. **Step 6 commits the legacy path.** Step 4 designates
   `plan/issues/probe-macos-tray-on-windows-findings-<DATE>.md` as the current
   path and marks `plan/diagnostics/build-macos-tray-from-windows-<DATE>.md`
   legacy, but Step 6 still `git add`s only the legacy path — so following the
   skill verbatim would commit nothing and silently lose the findings. This run
   committed the current path instead.

A fourth, operational: `cargo` here contends with `scripts/cycle-preflight.sh`
for the build-directory lock (`Blocking waiting for file lock on build
directory` appeared at the head of this run). The probe and the preflight
should not be run concurrently on this host, or the probe's wall-clock number
stops being comparable day to day.
