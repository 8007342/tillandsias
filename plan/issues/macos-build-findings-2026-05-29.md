# macOS tray build findings — 2026-05-29

Generated by `/build-macos-tray`. One section per build run. Sibling hosts
should grep `SECTION_KIND` to see whether the macOS build is green on a
given day; section bodies surface anything that affects cross-host work
(rare unattended regressions, install-script edge cases, smoke surprises).

trace: .claude/skills/build-macos-tray/SKILL.md (the skill that wrote this)
       plan/steps/20-macos-tray-v0_0_1.md (the macOS tray v0.0.1 ledger)
       plan/issues/osx-next-work-queue-2026-05-25.md (work queue)

---

### 20260529T190656Z — ok

- agent_id: macos-Tlatoani-MacBook-Air-claude-opus-20260529T190656Z
- head_sha: 4bc7f0e8
- version: 0.2.260528.1
- build_run_id: 20260529T190656Z

**Build**:
- duration: 14 s wall-clock (cargo build --release 13.79s + sign/tar/sha overhead)
- tarball: tillandsias-tray-0.2.260528.1-macos-arm64.tar.gz (1.52 MiB, sha256 bfb3a0dffc144dadd495ec548142e5ed98e626c97748e0ccc3b8a5e8fd84ff18)
- codesign verify: pass (`valid on disk` + `satisfies its Designated Requirement`)
- entitlement com.apple.security.virtualization: present (the script greps for it after sign; no `--die` triggered)

**Autonomous smoke**:
- `--diagnose --json` exit: 2 (degraded — `provisioned: false` because `kernel.bin` / `initrd.bin` are absent on this dev host; rootfs.img is present-but-empty at 0 bytes. Per the spec invariant the exit-code tri-state {0,2,1}, this is a clean "degraded" state, NOT a hard failure. Expected on a fresh dev host without a successful Start VM cycle.)
- `--diagnose --json` keys present: [exe_path, image_root, in_app, initrd_bytes, initrd_present, kernel_bytes, kernel_present, manifest_pin_aarch64_img, provisioned, release_tag, rootfs_bytes, rootfs_present, version] — all 13 schema keys per slice-26 `litmus:macos-tray-diagnose-cli-surface`
- detached launch: alive-after-3s (and the stderr-tail shows the auto-boot worker engaging the recipe-artifact fetch path — m5 wiring is live)
- SIGTERM round-trip: clean-SIGTERM-exit (no SIGKILL needed; the `quitWithDrain:` slice-6 plumbing handles SIGTERM cleanly)

**Install**:
- target: ~/Applications/Tillandsias.app
- backup made: no — first install on this host (no prior bundle to back up)
- post-install diagnose schema match: yes (installed copy produces the same 13-key JSON shape as the in-tree binary; install did not corrupt the bundle)

**Findings** (free-form; what regressed, what surprised, what to investigate):
- Nothing regressed. This is the **first /build-macos-tray invocation**; baseline-establishment run.
- `scripts/run-litmus-test.sh` fails on macOS with `declare: -A: invalid option` (bash 3 limitation — macOS ships /bin/bash 3.2). Litmus suite still runs cleanly on Linux integration loop; this is a known limitation the skill's §4 already calls out. **Recommendation**: at some point either (a) make the runner bash-3 compatible by replacing assoc-array with parallel arrays, or (b) require `brew install bash` and shebang to `/opt/homebrew/bin/bash`. Not a regression; tracking for sibling-host visibility.
- The tarball size 1.52 MiB is consistent with prior macOS builds (the work-queue iter 21 reported 0.27 MiB, but that pre-dated several waves of m4 sub-task B + m5 + UX-correction code; current 1.52 MiB is plausible).
- Auto-boot worker engages on launch and immediately enters the recipe-artifact-fetch path — this is m5 working as designed. For a truly headless smoke that won't sit and try to download a 100+ MiB artifact each run, future skill iterations might add a `--no-auto-boot` flag to the tray binary (currently no such flag exists; we just SIGTERM before the fetch completes, which the 3s timeout handles).

**Cross-host visibility note**: N/A (SECTION_KIND=ok).

**Next iteration ask**: N/A (SECTION_KIND=ok). Next run is tomorrow 12:07 PM local per CronCreate `2358d904`.
