# /probe-macos-tray-on-windows findings — 2026-09-13

Generated by `/probe-macos-tray-on-windows` (the windows host's daily
cross-platform health probe of the macos-tray crate). One section per
build run. A build SUCCESS = healthy cross-platform discipline; a
FAILURE means something broke in the shared modules
(`menu_disabled_v2`, `terminal_attach`) or shared crates
(`tillandsias-host-shell`, `tillandsias-control-wire`).

trace: skills/probe-macos-tray-on-windows/SKILL.md
       skills/build-macos-tray/SKILL.md (the macos host's authoritative
       full-build skill; do NOT invoke from windows)

---

### 20260913T174354Z — ok

- agent_id: windows-esmeraldinha-claude (esmeraldinha, Windows 11, N100 — floor tier)
- head_sha: ddd02e6b2
- cargo exit: 0
- first error line: none
- classification: expected (cfg-gated stub)
- shared-crate impact: none

**Findings**: green. The stub built cleanly on a head carrying a `--no-ff`
merge of `origin/linux-next`, so the shared portion of
`tillandsias-macos-tray` survives today's trunk content —
`menu_disabled_v2` and `terminal_attach` (two references each from the
crate's `main`), plus `tillandsias-host-shell` and
`tillandsias-control-wire`, all compile from a non-macOS host at this head.

Regime for the wall-clock number: Windows-native NTFS checkout (not drvfs,
not inside WSL), cargo invoked from Git Bash on the Windows side so no
`with-wsl2-builder` re-exec occurred, `--release`, **warm** target directory,
`CARGO_BUILD_JOBS=1`, no competing build — 27s.

That number is NOT comparable to this file's 2026-09-12 entry (49.50s at
default jobs). Two variables moved, not one: the job count changed and the
target directory was warm from unrelated builds earlier in the session. It
is recorded as a data point for a warm tree at jobs=1 and should not be read
as a jobs=1-versus-jobs=2 measurement, which this run cannot support.

**Cross-host visibility note**: N/A — nothing for the macos host to act on.

**Next iteration ask**: the skill's Step 1 still reads
`git merge --ff-only origin/linux-next`, which aborts whenever
`windows-next` carries any commit of its own, and the methodology's pre-push
gate wants trunk MERGED into the platform branch rather than fast-forwarded
onto it. A second, sharper reason to change it has since been measured: when
the platform branch is strictly BEHIND trunk, a plain `git merge` FAST-FORWARDS
and the branch becomes trunk plus your commits, putting trunk's own commits on
the first-parent line — which the pre-push plan-only lane then refuses, because
`_lane_scoped_diff` walks `--first-parent --no-merges`. The working form is
`git merge --no-ff --no-edit origin/linux-next`, and the one command that
predicts the verdict before pushing is
`git log --first-parent --no-merges --oneline origin/windows-next..HEAD` —
it must list only your own commits. This run used the `--no-ff` form.

The operational item from 2026-09-12 stands unfixed and is repeated here:
this probe and `scripts/cycle-preflight.sh` contend for the build-directory
lock on this host, so the two must not run concurrently if the wall-clock
number is to stay comparable across days. Added to it this cycle: the probe
must also not run concurrently with any other cargo build on this host. Two
builds were reaped for low memory during this session, and `CARGO_BUILD_JOBS=1`
is the setting that survives the reaper where `jobs=2` did not.
