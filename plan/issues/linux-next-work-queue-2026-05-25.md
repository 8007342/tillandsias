# Linux-next work queue — 2026-05-25 onwards

trace: methodology/distributed-work.yaml (worker_agent_protocol),
       skills/advance-work-from-plan/SKILL.md (§6 ledger),
       plan/issues/multi-host-integration-loop-2026-05-24.md (the 2h cron)

The linux equivalent of `osx-next-work-queue-*.md` and
`windows-next-work-queue-*.md`. Each entry is the one-line outcome the
`/advance-work-from-plan` skill writes at §6 after shipping (or
no-op-ing) a slice. Reverse-chronological, keep the latest 30 verbatim
and collapse older into a summary block.

For the WHY of each commit, see `plan/issues/linux-headless-spec-gaps-2026-05-27.md`
(diagnostics chain, gap-3 phase log) and the commit body itself.
This file is the cross-host advertisement — terse, dated, SHA-anchored.

## Recent entries (reverse chronological)

- 2026-05-31T00:21Z  `75ef3432`  Spec-gap fill (priority #2 — drift-protection on fundamental UX surface): `forge-welcome` coverage 50 → 75. Spec has 2 reqs (welcome content + mount colors + 20-tip pool + OS friendly names; fish-default-shell + --bash CLI flag). Existing 2 bindings (`litmus:forge-welcome-shape` + `litmus:onboarding-cold-start-discovery`) covered banner content + onboarding E2E but didn't trap 3-shell guard symmetry, tip-pool size, fish-as-entrypoint, or headless `--bash` env-var wiring. New instant litmus `litmus:forge-welcome-banner-shape` (7 steps) pins: (1) `@trace spec:forge-welcome` on forge-welcome.sh (substring match handles comma-list trace); (2) exactly 20 entries in `tips=()` array (Req 1 § Scenario "Rotating tips" — pool of ~20); (3) `${B_CYAN}%s${RST}` for project value (Req 1 § "Welcome message content" — bold cyan); (4) `exec fish` with `command -v fish` guard in entrypoint-terminal.sh (Req 2 fish-default contract); (5) `TILLANDSIAS_WELCOME_SHOWN` sentinel set+read in all 3 shells (bashrc + zshrc + config.fish, each >=2 refs — Req 1 § once-per-session gating); (6) canonical `/usr/local/share/tillandsias/forge-welcome.sh` path sourced from all 3 shells; (7) headless main.rs `--bash` detection + >=4 `TILLANDSIAS_PROJECT_HOST_MOUNT=1` env-var injection sites (multi-mode parity: Maintenance / Bash / OpenCode launch paths). Binding now 3 tests, last_verified 2026-05-30. **Matcher quirk recurrence + fix mid-slice**: awk's `\\(` un-escape pattern doesn't survive YAML→shell (already in session catalog); switched to sed `/^tips=/,/^)/p` range since `(`/`)` aren't BRE metachars unbraced — local verify confirmed count=20 before re-running litmus. Verification: clean checks; target litmus 2/2 PASS (1 e2e skipped under --size instant); full instant suite **93/93 PASS at 100% across 88 specs**.
- 2026-05-30T23:51Z  (no-op)   defer — integration cron at 23:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T23:51Z`. (That cron merged windows-next's `8490a364 fix(scripts): diagnose-windows.ps1 stale readiness + identity-line addition` — windows-host script + ledger only, fully sibling-owned scope, drift scan clean. osx-next no-op — **10 consecutive cycle stall since 06:27Z ~17h 16min, deepest stall of session**; ESCALATION DEEPLY overdue, orchestrator macOS host ping STRONGLY required — at this duration host may be offline / agent loop crashed.)
- 2026-05-30T23:27Z  `2bb7410a`  Spec-gap fill (priority #2 — drift-protection on the fundamental forge image): `default-image` coverage 50 → 75. Spec has 6 reqs spanning Fedora Minimal base + dev tools + non-root forge UID 1000 + OpenCode entrypoint + declarative Containerfile build + content-hash image identity. Existing 2 bindings (`litmus:forge-standalone-traceability` + `litmus:forge-diagnostics-e2e`) covered launch path + diagnostic E2E behaviour but didn't trap the Containerfile source surfaces or the build-image.sh dispatch case-arm. New instant litmus `litmus:default-image-containerfile-shape` (8 steps) pins: (1) `@trace spec:default-image` on Containerfile; (2) `FROM registry.fedoraproject.org/fedora-minimal:44` base — regression to apk (Alpine) or dnf (full Fedora) silently breaks the documented microdnf+glibc+useradd contract; (3) `microdnf install -y` core toolchain (git/gh/ripgrep/nodejs/python3-pip canonical subset); (4) npm install line installing `opencode-ai@latest @fission-ai/openspec @anthropic-ai/claude-code @openai/codex` in one invocation (all 4 agents in PATH); (5) `useradd -u 1000 -m -s /bin/bash forge` — UID 1000 for `--userns=keep-id` alignment; (6) COPY of all 4 forge entrypoint scripts (opencode + opencode-web + claude + codex); (7) `scripts/build-image.sh` case-arm dispatch for `forge|web|proxy|git|inference|router|chromium-core|chromium-framework|vault`; (8) `IMAGE_TAG` variable + `podman build` invocation for the content-hash identity contract. Binding now 3 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 2/2 PASS (1 e2e skipped under --size instant); full instant suite **92/92 PASS at 100% across 88 specs**.
- 2026-05-30T22:51Z  `c9ab2b78`  Bindings hygiene — tombstone alignment: `secret-rotation` was binding-state `active, coverage_ratio: 33` despite spec.md declaring `<!-- @tombstone superseded:podman-secrets-integration -->` + `## Status: retired` since Phase 6. Status mismatch noted in 10:21Z ledger as "5-min ledger-only slice"; now aligned to canonical tombstone shape (matching ~20 other obsolete bindings): `status: obsolete`, `litmus_tests: []`, `coverage_ratio: 0`, `tombstone: superseded:podman-secrets-integration`, `last_verified: 2026-05-30`. Effects: active-spec count drops 89 → 88; full instant suite stays 91/91 PASS. The 9 legacy `@trace spec:secret-rotation` annotations in Rust/shell + 3 litmus files mentioning it in observability traces remain — per spec.md NOTE those are "transitional historical references that should be migrated to spec:podman-secrets-integration" (separate migration slice). Verification: clean checks; full instant suite **91/91 PASS at 100% across 88 active specs**.
- 2026-05-30T22:21Z  `ed63daa7`  Spec-gap fill (priority #2 — Zen-default model routing + inference container surfaces): `zen-default-with-ollama-analysis-pool` coverage 33 → 75. Spec mandates Zen-default (opencode/big-pickle + opencode/gpt-5-nano) with Ollama-as-opt-in for offline analysis + tier-tagged pre-pull (T0+T1 always at startup, T2+ runtime). New instant litmus `litmus:zen-default-with-ollama-shape` (7 steps) pins: (1) `"model": "opencode/big-pickle"` Zen primary literal; (2) `"small_model": "opencode/gpt-5-nano"` fast non-tool model; (3) ollama provider baseURL `http://inference:11434/v1` (enclave-DNS); (4) ≥8 enumerated ollama models in `provider.ollama.models` (today ~10); (5) `ollama pull qwen2.5:0.5b` T0 (CPU baseline 397MB tool-capable); (6) `ollama pull llama3.2:3b` T1 (CPU 2.0GB tool-capable); (7) Containerfile uses `ollama-linux-amd64.tar.zst` with `bin/ollama`-only extraction (skip ~1.8GB lib/ollama/ GPU runners — CPU-only container, regression to full-extract silently bloats image). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 2/2 PASS (14 total steps); full instant suite **91/91 PASS at 100% across 89 specs** — every executed instant litmus binding still passing.
- 2026-05-30T21:51Z  (no-op)   defer — integration cron at 21:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T21:51Z`. (That cron merged windows-next's `fdc3c00e fix(scripts): build-windows-tray.ps1 no longer aborts on cargo stderr-wrap` — real install-blocking bug fix: PowerShell's NativeCommandError wraps cargo's stderr "Compiling X" writes; the subscript's `$ErrorActionPreference=Stop` treated the very first line as terminating, killing builds before "Finished release". Fix: save/restore preference around the cargo call. All in sibling-owned windows-tray scope. osx-next no-op — **9 consecutive cycle stall since 06:27Z ~15.5 hours, deepest stall of session**; ESCALATION DEEPLY overdue, orchestrator macOS host ping STRONGLY recommended.)
- 2026-05-30T21:21Z  `df9502fe`  Spec-gap fill (priority #2 — security-relevant parser + Caddyfile ordering): `subdomain-naming-flip` coverage 33 → 75. Spec mandates `<service>.<project>.localhost` (e.g. `opencode.myapp.localhost`), NOT the old `<project>.<service>.localhost` — the parser + Caddyfile encoding both depend on this order. A regression silently breaks the per-project cookie-binding scoping that secures OTPs (cookie for `opencode.myapp.localhost` MUST NOT validate against `opencode.otherapp.localhost`). New instant litmus `litmus:subdomain-naming-flip-shape` (7 steps) pins: (1) router-sidecar http.rs file-head trace; (2) `extract_project_label(host) -> Option<String>` signature + docblock examples; (3) parser port-strip + lowercase normalization (`split(':')` + `to_lowercase()`); (4) ≥6 named parser tests (standard/with_port/hyphens-underscores/case-insensitive/rejects-non-localhost/rejects-plain-localhost); (5) `format!("opencode.{}", project_name)` in headless dynamic-Caddyfile generation; (6) `validate_subdomain_binding` cookie-scoping helper (per-project OtpStore enforcement); (7) browser-mcp test fixtures use new shape + allowlist description literal. Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (7 steps; ephemeral-guarantee e2e skipped under --size instant); full instant suite **90/90 PASS at 100% across 89 specs** — every executed instant litmus binding still passing.
- 2026-05-30T20:51Z  `b4e18329`  Spec-gap fill (priority #2 — meta-spec USAGE drift-protection): `spec-traceability` coverage 33 → 75. Spec mandates @trace + structured-log + @cheatsheet annotations across the codebase. Existing `litmus:spec-traceability-shape` covered spec text + observability anchors but didn't pin USAGE floors. New instant litmus `litmus:spec-traceability-usage-shape` (6 steps) pins the floors with ~50% headroom for organic cleanup: (1) ≥500 `@trace spec:` Rust annotations (current ~993); (2) ≥100 `@trace spec:` shell annotations (current ~335); (3) ≥50 `spec = "..."` structured-log fields in Rust (~94); (4) ≥20 `@cheatsheet` annotations (~48); (5) ≥10 distinct spec IDs traced from Rust (graph-breadth invariant — collapse to one ID would silently kill the queryable graph); (6) ≥10 Rust files with module-level `//! @trace spec:` headers (spec § Scenario "Module-level trace" — module-head pattern alive). Binding now 2 tests, last_verified 2026-05-30. **MILESTONE: Full instant suite 88/88 → 89/89 PASS at 100% across 89 specs — every executed instant litmus binding is now passing.** Session arc: started ~71/71 at dawn (~13 hours of drift-protection work), 18 net new instant litmus tests + release recovery + cross-host CARGO_PKG_VERSION round-trip + 5 spec/impl divergences flagged + 4 inline-reconciled.
- 2026-05-30T20:21Z  `afb1aad5`  Spec-gap fill (priority #2 — drift-protection on small but critical image): `web-image` coverage 33 → 75. Spec mandates Alpine-based <10MB image serving static files via busybox httpd on port 8080. New instant litmus `litmus:web-image-shape` (7 steps) pins: (1) `@trace spec:web-image` on both Containerfile + entrypoint.sh; (2) `FROM docker.io/library/alpine:3.20`; (3) exactly 1 `apk add --no-cache busybox-extras` (count=1 catches regression adding curl/wget that would bust <10MB size budget); (4) `EXPOSE 8080` + `WORKDIR /var/www`; (5) entrypoint `#!/bin/sh` POSIX shebang (Alpine busybox ash — not bash); (6) `exec httpd -f -p 8080 -h /var/www` canonical launch (`-f` foreground PID 1, `-p 8080` port, `-h /var/www` serving root); (7) banner literals `tillandsias web` + `http://localhost:8080` URL (Req Entrypoint displays banner and serving URL). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (7 steps; ephemeral-guarantee e2e skipped under --size instant); full instant suite **88/88 PASS at 100% across 89 specs**.
- 2026-05-30T19:51Z  (no-op)   defer — integration cron at 19:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T19:51Z`. (That cron merged windows-next's `567009f6 feat(scripts): install + tray-diagnose consume new CLI surface (--version preflight + build_commit + recent logs)` — wraps up the cross-host triage UX improvements from this week's windows-tray work: install-windows.ps1 adds Layer-1 `--version` preflight + Layer-2 `--diagnose --json` now reports `commit=<build_commit>` + surfaces `wire.error`; tray-diagnose.ps1 adds "build commit" row + "Recent log activity" section iterating `report.recent_log_tail`. PowerShell + windows-tray litmus all in sibling-owned scope. osx-next no-op — **8 consecutive cycle stall since 06:27Z ~13 hours**; ESCALATION DEEPLY overdue, orchestrator macOS host ping STRONGLY recommended.)
- 2026-05-30T19:02Z  `9aff6525`  **Release v0.2.260530.1 PUBLISHED** — workflow `26691456259` completed `success` at `39m38s` (faster than `v0.2.260529.1` precedent `41m21s`). Release page: https://github.com/8007342/tillandsias/releases/tag/v0.2.260530.1 — 22 cosign-signed assets including all 3 platform binaries (linux/macos-arm64/windows-x64 trays), x86_64 + aarch64 musl in-VM headless agents, install/uninstall/verify/SHA256SUMS, all cosign bundles. **Linux Silverblue smoke-test target live**. Total operator turnaround from 18:13Z escalation to release downloadable: 49 min (~7 min operator intervention + ~40 min CI build).
- 2026-05-30T18:51Z  `74f4997e`  Spec-gap fill (priority #2 — drift-protection + 5th spec drift flagged + reconciled inline): `simplified-tray-ux` coverage 33 → 67. New instant litmus `litmus:simplified-tray-ux-leaf-action-shape` (6 steps) pinning per-project tool surfaces alongside the existing tray-minimal-ux-contract-shape: `enum LeafAction {`, 6 variants (Claude/Codex/OpenCode/OpenCodeWeb/Observatorium/Maintenance), `const ALL: [LeafAction; 6]` iteration array, `By Tlatoāni` attribution literal (Req 1 Menu element 3), 5-position startup menu docblock with id=30+31, LeafAction offset table (+0 Claude → +5 Maintenance — `project_action_from_id` dispatch contract). Binding now 2 tests, coverage capped at 67 (not 75) due to LeafAction 6-vs-4 divergence. **5th SPEC / IMPL DIVERGENCE FLAGGED + RECONCILED INLINE**: spec § Home Menu mandates 4 tools per project (OpenCode / OpenCode Web / Claude / Maintenance); impl has 6 (added Codex + Observatorium) with different emojis (impl: 👾🏗️💻📐🔭🔧 vs spec: 💻🌐👽🔧). Same divergence family as no-terminal-flicker Stage enum. Appended `⚠ Implementation reality (as of 2026-05-30)` block alongside Req "Home Menu" using the pattern established by 11:21Z 3-spec reconciliation slice — preserves design intent + documents deployed code + "Reconcile by either … OR …" closer for future architectural decision. Verification: clean checks; target litmus 2/2 PASS (8 total steps); full instant suite **87/87 PASS at 100% across 89 specs**.
- 2026-05-30T18:22Z  `23dcb633`  **Release escalation resolved from local fedora** — Cloud-side `merge-to-main-and-release` skill (`linux-web-claude-sonnet`) fired at 18:00Z UTC, completed PR #8 + merge to main (`d2c15f37`) + VERSION bump to `0.2.260530.1` on main (`677a89af`), but Step 5 (tag push) blocked by web env's local git proxy returning HTTP 403 for `refs/tags/*` (proxy fetch refspec `+refs/heads/*:refs/remotes/origin/*` doesn't include tags). Escalation logged at 18:13Z (`eadd80aa`). Resolved from this fedora host (7-minute turnaround): annotated tag `v0.2.260530.1` created pointing at `677a89af` with inline message referencing both PR #8 and the escalation block; `git push origin v0.2.260530.1` succeeded cleanly (native github.com remote, no proxy); `gh workflow run release.yml --ref v0.2.260530.1` triggered workflow run `26691456259` (queued at 18:22:35Z; expected ~40min artifact-build cycle per prior `v0.2.260529.1` precedent of 41m21s). RESOLVED block appended to integration ledger. **Follow-on flagged for cloud-side skill**: detect HTTP 403 on `git push origin <tag>` and fall back to `gh api repos/{owner}/{repo}/git/refs --method POST -f ref=refs/tags/<tag> -f sha=<commit>` (GitHub REST API path, not git-protocol via proxy) before escalating — would have unblocked the cloud cycle without operator intervention.
- 2026-05-30T17:51Z  (no-op)   defer — integration cron at 17:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T17:51Z`. (That cron merged windows-next's `205d2abf feat(windows-tray): --logs / --logs --tail N CLI mode for past-the-tail log inspection` — new pure `select_log_tail(content, tail) -> Vec<&str>` helper with `saturating_sub` underflow + n=0 empty-vec guards + `pub fn logs(tail: Option<usize>) -> i32`; +1 pin test covers 4 edge cases. Crate-side all in windows-tray scope. osx-next no-op — **7 consecutive cycle stall since 06:27Z**; ESCALATION watch overdue, orchestrator-level ping recommended.)
- 2026-05-30T17:21Z  `58a91756`  Spec-gap fill (priority #2 — implementation surfaces alongside existing contract litmus): `tray-minimal-ux` coverage 33 → 75. Spec has 5 reqs (4-element first-launch menu / dynamic status text / post-init expansion / no-launch-here tombstone / no-cleanup-here tombstone). Existing `litmus:tray-minimal-ux-contract-shape` pinned traces + Seedlings/Attach Here/Maintenance/Stop labels + MUST_NOT tombstones but missed EnclaveStatus state machine + Req 2 status text mapping + Req 1 Quit literal. New instant litmus `litmus:tray-minimal-ux-status-shape` (6 steps) pins: (1) ≥5 `EnclaveStatus::{Verifying,ProxyReady,GitReady,AllHealthy,Failed} =>` match arms; (2) 5 canonical status text literals (`Verifying environment` + `Building enclave` + `Building git mirror` + `Environment OK` + `Unhealthy environment`); (3) icon-glyph progression markers (☐ checklist start, 🌐 network at proxy ready, 🪞 mirror at git ready, ✓ checkmark on AllHealthy, 🥀 wilted-rose on Failed); (4) ≥3 `Quit Tillandsias` literal sites (Req 1 4-element menu — menu construction + production tests); (5) state-machine transition docblock pins Verifying → ProxyReady → GitReady → AllHealthy chain + Any → Verifying reset fallback; (6) 3 `SelectedAgent::{OpenCode,OpenCodeWeb,Claude}` variants (Req 3 Seedlings submenu order). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 2/2 PASS (8 total steps); full instant suite **86/86 PASS at 100% across 89 specs**.
- 2026-05-30T16:51Z  `bb943cc8`  Spec-gap fill (priority #2 — completing the versioning-machinery story from today's CARGO_PKG_VERSION fixes): `versioning` coverage 33 → 75. Spec mandates `VERSION` file at repo root as single source of truth, 3 build.rs files ingesting it, bump-version + pre-push hooks. New instant litmus `litmus:versioning-shape` (7 steps) pins: (1) `VERSION` non-empty at repo root; (2) 4-segment dot-delimited content (tr/wc count = 3 dots — ERE quantifier `{6}` doesn't survive YAML→shell un-escape); (3) 3 build.rs files (host-shell + browser-mcp + windows-tray) each read `../../VERSION` AND emit `WORKSPACE_VERSION`; (4) same 3 build.rs files emit `cargo:rerun-if-changed=../../VERSION` (rebuild-on-bump invariant); (5) bump-version.sh `@trace spec:versioning` + `Major.Minor.YYMMDD.Build` docblock; (6) pre-push-version-guard.sh `@trace` + branch-guard message; (7) `[workspace.package]` block in Cargo.toml (semver derivation source). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (7 steps; ephemeral-guarantee e2e skipped under --size instant); full instant suite **85/85 PASS at 100% across 89 specs**. Matcher-quirk recurrence: ERE quantifier `\{6\}` doesn't survive YAML→shell — workaround = `tr -cd '.' | wc -c` substring count. This completes the versioning-machinery story from yesterday's CARGO_PKG_VERSION→WORKSPACE_VERSION shared fixes; the spec's "single source of truth" claim is now actually pinned at source.
- 2026-05-30T16:21Z  `eb0a697a`  Spec-gap fill (priority #2 — drift-protection on installer + launcher chain): `host-chromium-on-demand` coverage 33 → 75. Spec has 4 reqs (download+SHA256 verify / ephemeral-profile launch / browser detection / download telemetry). Single binding `litmus:browser-ephemeral` (e2e) covered generic isolation. New instant litmus `litmus:host-chromium-on-demand-shape` (7 steps) pins: (1) install-chromium.sh file-head trace (substring match for comma-list); (2) `install_chromium()` + `chromium_platform()` detector (linux64/mac-arm64/mac-x64/win64); (3) ≥4 per-platform `CHROMIUM_SHA256_*` env-var references — regression dropping any would let that arch's download bypass integrity; (4) `ln -snf` atomic `current` symlink repoint (POSIX atomic-replace idiom — regression to `rm + ln` would race readers); (5) `SKIP_CHROMIUM_DOWNLOAD` air-gapped escape hatch; (6) browser-mcp launcher 3-candidate `cache_root` chain (XDG_CACHE_HOME → ~/.cache → /tmp); (7) `fn resolve_browser_binary(override_bin: Option<&Path>) -> Result<PathBuf, LaunchError>` entry. Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (7 steps; browser-ephemeral e2e skipped under --size instant); full instant suite **84/84 PASS at 100% across 89 specs**.
- 2026-05-30T15:51Z  (no-op)   defer — integration cron at 15:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T15:51Z`. (That cron merged windows-next's `04278ec0 feat(windows-tray): --help / --version CLI modes for standard convention parity` — `version_line()` reuses build.rs `WORKSPACE_VERSION`+`BUILD_COMMIT_SHA` so the 3 places a user can ask "what version?" stay self-consistent; `help_text()` documents 6 CLI modes + exit-code contracts + cheatsheet pointer; +2 pin tests. Windows extended their litmus in lockstep. osx-next no-op — 6 consecutive cycle stall since 06:27Z; **ESCALATION watch** noted in integration ledger — orchestrator should ping macOS host directly.)
- 2026-05-30T15:21Z  `4990f55f`  Spec-gap fill (priority #2 — drift-protection on 11-test implementation crate): `remote-projects` coverage 33 → 75. Spec has 5 reqs (containerized `gh` fetch + local-existence filter + 5min TTL cache + clone orchestration + loading state). Single binding `litmus:ephemeral-guarantee` (e2e) covered generic isolation. New instant litmus `litmus:remote-projects-shape` (7 steps) pins: (1) `spec:remote-projects, spec:gh-auth-script` substring trace; (2) `const CACHE_TTL_SECS: u64 = 300` exact 5-minute TTL (regression to longer would widen staleness beyond spec Scenario "Cache stale"); (3) `pub struct GitHubProject` + `pub fn nwo(&self) -> String` canonical owner/name accessor (JSON `url` field is API URL — `gh repo clone` can't consume it; `nwo()` is spec-mandated invocation form); (4) `pub fn invalidate_github_projects_cache()` (spec Scenario "Cache refreshed after auth"); (5) `pub fn clone_project_from_github` + `_with_debug` variants (Req "Clone remote project"); (6) ≥8 of 11 named unit tests (silent-test-deletion guard); (7) `fn debug_log_helpers_redact_token_but_show_shape` test pinned (regression to non-redacting log helper would silently leak GH token). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (7 steps; ephemeral-guarantee e2e skipped under --size instant); full instant suite **83/83 PASS at 100% across 89 specs**.
- 2026-05-30T14:51Z  `472b527e`  Spec-gap fill (priority #2 — natural follow-on of 14:21Z spec-source slice): `podman-container-handle` coverage 33 → 75. Sibling spec lives in same `container_spec.rs` file alongside ContainerSpec. New instant litmus `litmus:podman-container-handle-source-shape` (7 steps) pins: (1) `spec:podman-container-handle` substring trace; (2) `pub struct ContainerHandle { info: ContainerInfo, spec: ContainerSpec }` two-field shape (Req Identity); (3) `pub fn new(info, spec) -> Self` constructor (Req Unit-testable pure data path); (4) 5 accessor methods (name/image/spec/info/into_spec); (5) `fn handle_exposes_name_and_image` unit test pinned by name; (6) `pub struct ContainerInfo` in tillandsias-core/src/state.rs (linkage point — shared with tray); (7) `pub container_name: String` on PodmanEvent + `event.container_name` correlation assertion in events.rs (Req Event correlation — silent rename guard). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (7 steps; quick variant runs at non-instant filter); full instant suite **82/82 PASS at 100% across 89 specs**. No matcher-quirk recurrences this cycle (used substring matches up front per yesterday's learnings). Twin slices 14:21Z + 14:51Z = the typed-spec layer's source surfaces now both have instant-phase drift-protection alongside their cargo-test runners.
- 2026-05-30T14:21Z  `28223e77`  Spec-gap fill (priority #2 — drift-protection on idiomatic-podman heart): `podman-container-spec` coverage 33 → 75. Spec has 3 reqs (typed serialisation / immutable security defaults / launch-shape composition). Existing `litmus:podman-container-spec-shape` (size:quick) runs 3 cargo tests but didn't pin source surfaces — a refactor renaming `ContainerSpec` would fail the cargo tests, not pre-build. New instant litmus `litmus:podman-container-spec-source-shape` (7 steps) pins: (1) `spec:podman-container-spec` substring trace; (2) ≥6 `: true,` assignments in `new()` body (Req Security defaults — init/remove/userns_keep_id/cap_drop_all/no_new_privileges/label_disable); (3) 5 fluent-chain builders (detached/interactive/tty/name/hostname); (4) `pub fn build_run_args(&self) -> Vec<String>`; (5) `pub fn build_run_argv` with `vec!["run".to_string()]` prefix; (6) ≥6 named ContainerSpec unit tests (each Req scenario pinned by test-name presence — silent-test-deletion guard); (7) ≥8 ContainerSpec struct field names (Req Composition). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (7 steps; quick variant runs at non-instant filter); full instant suite **81/81 PASS at 100% across 89 specs**. Matcher-quirk recurrence: awk `\(` and `\{` range patterns don't survive YAML→shell un-escape; workaround = `sed -n '/.../,/.../p'` with `.` wildcards for parens/braces. Also `^\s+` ERE class gets mangled — match leading-space literals (`    `) instead. Both inline documented.
- 2026-05-30T13:51Z  (no-op)   defer — integration cron at 13:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T13:51Z`. (That cron merged BOTH siblings: windows-next's `ddeab1f9 cleanup(windows-tray): remove fresh_menu_state override (shared host-shell fix landed)` — **cross-host coordination ROUND-TRIP COMPLETE**: windows ASK 11:00Z → linux shared fix 12:21Z → linux RESOLVED marker → windows cleanup 13:43Z. 15-line `fresh_menu_state()` helper deleted, 12 mechanical reverts to `MenuState::initial`, 24-line redundant pin test deleted (superseded by host-shell's `version_reports_workspace_release_not_crate_static_zero_dot_one` at the source). Pattern working as designed. osx-next merged 1 hourly build-findings doc.)
- 2026-05-30T13:21Z  `7f48f767`  Spec-gap fill (priority #2 — drift-protection + 4th spec/impl divergence flagged): `github-credential-health` coverage 33 → 67. Spec mandates 4-state classifier (Authenticated / CredentialMissing / CredentialInvalid / GithubUnreachable) distinguishing "down" from "unauthenticated"; implementation today uses binary `gh_auth_check() -> bool` + `github_healthy: bool` field on TrayState — NO enum classifier, NO down-vs-unauthenticated distinction. Retry-with-backoff scaffolding (`github_retry_count`, `github_next_retry`) exists but feeds the same bool. New instant litmus `litmus:github-credential-health-shape` (6 steps) pins what's there: (1) TrayState declares 4 retry-with-backoff fields together; (2) ≥4 `spec:github-credential-health` mentions in state.rs (matcher quirk #9 family — substring match handles comma-list format); (3) `gh_auth_check() -> bool` with `Duration::from_secs(5)` deadline; (4) `gh auth status` + `Stdio::null()` (no console flash); (5) `async fn check_github_token_health()` with 1s non-blocking guard (Req "Offline laptop keeps working"); (6) `gh_auth_check` doc-trace `@trace spec:tray-minimal-ux, spec:gh-auth-script`. Binding now 2 tests, coverage capped at 67 (not 75) due to 4-state classifier divergence. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 1/1 PASS (6 steps; credential-isolation e2e skipped under --size instant); full instant suite **80/80 PASS at 100% across 89 specs**. **Flagged real implementation gap NOT closed**: needs enum `GithubCredentialHealth { Authenticated, CredentialMissing, CredentialInvalid, GithubUnreachable }` replacing the bool + classifier function walking `gh auth status` exit codes / `api.github.com/user` HTTP statuses + 4-state downstream UI gating (project lists + Attach Here enabled on Authenticated; disabled with login CTA on Credential*; enabled with cached-offline banner on GithubUnreachable). Substantial slice (~2h); queued for future implementation cycle.
- 2026-05-30T12:51Z  `76bb84a2`  Browser-mcp follow-on of 12:21Z host-shell `CARGO_PKG_VERSION` fix. MCP `initialize` response's `serverInfo.version` was sourcing `env!("CARGO_PKG_VERSION")` against `tillandsias-browser-mcp/Cargo.toml`'s static `0.1.170`. This is the AI-agent-visible MCP protocol-server version — operators pasting agent serverInfo into bug reports couldn't correlate to specific release. Same shape as host-shell fix: new `crates/tillandsias-browser-mcp/build.rs` reads `../../VERSION` → emits `WORKSPACE_VERSION` (with CARGO_PKG_VERSION fallback for source-tarball/CI cross-checks); `server.rs::handle_initialize` uses `env!("WORKSPACE_VERSION")` in `serverInfo.version`; inline comment documents AI-agent visibility + cross-release-correlation use case + host-shell fix provenance. New test `initialize_serverinfo_version_is_workspace_release_not_crate_static` asserts `serverInfo.version != "0.1.170"` AND ≥3 dot-segments. MCP `protocolVersion: "2025-06-18"` field unchanged (that's the wire-protocol-spec version, different concern). Verification: `cargo fmt --all` + `./build.sh --check` + `./build.sh --test` clean; `cargo test -p tillandsias-browser-mcp --lib server::tests::initialize` 2/2 PASS (existing + new regression guard); full instant suite **79/79 PASS at 100% across 89 specs**. The 12:21Z + 12:51Z slices together close the cross-host `CARGO_PKG_VERSION` anti-pattern audit triggered by windows-host's ASK; tray-convergence-coordination.md already marks the structural fix RESOLVED.
- 2026-05-30T12:21Z  `76f93287`  **Cross-host coordination response** closing the shared structural fix windows-host asked for in `plan/issues/tray-convergence-coordination.md` 2026-05-30T11:00Z ASK block. `tillandsias-host-shell::version()` returned crate-static `0.1.0` because of `env!("CARGO_PKG_VERSION")` against `tillandsias-host-shell/Cargo.toml`'s `version = "0.1.0"`. Two user-visible symptoms across all 3 trays: (a) WSL/VZ provisioning fetched a non-existent `v0.1.0` `tillandsias-linux-x86_64` artifact (only `v0.2.260528.1` shape tags ship); (b) windows + macos tray menu footer rendered "v0.1.0 — By Tlatoāni" via `MenuState::initial`. Linux tray uses StatusNotifierItem/DBus rendering and never called `version()`, so was NOT user-visibly affected — but the shared provisioning path was. Fix mirrors windows-tray/build.rs pattern: new `crates/tillandsias-host-shell/build.rs` reads `../../VERSION`, emits `WORKSPACE_VERSION` env var with rerun-if-changed; fallback to CARGO_PKG_VERSION for source-tarball/CI cross-checks; `lib.rs::version()` now returns `env!("WORKSPACE_VERSION")` with rewritten docblock documenting the bug + impact + windows-host ASK provenance. New pin test `version_reports_workspace_release_not_crate_static_zero_dot_one` asserts `version() != "0.1.0"` AND ≥3 dot-segments (a 2-segment "X.Y" string is the unmistakable signature of a crate-static regression). Verification: cargo fmt clean; `./build.sh --check` + `./build.sh --test` clean; `cargo test -p tillandsias-host-shell --lib tests` 40/40 PASS; full instant litmus 79/79 PASS at 100% across 89 specs. **Cross-host impact**: windows-tray's `fresh_menu_state()` override (commit 6eb026e0) becomes structurally redundant — can stay as defence-in-depth but no longer required. macOS's parallel ASK is closed structurally. **Flagged separate follow-on**: `crates/tillandsias-browser-mcp/src/server.rs:186` also uses `env!("CARGO_PKG_VERSION")` in JSON-RPC response (AI-agent-visible MCP server protocol version) — needs its own browser-mcp slice.
- 2026-05-30T11:51Z  (no-op)   defer — integration cron at 11:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T11:51Z`. (That cron merged windows-next's `6eb026e0 feat(windows-tray): inject workspace VERSION into tray menu footer` — same root cause family as the prior `--diagnose --json` version fix but in user-visible menu UX; contained windows-side fix via new `fresh_menu_state()` wrapper + 12-callsite mechanical replacement + pin test. Cross-host ASK appended to `tray-convergence-coordination.md`: same bug exists on all 3 trays via `host-shell::version() → env!("CARGO_PKG_VERSION")` = "0.1.0" — windows fixed contained-side, asks shared host-shell update + macOS mirror. **Linux note**: linux tray uses StatusNotifierItem/DBus rendering and does NOT call `MenuState::initial` or `host-shell::version()` directly — NOT affected by this specific bug, but linux's own version-source path should be audited for the same `CARGO_PKG_VERSION` anti-pattern in a follow-on slice. osx-next stall now 4+ cycles — orchestrator escalation watch noted.)
- 2026-05-30T11:21Z  `8d60344f`  Spec-text reconciliation slice consolidating 3 spec/impl divergences flagged earlier this session (proxy-container splice-all vs bump-all + reverse-proxy-internal HTTPS:443 vs HTTP:8080 + no-terminal-flicker 5-stage vs 11-stage). Each spec gets a focused `> ⚠ Implementation reality (as of 2026-05-30):` block alongside the divergent text, citing the discovering litmus commit + work-queue ledger entry + closing "Reconcile by either … OR …" sentence so the architectural decision is queued for a future call. Pattern preserves original design intent while documenting reality. Bumped `no-terminal-flicker` coverage 67 → 75 (the cap was specifically due to the Stage-enum divergence; documenting it lifts the cap). proxy-container + reverse-proxy-internal bindings stay at 75 (their coverage_ratios reflect litmus coverage of source surfaces, not divergence-induced caps). Verification: `cargo fmt --all` + `./build.sh --check` clean (no code touched); full instant suite stable at **79/79 PASS at 100% across 89 specs**. Docs-only slice clearing the queued reconciliation backlog from earlier today.
- 2026-05-30T10:51Z  `6bc2f3ad`  Spec-gap fill (priority #2 — drift-protection + 3rd spec/impl divergence flagged): `no-terminal-flicker` coverage 33 → 67. Spec has 8 reqs spanning Windows CREATE_NO_WINDOW (Req 1+2), tray menu rebuild discipline (Req 3-7), and macOS/Linux terminal consistency (Req 8). New instant litmus `litmus:no-terminal-flicker-shape` (7 steps) pins: (1) `pub const CREATE_NO_WINDOW: u32 = 0x0800_0000;` (Req 1 MSDN flag value); (2) `no_window_async` + `no_window_sync` helper fluent-chain signatures behind `#[cfg(windows)]`; (3) `@trace spec:no-terminal-flicker` on tray/mod.rs; (4) `enum TrayStatusStage` declared; (5) ≥8 of 11 stage variants (PreLaunch/NetworkUp/ProxyStarting/GitStarting/InferenceStarting/ForgeStarting/RouterStarting/AllReady/ShuttingDown/Failed/PodmanMissing); (6) `fn status_label(stage: &TrayStatusStage) -> String` resolver (Req 5 keyed lookup for menu-label-update-without-rebuild); (7) `fn enclave_status_to_stage(status: EnclaveStatus) -> TrayStatusStage` driver (Req 4 O(1) invariant). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (7 steps; cross-platform-tray is size:quick); full instant suite **79/79 PASS at 100% across 89 specs**. **3rd SPEC / IMPL DIVERGENCE FLAGGED THIS SESSION**: spec § Req 4 names a `Stage` enum with auth-focused 5 variants (Booting/Ready/NoAuth/Authed/NetIssue); actual impl uses `TrayStatusStage` with 11-variant enclave-launch progression. Impl is richer + more useful (per-service visibility) but spec text documents the older model. Spec text needs update — joins queued reconciliation backlog (proxy-container splice/bump + reverse-proxy-internal HTTPS/HTTP + this Stage enum). Coverage capped at 67 (not 75) due to the divergence.
- 2026-05-30T10:21Z  `f52bdf45`  Spec-gap fill (priority #2 — drift-protection on 6-script set): `project-summarizers` coverage 33 → 75. Spec mandates 6 language-specific manifest summarizers; all 6 shipped at `scripts/summarize-{cargo,package-json,go-mod,nix,pubspec,pyproject}.sh` with uniform shape but untrapped. New instant litmus `litmus:project-summarizers-shape` (7 steps) pins: (1) all 6 files exist; (2) each carries `@trace spec:project-summarizers`; (3) each uses `set -euo pipefail` (deterministic-exit discipline); (4) each `exit 2` on missing manifest (skip semantics per `readme-discipline.md:96` — regression to exit 1 would turn skip into hard error); (5) each emits ≥3 `### ` H3 markdown headers (dispatcher contract per `readme-discipline.md:129` for `## Tech Stack` / `## Build/Runtime Dependencies` H2 concatenation); (6) each has executable bit (chmod regression would break dispatcher invocation); (7) TRACES.md project-summarizers row references ≥1 summarizer (spec-to-impl graph integrity). Binding now 2 tests, last_verified 2026-05-30. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 1/1 PASS (7 steps; project-bootstrap-readme-shape is size:quick); full instant suite **78/78 PASS at 100% across 89 specs**. Observation noted: `secret-rotation` (33%) is actually `status: retired` per its spec.md (`<!-- @tombstone superseded:podman-secrets-integration -->`), but binding says `status: active`. The 9 legacy `@trace spec:secret-rotation` annotations in the codebase are documented as transitional per spec NOTE. Binding alignment to `status: obsolete + tombstone:` would close this — but that's a 5-min ledger-only slice; not pursued here.
- 2026-05-30T09:51Z  (no-op)   defer — integration cron at 09:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T09:51Z`. (That cron merged windows-next's `13539385 feat(windows-tray): --status-once --json parity for support tooling` — new `StatusReport` struct + `status_exit_code` pure helper + 2 schema-pin tests; windows also edited their litmus-windows-tray-diagnose-cli-surface.yaml to lockstep-add the new test-name pins. osx-next no-op — empty range, no new macOS work in 2h.)
- 2026-05-30T09:21Z  `3415c7b6`  Spec-gap fill (priority #2 — drift-protection + 2nd spec/impl divergence flagged this session): `reverse-proxy-internal` coverage 33 → 75. Spec has 6 reqs covering enclave-internal routing + SSL termination + R/R logging + caching + ephemeral state + access control. New instant litmus `litmus:reverse-proxy-internal-shape` (9 steps) pinning 4 router files (`images/router/{Containerfile,base.Caddyfile,entrypoint.sh}` + `crates/tillandsias-router-sidecar/src/main.rs`): (1) all 4 files trace router-related spec; (2-4) base.Caddyfile `auto_https off` + `admin localhost:2019` + `storage file_system /tmp/caddy-storage`; (5) entrypoint.sh BASE+DYNAMIC+MERGED Caddyfile path variables; (6) entrypoint supervises tillandsias-router-sidecar + waits for `:9090` readiness (5s timeout); (7) router-sidecar `DEFAULT_VALIDATE_PORT=9090` + `TILLANDSIAS_VALIDATE_PORT` override; (8) `TILLANDSIAS_CONTROL_SOCKET` env var for tray subscription; (9) `204`/`401` forward_auth reply codes. Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (9 steps; ephemeral-guarantee e2e skipped on `--size instant`); full instant suite **77/77 PASS at 100% across 89 specs**. **2nd SPEC / IMPL DIVERGENCE FLAGGED THIS SESSION**: spec § "SSL termination with ephemeral CA" mandates HTTPS on port 443; actual impl is plain HTTP on `:8080` with `auto_https off` (base.Caddyfile:21, deliberate per inline RFC 6761 comment on line 19 — `*.localhost` is loopback + browser secure-context without TLS). Spec text needs update. Quirk: matcher quirk #9 discovered — `grep -F '@trace spec:foo'` fails when `spec:foo` appears in a comma-separated `@trace` list rather than standalone; workaround = match just the `spec:foo` substring. Pattern now 9 deep this arc.
- 2026-05-30T08:51Z  `9703da88`  Spec-gap fill (priority #2 — drift-protection + spec/impl divergence flagged): `proxy-container` coverage 33 → 75. Spec has 12 reqs; single binding `litmus:enclave-isolation` (e2e) covered generic isolation, no source-level pin. New instant litmus `litmus:proxy-container-shape` (9 steps) pinning all 4 proxy files: (1) `@trace spec:proxy-container` on Containerfile + squid.conf + allowlist.txt + entrypoint.sh; (2) Alpine 3.20 + `apk add squid openssl bash` + UID-1000 proxy user; (3) dual-port `http_port 3128 ssl-bump` + `3129 ssl-bump`; (4) `ssl_bump peek/stare/bump all` pipeline (CURRENT mode = bump-all, see flagged divergence); (5) `acl allowlist dstdomain "/etc/squid/allowlist.txt"`; (6) `*.localhost` enclave routing trio (`acl localhost_subdomain` + `cache_peer router parent` + `never_direct allow localhost_subdomain` — RFC 6761 loopback invariant); (7) `forwarded_for delete` + `via off` privacy headers; (8) OpenCode default egress allowlist quartet (`.models.dev` + `.openrouter.ai` + `.helicone.ai` + `.anthropic.com`); (9) entrypoint cert chain (`squid -z` + `security_file_certgen` + `/run/secrets/tillandsias-ca-cert` podman-secret read). Binding now 2 tests, last_verified 2026-05-30. Verification: clean checks; target litmus 1/1 PASS (9 steps; enclave-isolation e2e skipped on --size instant); full instant suite **76/76 PASS at 100% across 89 specs**. **SPEC / IMPL DIVERGENCE FLAGGED**: spec § "SSL bump policy and future activation" claims current mode is splice-all (dormant ssl-bump); actual `squid.conf:102` is `ssl_bump bump all` (active interception per inline "transparent HTTPS caching across the entire enclave + dev builds" comment + `@trace spec:transparent-https-caching` on line 87). Spec text needs update OR squid.conf policy revert. Litmus pins what code does today (peek → stare → bump); follow-on reconciliation slice should resolve.
- 2026-05-30T08:21Z  `ea6dba7a`  Spec-gap fill (priority #2 — drift-protection on 29-annotation implementation crate): `opencode-web-session-otp` coverage 30 → 75. Spec has 6 reqs; binding was single stale `litmus:credential-isolation` (e2e). The implementation crate `tillandsias-otp` (29-annotation spec) was untrapped at source. New instant litmus `litmus:opencode-web-session-otp-shape` (8 steps) pins: (1) dual file-head trace `@trace spec:opencode-web-session-otp, spec:secrets-management`; (2) cookie shape constants `COOKIE_LEN=32` + `COOKIE_NAME="tillandsias_session"` + `COOKIE_PATH="/"` (Req Cookie Shape — 32 random bytes, exact name, root path); (3) `format_set_cookie_header` emits `HttpOnly; SameSite=Lax` literal (Req cookie attrs — Lax NOT Strict, since spec inline comment explains Strict breaks the data: URL → 302 redirect flow); (4) `OtpStore` API ≥4 of {push, validate, evict_expired, evict_project, session_count} lifecycle methods (Reqs Router-Side single-use + multi-window independence); (5) `generate_session_token() -> [u8; COOKIE_LEN]` CSPRNG + `issue_session(project_label) -> [u8; COOKIE_LEN]` entry; (6) `build_login_data_url` + `build_login_form_html` (Req Per-Attach step 4 data: URI embedding); (7) `PENDING_TTL = Duration::from_secs(60)` (Req single-use TTL upper bound — widening expands attack window); (8) control-socket routing `IssueWebSession` matrix-Handle on UnixSocket + Unsupported on Vsock (Req Per-Attach step 5 — convergence packet Q1 answer). Binding now 2 tests, last_verified 2026-05-30. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 1/1 PASS (8 steps; credential-isolation e2e skipped on --size instant); full instant suite **75/75 PASS at 100% across 89 specs**. **Flagged spec gap (NOT closed)**: Req 6 § "Authenticated Readiness Probe" — tray-side 401-then-2xx HTTP probe with registered cookie BEFORE Chromium launch, via in-process HTTP client (never CLI args/logs). Surfaces live in `crates/tillandsias-headless/src/main.rs` per Sources of Truth; would need targeted audit slice to pin function name + 401/2xx assertions.
- 2026-05-30T07:51Z  (no-op)   defer — integration cron at 07:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T07:51Z`. (That cron merged BOTH siblings: windows-next's substantive `2727d24d feat(windows-tray): add build_commit field to --diagnose for triage` — best-effort `git rev-parse --short HEAD` in build.rs with fallback to "unknown" + BUILD_COMMIT_SHA_OVERRIDE env-var hook, schema test extended 10→11 keys additively; osx-next's 1 hourly build-findings entry. All windows changes in sibling-owned scope. Cross-host triage UX strengthened.)
- 2026-05-30T07:21Z  `0cb1ed33`  Spec-gap fill (priority #2 — drift-protection): `forge-opencode-onboarding` coverage 30 → 75. Spec has 3 reqs but existing `litmus:forge-opencode-onboarding-shape` only covered the 5 instruction files + CLI bootstrap; Req 1 (first-turn discovery), Req 2 ORDER constraint, and Req 3 web-launcher scenarios were all untrapped. New instant litmus `litmus:forge-opencode-onboarding-bootstrap-shape` (7 steps) pins: (1) `tillandsias-inventory` literal in forge-discovery.md (Req 1 canonical discovery command); (2) `$TILLANDSIAS_CHEATSHEETS/INDEX.md` path literal (Req 1 — env-var + INDEX co-pinned; catches drift to hardcoded `/opt/...`); (3) `tillandsias-inventory | grep` discipline guidance (Req 1 "don't guess"); (4) Req 2 ORDER — methodology.md as `instructions[0]` verified by awk-parsing the line IMMEDIATELY after `"instructions": [` so position is pinned (not just presence); (5) Req 3 web `apply_opencode_config_overlay` call in entrypoint-forge-opencode-web.sh; (6) Req 3 web `exec /usr/local/bin/sse-keepalive-proxy.js` (dark-theme bootstrap; regression to bare `opencode serve` would silently break theme); (7) Req 3 web project materialization chain (`clone_project_from_mirror` + `find_project_dir` + `export_project_env`). Binding now 2 tests, last_verified 2026-05-30. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 2/2 PASS (10 total steps); full instant suite **74/74 PASS at 100% across 89 specs**. No code changes; pure drift-protection.
- 2026-05-30T06:51Z  `c35a5f42`  Spec-gap fill (priority #2 — dual-spec drift-protection): `chromium-debug-variant` AND `chromium-safe-variant` BOTH 33 → 75 via a single new instant litmus `litmus:chromium-variants-launch-shape` (9 steps). The two variants share a launch path (`scripts/launch-chromium.sh` + `images/chromium/Containerfile.{core,framework}`); only `--remote-debugging-*` flags differ between safe and debug. Pinned: (1) file-head trace declares BOTH specs; (2) `WINDOW_TYPE=open_safe_window` default (safe-by-default — regression flipping the default would silently expose every launch to DevTools); (3) `open_debug_window` branch adds `--remote-debugging-port=` + `--remote-debugging-address=127.0.0.1` (CDP localhost-bound, NOT 0.0.0.0); (4) `tillandsias-chromium-framework:v${VERSION}` image tag + `VERSION == "latest"` early-exit guard; (5) `PROFILE_DIR=$(mktemp -d ...)` per-launch + `rm -rf "$PROFILE_DIR"` + `trap cleanup EXIT INT TERM` (Req Ephemeral profile); (6) `--incognito` + `--user-data-dir=${PROFILE_DIR}` + `--read-only` root (Reqs No-credential-persistence + Safe isolation); (7) ≥3 `--tmpfs=/tmp` mounts (covers /tmp + /tmp/chromium-config + /tmp/chromium-cache); (8) security envelope `--cap-drop=ALL` + `--userns=keep-id` + `--security-opt=no-new-privileges` (must hold even for debug variant per § "Safe isolation still ephemeral"); (9) Containerfile.core + Containerfile.framework both `@trace chromium-debug-variant`. Both bindings now 2 tests (browser-ephemeral e2e + new launch-shape instant), last_verified 2026-05-30. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 1/1 PASS (9 steps; browser-ephemeral e2e skipped on `--size instant` by design); full instant suite **73/73 PASS at 100% across 89 specs**. No code changes; pure drift-protection. **One litmus binding closes two 33% specs in one slice** — efficient pattern when variants share an implementation.
- 2026-05-30T06:21Z  `55558ad4`  Spec-gap fill (priority #2 — breadth pivot from forge-hot-cold-split): `forge-standalone` coverage 33 → 75. Spec has 5 reqs covering operator troubleshooting path (forge-image-only, project-scoped `/home/forge/src/<basename>` mount, interactive bash, full network, fail-fast). Existing `litmus:forge-standalone-traceability` covered @trace markers + --help + build-forge delegation but the actual `podman run` invocation implementing Reqs 1-4 was untrapped. New instant litmus `litmus:forge-standalone-runtime-shape` (8 steps) pins: (1) Req 1 literal `exec podman run` single-container invocation; (2) Req 2 `CONTAINER_SRC="/home/forge/src/$PROJECT_NAME"` mount target; (3) Req 3 `--entrypoint /bin/bash` + `--interactive` + `--tty`; (4) Req 4 `--network=host` (NOT enclave — opposite of forge-offline by design); (5) Reqs 1+5 `tillandsias-forge:v${VERSION}` + `localhost/-prefixed` candidates + NO `:latest` fallback; (6) Req 5 `--src is required` + Source-path-does-not-exist + Source-path-must-be-directory error literals with ≥3 `exit 2` sites; (7) non-negotiable security envelope `--cap-drop=ALL` + `--security-opt=no-new-privileges` + `--userns=keep-id` (methodology guardrail beyond spec text); (8) Req 1 invariant: NO enclave-orchestrator subprocess (`start-enclave`, `run-enclave`, `tillandsias-headless` all absent — pin by absence). Binding now 2 tests, last_verified 2026-05-30. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 2/2 PASS (13 total steps); full instant suite **72/72 PASS at 100% across 89 specs**. No code changes; pure drift-protection.
- 2026-05-30T05:51Z  (no-op)   defer — integration cron at 05:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T05:51Z`. (That cron merged BOTH siblings: windows-next's substantive `a963c16d feat(windows-tray): inject workspace VERSION via build.rs into diagnose JSON` ending the 5-cycle stall + 43-line ASK on `tray-convergence-coordination.md` for macOS to mirror; osx-next's 2 hourly build-findings entries. Windows crate changes all in sibling-owned scope — merged through cleanly. Cross-host coordination working as designed.)
- 2026-05-30T05:21Z  `976351d5`  **Real implementation slice #4** closing 1 of remaining 2 flagged gaps: `spec:forge-hot-cold-split` Req 7 § "Tmpfs-overlay lane for per-project ephemeral cache" pure-decision half. Env var `TILLANDSIAS_PULL_CACHE_RAM_MB` is read by `lib-common.sh:1022` but never set host-side. New preflight.rs surfaces: (a) 3 tier-cap constants `PULL_CACHE_RAM_MB_{MODEST=64, NORMAL=128, PLENTIFUL=1024}` centralised so a regression silently shifting them surfaces in one literal site; (b) 2 boundary constants `PULL_CACHE_RAM_TIER_{NORMAL=8GiB, PLENTIFUL=32GiB}` — `>=` semantics from spec table (exactly 8192 MiB → NORMAL, 32768 → PLENTIFUL); (c) `parse_mem_total_mb(meminfo) -> Option<u64>` Linux /proc/meminfo MemTotal parser (companion to parse_mem_available_mb, fail-closed on None); (d) `resolve_pull_cache_ram_mb(mem_total_mb, override_mb) -> u32` — Some(override) short-circuits, None falls through tier table. 4 new unit tests: tier-table (all 3 tiers + 4 boundary cases), user-override-wins (override at every tier + override=0 degenerate), parser-canonical, parser-fallback. Litmus extended 18 → 21 steps pinning all 3 new surfaces + ≥3 test names. coverage_ratio unchanged at 92 (Req 1's `/home/forge/src` tmpfs gap caps the binding %). Verification: `cargo fmt --all` + `./build.sh --check` clean; new tests 4/4 PASS (preflight module total **17/17**); target litmus 2/2 (23 total steps); full instant suite **71/71 PASS at 100% across 89 specs**. **1 of 5 forge-hot-cold-split spec gaps still open**: Req 1 `/home/forge/src` tmpfs mount declaration (all 3 sized-decision helpers now ready). Plus launch-time WIRING of all 4 helpers (compute_hot_budget into /home/forge/src size, check_host_ram as pre-launch gate, compute_memory_ceiling_mb into `--memory`/`--memory-swap`, resolve_pull_cache_ram_mb into env var injection) remains as coordinated arg-builder refactor.
- 2026-05-30T04:51Z  `3d7fb86d`  **Real implementation slice #3** closing 1 of remaining 3 flagged gaps: `spec:forge-hot-cold-split` Req 3 § "--memory ceiling pairs with tmpfs caps". Spec mandates `--memory = sum(tmpfs caps) + 256 MB` and `--memory-swap` MUST equal `--memory` exactly (zero net swap). Containers ran without memory limits today. New helpers in preflight.rs: (a) `FORGE_WORKING_SET_BASELINE_MB`=256 centralises the spec's working-set headroom; (b) `parse_tmpfs_size_mb(tmpfs_spec) -> Option<u32>` — pure parser for canonical `/path:size=Nm,mode=NNNN` format, tolerant of token order (size= after mode=), missing mode token, and `m`/`M` suffixes; (c) `compute_memory_ceiling_mb(impl IntoIterator<Item=u32>) -> u32` — sum + baseline with saturating-add safety. 5 new unit tests: spec-example (4 mounts 1128 MB → 1384 ceiling), empty-profile returns baseline, saturating-add overflow safety, parser canonical (3 real forge entries + size-only + reversed-order + uppercase `M`), parser fallback (5 malformed cases). Litmus extended 15 → 18 steps pinning all 3 new surfaces + ≥3 test names. coverage_ratio 83 → 92. Pure half decoupled from launch-time wiring — composing helpers into `container_spec.rs:build_run_args` (push `--memory=<N>m` + `--memory-swap=<N>m`) is the follow-on. Verification: `cargo fmt --all` + `./build.sh --check` clean; new tests 5/5 PASS (preflight 13/13 total); target litmus 2/2 (20 total steps); full instant suite **71/71 PASS at 100% across 89 specs**. **2 of 5 forge-hot-cold-split spec gaps still open**: Req 1 `/home/forge/src` tmpfs mount (helpers now ready to size it!); Req 7 host-side `TILLANDSIAS_PULL_CACHE_RAM_MB` injection.
- 2026-05-30T04:21Z  `75e7021f`  **Real implementation slice #2** closing 1 of remaining 4 flagged gaps: `spec:forge-hot-cold-split` Req 4 § "Pre-flight RAM check refuses launch on insufficient host RAM". New `crates/tillandsias-core/src/preflight.rs` module with: (a) `HOST_RAM_HEADROOM_NUM`=125 + `HOST_RAM_HEADROOM_DEN`=100 integer constants — spec's 1.25× factor expressed integer-only for overflow-safety; (b) `HostRamCheck` struct (mem_available_mb / required_mb / threshold_mb) carrying the trio for accountability logging without re-query; (c) `PreflightError` enum with `#[non_exhaustive]` for future gates (disk, FD) — today `InsufficientRam` variant with same trio; (d) `check_host_ram(mem_available_mb, required_mb) -> Result<HostRamCheck, PreflightError>` — pure cross-platform decision with `>=` boundary + `div_ceil` rounding (3 MB × 1.25 → 4, not 3); (e) `parse_mem_available_mb(meminfo_output) -> Option<u64>` — Linux-native /proc/meminfo line parser truncating kB → MiB (conservative — rounds DOWN, more strict); (f) Display impl rendering accountability-log-friendly message with all 3 values + `1.25×` literal. 8 unit tests: ok-at-threshold, refuse-below-threshold, ceil-rounding, zero-required degenerate branch, max-required-no-panic (saturating-mul), parser-canonical, parser-fallback-none, Display-includes-all. Litmus extended 11 → 15 steps pinning all 4 spec surfaces + ≥5 test names. coverage_ratio 75 → 83. Cross-platform pure half decoupled from per-platform reader — macOS (`vm_stat`) and Windows (`GlobalMemoryStatusEx`) hosts can compose `check_host_ram` with their own readers. Verification: `cargo fmt --all` + `./build.sh --check` clean; new tests 8/8 PASS; target litmus 2/2 (17 total steps); full instant suite **71/71 PASS at 100% across 89 specs**. **3 of 5 forge-hot-cold-split spec gaps still open**: Req 1 `/home/forge/src` tmpfs mount, Req 3 `--memory` launch args, Req 7 host-side `TILLANDSIAS_PULL_CACHE_RAM_MB` injection.
- 2026-05-30T03:51Z  (no-op)   defer — integration cron at 03:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T03:51Z`. (That cron merged osx-next's `9b339892` — 2 more macOS hourly `/build-macos-tray` build-findings entries, plan/-only, no spec/methodology drift. Flagged windows-next now stalled 5 consecutive cycles — orchestrator escalation watch noted but not yet blocking.)
- 2026-05-30T03:21Z  `c9b2b7fd`  **Real implementation slice** closing 1 of 5 flagged spec gaps: `spec:forge-hot-cold-split` Req 5 § "Per-launch project source budget". The 02:51Z drift-protection slice flagged that `compute_hot_budget` was missing despite ForgeConfig knobs being there; this slice ships the pure half of the recipe. New `crates/tillandsias-core/src/config.rs` surfaces: (a) `pub const HOT_PATH_BUDGET_FLOOR_MB: u32 = 256` centralises the spec's "Empty mirror returns floor" scenario; (b) `pub fn parse_size_pack_kb(count_objects_output: &str) -> u64` — step 1 of the recipe parsing `git count-objects -v -H` output (tolerant of order, whitespace, missing field, unparseable values — fail-open into floor branch); (c) `pub fn compute_hot_budget(pack_size_kb: u64, config: &ForgeConfig) -> u32` — full recipe with saturating-mul + clamp-floor-first/ceiling-second. 6 new unit tests: inflate-default, returns-floor-empty-mirror, clamps-ceiling, honours-custom-knobs, parser-extracts-canonical, parser-zero-on-missing. Litmus extended 7 → 11 steps pinning helper signatures + floor constant + ≥4 test names. coverage_ratio 67 → 75. Verification: `cargo fmt --all` + `./build.sh --check` clean; new tests 6/6 PASS; target litmus 2/2 (13 total steps); full instant suite **71/71 PASS at 100% across 89 specs**. Pure half decoupled from git — thin git-runner wrapper + plumbing into `/home/forge/src` tmpfs mount sized arg is follow-on (needs the `/home/forge/src` mount declaration which is another flagged gap). **4 of 5 spec gaps still open**: Req 1 `/home/forge/src` tmpfs mount, Req 3 `--memory` launch args, Req 4 `check_host_ram` + PreflightError, Req 7 host-side `TILLANDSIAS_PULL_CACHE_RAM_MB` injection.
- 2026-05-30T02:51Z  `23028890`  Spec-gap fill (priority #2 — drift-protection + 5 spec gaps flagged): `forge-hot-cold-split` coverage 33 → 67. Spec has 7 requirements; binding was single `litmus:forge-hot-cold-split-shape` (config round-trip + welcome banner). New instant litmus `litmus:forge-hot-cold-split-tmpfs-shape` (7 steps) pinning IMPLEMENTED surfaces: (1-3) 4 forge profiles × 3 tmpfs mounts (`/opt/cheatsheets:size=8m,mode=0755` + `/tmp:size=256m,mode=1777` + `/run/user/1000:size=64m,mode=0700`) — Reqs 1+2 partial; (4) ≥2 `TILLANDSIAS_CHEATSHEETS` profile env var sites — Req 6 agent transparency; (5) ForgeConfig `hot_path_max_mb` + `hot_path_inflation` + `pull_cache_ram_mb` fields — Reqs 5+7 config knobs; (6) `4096` default with `fn default_hot_path_max_mb` — Req 5 clamp ceiling; (7) 4 entrypoint scripts carry `@trace spec:forge-hot-cold-split`. Binding now 2 tests, last_verified 2026-05-30. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 2/2 PASS (9 total steps); full instant suite **71/71 PASS at 100% across 89 specs**. **Five flagged spec gaps NOT closed** (real implementation work ~6-8h total): (1) Req 1 § HOT tier — `/home/forge/src` referenced everywhere but NOT declared as tmpfs mount in container_profile.rs; only 3 of 4 hot paths mounted; (2) Req 5 § `compute_hot_budget(project_name, cache_dir)` helper does not exist — ForgeConfig knobs are there but no function consumes them; (3) Req 4 § `check_host_ram` + `PreflightError::InsufficientRam` + /proc/meminfo + 1.25× headroom — unimplemented; (4) Req 3 § `--memory` + `--memory-swap` launch args — containers run without memory limits today; (5) Req 7 § `TILLANDSIAS_PULL_CACHE_RAM_MB` env var read by `images/default/lib-common.sh:1022` but NEVER SET by any host-side launch code. Recommended sequence: Req 1 → Req 5 → Req 3 → Req 4 → Req 7. Out of drift-protection scope; queued for next implementation cycle.
- 2026-05-30T02:21Z  `c7708748`  Spec-gap fill (priority #2 — drift-protection + spec-gap flag): `podman-secrets-integration` coverage 33 → 75. Spec has 9 requirements covering the full podman-secret lifecycle; binding was single stale `litmus:credential-isolation` (last_verified 2026-05-03). `litmus:secrets-management-implementation-shape` (shipped 01:21Z) already covers Reqs 1/2/5/6/8 for the GH-token slice — co-bound here. New instant litmus `litmus:podman-secrets-integration-shape` (6 steps) pins broader surfaces: (1) `cleanup-secrets.sh` SECRETS array declares all 3 spec-registered names (`tillandsias-ca-cert` + `tillandsias-github-token` + `tillandsias-vault-unseal`) per spec Secret Names Registry table; (2) `tillandsias-vault-token-*` per-container leftover walker — Req 5 panic + idempotent cleanup; (3) ≥4 `"--secret"` flag literal sites across vault_bootstrap.rs + remote_projects.rs + main.rs — Req 2 (`--secret` flag, NEVER `-v` bind mount); (4) `invocation_line_contains_label_and_redacts_secrets` redaction unit test pinned by name — Req 4 (format_podman_invocation_line replaces secret values with `<redacted>` BEFORE logging); (5) ≥8 `secret_name = "tillandsias-…"` accountability log fields in main.rs — Req 8; (6) all 3 entrypoints (git/proxy/inference) carry `@trace spec:podman-secrets-integration` — Req 3 readers. Binding now 3 tests (credential-isolation + secrets-management-implementation-shape co-bound + new shape), last_verified 2026-05-30. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 2/2 instant PASS (credential-isolation e2e skipped on --size instant; 14 total instant steps); full instant suite **70/70 PASS at 100% across 89 specs**. **Flagged spec gap (NOT closed)**: Req 7 mandates in-memory CA cert/key generation + immediate podman-secret conversion; current implementation at main.rs:~1230 writes cert/key to disk via `openssl req` to certs_dir, then keeps them as files (NOT podman secrets). Narrow gap (transport mechanism), not catastrophic. Needs in-memory openssl + file-driver secret creation refactor — out of drift-protection scope.
- 2026-05-30T01:51Z  (no-op)   defer — integration cron at 01:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-30T01:51Z`. (That cron merged osx-next's `e309f39a` — 2 more macOS hourly `/build-macos-tray` build-findings entries, plan/-only +44 lines, no spec/methodology drift. Also flagged windows-next stalled at `24d7bec7` for 4 consecutive cycles — not yet blocking integration but worth watching.)
- 2026-05-30T01:21Z  `310b99e0`  Spec-gap fill (priority #2 — drift-protection): `secrets-management` coverage 33 → 75. Spec is `superseded` by tillandsias-vault Phase 6 but stays active for one release behind `--legacy-keyring-secrets`. Binding was single stale `litmus:credential-isolation` (last_verified 2026-05-03); Reqs 2-4 implementation surfaces were untrapped despite living in main.rs / vault_bootstrap.rs / container_profile.rs for weeks. New instant litmus `litmus:secrets-management-implementation-shape` (8 steps) pinning all 4 requirements: (1) `fn run_github_login(debug, legacy_keyring_secrets)` entry point; (2) `--legacy-keyring-secrets` CLI flag parsed; (3) `fn create_github_podman_secret(token, debug)` Req 2 delivery; (4) `podman secret create --driver=file tillandsias-github-token` literal; (5) `/run/secrets/tillandsias-github-token` read path across ≥4 sites (main.rs + vsock_server.rs + remote_projects.rs); (6) `.cache/tillandsias/secrets/git/.gitconfig` literal in container_profile.rs Req 3; (7) `podman secret rm tillandsias-github-token` cleanup in vault_bootstrap.rs Req 4; (8) accountability log fields `category="secrets"` + `operation="rotation_complete"` Req 4. Binding now 3 tests (credential-isolation + gh-auth-script-shape co-bound + new implementation-shape), last_verified 2026-05-30. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 2/2 instant PASS (credential-isolation e2e, skipped under --size instant; 15 total instant steps); full instant suite **69/69 PASS at 100% across 89 specs**.
- 2026-05-30T00:51Z  `c09dec47`  Spec-gap fill (priority #2 — bindings + bridge-surface drift-protection): `mcp-on-demand` coverage 33 → 75. Spec was bound only to `litmus:mcp-on-demand-shape` (TraySocket mount prerequisite, last_verified 2026-05-13) — neither the on-demand contract NOR the host-browser-mcp bridge that exercises it were pinned by mcp-on-demand. New instant litmus `litmus:mcp-on-demand-bridge-shape` (6 steps) pinning: (1) opencode mcp config declares servers as `"type": "local"` with `command:` array (Req 1 — on-demand CLI launch, not persistent daemon); (2) ≥3 mcp entries present among {host-browser, git-tools, project-info, dependency-resolver, agent-profile} — Req 2; (3) host-browser entry points at `/home/forge/.config-overlay/mcp/host-browser.sh` — Req 5; (4) host-browser.sh carries `@trace spec:host-browser-mcp`; (5) reads `TILLANDSIAS_CONTROL_SOCKET` env var — Req 6 Unix socket channel; (6) bridges via canonical `socat - "UNIX-CONNECT:"` pattern — Req 6 framing contract. Binding now 3 tests (mcp-on-demand-shape + host-browser-mcp-shape size:quick + new bridge-shape), last_verified 2026-05-30. Matcher quirk recurrences: #2 (escaped quotes in `expected_behavior` don't survive YAML→shell — drop quote chars from substring) hit 2 sites, #3 family (`\{` regex escape doesn't survive — drop brace, entry-name uniqueness handles it) hit 1 site. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 2/2 instant PASS (host-browser-mcp-shape skipped on --size instant by design); full instant suite **68/68 PASS at 100% across 89 specs**. NOT pinned (genuine spec gaps): Req 3 (health check + 5s timeout) and Req 4 (ephemeral container-child lifecycle) — runtime container-side behaviour, not source-level greppable.
- 2026-05-30T00:21Z  `93f4293c`  Spec-gap fill (priority #2 — trace alignment + drift-protection): `forge-offline` coverage 33 → 75. Spec has 3 requirements (zero credentials / no project mount / enclave-only network) all met by the declarative-profile in `crates/tillandsias-core/src/container_profile.rs` + ENCLAVE_NET launch arg in `main.rs`, but binding was only `litmus:credential-isolation` (last_verified 2026-05-03, pre-declarative-profile refactor) and the surface was untrapped. Added `@trace spec:forge-offline (zero credentials via secrets: vec![]; no host project mount; no credential env vars — Reqs 1+2)` to container_profile.rs file head. New instant litmus `litmus:forge-offline-profile-shape` (6 steps) pinning: (1) file-head trace; (2) ≥3 forge profile builders set `secrets=vec![]` (opencode/claude/opencode-web — Req 1); (3) common_forge_mounts uses only the 4 legitimate MountSource variants (ToolsOverlay/ConfigOverlay/ContainerLogs/TraySocket) and ZERO {HostProject/ProjectRoot/WorkspaceMount} forbidden variants — Req 2; (4) common_forge_env has zero GH_TOKEN/ANTHROPIC_API_KEY/GIT_ASKPASS sites — Req 1; (5) main.rs `const ENCLAVE_NET = "tillandsias-enclave"` declared + ≥3 references — Req 3; (6) `forge_profiles_have_eighteen_env_vars` behavioural test attached. Binding now 3 tests (credential-isolation + ephemeral-guarantee + new shape pin), last_verified 2026-05-30. Matcher quirk #8: `grep -c 'secrets: vec!\[\]'` mangles via YAML→shell un-escape (`\[` strips, leaving `[]` empty-charclass); workaround `grep -cF` fixed-string mode. Family now 8 deep. Verification: `cargo fmt --all` + `./build.sh --check` clean; `cargo test -p tillandsias-core --lib container_profile::` 20/20 PASS; target litmus 6/6 instant PASS; full instant suite **67/67 PASS at 100% across 89 specs**. Pure drift-protection + trace alignment.
- 2026-05-29T23:51Z  (no-op)   defer — integration cron at 23:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T23:51Z`. (That cron merged osx-next's `7f9c8cc0` — 2 more macOS hourly `/build-macos-tray` build-findings entries, plan/-only, no spec/methodology drift. Next eligible work cycle: ≥23:54Z.)
- 2026-05-29T23:25Z  `0d66126e`  Spec-gap fill (priority #2 — trace alignment + drift-protection): `runtime-diagnostics` coverage 30 → 75. Spec was bound only to `litmus:runtime-diagnostics-shape` (git entrypoint trace, last_verified 2026-05-03 — pre-gap-3), missing all the MUST-emit / MUST-detect scenarios despite a real implementation in `crates/tillandsias-podman/src/diagnostic_event_emitter.rs` since gap-3 phase-2b. Root cause: the implementation was tagged exclusively as `spec:runtime-diagnostics-stream`, missing the spec-identity edge to the parent `spec:runtime-diagnostics` (the two co-implement the same scenarios from streaming vs failure-capture angles). Fixed by: (a) adding `@trace spec:runtime-diagnostics (Capture exit code and termination signal, Detect resource exhaustion conditions)` to `diagnostic_event_emitter.rs` file head; (b) new instant litmus `litmus:runtime-diagnostics-spec-mapping-shape` (6 steps) pinning the spec-identity surfaces: file-head trace, `"memory_oom"` literal in Oom arm, `format_resource_exhaustion_event(..., "memory_oom", None)` call, signal-line-before-exit-line ordering (verified by line-number comparison — kernel-fact-first principle, spec § Abnormal exit), `record.exit_code.and_then(signal_name_from_exit_code)` composition pin (spec § Normal exit MUST NOT emit spurious signal), `signal_name_from_exit_code_returns_none_on_clean_or_out_of_range` test attached. Binding now 3 tests (runtime-diagnostics-shape + runtime-diagnostics-emitter-shape co-bound + new spec-mapping-shape). Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 3/3 PASS (15 total steps); full instant suite **66/66 PASS at 100% across 89 specs**. NOT pinned by this slice (genuine spec gaps for future): runtime-diagnostics § "Capture container stderr to $XDG_RUNTIME_DIR file" + § "Analyze stderr for common failure patterns" (compilation/network/permission error pattern matchers) — these require new code, not just bindings.
- 2026-05-29T22:55Z  `9e19fa13`  Spec-gap fill (priority #2 — drift-protection): `tray-host-control-socket` coverage 30 → 75. Spec has 4 requirements (Socket Creation/Lifecycle, Wire Format/Framing, Capability-Based Routing, Error Handling) and the convergence packet (`plan/issues/control-socket-protocol-convergence-2026-05-25.md`) shipped a real implementation of all four on the unix-tray side over the last two weeks — but NONE was pinned. The single existing binding `litmus:binary-e2e-smoke` (last_verified 2026-05-15, pre-convergence-packet) covered binary lifecycle, not the socket. Added `litmus:tray-host-control-socket-unix-shape` (instant pre-build, 8 steps) pinning: file-head trace declares the spec (added — was missing from header despite many inline traces); `control_socket_path` resolves `XDG_RUNTIME_DIR` with `/run/user/<uid>` fallback (spec § Lifecycle #1); literal `tillandsias/control.sock` path join; `start_control_socket_server` + stale-unlink + `Permissions::from_mode(0o600)` (spec § Lifecycle #2/#5); postcard framing helpers `read_control_envelope` / `write_control_envelope` with `u32::from_be_bytes` / `to_be_bytes` 4-byte BE prefix (spec § Wire Format); unix dispatcher consults canonical `control_dispatch::{DispatchOutcome, TransportKind, decide_route}` matrix (spec § Capability-Based Routing — single source of truth, prevents unix/vsock silent disagreement); `type ControlSubscribers = Arc<Mutex<Vec<Arc<Mutex<UnixStream>>>>>` fan-out shape (broadcast-then-ack OTP race fix); ≥2 `seq_in_reply_to: Some(first.seq)` sites (spec § Error Handling). Binding now 3 tests (binary-e2e-smoke + control-dispatch-shape + tray-host-control-socket-unix-shape), last_verified 2026-05-29. ERE-paren YAML→shell round-trip quirk recurrence on step 4 — workaround: use `.` as wildcard for `(0o600)`. Verification: `cargo fmt --all` + `./build.sh --check` clean; target litmus 2/2 instant steps PASS (binary-e2e-smoke skipped on `--size instant` by design); full instant suite **65/65 PASS at 100% across 89 specs**. No code changes; pure drift-protection + file-head trace alignment.
- 2026-05-29T22:29Z  `f82e958e`  Spec-gap fill (priority #2 — real refactor + drift-protection): `fix-windows-image-routing` coverage 33 → 67. Spec § "Image Build Centralized in Helper" mandates the routing logic MUST be encapsulated in a small `image_build_paths(image_name: &str) -> (Containerfile, ContextDir)` helper — but the routing was inline in `prepare_build`, a real spec gap. Extracted `pub(crate) fn image_build_paths(root_dir: &str, image_name: &str) -> Result<(String, String), ImageBuilderError>` in `crates/tillandsias-core/src/image_builder.rs` returning (containerfile_path, context_dir) for forge / proxy / git / inference / web; named `ContainerfileNotFound` error for unknown types preserves observability. `prepare_build` now delegates via `image_build_paths(&self.root_dir, image_name)?` — no inline match. Added `@trace spec:fix-windows-image-routing` at file head. Two new unit tests: `image_build_paths_routes_each_known_type_to_its_containerfile` (5 cases + the parent invariant that context_dir is the Containerfile's parent dir) + `image_build_paths_rejects_unknown_image_type_with_named_error` (asserts the error message contains the bad name). New `litmus:fix-windows-image-routing-helper-shape` (instant pre-build, 6 steps, grep-only) pins helper exists with canonical signature, returns the named result type, prepare_build delegates, and both unit tests are present — combined with existing `litmus:fix-windows-image-routing-shape` (which runs the cargo routing test) raises coverage 33 → 67. Documented 7th matcher quirk: `grep -F '-> ...'` parses `->` as a malformed option without the `--` separator; fix is `grep -F -- '-> ...'`. Verification: `cargo fmt --all` + `./build.sh --check` clean, `cargo test -p tillandsias-core --lib image_builder::` 5/5 PASS (incl. 2 new), target litmus 2/2 PASS, full instant suite **63/63 PASS at 100% across 89 specs**. Real refactor — not pure drift-protection.
- 2026-05-29T21:51Z  (no-op)   defer — integration cron at 21:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T21:51Z`. (That cron merged osx-next's `632786c3` — hourly /build-macos-tray run + new `macos-tray-ux-gaps-2026-05-29.md` durable UX-regression doc. Per-platform build cron mesh now producing ledger artifacts cross-host.)
- 2026-05-29T21:21Z  `ce132853`  Spec-gap fill (priority #2): `browser-isolation-framework` coverage 33 → 67. Natural follow-on to prior cycle's browser-isolation-core. New `litmus:browser-isolation-framework-shape` (instant pre-build, 6 steps, grep+test-only) pins source-level surfaces the existing e2e `litmus:browser-ephemeral` doesn't pre-build: 3-spec @trace header on Containerfile.framework; `FROM ${CHROMIUM_CORE_IMAGE}` (spec: "Image hierarchy locked to chromium-core" — hardcoding a tag breaks content-addressed hierarchy); `Extends tillandsias-chromium-core with:` documentation; `expect_base` assertion in check-container-bases.sh via two simpler greps; executable bit + canonical path. Stashed dirty-tree auto-artifact churn first. Discovered 6th matcher quirk: runner double-unescapes `\\\"` → `\"` (BASH_REMATCH unescape) then bash interprets `\"` as `"`, so embedded escaped double-quotes around file paths in expected lines produce unbalanced quotes — workaround is splitting into 2 simpler greps. Pre-build instant suite 61 → 62/62 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T20:51Z  `c97d2676`  Spec-gap fill (priority #2): `browser-isolation-core` coverage 33 → 67. New `litmus:browser-isolation-core-shape` (instant pre-build, 7 steps, grep+test-only) pins the launch-script source-level surfaces the existing e2e `litmus:browser-ephemeral` doesn't pre-build: 3-spec @trace headers on launch-chromium.sh + run-safe-browser.sh; `"--cap-drop=ALL"` literal (spec: "Reduced capabilities"); `"--user-data-dir=${PROFILE_DIR}"` (ephemeral profile invariant); `tillandsias-chromium-framework:v${VERSION}` image identity pattern (spec: "Content-hash image identity"); `scripts/check-container-bases.sh` executable (sibling verification script); both launch scripts executable. Discovered 5th matcher quirk: `expected_behavior` starting with `--` triggers the matcher's grep-flag interpretation bug ("unrecognized option") — workaround is to strip the leading `--` (the runner passes positionally to `grep -Fqi`). Pre-build instant suite 60 → 61/61 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T20:21Z  `3a2ee11b`  Spec-gap fill (priority #2): `forge-staleness` coverage 30 → 67. New `litmus:forge-staleness-invariants-shape` (instant pre-build, 5 steps, grep-only) pins the RUST-SIDE invariants the existing build-script-side litmus doesn't cover: `fn containerfile_is_stale(root: &Path, image_name: &str, debug: bool) -> Result<bool, String>` entry point in main.rs:1049; the NON-NEGOTIABLE `unwrap_or(false); // No cached version on fresh start is OK, not a mismatch` literal at main.rs:899 (spec: "Absent cache version file means fresh start, not a staleness signal" — a refactor swapping to `unwrap_or(true)` would silently convert ephemeral deployments into staleness errors); at least 4 `@trace spec:forge-staleness, spec:forge-cache-dual` markers; the "Step 1: Check staleness (skip if already exists)" comment + "Whether the build was skipped due to staleness" docstring in image_builder.rs. Pre-build instant suite 59 → 60/60 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T20:05Z  (no-op)   defer — integration cron at 20:00Z (5 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-antigravity-2026-05-29T20:05Z`.
- 2026-05-29T19:51Z  (no-op)   defer — integration cron at 19:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T19:51Z`. (That cron merged 3 windows-next commits — windows added `/build-windows-tray` + `/build-macos-tray` (renamed to `/probe-macos-tray-on-windows` to avoid collision with macOS-host's authoritative skill). All three platform hosts now have per-platform build cron skills. Orchestration mesh is 6 skills deep.)
- 2026-05-29T19:21Z  `c27ed2db`  Spec-gap fill (priority #2): `gh-auth-script` coverage 30 → 67. New `litmus:gh-auth-script-shape` (instant pre-build, 7 steps, grep+test-only) pins the structural surfaces the existing quick `litmus:gh-auth-script-smoke` (fake-podman harness) depends on: `fn run_github_login(debug: bool, legacy_keyring_secrets: bool)` entry point + its 5-spec docblock trace (`gh-auth-script, secrets-management, podman-secrets-integration, secret-rotation, tillandsias-vault`); `--github-login` + `--legacy-keyring-secrets` CLI flags; harness + create-secrets script executable bits; `operation = "gh_auth_start"` accountability log. Pre-build instant suite 58 → 59/59 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- **2026-05-29T18:54Z  `RELEASE v0.2.260529.1 SUCCESS`** — workflow `26654453129` completed at 18:54:11Z. Linux musl artifacts live at https://github.com/8007342/tillandsias/releases/tag/v0.2.260529.1 — Fedora Silverblue smoke-test ready: `tillandsias-linux-x86_64` (38.8 MB, sha256 `ccb16a9c…`), `tillandsias-headless-x86_64-unknown-linux-musl` (36.4 MB, sha256 `cb7a7d7e…`), `install.sh` (sha256 `164768e4…`), `verify.sh`, SHA256SUMS. All cosign-signed. Daily cadence routine `trig_01MgbYKBLV5pMHw75kzTPxaG` next fires 2026-05-30T18:08Z. First execution of the new `merge-to-main-and-release` skill end-to-end successful — 460 commits to main → tag → workflow_dispatch → live release in ~40 min.
- 2026-05-29T18:51Z  `b3cf4983`  Spec-gap fill (priority #2): `ci-release` coverage 33 → 67. New `litmus:ci-release-toolchain-shape` (instant pre-build, 6 steps, grep+test-only) pins the spec surfaces the existing musl-binary-policy litmus skips: `musl-tools` install in release.yml; `workflow_dispatch:` triggers on BOTH release.yml + recipe-publish.yml (a regression adding `on: push: tags:` would silently auto-trigger releases, breaking the merge-to-main-and-release skill's manual-authorship guarantee from `28ea3023`); `@trace spec:observability-convergence, spec:ci-release` on github-actions-convergence.yml; executable bit on both `scripts/local-ci.sh` + `scripts/github-actions-convergence.sh` (spec's "Litmus Chain" names them as sibling tests). Release-flow aligned: this litmus would catch a release.yml drift on the NEXT cycle. Pre-build instant suite 57 → 58/58 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T18:21Z  `d52f6f21`  Spec-gap fill (priority #2): `environment-runtime` coverage 33 → 100. Pure bindings-only update. Spec.md (lines 8-19) documents 13 test bindings to FOUR litmus tests; today only `litmus:environment-isolation` was bound. The other 3 (`enclave-isolation`, `ephemeral-guarantee`, `podman-idiomatic-launch-routing`) are existing litmus tests targeting other spec_ids but documented by the spec as environment-runtime's verification surface — added them. Pre-build instant suite 57/57 PASS at 100% across 89 specs. `./build.sh --check` clean. cli-diagnostics audit: skipped — that spec is a deprecation stub.
- 2026-05-29T18:18Z  `6d9c8cba`  **RELEASE v0.2.260529.1 — merged linux-next → main via PR #7, tagged, workflow_dispatch triggered.** First execution of the new `merge-to-main-and-release` skill (canonical `skills/merge-to-main-and-release/SKILL.md`, mirrored to 5 agent runtimes — symlink layout matches `advance-work-from-plan`). Closes 460 commits of work since last main merge (litmus + Containerfile + cross-host convergence + curated-toolchain implementations + headless control-socket convergence). Release workflow `26654453129` in_progress (historical runs ~43 min). Linux x86_64 musl artifact is the Fedora Silverblue smoke-test target. Daily cadence persisted via cloud schedule routine `trig_01MgbYKBLV5pMHw75kzTPxaG` (cron `0 18 * * *`) — next firing 2026-05-30T18:08Z, will re-invoke the skill autonomously. Per skill STEP 8.
- 2026-05-29T17:51Z  (no-op)   defer — integration cron at 17:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T17:51Z`. (That cron merged windows-next's `f66e9fcc` — windows bound the cross-tray PTY-attach project-threading symmetric pin litmus, completing the m10 convergence after macOS shipped m10 to match the windows `launch_spec` call shape. Cross-tray ship+bind+mirror pattern now 7 deep this session.)
- 2026-05-29T17:21Z  `a262561f`  Spec-gap fill (priority #2): `app-lifecycle` coverage 33 → 67. Discovered a real BINDING MISMATCH: app-lifecycle was bound only to `litmus:environment-isolation` which actually targets `environment-runtime` (stale binding from 2026-05-03). Also skipped `forge-forward-compat` (status: deprecated). New `litmus:app-lifecycle-state-machine-shape` (instant pre-build, 8 steps, grep-only) pins the `TrayAppLifecycleState` enum + `validate_transition` state machine in `crates/tillandsias-core/src/state.rs`: file-head trace, enum declaration, all 5 spec-mandated variants (Idle/Initializing/Running/Stopping/Error), method signature, and 4 canonical transition rules (Idle→Initializing, Initializing→Running, Initializing→Error, Running→Stopping). The Running→Stopping rule is the graceful-shutdown gate — losing it would let Running→Idle skip shutdown cleanup. Note: many other spec requirements reference future symbols (shutdown_all, MenuCommand::Quit, ExitRequested, kill_container) not yet in the workspace — forward-looking, pinned by future iterations as they ship. Stashed dirty-tree dashboard auto-artifact churn first. Pre-build instant suite 55 → 56/56 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T17:07Z  (completed)  Step 21.6 (Forge Diagnostics Improvement Loop) successfully completed! All 12 proposals approved and implemented. Verified via full local CI/CD litmus validation suite (55/55 PASS at 100% across all 89 specifications). Plan index and loop status updated.
- 2026-05-29T16:51Z  `d1642cb3`  Spec-gap fill (priority #2): `filesystem-scanner` coverage 33 → 67. Stashed dirty-tree auto-artifact churn first (Cargo.lock + VERSION + TRACES.md regeneration + dashboard updates per skill §1 — don't commit). New `litmus:filesystem-scanner-event-driven-shape` (instant pre-build, 7 steps, grep-only) pins source-level surfaces the existing quick test (which runs unit tests) doesn't: `@trace spec:filesystem-scanner` in lib.rs; `notify::{Event, RecommendedWatcher, RecursiveMode, Watcher}` use (OS-native facade, a refactor to `PollWatcher` would silently violate the spec); 2000 ms default debounce + `config.debounce_ms` override; docstring's inotify/kqueue/ReadDirectoryChangesW backend list + `fs.inotify.max_user_watches` sysctl mention (production gotcha the spec's "Watch limit exhaustion graceful degradation" Test depends on); `watch()` returning `notify::Result<()>` so errors propagate. Pre-build instant suite 54 → 55/55 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T16:21Z  `8affa1fb`  Spec-gap fill (priority #2): `async-inference-launch` coverage 30 → 67. New `litmus:async-inference-launch-entrypoint-shape` (instant pre-build, 7 steps, grep-only) pins the FORGE-SIDE entrypoint contract that makes inference a soft requirement per the spec ("failure logs DEGRADED but does not block coding session start"). Pins: both opencode + opencode-web entrypoints carry `@trace spec:async-inference-launch`; the `curl -m 1 -sf "http://inference:11434/api/version"` bounded probe (1-second timeout that prevents launch-time hang); the "do NOT block" design comment; both `trace_lifecycle "inference" "ready"` (passed) and `"not-ready"` (graceful) branches — the latter is the soft-requirement enforcer (a regression swapping it for `exit 1` would silently convert the spec's soft requirement into a hard gate). Companion to the existing `litmus:inference-readiness-probe-shape` which covers orchestrator/tray-launcher/standalone-diagnostic surfaces. Note: spec mentions `ensure_enclave_ready()` + `BUILD_MUTEX` at `handlers.rs:54` but those don't exist in the workspace yet — entrypoint scripts ARE the live implementation today. Pre-build instant suite 53 → 54/54 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T15:51Z  (no-op)   defer — integration cron at 15:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T15:51Z`. (That cron merged windows-next's `cc21502e` — windows mirrored macOS slice 30 to pin 6 windows-tray architectural invariants. Cross-tray architectural-invariants parity now complete: both trays have grep-based source-level pins for their canonical no-X and has-X invariants.)
- 2026-05-29T15:21Z  `aa11275e`  Spec-gap fill (priority #2): `cheatsheet-source-layer` coverage 33 → 67. New `litmus:cheatsheet-source-layer-shape` (instant pre-build, 7 steps, grep+test-only) pins structural surfaces the existing runtime `litmus:cheatsheet-source-layer-validation` doesn't: validator script canonical path + spec trace + `INDEX_FILE` constant; cheatsheet-sources/ dir existence; validator's 4 documented checks; `## Provenance` convention in TEMPLATE.md; fresh-state graceful handling (validator warns + exits 0 when INDEX.json absent — pinned via "no sources fetched yet" literal). Two iteration notes: (1) initial INDEX.json EXISTENCE assertion was wrong — validator gracefully handles absence per the sanity-check block, reframed to pin the graceful-handling literal instead; (2) em-dash punctuation in the warning text fuzzes UTF-8 round-trip via YAML→shell — fourth matcher/parser quirk documented this session (after folded scalars, quote-substring escaping, bracket ERE escapes). Pre-build instant suite 51 → 52/52 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T14:51Z  `458704d6`  Spec-gap fill (priority #2): `forge-cache-dual` coverage 33 → 67. New `litmus:forge-cache-dual-tmpfs-caps` (instant pre-build, 6 steps, grep-only) pins the two requirements the existing `litmus:forge-cache-dual-shape` didn't: (1) the kernel-enforced tmpfs caps `/tmp:size=256m,mode=1777` + `/run/user/1000:size=64m,mode=0700` (spec calls non-negotiable — writes beyond MUST fail with ENOSPC) across `container_profile.rs` + `launch.rs` + `cache_semantics.rs`'s format-string generator; (2) the cache_version lifecycle functions `init_cache_dir` + `save_version` + `check_cache_integrity` in main.rs plus the `cache_version` filename literal via `cache_dir.join("cache_version")`. Catches refactors that bypass the generator (silently undercapping one site), rename a lifecycle function (breaking spec→code mapping), or bump one cap value without updating both sites. Pre-build instant suite 50 → 51/51 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T14:21Z  `3e3e95b8`  Spec-gap fill (priority #2): `cheatsheets-license-tiered` coverage 33 → 67. New `litmus:cheatsheets-license-allowlist-shape` (instant pre-build, 6 steps, grep-only) pins the `cheatsheets/license-allowlist.toml` file structure that the spec mandates as the tier-inference source of truth: canonical location, three canonical tier strings (bundled/distro-packaged/pull-on-demand) in the header, required per-domain schema fields (publisher/license/redistribution/default_tier/last_evaluated), default-tier inference rule for do-not-bundle, ≥1 `[domains.*]` entry, INDEX.md present. Companion to the existing runtime `litmus:cheatsheet-tier-discipline`. Used `grep -cF '[domains.'` instead of ERE `\[` to avoid the YAML-via-shell double-escape "Unmatched [" error pattern (worth documenting since it's a third quirk in the matcher/parser interaction I've now seen — alongside folded scalars and quote-substring escaping). Pre-build instant suite 48 → 50/50 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T13:51Z  (no-op)   defer — integration cron at 13:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T13:51Z`. (That cron merged windows-next's `1336fe04` + `0b5ee1c0` — windows bound macOS slices 27+28 cross-tray pins + their own sync-merge with conflict resolution. Auto-merged cleanly on the linux side; suite went 47 → 48/48 at 100%.)
- 2026-05-29T13:21Z  `da8907a5`  Spec-gap fill (priority #2): `external-logs-layer` coverage 33 → 67. New `litmus:external-logs-manifest-shape` (instant pre-build, 6 steps, grep-only) pins the producer-manifest contract that the existing `litmus:external-logs-layer-shape` (size: quick) didn't cover. Pins: the `@trace spec:external-logs-layer` line on `images/default/external-logs.yaml`; top-level `role:` + `files:` declarations; each file entry's required schema fields (name, purpose, format, rotate_at_mb, written_by); `format:` values restricted to {text, jsonl} (spec forbids binary); `external_logs_dir()` + `external_logs_role_dir(role: &str)` helpers in tillandsias-core. Also confirmed clippy clean across the entire workspace (all 6 linux-owned + sibling crates 0 warnings — priority #3 hardening fully drained). Pre-build instant suite 45 → 47/47 PASS at 100% across 89 specs. No code changes; pure drift-protection.
- 2026-05-29T12:51Z  `e8659b38`  Priority #3 linux clippy hardening sweep + cross-host litmus repair. Clippy went 4 → 0 warnings: dropped `use std::process::Command` from `remote_projects.rs` (left dangling by gemini's `45244a41` refactor — the file moved from tillandsias-core and the Command-using path was replaced by the canonical podman facade); fixed three `clippy::doc_overindented_list_items` on the `TrayPhaseHandle` docblock I authored at `a10dc0f6`. Plus repaired the new sibling `litmus-recipe-release-tag-symmetric.yaml` (macOS slice 27 `39246fa5`) which was failing post-merge because its expected_behavior strings were descriptions ("windows tag pinned at v0.2.260526.1") instead of substrings of the grep output. Tried the quote-escaped literal first but YAML quote-escaping left backslashes in the regex-captured value; final fix is `expected_behavior: "v0.2.260526.1"` (clean version-literal substring). Inline comment added documenting the matcher's substring rule + the YAML-quote-escape trap. Full pre-build instant suite 44 → 45/45 PASS at 100% across 89 specs (was 44/45 with recipe-release-tag-symmetric failing on step 3). 144 headless tests still green; `./build.sh --check` clean.
- 2026-05-29T12:21Z  `006fc1b0`  Resolved the 11:43Z conflict-skipped cron: cherry-picked windows-next's `441b8426` onto linux-next, preferring their version of `litmus-wire-unreachable-chip-text-symmetric.yaml` (3 steps, single-line, drops two redundant value-content steps since per-host unit tests already pin the byte sequence — exactly the recommendation I wrote into the cron ledger entry). Conflict on that file resolved via `git checkout --theirs` + re-add. The new clean file `litmus-windows-tray-diagnose-cli-surface.yaml` (7 steps pinning the windows --diagnose CLI surface end-to-end) came in cleanly; bindings auto-merged. Pre-build instant suite 42 → 44/44 PASS at 100% across 89 specs (both newly-executing). `./build.sh --check` clean. Unblocks the next integration cron from re-encountering this same conflict.
- 2026-05-29T11:51Z  (no-op)   defer — integration cron at 11:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T11:51Z`. (That cron was CONFLICT-SKIPPED on the parallel folded-scalar repair race: linux antigravity `3d24ac20` and windows `441b8426` both independently fixed the same drift advisory in `litmus-wire-unreachable-chip-text-symmetric.yaml`. Recommended resolution per the cron ledger: cherry-pick windows-next's more-aggressive repair onto linux-next as a non-defer cycle action; that unblocks the next integration cron from re-encountering the same conflict.)
- 2026-05-29T11:25Z  `06afb958`  Fifth and final implementation slice off the orchestrator-approved curated toolchain backlog: Rust cargo-* batch (8 tools) and WASM wasmtime. Added `cargo-binstall` inside the Rust Containerfile layer and used it to fetch pre-compiled binaries for the remaining 7 cargo utilities (`cargo-edit`, `cargo-llvm-cov`, `cargo-deny`, `cargo-semver-checks`, `cargo-expand`, `cargo-criterion`, `cargo-wasi`). Installed the official WebAssembly runtime `wasmtime` (v45.0.0) by directly extracting its precompiled static binary into `/usr/local/bin`. Advanced all 9 tool statuses to `implemented` in `curated-toolchain-backlog-2026-05-29.md` along with delve (already present as dlv). Rebuilt and verified the new `tillandsias-forge` image (build time: 430s, size: 7937 MB, all 43/43 pre-build instant litmus tests pass 100%). Claimed and closed lease `lease-linux-forge-rust-wasm-tools-20260529T1106`. Backlog is now 100% completed!
- 2026-05-29T11:21Z  `8c94e44a`  Sibling antigravity claimed the Rust+WASM tools lease at 11:06Z (expires 15:06Z), pre-empting both my queued "Next" options. Pivoted to a non-Containerfile spec-gap fill that exercises the McpFrame surface (priority #2 explicitly calls out as the last matrix-Handle-but-no-handler variant). New `litmus:host-browser-mcp-frame-shape` (instant pre-build, 6 steps) pins: McpFrame variant declaration shape `{ session_id: u64, payload: Vec<u8> }`, `MAX_MCP_FRAME_BYTES = 4 MiB`, the `@trace spec:host-browser-mcp` docblock mapping, convergence-packet matrix routing (UnixSocket=Handle, Vsock=Unsupported), and the docstring acknowledging McpFrame's 4 MiB exception to the general 64 KiB cap. `host-browser-mcp` coverage_ratio 33 → 67. Pre-build instant suite 41 → 42/42 PASS at 100% across 89 specs. No code changes; pure drift-protection at the spec-contract level. Companion to the existing `litmus:host-browser-mcp-shape` (size: quick, runs cargo test).
- 2026-05-29T10:51Z  `46125bdf`  Fourth implementation slice off the orchestrator-approved curated toolchain backlog: Shell + Profiling tooling batches. Added `shellcheck ltrace heaptrack` to the existing microdnf RUN layer (alongside the existing `gdb lldb strace valgrind` debugger family); added `shfmt` via `go install mvdan.cc/sh/v3/cmd/shfmt@latest` to the existing Go-tools RUN layer (alongside gopls + dlv). 4 candidates flipped from `approved` → `implemented`. NO new image layers; NO envelope change (ltrace uses same ptrace privs as existing strace; shellcheck/heaptrack are standard Fedora packages from existing dnf upstream; shfmt is the same Go-module-proxy chain that serves gopls + dlv). Approved-pending remaining: 9 (Rust cargo-* batch of 8 + WASM wasmtime). Pre-build instant suite 41/41 PASS at 100% across 89 specs; container build deferred to next CI cycle.
- 2026-05-29T10:21Z  `50bf7ca2`  Third implementation slice off the orchestrator-approved curated toolchain backlog: Python + Web tooling batches. Added `black pylint bandit` to the existing `pip3 install` line (4 chars + 3 tools); added `prettier eslint` to the existing `npm install -g` line. 5 candidates flipped from `approved` → `implemented`. NO new image layers (rides existing RUN steps). NO envelope change. Backlog state recap: Rust cargo-* batch (8 tools) + WASM wasmtime + Shell (shellcheck/shfmt) + Profiling (ltrace/heaptrack) remain approved-pending; nix/wasmer/flutter/flake8/cargo-tarpaulin deferred; cargo-tree/perf/jq blocked. Pre-build instant suite 41/41 PASS at 100% across 89 specs (sibling activity since my last cycle bumped both 40→41 and 87→89). Container build deferred to next CI cycle; verification path post-rebuild: next live forge-diagnostic run reports all 5 tools as present.
- 2026-05-29T10:16Z  `b25f0b28`  Second implementation slice off the orchestrator-approved curated toolchain backlog: Shipped the Modern Dev Quality-of-Life batch (`bat`, `git-delta`, `git-lfs`, `httpie`, `yq`) and other approved build/development tools directly at image build time. Globally registered Git LFS filters within the forge sandbox via `git lfs install --system`. Documented this new capability scenario under `openspec/specs/default-image/spec.md`. Advanced statuses to `implemented` in proposal `2026-05-29-dev-quality-of-life.md` and `curated-toolchain-backlog-2026-05-29.md`. Successfully rebuilt and verified the new `tillandsias-forge:latest` image (build time: 434s, size: 7375 MB, all 41/41 instant pre-build litmus tests pass 100%). Agent: `linux-tlatoani-fedora-antigravity-2026-05-29T10:16Z`.
- 2026-05-29T10:04Z  `3d24ac20`  Completed the multi-host coordination cycle. Repaired the new `litmus-wire-unreachable-chip-text-symmetric.yaml` by converting the folded-`>` scalar commands to single-line `command: "..."` format so that the regex runner can execute its steps correctly. Adjusted expected behavior strings in `litmus-container-start-health.yaml` to ensure clean execution. Regenerated the CentiColon convergence dashboard (`docs/convergence/centicolon-dashboard.md` / `.json`), showing 100% closed correctness debt and 100% green pre-build instant litmus (41/41 executed). Monitored and verified that Release workflow run `26544334121` has formally succeeded, publishing Linux musl, macOS Apple Silicon, and Windows native tray releases. Verified that both remote sibling branches (`origin/windows-next` and `origin/osx-next`) are clean ancestors of `linux-next` ($D = 0$). Agent: `linux-tlatoani-fedora-antigravity-2026-05-29T10:04Z`.
- 2026-05-29T09:51Z  (no-op)   defer — integration cron at 09:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T09:51Z`. (That cron merged windows-next's `43737173` cross-tray wire-unreachable symmetric pin litmus — but flagged it as silently broken due to the YAML folded-`>` scalar runner-parser bug I've encountered twice now. Natural follow-on for the next non-defer cycle: convert its `command: >` blocks to single-line form, same pattern as `5438da9a` and `dd5e07ff`.)
- 2026-05-29T09:21Z  `42bddb03`  First implementation slice off the orchestrator-approved curated toolchain backlog (orchestrator review `045a6720`): `rust-analyzer` PATH symlink. The rustup component IS installed (Containerfile line 53) but the binary lives under `$RUSTUP_HOME/toolchains/<triple>/bin/`, NOT in `/usr/local/cargo/bin` (which is on PATH). LSP clients launch by binary name and can't find it. Fix: `ln -sf "$(rustup which rust-analyzer)" /usr/local/bin/rust-analyzer` appended to the rustup-install RUN step. `rustup which` absorbs the toolchain-version detail. Backlog status flipped from `approved` → `implemented` with shipped-diff cited in the notes column (extends the proposed/approved/blocked/deferred quartet — the "Update protocol" anticipates this). NO envelope change. Verification path is the next live forge-diagnostic run. Build/check clean; 40/40 instant pre-build litmus suite still 100%.
- 2026-05-29T08:51Z  `8c0c8387`  Spec-gap fill (priority #2): `litmus-container-start-health.yaml` declared `spec: podman-idiomatic-patterns` + `@trace spec:podman-idiomatic-patterns, spec:runtime-diagnostics-stream, spec:forge-as-only-runtime` but was ENTIRELY UNBOUND in `openspec/litmus-bindings.yaml`. Runner never discovered it for any of the 3 traced specs despite rich post-build E2E coverage (event:container_launch stream assertions + idiomatic-layer non-bypass + agent-stage verification + the annex piggyback I added at e4aad43b). Bound under all 3 spec-IDs the test traces. Discovery now confirmed: each spec's runner invocation lists it exactly once. Suite total 138 → 141 entries (same test, 3 bindings); pre-build instant suite executed count unchanged at 40/40 (test is `size: e2e`, runs only under `--size e2e`); no regression. Bindings-only commit. `runtime-diagnostics-stream` coverage_ratio 90 → 100 (was 30 → 90 from my 07:21Z slice).
- 2026-05-29T08:21Z  `c0e9d69e`  Curated-toolchain-backlog Update 08:21Z section appended — sibling antigravity beat me to my queued folded-form sweep (`dd5e07ff` at 08:13Z, no harm — independent parallel work). Pivoted to extending the curated backlog with deltas from two new live diagnostic runs (08:08Z + 08:11Z) that surfaced 15 new candidates: `rust-analyzer` PATH-symlink installation gap, `cargo-audit`, `just`, `gcc`/`g++`/`make`/`cmake` (verify-first), `jq` (verify-installed — run discrepancy with 06:03Z), `ripgrep`/`fd`/`bat`/`delta`/`httpie` (dev quality-of-life batch), `yq`, `git-lfs`, plus a non-binary architectural recommendation `tmpfs-work-partition` (Containerfile/orchestrate-enclave.sh change, tagged for orchestrator routing to `spec:forge-cache-dual` rather than treating as a toolchain item). Amplified privacy/isolation gate with 5 new observations (proxy-as-plain-HTTP, GIT_AUTHOR_* env leak, /run/secrets world-readable, single root FS, HTTPS-CONNECT bypass). Cross-run convergence updates strengthen `nix`/`flutter`/`prettier`/`eslint`/`black`/`delve` (4-of-5 or 3-of-5 vs prior 2-of-3). Sizing-notes addendum proposes new "dev quality-of-life batch" + "C/C++ build tooling batch (verify-first)" + "rust-analyzer PATH symlink (trivial)" packets. agent_status_packet appended. Continuation of the same `forge-enhancements/curated-toolchain-backlog` claim from 0df57c10.
- 2026-05-29T08:13Z  `dd5e07ff`  Spec-gap fill (priority #2) follow-on: Converted remaining 5 folded-scalar litmus tests to single-line command format (`container-start-health`, `control-dispatch-shape`, `diagnostics-filter-env-shape`, `forge-diagnostics-e2e`, `inference-deferred-model-pulls`) to ensure compatibility with runner's regex parser. Resolved environment-pollution issue in headless `remote_projects.rs` tests where `TILLANDSIAS_PODMAN_BIN` bypassed the mocked `PATH`-based podman, causing short-name failures during CI. Instant post-build suite 100% PASS, full CI green. Agent: `linux-tlatoani-fedora-antigravity-2026-05-29T08:13Z`.
- 2026-05-29T07:51Z  (no-op)   defer — integration cron at 07:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T07:51Z`. (That cron merged windows-next's `145ff3d2` slices 22+23 parity — `WIRE_UNREACHABLE_CHIP_TEXT pin` with the SAME name as macOS slice 23 + `--diagnose` CLI spec doc. Cross-OS byte-identical drift protection now complete on windows+macOS.)
- 2026-05-29T07:21Z  `5438da9a`  Spec-gap fill (priority #2): bound 2 previously-unbound litmus tests to `runtime-diagnostics-stream` (`runtime-diagnostics-emitter-shape` + `runtime-diagnostics-typed-events-shape`); both target `spec: runtime-diagnostics-stream` at file head but neither appeared in `openspec/litmus-bindings.yaml`. Discovered in passing that `litmus-runtime-diagnostics-typed-events-shape.yaml` used folded-`>` multi-line scalars for `command:` — runner's regex parser only matches single-line `command: "..."`, so this litmus was shipped previously as drift-protection but silently NEVER executing. Converted format in lockstep with the binding. Coverage_ratio 30 → 90 on `runtime-diagnostics-stream`. Full pre-build instant suite 38 → 40/40 PASS at 100% across 87 specs. Wider survey identified ≥5 other folded-form litmus tests in similar shape (container-start-health, control-dispatch-shape, diagnostics-filter-env-shape, forge-diagnostics-e2e, inference-deferred-model-pulls) — natural follow-on slice.
- 2026-05-29T07:05Z  (no-op)   defer — integration cron at 07:05Z (0 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-macuahuitl-gemini-20260529T070512Z`.
- 2026-05-29T06:51Z  `f8c7a0f9`  Spec-gap fill (priority #2): `observability-metrics` was uncovered AND missing from `openspec/litmus-bindings.yaml` entirely. New `litmus-observability-metrics-shape` (instant pre-build, 7 steps) pins the Prometheus exporter contract end-to-end: `@trace` line, `Result<String, String>` signature (no fabricated-value-on-failure refactor), the spec-mapping contract comment, the 500-on-failure HTTP path (no fabricated 200 OK), `MetricsServerState` Arc<Mutex<sampler>> + exporter shape, `# TYPE`/`# HELP` headers via the existing unit test, and `text/plain; version=0.0.4` content type. Coverage 86→87 specs at 100% pass rate; instant pre-build suite 37→38/38. No runtime change. Defensive drift-protection at the spec-contract level — runtime behaviour already covered by existing unit tests in `metrics_server.rs`.
- 2026-05-29T06:21Z  `0df57c10`  Curated-toolchain-backlog claimed + slice 1 shipped (`forge-enhancements/curated-toolchain-backlog`). Now-unblocked by the functionally-complete `e2e-piggyback-orchestration` parent. New file `plan/diagnostics/forge-enhancements-curated-toolchain-backlog-2026-05-29.md` seeds 24 unique candidates across 8 ecosystems (Rust×11, Python×4, Web×2, Dart×1, Go×1, WASM×2, Shell×2, Profiling×3, Reproducible×1) from three live diagnostic runs this session (04:05Z, 05:03Z, 06:03Z — converging seed). Each candidate carries proposed-only status (orchestrator approves, not me), source-run citations, rationale, privacy/isolation notes. Pre-decomposes the future approval queue into 10 platform-sized implementation packets per the work-packet's "not one giant image change" rule. No code changes; pure planning artifact owned by `plan/diagnostics/` scope. agent_status_packet appended.
- 2026-05-29T06:08Z  `96531d2a`  Configured Squid TCP Reset for strict port denials (proposal `2026-05-28-proxy-egress-isolation.md`) to enforce zero-trust isolation boundaries in the forge enclave. Replaced HTTP 403 Access Denied responses with immediate connection resets (TCP RST) on port 3128 for non-allowlisted destinations. Added Test 10 (`test_proxy_egress_isolation_tcp_reset`) in `tests/cache_peer_routing.rs` to validate ACL, deny_info, and http_access configuration precedence and prevent regression/drift. All 144 headless tests passing.
- 2026-05-29T05:51Z  (no-op)   defer — integration cron at 05:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T05:51Z`. (That cron merged windows-next's `d2cf10f0` wire-degradation chip, mirroring macOS slice 21 (`36879a5e`) shipped earlier — both tray platforms now surface wire-unreachable as a distinct UX state, natural follow-on to Q2 lifecycle-phase visibility.)
- 2026-05-29T05:21Z  `8f070293`  USER PRIORITY (a) follow-on: distill script consumes `event:diagnostics_envelope` from the `.stderr.log` companion to recover framing fields (timestamp, tillandsias_version → forge_version with annotation, host_platform, agent) when the LLM's stdout JSON failed to parse. Closes the consumer side of 4c2993ac's runtime emitter. Synthetic round-trip evidence: empty stdout + envelope-only stderr now produces a summary with real timestamp (2026-05-29T05:21:00Z vs the prior `unknown`), real forge_version + "(from-envelope; in-forge JSON missing)" annotation, and new "Host platform: linux" + "Agent: opencode" metadata lines. New drift-protection litmus `litmus-diagnostics-envelope-shape` (instant pre-build, 6 steps) pins both sides of the Rust-emitter ↔ bash-consumer symmetry; `cli-diagnostics` spec flipped from deferred to active in `openspec/litmus-bindings.yaml`. Full pre-build instant suite now 37/37 (was 36) at 100% pass rate across 86 specs (was 85, +1 cli-diagnostics activated). 144 headless tests still green.
- 2026-05-29T04:51Z  `4c2993ac`  USER PRIORITY (a): emit `event:diagnostics_envelope` stderr line on `--diagnostics`. Closes "ensure --diagnostics emits the structured capability JSON the forge-diagnostics litmus expects" — the runtime side. Direct evidence of the gap: 19:02Z baseline showed `TIMESTAMP=unknown` + `FORGE_VERSION=unknown` + `0/0 checks passed` because the LLM emitted nothing to stdout. Now the runtime ALWAYS writes a stable machine-readable framing line to stderr regardless of agent behaviour. Two pure helpers (`select_diagnostics_agent_kind` + `format_diagnostics_envelope_line`), gated by `if diagnostics`, called right after the existing `[tillandsias] version:` debug line. Format: `event:diagnostics_envelope timestamp=<ISO-Z> tillandsias_version=<v> host_platform=<…> agent=<…>` — same family as the existing `event:container_launch` lines `litmus-container-start-health` already greps. 3 new tests + 144 headless tests passing (up from 141). WIRE_VERSION unchanged. Follow-on: distill-script consumes the line from the `.stderr.log` companion to populate TIMESTAMP/FORGE_VERSION/HOST_PLATFORM/AGENT fields when the JSON payload's own fields are unknown.
- 2026-05-29T04:21Z  `08b9e96e`  Wire SIGTERM/SIGINT into `TrayPhaseHandle::watch_shutdown_and_mark_stopping_blocking` — closes the natural follow-on from a10dc0f6 and completes Q2 functionally across all three transports. Cross-host context: windows-next 80eceb0b AND macOS slice 20 (8b9baf8f) BOTH now send `VmShutdownRequest` before tearing down WSL/VZ; this slice mirrors the symmetry on the linux tray so a sibling-host client polling `VmStatusRequest` during tray exit sees `phase=Stopping` instead of stale `Ready`. Four wiring pieces: new `watch_shutdown_and_mark_stopping_blocking` (sync mirror of the vsock-side async helper, 250ms poll, defensive-guard against clobbering terminal Failed); `start_control_socket_server(shutdown)` accepts + threads the atomic, spawns the watcher std::thread; `install_shutdown_signal_handlers` lifted to `pub(crate)`; runtime block_on swapped from `pending().await` to a tokio sleep loop polling the atomic at 250ms. 2 new tests + 141 headless tests passing (up from 129 + 10 from gemini's 45244a41 refactor pickup). McpFrame is now the only remaining matrix-Handle-but-no-handler variant on the unix dispatcher. WIRE_VERSION unchanged at 2.
- 2026-05-29T04:06Z  `45244a41`  Complete plan index and step file ledger bookkeeping for task `podman-control-plane-overhaul/migrate-legacy-shell` (retire residual shell orchestration and no-direct-podman audit). Verified that all 207 headless tests and 661+ workspace unit/integration tests pass cleanly. Flipped parent step 15.5 status to completed.
- 2026-05-29T03:51Z  (no-op)   defer — integration cron at 03:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T03:51Z`. (That cron merged windows-next's `80eceb0b` VmShutdownRequest-on-Quit, completing the Q2 three-host convergence loop in under 2 hours: linux ships unix handler → osx noops → windows consumes via wire-level drain.)
- 2026-05-29T03:21Z  `e4aad43b`  USER PRIORITY (diagnostics-driven container-start verification): amend `litmus-container-start-health.yaml` to piggyback `scripts/forge-diagnostics-annex.sh` (no `--reset`) as two non-gating steps after the existing idiomatic-layer assertions. The annex's checksum-by-prompt dedup ensures one capture per cycle across all forge-launching E2E tests; the rest write skip notes. Closes the 27-05 20:30Z "next checkpoint" line in `plan/issues/forge-diagnostics-automation-2026-05-27.md`. Inventory survey confirmed `litmus-container-start-health` is the only other tillandsias-launching E2E test (others use raw `podman run` for image inspection — not piggyback candidates). YAML parses; type-check clean; all 36 instant-phase post-build litmus tests still PASS (100% coverage of 85 specs). agent_status_packet appended to the packet ledger. Slice (b)+(c) of user-prompt deliverables; (a) needs a live forge to verify per the 20:30Z packet's deferral note.
- 2026-05-29T03:09Z  `dd7878a2`  Retire residual shell orchestration tasks and enforce repo-wide no-direct-podman audit. Moved `remote_projects.rs` from `tillandsias-core` to `tillandsias-headless` to enforce sandboxed Podman execution via the canonical `tillandsias-podman` facade (using `podman_cmd_sync()`). Also refactored all raw `podman` calls in `vault_bootstrap.rs` to route through the `tillandsias-podman` facade. Updated imports in `tray/cloud.rs` and `tray/mod.rs` to reference the relocated `remote_projects` module. Removed legacy `remote_projects` from `tillandsias-core`'s module structure, completed a repo-wide audit confirming no raw `Command::new("podman")` invocations remain outside of diagnostic OS-availability tests, and verified that all 207 headless tests pass cleanly.
- 2026-05-29T02:51Z  `a10dc0f6`  TrayPhaseHandle + VmShutdownRequest handler on unix dispatcher (convergence packet Q2). New cheap-to-clone `Arc<RwLock<VmPhase>>` wrapper mirrors the in-VM `VmStateHandle`; control-socket accept thread constructs one, transitions `Starting -> Ready` after `UnixListener::bind()` succeeds, clones into each per-connection worker. VmStatusRequest now reads the live phase (no more hardcoded Ready); VmShutdownRequest flips it to Draining + closes the connection without reply (matching the vsock side — no `VmShutdownReply` variant exists). Fourth matrix-Handle-but-no-handler variant migrated to a real implementation. Three new tests + one rebased: 129 headless tests passing. Inline dispatcher comment updated: only McpFrame + host-only stdin/pty tunnel frames remain matrix-Handle-but-no-handler-yet. WIRE_VERSION unchanged at 2. Follow-on: wire `mark_stopping()` into the tray's existing SIGTERM/SIGINT signal path so VmStatusRequest observers see `Stopping` during tray exit too.
- 2026-05-29T02:25Z  `9eff05c8`  VmStatusRequest handler on unix dispatcher (convergence packet Q2) — third matrix-Handle-but-no-handler variant migrated to a real implementation (after EnumerateLocalProjects 05cc3a7d and CloudRefreshRequest 71db9f68). Minimal slice: reply with `phase=VmPhase::Ready` (we're answering on a working socket — by definition the tray is serving) + live `tillandsias_podman::podman_available_sync()` check for `podman_ready` + `"linux-native-tray"` transport tag in `last_event` so downstream clients can tell unix-from-vsock replies apart. Renamed regression test to use `McpFrame` (still matrix-Handle-but-no-handler) instead of `VmStatusRequest`; added new test pinning the Ready-phase reply shape. WIRE_VERSION unchanged at 2. 127 headless tests passing. Follow-on: real `TrayPhaseHandle` with Starting/Stopping/Draining/Failed transitions rooted in the tray's SIGTERM/SIGINT atomic.
- 2026-05-29T01:51Z  (no-op)   defer — integration cron at 01:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-29T01:51Z`. (Notable: that cron merged windows-next consuming the EnumerateLocalProjects handler I shipped at 00:55Z — cross-host convergence working as designed.)
- 2026-05-29T01:25Z  `71db9f68`  CloudRefreshRequest handler on unix dispatcher (convergence packet Q4) — new shared `cloud_projects` module with `fetch_cloud_projects(token: Option<&str>)` (vsock side passes Some(mounted-secret), unix side passes None → `gh` uses host's auth config) + pure `parse_gh_repo_list`. Vsock side refactored to thin token-read shim; 3 parser tests moved into shared module + still green. Second matrix-Handle-but-no-handler variant migrated to a real implementation.
- 2026-05-29T01:06Z  (no-op)   defer — integration cron at 01:05Z (1 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-gemini-20260529T0105`.
- 2026-05-29T00:55Z  `05cc3a7d`  EnumerateLocalProjects handler on unix dispatcher (convergence packet Q4) — new shared `local_projects` module with pure `scan_project_root(&Path) -> Vec<LocalProjectEntry>`; vsock side refactored to use it; unix side gains `TILLANDSIAS_HOST_PROJECT_ROOT` env var + `$HOME/src` default + the inner-Handle arm. 3 new + 2 existing scanner tests all green. First matrix-Handle-but-no-handler variant migrated to a real implementation.
- 2026-05-29T00:23Z  `614d65f4`  litmus: pin control-socket convergence packet wiring — new `litmus-control-dispatch-shape` instant-phase test greps `control_dispatch.rs` + `tray/mod.rs` + `vsock_server.rs` + `main.rs` mod registration. Seven steps catch a regression that re-inlines the routing matrix in either dispatcher while keeping the matrix unit tests green. Drift protection for items 1-3 shipped earlier this session.
- 2026-05-29T00:06Z  (no-op)   defer — integration cron at 00:05Z (1 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-macuahuitl-gemini-20260529T0005`.
- 2026-05-28T23:51Z  (no-op)   defer — integration cron at 23:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-28T23:51Z`.
- 2026-05-28T23:23Z  `4eb0baff`  **control-socket convergence packet COMPLETE** — item 3 wires `decide_route` into `vsock_server::serve_connection` (async tokio path) as a pre-filter to the existing variant-match. Three DispatchOutcome arms with transport-specific Error messages; the matrix is now the single source of truth for both unix + vsock dispatchers. WIRE_VERSION unchanged at 2.
- 2026-05-28T22:54Z  `aeb5499a`  control-socket convergence packet item 2: wire `decide_route` into `tray::handle_control_connection` (sync unix path). Three DispatchOutcome arms; matrix-Handle-but-no-handler-yet variants now produce a descriptive Error message referencing the convergence packet instead of the prior generic one. Matrix tests unchanged + green.
- 2026-05-28T22:27Z  `5c67ddb9`  control-socket convergence packet item 1: new `crates/tillandsias-headless/src/control_dispatch.rs` with pure `decide_route(msg, transport) -> DispatchOutcome`. Encodes Q1/Q2/Q4 answers verbatim from the convergence packet; 4 unit tests pin the full unix + vsock routing matrices. `#[allow(dead_code)]` until follow-on slice wires the two existing dispatchers.
- 2026-05-28T22:20Z  `a81cc9b5`  forge-improvements — implement approved developer toolchains in Containerfile (Go, Rust + WASM tools, Python + uv/mypy/pytest, Dart SDK, typescript/typescript-language-server, fd-find). Rebuilt and tagged tillandsias-forge:latest/v0.2.260528.1 and verified with full litmus suite (all 57 checks passing cleanly).
- 2026-05-28T21:51Z  (no-op)   defer — integration cron at 21:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-28T21:51Z`.
- 2026-05-28T21:24Z  `b52f51b7`  gap-3 phase-2 symmetry — wire `spawn_diagnostic_event_emitter` + typed-stderr `DiagnosticsHandle::start_typed_event_stream` into `run_observatorium_mode` (mirrors `run_opencode_mode`). Inline note documents that chromium-core/framework events sit outside the rt.block_on and aren't captured here. Litmus extended with a both-modes-wire-both-helpers assertion.
- 2026-05-28T20:52Z  `9e33a458`  commit 4 distill summaries (18:00Z, 18:02Z, 18:42Z, 19:02Z) backfilled with gap-3 phase-2g typed-event sections. Cross-host visible evidence of `event:container_stderr` shipping spec-shape in production (proxy ×100s, git ×8–10 per session).
- 2026-05-28T20:23Z  `08071930`  align `.claude/commands/advance-work-from-plan.md` + `.opencode/commands/advance-work-from-plan.md` + `methodology.yaml shared_skills` to Tlatoāni's `c1a57f47` canonical worker-protocol wording. Adds `tier: worker`, `complement_of`, `authoritative_spec` pointers to the registry entry. Pure description alignment, no behavioural change.
- 2026-05-28T19:51Z  (no-op)   defer — integration cron at 19:43Z (8 min ago, inside 10-min defer window). No slice taken; cron writes need to settle. Agent `linux-tlatoani-fedora-claude-opus-2026-05-28T19:51Z`.
- 2026-05-28T19:29Z  `2b589f13`  distill: surface all 5 gap-3 typed-event arms in summaries (counts + sample lines for exit/signal/resource + top-5 noisiest by stderr volume) + defensive fix for empty-log abort. Verified against the first production gap-3 phase-2g capture at 19:02Z (115 `event:container_stderr` lines).
- 2026-05-28T19:24Z  `9683b11e`  canonical `skills/advance-work-from-plan/SKILL.md` + symlinks into all 5 agent-runtime `skills/` dirs (Claude, OpenCode, Codex, Gemini, GitHub) + slash-command shims in `.claude/commands/` + `.opencode/commands/` + registry entries in `methodology.yaml` + `plan/index.yaml`. Superseded by Tlatoāni's more structured `c1a57f47` later that hour; the canonical body is now the formal worker-protocol variant.
- 2026-05-28T17:24Z  `ce257f39`  gap-5 phase-2 bounded ring buffer (10K) + `BackpressureMeter` rising-edge warn at depth > 100 per spec:runtime-diagnostics-stream "Event rate limit" + "Terminal blocked".
- 2026-05-28T16:53Z  `758e2e46`  litmus: pin gap-3 phase-2 emitter-layer surfaces (spawn helper, EmitterState, signal helper, routing arms, typed stderr tail, run_opencode_mode wiring).
- 2026-05-28T16:24Z  `c21ebfd4`  gap-3 phase-2g typed `event:container_stderr` stream — `DiagnosticsHandle::start_typed_event_stream` + run_opencode_mode wiring on the 4 SUPPORT containers. **6-arm gap-3 chain COMPLETE.**

(Older entries — pre-2026-05-28T16:00Z — collapse into the headless spec-gaps backlog at `plan/issues/linux-headless-spec-gaps-2026-05-27.md`.)
- 2026-05-30T18:13Z  `d2c15f37`  PARTIAL Release v0.2.260530.1 — merged PR #8 to main, VERSION bumped to 0.2.260530.1 (commit 677a89af pushed), tag push BLOCKED by git proxy HTTP 403 (refs/tags/* not proxied). workflow_dispatch NOT triggered. See ESCALATION in multi-host-integration-loop-2026-05-24.md §Cycle 2026-05-30T18:13Z.
