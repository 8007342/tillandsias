- 2026-08-04T00:55Z forge-opencode-20260804t000814z: 583-dv9n DONE — dispatch-to-manifest drift guard (main.rs DISPATCH_ARMS + manifest_drift warning on `capabilities` stderr, never a gate; stdout tokens/exit 0 untouched). New litmus:capability-manifest-guard 4/4 (builds a real artifact from a manifest trimmed of `query`; asserts warning fires, fail-safe probe shape + blocked_capabilities=query, clean tree silent); skew-honesty 28/28. 569 verified already-completed by fefd21dd (litmus pattern fix) — my claim corrected back to completed. fmt/clippy/workspace-tests/build.sh --check green.
- 2026-07-12T18:06Z forge: order 300 DONE (explicit zero-match litmus filter fails loud; empty buckets preserved; targeted 5/5 + Forge build check PASS). Root-caused recurring mirror first-push stale-ref race and filed ready order 301; Forge E2E `skip:no-podman-binary`.
- 2026-07-11T19:45Z linux_mutable macuahuitl: RELEASE v0.3.260711.8 published (PR #71, run 29165261781, all platform assets + cosign bundles). Gate: 4th ci-full attempt green after updater-race + provider-throttling litmus fixes; first live smoke-mode meta e2e PASS. Shipped with the 8 known tray-parity gaps (orders 257/258), operator-directed.
- 2026-07-12T19:40Z linux_mutable macuahuitl: RELEASE v0.3.260712.1 published (PR #72, run 29205458140, merge 38d33cd8, 25 assets all platforms + cosign, run 19:15→19:39Z). Carries orders 298-302 fixes + operator smoke-test drain (forge TUI-spill silencing, GIT_SSL_CAINFO, agent exit-pause). Shipped with the 8 known tray-parity gaps, operator-approved; orders 257/258/274 UNBLOCKED for macOS/Windows wrapper completion. Linux artifact: https://github.com/8007342/tillandsias/releases/download/v0.3.260712.1/tillandsias-headless-x86_64-unknown-linux-musl
- 2026-07-13T01:15Z linux_mutable macuahuitl: /build-install-and-smoke-test-e2e PASS @ 0a5c2ca7 (installed v0.3.260713.1). Gates: ci-full+install 17/17 (first attempt failed on order-315 cheatsheet tier/sync — fixed 0a5c2ca7); podman system reset clean; cold --init exit 0 (vault healthy); forge lane exit 0 — in-forge agent FIXED order 316 (pre-receive subshell reject-loss, cb9bfd7f) with fixture and pushed via mirror to GitHub (live mirror relay evidence post-302-class). One finding: forge OpenSpec init WARNING (optimization/forge-openspec-init-fails-warning-2026-07-12.md). Evidence: target/build-install-smoke-e2e/20260712T235847Z
- 2026-07-13T12:05Z linux_immutable yolanda (fresh Silverblue, first cycle): /smoke-curl-install-and-test-e2e PASS on release v0.3.260712.1 (first curl-install verification of the latest published release). Steps: install rc=0 version-match; podman reset clean; cold --init exit 0 (all 10 images, vault healthy); forge full-meta lane exit 0 — in-forge OpenCode claimed order 313, landed slice 1 as 4281ce4e via mirror one-push; 4b PASS (proxy alive alongside lane — order-298 regression absent). Pre-e2e drain: 79682b9f (4 builder-wrapper fresh-host fixes, order-239 criterion falsified+fixed), 10671807 (clippy 1.97 drift + toolchain-pin issue), d877e199 (order 285 DONE, 3-env verification). 6 new findings packets: plan/issues/smoke-e2e-findings-v0.3.260712.1-2026-07-13.md. Evidence: target/smoke-e2e/ (local).
- 2026-07-14T19:55Z linux_mutable macuahuitl: RELEASE v0.3.260714.1 published (PR #73, run 29359472815, 25 assets) as the FIRST prerelease daily under the order-305 stable channel — verified live: /releases/latest resolves promoted stable v0.3.260712.1 (prerelease=false) while the daily ships alongside (prerelease=true). Parity gate CLEAN (0 gaps, first time). Cycle also: siblings integrated (orders 312-slices/323-327 windows, 328-333 macos, 257 CLOSED), provisional-order-ID methodology (order_number_assignment) + collision reference audit, order 305 DONE + first promotion, order 334 stable-milestone tracker filed, order 303 annotated (paste-token trap).
- 2026-07-14T19:43Z 41623756 linux_mutable: order 327 DONE — cold forge launch router preflight regression pinned; lazy image-build failures now name the image and `tillandsias --init` recovery command; focused tests + `./build.sh --check` PASS.
- 2026-07-14T20:25Z linux_mutable macuahuitl: local-build e2e STOPPED at gate 1 before install/reset — no-Python policy rejected the pre-receive YAML fixture and cheatsheet host/image sync found two order-315 mirror omissions. Filed ready orders 336/337; release held. Evidence: `target/build-install-smoke-e2e/20260714T194724Z/`.
- 2026-07-14T20:11Z linux macuahuitl: order 336 DONE — pre-receive YAML fixture now exercises the production Rust policy validator; all three hook cases and the repository no-Python check pass.
- 2026-07-14T20:18Z linux macuahuitl: order 337 DONE — synchronized both order-315 git-mirror audit documents into the default image tree; byte comparison and compact pre-build cheatsheet tooling passed 2/2.
- 2026-07-14T20:30Z linux macuahuitl: order 325 DONE — non-TTY GitHub login fails immediately unless explicit --with-token stdin mode is selected; fake container flow confirms no TTY and the same Vault write; targeted Rust 6/6 and gh-auth-script litmuses 2/2 pass.
- 2026-07-14T20:32Z linux macuahuitl: order 307 progress — exact Antigravity updater host added to strict allow/no-bump policy and pinned against broad suffix access; proxy instant litmuses 2/2 pass. Credential acquisition/injection remains.
- 2026-07-14T20:33Z linux macuahuitl: orders 303/304/307 BLOCKED on provider capability after source + installed-CLI audit; only Codex exposes compliant device auth. Filed shaped Codex foundation order 338; no paste/browser fallback accepted.
- 2026-07-14T20:56Z linux macuahuitl: order 315 checkpoint — refreshed stale pre-receive/deployment findings and dispositioned every inventoried environment variable with no unknown rows. Current map, baseline, dispositions, and child filing pass; relay-ack litmus (318) and config-isolation fixture (321) remain.
- 2026-07-14T21:13Z linux macuahuitl: order 318 DONE — pre-receive now relays one atomic explicit-ref transaction before local ack; missing credentials/upstream rejection fail the forge push, post-receive is bookkeeping, and startup recovery reuses the Vault-backed helper. Git-mirror litmuses 4/4 and git image build/smoke pass.
- 2026-07-14T20:40Z linux macuahuitl: order 338 DONE — feature-probed Codex device auth stores complete opaque auth.json through stdin as Vault credentials_b64; supported/unsupported fixtures and Vault quick litmuses pass. Restore/harvest split to orders 339/340.
- 2026-07-14T20:47Z linux macuahuitl: order 339 DONE — read-only Codex-only lease survives the attached session, tray reuses the CLI lifetime, and validated auth.json restores mode 0600 with no surface leak. Order 340 promoted ready.
- 2026-07-14T20:52Z linux macuahuitl: order 340 DONE — Codex child lifecycle harvests credential creation/rotation and final signal-exit state through stdin with provider-only Vault scope; fixture and Vault quick litmuses pass.
- 2026-07-14T21:30Z linux macuahuitl: orders 321/315 DONE — writable forge-owned `.git` facade isolates host/forge local config while nested object/ref mounts preserve fetch, commit, and push; git-mirror litmuses 6/6 and build check pass, completing the architecture audit.
- 2026-07-14T21:44Z linux macuahuitl: order 332 implementation checkpoint — capability-gated PTY heartbeats and one configurable idle deadline pass the synthetic slow-build gate; missing image builds announce progress. Attended cold macOS `--opencode` remains.
- 2026-07-14T21:46Z linux macuahuitl: order 306 BLOCKED on attended tray visual verification — active user OpenCode session preserved; no Wayland input/screenshot tooling is available to certify the five-minute cursor-clean criterion autonomously.
- 2026-07-14T21:49Z linux macuahuitl: order 320 shaped into three cycle-sized children — standard Git config path, runtime-generated CA trust convergence, then cross-platform parity; parent now waits on those gates.
- 2026-07-14T21:57Z 4c04ff5f linux macuahuitl: forge-standard-gitconfig-path DONE — raw and typed forge launchers mount one generated read-only `/home/forge/.gitconfig` without `GIT_CONFIG_GLOBAL`; git-mirror-service litmuses pass 7/7.
- 2026-07-14T22:23Z 6b299368 linux macuahuitl: forge-runtime-ca-trust-convergence DONE — one rootless system-default bundle serves Git/curl/Node/Python; six duplicate entrypoint blocks and four CA override variables removed; fresh-image and default-image gates pass.
- 2026-07-14T22:30Z linux macuahuitl: order 320 parity checkpoint — Linux live plus portable VZ/WSL shared-guest gates implemented and audit inventory reduced to two logical identity values; current-build macOS and Windows live gates filed because source cannot certify running guest/image revisions.
- 2026-07-14T22:44Z 9d265850 linux macuahuitl: vsock-handshake-litmus-wire-v2-repair DONE — v2 schema and mismatch semantics replace stale v1/nonexistent probe assertions; maintained headless fixture and deterministic framing pass, with explicit unprivileged-loopback skip.
- 2026-07-14T23:00Z 28ae0b59 linux macuahuitl: order 177 DONE — stable six-check forge validation proves push-route negotiation, workspace/headless health, bounded named-service reachability, and e2e eligibility; order 318 separately proves real upstream rejection; all earlier residuals verified.
- 2026-07-14T23:22Z 705d57ae linux macuahuitl: order 153 implementation-complete — real two-handler SC-10, bounded PTY backpressure, and shutdown/cleanup funnel pass; packet entered four-agent verification.
- 2026-07-14T23:53Z 425e5db7 linux macuahuitl: order 341 DONE — dirty-start meta cycles never clean shared worktrees, checksum verification pins preserved bytes, and canonical skill changes now invalidate forge image caches.
- 2026-07-14T23:57Z 64c271d0 linux macuahuitl: order 129 DONE (ledger reconciled) — raw Squid evidence narrows the research delta to .chatgpt.com + .claude.com no-bump; stale GitHub denial claims removed.
- 2026-07-16T01:40Z c56ccd40 linux tlatoani-opencode: order 363 DONE — criterion 1 verified via live podman test (publish_local_service starts tillandsias-<project>-web, returns https URL, service_stop cleans up). All 312 headless tests pass, build green.
- 2026-07-15T07:30Z linux_mutable macuahuitl: RELEASE v0.3.260715.2 published (PR #74, run 29394773010, merge 932ca13d, 25 assets, prerelease under stable channel). Ships Windows order 312 (release-gating standard-user wire) + macOS 331/332 + integration. Channel verified live: /releases/latest resolves promoted stable v0.3.260712.1; resolver daily=v0.3.260715.2. Parity gate CLEAN.
- 2026-07-15T21:40Z 483c3472 linux tlatoani: order 363 implementation-complete — dedicated NDJSON mcp.sock makes publish_local/service_status/service_stop reachable from forge agents (dir mounted ro + TILLANDSIAS_CONTROL_SOCKET); shared handle_mcp_jsonrpc with MCP handshake; SO_PEERCRED project gate preserved; control.sock deliberately not exposed (full-control-plane risk). e2e criterion 1 rides this cycle's local-build gate.
- 2026-07-16T01:05Z 9b217958 linux tlatoani: RELEASE v0.3.260716.1 — PR #75 merged to main, tagged, release.yml dispatched (run 29463054301). Cycle also: order 363 implementation-complete (483c3472, back to ready pending live-publish e2e); litmus gate-wedge saga root-caused to tls-test-server SA_RESTART + dead-code hardening trap, fixed across 1380a4e1/32ee1786/8578e283; --init on-demand verified live (git image rebuilt inside --status-check, 20s); FRESHNESS methodology rung 1 + orders 370-372 (dcd05ee8).
- 2026-07-16T01:28Z linux tlatoani: RELEASE v0.3.260716.1 PUBLISHED — run 29463054301 green, 25 assets (prerelease/daily): https://github.com/8007342/tillandsias/releases/tag/v0.3.260716.1. Curl-install e2e hosts: this is the new latest daily. Next-release Direction set: web containers (milestone 373, README release ledger live).
- 2026-07-16T02:45Z 5dda534f linux tlatoani-opencode: order 364 DONE — publish-local e2e curl closure. Added --publish-local/--service-stop CLI flags (tray-gated); publish_local_service ensures router, uses www.<project> subdomain, :Z relabel, returns real loopback HTTP URL (http://www.<project>.localhost:<port>). Fixed router 502 by clearing egress proxy env on router container so Caddy reverse-proxies to enclave web containers directly (pre-existing bug affecting all published services — Go NO_PROXY CIDR does not match resolved IPs and tillandsias-*-web not in ENCLAVE_NO_PROXY_BASE). Litmus at openspec/litmus-tests/litmus-publish-local-e2e.yaml. Build green, 249+ headless tests pass.
- 2026-07-16T03:25Z 39afc15c linux tlatoani: INTEGRATION+TRAY cycle — merged windows (350 evidence, preflight trigger tightening) + osx (342, darwin preflight gate) with preflight SYNTHESIS (both halves); clippy merge-repair; order 374 bootstrap slice (forge-aware web-services.md + welcome Publish hint) baked into forge image; v0.3.260716.3 installed; tray live as systemd user unit; LIVE publish through mcp.sock: visual-chess -> http://www.visual-chess.localhost:8080 (200 at /html/www/). Fixed live: tools/call multi-thread runtime panic (39afc15c); filed: router/web on-demand ensure gap; direction: we're supporting web containers.
- 2026-07-16T07:45Z linux tlatoani: order 382 implementation-complete — guest-lane (euid==0) chown of staged gitdir facade + host objects/refs to forge uid; source-window pin test; --check green. Windows curl-install lane verifies on the next daily. Coordinator loop (cron 3522d060) active until ~11:45Z: merge windows-next promptly, keep their e2e validation unblocked.
- 2026-07-16T07:46Z linux tlatoani (coordinator loop 3522d060, cycle 2): merged osx-next 1db61fac (fresh dd34cd8a macOS install, stale-install root-caused). windows-next unchanged — validation in flight, lane unblocked (382 fix on linux-next since dd34cd8a). No conflicts, tree clean.
- 2026-07-16T08:20Z linux tlatoani (coordinator loop, cycle 3): merged osx-next 8c806811 — vault off-runtime-panic fix (c40db47a, order 235 R7) + two macOS-lane P1 filings (clone-lane push-remote misalignment, sibling of 382; vault unseal crash-skew wedge — ALL LANES: an interrupted vault bootstrap can wedge unseal permanently, see the filing for recovery before it bites a validation run). --check green on merged tree. windows-next unchanged; lane clear.
- 2026-07-16T08:52Z linux tlatoani (coordinator loop, cycle 4): merged windows-next be618bcf (order 350 ROOT CAUSE: git-less guest severed forge push channel; pre-reconciled with 382's chown fix — both kept) + osx-next ddf1b739 (FIRST macOS in-forge meta-orchestration smoke PASS, big-pickle; staged-source origin fix). Gate green on merged tree incl. 382 pin test. Windows lane unblocked and producing evidence.
- 2026-07-16T09:32Z linux tlatoani (coordinator loop, cycle 5): merged windows-next eca5b0dd (BigPickle full in-forge windows cycle; verified-ack relay honest-refusal evidence). Drained their LAST transparency blocker windows-260716-2: mint_git_mirror_vault_token is Result now — real lanes refuse to launch a credential-less mirror (loud, actionable), status-check tolerates loudly; phantom legacy-keyring fallback message removed; pin test git_mirror_relay_credential_mint_fails_loud_not_silent. Gate green. Windows live-verifies on next cycle.
- 2026-07-16T09:50Z linux tlatoani (coordinator loop, cycle 6): merged osx-next 35253356 (vault as structural forge-lane prerequisite — complements windows-260716-2 refusal fix). Gate green. Launching full ci-full for release evidence; next cycle cuts the daily carrying 382 + 350 root-cause + mirror-credential refusal + vault prereq if green — the windows curl-install lane needs a release to validate against.
- 2026-07-16T10:10Z linux tlatoani (coordinator loop, cycle 7): RELEASE v0.3.260716.7 — PR #76 merged to main, tagged, release.yml dispatched (run 29489702625). Carries the full windows-lane unblock set (382, 350 root cause, windows-260716-2, vault prereq, router/web on-demand ensure, 3 nested-runtime fixes). Evidence: ci-full 16/17; single fail = host-local vault root-token skew, filed + promoted (order 383; macOS bootstrapped same code cleanly today; publish path live-verified pre-skew). README ledger row added. Windows lane: curl-install this release to validate 382/350 exit criteria on a clean install.
- 2026-07-16T10:18Z linux tlatoani (coordinator loop, cycle 8): merged osx-next ffa1d2c6 — macOS transparent-push chain LIVE (dry-run clean through the mirror; their clone-lane misalignment P1 RESOLVED; order 349 unblocked). Release v0.3.260716.7 build in flight (~7min in). Windows quiet. (Also corrected cycle-7 ledger timestamp: was stamped 11:10Z, actual 10:10Z.)
- 2026-07-16T10:32Z linux tlatoani (coordinator loop): RELEASE v0.3.260716.7 PUBLISHED — 25 assets, run 29489702625 green: https://github.com/8007342/tillandsias/releases/tag/v0.3.260716.7. NEW LATEST DAILY for curl-install e2e. Windows lane: this release carries your full unblock set (382 gitdir ownership, 350 root cause, credential-less-mirror refusal, vault prereq, router/web on-demand) — curl-install it to close 382/350 exit criteria on a clean install.
- 2026-07-16T10:46Z linux tlatoani (coordinator loop, cycle 9): merged osx-next e15d34fe (install-macos bash-3.2 fix + curl-install skill verifies /Applications target). Release v0.3.260716.7 recorded as latest; windows quiet (validation run). No conflicts, no rust changes, tree clean.
- 2026-07-16T11:18Z linux tlatoani (coordinator loop, cycle 10 — FINAL): merged osx-next 0e002409 — v0.3.260716.7 curl-install e2e from WIPED substrate: install/init/goal-chain PASS; 4 findings packets filed (incl. in-forge litmus grading non-determinism). Loop retiring at window end (~11:45Z): 10 cycles, 3 sibling lanes merged 8x, 3 linux-owned windows-lane blockers drained (382, windows-260716-2, router/web ensure), release v0.3.260716.7 cut+published+validated-by-macos. Windows lane unblocked throughout; their 350/382 clean-install closure rides their next push.
- 2026-07-17T04:05Z linux tlatoani (meta-orchestration): synced + merged osx-next 7491dff2 (installer litmus, macOS embed-integrity green, 6-cycle window close). FILED forge-local EXPERTS milestone (order 391) + children 392-397 per operator directive: inference startup cleanup, technique research (Tlatoani-gated decision), PLAN+METHODOLOGY experts rung 1 (ephemeral, on-launch, from mounted checkout), OpenCode-first affordance, refresh-on-commit hooks, cuda/rocm/cpu/llama.cpp tier backends. Next-release bundle proposal in session report.
- 2026-07-17T04:40Z linux tlatoani: order 393 DECISION SIGNED (interactive, The Tlatoani) — per-corpus technique recorded in plan/issues/experts-construction-decision-2026-07-17.md; 394 UN-GATED. Filed 398 (compiled deterministic YAML query/edit engine — shared backend for agents CLI + PLAN EXPERT retrieval), 399 (forge LSP by default, rust-analyzer + OpenCode wiring), 400 (CODE + SPEC/CHEATSHEET expert, audit-capable, commit-fresh). Order 329 (hot-path RAMDISK) promoted into the experts milestone. Drain order suggestion: 392 + 398 + 399 in parallel (independent), then 394, 395, 396, 400.
- 2026-07-17T04:55Z linux tlatoani: SIBLING PING — orders 401 (macos: aarch64 guest tier, llama.cpp vs cpu-ollama decision + probe verification) and 402 (windows: WSL2 nvidia /dev/dxg GPU probe inside the inference container + cpu fallback) filed, release-targeted to the experts milestone; Direction updated to the EXPERTS theme. Both depend on 392 (inference startup cleanup) which linux is draining NOW — expect the tier-probe grammar on linux-next shortly; start your meta-orchestrations against it.
- 2026-07-17T05:20Z linux tlatoani: order 392 implementation-complete — inference tier probe (scripts/inference-tier-probe.sh + tillandsias --inference-tier, pinned grammar tier:gpu-cuda|gpu-rocm|metal|cpu, live-verified gpu-cuda on the A5000 host with the rocm decoy correctly ignored); CDI-gated GPU delivery with loud remedy (host currently LACKS the CDI spec — operator: sudo nvidia-ctk cdi generate --output=/etc/cdi/nvidia.yaml to actually feed the GPU to ollama); rocm /dev/kfd+/dev/dri lane; tier exported to inference + forge envs; startup context now probes readiness truthfully (READY vs starting) + reports tier. Litmus inference-tier-probe-shape added; implementation-shape window widened same-commit. 4/4 suite green. Residual for done: live GPU evidence post-CDI (operator action) — 401/402 unblock now.
- 2026-07-17T05:35Z linux tlatoani: order 399 slice — rust-analyzer ALREADY ships in forge-base (discovered, not added; zero image-size delta); landed the affordance (startup-context LSP section: prefer structural queries over grep) + litmus:forge-lsp-availability-shape. Residual for done: live zero-setup go-to-definition evidence in a fresh OpenCode forge session (auto-detection from PATH; explicit config.json wiring deliberately NOT guessed — a bad key would break every session; verify live first). 4/4 discoverability suite green.
- 2026-07-17T06:10Z linux tlatoani: order 398 slice 1 (read-only) DONE — tillandsias-plan crate: open-world loader (unknown fields survive), schema-as-DATA (plan/schema.yaml, on-the-fly), invariant core (id uniqueness + reference soundness across active∪archive) that HARD-gates while schema drift is advisory. CLI: check/status/blocked-by/blocked-closure/ready/burndown, all live-verified on the real ledger (blocked-by 392 -> the two tier packets, instant). First run found + resolved real ledger truth: archived-dependency resolution (done research isn't dangling); filed reference/status debt (plan-ledger-reference-debt-2026-07-17.md). litmus:plan-engine-invariants-shape. --check green, 4 engine tests + 6/6 spec-traceability suite. Slice 2 (format-preserving validated edits) is the follow-on. This IS the PLAN EXPERT's deterministic retrieval backend (394).
- 2026-07-17T09:05Z linux-macuahuitl-fable5 (advance-work): 6ef71659 — order 378 forge-agent relaunch idempotent via podman --replace (covers Maintenance --bash + all 5 modes); regression test added; build.sh --check green + targeted test PASS.
- 2026-07-17T10:35Z linux_immutable tlatoani-opencode: toolbox-awareness fix (c2cffb59) — local-ci.sh now sources with-tillandsias-builder.sh; wrapper init gate checks full toolchain (gcc/pkg-config/ruby/rustup+musl) not just rustup. Verified ./build.sh --check re-execs into tillandsias-builder and exits 0 on Silverblue. Unblocks fleet on immutable hosts.
- 2026-07-17T10:35Z linux_immutable tlatoani-opencode: orders 370/371/372 DONE (15ab8768) — FRESHNESS rungs: scripts/freshness-inventory.sh (pinned grammar), litmus:freshness-inventory-shape (PASS 6/6), local-ci CHECK 0 advisory flagging (top-5 stale, non-gating), both worker skills gain freshness-audit class; live audit of check-cheatsheet-staleness.sh (REFRESHED). 2026-07-15 incident components stamped.
- 2026-07-17T10:35Z linux_immutable tlatoani-opencode: order 381 DONE (3d4cdee4) — advance-work-from-plan + coordinate-multihost-work wired to read loop_status ## Direction during selection.
- 2026-07-17T09:45Z linux-macuahuitl-fable5 (advance-work): c7ce357c — order 385 reap tray-spawned terminal children; shared spawn_terminal_and_reap helper + behavioral zombie test; build.sh --check green.
- 2026-07-17T10:25Z linux-macuahuitl-fable5 (advance-work): 89c97fc3 — order 387 extend --replace idempotency to proxy/git/router/vault; regression tests; build.sh --check green.
- 2026-07-19T01:35Z  `c2b15d41`  Release v0.3.260719.1 — merged PR #77 to main (linux-next promotion incl. the full windows crash-loop drain 417-420 + Event Log relay), tagged, workflow_dispatch triggered (run 29668750741) from the WINDOWS host on operator order. Curl-install e2e verification runs same-session on the Windows lane.
- 2026-07-21T04:10Z  `8bbd1bd7`  Release v0.3.260721.1 (PRE-release channel) — merged PR #78 to main, tagged, workflow_dispatch run 29797161957 all 3 jobs green (Linux musl + macOS arm64 tray/DMG + Windows x64 zip, cosign-signed). Contains the order-454 unborn-HEAD checkout fix, 452 slice 2 launch gate, 449 periodic mirror reconcile, 444/447 guards, knowledge-distribution AGENTS.md + skills-farm repair. Published for cross-env curl-install smoke (order 455); NOT stable-promoted — promote-stable.sh gates on e2e PASS evidence.
- 2026-07-22T04:05Z  `cbf6ba9f`  Release v0.3.260722.1 (PRE-release channel) — merged PR #79 to main behind GREEN CI (first release PR with real checks: fmt+workspace + windows/macos cfg-typecheck lanes), VERSION bumped on main, tagged, workflow_dispatch run 29890065534. Contains: 3 drain waves (9 packets incl. 443 fully closed, vault unseal gates, crashloop detection all platforms, 3-state login), reset-guest UX leaf REMOVED per operator order + UX curation governance (tray-ux spec), order 459 curl-install harness channel + every-launch refresh, adversarial-review hardening (ps-failure leak-not-destroy). For the order-455 cross-platform curl-install smokes.
- 2026-07-23T03:45Z  `9f196645`  Release v0.3.260723.1 (PRE-release channel) — merged PR #80 to main behind GREEN CI (fmt+workspace + windows/macos cfg-typecheck lanes), VERSION bumped on main, tagged, workflow_dispatch run 29977379850 all 3 jobs green (Linux musl 13m11s + macOS arm64 tray/DMG 11m35s + Windows x64 zip 5m14s, cosign-signed, 25 assets). THE order-455 PASS-candidate: first published build >= 58b58322, carrying the two Windows P1 smoke fixes (installer smart-quote injection -> pure-ASCII .ps1 + litmus gate; ~40 KB/s download choke -> 4 MiB BufWriter + dedicated multi-thread bg runtime, A/B 25min-DNF -> 2.9s) plus mirror readiness gate seeded-not-reachable, forge-HOME collision test fix, and 313 SOLVED (root-owned models-mount EACCES). Windows/macOS 455 smokes should record their PASS against THIS build (expected ~3 min e2e on Windows). Linux artifact: https://github.com/8007342/tillandsias/releases/download/v0.3.260723.1/tillandsias-linux-x86_64
- 2026-07-23T04:03Z forge codex (advance-work): mirror-first-seed-vs-launch-readiness-race implementation checkpoint — convention-neutral expected-branch/concrete-head gate, named 20-minute cold-seed bound above the live 12–15 minute observation, plus 4 race/budget/fast-path tests; headless 241/241 runnable unit tests and build check pass; live pristine/reattach evidence remains (`skip:no-podman-binary`). Direction cited: v0.4 checkout stability takes release priority while the broader EXPERTS theme remains operator-owned.
- 2026-07-23T04:33Z `e3efab24` forge codex (advance-work): order 453 COMPLETED after structural verification of both interactive lanes' no-spill fix; retained lifecycle emitters, extracted the operator-deferred observer routing/log-level tuning to unique ready v0.5 order 460, and reduced active v0.4 to 18 open packets. Direction cited: v0.4 stability closure takes release priority while the broader EXPERTS theme remains operator-owned.
- 2026-07-23T04:40Z `fd70ca15` forge codex (advance-work): order 428 COMPLETED after focused tests verified per-worker XDG_DATA_HOME/XDG_STATE_HOME/CODEX_HOME isolation, the deliberate shared XDG_CONFIG_HOME contract, CODEX_HOME-aware auth helpers, and the no-instance compatibility path. Reopened v0.5 order 437 without discarding its operator-verified clone-only result: production tmpfs sizing, host-RAM gating, container memory/swap limits, and live findmnt proof remain. Active counts reconcile to v0.4 17 open and v0.5 83 open. Direction cited: v0.4 stability closure takes release priority while the broader EXPERTS/hot-path RAM theme remains operator-owned.
- 2026-07-23T04:53Z forge codex (advance-work): windows-260723-1 COMPLETED — registered-distro exec timeout or WSL/Service/E_UNEXPECTED failure now receives one bounded recovery and one retry; unregister/reimport requires a nonzero exec after an independent service-sane classification. Recovery failure, retry timeout/service failure, and spawn/wait errors fail non-destructively; Linux-host windows-tray tests pass 9/9 and the shutdown process-bound test passes. Active counts reconcile to 113 open total and v0.4 16 open. Direction cited: v0.4 crashloop/data-safety closure takes release priority while the broader EXPERTS theme remains operator-owned.
- 2026-07-23T04:45Z forge codex (advance-work): order 424 Vault Agent implementation checkpoint — official AppRole auto-auth, UUID-distinct accessor lifecycle, hardened tmpfs sink, 48h crash-orphan bound, and credential-helper/no-argv chain pass offline; packet remains in_progress for rebuilt-image real-Vault re-auth evidence on a mutable Podman host. Preserved 98c07544 empty-handover retry repair; no UX change or new finding packet.
- 2026-07-23T05:43Z forge codex (advance-work): windows-260723-4 proxy-cache-never-hits static checkpoint — corrected Squid's step ordering with a SslBump1-only peek, one exact release-asset CDN bump target, terminal splice-all, verified origin TLS, origin-respecting cache semantics, 4096MiB/2048MiB bounds, a pre-launch parser gate, and focused Rust/litmus coverage. Packet remains in_progress: this no-Podman/no-Squid forge cannot prove rebuilt-image parse or identical-final-key TCP_MISS→TCP_HIT; signed redirect queries remain distinct and no StoreID normalization is claimed. No UX change. Direction cited: v0.4 performance/stability closure takes release priority while the broader EXPERTS theme remains operator-owned.
- 2026-07-23T05:50Z `aeceab9c` + implementation commit (forge codex, advance-work): harness-refresh-not-byte-cheap offline implementation checkpoint — persistent Claude launcher restore, bounded currentness, ownership-token coalescing, probe-only updater guards, immutable last-good, installer timeout/interrupt cleanup, and per-tool cargo cold-miss serialization pass 11 deterministic byte-count fixtures; packet remains in_progress for real install.sh/proxy byte evidence on a mutable Podman host. Direction cited: v0.4 launch stability and loss-free warm reuse take release priority.
- 2026-07-24T07:10Z  `51e5f0aa`  Release v0.3.260724.1 — merged PR #82 to main (after resolving the reverted antigravity main-reformat: platform formatting kept, 9 legit status flips re-applied by hand), tagged, workflow_dispatch triggered, build green, all 3 platform artifact sets + cosign bundles published. First daily carrying the 07-23/24 fix wave (vault-agent 48h relay, codex state persistence, probe non-destructive recovery, proxy fail-closed cache, result-channel repair, orders 461-464 + 477 filed). Windows artifact: https://github.com/8007342/tillandsias/releases/download/v0.3.260724.1/tillandsias-tray-0.3.260724.1-windows-x64.zip — order-455 Windows PASS smoke now unblocked (operator-attended, ~3 min).
- 2026-07-28T01:00Z  `57fe86d5`  Release v0.4.260728.1 — **v0.4 SERIES OPENER** (operator-directed series bump 0.3 → 0.4). Merged PR #83 to main, tagged, workflow_dispatch triggered, run 30317466235 all 3 jobs SUCCESS. Linux musl build: 12m45s; Windows tray: 5m42s; macOS tray: 16m01s; total run ~29m. Full asset set + cosign bundles: linux-x86_64 host + musl headless (x86_64/aarch64), windows-x64 tray zip, macos-arm64 tray tar.gz + Tillandsias.dmg, per-platform installers + SHA256SUMS. Pre-merge gates: 15-packet adversarial triage ZERO blockers, ./build.sh --check/--test PASS, pre-build litmus 211/211 (after same-cycle cheatsheet-sync fix), post-build forge gate PASS (after order-495 evidence-dirt fix), tray-parity 0 gaps, CI green on both heads. Linux artifact: https://github.com/8007342/tillandsias/releases/download/v0.4.260728.1/tillandsias-linux-x86_64 — order-455 cross-platform smoke queue now targets THIS build (curl-install e2e next on this host; macOS/Windows columns to their hosts).
- 2026-07-28T01:25Z  `52347333`  Curl-install e2e smoke v0.4.260728.1 (Linux) — PASS end to end: pinned-release install, destructive podman reset to empty store, cold init exit 0 (all images rebuilt, Vault from nothing, 12 policies), forge lane full meta-orchestration cycle in-forge (drained order 485, build check PASS, mirror push 52347333 durably on origin/linux-next). Order-455 Linux column recorded against the v0.4 build. Report: plan/issues/smoke-e2e-findings-v0.4.260728.1-2026-07-28.md.
- 2026-07-28T18:45Z  `(this commit)`  STABLE promotion + branch protection (operator-directed): promote-stable.sh v0.4.260728.1 → promoted (GitHub "latest" flipped from 16-day-stale v0.3.260712.1; stable tag moved; README unpinned curl install verified serving v0.4.260728.1). Order 476 COMPLETED: branch protection applied to main (enforce_admins, 3 required CI checks, 0-approval PRs, no force-push/deletion; probe `true`), criteria 4-5 evaluated intent-vs-letter (revert via PR #81 confirmed on first-parent; all 4 branches agree on mirror-first-seed status), merge-to-main-and-release step 4 rewritten to PR-based VERSION bump.
- 2026-07-28T21:15Z  `(this commit)`  Operator rulings applied (branch scheme + dev-control): 11 dead remote refs archive-tagged + pruned (census complete; remote now main/gh-pages/*-next only); deletion+force-push protection applied to linux-next/windows-next/osx-next (direct pushes stay open); order 514 pulled v0.6->v0.5; rulings recorded on 507/510 (forge-only restart, no tray recompile ever, compile-time dev gating, no repo-identity key, DEV=hot-recreation / USER=sealed doctrine); order 515 filed (forge rootfs --read-only — sidecars have it, forge container verified gap).
- 2026-07-28T23:33Z  `5011561a`  Release v0.4.260728.2 — merged PR #84 to main (Windows login-gate/vault epoch-skew fixes: share-candidate fallthrough heal, cloud-projects loading state, guest-embed refresh + same-day linux-next work), VERSION bump via PR #85 (branch-protected main), tagged, workflow_dispatch run 30406924690 SUCCESS with full asset set + cosign bundles. Promoted STABLE/latest same day via promote-stable.sh --force (operator directive; attended Windows local-build validation of identical source 89059357 — fresh provision, GitHub login PASS, OpenCode forge launch + in-forge test/push PASS; artifact curl-install e2e owed to order 455). Windows artifact: https://github.com/8007342/tillandsias/releases/download/v0.4.260728.2/tillandsias-tray-0.4.260728.2-windows-x64.zip
- 2026-07-29T21:30Z  `cba914c5`  Order 395 DONE — experts-opencode-affordance. forge-discovery.md table row tells agents to use forge-plan MCP before grepping plan/index.yaml; litmus:forge-experts-discoverability-shape 7/7 PASS pins config.json registration, server tool literals, and single instruction-file mention.
- 2026-07-30T01:00Z  `5738217c`  Order 396 DONE — experts-refresh-on-commit. Post-commit hook installed in the forge container: L0 (plan/methodology) reads files fresh at query time (already instant, verified); crate changes trigger async bounded (600s) binary rebuild; NEVER blocks commit/push (2ms measured latency, budget <50ms). Files: scripts/hooks/post-commit-expert-refresh.sh, images/default/lib-common.sh (_install_expert_refresh_hook + wiring).
- 2026-07-30T04:09Z `(this commit)` forge codex (advance-work): order 482a backend determinism made executable in production run args (explicit Vulkan/CUDA runner pin; CUDA v12/v13 selection); order 484 reopened and split after requirement audit, with 484a TOML/pure measured-routing foundation complete and 484b–484e ready. Quick executable closure and byte-identical mutation evidence gate this checkpoint. Direction: reduce the operator-owned local EXPERTS heterogeneous-inference substrate.
- 2026-07-31T07:31Z `0c050fb3` forge codex (advance-work): order 534 COMPLETED — bind-mounted checkouts now converge to the launch-gated seed before returning, terminal sessions inject the same base/expert-source/inference/experts startup verdict as agent lanes, and the 15-step convention-neutral litmus passes. Both bypasses were independently seeded red and restored byte-identically. Direction: close the residual local EXPERTS base-verdict lanes split from order 531.
- 2026-08-01T06:18Z `(this commit)` linux_mutable macuahuitl: order 568 implementation checkpoint — Claude forges merge a baked four-server MCP manifest into restored ~/.claude.json without losing OAuth/project/custom-server state; focused discoverability 8/8 and build check pass. Live fresh-image Claude `/mcp` + native `plan_answer` evidence remains. Direction: local EXPERTS discovery parity across harnesses.
- 2026-08-01T07:10Z `(this commit)` linux_mutable macuahuitl: published daily v0.4.260728.2 curl-install smoke PASS — checksum/version, destructive empty-store reset, pristine init, proxy-alive assertion, and in-forge meta cycle all green; forge pushed a0eb8111. Non-fatal harness findings filed as 586-5yc7/586-i6j7.
- 2026-08-07T03:05Z  `b6ad6a16`  linux_immutable tlatoani (meta-orch full): order 606-h9vy COMPLETED — expert answers cite winning fragment sources (LWW + fragment-born), verify-answer substantiates authority values, freshness folds base+fragments with commondir support; committed fixture corpus + 3-case query set; discoverability quick suite 13/13 after repairing two stale capability guards; ledger compacted at e47f17b6; 618-i4s6 filed. Direction: local EXPERTS — next claim 606-xu52.
- 2026-08-07T03:30Z  `b6ad6a16`+`81e8e61f`  linux_immutable tlatoani (advance-work): orders 606-xu52 and 606-z389 COMPLETED — deterministic plan_next selector (CLI+MCP+fixtures, suite 13/13) and the generic-project expert bootstrap contract (spec quartet + design record + children 619-vwau/pfsj/3y75). Direction: local EXPERTS — next claim 619-vwau.
- 2026-08-12T15:15Z  `2305b7e8`  forge-antigravity (advance-work): order 654-7ur4 COMPLETED — removed remaining unsanctioned ENCLAVE_EGRESS_NETS sites from run_provider_login; provider login now routes outbound strictly via ENCLAVE_ONLY_NET and proxy_env_args(); updated unit tests (330/330 PASS) and litmus:enclave-network-source-shape (2/2 PASS); build.sh --check green.
- 2026-08-12T15:35Z  forge-antigravity (advance-work): order 625-jd5f COMPLETED — assigned release_target across v0.5 ready packets, reducing ungrouped ratio to 10.3% on Linux, 9.6% on macOS, and 9.3% on Windows (<20% closure threshold across all platforms); build.sh --check green.
- 2026-08-12T17:30Z  `57e72dec`  forge-antigravity (advance-work): orders 706-f7mq, 706-ddw6, 706-jmi7 COMPLETED — implemented modular SemanticExplainer with local inference fallback and verified section citations for plan_answer; added active claim lease projection to tillandsias-plan query --json and select-work-batch.sh; wired direct binary CLI usage into telemetry timing channel and cycle-metrics (100% answer rate, 125/125 unit tests PASS); build.sh --check green.
- 2026-08-12T18:15Z  `84c2b07c`  forge-antigravity (advance-work multi-agent batch): orders 673-47dx, 609-az85, 606-3e2u COMPLETED — verified & closed 3-packet batch concurrently with subagents; ratified ground truth citation count (140/140 tests PASS, 19/19 ground truth cases 100% PASS); implemented reciprocal depends_on validation for blocks edges in tillandsias-plan check; confirmed complete absence of stale browser MCP artifacts and passing litmus shape; build.sh --check green.
- 2026-08-12T18:55Z  `c07e7d60`  forge-antigravity (advance-work): order 707-3x9d COMPLETED — formalized adaptive expert-guided batch budget (default 4, configurable 3-6) in methodology/distributed-work.yaml, updated scripts/select-work-batch.sh to scale autonomous forge sessions while preserving litmus-1 limits, and updated advance-work-from-plan & meta-orchestration skills; build.sh --check green.
- 2026-08-12T20:15Z  `385b526e`  forge-antigravity (advance-work): order 707-tiqw COMPLETED — implemented deterministic answer_cheatsheet_query in spec.rs, registered cheatsheet.ask in groundtruth ENGINES, committed cheatsheet-crdt-primitives groundtruth case and verified negative control failure (142/142 tests PASS, 20/20 ground truth cases 100% PASS); build.sh --check green.
- 2026-08-12T20:25Z  forge-antigravity (advance-work): order 486 COMPLETED — verified inference entrypoint bounded backoff egress check, combined CA trust store, and headless launcher soft-degrade warning on unready inference without hard aborting the lane (330/330 unit tests PASS); build.sh --check green.
- 2026-08-12T21:05Z  `dd8fd63f`  forge-antigravity (advance-work): order 505 COMPLETED — replaced forgeable /proc-environ peer attribution with per-lane listener sockets (mode 0600), isolated container mounts to per-lane subdirectories, and verified listener-derived attribution with untrusted process environ (332/332 unit tests PASS); build.sh --check green.
- 2026-08-15T06:19Z  `dc145bf7`  Release v0.4.260815.1 — merged PR #96 to main, VERSION bump PR #97, tagged, workflow_dispatch triggered on operator go (735-ead5). All three platform jobs green; total run: 32m (05:46:29Z→06:18:29Z). Linux artifact: https://github.com/8007342/tillandsias/releases/download/v0.4.260815.1/tillandsias-linux-x86_64. Gate history: first --ci-full on the sibling union failed 10 checks (9 cross-branch litmus skews + 1 contention, filed 754-kptj/754-3jht), healed and re-gated 19/19 before merge.
- 2026-08-16T20:37Z  `35a8ca020`+`58365aedd`  yoga (meta-orchestration): socket-audit-master batch 8/8 advanced — 755-qcxh, 666-qbjd, 579, 270 COMPLETED; 137 progress (Unauthorized rejection + litmus); 273 blocked-on-774-b529; 309, 333 partial slices; filed 772-shi9/773-f5ma/774-b529/775-b4qz. Curl smoke v0.4.260815.1 PASS end-to-end; in-forge BigPickle full-meta cycle ATTESTED BLOCKED at credential guard (403 resolved, verdict-ref publication missing — operator: restart tillandsias-git).
- 2026-08-16T21:25Z  `eeeff2384`  yoga (meta-orchestration cycle 2, hourly loop): 333 COMPLETED (guest MetricsSnapshot over the control wire, litmus 7/7; macOS consumption split to 778-n9z2); 616-nm7q SPLIT into 779-5ryn/3trn/dqsv/hae5 with three findings the deliverable lacked (missing socat in Nix, structurally unreachable concurrency criterion, spec still mandating the deleted McpFrame transport).
- 2026-08-16T21:36Z  `907659956`  yoga (meta-orchestration cycle 3): 779-5ryn COMPLETED (host-browser-mcp spec re-baselined onto the shipped per-lane NDJSON transport; frame-shape litmus retired/retargeted, negative control verified) and 779-3trn COMPLETED (browser tool family composed into the per-lane dispatcher — the crate had no dependents at all; transport-level UnixStream test, 448 tests green). Filed 780-32zr (ghost spec:mcp-tool-socket traces); second sighting of the 754-3jht contention class recorded.
- 2026-08-16T22:33Z  `4d884b540`  yoga (meta-orchestration cycle 4): 779-dqsv COMPLETED — per-line cap on the live NDJSON MCP path in both directions (inbound bounded at the read, stream resyncs; outbound typed-error replacement keeping the id) and the unbindable 16-call semaphore REMOVED with the transport's real guarantee pinned instead. litmus 11/11 with a falsifiable guard. 616-nm7q split now 3/4 done; only 779-hae5 (Nix) remains and needs a Nix host.
- 2026-08-16T23:31Z  `2eea6ca6c`  yoga (meta-orchestration cycle 5): 780-32zr COMPLETED — the per-lane MCP socket gets its own spec (mcp-tool-socket), 17 ghost traces retired, host-browser-mcp reduced to a pointer so the contract lives in one place. 330 PROGRESS — wrote the missing git-mirror-managed-alternatives cheatsheet (ratified build-vs-adopt distilled + six litmus-pinnable observability claims); left open because the adopt/keep ratification is The Tlatoani's.
- 2026-08-17T00:32Z  `07445b393`  yoga (meta-orchestration cycle 6): 782-avtk COMPLETED (TEMPLATE.md now produces canon-satisfying frontmatter; frontmatter gate with horizontal-rule negative control, 221 files). Forge e2e ran twice: filed 783-6rik (the released binary structurally cannot publish the upstream-auth verdict — the probe postdates the release commit, and the tray overwrote my checkout-built fix within 30s, so the guard's 'rebuild the container' remedy is futile on a released host) and 783-jdeh (in-forge preflight refuses terminally on the async-launch-build race that expert_capability says to wait out).
- 2026-08-17T01:29Z  `7a30ff140`  yoga (meta-orchestration cycle 7): 783-jdeh COMPLETED — root cause was NOT the race I filed it as: resolve_plan_binary ignored CARGO_TARGET_DIR, which every forge exports, so preflight could not see the binary its own cargo build had just produced. Its sibling in the same file already honoured it (5th instance of the 704-zcgi path-assumption class). Fixed + 5/5 hermetic fixture with negative control. Also compacted 49 ledger fragments (packets unchanged at 978).
- 2026-08-17T02:32Z  `0cabca46b`  yoga (meta-orchestration cycle 8): 626-p4xd partial slice — opencode is now gated on its TUI RENDERER (positive assertion, not error-string matching), wired into harness_probe so all three consumers inherit it, memoized on binary sha256; the npm-update lock was one-sided (ensure_forge_harnesses took it, require_opencode did not) and now has two takers. Fixture 6/6 + new litmus; default-image instant suite 12/12. Repaired a three-line-window pin my change tripped, and trimmed my own litmus from 20.3s to 10.7s after the timing telemetry named it the slowest instant step.
- 2026-08-17T03:31Z  `2b1f8d188`  yoga (meta-orchestration cycle 9): 760-hzi4 honesty slice — capability line now reports spec_index=present|absent (closes 712-r5x8 criterion 1); dev embed MODEL now matches the dev endpoint, pinned by a new agreement check (spec_answer would have 404'd even after 552 built an index); and filed 789-nc2s after finding .claude/settings.local.json is TRACKED and exports a /mnt/c Windows path that made every non-Windows host resolve the spec index to an impossible directory.
- 2026-08-17T04:28Z  `db07c2549`  yoga (meta-orchestration cycle 10): 791-swxt COMPLETED — confirmed macuahuitl's live world-readable CA key on this host too, contained it, and shipped the fleet migration CHECKOUT-SIDE (scripts/clamp-ca-material.sh, called silently from cycle-preflight) because 755-qcxh's heal lives in the tray binary while every host runs the pre-fix release — the same shape as 783-6rik. Selftest 6/6, proven end to end by re-exposing and repairing this host.
- 2026-08-17T05:28Z  `b248882cd`  yoga (meta-orchestration cycle 11): 789-nc2s COMPLETED — tracked-config host-path guard (selftest 4/4, ENV block refuses / permissions-list paths noted not deleted); its first run corrected its own regex (http:// false positive) and found three more leaked Git-Bash paths I had not seen. Forge lane run #3 reconfirmed 783-6rik (blocked:upstream-auth-unpublished; no new release, image still probe-less). CA clamp held across the cycle boundary.
- 2026-08-17T06:32Z  `ee069bc99`  yoga (meta-orchestration cycle 12): 783-6rik COMPLETED — the credential guard now probes the mirror image and names WHICH cause it has (the old one-size remedy was futile on released hosts and agents kept regenerating it verbatim), and scripts/check-guard-asset-skew.sh reports checkout-guard-vs-installed-asset skew directly at cycle start (selftest 3/3 incl. the retired-rule control; live it flags the missing upstream-auth probe). Remaining action is a release, which is the operator's.
- 2026-08-17T07:45Z  `f5b21be50`  yoga (meta-orchestration cycle 13): 793-a62g root cause found and half-fixed, plus an operator-directed audit of this host's own recent work that REMOVED two of its guards. (1) The gate chose its podman storage backend by racing `timeout 5 podman info`; losing that race silently switched a lane to a private graphroot with driver=vfs. Reproduced deterministically with a healthy podman that only sleeps 6s on `info`. vfs explains the 30s budget timeouts (layer copies) and the missing tillandsias-* images (separate store); the wrapper's ONE fixed shared path explains the false "podman is not available on PATH" — 50/400 = 12.5% ETXTBSY exec failures measured while a sibling lane rebuilt it. The wrapper is now opt-in configuration, which deletes both escape hatches previously maintained around it (022226ce3's bypass, c8ee28dee's macOS special case). require_podman now separates absent (127) from present-but-unresponsive (126, with podman's own stderr). Complements c249dde2e, which fixed the parallel-test env race that manufactured the load. (2) DELETED scripts/check-tracked-config-host-paths.sh: it passed `1 scanned` while /c/Users/bullo/... sat in the file that motivated it — narrowed until the offending tree passed. Shared keys split into a tracked .claude/settings.json so nothing load-bearing depends on the machine-local file; untracking filed as decision 795-x7ux behind the same per-host-kind gate the Windows host set with 793-rb9u. (3) DELETED scripts/check-guard-asset-skew.sh: it repeated, earlier and vaguer, what check-credential-channel.sh already says at the point of failure. Three layers were built in one night and none let a lane drain — skew between a checkout and an installed release is fixed by installing a release. (4) Filed 795-e5c7: the real cause under three CA permission fixes is CA_DIR = "/tmp/tillandsias-ca"; XDG_RUNTIME_DIR or DirBuilder::mode(0o700) ends the class, and clamp-ca-material.sh needs a retirement condition. (5) HOST STATE, operator action: this host runs Silverblue deployment 44.20260815.0 while 44.20260816.0 (13 upgraded packages) is staged and unbooted, uptime 1d15h. Not rebooted from an unattended cycle — that is the operator's call. Rejected the audit's suggestion to soften blocked:upstream-auth-unpublished: that block is what stops a forge draining into a mirror that cannot reach upstream (the 756-2jnj two-commit loss).
- 2026-08-17T09:25Z  `6f6e48e32`  yoga (meta-orchestration cycle 14): 760-hzi4/552 BOTH CRITERIA MET — the spec RAG index's missing piece was the EMBEDDING STEP, which existed on no host in any language, only as prose inside forge-plan.sh's refusal string (the chunker from 547 already made 9909 chunks in 25ms; spec-retrieve already read both files; nothing wrote vectors.jsonl). scripts/spec-index-ensure.sh is that step, resolving dir/endpoint/model exactly as the consumer does so it cannot build a silently-wrong index, with per-batch arity checks and atomic publish. spec_answer now returns confidence=retrieved with 6 citations, verified over real MCP JSON-RPC. Refresh wired into post-commit-expert-refresh.sh in the slot its header reserved for L1 indexes, with NO corpus path list (CORPUS_ROOTS is data in one place; a copy would drift) — the ensure fingerprints corpus+model and decides for itself. Measured 0.05s warm / 12m19s cold, so the hook forks and never awaits and the script takes a lock (verified against live and dead holders). NOT claimed: prose synthesis, which falls back to the documented zero-token cited-digest floor on a 0.5b model. Also delivered the linux-immutable verified-by for 793-rb9u (bash unambiguous here; option C is a no-op on this host kind; found that forge-plan.sh is 0644 while project-info.sh is 0755, which rules out the shebang route for one server). THIRD SIGHTING of "the fix landed somewhere the running thing never re-reads": FORGE_SPEC_INDEX_DIR still exported into this session as /mnt/c despite 2b1f8d188 removing it from the tracked config — after the binary-vs-release skew (783-6rik) and the staged-but-unbooted deployment (cycle 13).
- 2026-08-17T10:20Z  `pending`  yoga (meta-orchestration cycle 15): destructive slot spent on 626-p4xd criterion 3 — three cold-cache forge launches. The first was also the operator's BigPickle in-forge full-meta e2e and SUCCEEDED: exit 0, its own MO-FULL: COMPLETE 20867b4fc, delivering 795-57y6 (vsock cancel-safety) and a 74-fragment compaction; worker drain still gated by blocked:upstream-auth-unpublished while PR #98 is unmerged. Criterion 3's HYPOTHESIS IS REFUTED: no /$bunfs/ render failure in any of the three. Its recipe is also not runnable as written — /tmp/forge-lifecycle.log does not exist unless --debug is passed (the env var alone does not propagate into the container, main.rs:5992) and it lives in the container's discarded /tmp; the only route is the launcher's stderr. What the traces DID show is a live masked defect, filed 797-t9m7: on every cold cache the opencode curl-install fails ("Version 1.18.18 already installed" — the upstream installer short-circuits) and rolls back to a last-good that is not in the cache, yet the launch succeeds on a binary found elsewhere; after three cold launches harness-curl/opencode/bin is still empty. That undercuts 626-p4xd's own delivered machinery, whose rollback target and render probe both assume that path populates the cache. Also the e2e confirmation of cycle 13's podman change: the forge launched and cleaned up normally with the storage wrapper no longer chosen by a stopwatch.
- 2026-08-17T10:40Z  `pending`  yoga (meta-orchestration cycle 16): answered 797-t9m7's open question and CORRECTED my own overreach in it. The executed opencode comes from the NPM path (npm install -g opencode-ai -> $NPM_CONFIG_PREFIX/bin/opencode, confirmed on disk as npm/global/bin/opencode in the forge-cache volume), not the curl path whose cache stays empty; the curl installer short-circuits with "already installed" precisely BECAUSE npm put it on PATH first, and the curl path works fine for claude, so this is opencode-specific. CORRECTION: I had written that the render probe "guards a binary the harness neither installed nor version-controls" — wrong; harness_probe defaults bin_path to ${NPM_CONFIG_PREFIX}/bin/$1 and last-good is written to the same prefix, so 626-p4xd's machinery does cover the binary that actually runs. The defect is narrower and still real: a duplicate opencode-specific provisioning path that fails every cold start, announces a rollback against a last-good it never recorded, and is silent without --debug. Disposition is now a choice with evidence — retire curl_install_opencode (npm already delivers; the two actively conflict) or make it fail loudly. Also verified macuahuitl's 66a37e92c coexists with cycle 13's podman change, and recorded on 797-w8kf that both fixes are symptoms of one property (the wrapper's single fixed shared path) with my measured 50/400 ETXTBSY evidence, so deriving that path from its contents would delete both workarounds rather than maintain them.
- 2026-08-17T11:45Z  `pending`  yoga (meta-orchestration cycle 17): no new code; two corrections and one confirmation. (1) 797-t9m7 mechanism CONFIRMED from the upstream installer's own source: check_version() keys on `command -v opencode` from PATH, not OPENCODE_INSTALL_DIR, and exits ZERO on a match — so the harness's `bash $tmp` clause passes and only `[ -x $bin ]` catches that nothing was installed. Structural, not flaky: while npm puts opencode-ai 1.18.18 first on PATH, curl_install_opencode can never succeed. Retiring loses nothing (harness_probe applies the auth/render contracts to the npm binary; last-good lives in the npm prefix) but either fix changes WHICH binary the forge runs, so it wants 793-rb9u's per-host-kind treatment rather than an unattended 04:30 change. (2) CORRECTION to cycle 14: 760-hzi4 criterion 2 is IMPLEMENTED but INERT here — the post-commit dispatcher gates the expert-refresh body behind TILLANDSIAS_HOST_EXPERTS (685-yidq) and that is set nowhere on yoga, so after seven commits the hook log has ZERO spec-index lines. I checked wiring and not execution, which is the 599-4wzr class from the inside. Smallest next action is a per-host opt-in (shell profile or untracked machine-local config), deliberately NOT the tracked settings.json, since the gate exists to keep CI and plain checkouts from rebuilding. (3) CORRECTION: `pgrep -f spec-index-ensure` self-matches the waiting shell, so an `until ! pgrep -f ...` loop never terminates and briefly made me think an orphan builder was running.
- 2026-08-17T12:30Z  `pending`  yoga (meta-orchestration cycle 18, FINAL of the nightly loop): the new `expert-<report>` preflight segment reads `expert-absent` on this host, and that is the CORRECT state — forge-plan.sh:317-325 deliberately prefers the checkout's target/release over `~/.local/bin` on bare-metal dev, because on 2026-08-15 an Aug-12 user-level install refused "what is the current Direction?" while the checkout's binary answered it cited (behavioural staleness, so no capability gap fired). Having no user-level copy removes that hazard rather than relying on the preference to defend against it; do not "fix" expert-absent by installing a second binary there. macuahuitl independently confirmed both halves of cycle 17's correction (1cd1b1658): bare-metal spec_answer works and 760-hzi4 criterion 2 is inert on a second host, so the TILLANDSIAS_HOST_EXPERTS gap is fleet-wide and the opt-in is a fleet decision, not a yoga chore. Index at handover: 9910 chunks / 9910 vectors, fingerprint fresh, warm path 0.049s, spec_answer confidence=retrieved — with retrieval quality uneven by question, which is what order 551's ground-truth grading exists to measure and could not until tonight.
- 2026-08-17T12:30Z  `c96a4ce57`  Release v0.4.260817.1 — merged PR #98 to main, tagged, workflow_dispatch triggered (run 32030029176). Gate on the merged tree: ./build.sh --ci-full 24/24 checks, 305 litmus PASS / 0 FAIL, rc=0; scripts/release-preflight.sh ok; tray-parity 0 gaps. Held since 08:00Z on 794-57bv then 797-r6tc, both closed.
- 2026-08-17T13:30Z  `pending`  yoga (meta-orchestration cycle 19, FINAL — nightly loop ends): 783-6rik CLOSED BY RELEASE and verified from both sides. v0.4.260817.1 published 12:43Z and curl-installed here at 13:24Z (rc=0); runtime/0.4.260817.1/images/git/probe-upstream-auth.sh exists at 8807 bytes while the same path under 0.4.260815.1 — the release every host ran all night — does not exist at all. The class holds as filed: nothing in the checkout changed and no host could fix it locally; the guard was correct and unpassable until a release-side asset shipped. Re-verified the CA material stayed clamped after installing a new binary (791-swxt exists because a fresh install can re-widen host state). This host is now current, running the release it helped unblock — 793-a62g held that release and cycle 13 fixed its storage half. STILL OPEN FOR THE OPERATOR: the Silverblue deployment 44.20260816.0 remains staged and unbooted (a release install does not touch it; a reboot ends the session, any forge, and this loop), plus three filed decisions — 795-x7ux (untrack .claude/settings.local.json), 797-t9m7 (retire the duplicate opencode curl install path), and the fleet-wide TILLANDSIAS_HOST_EXPERTS opt-in that macuahuitl confirmed inert on a second host.
- 2026-08-17T21:30Z  `pending`  yoga (meta-orchestration, linux-immutable): 802-2536 CPU arm measured on the windows host's own methodology (284513c22) so it is comparable — 40 real chunks p50 298 chars, warm-up discarded, 3 reps, 0 failures: embed 61/60/62 ms per chunk, decode 98.2/109.1/121.2 tok/s, prefill 4351/4463/4764 tok/s (qwen2.5:0.5b). Their CPU was 586.6 ms/chunk and their iGPU 460.3, so this host's CPU lane is ~7.5x faster than their best lane on slightly larger chunks — NOT attributable to native-Linux alone (different part, AVX-512 VNNI/BF16, and theirs ran in WSL2). Consequence for 552: a full 9909-chunk re-embed is ~10 min here vs ~90-115 min there. TWO BLOCKERS FOUND: (1) the GPU arm cannot run — dev-inference-ensure.sh:167 passes a device only for legacy_tier=gpu-cuda and this host is gpu-rocm, and adding --device /dev/dri is NOT sufficient (tested: container sees renderD128 mode 666, ollama still reports GPU: CPU only, initial_count=0) because the image ships llama-server's vulkan BACKEND with no vulkan userspace driver — no icd.d, no loader, no libvulkan_radeon.so. Mirror image of order 408: NVIDIA needs host libs injected via CDI, Mesa/RADV belongs IN the image. Filed FLEET-GATED as 805-3egx rather than committed, because the windows host deliberately DELETED those packages (c6df04f17) after measuring a 1.23x decode regression — though they later withdrew the embedding half (284513c22: embeddings are prefill-shaped so the iGPU wins them) and arrived at route-by-workload-shape. (2) MeasurementRecord HAS NO WRITER — accel_probe.rs:127 hard-codes Vec::new() under a comment claiming on-demand microbenchmarks that do not exist anywhere; `measurements: []` means nothing writes, not that nothing ran, so 802-2536's "record into the existing MeasurementRecord" is not actionable by any host. Filed 805-wgbb, including the schema mismatch (tps-only, no latency/timestamp/workload field, so ms-per-chunk has nowhere to live). New shared artifact scripts/bench-accel-lane.sh carries the methodology as code; the dialect gate caught a `date +%s%3N` GNU-ism in my first draft, replaced with curl's %{time_total}, which is portable AND excludes shell overhead from the sample.
- 2026-08-17T22:21Z  `0a1c2ae24`  yoga (meta-orchestration, linux-immutable): 803-su4n code corpus landed and VERIFIED against the packet's own failing question. Q2 ("how is the inference tier selected, and what makes it fall back to CPU?") now returns retrieved with 3 of 6 citations in code — main.rs:3641-3675 (fn detect_inference_tier), :3676-3687 (fn effective_inference_tier, containing the literal `"gpu-cuda" if !nvidia_cdi_available() => "cpu"`), policy_router.rs:57-64 — and I read the spans rather than trusting them. FINDING: the "a new corpus is one row, not a code change" comment is FALSE for code and silently so — the dispatch derives extensions AND chunker from the kind, so a bare row inherits want_ext=[md]+chunk_markdown and matches zero files, an empty result rather than an error. Chunking is on named constructs with the file preamble kept (in this repo the header comment is often the only record of WHY), 120-line cap so the embedder cannot truncate a tail, each split piece taking its own key because inheriting the signature would break answer::verify — pinned by a mutation control plus a negative control on ordinary lines. COST, correcting my own estimate: 9,910 -> 18,269 chunks (+84%); I predicted ~19 min from 61 ms/chunk measured on SPEC chunks, the actual rebuild ran ~35 min because code chunks are larger, which scales to roughly 5-6h on macuahuitl's CPU lane — strengthening the case for 801-a2by's durable index and for re-embeds on the fastest lane. images/ deferred: mixed-type, wants its own boundary rules.
- 2026-08-17T22:45Z  `pending`  yoga (meta-orchestration, linux-immutable): 797-p2xa fail-closed landed and immediately caught a probe the fleet depends on. The mock's trailing bare `exit 0` is now a default arm naming the subcommand + full argv, exiting 97; network|compose|system stay explicit no-ops with a stated reason; the trailing exit 0 is restored after the esac and is safe only because the default refuses first. Negative control added to the already-wired hermetic test (absurd subcommand must exit non-zero AND name itself), plus a control-for-the-control (a known subcommand must still succeed, else the first check passes on a mock that refuses everything). BLAST RADIUS: first failure was immediate — build-image.sh exited 97 because it calls `podman --version`, which the flag-skip loop leaves as the subcommand while the case had only a bare `version)` arm; the FLAG form therefore matched nothing and previously returned success with NO OUTPUT. That is the probe require_podman itself uses, so any caller parsing a mock version got an empty string. Fixed as `version|--version|-v`. Hermetic fixture now 6/6; instant pre-build 243/246. The 3 remaining reds are NOT mine — stashed the change, re-ran two of them in isolation, both still fail, neither touches podman — and no packet referenced them, so filed as 808-euam (added-fragment-parse-gate-shape STEP 3 produces EMPTY output from a negative control; forge-clone-reachability-probe-shape STEP 1 reports an unresolvable alias does not fast-fail).
- 2026-08-17T23:32Z  `pending`  yoga (meta-orchestration, linux-immutable): 797-qm4t — unknown packet_id is now a verdict, and the correction is the finding. I first put the check on the D path (the `packets:` declaration join), which is how the packet's suggested fix reads; a control proved it UNREACHABLE — a `packets:` entry creates the packet in the fold, so `pid in st` always holds (my phantom raised checked 87→88 and correctly produced no violation). I removed that addition rather than leave a branch no control can exercise. The channels that declare WITHOUT creating are the events block and the LWW `status:` block, and both were silent: a `completed` event for a phantom id now refuses by name with rc 1 (the exact case recorded on the packet, where fragment-terminal-events printed the phantom as if it had found a closure), and the LWW block — previously not scanned at all, deliberately and rightly for LOSS — is now scanned for the unknown-pid question only, section-scoped since `  - packet_id:` also appears under packets:/events:. Controls both directions: phantom pid in an LWW status block → violation naming it rc 1 (silent before); same shape with a real packet_id → ok rc 0, so it is not refusing everything. Live: 90 checked, no false positives; pinned grammar unchanged; litmus ci-release 19/20 with the only failure being the pre-existing 808-euam one.
- 2026-08-18T00:28Z  `pending`  yoga (meta-orchestration, linux-immutable): 797-8dzt — swept every source-pinning guard (20 files using include_str!, 103 split-bounds). ZERO further instances: no bound names a needle absent from the file it reads, stable across three independent attribution strategies; macuahuitl's remote_projects case was the only one and is fixed. THE METHOD IS THE FINDING, since the packet's START HERE (grep include_str!) finds candidates but cannot judge them and each wrong pass produced a confident defect list: (1) comparing the ESCAPED literal `"\nfn "` against raw file text made every escaped needle read as absent — 34 false positives; (2) searching the file CONTAINING the test rather than the file it include_str!s — the lone survivor was a macos guard bounding on `"\nasync fn "`, absent from main.rs but present 9x in action_host.rs which it actually reads, where the next item after apply_vm_status is literally `async fn run_push_listener(`; (3) attributing splits to the most recent include_str! and missing the `include_str!(concat!(env!("CARGO_MANIFEST_DIR"), ...))` form, which mis-attributed all 25 vault_bootstrap bounds to a SELinux .cil file. LIMIT, and it is the deliverable: needle-presence is necessary, not sufficient — a needle that exists but appears AFTER the next item still widens silently, and only the packet's own method (delete what the guard protects, see if it still passes) distinguishes that. Cheap half complete and clean; the ~57 mutation controls remain. Did NOT ship the audit script: a permanent check verifying only that bounds name something real would read as coverage of "this guard can fail" while providing none — the exact class this milestone removes.
- 2026-08-18T01:29Z  `pending`  yoga (meta-orchestration, linux-immutable): 798-c4mq — the lane now names WHICH precondition is unmet, and the message it replaces was mine. check-credential-channel.sh read only `podman ps` (RUNNING containers), so a host with no mirror container fell into "could not inspect the mirror image … check whether the image carries probe-upstream-auth" — sending the reader to the one link that was fine (on macuahuitl the image was two days NEWER than the probe and carried it byte-for-byte, with no tillandsias-git container in the stack). Now six states, each numbered by precondition (1 image built / 2 container created / 3 service healthy) with its own remedy. VERIFIED live and hermetically: this host is in macuahuitl's exact state (6 mirror images, zero containers) and classifies as image-only; and by extracting the script's OWN ladder against a scripted podman — no-podman, no-image, image-only, container-stopped (Exited 137), running-probe-present, running-probe-absent all correct. The `podman ps -a` distinction is the packet: an ABSENT container and a STOPPED one are different faults that used to produce identical silence. My no-podman control initially failed on the HARNESS not the code (PATH=/nonexistent strips the tools the ladder needs — the same trap that cost a fixture earlier in this fleet's history). Stdout grammar untouched; litmus meta-orchestration 15/15; gate green. NOT claimed, as the packet withholds it: that the lane works once the mirror runs — that needs the stack up under destructive e2e.
- 2026-08-18T03:00Z  `pending`  yoga (meta-orchestration, linux-immutable): 798-tk7b — the enclave service set now reports itself in `tillandsias --diagnostics`, CONVERGED on the vocabulary squid-supervisor.sh and harness-supervisor.sh already emit (severity:event:key=value) rather than becoming the fourth mechanism the packet warned about. Lines: fail:enclave-service-dead:service=X:state=S:rc=N:signal=SIGKILL:age=5h:restarts=N / note:enclave-service-stopped / ok:enclave-service-up. Routed through PodmanClient::list_containers, not a shell-out; podman ps JSON already carried ExitCode/ExitedAt/Restarts and ContainerListEntry simply never surfaced them. LIVE CONFIRMATION OF THE PACKET'S CLAIM, produced BY ME and reported as such: running --diagnose to verify tore the stack down, and podman then showed `tillandsias-proxy Exited (139) 16 seconds ago (healthy)` and `tillandsias-vault Exited (137) … (healthy)` on this host — a SIGSEGV'd container advertising healthy. Then a timed-out command of mine SIGTERM'd the tray, whose exit cleanup tore the whole stack down again. Fully restored: tray relaunched detached, vault/proxy/dev-inference Up, endpoint reachable, experts healthy. NOT COMPLETE, and the reason matters: the host-side view has no teardown context, so it will emit fail: on every ordinary stop — squid-supervisor already normalises shutdown crashes to note:proxy-exit-teardown-crash for exactly this reason, and a report that cries wolf at every shutdown trains its reader to ignore it, which is how the original blind spot survived five hours. Next slice: read the same teardown state, or suppress fail: while a stop is in flight. 15 tests green including a negative control that running and cleanly-stopped services must NOT read as failures.
- 2026-08-18T03:45Z  `pending`  yoga (meta-orchestration, linux-immutable): 606-um5s — two corrections to the packet's own guidance, and it stays blocked here for a STATED reason. (1) The recorded command does not run: `nix-toolbox.sh run -- nix build` resolves the rung then execs WITHOUT injecting that rung's flags, and its toolbox arm execs `_toolbox`, a shell function (799-nx4r confirmed from source at nix-toolbox.sh:326). The flags live in the separate `nix-args` subcommand (`--store $CHROOT_STORE` for chroot), so the working shape is `nix $(scripts/nix-toolbox.sh nix-args) build …`. (2) The precondition is unsatisfiable on a host without host-nix: resolve_rung's ladder is daemon→chroot→toolbox; chroot needs a nix BINARY on the host, and the toolbox arm calls ensure_toolbox (creates the container, installs nothing) then tests `command -v nix` inside it — and nothing in the tree ever puts nix there, as this packet's own note records ("the init set was deliberately NOT extended with nix"). Measured here: nix-args → blocked:nix-toolbox:no-nix-and-no-toolbox rc 1; store-status → skip:nix-store:no-store; no host nix; daemon inactive; builder toolbox running with no nix. macuahuitl's proof ran at the CHROOT rung on their host's own nix — the rung an immutable host lacks — so the TOOLBOX rung has not been demonstrated anywhere. Not improvised: /nix wants root and there is no passwordless root in the toolbox here, so the rootless shape (static nix + the existing chroot store) is a design choice about where the fleet's nix comes from; filed as 814-5avq with three options rather than picking one. The migration itself is untouched: flake.nix pins nixos-24.11 (EOL) with crane held at v0.20.3 by its own comment because v0.21+ needs nixpkgs-25.11 — a coupled bump, not two independent ones.
- 2026-08-18T04:45Z  `pending`  yoga (meta-orchestration, linux-immutable): 803-su4n's third root was already landed by a sibling (070021f2f: images/ + section-based shell chunking) — my patch correctly refused to apply, and I verified their work instead of redoing it: Containerfile chunks on directive boundaries (ARG/RUN/COPY/ENV, 15 chunks for images/default/Containerfile), and their duplication test is written against the RULE (is_pruned) rather than the corpus, which is right because a corpus-level assertion passes vacuously today. Corpus is now 19483 chunks (code 9547); index rebuild running. 805-3egx has NO fleet responses yet, so the 802-2536 GPU arm stays blocked. Took 551 (spec-expert-groundtruth) far enough to record that its DEPENDENCY IS NOW SATISFIED — it was unrunnable purely because the index had no vectors — and to resolve the design question it appears to have: the grader is network-free while a spec answer needs an embedding, which looks like "call HTTP and lose hermeticity, or grade a lexical path production never runs". It is not a tension: spec-retrieve is already documented (main.rs:257) as "network-free cosine top-k over CALLER-SUPPLIED embeddings", so the engine supplies the query vector and grading stays hermetic while exercising the real retrieval+envelope path. Cost stated: a committed query vector is model-pinned (768-dim nomic-embed-text), already a managed fact via check-dev-embed-model-agreement.sh; a model change re-embeds the set rather than rewriting it. Shape otherwise ready: ENGINES rejects unregistered engines loudly, Expect grades the envelope not prose, and citations_exclude is already the falsifiability mechanism the third exit criterion wants. NOT claimed or started — 6h packet with ~15 min of cycle left, and half a grading engine reads as coverage.
- 2026-08-18T06:25Z  `pending`  yoga (meta-orchestration, linux-immutable): 551 — spec.answer engine registered and GREEN, with the falsifiability check. Reuses expert-groundtruth-rung1.yaml's format verbatim, as that file's header anticipated (register in ENGINES + Harness::run, drop a set beside it, same code path so the bar can't be lowered). Hermetic by the contract rather than by compromise: spec-retrieve is already specified as "network-free cosine top-k over CALLER-SUPPLIED embeddings", so each case carries a committed query_vec and the grader exercises the REAL path (top_k → retrieval_only_answer → build_envelope), not a lexical stand-in — a stand-in would be worse than nothing, since green would certify a path production never runs. MISTAKE THE CORPUS CAUGHT IN ONE RUN: my first shape re-derived chunks from the repo and took only vectors from the index — wrong precisely BECAUSE 803-su4n put crates/ in the corpus, so editing any Rust file shifts the pairing. Adding this engine took the tree 19483→19485 against 19483 stored vectors and the arity guard refused rather than grading a shift (a shift cites real files at wrong lines and looks healthy). Both sides now come from the index, written by one run over one tree; the arity check stays for a killed/half-copied builder; span_contains still re-reads spans from disk so a stale index makes cases FAIL, never pass. PASS 1/1 486ms; mutating one expected sentence turns it red with a closest-miss diagnostic. NOT DELIVERED: the synthesized envelope is not graded (needs a model call, non-deterministic) — only retrieval-only; and the set is NOT wired into the default grade or build --check, because the engine needs TILLANDSIAS_SPEC_INDEX_DIR and a host without an index would get a harness ERROR, turning a missing host artifact into a fleet-wide red build. The gate's committed-set test names one file rather than globbing, so this set cannot affect it.
- 2026-08-18T07:00Z  `pending`  yoga (meta-orchestration, linux-immutable): filed 821-73es (p1) — THE FAT SPEC EXPERT CANNOT REFUSE. Writing 551's set I went looking for a refusal case, since Expect treats `unsupported` as first-class, and could not write one: spec_answer answers "what is the best recipe for sourdough bread" with confidence=retrieved and six real, verifiable spec citations. Two causes, both needed: cosine top-k always returns k (no threshold anywhere), and NO relevance signal survives — spec-retrieve drops the score and the envelope's citations carry only authority/commit/kind/lines/path, so a caller who wanted a threshold has nothing to apply it to. p1 because the deterministic experts DO refuse (answer.rs:480-491, empty citations downgrade to unsupported), so the fleet has one expert family that refuses and one that cannot behind one envelope schema where `retrieved` reads identically. Deliberately NOT pinned in 551's set: a case asserting the current behaviour would make it the contract and turn the fix red. AND A CORRECTION TO MYSELF, same cycle: I first recorded that code-in-corpus costs a ~35-minute re-embed after every commit and framed three options around it — then ran the rebuild and it returned instantly (`ok:spec-index:fresh:19485`), because the durable index is content-addressed with one dir per corpus fingerprint plus a `current` pointer file, and the post-commit hook builds asynchronously. The 35 min is a cold cost paid in the background, not a wait. What survives, smaller: a grade can refuse on arity in the window before the hook finishes, and a host with TILLANDSIAS_HOST_EXPERTS unset never gets the async build at all. I should have measured before framing options — same order-of-operations error as the pgrep self-match and PATH=/nonexistent earlier in this loop. Also: 551's grade is green at HEAD's corpus (19485/19485, PASS 1/1, 471ms).
- 2026-08-18T07:30Z  `pending`  yoga (meta-orchestration, linux-immutable): 821-73es criterion 1 done and the threshold MEASURED — with a result that would have made the obvious choice wrong. spec-retrieve now emits spec::ScoredChunk (a wrapper, not a Chunk field: Chunk derives Eq which f32 can't satisfy, and a similarity belongs to a retrieval not a passage, so it must not enter chunks.jsonl). serde(flatten) keeps the shape identical plus one key so spec-envelope still parses the same bytes — verified live and PINNED by a test, because if that broke, retrieval would keep working while the envelope silently lost every citation. Second test: the index record gains no score column. MEASUREMENT, 21 questions, top-1 cosine on the live 19485-chunk index: in-corpus 0.720–0.806; FAR out-of-corpus (sourdough, world cup, Mongolia, Moby Dick, photosynthesis) 0.487–0.611 — an apparent 0.109 gap that is a TRAP; NEAR MISSES (Kubernetes ingress, S3 retry policy, Terraform auth, Postgres sharding, Kafka offsets — plausible infra questions this corpus does not answer) 0.591–0.693. Real separating band is 0.693–0.720, margin 0.027, four times narrower. A 0.65 threshold picked from the easy negatives would ADMIT FOUR OF FIVE near misses. Also ruled out: the top1−top6 SPREAD does not separate (in-corpus 0.020/0.035/0.058 vs near-miss 0.047/0.013/0.014 — overlapping, lowest in-corpus below highest near-miss). NOT DONE: no threshold implemented, nothing refuses yet; n=21 hand-picked is enough to disqualify 0.65 and kill the spread heuristic, not to justify a number on a 0.027 margin, and cosine isn't calibrated across corpora/models so other hosts should measure. Criterion 1 also wants the score in the ENVELOPE — answer.rs Citation is untouched.
- 2026-08-18T09:10Z  `pending`  yoga (meta-orchestration, linux-immutable): 821-73es criterion 1 COMPLETE — the score now reaches the ENVELOPE. Citation gains `score: Option<f32>` following the `commit` precedent beside it (additive, skip_serializing_if, absent when unknown: a deterministic expert omits it rather than writing 1.0, which would read as a perfect match instead of "not applicable"). Citation and Envelope drop the Eq derive to allow an f32 — checked first that nothing needed it (no set/key use, no trait bounds); PartialEq keeps assert_eq! working. build_envelope_scored is additive so the cheatsheet and deterministic paths are untouched. with_score DROPS values outside [-1,1] or NaN, the same discipline with_commit uses. Three property tests: the score reaches a citation and survives serialization; the unscored builder omits the field rather than inventing one; every impossible score (3.0, -2.0, NaN, INFINITY) is dropped. 23 spec tests green, grade PASS 1/1, gate green. NOTHING REFUSES — that changes what every agent on every host can learn, so filed as 824-6qxh (fleet decision) with the measurement attached, carrying the trap explicitly: far negatives invite a 0.65 threshold that ADMITS FOUR OF FIVE near misses; the real band is 0.693–0.720, margin 0.027; the top1−top6 spread heuristic is ruled out. Each host is asked for its own in-corpus AND near-miss bands — the near-miss half is what matters and what's easy to skip.
- 2026-08-18T09:41Z  `pending`  yoga (meta-orchestration, linux-immutable): 805-wgbb — MeasurementRecord HAS A WRITER, so 802-2536's "record into the existing MeasurementRecord" is finally doable. accel_probe::record_measurement merges by (device, engine) REPLACING in place (a rerun is a new measurement of the same thing, not history; the router reads this as an input surface), exposed as `tillandsias --record-measurement <file|->`. Verified e2e: records land; same key twice leaves ONE entry; a second lane appends; malformed input refuses naming the missing field with exit 2. This host's document now carries a real matrix — cpu decode_tps from a live run, and gpu degraded=true with reason "vulkan userspace absent from the inference image (805-3egx)". That degraded entry is the point: the GPU lane is unmeasurable here and the document now SAYS so instead of being silently empty, which is what `measurements: []` has meant on every host until now. bench-accel-lane.sh --record wires it so the writer has a caller. ITS FIRST RUN FAILED on the fleet's recurring version-skew class: the script called `tillandsias` from PATH (the INSTALLED release) while the flag lives in the checkout — "Unsupported option". Now resolves target/release first, same rationale as resolve_plan_binary; the fail-loud held, printing "record-failed (numbers still on stdout)" rather than claiming success. HONEST LIMITS: the mapping is partial and says so at the mapping site — MeasurementRecord has no latency or workload field, so embed ms-per-chunk still has nowhere to live (the open half of this packet, not pre-empted); joules_per_token untouched (no RAPL read exists); and load_or_probe re-probes on a tier flip, dropping measurements — correct but not durable, now documented. Verified e2e rather than unit-tested: an isolated test would move TILLANDSIAS_CACHE_DIR, and process-global env in a parallel test binary is the exact race 793-a62g cost a day.
- 2026-08-18T10:28Z  `pending`  yoga (meta-orchestration, linux-immutable): 802-2536 — this host's FULL three-lane matrix is now published in capabilities.json, with reasons for the two that can't run: cpu/ollama decode_tps 95.77 (degraded=false); gpu/ollama degraded "vulkan userspace absent from the inference image (805-3egx)"; npu/none degraded. Before 805-wgbb's writer an empty measurements array could not distinguish "unmeasurable" from "not measured yet". THE NPU FINDING CHANGES THE PACKET'S SHAPE rather than adding a number: /dev/accel/accel0 exists at mode 666, amdxdna IS loaded (fw 1.1.2.64, PCI 1022:17F0) with ZERO users — hardware and kernel are ready. Userspace is not, and it is not one package: XRT absent everywhere, no onnxruntime, and decisively the engines registry holds exactly ONE engine (ollama/llama-server, cpu+gpu only) with no XDNA backend. So the NPU lane is NOT the GPU lane's shape — the GPU is a driver package away with the backend already compiled in; the NPU needs a SECOND inference engine with a different model format (ONNX Runtime + Ryzen AI EP, models compiled for XDNA), i.e. a second inference path in the product, materially larger than the 10h estimate. Consequence for the routing question the packet exists to answer: on this host the near-term decision is two-way (cpu vs igpu, and 284513c22 already says route by workload shape), with the NPU a later capability rather than a tier the router should model today. ALSO: restored the host stack I had disrupted — the tray had died taking vault/proxy with it, and a wedged dev-inference container was holding :11434 while showing "Stopping". Tray now runs as a systemd --user transient unit so it is immune to my shell's process group; all four services Up, preflight services-down1 (nix, on-demand).
- 2026-08-18T11:55Z  `pending`  yoga (meta-orchestration, linux-immutable): 551's set is no longer one case — 3/3 PASS in 397ms, both new cases drawn from 803-su4n's own scorecard so the set grades what was measured broken rather than questions I invented, and both falsifiable (mutating `if !nvidia_cdi_available()` to a nonexistent function reddens exactly one; mutating "wipes Podman state" to "preserves all Podman state" reddens exactly one; unmutated stays green). Case 2 pins the scorecard's PARTIAL: the specs describe the tier architecture and structurally cannot state the mechanism, which is code — it now cites main.rs with span_contains on `fn effective_inference_tier` AND the fallback arm. Asserting TEXT not line ranges matters here: my own edits moved those functions 3641/3676 → 3819/3854 this session and the case is indifferent while still demanding the right function. Case 3 pins a FIX: the scorecard graded podman-reset a MISS (right file, wrong span — podman.md's Troubleshooting block, while the corpus holds exactly ONE authoritative line). Measured now: at k=4 the miss reproduces, but at k=6 (what the engine uses) podman-logging.md:38-49 IS retrieved and contains the rule, so Q3 is a hit today and the case makes any regression loud. Still NO refusal case — 821-73es: spec_answer cannot refuse, and pinning today's confident-wrong behaviour would make it the contract. CORRECTION to last cycle's report: I said 805-3egx had fleet responses; all three fragments are mine (the packet plus two of my own events citing it). No fleet response on any of the five open decisions.
- 2026-08-18T12:35Z  `pending`  yoga (meta-orchestration, linux-immutable): THE MOST IMPORTANT FINDING OF THE NIGHT, and it is a correction to my own verification. Called spec_answer the way an AGENT does — through the live MCP tool — and it returns confidence=unsupported: "the spec RAG index is not built at /mnt/c/Users/bullo/claudia/tillandsias/target/spec-index". A Windows path, on Fedora. The TRACKED FILE IS CLEAN (zero occurrences of FORGE_SPEC_INDEX_DIR in .claude/settings.local.json, .mcp.json, anywhere under .claude/ — 2b1f8d188 holds); what carries it is THIS SESSION's inherited environment, and the long-running MCP servers inherited it from the session at launch. Consequence: tonight's index build, code corpus (803-su4n), ground-truth engine (551) and retrieval scores (821-73es) were each verified by invoking forge-plan.sh directly under `env -u FORGE_SPEC_INDEX_DIR` — a fresh process with a scrubbed env, which is NOT the path an agent takes. Through the live MCP server spec_answer has been refusing all session. The capability is genuinely built and correct AND genuinely unavailable to agents on this host; both are true and I had only checked the first. Fourth time tonight the harness rather than the system produced a finding (after pgrep self-match, PATH=/nonexistent, and the 35-min rebuild claim) and by far the most consequential. FIX IS A NEW SESSION, not code — the file is already right, so any session started from here resolves correctly; same shape as the staged-but-unbooted deployment and the 783-6rik binary skew. Strengthens 795-x7ux: untracking removes the class, but nothing removes it from a process that already started.
- 2026-08-26T02:53Z  `341ab0010`  Release v0.4.260826.1 — merged PR #100 to main (2312 commits since the v0.4.260810.1 stable), VERSION bump PR #101, tagged, workflow_dispatch 32920839524 all three jobs SUCCESS, 32 assets. Gate at the exact tagged tree 5da210169: `./build.sh --ci-full` = 0, 2014 checks, 335 specs, zero failures; tray parity 0 gaps. Linux job 36m (vs 15m for v0.4.260817.1 — probable Nix cache miss across 2312 commits of changed derivations); full run 01:56Z→02:53Z. Published as PRE-release (unstable/daily channel). Linux artifact: https://github.com/8007342/tillandsias/releases/download/v0.4.260826.1/tillandsias-linux-x86_64 — stable promotion GATED on four independent curl-install e2e legs with falsifiers pre-registered in git before the tag existed (yoga + lenovinha Linux, macbook macOS, yolanda Windows).
- 2026-08-26T03:33Z  `341ab0010`  **STABLE PROMOTION v0.4.260826.1** — `scripts/promote-stable.sh v0.4.260826.1` returned `promoted:v0.4.260826.1` **without `--force`**, satisfying the evidence gate on three real curl-install e2e reports (macbook/macos, yolanda/windows, yoga/linux). Stable channel moves off v0.4.260810.1 after 16 days. VERIFIED after promotion, not assumed: `/releases/latest` -> v0.4.260826.1, `prerelease=false`, `stable` tag -> 341ab0010, all seven advertised `/releases/latest/download/` assets HTTP 200, and the stable Linux binary reports `Tillandsias v0.4.260826.1` with sha256 d1d3174619f1e094f3c61d0d46b4e0904a72d961b7505f983dacd0bb12611d2f matching published SHA256SUMS. **NAMED GAPS, from the legs themselves rather than inferred:** macOS VM provisioned but never booted (guest-side, control wire, vault init untested; `guest_version` null); Windows falsifier 2 UNTESTED not passed (interactive device-flow auth impossible unattended — five of six exercised); Linux reset is NOT credential-cold (Vault recovered a 2026-06-15 Shamir share and logged `preserving existing data volume`, so the re-init path SKILL.md:52-53 claims this smoke exercises has never been covered on Linux — no equivalent of Windows' 804-ckst exists). STILL OWED per promote-stable's own NEXT: a one-shot `SMOKE_CHANNEL=stable` install e2e.
- 2026-08-30T05:36Z  42a7f8145  795-5itp slice 6: vsock_exec read_envelope onto control_frame_codec(); last Linux production raw frame-length decode gone (ratchet 18/10 -> 17/9). Session-scoped Framed, write side a recorded 690-eug2 keep. Two mutation-tested negative controls. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T06:18Z  (pending)  335 slice: F1 reproduced on lenovinha over 3 cycles and RECLASSIFIED — pickup_role does filter (36 windows-ready excluded from linux), so the single-lane half is discipline, not a missing mechanism; 795-zshi re-pointed to windows and verified. Real gap is that pickup_role cannot express a multi-lane remainder (795-5itp exhibit). F7 thrashing searched-and-absent; F8 burndown already mechanised. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T07:18Z  (pending)  689-zwzm criterion 2: stage_lift_gates G1-G5 defined in methodology/in-forge-delegation.yaml with per-gate source, threshold and dated fed: verdict. Rule: an unfed metric is a RED gate. Only G2 green; G1 overhead_ratio has NEVER been computable (--emit-flow has no caller — filed 943-7dn5), G5 mcp half source=absent, G3 currently failing on a real expired claim. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T08:26Z  (pending)  689-zwzm G4 slice: the delegate liveness DEFAULT probe matched the shell that ran it, so died-without-outcome could NEVER fire while its litmus stayed green (every arm passed an explicit --alive-cmd). Fixed as 943-3xyf, two paired litmus arms added and mutation-tested, legacy records recognised on read. G4 verdict corrected in the methodology. Criterion 1 needs operator authorization for THIS host. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T09:18Z  (pending)  245 NA-03 slice: section 3 re-verified and symbol-anchored. Content drift found, not just rot — ForgeLaunch DEPS has SIX edges (NixCache, order 801-vm4p) vs the five documented, and NixCache is an undocumented Service node; order-252 convergence went from two call sites to one reached transitively. 10/10 citations wrong, worst one lands on a doc comment. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T10:23Z  (pending)  251 LM-04 slice: the long-running-view gate read only the folded base, so a set-field closure (the MANDATED method, writes a fragment) left 831-ezea reading live — the view advertised a completed packet as claimable AND the gate refused the push removing it. 321 unfolded fragments over 4 days, 144 with status writes. Fixed as 943-26rx with a fold-aware overlay, two paired litmus arms, mutation-tested. Third cycle running in a row where a litmus was green because its arms construct the input production derives. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T11:33Z  (pending)  245 NA-06 slice: all three root causes re-verified TRUE (ten-image set with vault third, cooked-read fix, build_vault_image early-return, squid splice), but 11 of 12 citations drifted — main.rs:7193-7205, cited for the "it was NOT the proxy" finding, now lands on a comment about proxy-resolution failures. Section Observation repaired too. The 881-29me gate caught this slice twice, both true positives, the second on a PROSE line. All three GPT-failed criteria now have a revision pass. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830. <!-- cite-ok: the drifted citation IS the finding this line reports -->
- 2026-08-30T12:20Z  (pending)  245 NA-05 slice: patch list audited against the tree — P2 CLOSED (draft recommendation refuted; tombstoning it would have deleted a correct requirement), P4 half-applied, P8 NEW (nix cache is an enclave member absent from the spec — second doc to miss order 801-vm4p), P6 open and drifting dangerously (squid declares :3129 the image-build lane, entrypoint announces it, nothing builds through it, bypass still live). Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T13:22Z  (pending)  245 P6 slice: squid.conf/entrypoint.sh advertised :3129 as the image-build lane while 3129 appears nowhere in crates/ and builds still egress via --dns 8.8.8.8. Config now states the truth; scripts/check-proxy-permissive-port-routing.sh enforces the agreement in BOTH directions (wired into build.sh, litmus 5/5, mutation-tested against the original defect). The operator decision — wire it or delete it — deliberately untouched. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T14:23Z  (pending)  245 P8 slice: the enclave-network spec enumerated four members in prose; the actual attach sites are TWELVE and six were missing (vault, router, nix cache, catalog, observatorium web, ssh-lane sidecar). Spec now symbol-anchored, scripts/check-enclave-membership-documented.sh refuses divergence both ways (build.sh + litmus 6/6). The sixth member was found by the guard, not the hand audit — the enclave is named by THREE constants and my sweep covered two. My guard was wrong three times before it was right; all three failures are pinned by a scope-control arm. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T15:21Z  (pending)  245 P3 slice: documented the NO_PROXY/Node egress contract in the proxy-container spec and the enclave-proxy cheatsheet — and found a code half nobody named: proxy_env_args and apply_proxy_env are parallel hand-maintained lists with only 1 of 7 vars pinned across them, by source scan. Now pinned behaviourally via build_run_args, mutation-tested. Also withdrew my own "second spec silent about vault" claim — 18/176 specs mention vault, no pattern. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T16:20Z  (pending)  245 P7 slice: headless-mode spec now tabulates all SIX per-operation image ensure lists plus run_init ten-image set, symbol-anchored. Two findings: vault absent from every per-operation list is order 253 DESIGN not omission (same for nix-cache via the ForgeLaunch graph), and the offered unification is wrong — the real risk is an INCOMPLETE list, which fails as a phantom registry pull 125. Recorded uneven drift protection: 2 of 6 sites pin list CONTENTS, 4 only the call string. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T17:24Z  (pending)  245 P5 slice: published openspec/specs/network-scenarios/spec.md — and verifying it corrected the draft three times: S0 DROPPED (no taker), S6 ADDED for the browser posture the draft admitted was unnamed (build_project_browser_spec sets no network at all), S4 retirement mechanism CORRECTED (an allowlist admitting only --device=, not a --network=host denylist — stronger than recorded, and the citation pointed at a test). Left unbound deliberately; binding it to the enclave-membership litmus would overstate coverage. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T18:18Z  (pending)  245 P4 slice: vsock-transport spec gains the platform matrix. Section 4 drew the boundary a level too high — hvsock is the HOST side, the guest listens on plain AF_VSOCK everywhere, which is what makes the uniformity claim checkable; macOS was imprecise the same way (Virtualization.framework does NOT expose AF_VSOCK); and section 4 omitted that the Windows service GUID is DERIVED from CONTROL_WIRE_VSOCK_PORT, so port and endpoint-id are one fact (42420 -> 0000a5b4-facb-11e6-bd58-64006a7986d3). Windows/macOS rows marked source-read, not exercised. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T19:24Z  (pending)  245 section-7 slice: re-checked the proposed follow-ups. One was NEVER FILED (RuntimeContext enum). And I re-discovered the two-squid distinction that section 1 already recorded on 2026-08-25 — my own P6 note three cycles ago used the over-broad phrasing section 1 explicitly warns against; corrected in squid.conf and the guard now carries a scope comment. Also: build.sh dev-proxy uses stock squid:6.1 precisely to dodge the chicken-and-egg P6 wanted a fallback for. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T20:23Z  (pending)  335 F10 slice: claiming does not claim — 245 carries 43 claim events and is still ready/offered; in_progress=4 fleet-wide, 3 of them stranded. methodology says a claim event changes status; append-event has no status flag. Same sentence shape as the completed rule that section 7.2 already corrected. Filed 943-unii as a decision. SEPARATELY: the gate caught that FOUR packets I filed this session never existed — I keyed the fragments steps: instead of packets:, the exact G-Set no-op section 7.2 documents; check went 696 -> 700 after repair. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T21:20Z  (pending)  944-vim8: built the guard F10 deferred — scripts/check-fragment-keys-are-read.sh refuses any plan/index.d/ fragment keyed outside packets/events/fields/status. Wired into build.sh, litmus 6/6, mutation-tested (adding "steps" to READ_KEYS makes the negative control fail with the original symptom). check went 700 -> 701. Filed under packets:, which is itself the demonstration. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T22:19Z  (pending)  943-unii slice: measured the reaper the decision depends on. scripts/reclaim-stranded-claims.sh has NO CALLER outside its own source and fixture; build.sh only shape-checks expire-claims --list-live. Same shape as 943-7dn5 and 943-3xyf — a correct, fixture-tested tool that nothing invokes. So the exclusive-claiming branch needs a discriminating invoker built first (the meta-orchestration skill warns indiscriminate reaping launders finished work into lost work); the audit-record branch does not. Corrected the worker skill: claims are "reclaimable after 24h", not "reclaimed after 4h" — both tools say 24 and nothing runs them. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T23:20Z  (pending)  943-unii slice: the nit I deferred was worse — bare --ttl-hours and bare --now HUNG the reaper (shift 2 with one arg left, $# unchanged, infinite loop), and a bad TTL blamed expire-claims. Fixed with named refusals, +3 fixture cases (3/3 -> 6/6), mutation-tested. FOURTH tool in four cycles found with no caller: the fixture itself was never run — now wired into build.sh. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-30T23:31Z  (pending)  943-unii: reaper parser fixed (bare --ttl-hours and --now HUNG; bad TTL blamed expire-claims), +3 fixture cases, and the fixture WIRED into build.sh — fourth no-caller tool in four cycles. Push was blocked by version-not-monotonic; the hook printed a remedy (merge origin/main) that could not work, while verify-version-monotonic.sh named the real one (post-release back-merge, order 800-vk2p, precedent f6424070d). Back-merged v0.4.260830.5. The tag is on NO branch and the check reads FETCHED tags, so it blocks only hosts that have fetched it. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-31T01:18Z  (pending)  Unblocked via the VERSION guard OWN exemption (643-64bx integration-branch catch-up): origin/linux-next took the 0.4.260830.5 bump, so a same-branch rebase makes my VERSION equal to it and both guards pass. No hand-edit, no --no-verify. Also flagged in advance-work-from-plan section 3 the DIVERGENCE from meta-orchestration: the coordinator claim step is set-field status in_progress pushed BEFORE the work, plus timestamp arbitration and unconditional release; section 2 appends an event that changes no status. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-31T01:47Z  (pending)  SMOKE v0.4.260830.5 daily, clean-room: PASS — install exit 0 and version matches tag, reset clean, pristine --init exit 0 with 10 images built and vault re-init healthy (no 738059bc brick), forge lane up, order-298 egress assertion holds, and CUDA placement proven (size_vram=524602571). TWO FINDINGS FILED: the runbook section 0.2b ledger-row awk uses undefined "daily" instead of $0 so it reports NO LEDGER ROW for every tag including ones that have rows; and v0.4.260830.5 genuinely has no README row (table stops at v0.4.260815.1) — the second was undetectable by the first. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260830.
- 2026-08-31T01:53Z  (note)  The preceding smoke commit message is MANGLED and cannot be fixed (pushed). I put backticks in a shell-passed -m string, so the shell substituted them: "daily" and "core" were executed as commands and replaced with empty strings, and "$0" expanded to /bin/bash — the message now reads "instead of /bin/bash" where it should read "instead of $0". The REPORT FILE is intact (written via a quoted heredoc); read plan/issues/smoke-e2e-findings-v0.4.260830.5-2026-08-31.md for the accurate text. This is the second time this session I hit backtick substitution in a shell-passed ledger string after recording a rule against it.
- 2026-08-31T02:27Z  b19f4ef79  943-unii CLOSED, and it was not the decision I filed it as. The worker skill's section 3 claim step ("append-event <id> claim", pushed WITH the work) changes no status, and "unleased" means exactly "status: in_progress" — so it hid nothing from anybody. The coordinator skill and methodology/distributed-work.yaml already agreed on the right recipe; the outlier was the one document every worker host runs every cycle. Section 3 now carries set-field status in_progress pushed BEFORE the work, plus the two rules it lacked entirely: collisions arbitrated by TIMESTAMP (a push can succeed and still have lost) and UNCONDITIONAL release-on-exit, with the 24h reaper demoted to a dead-host backstop rather than the return path. POSITIVE CONTROL taken while writing it: set-field moved the selector eligible count 174 to 173 and dropped 943-unii out of both "ready linux" and the printed batch, in one command — the mechanism was never broken, this host simply never invoked it across nineteen cycles. GUARD: scripts/check-claim-protocol-agrees.sh normalises each skill's set-field recipe to its command shape and refuses a tree where they differ, wired into build.sh --check, five arms mutation-tested red before being believed. The divergence lasted because each file was coherent alone and nothing in the tree ever read the two together — a check that lives between files was the only kind that could have caught it. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831.
- 2026-08-31T04:58Z  (pending)  335 criterion-1 F4 measured first-hand, then RELEASED back to ready (criterion 2 untouched). F4 is half refuted: claiming does hide a packet from ready/the selector, but burndown lists in_progress children with their status, so the coordinator skill's "hides it from ready AND from burndown" is wrong on the burndown half. THE REAL GAP IS THE OBSERVER. check-stranded-in-progress.sh's comment says "no progress event SINCE the claim"; its code counts progress|completed|blocked over the packet's ENTIRE history and flags zero. It reads no timestamp and never looks at the claim. Both error directions in one run: 335 (5 qualifying events, claimed 1 min earlier) NOT flagged and permanently immune -- every packet ever worked is invisible to this sweep by construction, including exactly the multi_cycle ones most likely to be re-claimed; 944-jaef (18 events, 0 qualifying, claimed 35 min earlier) FLAGGED. expire-claims said expired=0 while the sweep said stranded=4 of 5. That 80% is an artefact measuring newness, not abandonment -- and the coordinator adopted stranded-count as a tracked metric this hour assuming a near-zero baseline, so the calibration matters now rather than eventually. I minted 948-uhhw for this and then WITHDREW it unpushed: macuahuitl had already filed 946-pdpi at 02:34 from my earlier note, so mine was a duplicate remedy. That order token is spent and must not be reused. My measurement went onto 946-pdpi instead, correcting two things in it: its compaction framing is stale (882-vqe4 already fixed that half; the loop folds via plan-events now) and came from me before I measured, and its exit criterion 2 is VACUOUS -- it asks a fixture to prove a long-worked packet is NOT flagged, which the unfixed code already guarantees unconditionally, so that arm has no teeth against the bug. The direction needing a fixture is the inverse. Still not claimed: their script, their deliberate deferral past the 24h review. Also noted: the skill's own section 6 Defer Rule reads plan/issues/multi-host-integration-loop-2026-05-24.md, which does not exist in the tree, so that rule can never fire. Batch head 689-zwzm was correctly declined: criterion 1 needs operator authorization this host cannot supply and its next_action says so. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831.
- 2026-08-31T08:57Z  (pending)  245 NA-02 re-verified and re-anchored (L5), then RELEASED — multi_cycle/verification_required, no worker may close it. Section 2 held 28 of the document's 67 remaining line citations, the largest concentration left; all 28 are now tree-verified symbol anchors and the section carries zero bare line citations. Drift total again, three sections for three: main.rs:2556 lands on .output_bounded, 5847 on a doc comment, 5879-5895 on a codex-worker format!, 10531 on an else-if for a hyphen. Real symbols are run_init 7326, run_status_check 8453, run_provider_login 8988, ensure_enclave_for_project 12874 — not offsets, unrelated. THE FINDING: a citation that still RESOLVES, into a TEST, defending a claim its own section had already retracted. S4 said the policy layer denylists --network=host citing policy.rs:271,280; both lines do still contain that literal, but they are fixture rows inside fn passthrough_allowlist_keeps_only_gpu_device_flags, while the mechanism is fn is_allowlisted_passthrough_option (allowlists --device=, nothing else). Section 2's own superseding note had stated that correction the day before and the S4 bullet twelve lines below kept the retracted claim anyway — a correction at the head of a section does not propagate to its body, which is the document's standing warning proved one level in. Unlike L1/L2's drift into neighbouring code, this one lands on the exact string the claim is about in a test asserting the opposite structure, so following it CONFIRMS the wrong mechanism; no line-range checker can catch that. Also found P4 already DONE (matrix normative in openspec/specs/vsock-transport/spec.md since 2026-08-30) while next_action still called it the one open item — pointing fresh cycles at finished work. MY OWN ERROR, caught and repaired in-cycle: my first next_action rewrite TRUNCATED four live items (the completion gate's three named verifiers, sibling-lane confirmation of the two source-read matrix rows, the never-filed RuntimeContext follow-up, and the cross-link warning itself); restored in a second set-field. Overwriting an LWW field discards what you did not re-type. Direction: architecture-audit-epic, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831. <!-- cite-ok: the ledger entry quotes the drifted citations as the finding itself -->
- 2026-08-31T11:04Z  (pending)  949-uv5k filed, fixed and pinned: select-work-batch.sh promised "pass --seed explicitly to override when it happens" for a host routed onto an epic it cannot drain, and under ordinal routing the seed was NEVER CONSUMED — the pick read routed_pick and the seeded branch was dead. Measured: seven unrelated seeds gave byte-identical output on a host reporting route=rank:2/7:width=7. This is NOT the documented host-KIND seed collision (fixed by derive-host-identity.sh) and not an argument against routing, which is right and gives zero collisions where hashing gives ~2.5. It is that the documented escape hatch became unreachable, and it decays as the rollout SUCCEEDS: routing engages only for hosts with a published capability row, so every host gaining one under 850-bif2 silently loses the override. FIXED: explicit --seed takes the seeded pick under routing; bare default still routes by rank, so separation and cross-cycle cohesion are untouched; ROUTE resets to "seed" so an explicit run prints no route= field rather than claiming a rank provenance it does not have. After: nine seeds reach three distinct epics; default still reports route=rank:2/7:width=7 and is stable. THREE LITMUS ARMS, each mutation-tested in the direction it exists for: disabling the override reddens the positive arm and leaves the negative control green; making it unconditional reddens the negative control and leaves the positive green. ALSO FOUND: the suite's existing "same seed yields the same batch" arm was VACUOUS on every routed host — it passed BECAUSE the seed was ignored and would pass against a build with seeding deleted; its sibling entropy-floor arm already declares itself n/a under routing, this one silently did not. WHAT SURFACED IT: four consecutive cycles routed to architecture-audit-epic, whose four packets are all unactionable HERE (689-zwzm needs operator authorization recorded on another host, 795-5itp's remaining sites are macOS/Windows-owned, 245 and 251 are multi_cycle in phase review that no worker may close), while socket-audit-master sat at residual 22.1 with blocking=9 holding real Linux work. LEFT TO THE COORDINATOR, not decided by a worker: routing weighs residual, which is a property of the EPIC, and has no notion of actionability, which is a property of the (epic, host) PAIR. Direction: architecture-audit-epic by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831.
- 2026-08-31T13:08Z  (pending)  946-pdpi implemented and closed (coordinator handover, claimed over the routed batch and recorded as such). A sweep structurally blind to ever-worked packets is blind to exactly the multi_cycle set most likely to be claimed and abandoned, while crying wolf on fresh cycles — both errors from one missing timestamp read. The sweep now ages the claim via expire-claims --ttl-hours N --dry-run rather than counting progress events over all history, so the arithmetic is the binary's and not a second copy (704-zcgi). Threshold 8h by default, env-overridable, deliberately shorter than the reaper's 24h TTL: equal thresholds would make the report a duplicate of threshold_events and delete the early warning that is the sweep's only purpose. LIVE: 4-of-5 stranded became 2-of-5, both genuinely idle; the 35-minute claim stopped crying wolf and a long-worked packet can be flagged at all, which was structurally impossible before. FIXTURE 6/6 wired into build.sh, proving the coordinator's RESTATED criterion-2 pair (recent claim not flagged, stale claim flagged) with both fixture packets carrying progress events so the OLD predicate flags neither — mutation-tested, and the restored old predicate fails arm (b) while reporting stranded=0 next to threshold_events=1, the exact incoherence that motivated the packet. THREE FINDINGS IN MY OWN WORK, each caught by a check rather than by me: my first data source (--list-live) OMITS expire-candidates, the most stranded rows there are; row shapes differ by kind so a fixed column index was right for two of three; and my shell used date -u -d, GNU-only, which BSD date succeeds at with garbage so no exit-code guard could catch it — 761-g36m refused it and the right answer was to stop doing date arithmetic in shell entirely, which also made the fixture wall-clock independent. Criterion 3 documented in methodology/distributed-work.yaml beside the rule it contradicts. Also corrected: the header's claim that in_progress is invisible to ready AND burndown — the burndown half is false. Direction: architecture-audit-epic by routing; taken off-batch on explicit handover d8a7b0242.
- 2026-08-31T14:58Z  (pending)  245 NA-02 operation table RE-VERIFIED (L6), closing the gap L5 explicitly left open, then RELEASED — multi_cycle/verification_required, no worker closes it. SIX OF SIX CLAIMS HOLD, NO FINDING: --init builds 10 images including vault and does NOT ensure the enclave/egress networks in its body; status check ensures EIGHT; forge launch's image-ensure of 4 is [router git inference forge] verbatim; CLI opencode is 5 with router present (confirming router is no longer web-mode-only); opencode web is 7 WITHOUT web. Recorded deliberately even though nothing was found — a verification with a negative result is still a result, and without it the next reader either redoes the work or assumes the table was never checked. THE PATTERN IS NOW THREE FOR THREE AND IS A PROPERTY, NOT A COINCIDENCE: sections 1 (L1, yoga), 3 (L2) and now 2 all had TOTALLY rotted citations sitting over claims that were entirely TRUE. Claims here are about STRUCTURE — how many images a lane ensures, whether a body creates a network — which changes only when someone deliberately changes it: rare and visible. Line numbers change when anyone edits anything above them: constant and invisible. So citation rot in this document is a NAVIGATION failure, not a correctness signal, and inferring a claim's staleness from its pointer's state would have discarded six accurate rows today. CONSEQUENCE: section 2 is closed for a Linux host, and 245's actionable-for-lenovinha set is now EMPTY — what remains needs three named verifier agents, Windows/macOS lanes, or operator decisions. I recorded that emptiness in next_action rather than leaving the next cycle to rediscover it, and it is a datapoint for 950-icqy: this is the fifth consecutive cycle routed to an epic whose residual is real but whose actionability HERE is not. Direction: architecture-audit-epic by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831.
- 2026-08-31T16:58Z  (pending)  950-icqy claimed (unclaimed 6h, pickup_role linux) and BLOCKED after measurement, deliberately without implementing: the designed router fix cannot reproduce its own motivating scenario. The three named signals are not three. pickup_role is ALREADY APPLIED — the selector's pool is role- and capability-filtered before epics are scored, so epic membership is post-filter. An operator-authorization flag DOES NOT EXIST: the whole ledger carries only operator_approval(1), operator_note(6), operator_ruling(3), none marking unactionability. multi_cycle phase is the only genuinely new machine-readable signal, and on my own four-cycle pin the epic's filtered pool of 4 scores actionable=2 (795-5itp and 335 are not multi_cycle), so the router WOULD NOT HAVE SKIPPED. Exit criterion 2 is therefore satisfiable only by modelling 689-zwzm and 795-5itp with signals they do not carry — a fixture that constructs the input production does not derive, this epic's own recurring defect, and I would have authored it. AND A CORRECTION THAT DEFLATES MY OWN EARLIER REPORT: I told the coordinator this epic was unactionable here and it was not. 335's criterion 2, the prior-art survey, has been available on this host every one of those cycles; I did criterion 1's F4 measurement, left criterion 2, and then called the epic drained. Part of what I reported as a routing defect was me not taking available work, and the pin cost less than I claimed. THE REAL HARM IS DATA, NOT CODE: 689-zwzm is status ready while unworkable by anyone until an operator acts on another host — the vocabulary already has "blocked" and that is what it should be; 795-5itp carries pickup_role any while its Linux work is DONE and every remaining site is macOS/Windows-owned. Two data corrections fix most of it for free, with no new mechanism. Decision handed up: retire in favour of data hygiene, narrow to the multi_cycle-only signal accepting it rarely fires, or add a real unactionability field (schema change, all hosts, coordinator's call). Direction: architecture-audit-epic by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831.
- 2026-08-31T19:00Z  (pending)  ROUTING MOVED, and the data correction is why — empirical validation of retiring 950-icqy. 689-zwzm going ready->blocked (macuahuitl) dropped architecture-audit-epic from packets=4 to 3 and its score 12.421->10.616, demoting it rank 2 -> 3, so this host now routes to forge-local-experts-milestone with ten actionable packets. ONE DATA CORRECTION DID WHAT THE PROPOSED ROUTER FEATURE COULD NOT, which is the argument for retirement made in evidence rather than in prose. (795-5itp still reads ready for linux, so that half looks partial — noted, not claimed.) THEN 821-73es, claimed and released: landed the declined-alternatives corpus content, the last item macuahuitl's 2026-08-29 event named as remaining, AND FOUND ONE OF ITS THREE RECORDS FALSE. The record asserts Compose/Ansible/Terraform/systemd unit files are not used for orchestration; the first three check out, but packaging/systemd/user/tillandsias.service EXISTS, is referenced from build.sh, is documented in cheatsheets/runtime/dedicated-service-account-podman.md, and its Description is literally "Tillandsias Headless Orchestrator" — the exact word the record uses to deny it. Landing it verbatim would have written a falsehood into the corpus AS AUTHORITY, so an agent asking whether a systemd unit exists would have been confidently refused by the machinery built to prevent confident wrong answers. THE REUSABLE HALF: the 824-6qxh harness measured that these records CONVERT questions (2/5 near misses, zero in-corpus regression); it never measured that they are TRUE. Different properties, only one tested — an effect measurement being read as a correctness measurement. Landed methodology/declined-alternatives.yaml (two records verbatim minus the systemd clause, plus an explicit systemd_is_used_not_declined entry) and methodology/not-enough-information.yaml (refusal sentinel + why a confident wrong answer costs more than a refusal + unsupported is first-class for agents too). NOT claimed: I did not re-run the conversion measurement against the corrected text — that needs an index rebuild and belongs to whoever does one; next_action now says so and says the number must be recorded as theirs, not inherited. Also stale-next_action for the second cycle running: it still pointed at Tier A citation verification, which the 2026-08-29 event reports needed no new work. Direction: forge-local-experts-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831.
- 2026-08-31T21:00Z  (pending)  797-t9m7 loud-failure clause landed, then RELEASED with the retire-or-repair decision deliberately untaken. opencode_validate_or_rollback announced "rolling back to last-good" BEFORE checking whether a last-good existed, so on a cold cache it reported a rollback to a binary never recorded — yoga's three cold launches, HARNESS_CURL_ROOT/opencode/bin empty every time. And it used trace_lifecycle, which returns early without TILLANDSIAS_DEBUG, so the curl provisioning path failed silently on EVERY launch while the lane kept working from the npm path. Invisible in normal operation and mis-described in debug output. Three states now produce three outcomes: last-good restores (quiet, unchanged); last-good present but unusable (loud, named distinctly because a present-and-broken snapshot is worse than none); no last-good at all (loud, says the curl path is not populating its cache AND that this is not fatal since npm provides the running binary). Fixture 7/7 wired into build.sh, pinning the DISCRIMINATION not the wording, mutation-tested both directions — restoring the original reddens the empty-cache arm, warning unconditionally reddens the POSITIVE CONTROL, which is the arm that stops "always be loud" passing as a fix. I did NOT retire or repair the curl path: yoga's 2026-08-17 judgement was that switching fleet harness provisioning unattended is a wider blast radius than the defect, wanting the per-host-kind treatment 793-rb9u is getting, and that reasoning still holds — overriding a prior agent's deliberate restraint to make my cycle look bigger would be the wrong trade. A cost clock now runs: the warning fires every cold launch until the decision lands, which is the point. EARLIER IN THE CYCLE, two skips with named reasons rather than manufactured work: 821-73es's remaining slice is re-measuring my own correction, and this host cannot — experts-probe reports l1=unreachable, embed endpoint not answering, no index; and 919-vvyv's deferred one-line forge-plan.sh embed-model fix is ALREADY DONE at line 877 with a 919-vvyv D1 comment. That is the THIRD packet this week whose named remaining item was already complete — 245 P4, 821-73es Tier A, now this. Checking first has paid three times. Direction: forge-local-experts-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831.
- 2026-08-31T22:20Z `ad4a84773` Release v56.8.31.3 STABLE — merged PR #102 to main, prerelease flipped (no re-cut; blessed assets promoted verbatim). Linux artifact: https://github.com/8007342/tillandsias/releases/download/v56.8.31.3/tillandsias-linux-x86_64
- 2026-08-31T23:00Z  (pending)  525 EC3 closed — the residual its 2026-07-29 event named — but NOT where that event expected, and the difference is the finding. The event said it "needs a new named reason in lib-inference-state.sh's closed vocabulary". I refused to add one. THE PROBE CANNOT PRODUCE IT: lib-inference-state.sh runs in the FORGE, speaking plain HTTP to inference:11434/api/tags, while the TLS failure is on the INFERENCE container's OUTBOUND path through squid's bump. The forge does not mount the models volume and no other channel crosses, so a reason token there would have had NO PRODUCER — the same no-caller shape this epic found four times last week, authored knowingly this time rather than discovered later. Disambiguation went where the evidence is: the engine classifies its own pull failures into pull-failed-tls-untrusted / -unreachable / -not-found / -other at both preload sites, and the trust case additionally prints that an agent probing it will see inference_reason=no-models which is NOT an empty cache. The last three lines of real output are echoed with each verdict, because a classifier that hides its evidence is a nicer-looking swallow than the bare "|| true" it replaced. Vocabulary comment and no-models detail now name the procedure: podman logs tillandsias-inference | grep reason=pull-failed. Fixture 12/12 in build.sh, pinned against the shapes Go's crypto/x509 AND OpenSSL actually emit (they differ; ollama surfaces both), mutation-tested both ways — folding TLS into generic reddens five arms, always-answering-TLS reddens the negative control, and a non-trust failure must not carry the trust remedy or the remedy is noise. NOT VERIFIED ON A LIVE ENCLAVE and next_action says so: pinned against recorded output shapes, not a pull that actually failed here — experts-probe reports l1=unreachable on this host. Direction: forge-local-experts-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260831.
- 2026-09-01T01:01Z  (pending)  702-6jza D3+D4 closed from the linux lane, D1/D2 left to macOS with reasons, then RELEASED. Each fix falsified by reverting it, per the packet's fifth criterion. D3: build_forge_agent_run_args_with_vault called spec.interactive().tty() and set no TERM/COLORTERM, so podman injected its default TERM=xterm and downgraded the palette on a live TUI — the 2026-07-27 fix had been applied to ONE caller of a shared builder (OpenCode) and never carried to Claude/Codex/Antigravity/Maintenance. Now pinned in the builder itself; test asserts all five modes, mutation-tested. THE NEGATIVE CONTROL CAUGHT MY OWN BUG: a prompt-driven Codex run must claim NO TTY and pin no terminal identity, and my test failed because I passed the prompt into the vault_secret parameter — the signature is (.., debug, vault_secret, prompt) and I had guessed rather than read it. That is the same "verify the data source" error as the --list-live one, caught this time by an arm I wrote for a different purpose. D4: THE PACKET NAMED ONE FILE AND FIVE HAD IT — claude, codex, opencode, opencode-web, antigravity, ten sites all redirecting fd 1 only and leaving fd 2 on the container tty to write over a live TUI. That is D3's shape repeated inside the same packet: a defect found on one lane of a repeated pattern is a defect on the pattern. All ten now 2>&1, guarded by scripts/check-backgrounded-jobs-redirect-stderr.sh in build.sh, four arms including a SCOPE CONTROL that a non-backgrounded fd1-only redirect is not flagged, and reverting one real site takes the tree from ok:10 to violation:2. NOT TAKEN: D1's fix (ED+CUP in attach_client.rs MODE_RESET) is in cross-host SHARED scope so I could write it, but its criterion verifies by launching a lane and confirming row 1 with no banner above — that needs a macOS window, and making an unverifiable edit to a shared file to look productive is the wrong trade. D2's code is macOS-owned outright. next_action records both, plus a DO-NOT-RE-DERIVE note on the three refuted hypotheses the packet calls the expensive part. Direction: harness-mcp-expert-validation by routing (new UTC day rotated the epic), seed silverblue-ryzen7-amd+nvidia-lenovinha-20260901.
- 2026-09-01T03:10Z  (pending)  404 all four exit criteria met with the mock evidence the packet explicitly accepts (live run is order 405). GENERALIZED, NOT COPIED: the full-vs-smoke decision and the de-escalation-only override moved to scripts/lib-e2e-mode.sh, sourced by both launchers, because a copied launcher is exactly what I fixed twice yesterday in 702-6jza (D3 a TERM fix on one lane of five, D4 a stderr redirect on one entrypoint of five). A shared budget whose rule exists twice is a budget that drifts. Checked BEFORE refactoring that litmus-forge-e2e-rate-limit-shape greps the exact literal invocation line inside the opencode launcher, so each lane keeps its own "tillandsias . --<harness> --prompt" written out in full — which also lets a reader see which flag a lane launches without an indirection. EC2 proven BEHAVIOURALLY, not by grep: a codex full run is executed against a stub binary, after which the OPENCODE lane resolves mode=smoke — a private codex limiter class would leave opencode resolving full, so the arm fails precisely when the budget doubles. THE GATE CAUGHT ME TWICE AND WAS RIGHT BOTH TIMES. 634-39ik refused four of my litmus steps for pinning literal source expressions with no negative control — asserting how a line is written rather than what it does, the exact class I have spent the week finding, authored by me this time; rewritten so every step executes a launcher against a stub tillandsias on PATH, with a positive control without which the refusal arm is satisfied by a lane that refuses everything. Then 660-ryhn refused the new litmus as "an executable assertion NOTHING has ever run" until it was bound in litmus-bindings.yaml and actually run — the no-caller class, enforced against me by a guard someone else left behind. That is the second time this week a check written by a stranger caught me. ALSO: a mutation that fails to apply looks exactly like an arm with no teeth — two of my three mutation probes silently did not match, and I only learned the arms were sound by asserting the mutation had landed first. Codex full mode deliberately does NOT assert MO-FULL: that surface is order 614-2gqx's and this lane has never been shown to emit the marker. Direction: harness-mcp-expert-validation by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260901.
- 2026-09-01T05:05Z  (pending)  759-vceg EC1+EC2 landed, EC3 left as the named follow-on, then RELEASED. AUTHENTICATION IS NOT AUTHORIZATION: the login flow checked non-emptiness, gh auth status and a Vault round-trip — all authentication — and never asked whether the identity may PUSH. A fine-grained PAT missing Contents write authenticates perfectly and is denied at push time, which is how the 2026-08-15 "Permission denied" surfaced hours into a work cycle, long after seeding. The gate now sits exactly between authentication and persistence, inside the SAME ephemeral container, so the token still never reaches host disk, argv or env. Three PURE helpers made the seam tractable — origin parser, probe argv builder, and verdict — all unit-testable without podman, which is precisely what macuahuitl said the hard part would be. Their release note also warned the cited spans had drifted; re-finding by symbol landed immediately, third confirmation this week that citation rot is navigation failure, not staleness. An UNPARSEABLE probe answer REFUSES rather than assuming permission, and a missing upstream says so on stderr rather than skipping silently — a silent skip is indistinguishable from a passed check to whoever reads the output, the same ambiguity the packet exists to remove. Six tests, two negative controls, one positive control, mutation-tested both ways. THREE OF MY OWN ERRORS, all caught by running rather than reasoning: called a capture helper that does not exist; wrote the unparseable test with unwrap_or_else, which panics on Err and asserted the exact opposite of intent (code right, test inverted); and inserted my test block INSIDE another test's doc comment, which clippy caught. AND A FOURTH, WORSE BECAUSE IT WAS A REPEAT: I put backticks around a tool name in a shell-passed next_action and the shell EXECUTED it, splicing gh help output into the ledger field. Third time this session against a rule I recorded myself. next_action is LWW so I rewrote it from a quoted heredoc and verified zero matches; a pushed commit message would not have been recoverable. Direction: fail-loud-diagnosis-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260901.
- 2026-09-01T07:15Z  (pending)  958-b36m filed, implemented and closed — the coordinator assignment from the defect I caused. Three gates stood over every litmus file and none asked the RUNNER whether it could run one: valid YAML (933-4gm8), correctly bound (660-ryhn), behaviour-asserting steps (634-39ik) — three green checks over a file no runner could enter. run-litmus-test.sh now has --parse-only, which runs the SAME parse the real run does, returns after the existing refusal checks, and executes nothing (verified by a marker a step would touch, left absent). The binding gate calls it per NEWLY-BOUND name, diff-scoped. Option (a) on the coordinator's ruling and mine: a reimplemented parser would assert the GATE's idea of the format and could go green while the runner refuses the file, strictly worse than no gate. THE CORPUS SCAN'S NUMBER IS WORTH MORE THAN ZERO WOULD HAVE BEEN: first run found 2 bound-unparseable, and BOTH are phase:retired — deliberately shelved, never executed, harmless. Counting them would have been crying wolf, the exact defect I fixed in the stranded sweep where a number that looked like abandonment measured something else. Advisory now skips retired and reports nothing: zero live bound-but-unrunnable, so the line starts life proving quiet. Two files listed but not gated: litmus-status-check-stack-verification.yaml (retired, unbound) and litmus-no-dangling-removed-component-refs.yaml, which is not a litmus file at all (top-level command:/args:, no name:) and is unbindable by construction. COST MEASURED AND REMOVED RATHER THAN IMPOSED: the 394-file sweep costs 36s, a 29% tax on a 126s gate paid by every host every cycle; it now runs only when the registry or a litmus file actually moved — 36s to 1.9s untouched. THREE GUARDS CAUGHT ME WHILE I BUILT THE FOURTH: bash-dialect on an empty-array expansion fatal only on the bash 3.2 macOS ships, pin-claims on my fixture's synthetic names reading as unbacked verification claims, and expression-pinning earlier in the lane. I assembled the synthetic names from a variable rather than spelling them, which is the precedent that guard sets in its own header. Direction: fail-loud-diagnosis-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260901.
- 2026-09-01T09:00Z  (pending)  759-vceg EC3 NOT MET and deliberately not claimed, then RELEASED. I took the cycle to build the hermetic mocked-gh litmus and measured that the criterion costs far more than it reads. Mocking gh is the easy half: the gate sits DOWNSTREAM of vault bootstrap and a full container preflight, so driving tillandsias --github-login with a stubbed podman (TILLANDSIAS_PODMAN_BIN is a real seam) never reaches the probe — it dies at "auth preflight failed: tillandsias-vault is not running" after 20+ podman calls. Reaching the gate hermetically means faking a healthy Vault with TLS on 127.0.0.1:8201, which is reimplementing the enclave, and a fixture that large is a second implementation to keep in sync — the drift shape this corpus hit twice this week. BUILT INSTEAD, covering the gap unit tests genuinely could not: an ORDERING test on run_provider_login's own body asserting the verdict is reached BEFORE the vault write, using the same source_window pattern idiomatic_podman_launch_paths_do_not_bypass_shared_layer uses, plus a scope control proving the window is one function and has not swallowed the helper definitions. Move the write above the probe and every pure-function test still passes while the defect returns whole — that is the property no unit test can see. Mutation-tested. METHOD NOTE, second time this week: my first mutation reported NOT DETECTED and the mutation had simply not applied; I only learned the arm was sound by asserting the occurrence count dropped 8 to 7 first. A mutation that fails to apply is indistinguishable from an arm with no teeth, and checking that it landed is now the habit. DECISION HANDED UP rather than taken: either restate EC3 to what is cheap (decision by unit tests, wiring by the ordering test, container boundary by the LIVE login 759-vsaj already runs on three platforms), or keep it and file the Vault-faking harness as its own packet. I did not restate my own criterion to match what I delivered — that is the move I refused on 950-icqy and it would be worse here, where I wrote the code being judged. Direction: fail-loud-diagnosis-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260901.
- 2026-09-01T11:00Z  (pending)  808-zrzz decided and closed: NARROW THE STOP RULE, do not move the gate. Premise verified before deciding — build.sh's _run_litmus_phase post-build e2e is real and litmus:opencode-prompt-e2e-shape is phase:post-build size:e2e, so Step 1's --ci-full --install genuinely launches a forge. Section 1 subsumes section 4; the runbook's four steps were never four independent things. The stop rule now discriminates: build/CI/install/path/version failures still forbid destruction (no valid local build was installed, so destroying leaves nothing to re-provision from), but a post-build FORGE-LANE failure alone does not — build, install and version probe all succeeded, and "a valid local build was installed" is exactly what the version probe already asserted. The incident tally supports it precisely: pre-build 307 PASS / 0 FAIL, post-build 10 PASS / 1 FAIL with the forge lane the only failure, i.e. everything the build gate is nominally for was green while the idempotence test sat unreachable. REFUSED THE OTHER PERMITTED OPTION and said why in the skill itself: moving the forge litmus into section 4 would silently drop forge coverage from every NON-e2e --ci-full run, and the packet itself calls the lane a real gate the operator's cycles depend on. Narrowing the coupling costs nobody coverage; relocating the gate trades one problem for another and should be an explicit fleet decision. Also corrected section 4, which presented the lane as a fresh step: it now records that the step already ran inside section 1, that re-running it against the RE-PROVISIONED substrate is a different and stronger claim (hence still worth doing), and that a section-1 forge failure must not be read as "section 4 was never attempted". A run that continues past a red lane must record forge_lane: FAIL and file the failure as its own packet — otherwise it is indistinguishable from a clean run. Direction: fail-loud-diagnosis-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260901.
- 2026-09-01T13:10Z  (pending)  618-i4s6 closed: the scoping was ALREADY DONE, the negative fixture was not, and it took THREE controls before one had teeth. The runner's podman preflight is already narrowed — and by a better discriminator than the packet proposes: not "preconditions mention podman" but "a command: line actually INVOKES podman", plus a Linux-only platform gate and a backend:fake exemption. Its own comments carry the history: macOS added the platform gate after an un-gated preflight blanket-ENV-FAILed 35 source-shape checks on darwin (96%->72%), Windows tightened a whole-file grep to command lines after a failing podman shim reddened every grep-shape litmus. Fifth packet this week whose named work was already complete. WHAT WAS MISSING: litmus-podman-preflight-diagnosis-shape had ONE step pinning how the preflight DESCRIBES a broken podman, and nothing pinning WHEN it fires — so both narrowings, each costing a lane a day, were unprotected and both existing fixtures stay green if the trigger is re-broadened. THE THREE CONTROLS, each green for a DIFFERENT wrong reason: (1) methodology-accountability never says "podman" at all, so a re-broadened whole-file grep would not fire on it either — mutation applied, verified landed, arm still passed; (2) cross-platform-compilation, the exact case the runner's comment names, is not a selectable bound spec_id, so the run printed "no litmus tests matched filter" and the arm asserted absence-of-ENV-FAIL over an EMPTY selection, a green from nothing; (3) enclave-network — selectable, mentions podman, invokes it zero times — fails loudly under the re-broadening. Guarded against both rot paths: the arm refuses if its control stops mentioning podman, refuses if the run selected nothing, and proves the stub podman was broken and first on PATH so a green is scoping rather than luck. All three were found only because I now verify a mutation APPLIED before believing NOT-DETECTED — third time this week that habit changed the conclusion. Direction: fail-loud-diagnosis-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260901.
- 2026-09-01T15:00Z  (pending)  828-h7kw premise CORRECTED, nothing deleted, released. The packet says six exit_pause traps are inert and deletable ("none of which can fire"). FIVE ARE; ONE IS NOT, and deleting all six would have removed working behaviour on the one path where it matters. The trap is armed before the exec everywhere, so exec destroys it on the SUCCESS path — but exit_pause was never for the success path and its own comment says so: "a successful exec <agent> replaces the shell, so the trap never fires on the happy path". It covers FAILURE exits. entrypoint-forge-antigravity.sh is live at its require_antigravity failure branch: require_antigravity failing after three install attempts prints a block naming the proxy allowlist hostname, then exit 1, trap armed, no exec between — so the trap fires, holds the window, and the operator can read the hostname they need. Counts after the trap arms: antigravity 1 bare exit, the other five 0, none of the six sets -e. Verified the mechanism experimentally rather than by reasoning: a scratch script with an EXIT trap dying before its exec printed TRAP FIRED. The packet is also inconsistent with itself — title says five, context says six. SEQUENCING: "the six become deletable once the wrapper lands" holds ONLY if the wrapper holds the window on FAILURE exits too; if it holds only on clean exit (the bug in question), deleting the traps silently removes failure-path hold and the regression appears exactly when someone is reading an error. NOT IMPLEMENTED and why: the operator settled the route (a $TERMINAL holding wrapper, not the gsettings key, because it leaves no trace) and its acceptance test is to watch a window survive a clean exit with readable scrollback and leave no residue. That needs a desktop session this host does not have. I could have written it blind; shipping an unobservable GUI change as done is the same move as claiming a criterion I did not meet. Also did NOT substitute command-wrapping for the operator's chosen route, though it is a plausible third option — theirs to choose. SKIPS with named reasons: 759-vceg awaits the coordinator's EC3 ruling; 811-28eh needs a host running the inference container under sustained load and this one reports l1 unreachable. TRIAGED, not touched: 944-jaef looks stranded to the sweep but is not abandoned — its remaining closure is the operator's live click-through session, exactly the human-judgement case the no-bulk-close rule protects. Direction: fail-loud-diagnosis-milestone by routing, seed silverblue-ryzen7-amd+nvidia-lenovinha-20260901.

- 2026-09-01T17:22Z  33d65d73c  832-me6z: bounded the tray broadcast write with a 5s SO_SNDTIMEO deadline applied at register_control_subscriber, the only door into the broadcast set; a stalled subscriber is now evicted and logged instead of wedging every future broadcast to every other subscriber. Two mutation-tested arms; the wedge arm was reshaped onto a watchdog thread so the regression fails in 60s instead of hanging the gate. Direction: fail-loud-diagnosis-milestone (v0.5).
- 2026-09-01T19:05Z  7a939989e  665-zddn: classified the stale/unresolvable NVIDIA CDI launch failures in classify_typed_launch_failure, after measuring that the failure exits 127 — the classifier's 125-only gate would have made every new arm unreachable. Also corrected the packet: --replace has recreated the container since 2026-07-12, so the "stale Created container is reused" premise is false and EC2's recreate strand was already satisfied. Five mutations, each asserted to land; behavioural litmus step asserts 4 arms ran, not just passed. EC4 (durable Linux breadcrumb) left unclaimed. Direction: fail-loud-diagnosis-milestone (v0.5).
- 2026-09-01T21:00Z  3577aa430  665-zddn: EC4 — the Linux launcher now writes a durable host breadcrumb (<state_dir>/launch-failures.log) from format_observed_launch_failure, naming the failed service, the typed verdict, and the inspect command; bounded to 50 lines, carrying no stderr or argv. Litmus step asserts the helper has a CALLER, since a helper with no caller is the defect EC1 nearly shipped. Not yet observed on a real launch failure — stated in the packet. Direction: fail-loud-diagnosis-milestone (v0.5).
- 2026-09-01T23:05Z  097f40c8e  888-miiy: verified criteria 1-3 and criterion 4's NO-endpoint branch at gate level on this endpoint-less host (fixture 9/9, litmus spec 24/24, skipped_engines=spec.answer with the fail-open control still red). Criterion 4's WITH-endpoint half left OPEN and host-bound. Filed 963-vmbi for the real blocker: the FIRST spec-index build is all-or-nothing, so ~60min across two attempts persisted nothing — scoped to resumability, explicitly NOT to trading away the atomic publish. Also noted 667-se87 as host-bound (its coredump is not on this host). Direction: fail-loud-diagnosis-milestone (v0.5).
- 2026-09-02T01:05Z  be0638250  702-6jza: implemented D1 (SCREEN_HOME = ED2+CUP at the ATTACH boundary only) after finding that the criterion's literal wording — "add ED+CUP to MODE_RESET" — ships a regression, because MODE_RESET is also written at TEARDOWN and would erase the finished session. Five tests / five mutations incl. a wiring guard and a regression guard that catches the naive fix. NOT closed: verifying the rendering needs a macOS window. Direction: harness-mcp-expert-validation epic, v0.5.
- 2026-09-02T03:05Z  d56cfe571  freshness audit (order 372 standing class), verdict=updated on openspec/specs/subdomain-routing-via-reverse-proxy/spec.md: three drifts corrected against the live runtime — the binding requirement pinned 127.0.0.1:80/proxy:80 (real: in-container :8080, host port from the 80->8080->--port fallback, loopback-only invariant intact); the routing table is dynamic.Caddyfile merged with base.Caddyfile, not a whole-config Caddyfile; and the reload requirement specified a POST to the Caddy admin API that CANNOT work and must not be made to — the admin port is deliberately unpublished, so satisfying it would have opened a reconfiguration hole. Stamp verified to register (coverage delta=+1, freshness-next advanced). Batch (harness-mcp-expert-validation) was otherwise unworkable here: 702-6jza+317 macOS-bound, 405 needs a live paired forge run with provider budget, 468 gated on an operator sign-off open since 2026-07-28 — noted on the packet. Direction: harness-mcp-expert-validation, v0.5.
- 2026-09-02T05:00Z  bc1c3bed4  468 OBSOLETED (audit-dispose, not sign-off): all three legs of capture->Vault->inject->refresh already ship
- 2026-09-02T05:35Z  (pending)  coordinate (macuahuitl): 811-28eh CLAIMED for this host — the root-cause half needs a GPU host that can hold the inference container under sustained embedding load, and this is the one that can; diagnosis half landed 3807164f4. 956-llei rung landed (c6ec9a3cc, dfa01f243): kill-time adjudicator diffs the runner's own-cgroup cpu.pressure; four closure rungs stay open. Post-restart pass b83d8e730 (checklist step 3 corrected, daily gate stamped, crons re-armed). BOARD CORRECTION: lenovinha landed a packet every cycle from 17:22Z through 05:00Z while the coordinator's entries said "all siblings idle" — sibling-BRANCH drift was zero, but lenovinha commits straight to linux-next; idle was misread from the wrong instrument. — device flow landed 4edc24946 on 2026-07-14, NINE DAYS BEFORE the packet was filed. The cited TUI-smear fallback no longer reproduces: restore_auth returns 2/3 naming the login command and lib-common sets `set -euo pipefail`, so the entrypoint dies before any agent launches uncredentialed (verified by reproduction, after nearly misreading a `set -uo` that lives INSIDE a heredoc as live code). The prescribed setup-token/single-token mechanism also conflicts with the documented "extracting a single token would break refresh" design. Deliverable doc tombstoned, not deleted. Withdrew the operator sign-off request I had raised last cycle as unnecessary. Direction: harness-mcp-expert-validation, v0.5.
- 2026-09-02T06:55Z  4a1d16da0  405 readiness slice, no provider spend: found that the obvious sequential paired run is SILENTLY INVALID — full-meta is rate-limited once per 4h per host and both launchers RECORD on full, so lane 1 runs full and downgrades lane 2 to smoke, and the two modes send DIFFERENT prompts. Exit criterion 1 requires the same prompt. Corrected invocation recorded and verified in both directions via --print-mode (both full without TILLANDSIAS_E2E_FORCE_MODE=smoke, both smoke with it; the override is de-escalation-only and cannot be abused). Live run still needs operator budget authorisation — not taken autonomously. Direction: harness-mcp-expert-validation, v0.5.
- 2026-09-02T17:40Z  `d721e9946`  Release v56.9.2.1 STABLE — merged PR #103 (linux-next → main, 163 commits), VERSION bump PR #104 (d6d3e3ed9), tagged, workflow_dispatch run 33654705874 green (Linux 57 min, macOS 14, Windows 6; 72 min total), 32 assets, flipped prerelease=false + latest. Gate: ci-full green on f9f383256 at 08:50Z after two host-state reds (cold keep-id layer copy of the rebaked forge image; stale guest staging from a bumped --install run) were traced and cleared. Session stalled ~08:50–16:15Z between gate and cut (no host suspend in the journal). Linux artifact: https://github.com/8007342/tillandsias/releases/download/v56.9.2.1/tillandsias-linux-x86_64
