# macOS tray build findings — 2026-07-16

Generated by `/build-macos-tray`. One section per build run. Sibling hosts
should grep `SECTION_KIND` to see whether the macOS build is green on a
given day; section bodies surface anything that affects cross-host work
(rare unattended regressions, install-script edge cases, smoke surprises).

trace: .claude/skills/build-macos-tray/SKILL.md (the skill that wrote this)
       plan/steps/20-macos-tray-v0_0_1.md (the macOS tray v0.0.1 ledger)
       plan/issues/osx-next-work-queue-2026-05-25.md (work queue)

### 20260716T073735Z — ok

- agent_id: macos-Tlatoanis-MacBook-Air-fable5-20260716T073735Z
- head_sha: dd34cd8a
- version: 0.3.260716.5
- build_run_id: 20260716T073735Z

**Build**:
- duration: ~102s compile total (tray 19.17s + guest x86_64 42.06s + guest aarch64 40.19s, cargo zigbuild)
- tarball: tillandsias-tray-0.3.260716.5-macos-arm64.tar.gz (26.51 MiB, sha256 49a6afa8748c6bbdd6bbc9bc8458663cc3c89c3601c5ea3e478ea51b878749f6)
- codesign verify: pass (valid on disk, satisfies Designated Requirement)
- entitlement com.apple.security.virtualization: present
- freshness gate: pass — `--version` reports `git dd34cd8a` (clean, matches HEAD)
- guest bundle: both musl binaries embed 0.3.260716.5 (orders 342 + 382 now shippable to the guest)

**Autonomous smoke**:
- `--diagnose --json` exit: 0
- `--diagnose --json` keys present: [exe_path,image_root,in_app,initrd_bytes,initrd_present,kernel_bytes,kernel_present,manifest_pin_aarch64_qcow2,provisioned,release_tag,rootfs_bytes,rootfs_present,version]
- detached launch: alive-after-3s (auto-boot spawned worker; staged embedded guest binary at ~/src/.tillandsias/guest-bin)
- SIGTERM round-trip: clean-SIGTERM-exit

**Install**:
- target: ~/Applications/Tillandsias.app AND /Applications/Tillandsias.app
  (both user-owned copies refreshed in the same run; the week-long staleness
  this run fixes lived in the /Applications copy while the skill only ever
  updates ~/Applications — dual-install divergence is a standing hazard,
  see Findings)
- backup made: yes — .bak present in both locations
- post-install diagnose schema match: yes

**Findings** (free-form; what regressed, what surprised, what to investigate):
- STALENESS ROOT CAUSE (goal-relevant): the installed app on this host was
  built 2026-07-08 (git ed769a1c-dirty) — a full week stale — while osx-next
  advanced through orders 342 (clone isolation) and the linux-next merge
  (order 382). Because the tray bundles + stages the guest headless at every
  VM boot (`~/src/.tillandsias/guest-bin` beats the release download in
  fetch-headless.sh), a stale installed tray silently pins the GUEST to
  week-old headless code too. Any "order N COMPLETED (live proof)" claim made
  from a dev-tree bundle does not update what the operator's menubar app runs.
  Iteration ask recorded below.
- Litmus §4 filter contract: `scripts/run-litmus-test.sh` matches SPEC names
  (`macos-native-tray`), not litmus names — the skill's suggested
  `litmus:macos-tray-diagnose-cli-surface` filter matches nothing (exit ✗ "no
  litmus tests matched"). Result with the correct filter: 6/6 executed PASS,
  1 skip. Skill body §4 needs the corrected invocation.
- bash `declare -A` caveat from the skill did NOT trigger (runner works on
  this host).

**Cross-host visibility note**:
- Green run; no sibling impact. Guest binaries for BOTH arches now embed
  0.3.260716.5, so the macOS lane can live-test order 382's forge-uid gitdir
  handoff (filed from Hy3/WSL2 evidence) before it ships in a release.

**Next iteration ask**:
- Add an install-freshness probe to the skill pre-flight: compare installed
  `--version` git SHA against HEAD *before* building, and file
  `stale-install` when the gap exceeds a day — a week of silent skew is what
  blocked the in-forge goal on this host.
