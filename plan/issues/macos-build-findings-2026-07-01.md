# macOS tray build findings — 2026-07-01

Generated by `/build-macos-tray`. One section per build run. Sibling hosts
should grep `SECTION_KIND` to see whether the macOS build is green on a
given day; section bodies surface anything that affects cross-host work
(rare unattended regressions, install-script edge cases, smoke surprises).

trace: .claude/skills/build-macos-tray/SKILL.md (the skill that wrote this)
       plan/steps/20-macos-tray-v0_0_1.md (the macOS tray v0.0.1 ledger)
       plan/issues/osx-next-work-queue-2026-05-25.md (work queue)

### 20260701T225151Z — build-failed

- agent_id: macos-Tlatoanis-MacBook-Air-claude-fable-5-20260701T225151Z
- head_sha: b2dd73ad
- version: 0.3.260630.1
- build_run_id: 20260701T225151Z

**Build**:
- duration: 2s (failed during `cargo build -p tillandsias-macos-tray`)
- error: E0425 — `diagnose.rs:450` calls `tillandsias_vm_layer::vsock_exec::exec_interactive`, which does not exist in any committed tree

**Findings**:
- Violation commit `1325bea9` added `exec_guest_interactive_main()` to
  `diagnose.rs`; its `vsock_exec::exec_interactive` counterpart only ever
  existed in that session's *uncommitted* working tree. Remediation commit
  `dc668287` removed the base64 blobs but missed this orphan, so every
  commit from `1325bea9` through `b2dd73ad` fails to compile the macOS tray.
- Nothing dispatched or tested the orphan; removed in `81a0478c`.

**Cross-host visibility note**:
- Linux/Windows CI never saw this because `tillandsias-macos-tray` is only
  built on macOS. If CI ever grows a cross-platform `cargo check` of the
  tray crates, this class of half-committed breakage gets caught at commit
  time instead of at the next macOS build.

**Next iteration ask**:
- None; fixed in `81a0478c` (same day, see next section).

### 20260701T225838Z — ok

- agent_id: macos-Tlatoanis-MacBook-Air-claude-fable-5-20260701T225838Z
- head_sha: 81a0478c
- version: 0.3.260630.1
- build_run_id: 20260701T225838Z

**Build**:
- duration: 12s
- tarball: tillandsias-tray-0.3.260630.1-macos-arm64.tar.gz (1.58 MiB, sha256 3d35676dd05ff5544296b803b13c279c7fe78aeea0aa824556495c2cc535f0b9)
- codesign verify: pass
- entitlement com.apple.security.virtualization: present
- freshness gate: pass (`--version` embeds git 81a0478c == HEAD)

**Autonomous smoke**:
- `--diagnose --json` exit: 0
- `--diagnose --json` keys present: [version,in_app,exe_path,image_root,rootfs_present,rootfs_bytes,kernel_present,kernel_bytes,initrd_present,initrd_bytes,release_tag,manifest_pin_aarch64_qcow2,provisioned]
- detached launch: alive-after-3s
- SIGTERM round-trip: clean-SIGTERM-exit
- stderr during smoke: only auto-boot lines ("spawning worker", "VM is running"); no errors

**Install**:
- target: ~/Applications/Tillandsias.app
- backup made: yes — .bak present
- post-install diagnose schema match: yes

**Findings** (free-form; what regressed, what surprised, what to investigate):
- Phase 3d (`fe66b10d`) is in-tree but NOT in any published release. Latest
  release is v0.3.260630.1, cut from `739d6b1d` — its headless still has
  `label=type:vault_container_t` with no `ensure_vault_selinux_module`, i.e.
  the broken Phase 3c state.
- The macOS guest's installed headless is **v0.3.260628.1** (fetched at
  first provision; sha256 prefix 08e4e3e2c86edafb), which predates the vault
  bootstrap itself (`65bd9751` is not in that tag). `fetch-headless.sh`
  exits early when the binary exists, so guests never pick up new releases
  without a manual delete + refetch or a re-provision.
- Consequence: "Phase 3d macOS unblock" is source-complete but not
  runtime-complete. The github-login / list-cloud-projects flows cannot work
  on macOS until (1) a release ships a headless containing `fe66b10d` and
  (2) the guest refetches it.

**Cross-host visibility note**:
- Affects all platforms, not just macOS: any guest provisioned before a
  given release keeps its original headless forever (fetch-headless.sh is
  fetch-if-missing). Worth a linux-next decision: version-pin the headless
  fetch to the recipe's release tag and refetch on mismatch.

**Next iteration ask**:
- Cut a release containing `fe66b10d` (macOS host is running
  /merge-to-main-and-release this session), then refetch guest headless and
  re-verify the vault path end-to-end on the enforcing VZ guest.

### 20260705T010145Z — secure-wire helper OK, dev-launch blocked by missing entitlement

- `cargo check -p tillandsias-macos-tray`: pass.
- `cargo test -p tillandsias-macos-tray`: pass.
- `cargo run -p tillandsias-macos-tray`: launches the binary, then auto-boot
  fails with `Invalid virtual machine configuration. The process doesn’t have
  the “com.apple.security.virtualization” entitlement.` after `Auto-boot:
  spawning worker`.
- The macOS tray now routes the GitHub login, list-cloud-projects, opencode,
  status poller, shutdown request, and PTY attach control-wire opens through a
  reusable secure-or-raw helper. Flag `TILLANDSIAS_SECURE_CONTROL_WIRE=on`
  upgrades the socket with `tillandsias-secure-channel` once the guest side is
  ready; default remains OFF.

**Follow-up filed**:
- `plan/issues/macos-dev-launch-entitlement-2026-07-05.md`

### 20260705T012018Z — secure control wire blocked by stale guest image

- Packaged `Tillandsias.app` launches and auto-boots the VM.
- `--github-login` without the secure flag reaches the credential prompts.
- `TILLANDSIAS_SECURE_CONTROL_WIRE=on ./dist/Tillandsias.app/Contents/MacOS/tillandsias-tray --github-login`
  fails with `secure control wire handshake failed: early eof` after the host
  reaches the control wire.
- This indicates the guest image currently in the VM is still plaintext-only
  and must be refreshed to a headless build that includes the secure responder
  path.

**Follow-up filed**:
- `plan/issues/headless-secure-control-wire-image-refresh-2026-07-05.md`
