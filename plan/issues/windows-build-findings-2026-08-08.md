# Windows tray build findings — 2026-08-08

Generated by `/build-windows-tray`. One section per build run. Sibling hosts
should grep `SECTION_KIND` to see whether the Windows build is green on a
given day; section bodies surface anything that affects cross-host work
(rare unattended regressions, install-script edge cases, smoke surprises).

trace: skills/build-windows-tray/SKILL.md (the skill that wrote this)
       plan/steps/windows-next-thin-tray.md (the windows tray v0.0.1 ledger)

---

### 20260809T003000Z — degraded

- agent_id: windows-esmeralda-claude-fable-20260808t235000z
- head_sha: 63784ddb (windows-next fast-forwarded onto origin/linux-next; local
  low-end fixes uncommitted at section time, committed this session)
- version: 0.4.260804.1
- build_run_id: 20260809T003000Z

**Context**: NEW low-end field host (Esmeralda: Intel N100 4×E-core, 16GB,
Windows 11 26200, WSL 2.7.10→2.7.11 mid-session). First Tillandsias dev
bring-up on this machine: rustup + VS Build Tools + Windows SDK installed
(dev-time only; a partial winget kill left the SDK libs missing —
LNK1181 dbghelp.lib — fixed by re-running the VS bootstrapper). Guest musl
binary built via scripts/with-wsl2-builder.sh after adding
musl-gcc/musl-devel/musl-libc-static to the builder init (ring build script
hard-requires x86_64-linux-musl-gcc; init list gap now closed in the script).

**Build**:
- duration: 27m 07s cold workspace release (2 jobs, shared with guest build);
  incremental with staged guest asset ~5m
- exe: target/release/tillandsias-tray.exe (7,664,128 bytes bare;
  ~21MB with embedded 13.7MB musl guest binary)
- guest binary: target-guest/tillandsias-headless-x86_64-unknown-linux-musl
  v0.4.260804.1 (source-matched, `--features listen-vsock`, via
  tillandsias-build distro + musl-gcc)

**Autonomous smoke** (post-install diagnose, final reconcile-capable build):
- `--diagnose --json` exit: **0** (fully healthy)
- version: 0.4.260804.1; build_commit: 63784ddb
- guest_version: 0.4.260804.1 (was null under the release install)
- install_path: %LOCALAPPDATA%\Programs\Tillandsias\tillandsias-tray.exe
- distro registered / running: true / true
- WSL / OS: 2.7.11.0 / 10.0.26200.8894
- control wire reachable: **true** (phase=Ready, podman_ready=true,
  last_event="Securing Vault"); non-elevated tray reached Ready via the
  socat bridge on handshake attempt 1 at 2026-08-09T00:37:04Z, ~3 min after
  launch, after reconcile healed a bare interrupted-provision guest
  (configure step + embedded-binary injection + vsock_loopback modules-load)

**Install**:
- target: %LOCALAPPDATA%\Programs\Tillandsias\tillandsias-tray.exe
- backup made: no — first install on this host (previous state was the
  release zip run directly from Downloads)
- post-install diagnose schema match: yes (16-key schema parsed)

**Findings** (what the release failed on, what regressed, what to investigate):
- RELEASE FIELD FAILURE (v0.4.260804.1 from Downloads): the zip itself is
  CORRECT (release pipeline hard-gates the embedded source-matched guest).
  Failure was unreconciled guest state: the July-22 local build had
  half-provisioned the distro (stale v0.3.260712.1 guest fetched via
  releases/latest, retired NoNewPrivileges unit hardening breaking rootful
  podman uid_map, no vsock_loopback for the non-elevated socat bridge), and
  the adopt fast path (order 418) never re-injects wiring. Non-elevated
  trays therefore looped `handshake: early eof` forever. Fix: adopt-path
  reconciliation (packet 620-ujyc) — guest headless version checked against
  WORKSPACE_VERSION on every adopt; mismatch/absence re-runs the idempotent
  injection. Durable + runtime-detected, not a hand-fix: the one manual
  modprobe done during diagnosis was invalidated by later WSL restarts and
  the injected modules-load.d entry is the persistent mechanism.
- NEW low-end defect: ensure_base_packages' 300s ceiling fired while dnf was
  legitimately mid-transaction (in-guest DNS verified healthy; the orphaned
  dnf finished after the tray declared failure). Bumped to 1500s
  (620-ujyc scope). Interrupted-provision adoption is now also healed:
  reconcile detects a non-systemd-booted guest (/run/systemd/system absent)
  and completes configure_recipe_distro before injection.
- UNKNOWN EVENT (unresolved): the pre-existing `tillandsias` distro was
  unregistered between 23:52Z and 23:58Z with no unregister command in the
  session history; strongest suspect is the WSL 2.7.10→2.7.11 self-update
  ("finishing an upgrade" hang observed 22:26Z). Watch for recurrence on
  WSL updates.
- Low-power: TILLANDSIAS_NO_LOCAL_INFERENCE gate landed (packet 620-ca7g);
  set as a user-level env var on this host. Bridge EOF attribution landed
  (620-cine). Runtime-vs-dev-time dependency audit packet filed (620-duta):
  this session added ZERO runtime dependencies — rustup/VS BT/musl-gcc/
  tillandsias-build distro are dev-only.

- LOGIN DEFECT (operator-reported live, packet 620-9xpg): tray GitHub Login
  crashed instantly; its error text carried terminal-hostile escapes that
  crashed a neighboring terminal on copy. Two causes: (a) is_running_in_vm()
  never fires in WSL guests (hostname is the WINDOWS name, not
  tillandsias-vm) so bare login lanes probed vault at the TLS-hanging
  127.0.0.1:8201 port-forward; (b) the login terminal argv (inline
  bash -lc script) goes through wt.exe's re-parser. Fixed:
  /etc/tillandsias/in-vm marker (injected + reconcile-deployed) checked by
  is_running_in_vm; injected github-login.sh wrapper (bare-path argv, output
  tee'd to /root/.cache/tillandsias/github-login-last.log); tray GithubLogin
  intent launches the wrapper. Control evidence: dummy-token flow with the
  enclave URL completes in seconds and 401s cleanly at GitHub validation.
  Known limitation filed: same-VERSION wiring changes need a forced
  reconcile (guest binary removal) to redeploy.
- Tray icon visible in the taskbar overflow (^) but ABSENT from Windows
  Settings > Taskbar > Other system tray icons (operator report) —
  app-identity registration watch-item for the next version bump.

**Cross-host visibility note**: windows-next carries the low-end fixes
(wsl_lifecycle reconcile + timeout, transport_windows EOF attribution,
headless inference gate, with-wsl2-builder musl deps); packets + deliverables
ride linux-next as plan/index.d/20260808t235000z-620-lowend-windows.yaml +
four plan/issues notes. Linux/macOS siblings: the headless inference gate and
the builder-init musl deps are shared-scope.

**Next iteration ask**: pick up 620-duta (diagnose-visible reconcile state +
vsock_loopback preflight assert + import-surface litmus) and the follow-ups
listed in plan/issues/low-end-no-local-inference-gate-2026-08-08.md
(spec delta, accel-probe auto-detection, PRELOAD plumbing, KEEP_ALIVE knob).
