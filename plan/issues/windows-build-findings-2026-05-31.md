# Windows tray build findings — 2026-05-31

Generated by `/build-windows-tray`. One section per build run. Sibling hosts
should grep `SECTION_KIND` to see whether the Windows build is green on a
given day; section bodies surface anything that affects cross-host work
(rare unattended regressions, install-script edge cases, smoke surprises).

trace: skills/build-windows-tray/SKILL.md (the skill that wrote this)
       plan/steps/windows-next-thin-tray.md (the windows tray v0.0.1 ledger)
       plan/issues/windows-build-findings-2026-05-30.md (yesterday's ledger
       — covers the 4-tick workspace-VERSION arc + the CLI surface
       completion + install/diagnose script enrichment + build-script
       stderr-wrap fix + install lifecycle + tray tooltip + --help
       OPTIONS/ENVIRONMENT sections)

---

### 20260531T080000Z — ok (README.md Windows section: "Coming soon" → real install + diagnostics block)

- agent_id: windows-bullo-claude-opus-20260531T080000Z
- head_sha: 734c3874 (last night's --help OPTIONS + ENVIRONMENT additions
  push; linux-next at 89483d0d unchanged this tick; osx-next finally moved
  b4a45622 → 05b47860 after ~24h stall with macos-build-findings entries
  + a new e2e gap-4 discovery)
- version: 0.2.260528.1 (linux-next's VERSION file still pre-main-bump;
  next sync cycle will absorb main's `0.2.260530.1`)
- build_commit: 734c3874
- build_run_id: 20260531T080000Z

**Sibling context**:
- **osx-next stall broken**: 05b47860 reactivates with a back-fill build-
  findings entry + a brand-new `macos-e2e-findings-2026-05-30.md` file
  surfacing **gap-4: tray bundle dies between t=3s and t=30s** after a
  successful recipe fetch. This is macos-tray-specific (NSStatusItem
  doesn't survive the post-fetch worker exit per the hypothesis). Not
  cross-host actionable; flagged for macos-host's next iteration.
- linux-next: no movement this tick (velocity-cooldown continues per the
  prior tick's no-op work-queue entries).
- merge-tree clean.

**Change made**: README.md `### Windows` section was materially stale —
said "Coming soon" + "Planned via WSL2" even though
`tillandsias-tray-0.2.260530.1-windows-x64.zip` shipped yesterday as a
real, signed release artifact (the v0.2.260530.1 release CI ran 39m38s
and published 22 cosign-signed assets including the Windows zip).
First thing GitHub visitors see when evaluating the project for Windows
use; "Coming soon" was wrong by a release event.

Files touched (Windows-owned only — left macOS section untouched per
host boundaries):
- `README.md` § `### Windows`:
  - Header line: `**Coming soon.**` → `**Beta** (since v0.2.260530.1,
    2026-05-30).` with a brief description of what it actually is
    (Win32 NotifyIcon tray around a WSL2-hosted Fedora utility VM
    running the same headless tillandsias + podman enclave as Linux).
  - **New Install block**: 4 PowerShell command lines covering the
    full lifecycle (`-Launch`, `-Provision -Launch`, `-Uninstall`,
    `-Purge`) with inline comments explaining each. Pointer to the
    GitHub releases page for the zip.
  - **New Requirements line**: Windows 10/11 + WSL2 prerequisite.
  - **New Diagnostics paragraph**: enumerates the 4 diagnostic surfaces
    (`tillandsias-tray.exe --diagnose --json`, `--help`, two PowerShell
    consumer scripts) + pointer to the cheatsheet.

**Build**: N/A (docs-only change; binary unchanged from prior tick's
`734c3874`).

**Smoke**: `git diff README.md` shows clean removal of the 4-line
"Coming soon" + "Planned" block and addition of the 18-line
real-state block. No content-leakage to other sections (macOS
unchanged, Linux unchanged, downloads table unchanged).

**Litmus**: `windows-native-tray` 7/7 PASS (no code changes; README
isn't pinned by the windows-tray litmus).

**Findings** (free-form):
- The README description now matches the actual user experience. The
  cross-host visibility note: macOS's section (`### macOS`, line ~128)
  still says "Coming soon" — that text mirrors the Windows staleness
  prior to this fix. The macOS-host should consider updating it
  symmetrically; their release artifact
  (`tillandsias-tray-<version>-macos-arm64.tar.gz`) also ships, and
  today's gap-4 finding (NSStatusItem dies post-fetch) is honest
  context for a "Beta" tag like the Windows side now carries.
- The Install block uses the same `scripts\install-windows.ps1` flag
  patterns the install script's own docblock + this morning's
  install-windows.ps1 `-Purge` documentation use, so a user reading
  the README → install script → cheatsheet path sees consistent flags
  + semantics throughout.
- The Diagnostics paragraph cites the 4-surface story (`--diagnose
  --json`, `--help`, `tray-diagnose.ps1`, `diagnose-windows.ps1`) —
  the same 4 surfaces yesterday's ledger documented as "mutually
  consistent + accurate" after the script-enrichment arc. README
  reflects current state.

**Cross-host visibility note**: README change is Windows-section-only.
Recommend (not blocking) macOS-host updates its parallel `### macOS`
section in a follow-on commit. Linux section unchanged (already
accurate).

**Next iteration ask**: N/A (SECTION_KIND=ok). The user-facing entry
points for Windows (README → install script → CLI surface →
diagnostics → cheatsheet) are now all current and self-consistent.
