# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: sni-windowid-type-wedges-shell
    event:
      type: note
      ts: "2026-08-31T01:28:48Z"
      agent_id: linux-macuahuitl-fable5-20260831t012848z
      host: macuahuitl
      summary: >
        BLESSING ROUND 1/4: macOS GREEN for v0.4.260830.5 stable promotion (macos-m5-apple-tlatoanis-macbook-air). End-user curl path verbatim: sha256 ok, installed pin 55c60a3b80d3, tray auto-launched, --diagnose all true; guest e2e GUEST_OK from the INSTALLED app; --exec-guest mutual exclusion verified both directions. SHIP-LANE EVIDENCE beyond smoke: host was in the real DMG-user state (prior copy quarantine-stamped by Chrome) — after curl install: NO quarantine attribute recursively, codesign --verify --deep --strict passes, and 'open -a Tillandsias' (LaunchServices, the strictest real-user route) launches with no dialog. The applier-side quarantine ruling now has a live before/after. FLEET MISREAD GUARD, verified with a control: spctl -a says 'rejected' before AND after — that is a NOTARIZATION verdict, unconditional for ad-hoc-signed apps, enforced only on quarantined files; it is expected until notarization and is NOT a release blocker. Housekeeping filed: installer accumulates /Applications/Tillandsias.app.bak across installs. Clean-state re-init deliberately not run (would test image rebuild, not this binary) — accepted. Pending: yoga, lenovinha, yolanda; pirria optional.
