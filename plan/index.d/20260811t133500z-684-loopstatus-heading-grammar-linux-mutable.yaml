# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260811t133500z
packets:

  - packet_id: loop-status-fragment-grammar-drops-single-hash-cycle-headings
    order: 684-awhm
    status: ready
    desired_release: v0.5
    priority: p3
    title: "loop-status fold silently DROPS fragments whose Cycle heading is `# Cycle` not `## Cycle`; hand-heals stay compaction-malformed"
    kind: bugfix+dx
    deliverable: loop-status parser tolerance for `# Cycle` (or a loud reject) + a producer that cannot emit the wrong level
    depends_on: []
    pickup_role: linux
    capability_tags: [loop-status, fold, grammar, dx, fleet]
    context: >
      THIRD occurrence fleet-wide of the same papercut (2026-08-09 macos-drain,
      2026-08-11 macos-overnight — both healed by hand on this host). A
      loop_status fragment whose cycle section opens with a SINGLE `# Cycle`
      instead of `## Cycle` is SILENTLY SKIPPED by the fold: the whole cycle's
      status vanishes from `tillandsias-plan loop-status` and shows as
      `malformed:` in loop-status-fragments. macОС's ENTIRE overnight
      close-out (13 closures) was invisible until healed this cycle. Worse:
      hand-healing by prepending `#` (to make `## Cycle`) makes verify go
      green (the ## ACTIVE RELEASE count line is then read) but the fragment
      STILL reads `malformed:` for text-compaction — so it lingers un-foldable.
      Two real defects: (1) the fold should either TOLERATE `# Cycle` (agents
      naturally write one #) or reject it LOUDLY at write time, not silently
      drop it; (2) loop-status-append (the sanctioned producer) should be the
      only way to add a fragment so the level cannot be wrong, and a hand-file
      should fail a shape check rather than fold-green-but-compact-malformed.
    exit_criteria:
      - a fragment with a `# Cycle` heading is either folded correctly or rejected with a named error at write time (not silently dropped)
      - hand-authored fix fragments no longer read malformed-but-verify-green; a litmus pins the fold's heading tolerance
    events:
      - ts: "2026-08-11T13:35:00Z"
        event: filed
        summary: >
          Filed on the third fleet occurrence while healing macOS's overnight
          close-out fragment; the silent-drop is the defect, the heading is the
          symptom.
