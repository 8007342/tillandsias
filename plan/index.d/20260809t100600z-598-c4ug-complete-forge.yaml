# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-20260809t100600z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# ORDER 598-c4ug (worker-skills-teach-the-pre-crdt-plan-write-path) CLOSED with
# HEAD-verified evidence. The packet carried a `completed` event from
# 2026-08-07T08:07:43Z but its status field was never flipped, so the selector
# kept offering it as ready. This fragment verifies the changes are present at
# HEAD and records completion.
events:
  - packet_id: worker-skills-teach-the-pre-crdt-plan-write-path
    event:
      type: completed
      ts: "2026-08-09T10:06:00Z"
      agent_id: forge-20260809t100600z
      host: forge
      summary: >
        CLOSED with HEAD-verified evidence (the 2026-08-07T08:07:43Z completed
        event was never folded into a status update). (EC1) Every instruction
        file that tells an agent to write the ledger names the fragment path:
        skills/advance-work-from-plan/SKILL.md teaches `plan/index.d/`
        fragments via `tillandsias-plan append-event` (claim :137-140, progress
        :196, completed :288-289) and never instructs in-place plan/index.yaml
        writes; skills/coordinate-multihost-work/SKILL.md routes status writes
        through `tillandsias-plan loop-status-append` and reads the folded view
        (:19, :60, :130, :144-146). (EC2) scripts/check-plan-ledger-readers.sh
        scan set now includes skills/ and methodology/ (grep for
        'plan/index.yaml' over scripts/ images/ crates/ skills/ methodology/),
        so litmus:no-unprotected-plan-ledger-readers can see instruction files.
        (EC3) The linux-next-checkout contradiction is resolved in ONE
        direction: advance-work-from-plan uses the host table's <active-branch>
        (linux-next / windows-next / osx-next per uname) guarded by
        scripts/check-committable-branch.sh, and methodology/multi-host-
        development.yaml plan_write_discipline now says platform agents commit
        plan fragments directly to their ACTIVE platform branch.
status:
  - packet_id: worker-skills-teach-the-pre-crdt-plan-write-path
    field: status
    value: completed
    ts: "2026-08-09T10:06:00Z"
    host: forge
packets:
  - packet_id: worker-skills-teach-the-pre-crdt-plan-write-path
    order: 598-c4ug
    status: completed
    desired_release: v0.5
    role: methodology
    pickup_role: linux
    priority: p0
    tags:
      - crdt
      - skills
      - shared-state
      - data-loss
      - cross-platform
    title: "The skill every host runs still teaches in-place plan/index.yaml edits. The Windows and macOS cycles would corrupt shared state by following it."
    outcome: >
      Every instruction file an agent reads first teaches the fragment write
      path (plan/index.d/, plan/loop_status.d/ via append-event and
      loop-status-append) instead of in-place ledger edits, and the
      ledger-reader litmus can see instruction files at all.
    next_action: >
      None — packet complete. The remaining pre-CRDT write path is the CLI
      append-event handler itself, tracked separately by order 600-c266.
    exit_criteria:
      - "Every instruction file that tells an agent to write to the ledger names the fragment path and next-order — PROVEN: skills/advance-work-from-plan/SKILL.md (fragments + append-event, claim/progress/completed), skills/coordinate-multihost-work/SKILL.md (loop-status-append + folded reads)"
      - "check-plan-ledger-readers.sh scan set includes skills/ and methodology/ — PROVEN: scan globs scripts/ images/ crates/ skills/ methodology/"
      - "The linux-next-checkout contradiction is resolved in ONE direction and the losing text is deleted — PROVEN: host-table <active-branch> + check-committable-branch.sh; plan_write_discipline teaches active-platform-branch writes"
    events:
      - type: filed
        ts: "2026-08-03T20:30:00Z"
        agent_id: linux-mutable-macuahuitl-opus5-20260803
        host: linux_mutable
        summary: Filed as p0; compaction had just rewritten the base, so an in-place edit now collides whole-file.
      - type: completed
        ts: "2026-08-07T08:07:43Z"
        agent_id: "linux-mutable-macuahuitl-gemini-20260807"
        host: linux_mutable
        summary: "DONE: instruction files updated to enforce fragment overlay and active branch writes; check-plan-ledger-readers.sh scan expanded to skills/ and methodology/."
      - type: completed
        ts: "2026-08-09T10:06:00Z"
        agent_id: forge-20260809t100600z
        host: forge
        summary: >
          Re-verified at HEAD and status flipped to completed (the earlier
          completed event was never folded into the status field, leaving the
          packet claimable as a ghost). Evidence for all three exit criteria
          confirmed against the working tree at HEAD.
