# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: periodic-deslopification-sweep
    field: next_action
    value: 'DECIDE FIRST, then sweep: archiving makes a row UNQUERYABLE (`no packet matches`), which destroys the anti-re-proposal reasoning that the convergence ruling says makes aggressive retraction safe — and `obsoleted` is in the archiver terminal set. Pick (a) exempt obsoleted, (b) make plan/archive readable by the fold, or (c) exempt obsoleted + treat archive as cold storage; I lean (c) now, (b) later. Cheap to decide now at 8 obsoleted vs 564 completed; expensive after the first 577-packet sweep. THEN: the archiver itself is FIXED and safe (ready set preserved, gated in build.sh). THEN build check-deslop-due.sh: trigger is EVENT-COUNTED ONLY, (current_order - order_at_last_sweep) >= 200, floor 48h, NO calendar ceiling — a ceiling interacts fatally with the kill rule, since a quiet fortnight fires two low-yield sweeps and red:two-sweeps-zero-confirmed retires the reconciler for doing nothing wrong.'
    ts: "2026-08-19T23:40:29Z"
    host: linux
