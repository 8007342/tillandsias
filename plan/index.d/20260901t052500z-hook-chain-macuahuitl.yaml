# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: forge-guest-hygiene
    event:
      type: note
      ts: "2026-09-01T05:25:00Z"
      host: macuahuitl
      summary: >
        CHAIN CORRECTION (esmeraldinha, mtime-proven): forge LAUNCH installs
        only prepare-commit-msg and post-commit (02:53) — neither blocks
        commits. The commit-blocking openspec pre-commit reached the global
        hooks dir at 03:33, mid-litmus, because hook-install-portability's
        installer step wrote it there. So the litmus-contamination arm of
        this packet is not hygiene — it is THE DELIVERY VECTOR of the
        commit-blocking defect: the suite installs the hook globally, the
        hook then breaks every foreign repo including the suite's own later
        fixtures, which is why failures appeared mid-suite. A guest that
        never runs the suite may never break. Cross-reference
        litmus-low-end-instrument. DELIVERY of the v3 guard to running
        guests: NOT by git pull alone (launch never runs install-hooks.sh —
        esmeraldinha grep-verified lib-common and /usr/local/bin) but by
        pull + the next build.sh invocation (build.sh:531 runs the
        installer, which resolves the global hooksPath by design and the
        v2 marker is upgrade-recognized) — or by the same litmus step that
        delivered the defect, now delivering the cure. Fresh images ship
        correct once rebuilt. The packet must not close on 'pull fixes it';
        it must verify a running guest's global pre-commit reads v3 after
        the guest's next gate run. Esmeraldinha holds her guest in the
        reproducible defect state (v2 hook live) for this packet's fixer;
        her 4-minute field verification (pull, install-hooks, negative
        control, 11/0 re-run) is banked as the acceptance script.
