# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: in-forge-findings-die-with-the-container
    field: status
    value: implemented
    ts: "2026-08-15T00:20:42Z"
    host: linux_immutable

events:
  - packet_id: in-forge-findings-die-with-the-container
    event:
      type: note
      ts: "2026-08-15T00:20:42Z"
      host: linux_immutable
      summary: >
        Gate + startup-context wiring + litmus landed; exit criteria 2/3/4 met. Criterion 1 (end-to-end demonstration through a real forge teardown) is this cycle's in-forge validation.
