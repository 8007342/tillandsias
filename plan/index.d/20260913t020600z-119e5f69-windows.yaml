# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-cheatsheet-tier-check-protects-neither-windows-host-by-two-independent-routes
    field: title
    value: 'check-cheatsheet-tiers refuses falsely on esmeraldinha: resolve_target_binary prefers a stale PE32+ over the fresh ELF on a shared Windows/WSL checkout with interop enabled, and that Windows binary cannot resolve the /mnt/c repo root it is handed'
    ts: "2026-09-13T02:06:00Z"
    host: windows

events:
  - packet_id: the-cheatsheet-tier-check-protects-neither-windows-host-by-two-independent-routes
    event:
      type: note
      ts: "2026-09-13T02:06:00Z"
      host: windows
      summary: |-
        route B retracted: the cargo-absent SKIP lines were a fixture assertion, not a production verdict; yolanda validated 228 cheatsheets
