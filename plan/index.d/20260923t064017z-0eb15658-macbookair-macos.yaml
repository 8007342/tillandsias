# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: fixtures-print-their-own-scenario-total-as-a-literal-so-an-arm-that-stops-running-is-reported-as-the-old-count
    field: next_action
    value: |-
      Start with the three already-derived files as worked examples (test-script-exec-bits.sh on trunk, and test-skills-single-source.sh / test-litmus-bindings.sh once #157 and #158 land) — each shows the shape and the arm that proves it. Then take the twelve one at a time. Cheapest first check per file: delete or comment one arm and see whether the printed total moves.


      [2026-09-23T06:40:17Z macbookair-macos] Released by macbookair 2026-09-23. LEFT: implement the survey's verdicts (da587a8dd, f93df1f0c) — each fixture derives its scenario total from the arms that actually ran rather than printing a literal; a pin alone does not rescue a literal.
    ts: "2026-09-23T06:40:17Z"
    host: macbookair-macos
