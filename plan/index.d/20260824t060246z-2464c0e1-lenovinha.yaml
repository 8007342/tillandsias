# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: builder-toolbox-init-set-too-small-for-the-dispatch-pattern
    field: next_action
    value: 'STATE 2026-08-24 (lenovinha). DONE: init set carries jq/yq/rg/openssl (+8s); run-litmus-test.sh materialises the toolbox yq once into target/litmus-runtime/bin BEFORE the runtime bin joins PATH (that dir holds the podman wrapper and toolbox shells out to podman, so the reverse order fails silently); and a degraded run now prints warn:litmus-degraded-no-yq once, verified to be absent when yq is present. RETRACTED: my 2026-08-23 claim that the grep fallback under-selects nine tests. Controlled runs give Total 28 / executed 26 in EVERY arm — selection is unaffected by yq; that measurement varied the runner version and yq together and blamed yq. The real harm from a missing yq is WRONG STEP ANSWERS, two confirmed: skills-canonical-and-mcp-first STEP 6 reports a present methodology rule as missing, and added-fragment-parse-gate STEP 8 returns empty. TO REPRODUCE a genuine no-yq run you need an EXECUTABLE ''toolbox'' stub that exits 1, early on PATH — deleting the shim does not work (it re-provisions) and a non-executable shadow does not work (command -v skips it). REMAINING: the ~50 SCRIPT callers, split as before — dev-host scripts take the documented dispatch; shipped diagnostics (tray-diagnose.sh, diagnose-macos-provision.sh) run where no builder toolbox exists and need a different answer, decided before sweeping.'
    ts: "2026-08-24T06:02:46Z"
    host: lenovinha
