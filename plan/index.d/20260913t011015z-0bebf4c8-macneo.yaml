# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-ledger-has-more-than-one-place-to-look-and-every-reader-looks-in-one-so-five-writes-landed-somewhere-nothing-consults
    field: next_action
    value: 'REMAINS (after macneo content test, origin/linux-next e8abb32a2): 1. SALVAGE REF — DELETION STILL UNPERFORMED AND STILL NOT AUTHORISED. The relay landed the three restored fragments (verified byte-identical on trunk). The coordinator content test over the five files the tip 94f12eeb7 carries above merge-base 85e67c300 returns 4 byte-identical, 1 differing (scripts/test-ledger-write-reaches-its-reader.sh, +207/-10), so the gate as stated says NOT DELETABLE. Per-line accounting of the 10 salvage-only lines is on the packet event: 7 are the denominator guard trunk kept and widened 4->7 fixtures, 2 are renamed signature comments on report_ready_but_landed, 1 is the loose extraction grep trunk deliberately replaced with the anchored group-2 match. Nothing unique survives only on the ref, so the EVIDENCE says deletable while the GATE says not — and the agent holding the delete command does not get to override its own gate on its own reading, which is exactly the failure this packet already logged once. BLOCKED ON: coordinator ratifying the refined criterion (per-line supersession accounting, since byte-identity is unpassable by construction for any file trunk evolved after the salvage). Delete only after that ratification. 2. SWEEP/ROLLUP (unchanged, still untouched): promote the 46-hit live-lane list into the fleet queue so a host budget re-statuses or completes them; readers fire on ^((fix|close|feat)\(ORDER\)) and the sweep judges prominence per packet.'
    ts: "2026-09-13T01:10:15Z"
    host: macneo

events:
  - packet_id: the-ledger-has-more-than-one-place-to-look-and-every-reader-looks-in-one-so-five-writes-landed-somewhere-nothing-consults
    event:
      type: note
      ts: "2026-09-13T01:10:15Z"
      host: macneo
      summary: |-
        content test run: 4/5 identical, 1 superseded; gate says not deletable, deletion withheld pending ratification
