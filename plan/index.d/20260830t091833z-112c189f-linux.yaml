# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: network-architecture-audit
    field: progress_summary
    value: 'GPT verification 2026-07-14 failed NA-01, NA-03 and NA-06. Two revision slices have landed since: yoga 2026-08-25 took section 1/1.1 (CONTAINER matrix — every FACT re-verified true, every CITATION drifted), lenovinha 2026-08-30 took section 3 / NA-03 (a criterion that FAILED). Section 3 found real content drift, not only rot: ForgeLaunch''s DEPS carries SIX edges, not the five the draft lists — Service::NixCache joined via order 801-vm4p — and NixCache is a Service variant the draft omits entirely; separately the order-252 convergence changed shape from two call sites to one reached transitively. Ten of ten section-3 citations were wrong. STILL TO REVISE before re-verification: NA-01 and NA-06 (the other two failed criteria), and sections 2/4/5/6/7 which carry ~68 remaining line-number citations. STANDING RECOMMENDATION from both slices: NA-05''s exit criterion mandates ''file:line'' references, which is the requirement that produced the rot; it should say ''file:symbol''. Changing an exit criterion is the packet owner''s call.'
    ts: "2026-08-30T09:18:33Z"
    host: linux

events:
  - packet_id: network-architecture-audit
    event:
      type: note
      ts: "2026-08-30T09:18:33Z"
      host: linux
      summary: >
        record the two landed revision slices and what re-verification still needs
