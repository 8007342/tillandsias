# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-cycle-that-relocates-to-a-clean-clone-can-never-attest
    event:
      type: note
      ts: "2026-09-03T08:06:58Z"
      agent_id: linux-macuahuitl-fable5-20260903t080658z
      host: macuahuitl
      summary: |-
        THE HOST REFUSED TO CLOSE ITS OWN PACKET WITH A MARKER AS EVIDENCE, and this is the fourth time tonight it declined the same shortcut. This cycle exits UNATTESTED and that is CORRECT: the fix does not reach backwards, no boundary was snapshotted at its start, and snapshotting now then verifying immediately would compare a tree against itself and emit a valid-looking proof of nothing - the exact 651-2x5s failure. It was trivially available and would have closed the packet with a marker as its own evidence. In the host words, the record should show the defect cost three cycles rather than two-and-a-flattering-round-number. It also landed that reasoning INTO the skill, so the next agent reaching Finalization without a boundary is told what not to do rather than left to invent it. A PATTERN WORTH NAMING, four instances in one session, in two distinct shapes. SHAPE ONE, code that exists and nothing calls: fetch_recipe_artifact (979-3kfb) and compute_memory_ceiling_mb (981-n5vx), both called only by their own tests while a spec requirement claimed discharge. SHAPE TWO, a capability that exists and the instructions never connect: land-on-platform-branch.sh, which the meta-orchestration skill named ZERO times while teaching the hand-rollable ritual it replaces, and now this - per-checkout boundary stamps that two skill steps never joined. Shape two is the one that produces proposals to BUILD what is already built, and the cheap counter is to check for the capability before designing its replacement.
