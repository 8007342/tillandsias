# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: an-install-resets-and-reprovisions-the-local-state-on-every-platform-and-then-runs-the-platform-initialisation-itself-so-a-curl-bash-or-irm-iex-install-is-also-the-repair-for-a-broken-local-state
    event:
      type: note
      ts: "2026-09-20T11:56:52Z"
      agent_id: linux-macuahuitl-fable5-20260920t115652z
      host: macuahuitl
      summary: |
        HOW THIS ROW'S PRE-FIX CLAIMS WERE COMPILED, by their author (the coordinator, 2026-09-20T12:20Z), because three of the four per-platform claims were wrong when RUN and the implementers cannot speak to the method. The row was filed on 2026-09-20 from the operator's ruling, the day's three smoke reports, and READS of scripts/install.sh, scripts/install-macos.sh and scripts/install-windows.ps1; the one MEASURED pre-fix fact was the absence of any reset in the installers (true on all three). Every other claim was a read: "Linux install.sh installs the launcher and stops" (wrong: --init runs from install.sh:294 since 63fe7b494, and set -euo pipefail propagates its status; pirria, 4b9e6867e), "the Windows installer defers provisioning to the next launch" (wrong: it launches provisioning fire-and-forget at line 504 and exits 0 without waiting or reporting, which is a STRONGER defect and a different fix; yolanda), and "never unregisters an existing distro" (true, the only unregister is the -Purge uninstall path; yolanda). All three errors point the same way: understating what already works and missing what actually fails, which is what a read of an installer produces and a run does not. Rule for the filer, from this row: every pre-fix claim on a row is tagged READ or RUN in the fragment; a READ claim is a question for the implementer to answer by execution before it is used as a premise, never a pre-fix result. The ruling on the skip affordance also moved this pass: the TILLANDSIAS_INSTALL_SKIP_RESET variable the three arms and the coordinator had agreed is WITHDRAWN (pirria): it would have forked the existing opt-out, TILLANDSIAS_DESTRUCTIVE_RESET_OK=0, which --reset-guest already honours through destructive_reset_allowed(); run_reset_state honours the same variable, announces the skip when it says no, and reprovisions through plain --init; every installer calls --reset-state unconditionally and only after the new binary is in place and executable; the dispatch's exit 1 on any Err is the one exit convention across the three arms.
