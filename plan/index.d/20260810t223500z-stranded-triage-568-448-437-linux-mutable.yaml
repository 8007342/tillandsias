# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t223500z
status:
  - packet_id: claude-mcp-config-registration
    field: status
    value: ready
    ts: "2026-08-10T22:35:00Z"
    host: linux
  - packet_id: cheatsheet-image-copy-drift-prevention
    field: status
    value: ready
    ts: "2026-08-10T22:35:00Z"
    host: linux
  - packet_id: forge-src-tmpfs-topology
    field: status
    value: ready
    ts: "2026-08-10T22:35:00Z"
    host: linux

events:
  - packet_id: claude-mcp-config-registration
    event:
      type: triage
      ts: "2026-08-10T22:35:00Z"
      agent_id: linux-mutable-20260810t223500z
      host: linux_mutable
      summary: >
        Stranded-sweep disposition (the 641-e2qa class; windows nudged the six
        linux-role stragglers 2026-08-10): in_progress since filing 2026-08-01
        with ZERO progress events, claim long past the 4h TTL. Exit criteria
        checked against the tree: no Claude-side MCP registration exists in
        images/default (entrypoint-forge-claude.sh unchanged), so nothing is
        done and nothing was lost. Returned to ready. The work statement and
        the B-vs-C mechanism decision in
        plan/issues/claude-mcp-expert-validation-2026-08-01.md remain valid
        and current. Related but distinct: the HOST harness session also
        lacks these MCP servers (this loop falls back to the CLI nightly) —
        that host-side registration is part of this packet's spirit and a
        natural same-mechanism companion for whoever claims it.
  - packet_id: cheatsheet-image-copy-drift-prevention
    event:
      type: triage
      ts: "2026-08-10T22:35:00Z"
      agent_id: linux-mutable-20260810t223500z
      host: linux_mutable
      summary: >
        Stranded-sweep disposition: last progress 2026-07-25, claim expired.
        Criteria not met by the tree (the committed image copy remains
        build-generated and re-drifts). Returned to ready; content stands.
  - packet_id: forge-src-tmpfs-topology
    event:
      type: triage
      ts: "2026-08-10T22:35:00Z"
      agent_id: linux-mutable-20260810t223500z
      host: linux_mutable
      summary: >
        Stranded-sweep disposition: operator-approved 2026-07-20, last
        progress the same day, claim expired; 10h estimate suggests it needs
        an explicitly budgeted session, not a loop slice. Returned to ready.
        NOTE for the eventual owner: its "container memory ceiling" scope now
        overlaps 667-se87's explicit-resource-policy criterion (pids ceiling
        fixed 2026-08-10; memory ceiling remains unset) — land them
        coherently.
