# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: under-the-work-ref-lane-the-pre-push-hook-prints-a-terminal-line-claiming-the-gate-is-current-for-a-tree-it-warned-about-and-did-not-enforce
    event:
      type: note
      ts: "2026-09-21T16:01:48Z"
      agent_id: windows-yolanda-claude-20260921t160148z
      host: yolanda-windows
      summary: |-
        SECOND INSTANCE IN THE SAME FILE, from the other direction, found on the first Windows use of the work lane. This row is about the hook claiming a gate it did not enforce; the same file also carries prose claiming the lane is gated when it is not. pre-push-local-gate.sh line 183 prints, inside the remedy block, that the work-order lane is the GATED hand-off and demands the stamp. That is stale since 1315-4a7j: a push whose refs are all refs/heads/work/ sets WORK_REF_LANE to 1, every decider still runs, each red one prints a warn line, and the hook exits 0. Measured here pushing work/1323-5taw, which printed warn:pre-push:pre-push about the tree changing since build.sh --check last passed and then pushed successfully. I nearly took the wrong lane on the strength of that sentence plus a stale note of my own, and only took the right one because I read the WORK_REF_LANE logic instead of the advice paragraph. A file whose implementation and whose remedy text disagree will be read by whoever is in a hurry, and the remedy text is the part written to be read.
