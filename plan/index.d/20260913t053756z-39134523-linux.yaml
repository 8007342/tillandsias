# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: inference-container-pinned-integrity-and-npu-alignment
    field: status
    value: completed
    ts: "2026-09-13T05:37:56Z"
    host: linux

events:
  - packet_id: inference-container-pinned-integrity-and-npu-alignment
    event:
      type: completed
      ts: "2026-09-13T05:37:56Z"
      host: linux
      summary: |-
        status 'completed' with evidence_refs: scripts/test-inference-engine-pinned-and-npu-honest.sh 12/12 at gate step 255-1118-d3b6, deterministic over 8 runs; 2/12 against the reverted entrypoint with all pin/verify/NPU arms red
  - packet_id: inference-container-pinned-integrity-and-npu-alignment
    event:
      type: note
      ts: "2026-09-13T05:37:56Z"
      host: linux
      summary: |-
        engine pinned to v0.34.0 with per-arch SHA-256 verified before extraction (fail closed on 3 paths); NPU passthrough deferred and the telemetry made honest
