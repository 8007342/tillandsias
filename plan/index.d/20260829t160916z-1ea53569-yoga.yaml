# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: mirror-vault-github-credential-is-denied-by-upstream
    field: next_action
    value: 'operator: confirm the Vault PAT is current, then any host launches a forge lane so the mirror republishes a FRESH authorized verdict (the 2026-08-26T03:13:24Z one on yoga is 3 days stale against a 900s bound)'
    ts: "2026-08-29T16:09:16Z"
    host: yoga

events:
  - packet_id: mirror-vault-github-credential-is-denied-by-upstream
    event:
      type: note
      ts: "2026-08-29T16:09:16Z"
      host: yoga
      summary: >
        fail-loud half landed; closure needs a live verdict, not a stale one
