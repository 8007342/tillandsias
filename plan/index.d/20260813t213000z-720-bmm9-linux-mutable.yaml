events:
  - packet_id: freshness-inventory-subset-source-and-specs
    event:
      type: progress
      ts: "2026-08-13T21:30:00Z"
      agent_id: "linux-mutable-20260813t2117z"
      host: linux_mutable
      summary: >
        All three exit criteria met. The inventory now enumerates the rest of
        the ruled subset: openspec/specs (171 components) joins INVENTORY_PATHS,
        and crates/**/*.rs (206) is enumerated through a separate SOURCE_PATHS
        list because the yaml/yml/md loop was never going to find code. target/
        is excluded by construction — a build output is not a component.
        Denominator 868 -> 1250.
        The measured effect is the reason criterion 2 exists: coverage fell from
        1.3% to 1.0% without a single stamp being lost. A percentage that drops
        because the target got BIGGER reads as decay to anyone looking at the
        report alone. The report now carries denominator_delta on the
        freshness-target line, tracked through a third cache field, and an older
        two-field cache yields denominator_delta=unknown rather than a
        fabricated zero.
        Together with 640-iujb the denominator is now pinned in both directions:
        generated trees may not enter it, ruled-in trees may not leave it.

  - packet_id: freshness-inventory-subset-source-and-specs
    event:
      type: verified-by
      ts: "2026-08-13T21:30:30Z"
      agent_id: "linux-mutable-20260813t2117z"
      host: linux_mutable
      summary: >
        verifiable_closure executed as litmus:freshness-inventory-shape steps 5
        and 6 — step 5 asserts both crates/ and openspec/specs/ contribute
        components (fails loud if either tree is dropped again), step 6 pins the
        denominator_delta field's grammar. Suite 12/12 PASS.
