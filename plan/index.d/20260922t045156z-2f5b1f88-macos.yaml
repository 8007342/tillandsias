# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-probe-proves-a-container-placed-a-model-on-the-gpu-and-still-records-the-container-lane-unverified-so-a-live-gpu-lane-reads-as-present-unscheduled-fleet-wide
    field: progress_summary
    value: |2-

      CRITERION 2 IS OUTSTANDING. It is the criterion that proves the fix MATTERED,
      and a green from the fixture alone would be a one-regime green.

      BLOCKED ON A CAPABILITY, not on a host name: any host with an AMD (or other
      non-NVIDIA) GPU whose INFERENCE CONTAINER can reach the render node, i.e. one
      that produces a render_nodes entry with vantage=container and proof
      reachable-or-placed. This host is macOS/Apple Silicon — PROBE-7 makes Metal
      host-native ONLY and forbids a container lane by spec — so it cannot produce the
      evidence the promotion consumes, in any arrangement.

      WHAT THE RE-RUN MUST DO, on the fixed binary:

        1. re-probe and re-file that host's capability row
             tillandsias-headless --probe-accel          # writes the document
           (or whatever this host's normal probe/contribute path is; the point is the
           row must be REGENERATED by the fixed binary, not re-read)

        2. read it back through the matrix, unchanged:
             tillandsias-plan capability-matrix

      EXPECTED, and this is the whole criterion:
          schedulable: gpu/container/ollama
      instead of
          present-unscheduled: gpu/... (usable; no engine covers lanes [host-native])

      THE MATRIX MUST NOT BE CHANGED TO ACHIEVE IT. Its intersection is already
      correct, and a fix that edits the reader is fixing the wrong component. The
      `reading:` line added for criterion 5 is presentation only and touches no
      intersection.

      WORTH CHECKING IN THE SAME RUN, because it is the cheapest possible regression
      on the negative control: any device on that host WITHOUT a container-vantage
      proof must still read container-lane-unverified.
    ts: "2026-09-22T04:51:56Z"
    host: macos
