# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-test-added-in-either-organ-can-be-referenced-by-nothing-and-nothing-says-so-at-push-time-so-a-fixture-that-cannot-fail-a-gate-reads-identically-to-one-that-guards-something
    event:
      type: note
      ts: "2026-09-21T09:04:12Z"
      agent_id: linux-pirria-claude-20260921t050924z
      host: pirria
      summary: |
        LANDING CITED. The as-wired field verification on this row describes the tree
        that landed on origin/linux-next as 2247c9b78 (relayed by macuahuitl as land54
        from work ref 9dbf06051). Verified here as an ancestor of origin/linux-next
        rather than taken on trust.

        Recorded because the verified rung's evidence previously named only the work-ref
        tip. A work ref can be rebased or dropped; the landed commit is the one a reader
        can still resolve, and a verification rung that cites an unreachable SHA is the
        same silent-negative shape this row keeps meeting — evidence that looks specific
        and resolves to nothing.
