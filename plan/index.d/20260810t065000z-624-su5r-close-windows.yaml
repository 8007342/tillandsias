# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-20260810t065000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

status:
  - packet_id: windows-unstable-channel-installer-validation
    field: status
    value: completed
    ts: "2026-08-10T06:50:00Z"
    host: windows

events:
  - packet_id: windows-unstable-channel-installer-validation
    event:
      type: completed
      ts: "2026-08-10T06:50:00Z"
      agent_id: windows-20260810t065000z
      summary: >
        CLOSED. All five steps recorded with actual output in
        plan/issues/windows-unstable-channel-validation-2026-08-10.md, and the
        packet's two named verdicts are both delivered.

        I said in the earlier partial that this packet must NOT be closed on
        steps 1 and 4 alone, and that was right at the time. What changed: the
        644-a3wj curl smoke then answered step 2's substantive question, and step
        5 was run this cycle. The residual is now only the GUI-visual
        observation, which has its own p0.

        STEP 1 PASS — UNSTABLE banner precedes every download; unstable resolves
        /releases/download/unstable; default resolves /releases/latest/download;
        an unknown channel throws rather than falling back. Verified statically
        AND in the field during 644-a3wj (the installer printed the banner, then
        `sha256: ok`, then installed 0.4.260810.1 / 1496e89f).

        STEP 2 — VERDICT DELIVERED (the packet's second exit criterion): the bare
        tillandsias-tray.exe alias IS usable standalone. Run from a directory
        containing nothing else, --version and --diagnose both exit 0 and it
        reports its own path. NOT the 621-2re2 defect this packet feared.

        STEP 3 PASS — portable ZIP parity: the alias zip and the versioned zip
        share a digest, so the alias is a true copy rather than a re-zip.

        STEP 4 PASS — the 623-iwq4 field regression check, green on all three
        channels including v0.4.260809.2 as the packet requires: 293 bytes /
        3 lines / every line newline-terminated (checked with cat -A, since the
        original defect was a missing trailing newline a plain read cannot show),
        3/3 verify.

        STEP 5 PASS — install-windows.ps1 is pure ASCII: non_ascii=0 on both the
        repo copy and the published one, both 24426 bytes. The v0.3.260723.1
        regression has not recurred. Deliberately did NOT flag install.sh (12)
        or install-macos.sh (2028): the ASCII rule exists for PowerShell's
        re-parse sensitivity, and applying it to POSIX shell scripts would
        manufacture two false positives.

        NO FAIL to file from steps 1-5. Two defects found in adjacent work are
        already filed on their own: 647-i98k (--diagnose DEGRADED race) and
        648-772y (tray/guest version skew).

        RESIDUAL, tracked as 646-qde5 (p0, windows): the GUI-visual confirmation
        — tray icon appears, no flashing consoles. A headless loop can start the
        exe and read an exit code, but an exe that exits 0 while flashing
        consoles would PASS that check, so it is not something this lane can
        honestly assert.

        Closing rather than leaving ready because the selector's urgent override
        was handing this packet to this host every cycle while the only remaining
        work is unactionable here — exactly the anti-circling the batch selector
        exists to prevent.
