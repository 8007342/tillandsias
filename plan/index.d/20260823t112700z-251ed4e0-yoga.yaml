# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: selector-pool-drops-deps-clear-and-unleased
    field: status
    value: completed
    ts: "2026-08-23T11:27:00Z"
    host: yoga

events:
  - packet_id: selector-pool-drops-deps-clear-and-unleased
    event:
      type: completed
      ts: "2026-08-23T11:27:00Z"
      host: yoga
      summary: >
        status 'completed' with evidence_refs: commit 90fc4af66 + litmus:cycle-batch-triage-shape step 23/23 POOL FIDELITY two-sided PASS (run-litmus-test.sh ci-release pre-build instant)
