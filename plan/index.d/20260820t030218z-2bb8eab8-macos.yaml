# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: cargo-test-workspace-stops-at-the-first-failing-target
    field: status
    value: done
    ts: "2026-08-20T03:02:18Z"
    host: macos

events:
  - packet_id: cargo-test-workspace-stops-at-the-first-failing-target
    event:
      type: completed
      ts: "2026-08-20T03:02:18Z"
      host: macos
      summary: >
        status 'done' with evidence_refs: 66a85ead6 — fix(test-signal); cargo test --workspace --lib --no-fail-fast: 1 reported failure -> 7 real, then 1160 passed/0 failed across 15 targets after 833-8u5g+833-u85z
  - packet_id: cargo-test-workspace-stops-at-the-first-failing-target
    event:
      type: note
      ts: "2026-08-20T03:02:18Z"
      host: macos
      summary: >
        no-fail-fast landed in both suite entry points; verified by the 1->7 failure delta and a now-green suite
