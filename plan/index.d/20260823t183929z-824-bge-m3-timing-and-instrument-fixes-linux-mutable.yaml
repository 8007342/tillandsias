# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-23T18:39:29Z.
events:
  - packet_id: spec-expert-refusal-threshold-fleet-decision
    event:
      type: progress
      ts: "2026-08-23T18:39:29Z"
      agent_id: linux-macuahuitl-opus5-20260823T183400Z
      host: linux_mutable
      evidence_refs:
        - "target/index-rebuild/bge-m3-run2/timings.tsv — 313s, batch 64, 21102 chunks, 1024-dim, arity exact"
        - "GPU: peak 83% util, peak non-ollama VRAM 309 MiB against an 800 MiB threshold, contaminated_samples=0"
        - "scripts/refusal-calibration/rebuild-index-with-embedder.sh"
      summary: >
        A FULL bge-m3 INDEX TAKES 313 SECONDS, NOT 2.4 HOURS. The README
        estimated ~2.4h at TILLANDSIAS_SPEC_INDEX_BATCH=1 because batching was
        believed impossible; batching works, and the whole 21,102-chunk index
        rebuilds in 5m13s on a quiet card. The estimate was wrong by roughly
        28x. Measured with 0 contaminated samples — peak non-ollama VRAM 309
        MiB (gnome desktop chrome) against an 800 MiB threshold.

        I ALSO NEARLY REDID FINISHED WORK, and the near-miss is the more useful
        record. next_action item (2) reads "rebuild the index with bge-m3 OR
        qwen3-embedding:4b and re-derive the bands". The bge-m3 half was
        already answered at 03:54 today by this host (d7f2821fd): same 561
        questions, full index per model, band overlap nomic 0.2058 -> bge-m3
        0.2513, margin overlap 0.1104 -> 0.1222. A BIGGER EMBEDDER MAKES
        SEPARATION WORSE. Re-deriving it cost a cycle's GPU time and produced
        no new band data.

        What the re-run DID establish that the 03:54 measurement did not: the
        timing above, and an end-to-end proof of the serialiser. So the open
        half of item (2) is qwen3-embedding:4b alone — 4.0B params, 2560-dim,
        never built. Launched 18:36Z, running at 7,055 MiB and ~79% util.

        THE CAVEAT FROM 03:54 STILL STANDS AND NOW APPLIES TO qwen3 TOO:
        neither index used model-specific query/passage prefixes. That refutes
        "a bigger embedder rescues the threshold"; it does NOT establish that
        these models are weak. Any qwen3 result inherits the same limitation
        and must be reported with it.

  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    event:
      type: progress
      ts: "2026-08-23T18:39:29Z"
      agent_id: linux-macuahuitl-opus5-20260823T183400Z
      host: linux_mutable
      next_action: >
        CAMPAIGN STATE 2026-08-23T18:39Z. FLEET DELIBERATELY STOOD DOWN by the
        operator: only macuahuitl (hourly) and lenovinha (~4h) loop; pirria is
        being armed to a 4h loop; yoga rejoins tonight. esmeraldinha (6h03m),
        yoga (6h23m) and the ephemeral forge (95h) read as SILENT >6h and are
        NOT stalls — do not chase them.

        ITEM (2) IS HALF DONE AND THE OTHER HALF IS RUNNING.
        - bge-m3: ANSWERED at 03:54 (d7f2821fd). Band overlap nomic 0.2058 ->
          bge-m3 0.2513; bigger is WORSE. Do not re-measure it; I did today and
          it bought nothing but a timing.
        - bge-m3 timing, new: full 21,102-chunk index in 313s, not the ~2.4h
          the README estimated. Batching works.
        - qwen3-embedding:4b (4.0B, 2560-dim): LAUNCHED 18:36Z as a background
          job, first build ever attempted. Read
          target/index-rebuild/qwen3-4b/.state — NOT pgrep, which self-matches.

        NEXT, in order:
        (1) When qwen3-4b lands, re-derive the bands against the SAME 561
            relabelled questions and compare to nomic 0.2058 / bge-m3 0.2513.
            If overlap grows again the trend is monotonic and the threshold
            hypothesis is dead at every model size, which closes 824-6qxh.
        (2) THE PREFIX CAVEAT is now the biggest hole in this whole comparison.
            No index has used model-specific query/passage prefixes, and both
            bge-m3 and qwen3 expect them. Everything measured so far refutes
            "a bigger embedder rescues the threshold" but says nothing about
            those models used correctly. Worth its own packet before anyone
            concludes bigger embedders are useless here.
        (3) 846-idhn capabilities channel base representation.

        STANDING COORDINATOR DUTY EVERY CYCLE: merge platform branches into
        linux-next; nobody else has it. Both ahead=0 at 18:35Z.

        FLEET GAP, NOW BEING CLOSED: pirria is in the roster and filing work but
        has NEVER attested. check-mo-full-attestations.sh reports this inside a
        GREEN ok: line at rc=0, so coverage can be incomplete while the gate
        passes. Operator is arming pirria to a 4h loop with the exit contract.

        NOT MINE: 856-s56y (yoga), 857-4kqv (yoga), 860-g798 (yolanda), and the
        low-end tier rows on pirria/esmeraldinha.
      summary: >
        Cycle 2026-08-23T18:34Z. Heartbeat: platform branches ahead=0;
        expire-claims in_progress=3 expired=0; three hosts >6h silent, all
        explained by the operator's stand-down. Landed a bge-m3 timing, caught
        a near-duplicate of finished work, fixed two instrument defects.
