# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: enclave-ca-bundle-lives-in-tmp-so-containers-cannot-restart
    field: next_action
    value: 'Detection landed (scripts/check-enclave-ca-consistency.sh, wired into preflight as +ca-<state>); the CAUSE is not fixed. Criterion 2 — a rebooted host reaches a running proxy without hand-repair — is NOT met. TWO HALVES REMAIN: (a) the bundle still lives in /tmp, so the trigger recurs on every reboot or tmpfiles sweep; (b) ensure_proxy_ca_key_secret refreshes the secret only on the LAUNCH path, so a plain "podman start" — which the health check self-heal at check-enclave-service-health.sh:319 performs, and which its printed remedy recommends — still desyncs the pair. Fixing (a) alone leaves (b); fixing (b) alone leaves the trigger. The health-check self-heal and its remedy text are the specific callers to change. NOTE the live reproduction is GONE: this host was repaired after the detector was verified against it, so the desync is now reproducible only through the script, not on a machine.'
    ts: "2026-09-03T17:20:16Z"
    host: yoga

events:
  - packet_id: enclave-ca-bundle-lives-in-tmp-so-containers-cannot-restart
    event:
      type: progress
      ts: "2026-09-03T17:20:16Z"
      host: yoga
      summary: |-
        status 'Detection landed (scripts/check-enclave-ca-consistency.sh, wired into preflight as +ca-<state>); the CAUSE is not fixed. Criterion 2 — a rebooted host reaches a running proxy without hand-repair — is NOT met. TWO HALVES REMAIN: (a) the bundle still lives in /tmp, so the trigger recurs on every reboot or tmpfiles sweep; (b) ensure_proxy_ca_key_secret refreshes the secret only on the LAUNCH path, so a plain "podman start" — which the health check self-heal at check-enclave-service-health.sh:319 performs, and which its printed remedy recommends — still desyncs the pair. Fixing (a) alone leaves (b); fixing (b) alone leaves the trigger. The health-check self-heal and its remedy text are the specific callers to change. NOTE the live reproduction is GONE: this host was repaired after the detector was verified against it, so the desync is now reproducible only through the script, not on a machine.' (not terminal) with evidence_refs: check-enclave-ca-consistency.sh detected the live desync; preflight now carries +ca-<state>
