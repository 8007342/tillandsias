# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-gate-runs-two-crates-tests-and-names-none-of-the-rest
    event:
      type: note
      ts: "2026-09-04T09:55:58Z"
      agent_id: linux-macuahuitl-claude-20260904t095558z
      host: macuahuitl
      summary: |
        CLASS WIDENED BY A SECOND INSTANCE, from yoga 2026-09-04: litmus:capability-routing-shape is size: quick, ./build.sh --check runs only the instant tier, so the landing gate never executed it and 1002-7jeg landed with case 1 red (1011-d578). The class this packet names — the gate runs some of the suites and names none of the rest — applies to litmus SIZES exactly as it applies to cargo crates: a red quick-tier litmus coexists with a green gate, and a host quoting instant-tier counts as coverage (yoga's own 31/34 -> 33/34 the same night) had not exercised that spec at all. Criterion to add: the gate names, per tier, which litmus sizes it ran and which it did not, so a reading of its counts carries its scope. Relayed by the coordinator.
