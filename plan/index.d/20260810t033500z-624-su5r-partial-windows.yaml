# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-20260810t033500z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# 624-su5r stays READY on purpose. Steps 1 and 4 pass; steps 2 and 3 — the ones
# the packet was actually written for — are untouched. See the event.

packets:

  - packet_id: windows-portable-exe-gui-execution-check
    order: 646-qde5
    status: ready
    desired_release: v0.5
    priority: p0
    title: "Nobody has yet RUN tillandsias-tray.exe on Windows — needs a human at the desktop or a GUI automation channel"
    kind: validation
    deliverable: plan/issues/windows-unstable-channel-validation-2026-08-10.md
    depends_on: []
    pickup_role: windows
    capability_tags: [windows, release, installer, validation, gui, e2e]
    context: >
      Carved out of 624-su5r after cycle 7 completed the parts a non-interactive
      loop can honestly complete (steps 1 and 4: channel resolution and checksum
      integrity, both PASS — see the deliverable).

      Steps 2 and 3 require running the portable tillandsias-tray.exe and the
      extracted zip and observing GUI behaviour: "tray appears, no flashing
      consoles, WSL2 provisioning starts, --diagnose works". Those are
      observations about a desktop session. A headless agent can start the
      process and read an exit code, but an exe that exits 0 while flashing
      console windows or never showing a tray icon would PASS that check — which
      is measuring the wrong thing, the failure class this session has been
      cataloguing all evening.

      So the packet's own headline claim is still true and unchanged: the
      unversioned alias tillandsias-tray.exe, a primary landing-page download
      since 621-2re2, HAS NEVER BEEN EXECUTED.
    request: |
      On a Windows desktop session (a few minutes of clicking, not a large task):

      1. Download https://github.com/8007342/tillandsias/releases/latest/download/tillandsias-tray.exe
         and run it directly — no installer, no unzip.
         Confirm: tray icon appears; NO flashing console windows; WSL2
         provisioning starts; --diagnose works.
      2. Download .../tillandsias-windows-x64.zip, extract, run the tray from the
         extracted directory. Confirm parity with step 1.
      3. If the bare exe CANNOT work standalone because it needs sibling files
         from the zip, that is a REAL DEFECT in 621-2re2. File it and say so
         plainly — do not work around it by documenting the zip as required.
    verifiable_closure: >
      plan/issues/windows-unstable-channel-validation-2026-08-10.md gains a
      steps 2-3 section recording, for each of the bare exe and the extracted
      zip: tray appeared yes/no, console flashes yes/no, WSL2 provisioning
      started yes/no, and --diagnose output.
    notes: >
      Do not close 624-su5r on the strength of steps 1 and 4 alone. The intact
      checksums prove the download path is sound, which means a failure in the
      exe would be in the binary or its packaging — it does not prove the exe
      runs.
    events:
      - ts: "2026-08-10T03:35:00Z"
        event: filed
        summary: >
          Split out by the windows host so the GUI-bound half is visible as its
          own p0 rather than hidden inside a packet whose other half passed.

events:
  - packet_id: windows-unstable-channel-installer-validation
    event:
      type: progress
      ts: "2026-08-10T03:35:00Z"
      agent_id: windows-20260810t033500z
      summary: >
        PARTIAL — steps 1 and 4 PASS, steps 2 and 3 NOT RUN. Deliverable written:
        plan/issues/windows-unstable-channel-validation-2026-08-10.md

        STEP 4 (the 623-iwq4 regression check) PASSES on all three channels.
        SHA256SUMS-windows is 293 bytes / 3 lines with every line properly
        newline-terminated (checked with cat -A, since 623-iwq4 was exactly a
        missing trailing newline merging lines). All three artifacts verify with
        `sha256sum -c`, exit 0, for latest (v0.4.260809.2), v0.4.260810.1, and
        unstable. The alias tillandsias-windows-x64.zip shares a digest with the
        versioned zip, so it is a true copy; unstable and v0.4.260810.1 publish
        identical digests.

        STEP 1 PASSES, verified statically: the published install-windows.ps1 is
        BYTE-IDENTICAL to scripts/install-windows.ps1, default resolves
        /releases/latest/download, 'unstable' resolves /releases/download/unstable,
        an unknown channel THROWS rather than falling back to stable, and the
        UNSTABLE banner (L338-341) precedes the first Invoke-WebRequest (L354).
        Read rather than executed: running an installer to discover which URL it
        resolves is a worse test than reading the resolution, and it mutates the
        host.

        STEPS 2 and 3 NOT RUN and NOT claimable by this loop — they require
        observing a GUI (tray appears, no flashing consoles). Split into
        646-qde5. This packet must NOT be closed on steps 1 and 4 alone: its
        headline claim, that tillandsias-tray.exe has never been executed, is
        still true.

        ALSO: the destructive curl-install e2e (wsl --unregister tillandsias) was
        NOT run. TILLANDSIAS_DESTRUCTIVE_RESET_OK is unset, which the skill reads
        as permission, but this host is the operator's interactive workstation
        and the loop is unattended — and the same host carries the
        tillandsias-build distro the cargo lane depends on. Wiping the runtime
        distro at 03:35 with nobody watching is not a call a loop should make
        silently. Raised for an explicit decision instead.

        SECONDARY: cosign is absent here so the Cosign bundles could not be
        verified (present and well-formed, 10566 bytes, sigstore v0.3). Since
        623-iwq4 shipped a validly signed CORRUPT file, signature and content
        verification are independent — this covers content only.
        TILLANDSIAS_VERSION silently overrides TILLANDSIAS_CHANNEL without saying
        so. /releases/latest/download still serves v0.4.260809.2 because
        v0.4.260810.1 is a pre-release.
