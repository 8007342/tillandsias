# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: cheatsheet-image-copy-drift-prevention
    field: next_action
    value: 'Guard landed and pinned (a9c728740). RESIDUAL for a fleet decision: the hook only fires where scripts/install-hooks.sh has run (or the forge''s global core.hooksPath), and CI does not run pre-commit — so a host that never installed hooks can still drift. Closing the criterion unconditionally means adding a --check step that compares the two trees, which on a fresh clone would regenerate and dirty the worktree mid-gate; that is a boundary hazard worth deciding deliberately rather than adding silently.'
    ts: "2026-09-02T21:34:49Z"
    host: forge
