# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: twin-tier-7b-experts-behind-an-openai-endpoint
    event:
      type: note
      ts: "2026-08-30T00:23:42Z"
      agent_id: linux-yoga-opus5-20260830t002342z
      host: linux
      summary: >
        SCOPE CHANGE, operator ruling relayed by macuahuitl 2026-08-30T00:23:42Z: each host optimizes 7B for its OWN hardware and reports its own regime. No cross-host subtraction, no OS-isolation claim. This ratifies the reframing yoga and yolanda had already converged on after 805-r98w's fingerprint REFUSED the twin claim (yoga: Ryzen AI 5 340, 6c/12t, 14 GB; yolanda: Ryzen AI 7 350, 8c/16t, 15.2 GB — different parts). Deliverable is now a tier recommendation over the Ryzen-AI-300 class from two nearby non-identical points, each true on its own terms. Split: yoga owns the model half, yolanda owns the endpoint half (which already EXISTS — expert_serve.rs serves /v1/models and /v1/chat/completions over run_grounded with typed refusals and SSE, verified live on Windows — so that half is verification plus OpenCode wiring, not construction; the packet's 'what does not exist' section was stale, the second such case today).
