# Ledger fragment — append-only, IMMUTABLE once written.
packets:

  - packet_id: forge-ships-prev3-post-commit-hook-and-no-expert-env
    order: 713-mirp
    status: ready
    desired_release: v0.6
    priority: p2
    title: "594-gqeu first-fire fails in-forge: forge ships the pre-v3 post-commit hook (via core.hooksPath) and does not set TILLANDSIAS_HOST_EXPERTS"
    kind: finding
    deliverable: a fresh forge installs the v3 env-gated expert-refresh post-commit hook (685-yidq) AND sets TILLANDSIAS_HOST_EXPERTS, so the commit-triggered expert refresh actually fires in-forge
    depends_on: []
    pickup_role: linux
    capability_tags: [experts, git-hooks, forge, env-gating, observability, finding]
    context: >
      Found by in-forge delegate bigpickle-20260813t0248 (2026-08-13) validating
      594-gqeu (experts-refresh-on-commit first-fire, read-only). Precise finding:
      the forge's EFFECTIVE post-commit hook (resolved via core.hooksPath, since
      the literal .git/hooks/post-commit is absent) is the PRE-v3 order-396
      dashboard form — NOT the v3 form (685-yidq) that composes the expert-refresh
      step gated on TILLANDSIAS_HOST_EXPERTS. Also TILLANDSIAS_HOST_EXPERTS is
      UNSET in the forge. So the commit-triggered expert refresh does not fire
      in-forge for two independent reasons (stale hook + missing env gate). Likely
      root cause: the forge seeds hooks from the binary's BUNDLED asset snapshot
      (683-g7p6 refresh footgun), so a v3 hook landed after the snapshot is not
      present in-forge. This is the same class as 712-r5x8: a feature verified on
      the dev host that is not actually wired in a fresh forge. Since the forge IS
      an expert host, it SHOULD both install the v3 hook and set the env gate.
    exit_criteria:
      - "a fresh forge installs the v3 env-gated expert-refresh post-commit hook (685-yidq) as the effective post-commit hook (literal or via core.hooksPath)"
      - "TILLANDSIAS_HOST_EXPERTS is set in the forge environment (the forge is an expert host)"
      - "an as-wired check proves a commit in-forge fires the expert refresh (594-gqeu first-fire green in-forge), or the not-firing state is documented + discoverable"
    events:
      - type: filed
        ts: "2026-08-13T02:53:04Z"
        agent_id: linux-mutable-session-20260812
        host: linux_mutable
        summary: "Filed from in-forge delegate 594-gqeu validation: forge has the pre-v3 hook via core.hooksPath and no TILLANDSIAS_HOST_EXPERTS, so expert-refresh-on-commit does not fire in-forge. Relates 685-yidq, 683-g7p6, 594-gqeu."
