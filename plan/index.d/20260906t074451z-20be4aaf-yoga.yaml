# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-worker-skill-forbids-touching-startup-dirt-and-never-says-to-copy-it-first
    field: status
    value: completed
    ts: "2026-09-06T07:44:51Z"
    host: yoga

events:
  - packet_id: the-worker-skill-forbids-touching-startup-dirt-and-never-says-to-copy-it-first
    event:
      type: completed
      ts: "2026-09-06T07:44:51Z"
      host: yoga
      summary: |-
        status 'completed' with evidence_refs: ./build.sh --check rc=0 489s; worker skill salvage mentions 1 -> 3 with the ok:salvaged verdict named twice; salvage exercised on a scratch repo — worktree byte-identical before/after including the failing arm, untracked file present in the salvaged tree; push dependency filed as 1103-i7xq
  - packet_id: the-worker-skill-forbids-touching-startup-dirt-and-never-says-to-copy-it-first
    event:
      type: note
      ts: "2026-09-06T07:44:51Z"
      host: yoga
      summary: |-
        closed: the worker skill says to copy before it says to refuse
