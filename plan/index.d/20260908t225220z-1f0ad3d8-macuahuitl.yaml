# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: ghost-trace-ephemeral-guarantee-spec-and-litmus-closure
    field: status
    value: completed
    ts: "2026-09-08T22:52:20Z"
    host: macuahuitl

events:
  - packet_id: ghost-trace-ephemeral-guarantee-spec-and-litmus-closure
    event:
      type: completed
      ts: "2026-09-08T22:52:20Z"
      host: macuahuitl
      summary: |-
        status 'completed' with evidence_refs: af7529a265ee0ec3b61f1f6fb7d3537cf01874a3; TILLANDSIAS_SKIP_VERSION_BUMP=1 TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check: PASS
  - packet_id: ghost-trace-ephemeral-guarantee-spec-and-litmus-closure
    event:
      type: note
      ts: "2026-09-08T22:52:20Z"
      host: macuahuitl
      summary: |-
        Ephemeral guarantee has an owning spec; stale baseline entry removed.
