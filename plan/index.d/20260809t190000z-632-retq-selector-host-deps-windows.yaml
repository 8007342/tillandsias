# @trace spec:ci-release
# Filed by the windows host, 2026-08-09, from the finding in
# plan/issues/selector-silent-no-work-on-missing-jq-2026-08-09.md
packets:
  - packet_id: selector-drop-host-tool-dependency
    order: 632-retq
    title: "select-work-batch.sh: project batches without jq so the selector runs on every host"
    status: ready
    kind: enhancement
    priority: p1
    pickup_role: any
    desired_release: v0.5
    capability_tags: [plan, orchestration, host-portability, tooling]
    deliverable: scripts/select-work-batch.sh
    rationale: >-
      The batch selector is unusable on Windows because every projection is a jq
      call and jq is absent there. Order 632-retq made that fail loud
      (refused:missing-tool) instead of counterfeiting a drained ledger, but the
      host still cannot select work. Satisfying the dependency per host repeats
      the yq/ruby exposure already recorded on 2026-08-03; emitting the
      projection from tillandsias-plan itself deletes the whole class.
    verifiable_closure: >-
      litmus:cycle-batch-triage-shape passes with TILLANDSIAS_JQ set to an absent
      binary AND scripts/select-work-batch.sh windows emits a batch line on a
      host with no jq on PATH.
