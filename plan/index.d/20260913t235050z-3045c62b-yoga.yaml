# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    field: next_action
    value: 'OPEN: C2 name the rc=3 mechanism for arm 4, and C3 catch one refusal with the out2 print armed, which has still never happened. C4 is now DONE — arm 5''s scratch case was exhibited directly by killing the archiver mid-run. C1 and C5 hold by execution. Capture ps BY NAME at the fixture step on every gate, refusals and passes; the discriminator is whether the host-side podman exec still exists, NOT conmon parentage, which a healthy gate also has'
    ts: "2026-09-13T23:50:50Z"
    host: yoga

events:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    event:
      type: note
      ts: "2026-09-13T23:50:50Z"
      host: yoga
      summary: |-
        correcting my own previous next_action, which said criterion 2 was the only one open and dropped C3
