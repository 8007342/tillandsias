# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-23T17:38:44Z.
events:
  - packet_id: expert-validation-layer-returns-verified-not-merely-verifiable
    event:
      type: progress
      ts: "2026-08-23T17:38:44Z"
      agent_id: linux-macuahuitl-opus5-20260823T173400Z
      host: linux_mutable
      evidence_refs:
        - "scripts/refusal-calibration/results-recall-at-5-qwen7b.tsv (309 rows, 73 questions, k=5, judge qwen2.5:7b)"
        - "GPU AT MEASUREMENT: 1% utilisation, 2910 MiB / 24564 MiB, no game or Steam process; the two resident llama-server runners account for 2260 MiB"
      summary: >
        TIER B OVER TOP-K, which was next_action item (1). The two error sources
        are now measured SEPARATELY and they are not the same size.

          band          n   recall@1   recall@5      lift
          in           40      22.5%      50.0%   +27.5 pp
          near         33       0.0%       0.0%     0.0 pp

        RETRIEVAL IS THE BOTTLENECK, NOT THE JUDGE. Looking past top-1 more than
        doubles in-corpus support detection — 11 of 40 in-corpus questions
        (27.5%) have a supporting passage somewhere in the top 5 that top-1
        missed entirely. The previously reported 20% Tier B support rate was
        therefore never a judge quality number; it was recall@1 wearing a judge's
        label, exactly as suspected when this split was specified.

        AND WIDENING k COSTS NOTHING ON THE REFUSAL SIDE. The near band is 0.0%
        at k=1 and 0.0% at k=5: giving the judge five chances to claim support
        for a question the corpus cannot answer produced zero false positives,
        33/33. That is the result that makes k>1 safe to adopt. The obvious fear
        — more candidates means more opportunities to hallucinate support — is
        measured and absent, which matches the earlier 0/33 fabrication finding
        rather than merely repeating it at a different k.

        WHAT IS STILL OPEN. recall@5 is 50%, so half of in-corpus questions have
        no supporting passage in the top 5 at all. Combined with the earlier
        judge@source finding (6 of 10 refusals flip to YES when judged against
        the recorded source file), the remaining loss sits in retrieval, not in
        judgement. That is what makes next_action item (2) — a bigger embedder —
        the right next move rather than more judge work: the judge is already
        outperforming what retrieval hands it.

  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    event:
      type: progress
      ts: "2026-08-23T17:38:44Z"
      agent_id: linux-macuahuitl-opus5-20260823T173400Z
      host: linux_mutable
      next_action: >
        CAMPAIGN STATE 2026-08-23T17:38Z. FLEET STOOD DOWN BY THE OPERATOR: only
        lenovinha (2h cadence) and pirria remain awake; macuahuitl continues
        hourly. Re-arming the rest is an operator action, so do not read silence
        from esmeraldinha, yoga, yolanda or the macbook as a stall.

        ITEM (1) IS DONE — Tier B over top-k measured. recall@1 22.5% ->
        recall@5 50.0% in-corpus (+27.5pp, 11/40 rescued), near band 0.0% at
        BOTH k — widening k is free on the refusal side, 33/33. Retrieval, not
        the judge, is the bottleneck; the old "20% support" figure was recall@1
        mislabelled.

        NEXT, in order:
        (1) ITEM (2) IS NOW THE HEAD AND IT IS UNBLOCKED: rebuild the index with
        bge-m3 or qwen3-embedding:4b and re-derive the bands. Serialise the
        model loads — concurrent runner residency is the real 849-tz8g defect,
        NOT a size ceiling. Record GPU utilisation and VRAM with every timing.
        (2) 846-idhn capabilities channel base representation, now that seven
        hosts publish rows.
        (3) The stale tail of scripts/refusal-calibration/README.md still says
        "Model size is blocked" and lists mxbai/qwen3/bge-m3 as aborting. That
        section was REFUTED by 849-tz8g and is now the most misleading page in
        the corpus on this subject — a low-end host reading it would conclude
        the ceiling is size when it is residency.

        STANDING COORDINATOR DUTY EVERY CYCLE: merge platform branches into
        linux-next; nobody else has it. Both were ahead=0 at 17:35Z.

        FLEET GAP, UNOWNED: pirria is in the capability roster and filing work
        (agent linux-pirria-opus5-20260823T170000Z, 17:00Z) but has NEVER
        written an MO-FULL attestation. check-mo-full-attestations.sh reports
        this INSIDE a green ok: line at rc=0, so the fleet's attestation
        coverage can be incomplete while the gate reads as passing.

        NOT MINE: 856-s56y (yoga), 857-4kqv (yoga), 860-g798 (yolanda), and the
        low-end tier rows on pirria/esmeraldinha including the 864-dvhk /
        864-c956 / 864-6qsv image-ceiling chain pirria filed at 17:22Z.
      summary: >
        Cycle 2026-08-23T17:34Z. Heartbeat: platform branches ahead=0 (nothing
        to merge); expire-claims in_progress=3 expired=0; all durable hosts
        attested within 6h before the operator stood the fleet down. Queue item
        (1) closed with a measurement; item (2) promoted to head.
