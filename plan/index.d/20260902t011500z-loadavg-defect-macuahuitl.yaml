# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: litmus-low-end-instrument
    event:
      type: note
      ts: "2026-09-02T01:15:00Z"
      host: macuahuitl
      summary: >
        INSTRUMENT DEFECT, the packet's sharpest (pirria): the runner's
        starvation adjudicator (run-litmus-test.sh:1307-1330) reads
        /proc/loadavg, which is NOT NAMESPACED — inside a forge it
        measures the HOST desktop and stamps the CONTAINER. Both pirria
        timeouts were stamped SATURATED while the cgroup's own PSI showed
        full-CPU stall of 1.7% avg300, zero IO, zero memory (sampling
        caveat: point-load1 vs 60/300s PSI windows, stated not hidden).
        The advice is INVERTED in guests: 're-run before treating as
        regression' dismisses genuine floor budget failures as flakes,
        and the busier the developer desktop, the more real regressions
        get waved through. Second denominator bug: nproc is
        container-visible while load1 is host-wide. FIX CLAIMED
        (macuahuitl, with the 959 fix batch): prefer
        /sys/fs/cgroup/cpu.pressure full avg10 when present, fall back
        to loadavg, reusing the existing UNCLASSIFIED branch for absent
        instruments. RETRO-QUALIFICATION: esmeraldinha's night-1
        saturated/not-saturated adjudications were made by this same
        instrument — her idle-run conclusions survive because she idled
        the WHOLE host (the instrument was right by accident); the
        CATEGORY stands, the adjudicator was unsound in guests.
        RECLASSIFIED BY PSI, both now two-host GENUINE budget failures:
        cheatsheet-source-layer-validation step1 (5s budget; over on
        both regimes) and uninstall-preserves-vm step2 (30s; 30.1s at
        the edge on N100, blown through on N150). Precision note for
        all future attributions: hostname, entrypoint, and the tmpfs
        trio are SHARED surface across the 512 and 4096 launchers —
        only trio+512 TOGETHER pins a site; both scouts filed one wrong
        attribution each on a shared leg before this was understood.

  - packet_id: forge-pids-ceiling-512
    event:
      type: note
      ts: "2026-09-02T01:15:00Z"
      host: macuahuitl
      summary: >
        FRAMING CORRECTED, two-host complete: pids.events unchanged
        through BOTH full litmus runs including 27 cargo-compiling steps
        (pirria max 140, esmeraldinha max 100, zero new denials) —
        single-crate -j4 builds do not approach 512 on floor hardware.
        The ceiling is reached by TOOL-INSTALL FANOUT (364s tokei
        bootstrap; hyperfine attempt), not builds. The right ceiling is
        sized against installer fork storms, and the 667-se87 folklore
        (build fanout) is inverted on floor hardware. Line-number note:
        the fix targets linux-next coordinates; pirria's ad4a84773 table
        (13407/13430/13502-13504, tray 2157, utility 10562) is the same
        attribution on the stable ref. watchexec dating ANSWERED from
        history: in the allowlist since the ORIGINAL brew adoption
        (39d2460ba, 2026-07-11), no add/remove since — the 64-vs-65 shim
        split is materialization-side and dates the IMAGES; guests can
        compare /run/.containerenv image digests to sequence them.
