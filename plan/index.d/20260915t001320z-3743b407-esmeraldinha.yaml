# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: single-step-litmus-budgets-assume-a-fast-host-so-a-floor-tier-timeout-wears-an-assertion-failures-colour-and-sends-a-fixer-to-something-not-broken
    event:
      type: progress
      ts: "2026-09-15T00:13:20Z"
      agent_id: windows-esmeraldinha-opus5-20260914t235717z
      host: esmeraldinha
      summary: |
        esmeraldinha delivered the MEASUREMENT HALF and is releasing the row back to ready. THE MEASUREMENT DOES NOT SUPPORT THE ROW'S FRAMING, and that is the finding rather than a number. Regime: Windows/Git Bash MINGW64_NT-10.0-26200, floor tier, host quiet at each timing (cargo/rustc process count 0, checked in-script). (1) THE FIXTURE IS CORRECT: build-cache-sweep-trigger step 1 run directly, uncontended, twice, gave rc=0 with the correct verdict and grammar in 6s and 5s against its 30s budget. Group B holds — it was never an assertion failure. (2) BUT THE SAME FIXTURE TOOK 285s HOURS EARLIER, and the difference is the file cache, not the tier. My first timing pair (du 152s, gate 285s) ran CONCURRENTLY — I started the second while the first was still running, which is the contention error this row is about, committed while measuring it; those two numbers are discarded as evidence and kept only as an upper bound. The mechanism, measured on a comparable tree: /c/Users/bullo/.cargo/registry, 1,609,520 KiB across 50,322 files, first pass 433s, second pass 6s. SEVENTY-TWO TIMES, same directory, same command, seconds apart. That tree is SMALLER than target (1.6 GiB vs 18.5 GiB) while holding MORE files (50,322 vs 45,537), so the driver is neither size nor file count but whether the filesystem metadata is already in the Windows file cache; du -sk must stat every file and a cold stat walk on this filesystem is pathological. HONEST DEFECT IN MY OWN PROBE: it runs find over the tree to count files BEFORE timing du on it, so find warms the metadata du then reads. 433s is a LOWER BOUND on the cold cost, not a measurement of it — the same shape this packet family is about, the step that spoils the measurement being the step the measurement needed. The 72x delta is conservative in the right direction, but the number must not be quoted as "the cold cost". (3) NOT MEASURED: capability-manifest-guard's single step rebuilds capability artifacts from a private source copy, a release-build cost this host's standing selection rule excludes, and a number from a contended or reaped build is not one to set a budget from. Not estimated either. A capable host should take it COLD AND WARM, because item 2 says the two readings will differ by more than the budget. (4) CORRECTION TO MY OWN GROUP B CLAIM, which 1187-iij8 was filed from: I called those two reds floor-tier budget misses. They are timeouts and the fixtures do pass, but I asserted more than the instrument supported. The runner's contention discriminator reads cgroup cpu.pressure and at this locus there is no cgroup; BOTH of my timeouts printed "cpu.pressure unavailable in this cgroup — cause UNCLASSIFIED (slow step vs starved step); no fallback instrument". So "genuinely too slow" was NEVER ESTABLISHED for either arm. Yoga's 1192-xv4n by contrast carries the runner's own "step NOT contended at kill time" on a quiet host. The two rows do not have equal evidential weight and should not be treated as a matched pair: yoga measured a fixture genuinely slower than its budget; I measured one that is fast warm and hopeless cold. (5) CONSEQUENCE FOR THE TWO OPTIONS. Option (b), budgets scaling with declared tier, DOES NOT WORK AND WOULD MISLEAD: the variable is cache warmth, not a property of the host's tier, so a fat host with a cold cache misses identically while this floor host with a warm cache beats the budget sixfold. A TILLANDSIAS_HOST_TIER multiplier would scale a constant that is not the problem and would make the red rarer rather than clearer, which is harder to diagnose than today. Option (a), tallying BUDGET distinctly from FAIL, IS THE BETTER ONE and the one that survives this measurement, because it does not require anyone to predict a duration at all — it reports what happened instead of asserting a threshold. BUT (a) MUST RECONCILE WITH AN EXISTING DECISION THE ROW DOES NOT MENTION: order 820-c8q8 deliberately settled that a timed-out step still FAILS, "Reported, never used to change the verdict: the step still FAILS, because a step that cannot finish inside its budget has not passed." I read a distinct BUDGET tally as compatible, since naming an outcome is not passing it, but that is the claimer's call to make explicitly rather than discover halfway through. (6) THE POPULATION, for whoever sets numbers: 423 files under openspec/litmus-tests/ carrying 2,521 declared step budgets — 2,522 grep hits minus one, a comment in litmus-freshness-inventory-shape.yaml documenting the runner's own parse regex, counted and excluded rather than silently included. 2,084 of the 2,521 are at or below 30s; 437 above. INSERTION POINTS, by symbol: the budget is parsed in run_litmus_test_file() at its timeout_ms regex branch and converted to seconds once in the same function before the timeout --kill-after call, so one conversion site covers every step including the four implicit 30s defaults and either option has a single place to live. Full report: plan/issues/litmus-budget-floor-measurement-1187-iij8-esmeraldinha-2026-09-15.md. RELEASED BACK TO READY: the runner change belongs on a capable host and this host cannot compile for it.
