# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: ca-bundle-moves-to-a-durable-directory
    field: status
    value: ready
    ts: "2026-09-04T03:18:07Z"
    host: yoga

events:
  - packet_id: ca-bundle-moves-to-a-durable-directory
    event:
      type: note
      ts: "2026-09-04T03:18:07Z"
      host: yoga
      summary: |-
        released: detector landed, value unchanged; the location is a design decision surfaced rather than picked, and a macOS confirmation is outstanding
