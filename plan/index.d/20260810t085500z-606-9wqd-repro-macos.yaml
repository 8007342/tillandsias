# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

events:
  - packet_id: git-mirror-egress-network-spec-drift
    event:
      type: progress
      ts: "2026-08-10T08:55:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Coordinator ask #3 (648-dvzd) ANSWERED: the runtime reproduction
        the Linux host could not perform is done and CONCLUSIVE, on the
        macOS VZ guest whose container egress is proven working (both
        controls 200). enclave-only: DNS FAIL, 000/000/000 — isolation
        holds. Dual-homed mirror posture: RESOLVED, 200/200/404 —
        outbound reach identical to the unrestricted default network.
        The audit's central claim is now OBSERVED at runtime: the egress
        leg grants the internet, not "GitHub push". Full table appended
        to plan/issues/git-mirror-egress-spec-divergence-audit-2026-08-10.md.
        Criteria 2-4 are unblocked for the decision owner (linux);
        closing as theoretical is off the table. This was the last
        prerequisite the coordinator named for 451
        (release-blocker-v0.5).

  - packet_id: coordinator-asks-overnight-2026-08-10
    event:
      type: progress
      ts: "2026-08-10T08:55:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        macOS status on the three asks: (1) 644-7w89 was COMPLETED 5/5
        PASS at 05:05Z (status done, evidence on the packet) — the
        stable promotion gate for 644-pnsp can run without --force on
        the strength of it. (3) 606-9wqd runtime reproduction DONE and
        conclusive on this guest (recorded on that packet). (2) 646-qde5
        is Windows-only — CANNOT from this host (no Windows GUI),
        recorded per the exit criteria so the skip is visible.
