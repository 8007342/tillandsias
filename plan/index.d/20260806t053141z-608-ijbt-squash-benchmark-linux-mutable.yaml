# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: container-image-squash-isolated-ab-benchmark
    order: 608-ijbt
    status: pending
    desired_release: v0.5
    priority: p2
    title: "Measure squash-new versus layered images in benchmark-owned isolated Podman stores"
    kind: performance+benchmark
    deliverable: plan/issues/optimization/container-image-squash-isolated-ab-benchmark-2026-08-05.md
    depends_on: [container-image-final-layer-squashing]
    pickup_role: linux
    capability_tags: [performance, podman, images, overlay, benchmark, cache]
    estimated_hours: 4
    outcome: >
      Reconstruct the missing laptop benchmark as a reproducible isolated-store
      A/B. Quantify cold/no-op/late-change builds, archive and physical store
      bytes, load duration, inherited sharing, and repeated start-to-ready for
      representative images without touching the operator's shared store.
    next_action: >
      After order 607-vbbt closes, create benchmark-owned storage roots and run
      the layered versus squash-new matrix with machine and Podman provenance.
    exit_criteria:
      - "the layered and squash-new variants use identical source inputs and isolated benchmark-owned storage roots"
      - "results separate RootFS depth, Podman .Size, OCI archive bytes, physical store bytes, build/cache timing, load timing, and repeated start-to-ready"
      - "forge/chromium inherited-base reuse is measured rather than inferred"
      - "raw receipts and machine/storage provenance support a keep, revise, or revert recommendation"

events:
  - packet_id: container-image-squash-isolated-ab-benchmark
    event:
      type: filed
      ts: "2026-08-06T05:31:41Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Filed from the operator's unrecorded laptop benchmark and Retry 4's
        paired-image measurement. Podman .Size increased sharply for some
        squashed tags while exact OCI archives for proxy, git, and router were
        slightly smaller, proving that one size field cannot stand in for the
        requested performance conclusion.
