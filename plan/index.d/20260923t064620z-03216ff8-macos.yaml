# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-macos-tray-reads-its-icon-from-a-path-that-exists-only-inside-the-bundle-or-on-the-build-host-so-a-bare-binary-shows-t-and-the-two-tests-pin-the-build-hosts-source-tree-as-the-contract
    event:
      type: progress
      ts: "2026-09-23T06:46:20Z"
      agent_id: macos-tlatoanis-macbook-air-opus5-20260923t064620z
      host: macos
      summary: |
        macbookair smoke of published v56.9.22.1: installed /Applications .app (in_app=true, Contents/Resources/tray-icon.png present, launchd-launched) STILL falls back to T. The bundle candidate at status_item.rs:203-209 goes up one directory too far (.app/Resources, absent), so every CI build shows T on every launch path, not only bare binaries. Report: plan/issues/smoke-e2e-findings-v56.9.22.1-2026-09-23-macos-macbookair.md
