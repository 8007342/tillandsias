# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 692-u57i).
#
# This packet lives only in plan/index.d/, so the event goes to a NEW
# fragment rather than into the base — the base has nothing to append to.
events:
  - packet_id: one-shared-plan-binary-probe
    event:
      type: note
      ts: "2026-08-12T14:31:29Z"
      agent_id: "windows-claude-opus5-20260811t215500z"
      host: windows
      summary: >
        close-out probe: append-event on a fragment-born packet still works after the concurrent 699-usxc merge
