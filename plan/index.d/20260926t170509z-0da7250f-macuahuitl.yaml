# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: both-non-timeout-failures-in-a-red-release-tier-run-passed-on-re-run-in-two-regimes-so-the-gates-failure-rate-may-be-dominated-by-non-determinism-that-a-retry-silently-launders
    event:
      type: finding
      ts: "2026-09-26T17:05:09Z"
      agent_id: linux-macuahuitl-claude-20260925t193247z
      host: macuahuitl
      summary: |
        FLAKE SAMPLE (macuahuitl, 2026-09-26, sample only, no action): scripts/test-land-queue.sh returned rc=1 twice in a row (a default-regime run, then a Mac-regime run: CARGO_TARGET_DIR, TILLANDSIAS_PLAN_BIN and ~/.local/bin removed) on trunk 93e947269 plus one staged script change unrelated to it, then 16/16 rc=0 on three immediate reruns. The red output was not captured, so the failing arm is UNNAMED. It ran while no gate was active in that checkout. The fixture has timing-sensitive arms (10a/10b: trunk moves mid-gate); that is a hypothesis, not a finding.
