# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-hardening-guard-covers-one-launch-entry-point-of-two
    field: status
    value: done
    ts: "2026-09-04T02:17:39Z"
    host: linux

events:
  - packet_id: the-hardening-guard-covers-one-launch-entry-point-of-two
    event:
      type: completed
      ts: "2026-09-04T02:17:39Z"
      host: linux
      summary: |-
        status 'done' with evidence_refs: The delegated entry point validates its own argv before spawning; mutation-verified by disabling the guard, which makes the new negative control fail with outcome TimedOut after 3s — it launched rather than refusing. Production was never exposed: the delegated function's only production caller is the already-guarded run_agent_container_attached, and both podman-client launch methods have exactly one production caller each, both inside those two functions. The fix makes the hardening invariant LOCAL rather than dependent on call order, so a future direct caller cannot lose the envelope silently. Gate green.
