# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: security-litmus-steps-cannot-fail
    event:
      type: progress
      ts: "2026-09-03T01:58:12Z"
      agent_id: linux-lenovinha-opus5-20260903t014457z
      host: lenovinha
      summary: >
        IMPLEMENTED AND CLOSED at e00d0895a (+ a follow-up fixing two rules my own additions broke). All three exit criteria met, and the second one — the judgement call the coordinator explicitly routed to me — is answered below with its boundary stated rather than assumed.  FIRST, I VERIFIED THE SWEEP RATHER THAN TRUSTING IT. The dispatch said five occurrences, all in one file, at lines 31/36/41/46/51. The guard I wrote finds exactly those five and nothing else across 411 litmus files. Same answer, independently derived. I checked because a relayed count is exactly the kind of claim I falsified on 793-zumy yesterday, and because the guard had to exist anyway.  CRITERION 1 (the steps can fail): each of the five now has a DISTINCT failure token that names what went wrong — CAPABILITIES_PRESENT with the offending caps, PRIVILEGED_OP_ALLOWED with the command output, HARDENING_NOT_APPLIED naming which of the three is missing, RUNNING_AS_ROOT, NO_CONTAINER. A step that reports which state it found is worth more than one that merely can fail.  CRITERION 2, AND THIS IS THE PART THAT NEEDED A DECISION. The coordinator was right that fixing the tautologies alone leaves the test inspecting a container it launched ITSELF with the hardening flags typed on its own command line — so every repaired step would be asking podman to confirm flags the test just supplied. A real question about podman; no question at all about Tillandsias. A step can be perfectly falsifiable and still measure the wrong system.  WHAT I DID: the launch argv is now DERIVED from MANDATORY_HARDENING_FLAGS in crates/tillandsias-podman/src/policy.rs, the single place the product declares what hardening it considers mandatory, and a new step asserts that declaration is complete. Delete a flag there and this test launches without it and the enforcement steps fail. The fixture observes a product decision instead of its own literals.  WHAT I DELIBERATELY DID NOT DO, and the file's header now says so in the place a reader will look: this still does not prove that production launch sites APPLY the envelope. The validator that would answer it, validate_launch_argv, has zero production call sites — that is 972-6vaj, a different packet with a Rust shape. Turning this fixture into a call-site audit would have been the over-fix, and leaving the file silently implying it covers that would have been the under-fix. It now proves two things exactly: the declared envelope is complete, and podman enforces it on this host.  CRITERION 3: scripts/check-litmus-steps-can-fail.sh, wired into ./build.sh --check, with scripts/test-litmus-steps-can-fail.sh as its fixture. It refuses ONLY identical tokens on both branches. The two-token form is how a step reports which of two states it found and is everywhere in this corpus; flagging it would make the guard noise, and noise is how a guard stops being run. Negative control run for real: against the pre-fix file recovered from git, the guard reports exactly 5 violations; against the repaired corpus, silence.  THREE BUGS IN MY OWN REPAIR, ALL THE SAME FAMILY AS THE DEFECT, all caught before commit because I dry-ran the shell instead of reading it.  (1) I first built the launch as `xargs podman run ... < flags.txt -- IMAGE sleep 300`. xargs APPENDS, so the flags landed after the image and after sleep 300 — the container would have launched with NO hardening whatsoever, and the enforcement steps would have failed loudly for entirely the wrong reason. Caught by echoing the constructed argv.  (2) The envelope-completeness step used `grep -qxF "$want"` where every $want begins with `--`, so grep parsed the pattern as an option and the step could NEVER pass. The mirror image of the original defect: a step that always fails instead of always passing. Needs -e. Caught by running the step's body against the real tree.  (3) The guard's own fixture caught a hole in the guard: 'X' and \"X\" compared unequal, so changing a quote style evaded it. It now deletes all quote and backslash characters and compares the first word — a guard that a spelling change defeats is a guard against typos.  That the fixture for a guard-against-tests-that-cannot-fail immediately caught the guard failing to catch something is the strongest argument for it that I can offer.  AND TWO PROJECT RULES MY ADDITIONS BROKE, both caught by ./build.sh --check rather than by me: spec:litmus-testing does not exist (litmus-framework and methodology-accountability do), and 795-imz3 refuses `if ! <pipeline>` because pipefail plus SIGPIPE can invert the guard. Worth recording as evidence the gate catches what a careful author does not.  NOT RUN HERE: the test itself is phase integration, size e2e, and needs a local tillandsias-forge image. Its shell was exercised piecewise against the real tree — envelope extraction, completeness in both directions, argv construction — but the container has not been launched by this cycle. A host with the image should run it once before this is treated as green in anger.
