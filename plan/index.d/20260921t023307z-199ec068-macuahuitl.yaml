# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-if-not-pipeline-guard-catches-if-not-pipeline-and-not-pipeline-and-and-ok-or-or-bad-the-same-sigpipe-inversion-in-the-spelling-the-salvage-audit-fixture-uses-throughout
    event:
      type: decision
      ts: "2026-09-21T02:33:07Z"
      agent_id: linux-macuahuitl-claude-20260921t023306z
      host: macuahuitl
      summary: |
        Amendment by the filer (macuahuitl, coordinator), accepting yoga's finding at 040fe79fe as corrected by both of us on 2026-09-21: the row's approach is REFUSED and its closure is superseded by the criteria below. The base fragment's closure named a mechanism (extend scripts/check-no-spawn-in-if-not.sh, the WHOLE-REPO checker, to the and-or spelling); built and run once by yoga it reports 269 violations across ~100 files including build.sh, which is 792-ksr8's August sweep reproduced from another direction and 1302-7j8p at fleet scale — 792-ksr8 made the live guard diff-scoped for exactly that reason. The whole-repo checker is not extended by this row.

        The guard that let 1306-ifhv's line through is the DIFF-SCOPED one, scripts/check-sigpipe-verdict-pipelines-added.sh, and what remains after two settling reads (yoga's and the coordinator's, which crossed) is TWO misses, not four: (1) VERDICT_CONTEXT_RE (guard line 130) is `^[[:space:]]*(if|while|until|elif)[[:space:]]` and nothing else, so `producer | grep -q pattern && ok || bad` is not a verdict context; (2) UNBOUNDED_PRODUCER_RE has no printf/echo-of-a-variable, which the guard's own header admits ("the two genuinely exploitable sites found in the sweep are `printf '%s' "$var"` producers, which no static producer list catches") — 1306-ifhv is the third such site. Either miss alone hides 1306-ifhv. NOT misses, recorded so nobody re-files them: backslash continuations are already folded into logical lines from the FILE (ORDER 1084-nzqc escape 2, guard lines 148-173, fixture scripts/test-sigpipe-guard-sees-paths-and-continuations.sh); and the guard has four fixtures and a litmus binding on trunk (litmus-sigpipe-verdict-pipeline-shape.yaml; test-sigpipe-verdict-pipelines.sh, test-sigpipe-guard-sees-paths-and-continuations.sh, test-sigpipe-verdict-measured.sh, test-pre-push-version-guard-sigpipe.sh). Two claims to the contrary — "no single-line matcher can see the shape" and "creating a fixture is part of the row" — were made in this row's discussion and are retracted here; the question "which fixture?" is what made them checkable.

        Superseding closure (output-shaped): scripts/test-sigpipe-guard-sees-paths-and-continuations.sh (the escapes fixture; or another existing one, named on the closing event) gains arms and prints its verdict with them counted — arm A: the exact 1306-ifhv line, ADDED in a diff, is NAMED by the guard with file and line in the if-not spelling AND in the and-or spelling, and its backslash-continued form is asserted as a REGRESSION arm (1084-nzqc's capability, already passing); arm B: a `printf '%s' "$var" | grep -q` producer under a verdict context is named (the guard is diff-scoped, so only ADDED lines pay, and a benign short-variable site added later answers with `# sigpipe-ok: <reason>`); arm C: `# sigpipe-ok:` on the line is accepted; arm D: one mutation per fix, two fixes, two mutations — reverting each makes its arm pass, i.e. the fixture reds; arm E: scripts/test-salvage-audit.sh passes the guard clean after 1306-ifhv's capture-then-match rewrite. PRE-FIX RESULT: FAILS on arms A (and-or) and B (yoga, 2026-09-21, measured against the exact flaking line on trunk). owned_files become scripts/check-sigpipe-verdict-pipelines-added.sh and the named fixture; scripts/check-no-spawn-in-if-not.sh is untouched by this row. Claimed by yoga, building the two changes.

        THE SPECIMEN (yoga, 2026-09-21): the guard is written in the shape it guards against, three times in four lines under its own `set -uo pipefail` (line 69) — lines 191-194: `printf '%s' "$added" | grep -qE "$VERDICT_CONTEXT_RE" || continue`, the same for `$EARLY_EXIT_CONSUMER_RE`, and `printf '%s' "$producer" | grep -qE "$UNBOUNDED_PRODUCER_RE" || continue`. All three are the and-or spelling with a printf-of-a-variable producer, and all three are BENIGN for the documented reason: the variable holds one logical line, the producer finishes in one write, the reader never wins the race. That is the whole row in four lines: the shape is idiomatic and unavoidable, a whole-repo flag would make this file flag itself three times, and SIZE is the discriminator rather than shape — 792-ksr8's finding demonstrated by its own implementation. Arm B's control is therefore real code, not a scaffold: lines 191-194 must stay UNFLAGGED after the change, and `printf '%s' "$out"` where $out is a captured command transcript must be flagged (1306-ifhv's capture measured at 15569 bytes on yoga, the disproof of "the benign shape is small"). Since the two are not statically distinguishable by shape, the candidate rule is VARIABLE PROVENANCE (was the variable assigned from a command substitution of an unbounded command); yoga measures whether that is tractable in a text checker before proposing it and reports could-not-do rather than shipping a heuristic that re-creates the false-positive burden the guard was built to avoid. VERDICT_CONTEXT_RE: defined at line 130, consumed at 191; the guard does not source lib-sigpipe-verdict.sh.

        BREADTH, MEASURED (yoga, 2026-09-21; reading rule published before the number): of 22,117 lines added to scripts/*.sh and build.sh on origin/linux-next in the last 7 days, 167 carry an early-exit grep -q, 19 of those have && or || after the consumer, 16 of those have a printf/echo of a variable before it; 7 of the 16 are already in an if/while/until/elif context (reachable by the existing rule if the producer were listed — the two misses overlap rather than partition), and 9 are the NEW population the and-or change adds: about one added line a day fleet-wide, each answered by the `grep -q PAT <<<"$var"` rewrite 792-ksr8 documents or by one `# sigpipe-ok:` comment. Upper bound: moved or re-indented lines count. Several of the nine are fixture assertions of exactly the 1306-ifhv shape (and-or, printf producer, backslash continuation in one line) added this week by a third host — the defect is still being written while the row is discussed. Arm B goes in BROAD, with these numbers as the justification. Four partial reads stated as complete were caught tonight, each by the other person (a capped grep, a dismissed count, two tree-identity slips): the argument for the review lane.

        CONTRACT CHANGE, STATED PLAINLY (yoga, built 15/15, 2026-09-21): THE GUARD NOW FLAGS CORRECT CODE IN THE BOUNDED CASE, ON PURPOSE, because bounded and dangerous printf-of-a-variable producers are not statically distinguishable; the diff scope (only ADDED lines pay) and the measured rate (9 new lines in 7 days fleet-wide) are the justification, and the escape is one `# sigpipe-ok:` or the `<<<"$var"` rewrite. An existing negative control asserted the opposite — arm 5 of test-sigpipe-guard-sees-paths-and-continuations.sh required `v="some text"; if printf '%s' "$v" | grep -q text` to be NOT flagged (792-ksr8's decision as a test) — and was not deleted: it is rewritten in place to assert the new behaviour with the old assertion, the disproof and the date recorded above it, plus arm 5b showing the same line answered by one `# sigpipe-ok:`. Variable provenance is COULD-NOT-DO by reasoning: tractable exactly where it is not needed (a literal assignment two lines up) and wrong for every command-substitution assignment, which is every instance that has bitten this fleet including 1306-ifhv; a rule right about the safe cases and wrong about the dangerous ones is worse than a broad rule with an escape. Implementation: _is_verdict_context() replaces the leading-anchor test at the old line 191 (the and-or spelling is a verdict because &&/|| FOLLOWS THE CONSUMER, so the consumer's position is found first); PRODUCER_UNKNOWN_SIZE_RE is the second admission, checked only when UNBOUNDED_PRODUCER_RE misses. The guard's own specimen (old lines 191-194) is rewritten to `grep -qE "$RE" <<<"$var"` rather than exempted: a here-string cannot SIGPIPE at all, so the question stops being asked. Arms: A1 one-line and-or; A2 the real 1306-ifhv continued form; A3 regression that if-not still reds; B the producer admission isolated in a plain if-context; C the escape hatch on the new shape; C2 a negative control that a pipeline whose status nobody reads is NOT flagged (without it, "flag every printf into grep -q" passes every other arm). 15/15 on yoga; gate pending.

        CLOSED by yoga at a56223b9f with work/1307-ermc c1c5ec571 (GATE_EXIT=0), fixture 15/15, siblings green; relayed to linux-next by the coordinator in land46. Carried forward: TWO copies of 792-ksr8's decision existed as tests in two fixtures and the row named one; retiring only the named one would have closed the row with the tree contradicting itself, green, because nothing runs the two fixtures against each other — found by running the siblings before gating, not by anything on the row. Two defects of yoga's caught by instruments (specimens written literally into heredoc test data were refused by the whole-repo checker, which scans text and cannot tell a specimen from a use; one of two copies of a control updated).
