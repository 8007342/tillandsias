# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260809t061000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# 622-rmit lives only in a fragment (filed 2026-08-09, not yet compacted), so
# `tillandsias-plan append-event` cannot reach it — that command locates packets
# by their item prefix in plan/index.yaml. Re-declaring the packet under
# `packets:` does NOT move its status either: `packets:` is a G-Set and the base
# wins a re-add. Status transitions go through the dedicated LWW channel below
# (plan/index.d/README.md), resolved by (ts, host).

events:
  - packet_id: litmus-expression-pinning-candidate-scan
    event:
      type: progress
      ts: "2026-08-09T06:05:00Z"
      agent_id: linux-mutable-20260809t061000z
      summary: >
        APPROVED by The Tlatoani 2026-08-09 and executed the same cycle.
        scripts/scan-litmus-expression-pinning.sh emits one ranked line per candidate under
        a fixed grammar (candidate/score/test/step/signals/title, plus one summary line) and
        never fails a build — ranking only, per the packet's scope limit. A candidate must
        match fixed text AGAINST SOURCE (literal-grep AND src-pinned); no-exec, no-negative
        and long-literal only rank it.
        Baseline plan/evidence/litmus-expression-pinning-scan-2026-08-09.tsv: 1762 steps
        across 300 files, 643 candidates (383 score-5, 202 score-4, 54 score-3, 4 score-2),
        and 166 tests carrying NO negative control anywhere. Worst single test:
        litmus:forge-hot-cold-split-tmpfs-shape (15 score-5 steps), then
        litmus:windows-tray-diagnose-cli-surface (10).
        A high score is a smell, not a verdict — some steps legitimately assert an exact
        string and are expected to rank high and stay. Repair is deliberately out of scope
        here; platform-owned repair is 624-cf9f. The enforcement rung ("no new
        expression-pinning guard may be added") remains Tlatoani-gated per
        methodology/convergence.yaml -> bar_raise_governance and was NOT enabled.

status:
  - packet_id: litmus-expression-pinning-candidate-scan
    field: status
    value: completed
    ts: "2026-08-09T06:05:00Z"
    host: linux-mutable
