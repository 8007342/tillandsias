# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: litmus-runner-reports-pass-when-it-executed-nothing
    event:
      type: progress
      ts: "2026-08-26T13:52:29Z"
      agent_id: linux-macuahuitl-claude-20260826t135229z
      host: macuahuitl
      summary: >
        PARTIALLY DELIVERED on macuahuitl. The misleading half is fixed; the exit-code half is deliberately NOT taken and is now an explicit fleet decision. Status returned to ready with a narrowed next_action — this packet is NOT closed by its own criteria and I am not marking it so.  DELIVERED: a run that executed zero tests no longer prints `Status: [PASS]`. It prints `Status: [NO-TESTS-EXECUTED]` plus a line saying the run is not evidence of anything, and — when a filter was named — how many tests the filter selected and that every one was excluded by --phase/--size. Verified on the exact reported case: `run-litmus-test.sh subdomain-routing-via-reverse-proxy --phase pre-build --size e2e` now reports NO-TESTS-EXECUTED where it reported PASS.  NOT DELIVERED, AND HERE IS WHY IT SHOULD NOT BE DECIDED BY ONE CYCLE. Criterion 1 also asks for a non-zero exit. Implementing that breaks a DELIBERATE PIN: litmus:litmus-name-filter-fail-loud-shape (order 300) case 2 asserts that "a valid explicit spec with every bound test excluded by phase still passes", exit 0, and case 3 asserts the same for the filterless variant. Those cases exist because the shape is LEGITIMATE in ordinary use — iterating specs across phases, and `--diff-scope` selecting nothing whenever a commit touches no litmus input, which is most commits. Making them non-zero would redden the fleet for the normal case, and a guard that fires constantly gets muted, which criterion 2 explicitly warns against.  I WROTE THIS PACKET'S CRITERIA WITHOUT KNOWING THAT PIN EXISTED. Discovering it while implementing is the useful outcome here: order 300 and 913-27ex disagree about whether a named spec with everything phase-excluded is a pass, and both have a real argument. Order 300's is "empty buckets are normal"; mine is a MEASUREMENT — a phase filter turned a genuinely failing e2e (913-7m3t) green and I nearly filed that green as evidence.  The verdict-string change resolves my measured harm at ZERO blast radius: nobody reads [NO-TESTS-EXECUTED] as a pass. The exit-code change is a fleet-wide behaviour change against a deliberate pin and belongs to the operator, not to the cycle that happened to find it.  Both order-300 pin cases were UPDATED, not deleted: they still assert exit 0 (order 300's actual contract, untouched), and now additionally assert `Status: [NO-TESTS-EXECUTED]` AND `! Status: [PASS]`, so the misleading verdict cannot silently return. The existing order-300 guard for an explicit filter matching NO BOUND TESTS AT ALL still exits non-zero and is unchanged — that distinction is intact.  VERIFIED: spec-traceability suite 10/10 including litmus:litmus-name-filter-fail-loud-shape. Negative controls: a real suite passing WITH a skip still reports PASS exit 0 (22 passed, 1 skipped); a genuinely failing suite still reports FAIL exit 1.
