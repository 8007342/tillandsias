# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: implement-the-obligation-lattice-and-wire-the-scorer
    field: next_action
    value: SPLIT INTO FIVE 2026-09-03 (yoga) — do not claim this row for implementation; take a child. 977-56fd model -> 977-dpbj properties -> 977-j6qu scorer -> 977-3dee backfill -> 977-448j enforcement, each depending on the previous. Backfill and enforcement are SEPARATE rows because under the coordinator's ruling the backfill can succeed while covering almost nothing, and enforcement must not inherit that as evidence it worked. The fixpoint-citation gap (idempotence is strictly weaker than Tarski/Kleene) is scoped into 977-dpbj rather than a sixth child, since that rung stands up the operator anyway.
    ts: "2026-09-03T05:11:20Z"
    host: yoga

events:
  - packet_id: implement-the-obligation-lattice-and-wire-the-scorer
    event:
      type: progress
      ts: "2026-09-03T05:11:20Z"
      host: yoga
      summary: >
        status 'SPLIT INTO FIVE 2026-09-03 (yoga) — do not claim this row for implementation; take a child. 977-56fd model -> 977-dpbj properties -> 977-j6qu scorer -> 977-3dee backfill -> 977-448j enforcement, each depending on the previous. Backfill and enforcement are SEPARATE rows because under the coordinator's ruling the backfill can succeed while covering almost nothing, and enforcement must not inherit that as evidence it worked. The fixpoint-citation gap (idempotence is strictly weaker than Tarski/Kleene) is scoped into 977-dpbj rather than a sixth child, since that rung stands up the operator anyway.' (not terminal) with evidence_refs: plan/index.d 976-split-yoga.yaml; five children filed
