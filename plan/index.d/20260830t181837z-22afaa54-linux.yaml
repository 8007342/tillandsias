# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: network-architecture-audit
    field: next_action
    value: 'Ten slices landed. THE NA-05 PATCH LIST HAS NO OPEN WORK ITEM A LINUX HOST CAN TAKE: P2 closed-as-refuted, P3/P4/P5/P7/P8 done, P6 half-acted with an OPERATOR DECISION remaining (wire builds through :3129 or delete the port), P1 blocked on the git-mirror-egress verdict (plan/issues/git-mirror-egress-spec-divergence-audit-2026-08-10.md). WHAT REMAINS ON THE PACKET, in order: (1) THE COMPLETION GATE, which is not a worker''s to satisfy — opencode-bigpickle, antigravity-gemini and codex-gpt55-highthink still owe verified-by events on NA-01..NA-06. A verifier should know that FIVE of six criteria now have artifacts that did not exist at the 2026-07-14 verification: openspec/specs/network-scenarios/spec.md (NA-02, new), the §3 rewrite (NA-03), rewrites of enclave-network + proxy-container + headless-mode + vsock-transport (NA-04/NA-05), and §6 + §Observation (NA-06). (2) SIBLING-LANE CONFIRMATION of two source-read rows in the new vsock-transport matrix: a Windows lane should confirm the derived HvSocket GUID resolves against a live WSL utility VM, a macOS lane that the Virtualization.framework connector is the only host-side path. (3) Sections 2, 4 and 7 still carry line citations — mechanical, but the expensive half is re-verifying each claim. A LINUX HOST DRAWING THIS PACKET AGAIN SHOULD PREFER (3) OR ANOTHER PACKET: everything cheaper is done. FOR THE PACKET OWNER, tenth request: NA-05''s exit criterion mandates ''specific file:line references'' and now contradicts nine slices, five spec rewrites, one new spec and a cheatsheet.'
    ts: "2026-08-30T18:18:37Z"
    host: linux

events:
  - packet_id: network-architecture-audit
    event:
      type: note
      ts: "2026-08-30T18:18:37Z"
      host: linux
      summary: >
        P4 done; no open patch-list work a linux host can take
