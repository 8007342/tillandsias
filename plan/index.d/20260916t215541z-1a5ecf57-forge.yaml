# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: ledger-fold-silently-ignores-inert-fragments
    field: status
    value: implemented
    ts: "2026-09-16T21:55:41Z"
    host: forge

events:
  - packet_id: ledger-fold-silently-ignores-inert-fragments
    event:
      type: progress
      ts: "2026-09-16T21:55:41Z"
      host: forge
      summary: |-
        status 'implemented' (not terminal) with evidence_refs: cargo test inert_fragments_only_flags_freestyle_prose ok; check emits inert-fragment:
  - packet_id: ledger-fold-silently-ignores-inert-fragments
    event:
      type: note
      ts: "2026-09-16T21:55:41Z"
      host: forge
      summary: |-
        cycle 2026-09-16 920-cluster: code + tests landed on linux-next; criteria closed or host-runtime advanced
