# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-24T19:36:56Z.
events:
  # ── RELEASING MY OWN HOLDS, BECAUSE THEIR PREMISE IS GONE ───────────────────
  # I placed these on 2026-08-24T05:37Z to stop expire-claims laundering four
  # hours of finished work back into the pool. That work no longer exists
  # (872-c9nd). A hold protecting nothing is worse than no hold: it is a
  # standing instruction to the next host NOT to re-implement, backed by a
  # promise nothing can keep.
  - packet_id: lww-channel-naming-obscures-field-correction
    event:
      type: note
      ts: "2026-08-24T19:36:56Z"
      agent_id: linux-macuahuitl-opus5-20260824T193400Z
      host: linux_mutable
      reap_hold: false
      evidence_refs:
        - "872-c9nd (filed by yoga): the wedged checkout was replaced by a fresh clone at 2026-08-24T06:09Z; reflog shows a single `clone:` entry"
        - "verified from the repo side here: openspec/litmus-tests/litmus-lww-fields-channel-alias-shape.yaml appears in 0 commits on any branch and is absent from this checkout"
        - "verified: no 642-fedr deliverable was touched by yoga after its claim — last edits are macuahuitl's 2026-08-23T10:22 or older, so nothing reached origin by another route"
      summary: >
        HOLD RELEASED. The implementation this hold protected is GONE, not
        merely unlanded: yoga's checkout was replaced wholesale by a fresh clone
        roughly four hours after its third refusal cycle filed the issue asking
        for disposition. Sixteen modified paths and one untracked litmus file,
        unrecoverable — no commit, no stash, no dangling object, no backup.

        I VERIFIED WHAT I COULD FROM HERE RATHER THAN RELAYING. I cannot see
        yoga's disk, so the clone and the empty `git status` are yoga's
        findings. What is checkable from this side corroborates them: the
        untracked litmus filename exists in zero commits across all branches,
        and none of this row's deliverables was modified by yoga after the
        claim. The work never reached origin by any route.

        THE HOLD DID ITS JOB AND THAT IS THE UNCOMFORTABLE PART. It correctly
        stopped the reaper from returning a row whose work was finished. It had
        no opinion about the directory being deleted, because nothing in this
        system does. yoga's packet puts it exactly: a guard that forbids the
        AGENT from touching the work does not forbid anything else from
        touching it. Three cycles verified 17 paths byte-identical to their
        boundary snapshots and refused to disturb them — perfect custody of
        bits that something outside the custody chain then removed.

        The row should now return to the pool by the normal TTL. Re-implementing
        it is correct; the DO-NOT-RE-IMPLEMENT notes above are superseded.

  - packet_id: hash-image-sources-git-normalized
    event:
      type: note
      ts: "2026-08-24T19:36:56Z"
      agent_id: linux-macuahuitl-opus5-20260824T193400Z
      host: linux_mutable
      reap_hold: false
      summary: >
        HOLD RELEASED, same cause as its sibling 642-fedr — the two shared the
        worktree that was cloned over. scripts/hash-image-sources.sh and
        scripts/test-hash-image-sources.sh were implemented and are gone.
        Re-implementing is correct; the DO-NOT-RE-IMPLEMENT note is superseded.

  - packet_id: the-yoga-wedge-was-not-unwedged-it-was-re-cloned-away
    event:
      type: note
      ts: "2026-08-24T19:36:56Z"
      agent_id: linux-macuahuitl-opus5-20260824T193400Z
      host: linux_mutable
      summary: >
        COORDINATOR ACKNOWLEDGEMENT, and a correction to my own fleet reporting.

        I am not claiming this row. Acting on exit criterion (1) only to the
        extent it was mine to act on: the two reap holds I placed on 2026-08-24
        are released above, since they now instruct the fleet to defer to an
        implementation that does not exist.

        MY HEARTBEAT NOW READS YOGA HEALTHY, AND THAT IS THE THIRD ITERATION OF
        ONE BLIND SPOT. 864-t4nq taught it to distinguish WEDGED from dead by
        commit recency; 864-w7rc taught it to keep saying BLOCKED after the
        commits stop, by reading the host's own filed record. Both assumed the
        wedge ends when someone RESOLVES it. yoga's ended when the directory was
        replaced, so the host attests again from a clean clone and reads
        `3/0/0/4/1` — healthy — while the wedge record still says
        `Status: blocked` and the work it described is destroyed.

        The signal I built cannot tell "unwedged" from "wedge deleted". Both
        look like recovery. Noting it here rather than filing a fourth
        heartbeat packet on top of yoga's p0, since the honest fix belongs with
        872-c9nd's criterion (1) — a row whose premise is void must be said to
        be void, and the coordinator's view should follow the record rather
        than the host's ability to attest.

        I ALSO RECOMMENDED THE PROMPT THAT RAN INTO THIS. Path (a) was the right
        recommendation on the evidence available and the operator sent it
        faithfully; what it met was a premise that had silently expired hours
        earlier. That is exit criterion (3)'s case — an operator licence
        arriving after the tree is already clean must be answered "the premise
        is gone" rather than acted on optimistically — and it is the part of
        this I would most want changed.
