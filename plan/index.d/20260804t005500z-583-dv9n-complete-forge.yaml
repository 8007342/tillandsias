# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-opencode-20260804t000814z (forge host, linux-next)
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
status:
  - packet_id: capability-manifest-must-list-new-subcommands
    field: status
    value: completed
    ts: "2026-08-04T00:55:00Z"
    host: forge
events:
  - packet_id: capability-manifest-must-list-new-subcommands
    event:
      type: completed
      ts: "2026-08-04T00:55:00Z"
      agent_id: forge-opencode-20260804t000814z
      host: forge
      summary: >
        Implemented and verified. main.rs gains DISPATCH_ARMS (the 25-arm
        dispatch surface) + manifest_drift_against/manifest_drift; the
        `capabilities` self-report (which the forge runs at every launch probe)
        emits an ORDER 583-dv9n stderr WARNING naming any dispatch arm absent
        from capabilities.txt — stdout stays exactly the bare token list and the
        exit code stays 0, so the fail-safe under-report the MCP wrapper relies
        on is untouched (lib-expert-capability.sh reads stdout with 2>/dev/null).
        Two unit tests pin the mechanism and the USAGE-coverage of the arm list.
        New litmus:capability-manifest-guard (bound to
        forge-environment-discoverability) passes 4/4: it BUILDS a real artifact
        from a capabilities.txt trimmed of `query` and asserts the warning fires
        on stderr with exit 0 + clean stdout, the probe shape check survives and
        reports blocked_capabilities=query, and the same source restored clean
        warns about NOTHING. Exit-criterion audit: (1) an undeclared arm is
        reported as a warning, not a gate — pinned; (2) under-declaring never
        breaks the wrapper — pinned (stdout/exit untouched, probe shape green);
        (3) current tree audited — manifest_drift() is EMPTY: every one of the
        25 dispatch arms (including 'fragments' and 'compact', which the packet
        flagged as possibly-undeclared) is declared in capabilities.txt, and the
        clean-tree negative control confirms the warning stays silent.
        Verification: cargo fmt/clippy clean, cargo test --workspace green,
        ./build.sh --check passed, litmus:expert-capability-skew-honesty 28/28,
        litmus:capability-manifest-guard 4/4.
