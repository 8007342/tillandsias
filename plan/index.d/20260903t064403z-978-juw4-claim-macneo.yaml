# Ledger fragment — append-only, IMMUTABLE once written.
# Hand-authored: this host has NO tillandsias-plan binary and no Rust
# toolchain to build one (order 979-xyub — the same gap that blocks its
# capability row). Written with a QUOTED heredoc so nothing in the body is
# expanded, per the incident this directory's README records.
status:
  - packet_id: guest-floor-defeats-the-host-memory-reserve-below-10-gib
    field: status
    value: in_progress
    ts: "2026-09-03T06:44:03Z"
    host: macneo

events:
  - packet_id: guest-floor-defeats-the-host-memory-reserve-below-10-gib
    event:
      type: note
      ts: "2026-09-03T06:44:03Z"
      host: macneo
      summary: >
        claimed by macneo-macos (8 GiB / 6-core Apple A18 Pro, macOS 26.6) — the
        host the packet was filed about. Forensic capture of the first-ever
        launch is committed at plan/issues/macneo-first-launch-forensics-2026-09-03.md
        (osx-next 09d13de3f), generated by the machine rather than retyped, and
        carries the evidence for the SUSPECTED CONSEQUENCE in the packet context:
        ps showing no VM process, lsof showing zero rootfs.img descriptors held
        by the tray, crashloop.state verbatim (ever_ready=1 last_phase=ready),
        complete console.log with both bootids, and rootfs.img mtime 11 minutes
        after the final console write.
  - packet_id: guest-floor-defeats-the-host-memory-reserve-below-10-gib
    event:
      type: note
      ts: "2026-09-03T06:44:03Z"
      host: macneo
      summary: >
        CORRECTION to exit criterion 3, which assumes the below-crossover case is
        untested. It is tested twice, and both assert the harmful behaviour as
        correct. vz.rs:2664-2674 guest_sizing_leaves_the_host_its_reserve_and_caps_large_hosts
        contains an exact-equality assertion for an 8 GiB host — this host's size —
        with the comment "8 GiB: half is 4, reserve allows 2 — the floor wins, host
        is small". vz.rs:2636-2649 guest_sizing_never_drops_below_the_pinned_floor
        iterates (2c,4G) (4c,8G) (8c,8G) (1c,2G), every one below the crossover, and
        asserts mem >= GUEST_FLOOR_MEMORY_BYTES. So the fix must INVERT two existing
        deliberate assertions rather than add a missing one. Recorded here because
        that is a decision that belongs on the record, not a side effect of the fix.
  - packet_id: guest-floor-defeats-the-host-memory-reserve-below-10-gib
    event:
      type: note
      ts: "2026-09-03T06:44:03Z"
      host: macneo
      summary: >
        Policy extended below the packet's table by porting guest_sizing to a
        faithful integer model (u64 saturating_sub, integer division, clamp) and
        reproducing the published curve exactly. New result at the bottom end: a
        2 GiB / 2-core host is sized to a 4 GiB guest, i.e. MORE MEMORY THAN THE
        HOST PHYSICALLY HAS (host_left = -2 GiB). Below roughly 4 GiB the policy
        returns an unsatisfiable allocation rather than merely an unkind one, and
        guest_sizing_never_drops_below_the_pinned_floor explicitly asserts the
        (1 core, 2 GiB) case is correct. Whatever mechanism is chosen for exit
        criterion 1, the policy should additionally be incapable of returning more
        memory than the host has.
  - packet_id: guest-floor-defeats-the-host-memory-reserve-below-10-gib
    event:
      type: note
      ts: "2026-09-03T06:44:03Z"
      host: macneo
      summary: >
        BLOCKER on implementation, not on diagnosis — this host has no Rust
        toolchain (cargo and rustc both absent on a factory-fresh Mac; the
        published release ships only Tillandsias.app plus two Linux musl guest
        binaries). The patch can be written here but CANNOT be compiled or tested
        here, and exit criterion 4 (re-test the wedge after the fix) additionally
        requires an installed build. Awaiting the operator's decision on installing
        a toolchain; if it is declined, this packet needs a host that can build,
        and macneo remains the only host that can discharge criterion 4.
