# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    field: next_action
    value: 'FOUR GATES ARE LIVE (28553c3db test-baseline ratchet, 4c221e731 archive-answerable fold, 9bb3b47bf arrival-routing advisory, 04c927249 deslop clock) and local-ci is unblocked (8d41d2c95). ORDERED NEXT: (1) 843-624y p0 — compaction DELETES fragments it never folded (fragments.rs:642 consumed=loaded; loop_status.rs:625 has the correct guard 300 lines away). Its second defect is worse: the capabilities: channel has NO base representation, so compact never persists it and `capability-matrix` returns 0 rows with exit 0 while 808-7yrd reads completed. DO NOT run `plan compact` until this lands; loop-status-compact is safe. (2) RE-RUN THE ARCHIVE SWEEP — now safe: check-archive-answerability.sh proves 204/204 after archiving 664 packets with the ready set unchanged. (3) File a row for the test-known-red seed entry (proxy_cache_policy bumped_origin_tls_and_signed_url_logs_fail_closed) — the precedent requires every entry have a filed issue and it does not. (4) Expert rung 2, which is the path to a DERIVED next_action per the 2026-08-21 operator design: 821-73es and 824-6qxh are the refusal interlock, 760-hzi4 and 552 its availability, 797-qv4z its fidelity, 810-k8jy the corpus boundaries. STANDING: verification MUST use TILLANDSIAS_FORCE_CHECK=1 — plain --check short-circuits on the gate stamp now. Never run ./build.sh without TILLANDSIAS_SKIP_VERSION_BUMP=1 (643-64bx). Linux pre-existing test failures = exactly 1. R2 target is b < 1, not 1.30.'
    ts: "2026-08-21T21:39:26Z"
    host: linux
