# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-20260810t235500z
packets:
  - packet_id: groundtruth-query-set-red-at-head-citation-count
    order: 673-47dx
    status: ready
    desired_release: v0.5
    priority: p2
    title: "groundtruth::the_committed_query_set_is_green_at_head is RED at HEAD — plan-upstream-prerequisites-of-experts-milestone now yields 2 citations, query set pins at most 1"
    kind: bug
    deliverable: crates/tillandsias-plan/src/groundtruth.rs (or the committed query set)
    depends_on: []
    pickup_role: any
    capability_tags: [plan, experts, tests, ledger]
    context: >
      Found 2026-08-10 during 672-bz7u implementation: cargo test -p
      tillandsias-plan fails at CLEAN HEAD (verified via stash) with
      "plan-upstream-prerequisites-of-experts-milestone: citations: expected
      at most 1, got 2". The test is ledger-data-driven, so some recent
      fragment/base change added a second citation source for that committed
      query. Every host running the crate's tests eats this failure; whoever
      owns the query set should either ratify the second citation (raise the
      pin) or fix the duplicate source. NOT fixed in the 672-bz7u commit —
      absorbing an unrelated red test silently is how drift hides.
    exit_criteria:
      - >
        cargo test -p tillandsias-plan is green at HEAD, with the query-set
        change (pin raised or duplicate source removed) explained in the
        closing event.
    events:
      - ts: "2026-08-10T23:55:00Z"
        event: filed
        summary: Captured during the overnight drain; pre-existing at HEAD, verified by stash.

status:
  - packet_id: plan-expire-claims-subcommand
    field: status
    value: completed
    ts: "2026-08-10T23:55:00Z"
    host: windows

events:
  - packet_id: plan-expire-claims-subcommand
    event:
      type: completed
      ts: "2026-08-10T23:55:00Z"
      agent_id: windows-20260810t235500z
      summary: >
        Implemented and live-verified. `tillandsias-plan expire-claims
        [--ttl-hours N] [--dry-run] [--now-epoch S] [--host H]` partitions
        in_progress packets by newest event timestamp against the TTL cutoff
        (default 24h, minimum 1h): stale claims get ONE new fragment (never an
        edit) flipping status back to ready with a progress event naming the
        expiry, TTL, and last-activity ts; fresh claims are untouched;
        packets with no parseable timestamp are reported unknown-age and
        NEVER expired. Policy pinned by unit test
        expire_claims_partitions_stale_fresh_and_unknown_age (synthetic
        stale/fresh/ageless/completed ledger); fragment emission verified
        LIVE: first real run expired the two claims stranded since July (394e
        at 2026-07-30, 463 at 2026-07-25), wrote
        plan/index.d/20260810t235053z-3271e4f5-windows.yaml, and
        check-stranded-in-progress.sh converged from stranded=3 to
        stranded=1 (184, unknown-age, correctly untouchable).
        check-stranded-in-progress.sh now reports threshold_events from a
        dry-run and prints a hint when claims are past TTL, capability-gated
        so old binaries degrade to the previous behavior. Residual noted
        honestly: fragment emission is proven live, not hermetically; a
        litmus seeding a synthetic claim end-to-end can be added if wanted.
        Capability manifest + DISPATCH_ARMS + usage updated in the same
        commit per order 569/583-dv9n.

  - packet_id: stranded-in-progress-packets-are-invisible-both-ways
    event:
      type: progress
      ts: "2026-08-10T23:55:00Z"
      agent_id: windows-20260810t235500z
      summary: >
        Criterion 2 CLOSED by plan-expire-claims-subcommand (672-bz7u): the
        claim protocol now has a mechanical expiry. First live run returned
        394e and 463 to ready after 11 and 16 stranded days respectively.
        Remaining stranded set is exactly one packet (184,
        secure-channel-maturity-ladder), which has no parseable activity
        timestamp and is deliberately not auto-expired; its linux owner
        should disposition it by hand.
