# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: story-shaped-eager-work-packets
    field: status
    value: in_progress
    ts: "2026-09-17T07:50:53Z"
    host: macos

events:
  - packet_id: story-shaped-eager-work-packets
    event:
      type: note
      ts: "2026-09-17T07:50:53Z"
      host: macos
      summary: |-
        claimed for cycle 2026-09-17T06 by macos-tlatoanis-macbook-air-claude-20260917t075052z: criteria 2 and 4 — story-shaped routing with the expert ABSENT (this host reports expert-absent, so the degradation path is real rather than mocked) and the same-seed negative control
