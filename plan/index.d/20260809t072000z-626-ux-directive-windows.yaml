# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-esmeralda-claude-fable-20260808t235000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# OPERATOR UX DIRECTIVE (The Tlatoani, live field session on Esmeralda,
# 2026-08-09 ~07:1xZ) — recorded verbatim-condensed as the approval input the
# 626-* UX packets were blocked on.
status:
  - packet_id: first-run-long-work-is-silent-across-surfaces
    field: status
    value: ready
    ts: "2026-08-09T07:20:00Z"
    host: windows
events:
  - packet_id: first-run-long-work-is-silent-across-surfaces
    type: note
    ts: "2026-08-09T07:20:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      OPERATOR APPROVAL RECEIVED (unblocks needs_clarification): during a
      live opencode first-launch on the N100 field host the operator
      directed, in their words: the tray status row "should be showing the
      latest meaningful message, pretty printed" and "should now be getting
      refreshed every new container creation starts/succeeds"; the tray must
      "show meaningful Alt/Tips always, and have that Status menu kept
      meaningful"; overall "the ux must be responsive and make sure that the
      user knows something is happening whenever relevant" because "users
      will get lost and try to launch stuff again". Fresh field evidence in
      the same session: status row pinned at "Securing Vault" since launch
      through image builds; opencode terminal pinned at "cloud: cloning
      8007342/tillandsias ..." with zero progress output while the clone
      demonstrably advanced (141MB on-disk mid-observation, slow NVMe);
      wsl.exe visibly busy in Task Manager while the tray showed nothing.
      Status flipped to ready on this approval; claiming agent should treat
      the quoted directive as the approved surface contract (latest
      meaningful lifecycle message in the status row incl. container
      create/start/success transitions, meaningful tooltip, and progress
      indication for long transfers like clones).
  - packet_id: tray-sign-in-state-unknown-until-observed
    type: note
    ts: "2026-08-09T07:21:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      Same-session operator responsiveness directive ("user must know
      something is happening whenever relevant") supports F1's proposed
      no-new-strings fix (extend the approved Setting-up row across the
      not-yet-known window). Claiming agent: confirm the exact row copy with
      the operator on claim; the general directive is recorded, the
      packet-specific surface sign-off was not separately spelled out.
