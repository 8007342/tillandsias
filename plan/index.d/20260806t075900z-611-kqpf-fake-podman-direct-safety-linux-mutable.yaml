# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: litmus-fake-podman-direct-invocation-safety
    order: 611-kqpf
    status: ready
    desired_release: v0.5
    priority: p1
    title: "Make advertised fake-Podman litmus commands hermetic outside the runner"
    kind: testing+safety+developer-experience
    deliverable: plan/issues/litmus-fake-podman-direct-invocation-is-not-hermetic-2026-08-05.md
    depends_on: []
    pickup_role: linux
    capability_tags: [litmus, podman, hermetic, cleanup, developer-experience]
    estimated_hours: 2
    outcome: >
      Copying a documented LITMUS_PODMAN_MODE=fake command into a plain shell
      either provisions the real fake backend or refuses before Podman; it can
      never create a real temporary containers/storage tree.
    next_action: >
      Add a real-Podman spy and a failed-cleanup fixture, then make the direct
      command self-contained or route all displayed commands through the
      canonical litmus runner.
    exit_criteria:
      - "the exact displayed fake-mode command is safe and deterministic in a plain shell"
      - "a spy proves fake mode invokes the real Podman binary zero times"
      - "injected failure leaves no containers/storage tree or user-namespace-owned residue"
      - "runner and direct/documented entrypoints return the same verdict"

events:
  - packet_id: litmus-fake-podman-direct-invocation-safety
    event:
      type: filed
      ts: "2026-08-06T07:59:00Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Direct execution of the command printed in
        litmus:image-build-convergence-shape reached real rootless Podman and
        left a user-namespace-owned temporary overlay tree. The exact residue
        was removed with podman unshare; the canonical runner remained green.
