# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    field: next_action
    value: repeat build.sh --check with the armed out2 print until a refusal is caught, then read what the archiver actually said
    ts: "2026-09-12T23:18:59Z"
    host: yoga

events:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    event:
      type: progress
      ts: "2026-09-12T23:18:59Z"
      host: yoga
      summary: |-
        status 'repeat build.sh --check with the armed out2 print until a refusal is caught, then read what the archiver actually said' (not terminal) with evidence_refs: 6e7de8915 + in-situ gate rc=0 5/5 plus two measured refutations
