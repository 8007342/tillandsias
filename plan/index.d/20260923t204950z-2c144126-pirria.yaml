# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-standalone-idempotent-initialize-bare-metal-host-skill-brings-up-rebuilds-and-troubleshoots-the-host-s-own-enclave-and-git-mirror-and-evolves-with-every-repair-recorded-in-it
    event:
      type: note
      ts: "2026-09-23T20:49:50Z"
      agent_id: linux-pirria-claude-20260923t204950z
      host: pirria
      summary: |
        BRING-UP STEP LEAKS A FORGE WHEN RUN NON-INTERACTIVELY (pirria, 2026-09-23; recorded at the coordinator's request). Run non-interactively under `timeout`, `tillandsias --bash <project>` leaves its forge running after the wrapper is killed. That forge then blocks order 443 (never recreate a mirror under a sibling) for the next mirror job. Measured: `TILLANDSIAS_HOST_PROJECT_ROOT=/var/home/lapto/claudia timeout 500 tillandsias --bash <checkout> </dev/null` printed "Terminated". check-bare-metal-host-initialized then read ok (enclave/mirror/router/inference up), but tillandsias-tillandsias-forge-maintenance (created 11:50:22 PDT) was still Up 2 hours later. That held 1350-ku7v's install regime at its precondition. lenovinha reproduced a second path: with `(sleep 600; echo exit) | tillandsias --bash "$PWD" &` the piped exit was never read, and the wrapper and forge were alive at 49 min. The fix belongs in this skill's §1 bring-up step: a non-interactive hold that starts no forge. Filed with both cases as 1368-dan8; its sibling is 1348-gkvi (lane exit removes the mirror). Cleanup here: the forge checkout held 57 files of openspec tool regeneration (no agent work), salvaged to salvage/forge-tillandsias/20260923-pirria-forge-openspec-regen = bc6051054 (ls-remote verified), then `podman stop`. After the stop, tillandsias-git-tillandsias, router, vault and proxy stayed Up. So on this host, stopping the leaked forge after its wrapper was already dead did NOT remove the mirror. That is a data point for 1348-gkvi.
