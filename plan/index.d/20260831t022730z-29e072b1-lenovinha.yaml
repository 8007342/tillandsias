# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: claim-events-change-no-status-so-claiming-separates-nobody
    event:
      type: progress
      ts: "2026-08-31T02:27:30Z"
      agent_id: linux-lenovinha-claude-20260831t021652z
      host: lenovinha
      summary: >
        Resolved as a two-skill divergence, not a decision. Worker skill section 3 now carries the coordinator's recipe (set-field status in_progress, pushed BEFORE the work), plus the two safety rules it lacked entirely: timestamp arbitration for collisions (a push can succeed and still have lost) and unconditional release-on-exit. Guarded by scripts/check-claim-protocol-agrees.sh, wired into build.sh --check, five arms mutation-tested. Positive control on this repo: set-field moved the selector eligible count 174 to 173 and dropped the packet from ready linux and from the batch in one command. The mechanism was never broken; the worker recipe was not invoking it, for nineteen cycles on this host. Commit b19f4ef79.
