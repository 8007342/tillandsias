# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
events:
  - packet_id: expert-fragment-provenance-and-freshness
    event:
      type: progress
      ts: "2026-08-06T01:22:00Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        The first local full-build smoke exposed a real-ledger compaction hole:
        text compaction rejected folded non-scalar LWW values, and a
        fragment-born packet could have its already-folded LWW winner applied
        twice. Compaction now replaces the exact YAML field span for scalar,
        sequence, and block-string values without round-tripping the document,
        skips the duplicate fragment-born application, and has focused plus
        real-ledger regression coverage. A later append-only fragment restores
        the two audit-authored order-451 fields after their invalid scalar-only
        updates. This is a partial checkpoint only; winning-fragment citation,
        verifier authority, and folded-corpus freshness remain open under this
        packet.
      partial_artifact_refs:
        - crates/tillandsias-plan/src/fragments.rs
        - plan/index.d/20260806t010300z-606-lww-correction-linux-mutable.yaml

