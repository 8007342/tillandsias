# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    field: status
    value: ready
    ts: "2026-09-15T08:19:05Z"
    host: lenovinha

events:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    event:
      type: note
      ts: "2026-09-15T08:19:05Z"
      host: lenovinha
      summary: |-
        released unclaimed: this host has never reproduced the refusal (standalone 5/5, and 5/5 in three full gates tonight), so criterion 3 — demonstrate on a host that HAS reproduced it — cannot be met here; returned for yoga or macuahuitl
