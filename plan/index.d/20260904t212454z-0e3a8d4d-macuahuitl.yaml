# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: ci-full-is-red-on-linux-next-at-eef84d6af-and-blocks-the-daily-release
    event:
      type: note
      ts: "2026-09-04T21:24:54Z"
      agent_id: linux-macuahuitl-claude-20260904t212455z
      host: macuahuitl
      summary: |
        CLOSED: `./build.sh --ci-full` exit 0 on linux-next at 822560ae9, the fourth attempt, 2026-09-04T20:55:53Z-21:14:35Z (18m42s, keep-id warmed), log line "ALL CHECKS PASSED — Safe to push!" with the gate stamp recorded; landed as e0e6938d5 with the tool's gate (ok:land:e0e6938d5:attempt-1), and the daily release v56.9.4.1 was cut from it the same hour (PR #105 merged a995c8cdd, bump merge 377ba7794 tagged, back-merge 4fc3ade63, release.yml run 33920810738 dispatched 21:22:42Z; artifact validation in the meta cycle's entry). CRITERIA: (1) ci-full exits 0 on linux-next: met, above; the three earlier reds were 12:39Z (causes 1-6), 16:39Z (one pre-build litmus red, causes 10-11, with 12-13 hidden behind it) and 20:50Z (causes 12-13). (2) The four orphaned guards have invokers and pass on the trunk: yoga's cause-2 wiring (orphan count 5 -> 1, the remaining one deliberate and named on its packet) ran green inside the fourth gate. (3) Causes 3, 4, 5 have owner events: cause 3 on yoga's nix-lane packet (4b, twice), cause 4 on lenovinha's RETRIEVE_K packet (6 -> 8), cause 5 on this packet (the coordinator's stale pin). (4) Cause 6 re-run with the rc visible after 1018-5f5a and classified: defect (stale pins), fixed on this packet. Thirteen causes in all, each with its pre-fix red quoted on this packet; cause 7 (the bare ci-full installs a downgraded launcher) and cause 8 (memoisation intermittent, archive-sweep scratch candidate) stay recorded as open observations with their own follow-ups, neither of which blocks the gate. This packet's closure is the gate exit code, which no litmus wraps (unscoreable as declared).
