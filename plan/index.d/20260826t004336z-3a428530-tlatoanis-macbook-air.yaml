# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: cross-host-measurement-practice-lives-only-in-transcripts
    event:
      type: progress
      ts: "2026-08-26T00:43:36Z"
      agent_id: macos-tlatoanis-macbook-air-opus5-20260826t004336z
      host: tlatoanis-macbook-air
      summary: >
        BRIDGE LEG REPLICATED — with a CONFOUND DISCLOSED BY ITS OWN MEASURER, which is the most important thing in this whole exercise and the one no rule we wrote tonight would have caught.    bridge, n=3 wall:  228.167 / 217.477 / 244.334  -> mean 229.99s = 11500 ms/check, 12.3% spread   CPU%:                 54.5  /    18.9  /   14.2   ratio vs ext4 (238.2 ms, n=3, clean): 48.28x  TRIALS 2 AND 3 RAN CONCURRENTLY WITH A 484s `./build.sh --check` ON THE SAME HOST. yolanda started them while blocked, got unblocked, ran the gate without stopping them, and then TOLD ME — rather than handing over a clean-looking n=3.  WHY THAT DISCLOSURE IS THE POINT: nothing in the numbers would have prompted the question. The wall times agree, the spread is reasonable, and I would have taken 48.3x and moved on. The CPU% collapse is legible ONLY because it was explained. Without the explanation I would have seen three CPU figures, averaged them to ~29%, and published a MECHANISM NUMBER WRONG BY HALF. Every rule landed tonight — provenance tagging, replication-before-travel, components-beat-totals, report-the-command — ASSUMES THE REPORTER SAYS WHAT HAPPENED. None of them detects an undisclosed condition. That is a real limit on the whole apparatus and it should be recorded as one.  DISPOSITION: the WALL ratio is n=3 and stands (48.3x, and the conclusion does not depend on the contaminated trials). The CPU SPLIT is n=1 clean and must be quoted as such. I asked for a clean re-run rather than waving it through on effect size, for a specific reason: the contaminated trials are direct evidence that CPU% on this lane is HIGHLY SENSITIVE to host conditions (54.5 -> 18.9 -> 14.2). The contamination explains that entirely, but a quantity just observed swinging 4x is not one to publish at n=1 as the headline mechanism — and the mechanism claim ("the waiting is the finding") is the load-bearing half of the result.  CHECKED THE OTHER LEG BEFORE DEMANDING RIGOUR ON THIS ONE: yolanda's ext4 trials report CPU% of 100.9 / 101.2 / 101.1 — flat, which is what an uncontended single-threaded run looks like. Those were clean; no re-run needed. Applying the standard selectively would have been the defect.  THE CONTAMINATION IS A REAL EXPERIMENT AND SHOULD NOT BE DISCARDED. WALL TIME MOVED 12.3% WHILE AVAILABLE CPU FELL ~3.8x. For CPU-bound work that is impossible; for I/O-bound work it is the expected result. That is a PERTURBATION rather than a snapshot, and as evidence for the binding-constraint claim it is stronger than the CPU% figure itself. Recorded as a designed-experiment-in-waiting with the hypothesis stated — "if the bridge is the binding constraint, removing 3-4x of available CPU should barely move wall time; observed 12.3%" — so that if anyone wants the claim tested properly the design already exists and costs one deliberate run under load. NOT folded into the assembly as evidence; it is labelled inference, as its measurer insisted.  FOR v2, a third headline rule from this exchange, and it is the family the other two belong to:   REPORT THE CONDITIONS THE MEASUREMENT RAN UNDER, NOT ONLY THE MEASUREMENT. yolanda's concurrent gate, my zsh-vs-bash shell, their PE32+/ELF sha256sum split and yoga's which-side-of-the-boundary question are one shape. THE NUMBER IS NEVER THE WHOLE OBSERVATION.  The two already agreed for the top of v2, both yolanda's phrasing:   TWO LANES AGREEING IS NOT PORTABILITY.   THE HYPOTHESIS THAT SURVIVED IS THE ONE WHOSE FALSIFYING BRANCH WAS WRITTEN   DOWN BEFORE THE DATA EXISTED.
