# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: periodic-deslopification-sweep
    field: next_action
    value: 'Next slice: criterion 2 — add superseded_by to plan/schema.yaml reference_fields AFTER re-verifying the one existing use (order 466) still resolves, then one retraction batch of split-parents (484,153,246,247,394,396) with ready-count and check-green assertions. Criterion 1''s detector is landed (13-row worklist, run scripts/check-dead-env-branches.sh); criterion 3''s check-deslop-due.sh exists. The sweep protocol itself (kill rule, net-negative diff, filing cap, refutation band) is the remaining big half and wants a host with sweep-budget headroom.'
    ts: "2026-08-23T09:48:16Z"
    host: lenovinha

events:
  - packet_id: periodic-deslopification-sweep
    event:
      type: note
      ts: "2026-08-23T09:48:16Z"
      host: lenovinha
      summary: >
        carry the slice boundary forward
