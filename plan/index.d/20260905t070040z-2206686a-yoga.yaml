# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: plan-binary-built-on-the-host-and-consumed-inside-the-toolbox-dies-on-glibc-skew-and-the-fixture-guard-tests-the-wrong-property
    event:
      type: progress
      ts: "2026-09-05T07:00:40Z"
      agent_id: linux-yoga-claude-20260905t070040z
      host: yoga
      summary: |-
        Closed, landed 5c4682c31. HALF (a) DONE: the precondition guarded on the executable BIT and now resolves through the shared probe, which decides by executing capabilities. An unlinkable ELF is exactly as unusable as an absent one and must take the same branch; it now yields skip:plan-binary-not-runnable-here with the linker own message, because "no runnable binary" and "the binary you built cannot link here" send a reader to different places and not knowing which cost pirria a day. Pre-fix FAILS. HALF (b): I DID THE DETECTION, NOT THE RELOCATION, stating which as asked. cycle-preflight now runs the resolved binary inside the builder the gate uses and NAMES a disagreement with reason and remedy. The relocation (preflight sourcing with-tillandsias-builder.sh the way build.sh line 44 does) is the stronger fix but changes where every host preflight compiles and I could test only one, so it is recommended in the comment rather than done. STRUCTURAL CAUSE CONFIRMED HERE even though the symptom is not: build.sh line 44 re-execs into the toolbox, cycle-preflight has no builder line at all, two loci and nothing compared them. yoga has no skew (host glibc 2.43 equals toolbox 2.43, host-built binary runs in both), so the detector is proven in BOTH directions instead: silent here, and naming the skew when pointed at a binary that dies on exec. A first cut of it reported skew on this host because it treated any stderr as failure and the wrapper announces itself; the verdict now comes from a sentinel the inner command prints. AND MY OWN FIX REGRESSED THE FIXTURE BEFORE THE CONTROL ARM CAUGHT IT: the precondition runs after the script has cd-ed into its scratch repo and the probe candidates are cwd-relative, so it searched a tree with no target/ and fell through to command -v; on a host with ~/.local/bin on PATH that silently still worked, but INSIDE THE TOOLBOX every ledger arm quietly stopped being exercised, a green fixture measuring nothing, which is this packet own class. The replaced [ -x $ROOT/target/... ] was absolute and had no such dependency. Now resolved from ROOT, verified 5 of 5 in BOTH loci. NEW PIN scripts/test-plan-binary-runnability-precondition.sh 5 arms wired into build.sh --check, including the control that a runnable binary still folds the base, without which skip-always would pass everything and delete the validation the lane depends on. It drives a scratch CHECKOUT rather than exporting TILLANDSIAS_PLAN_BIN because the probe honours an override on existence alone by design, a different condition than pirria; and it strips PATH because the probe correctly steps over the unlinkable ELF to a PATH copy, right behaviour and wrong condition, which the first run did while testing nothing. THREE FINDINGS FILED, none in this packet owned_files: 1060-wxdh p1 cycle-preflight installs a non-runnable binary as the canonical expert copy (found by CAUSING it, my positive-control run replaced this host ~/.local/bin/tillandsias-plan with the stub and the next plan call died on GLIBC_2.44; restored by hand from target/release); 1060-6fx7 the plan-only lane blames the ledger when the binary cannot run; 1060-428m the probe-usage guard accepts a COMMENT as compliance, which is why the guard that exists to stop hand-rolled probes passed this one. Gate rc=0 187s.
