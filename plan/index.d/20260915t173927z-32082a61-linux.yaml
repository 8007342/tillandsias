# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: codex-lane-mirror-wait-unbounded
    event:
      type: progress
      ts: "2026-09-15T17:39:27Z"
      agent_id: linux-yoga-claude-20260915t173926z
      host: linux
      summary: |-
        BOTH CRITERIA MET. Criterion 1 was already true and I verified it rather than re-implementing; criterion 2 did not exist and now does.

        CRITERION 1 — VERIFIED, NOT ASSUMED, and I did not stop at the for-loop. The chain, each link measured or read:
          - Both lanes call the SHARED lib-common clone_project_from_mirror; the opencode entrypoint records that its inline block was removed in favour of it. The divergence this row describes was closed by consolidation.
          - The daemon transport loops over seq 1 max_retries with backoff.
          - On exhaustion it writes a FATAL naming the mirror and the project, plus 777-i7hf's three-cause message separating still-seeding from genuinely-empty from upstream-auth-refused — the disambiguation this criterion asks for.
          - The public wrapper preserves the implementation's status verbatim and the experts-async call cannot mask it.
          - errexit is ON in a lane after sourcing lib-common. MEASURED, not inferred: a probe sourced the library and read the shell's own option string, and a second probe showed a failing function stops the script while a control without errexit reaches the line after. So a non-zero return terminates the lane.
        So the lane is bounded, loud, and stops. Nothing to implement.

        A CORRECTION I MADE TO MYSELF MID-AUDIT, because it nearly became a filed defect. I read a set -uo pipefail at line 503 as the file turning errexit back OFF for every sourcing shell, which would have meant the lane ignored the clone's failure. It is the first line of a generated post-commit hook inside a heredoc — the content of another file, not a statement this file executes. I caught it by testing the option rather than reading further, and the same trap produced a second false start: six exit statements that a naive count makes seven, because one is inside that hook body. Filed the underlying confusion as 1203-bxz8.

        CRITERION 2 — BUILT. scripts/test-forge-clone-wait-is-bounded.sh, 4 arms, hermetic and offline: the implementation is extracted with awk (the idiom litmus-forge-clone-reachability-probe-shape already uses on probe_mirror_reachable) and every external is stubbed — git always fails, sleep is a no-op so twelve attempts cost nothing, the mirror host is a fixed string. No network, no podman, nothing read from or written to the real checkout.

        WHAT IT PINS THAT THE EXISTING SOURCE PIN CANNOT. The reachability litmus greps for the loop's source line, so a while-true would red there; that is worth having and I am not duplicating it. This fixture asserts the BEHAVIOUR on exhaustion: that it terminates at all, that a FATAL names the mirror, and — separately — that the status is NON-ZERO. A loop can be bounded in source and still return success on exhaustion, and bounded-loud-and-quietly-successful is the combination no existing arm catches. The three are separate arms because the FATAL text and the status come from different statements and a refactor can keep one without the other.

        Every arm runs the extracted function in a SUBSHELL, because the exhaustion path ends in exit 1 rather than return 1: called in-process it would kill the fixture instead of failing an assertion.

        FALSIFIED, and the first attempt was not falsifiable. Mutating the real library:
          UNBOUNDED loop (seq replaced by while-true)      -> arm 1 REDS
          exhaustion returns 0 instead of exiting 1        -> arm 2 REDS, others green
          a clone that succeeds                            -> arm 4 stays green, so the fixture is not satisfied by an implementation that always dies
        MY FIRST PREMISE GUARD MADE ARM 1 UNFALSIFIABLE: it anchored on the loop literal, so replacing the bounded loop with a while-true — the exact defect arm 1 exists to catch — removed the anchor, and the fixture refused to run and printed NOTHING rather than a red. A premise guard must not key on the thing the arms are trying to detect the absence of. Re-anchored on the FATAL string, which the mutation leaves alone, and only then did arm 1 red.
