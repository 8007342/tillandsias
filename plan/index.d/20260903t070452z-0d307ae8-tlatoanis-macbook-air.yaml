# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: per-phase-accelerator-routing-policy
    field: next_action
    value: 'MEASUREMENT DONE — do not re-measure the crossover. plan/issues/unified-memory-decode-crossover-measured-2026-09-03.md has it: on the fleet''s only unified-memory host the GPU wins decode at EVERY size (1.27x at 0.5B, rising to 1.64x at 7B), so there is no crossover and accel_decode_crossover_b now reads 0.5 rather than Unmeasured. Three defects that made measurements[] structurally unusable are fixed (dedup key ignored model size, the bench never sent the parameter axis, --lane did not force a lane on a one-server host). TWO THINGS REMAIN. (a) EXIT CRITERION 1 IS FALSIFIED AS WRITTEN — ''a 0.5B-class model''s decode is NOT sent to the iGPU on a unified-memory host'' costs 21% of decode throughput here. Rewrite it as a consequence of the per-host measurement, not a fixed rule; that is an operator/author call, not a drive-by edit. (b) THE ROUTING STILL SAYS CPU and this is the real blocker: accel_envelope derives device state from the CONTAINER lane, so macOS Metal reads present-unusable (correct for a forge container per PROBE-7, wrong for host-native inference). Routing therefore sends both phases to the CPU on a host where the GPU wins decode 1.27-1.64x and prefill 3.2-3.8x. The fix is to make the routing question locus-aware — MeasurementRecord.locus is already the axis — and it is a design decision, not a measurement. Also still open from the previous next_action: wire route_phase() into the inference call path; nothing but the envelope renderer calls it.'
    ts: "2026-09-03T07:04:52Z"
    host: tlatoanis-macbook-air

events:
  - packet_id: per-phase-accelerator-routing-policy
    event:
      type: note
      ts: "2026-09-03T07:04:52Z"
      host: tlatoanis-macbook-air
      summary: >
        measurement complete; routing and criterion-1 rewrite remain
