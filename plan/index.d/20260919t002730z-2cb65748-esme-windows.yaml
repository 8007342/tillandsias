# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: accel-probe-blind-to-wsl2-paravirtualised-gpu
    field: next_action
    value: |-
      RELEASED BACK TO READY 2026-09-13 by yolanda, who did the Rust half and cannot do the rest. The detection the prior next_action asked for is LANDED: wsl2_paravirtual_gpu_reason is no longer a string literal. It now reads the live filesystem through wsl2_vulkan_facts_at and decides through the pure, unit-tested wsl2_paravirtual_gpu_reason_from, which distinguishes three states — no loader, loader but no ICD, and loader+ICD present (which is NOT reported as engine-missing any more, because on esmeraldinha it is not). SMALLEST NEXT ACTION IS NOW ESME'S AND NEEDS NO RUST: run the probe on esmeraldinha and paste what the dxg device's unusable_reason says. It should read engine-unverified:vulkan-present-not-enumerated there, where it previously read engine-missing:no-vulkan-icd over a host that carries both. MOVE ~/.cache/tillandsias/capabilities.json ASIDE FIRST AND RESTORE IT — --capabilities serves that cache with nothing in the envelope saying so (1139-xe5m), so an un-bypassed run replays an earlier provisioning state and would show the old reason no matter what the code does. THEN criterion 2's remaining half — enumerate and reject PHYSICAL_DEVICE_TYPE_CPU / DRIVER_ID_MESA_LLVMPIPE — which still needs a host that enumerates, i.e. esmeraldinha, and is the only part of this packet that yolanda could never verify. Criterion 1's operator gate remains lifted and yolanda's declined ICD install remains the right call and is still not the route.


      [2026-09-18T05:50:37Z yolanda] BLOCKER, STATED AS WHAT IS LEFT RATHER THAN AS HISTORY (yolanda, 2026-09-18): THIS ROW CANNOT BE CLOSED BY A WINDOWS HOST AND HAS NOW BEEN HANDED BACK TWICE FOR THAT REASON, on 2026-09-13 and again today. The reason was in the events but not in this field, so the selector offered it a third time.

      WHAT REMAINS NEEDS A HOST THAT ENUMERATES A NON-CPU VULKAN DEVICE OVER /dev/dxg. esmeraldinha is the only host recorded as being in that state (Microsoft Direct3D12, DRIVER_ID_MESA_DOZEN, /dev/dri absent). Criterion 1, the WSL2 half of criterion 3, and BOTH arms of criterion 2 are satisfiable by observation there and nowhere else currently known.

      WHY NOT YOLANDA, precisely: the probe runs NATIVELY on this Windows host, so it can never execute the Linux path this packet is about. Writing the Rust here and handing off the observation is the split that produced both releases. A third Windows pickup would reproduce it.

      PICKUP RULE: take this only if you ARE esmeraldinha, or you have an equivalent dxg-plus-ICD guest AND can run the probe in it. Otherwise leave it ready.


      [2026-09-18T22:26Z esmeraldinha] TAKEN AND DONE UNDER YOLANDA'S PICKUP RULE — I AM esmeraldinha, and the enumeration ran here.

      CRITERION 1 IS THE WHOLE REMAINDER AND IT IS A DECISION, NOT CODE. Criteria 2 and 3 are done and measured on esmeraldinha 2026-09-18 (enumeration over /dev/dxg, llvmpipe/CPU rejected, 95 tests green on Linux); do not re-implement them. What is left: accel_envelope's `state()` counts a device as usable ONLY if its lanes contain `container`, and the dxg device is host-native only, so the envelope still reads accel_class=cpu-only accel_gpu=present-unusable on a host whose GPU now enumerates. THE OWNER MUST PICK ONE OF THREE, because widening `state()` to accept host-native flips macOS Metal and the AMD/Intel host-native arms from present-unusable to usable and breaks criterion 4 in the same commit: (a) amend criterion 1 to require accel_gpu_name and a non-`none` gpu state rather than a class change, which is what the evidence actually supports; (b) probe /dev/dxg from inside a container and, if it passes through, give the device the container lane honestly — this is the only route that satisfies 1 and 4 together and is UNTESTED, the reason the record says container-lane-unverified:dxg-unprobed rather than a structural claim; (c) split the envelope's notion of usable by lane, which is a grammar change touching every consumer and needs its own packet. NEXT PHYSICAL STEP if (b): `podman run --device /dev/dxg` on a WSL2 guest with the Mesa ICD set and see whether a non-CPU device still enumerates inside. TWO STANDING HAZARDS for whoever touches this file: any new accel_reason must stay under 48 characters or slug() truncates it silently into a different token; and NEVER restore a vkDestroyInstance call in enumerate_vulkan_physical_devices without re-running the four-way thread experiment recorded in the 2026-09-18 note — with it, the probe segfaults at thread exit on any non-main thread, which would crash the tray, and neither the Windows nor the macOS gate compiles that code at all. ALSO STILL OPEN, untouched by this session: the wsl2_paravirtual_gpu_reason_at(root) seam 1135-z8gn's follow-up asked for, so the production wrapper remains uncovered on every host.

      PROVENANCE OF THIS FIELD, so the next reader knows why it looks rebuilt: my first write of this field (20:38:24Z) was --append against a fold that did not yet carry yolanda's 05:50:37Z append, because their fragment reached trunk at 6ac0f8d2a only afterwards. LWW is per-FIELD, not per-line, so my stale-base value would have silently deleted the blocker and the PICKUP RULE that exist precisely to stop a fourth Windows pickup of this row. This value is yolanda's text verbatim with mine appended after it. --append is only append-safe against the base you actually hold; fetch before you use it on a field other hosts are writing.

      [2026-09-19T00:27:30Z esme-windows] [2026-09-19T00:28Z esmeraldinha] THE dxg CONTAINER MEASUREMENT IS DONE; WHAT IS LEFT IS ONE PRODUCT DECISION AND NOTHING ELSE. Do not re-run the probe and do not re-implement criteria 2 or 3 — enumeration, llvmpipe/CPU rejection, and the container measurement are all recorded with evidence in the 2026-09-19 notes.

      THE DECISION, which is the coordinator's or the owner's and not a worker's: MAY THE PRODUCT BIND-MOUNT THE HOST'S WINDOWS DRIVER STORE (/usr/lib/wsl, read-only, which includes `drivers` as well as `lib`) INTO THE INFERENCE CONTAINER ON WSL2 HOSTS? That single question decides this row.
        - IF YES: the dxg device gains lanes=[host-native, container] BY MEASUREMENT, accel_envelope's existing container-lane predicate then reports it usable with no change to state(), and criterion 1 is satisfied from evidence. The work is then: mount it in the inference container recipe, change the record's reason accordingly, and re-run the probe to confirm accel_gpu stops reading present-unusable.
        - IF NO: this row CLOSES with dxg recorded present-unusable, lanes=[host-native], and the reason named — and the reason should stop saying `unprobed`, because it has now been probed. A measured replacement under the 48-character slug() cap: `container-lane-absent:dxg-needs-wsl-drivers` (43).

      WHY THE DECISION IS NOT OBVIOUS AND MUST NOT BE MADE BY WHOEVER PICKS THIS UP NEXT: it means projecting thousands of Windows .inf driver directories into a Linux container, host-specific and WSL2-only, purely so a translation layer can resolve a D3D12 device. That is a product topology and security question, not an accel-probe question.

      MEASURED FACTS THE DECISION RESTS ON, so nobody has to re-derive them: --device /dev/dxg alone puts the node in the container and yields llvmpipe ONLY. Adding /usr/lib/wsl/lib makes dzn load and fail with `ID3D12DeviceFactory::CreateDevice failed` — necessary, not sufficient. Adding the whole /usr/lib/wsl including `drivers` enumerates the SAME TWO DEVICES as the host: Dozen INTEGRATED_GPU driverID=23 beside llvmpipe CPU driverID=13. So the node crosses, the libraries are mountable, and the driver store is the blocker.

      A HARD REQUIREMENT ON ANY IMAGE FOR THIS LANE, independent of the decision: it MUST pin a Mesa that carries dzn. debian:13 and fedora:41 both ship Mesa 25.0.7 with NO dzn_icd; dzn arrives with mesa-vulkan-drivers-26.1.6-1.fc44. An image without it enumerates llvmpipe only and is INDISTINGUISHABLE from a dxg passthrough failure — that false negative cost two image builds here, and shipping it would be the criterion-3 failure wearing a container.

      STILL OPEN AND UNTOUCHED by any of this session's work: the wsl2_paravirtual_gpu_reason_at(root) seam that 1135-z8gn's follow-up asked for, so the production wrapper remains uncovered on every host. And the standing hazards: any new accel_reason must stay under 48 characters or slug() silently truncates it into a different token, and NEVER restore a vkDestroyInstance call in enumerate_vulkan_physical_devices without re-running the four-way thread experiment in the 2026-09-18 note.
    ts: "2026-09-19T00:27:30Z"
    host: esme-windows
