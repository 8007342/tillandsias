# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: forge-pids-ceiling-512
    event:
      type: note
      ts: "2026-09-01T22:15:00Z"
      host: macuahuitl
      summary: >
        Three refinements from the pair's cross-correction (each corrected
        the other once on this packet within three hours). (1) DEAD-CODE
        CORRECTION (esmeraldinha): container_profile.rs's forge/terminal
        profiles have ZERO production callers — test-module references
        only — so pirria's fingerprint stands on THREE live legs, and the
        root cause reframes as DUAL-SOURCE-OF-TRUTH: 65fe4e766 fixed the
        copy that documents the reasoning (main.rs, comments attached) and
        missed the copy that has the effect (tray/mod.rs:2157, launched
        both floor guests — confirmed on esmeraldinha's host too by three
        live values), while pids_limits_match_container_roles() guards the
        DEAD copy at the buggy value (inference_profile env_vars
        precedent). Fix scope updated: raise the live site, delete-or-
        align the dead profiles, move the test with whatever survives,
        rationale on every touched site. (2) NEW EXHAUSTION VECTOR
        (esmeraldinha, adjacent-log-line attribution): the 64 brew
        auto-install shims fork a full Homebrew install off a bare
        --version probe — her 100 denials are ATTRIBUTED to a hyperfine
        shim install racing ordinary commands. The 512 ceiling is
        reachable by an INCIDENTAL TOOL PROBE during litmus/CI/normal
        agent activity, not only by builds. (3) MEASUREMENT-PROTOCOL
        RULING: TILLANDSIAS_BREW_AUTOINSTALL=0 (the shim's documented
        off-switch) is pair-standard for every timed run, with the
        installed-tool surface banked pre-run on both hosts — a run that
        silently installs software over the network is not a measurement.
        Step (d) remains HELD; also on record: floor hardware fans out
        LESS than the 62GiB 667-se87 incident host (cargo -j4), so the
        crash folklore overstates floor danger, while 240 ambient denials
        mean any build starts from a pressured cgroup.
