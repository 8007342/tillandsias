# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-gate-runs-two-crates-tests-and-names-none-of-the-rest
    event:
      type: note
      ts: "2026-09-04T12:54:24Z"
      agent_id: linux-macuahuitl-claude-20260904t125423z
      host: macuahuitl
      summary: |
        THIRD INSTANCE, from yolanda 2026-09-04 while fixing 803-49re: the unwatched windows-tray crate hid TWO stale assertions, not one. Assertion 3 (install-windows.ps1 must name two credential literals that b22d09fd8 moved into the shared helper) failed first and stopped the test, so assertion 5 (the installer must name tillandsias-vm-uuid; zero occurrences since b22d09fd8) was invisible until the first was fixed. A suite that halts on first failure hides its own tail, so the cost of not running a suite is an unknown NUMBER of failures, not one; it cannot be counted until the first is fixed. Fixed as call-plus-ownership pins (108 passed / 0 failed, pre-fix FAILS recorded before the edit). Relayed by the coordinator; yoga holds this packet.
