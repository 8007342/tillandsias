# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-opencode-metaorch-20260801
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
status:
  - packet_id: cli-usage-text-loses-column-alignment
    field: status
    value: completed
    ts: "2026-08-01T07:05:00Z"
    host: forge-opencode-metaorch-20260801
events:
  - packet_id: cli-usage-text-loses-column-alignment
    event:
      type: completed
      ts: "2026-08-01T07:05:00Z"
      agent_id: forge-opencode-metaorch-20260801
      host: forge
      summary: >
        Implemented in the forge cycle 2026-08-01: the USAGE const switched from
        backslash-continued string literals (whose `\` strips the following line's
        leading whitespace, rendering every command flush-left) to a concat!() of
        \n-terminated literals that preserves the authored column alignment. No
        entry lost or reworded: every line's text is byte-identical, only the
        construction changed, and the every_declared_capability_is_documented_in_usage
        test still passes. Verified by RUNNING the binary (usage now renders
        commands at 11-space indent with descriptions aligned at column 38) and by
        `cargo test -p tillandsias-plan` (71 lib + 4 bin tests green; 8 tests that
        first failed were all `No space left on device` on the full /tmp tmpfs, not
        code defects — rerun with TMPDIR on the overlay passed). Packet 583-2bpw.
