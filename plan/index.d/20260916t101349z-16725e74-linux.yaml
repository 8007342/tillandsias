# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: gate-stamp-refusal-names-mtime-while-deciding-on-content
    field: status
    value: completed
    ts: "2026-09-16T10:13:49Z"
    host: linux

events:
  - packet_id: gate-stamp-refusal-names-mtime-while-deciding-on-content
    event:
      type: completed
      ts: "2026-09-16T10:13:49Z"
      host: linux
      summary: |-
        status 'completed' with evidence_refs: 774070162 on linux-next: gate-stamp.sh movers names content movers on the digest's own axis, the mtime list survives as a labelled live-writer hint, and scripts/test-gate-stamp-names-content-movers.sh pins both shapes as opposites (14 arms)
