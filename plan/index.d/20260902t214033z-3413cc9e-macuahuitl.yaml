# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: tray-launched-forge-seeded-from-main-not-the-checkout-branch
    field: priority
    value: p2
    ts: "2026-09-02T21:40:33Z"
    host: macuahuitl

events:
  - packet_id: tray-launched-forge-seeded-from-main-not-the-checkout-branch
    event:
      type: note
      ts: "2026-09-02T21:40:33Z"
      host: macuahuitl
      summary: >
        downgraded from p1: the reader is correct and the two dangerous causes are ruled out; what remains is an unreported path on a multi-checkout host, which is a diagnosability defect rather than a wrong-branch bug
