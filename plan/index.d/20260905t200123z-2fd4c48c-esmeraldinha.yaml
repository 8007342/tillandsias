# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: guest-pulls-full-multiarch-qemu-user-static
    field: status
    value: in_progress
    ts: "2026-09-05T20:01:23Z"
    host: esmeraldinha

events:
  - packet_id: guest-pulls-full-multiarch-qemu-user-static
    event:
      type: note
      ts: "2026-09-05T20:01:23Z"
      host: esmeraldinha
      summary: |-
        claimed 2026-09-05T17:11:34Z; the claim event landed without the status transition (1079-qb8k). Open question: --exclude=qemu-user-static* versus weak-deps-off, then a cold provision re-measure.
