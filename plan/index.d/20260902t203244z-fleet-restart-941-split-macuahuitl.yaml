# Ledger fragment — append-only, IMMUTABLE once written.
# macuahuitl, fleet restart 2026-09-02T20:21Z: 941-trcf split into its three claimable slices; 959-fpc5 workstation sample.

packets:
  - packet_id: gate-shared-ledger-parse
    order: 964-tzmp
    status: ready
    kind: enhancement
    priority: p1
    desired_release: v0.5
    pickup_role: linux
    release_target: convergence-velocity-milestone
    split_from: gate-cycle-runs-a-flat-list-instead-of-a-plan
    capability_tags:
      - build-gate
      - performance
      - ledger
    estimated_hours: 6
    title: |
      One warm --check makes ~108 ledger-loading tillandsias-plan invocations (~59s on the N150, ~26% of the warm gate) — parse the ledger ONCE per gate and let the checks consume the fold.
    deliverable: |
      A per-gate folded ledger snapshot (or a warm plan-binary server) that every
      ledger-reading gate step consumes, so a forced --check performs one full
      parse; invocation count and per-step wall measured before/after on
      macuahuitl and on a floor host; verdict behaviour unchanged on a
      deliberately-broken tree.
    context: |
      Slice A1 of 941-trcf (its next_action; pirria's counter, evidence
      cc8a1feb1). The parent's measurement criterion is already met: ~108
      ledger-loading invocations at ~560ms on the N150 (220ms on the
      workstation). METHOD WARNING carried from the parent: a PATH shim
      measures ZERO — plan-binary-probe.sh prefers
      ./target/release/tillandsias-plan and consults PATH only as fallback
      (721-nyev); instrument the probe's resolution, not PATH. The 911-m7js
      memo already removes the archiver check from the unchanged-ledger path;
      this slice is about the other ~107 invocations.
    exit_criteria:
      - a forced --check performs one full ledger parse, COUNTED (not inferred), and the count is recorded beside the ~108 baseline
      - per-step gate wall is re-profiled on macuahuitl and on one floor host with the numbers beside the parent's baseline
      - the gate's verdict behaviour is unchanged — the same deliberately-broken tree fails the same checks before and after
  - packet_id: archiver-check-internal-profile
    order: 964-js34
    status: ready
    kind: enhancement
    priority: p2
    desired_release: v0.5
    pickup_role: linux
    release_target: convergence-velocity-milestone
    split_from: gate-cycle-runs-a-flat-list-instead-of-a-plan
    capability_tags:
      - build-gate
      - performance
      - ledger
    estimated_hours: 3
    title: |
      archive-plan-packets.sh --check is 48s warm in ONE invocation on the N150 (22-25s on stronger hosts) — check-internal work a shared parse does not touch; profile inside it before designing anything.
    deliverable: |
      Per-phase timing of the archiver check on a floor host and on the
      workstation, and the single largest phase either cut or justified. The
      911-m7js memo already skips the check on an unchanged ledger, so the
      target is the MISS path.
    context: |
      Slice A2 of 941-trcf (its next_action). Independent of A1: a shared
      parse cannot reach work that happens inside one invocation.
    exit_criteria:
      - the archiver check's internal phases are timed on two regimes and recorded as an event
      - the largest phase is reduced and the memo miss path re-measured, or a recorded reason why not
  - packet_id: gate-ledger-native-fs-staging-windows
    order: 964-9yyp
    status: ready
    kind: enhancement
    priority: p1
    desired_release: v0.5
    pickup_role: windows
    release_target: convergence-velocity-milestone
    split_from: gate-cycle-runs-a-flat-list-instead-of-a-plan
    capability_tags:
      - build-gate
      - performance
      - windows
      - wsl2
    estimated_hours: 4
    title: |
      Windows pays a 24.6x multiplier on every ledger-reading gate step (9P/drvfs, the 895-bkrn family) — stage plan/ to a native FS once per gate and point the ledger checks at it.
    deliverable: |
      The ledger-reading gate checks read a native-FS copy of plan/ (tmpfs or
      the distro ext4) refreshed once per gate on WSL2; verified against the
      archiver check first (552.5s baseline), then the full gate (1214s).
    context: |
      Slice B of 941-trcf (its next_action; promoted on yoga's control arm;
      belongs with yolanda's WSL2 build-parity thread per the operator). It
      changes WHERE the ledger the checks read lives, never how many times
      build.sh invokes tillandsias-plan — that is A1 (964-tzmp) and the two must
      not be conflated.
    exit_criteria:
      - the archiver check on yolanda is re-measured against the staged copy beside the 552.5s baseline
      - a full forced --check on yolanda is re-profiled per step beside the 1214s baseline
      - the gate's verdict behaviour is unchanged on a deliberately-broken tree

events:
  - packet_id: gate-cycle-runs-a-flat-list-instead-of-a-plan
    event:
      type: note
      ts: "2026-09-02T20:33:02Z"
      host: macuahuitl
      summary: >
        SPLIT at the fleet restart (2026-09-02T20:21Z) into the three slices its
        own next_action declared claimable: A1 gate-shared-ledger-parse (964-tzmp,
        linux, claimed by macuahuitl), A2 archiver-check-internal-profile
        (964-js34, linux), B gate-ledger-native-fs-staging-windows (964-9yyp, windows,
        dispatched to yolanda). The parent retains only the Lua check-planner
        evaluation, which its SEQUENCE places after the cheap layers land, so
        it goes blocked rather than competing with its own children (750-zrt4).
        Not obsoleted: that status would falsely satisfy dependents.
  - packet_id: forge-pids-ceiling-512
    event:
      type: note
      ts: "2026-09-02T20:33:02Z"
      host: macuahuitl
      summary: >
        WORKSTATION-REGIME SAMPLE (the fresh-4096 sample the 09-02 handoff
        asked for), NOT a floor measurement. macuahuitl, v56.9.2.1 stable
        curl-installed, tray-launched lanes: podman inspect PidsLimit=4096 on
        both running forges (tillandsias-tillandsias-forge-claude and the
        operator's tillandsias.org forge); inside: pids.max=4096,
        pids.peak=81 and 66, pids.current=59 and 32, pids.events 'max 0',
        uptime 5589s (~93 min) with an agent session live in each. So the
        Rust launcher path (main.rs:3236/:6510) delivers 4096 on this host
        and no denial occurred at these peaks. What this does NOT say: the
        floor's peak, or which launcher launched the floor forges — pirria
        and esmeraldinha were asked for the same read (pids.max + events +
        /proc/1/cmdline) in their own regimes; their lines decide the packet.
