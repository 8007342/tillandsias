# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: headless-reads-the-vsock-forwarder-target-the-tray-pushed-over-the-control-wire-after-ready-and-no-capability-bit-is-needed-because-the-psk-binds-the-build-version
    event:
      type: evidence
      ts: "2026-09-21T03:56:20Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260918t210700z
      host: macbookair-macos
      summary: |-
        THE WRITER WORKS AND THE SEAM IS GONE. work/1309-rb3p at 81fe8e9be, gate BUILD_RC=0 (1658s, stamp fresh). Closure with NOTHING written by hand in the guest: LIVE target-from-tray-after-5s=[2:42421], forwarder=running, inference=absent, body=[{"version":"host-native-closure-probe"}]; DEAD (same forwarder, dead host port) body=[]; ok:macos-vsock-inference-closure:5. A CORRECTION TO THIS ROW'S OWN PREDICTION, which said 'the same probe minus its one line is the unchanged closure': removing the line is NOT sufficient. A fixture that merely stops seeding the target measures nothing about the writer — it would pass whether or not the tray can configure the guest. The fixture must DELETE the target, WAIT for the tray, and ASSERT that it arrived; that is a new arm (ARM 0), not a deletion. I have made that change and the row's prediction should be read as superseded rather than satisfied. PLACEMENT WAS A REAL DEFECT OF MINE, found by measurement and worth recording because the reasoning sounded right: I first sent from poll_vm_status_once — post-Ready, holding a handshaked client, both TRUE — and the guest's target file was still MISSING after 240 seconds. The cause is in that file's own comment: 'while the push subscription is delivering, the poll is suppressed entirely'. I had verified a PROPERTY of the location (it runs after Ready) and not the thing that mattered (whether it runs at all). The send now rides the push listener's connect path, which is strictly better than a patch: it fires on every RECONNECT, and since /run is tmpfs a guest that restarted under a still-running tray has lost the file and is reconfigured exactly then. The poll copy remains for the unhealthy-push regime, so both are covered. HOW IT WAS ISOLATED, four runs each removing a candidate rather than adding a theory: run 1 failed (could have been anything); inspecting both rebuilt artifacts ruled out deployment (tray carries the sender, guest carries the handler x3 and its log line); run 2 added a wait; run 3 surfaced the guest's full result and gave the decisive MISSING-after-240s, which ruled out a race and pointed at the writer; the source read then named the suppression. THE VARIANT: appended at index 32 with the pinning map, sample list, DECLARED_VARIANTS 32->33 and kind() moved together. The SAMPLE entry is the one that matters — a trailing unsampled variant passes the contiguity check and is caught only by the one-past-the-end probe, which that test says in as many words. Registered explicitly in the dispatch matrix (Vsock=>Handle, UnixSocket=>Unsupported) rather than left to a catch-all, because that file records two earlier omissions caught on their first live run by an unregistered variant answering Unsupported. NO CAPABILITY BIT, as the row predicted and for the reason it gave: derive_psk binds the build version into the channel key, so a peer that does not know this variant cannot complete a handshake to receive it.
