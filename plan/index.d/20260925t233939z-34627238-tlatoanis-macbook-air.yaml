# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: both-non-timeout-failures-in-a-red-release-tier-run-passed-on-re-run-in-two-regimes-so-the-gates-failure-rate-may-be-dominated-by-non-determinism-that-a-retry-silently-launders
    event:
      type: progress
      ts: "2026-09-25T23:39:39Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260925t233939z
      host: tlatoanis-macbook-air
      summary: |
        Slices 1-2 on work/1242-4x53 (73333ff0d), not landed.
        1 (aa080106d): cycle-metrics --emit-timing accepts reproduced=yes|no; absent or invalid writes no key, so absent never reads as "did not reproduce".
        2 (73333ff0d): timing_emit takes an optional 5th arg, archive_check_log passes it on for check:<id> records; four-argument callers unchanged.
        Fixture scripts/test-timing-record-carries-reproduced.sh 7/7 on macOS bash 3.2; refuses 3/5 against the pre-slice-1 emitter and 6/7 against pre-slice-2 timing_emit. Bound as step 5 of litmus:timing-telemetry-implausible-guard-shape.
        NEXT (slice 3): local-ci checks are inline blocks with no shared runner, so a same-regime re-run needs a runner wrapper, or a per-check re-run starting with the two measured failures (the rust groundtruth test, enclave-service-health step 3). Then the seeded-regression control (criterion 4), then the per-host split (criterion 2).
