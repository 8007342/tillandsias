# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260811t205035z
packets:

  - packet_id: status-ladder-enforcement-wave-2
    order: 686-7qcm
    status: ready
    desired_release: v0.5
    release_target: convergence-velocity-milestone
    priority: p2
    title: "Finish enforcing the 650-dq6u closure ladder: rank-aware fold merge, invisible-block visibility, and the new-closure evidence gate"
    kind: infra
    deliverable: fold merge respects closure_rank; check/plan_status report dependents parked behind non-terminal states; check hard-fails evidence-less new closures
    depends_on: []
    pickup_role: linux
    capability_tags: [plan, ledger, crdt, enforcement, vocabulary]
    context: >
      Wave 1 landed with the operator ruling (650-dq6u, 2026-08-11): four-rung
      closure ladder implemented < completed < verified < done in methodology
      status_transition_protocol, reconciled schema/index vocabulary
      (retired: provisional, failed-retryable, claimed, stalled), shared
      is_terminal_status + closure_rank helpers, the set-field write gate
      (out-of-vocabulary refusal; downgrade refusal without --reopen-evidence,
      which records the mandatory falsified event; --evidence required for
      closure rungs), the reconciled fragment-status-loss guard, and the
      re-pinned litmus (8 steps green as-wired incl. isolated-copy write-gate
      probes and a positive control). This packet is the remaining
      enforcement so the ladder holds against paths that bypass set-field.
    exit_criteria:
      - the fold's LWW status merge is rank-aware — among closure-ladder values the HIGHER closure_rank wins regardless of timestamp; a rank-lowering entry folds in ONLY when the same fragment carries a falsified event for that packet; working-state values keep plain LWW; unit tests cover both orders of arrival
      - tillandsias-plan check (and plan_status for a single packet) emit an INVISIBLE-BLOCK report — every packet whose depends_on includes an implemented / needs_clarification / blocked / failed packet is listed with the parked status and its named outstanding action, so 627-sgtt-shaped hand-offs are visible instead of silently blocking
      - check hard-fails any plan/index.d fragment dated on/after 2026-08-12 whose closure-rung status entry lacks an accompanying event with evidence refs (base-ledger history exempt — no retroactive falsification); the litmus gains a step pinning this
      - plan_next surfaces implemented packets whose verification hand-off matches the caller's role as explicit 'verification pickup' rows (distinct from claims), or records a reasoned decline of that feature in this packet
    events:
      - type: filed
        ts: "2026-08-11T20:50:35Z"
        agent_id: linux-mutable-session-20260811
        host: linux_mutable
        summary: >
          Split from the 650-dq6u implementation session: write-time gates
          landed same-day; fold-merge rank rule, invisible-block visibility,
          and the fragment evidence gate need engine work with their own
          tests, filed here so the loop can drain them.

events:
  - packet_id: terminal-status-vocabulary-is-split
    event:
      type: note
      ts: "2026-08-11T20:50:35Z"
      agent_id: linux-mutable-session-20260811
      host: linux_mutable
      summary: >
        The 649-b2e4 interim union (completed|done|obsoleted) is SUPERSEDED by
        the 650-dq6u operator ruling 2026-08-11: the union was ratified and
        widened to completed|verified|done|obsoleted as the terminal set of a
        four-rung closure ladder (implemented < completed < verified < done,
        merge = max, falsified event is the only path down). is_terminal_status
        now documents the ruling instead of the disagreement; enforcement wave 2
        tracked as 686-7qcm.
