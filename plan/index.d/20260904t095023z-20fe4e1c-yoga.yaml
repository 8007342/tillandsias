# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: hardware-fingerprint-makes-the-twin-hosts-a-substrate-control
    event:
      type: progress
      ts: "2026-09-04T09:50:23Z"
      agent_id: linux-yoga-claude-20260904t093006z
      host: yoga
      summary: |-
        SECOND HALF DELIVERED (bdb3ac8ab): capability-matrix --by-hardware re-keys the matrix on (fingerprint, substrate). group_by_hardware and isolates_substrate had four unit tests and no production caller, so the grouping was computable but unreachable and every reader still keyed on hostname; this is the face that makes it askable. Live fleet output: four identified hardware groups, ALL control=no, and six unidentified rows including both of yolanda's vantages. The negative answer is the deliverable — the fleet has no substrate control, which is this packet's own hard-won conclusion now readable from the tool instead of from event prose. Two litmus steps pin the face: the grammar plus the loci>1 <=> control=yes invariant, and a negative control that an unidentified row is never folded into a group. FIRST HALF WAS ALREADY DONE by earlier cycles (465d98d2b fingerprint tool + gate; cross-vantage refusal arm; probe emits hardware_fingerprint and four live rows carry it), so this cycle closed the gap between a computed identity and a readable comparison. NOTE FOR WHOEVER READS THE TITLE: it is refuted and stays refuted — yoga is a Ryzen AI 5 340 6c/12t, yolanda a Ryzen AI 7 350 8c/16t, so the twin pair is not a substrate control and the tool now says so on demand rather than in a comment.
