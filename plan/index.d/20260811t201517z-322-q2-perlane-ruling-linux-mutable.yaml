# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260811t201517z
events:
  - packet_id: mirror-authenticated-push-transport
    event:
      type: decision
      ts: "2026-08-11T20:15:17Z"
      agent_id: linux-mutable-session-20260811
      host: linux_mutable
      summary: >
        OPERATOR RULING (The Tlatoani, interactive session 2026-08-11): design
        doc Q2 is RULED — signing sidecar PER LANE, as D5 specifies. One
        ssh-agent sidecar container per forge lane; the per-project
        single-sidecar alternative is declined. Isolation wins the
        resource-vs-isolation call. Recorded in the design doc Q2 section
        (plan/issues/ssh-ca-forge-mirror-push-design-2026-07-31.md) in the
        amendment style. STILL OPEN on the same doc: Q1 (env-token hole scope
        — under discussion with the operator this session), Q3, Q4, and the
        S1 signature block; the operator asked for an interactive walkthrough
        of the transport design before signing, so 451 remains blocked on the
        signature, not on Q2. This ruling does not alter any prior decision;
        it narrows D5's open alternative to its primary reading.
