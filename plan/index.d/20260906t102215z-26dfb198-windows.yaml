# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: compaction-renders-packet-fields-in-a-different-order-than-the-fold-so-trunk-is-red
    field: status
    value: in_progress
    ts: "2026-09-06T10:22:15Z"
    host: windows

events:
  - packet_id: compaction-renders-packet-fields-in-a-different-order-than-the-fold-so-trunk-is-red
    event:
      type: progress
      ts: "2026-09-06T10:22:15Z"
      host: windows
      summary: |-
        status 'in_progress' (not terminal) with evidence_refs: claimed by yolanda; independently reproduced on this host BEFORE claiming, and independently confirmed not-mine by removing my own two fragments and re-running (FAILED both ways)
