# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260812t150500z
packets:

  - packet_id: freshness-buckets-by-criticality
    order: 706-iabh
    status: ready
    desired_release: v0.6
    priority: p2
    title: "Freshness BUCKETS: close-eye audit for the critical path, batched quick-look for the unimportant bulk, telemetry-flagged untracked candidates"
    kind: enhancement
    deliverable: bucketed component_freshness (criticality tiers + per-tier rate) + telemetry that flags untracked freshness candidates + size/kind batchability heuristics
    depends_on: [freshness-coverage-rate-decision]
    pickup_role: linux
    capability_tags: [methodology, freshness, convergence, telemetry, scope]
    context: >
      OPERATOR DIRECTIVE (The Tlatoani, 2026-08-12, while ruling 640-iujb):
      land the flat near-term rate first (batch unchanged re-stamps + one
      careful audit per cycle for changed/new, recorded in methodology.yaml
      component_freshness.audit_rate), THEN evolve it into a richer bucketed
      model. Three pieces: (1) CRITICALITY BUCKETS — components on the
      critical path (release path, security/vault/credential flows, the gate
      itself, the plan engine) get a FREQUENT close-eye audit; globally
      unimportant components get a batched "quick look" that is cheap and
      infrequent. The bucket a component falls in must be script-derivable
      (path/kind/dependents), not hand-assigned. (2) TELEMETRY: flag UNTRACKED
      components (in-scope per 640-iujb's defined-subset but never stamped) as
      freshness candidates so the queue surfaces what it has never seen, not
      just what is stale. (3) size/kind HEURISTICS for batchability beyond the
      content-hash-unchanged rule (e.g. short config files, low-risk kinds).
      Supersedes the flat rate in methodology component_freshness.audit_rate
      when it lands. Reuses the canonical 30d threshold (606-vaua) and the
      defined-subset target (640-iujb).
    exit_criteria:
      - "component_freshness declares criticality buckets whose membership is script-derivable, with a per-bucket audit cadence; a litmus asserts a critical-path component lands in the close-eye bucket and an unimportant one in the quick-look bucket"
      - "the freshness advisory flags untracked in-scope components as candidates (distinct from stale stamped ones), with a count reported"
      - "batchability heuristics beyond content-hash-unchanged are stated script-checkably, or a reasoned decline recorded"
    events:
      - type: filed
        ts: "2026-08-12T15:05:00Z"
        agent_id: linux-mutable-session-20260812
        host: linux_mutable
        summary: >
          Filed from the operator's 640-iujb ruling: flat rate now, bucketed
          criticality model + untracked-candidate telemetry + size/kind
          heuristics as the follow-on for v0.6.
