# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: macos-terminal-attach-renders-corrupt-from-launch
    field: next_action
    value: 'macOS lane, two items. (a) VERIFY D1 VISUALLY — the escape bytes are already in place and unit-pinned (SCREEN_HOME at the ATTACH boundary in attach_client.rs, deliberately NOT in MODE_RESET, which is also written at teardown where a clear would erase the finished session). Launch a lane, confirm the session begins at row 1 with no Terminal.app banner above it, and falsify by reverting the SCREEN_HOME write so the offset returns. A linux host made the edit and cannot honestly close this criterion. (b) IMPLEMENT D2 (action_host.rs is macOS-owned): the 10s attach-client wait must stop birthing the PTY at a 24x80 fallback a real window contradicts. DISTINGUISHING SIGNATURE already recorded: D2 self-corrects on the first window resize, D1 does not. REQUIRED NEGATIVE CONTROL: a genuinely headless launch with no attach client must still succeed rather than hang. DO NOT RE-DERIVE THE THREE REFUTED HYPOTHESES in the context — PtyOpen is NOT hardcoded 24x80 on the interactive path, TERM is NOT dumb there, and raw mode IS established before any guest byte reaches the tty. The packet says the refutations are the expensive part; they remain valid and re-deriving them is the waste it warns about.'
    ts: "2026-09-02T01:01:01Z"
    host: linux
