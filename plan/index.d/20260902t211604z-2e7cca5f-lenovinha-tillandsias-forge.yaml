# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: build-check-unrunnable-in-forge-ruby-absent
    event:
      type: progress
      ts: "2026-09-02T21:16:04Z"
      agent_id: forge-forge-tillandsias-claude-20260902t211556z
      host: lenovinha-tillandsias-forge
      summary: >
        ESCALATION, measured minutes after filing: the missing-ruby shim does not merely produce a false verdict — it WEDGES THE CONTAINER.  Each invocation of the ruby shim launches 'timeout 150 brew install --formula ruby', which fails by design under attestation verification. Nothing reaps them and nothing backs off, so every check step that touches ruby stacks another. Measured on lenovinha-tillandsias-forge:      ps -eo comm | sort | uniq -c | sort -rn        3206 bash         801 timeout        <- all 'brew install --formula ruby'      /sys/fs/cgroup/pids.current  4065     /sys/fs/cgroup/pids.max      4096  At 4065 of 4096 PIDs the container could no longer fork a thread, and the failure surfaced NOWHERE NEAR its cause: git began failing with 'fatal: unable to create threaded lstat: Resource temporarily unavailable' and 'fatal: fetch-pack: invalid index-pack output', so a commit and a push failed while the visible symptom looked like repository or network corruption. pkill of the stacked brew processes took pids.current from 4065 to 60 and git recovered immediately.  So the severity is higher than 'a check cannot run': a forge that runs ./build.sh --check is left unable to commit or push its own work, which is the exact state the Non-Negotiable Exit Contract exists to prevent, and the diagnostic trail points at git rather than at ruby.  Add to the exit criteria: the on-demand tool shim must not stack unbounded install attempts — one failed acquisition per process lifetime, remembered, with the refusal returned immediately thereafter.
