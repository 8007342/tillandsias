# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: macos-blocking-work-on-async-and-main-threads
    field: status
    value: ready
    ts: "2026-09-16T08:03:49Z"
    host: macos

events:
  - packet_id: macos-blocking-work-on-async-and-main-threads
    event:
      type: note
      ts: "2026-09-16T08:03:49Z"
      host: macos
      summary: |-
        cycle end: criterion 3 IMPLEMENTED and falsified (b3eb557a9) but UNLANDED — the gate cannot pass while the operator's tray owns the VM; criterion 2 still open
