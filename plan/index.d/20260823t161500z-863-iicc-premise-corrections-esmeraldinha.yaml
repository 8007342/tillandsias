# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by windows (esmeraldinha), 2026-08-23T16:15:00Z.
#
# Corrections to 863-iicc's own premise, found by an adversarial verification
# pass AFTER the packet was landed. Fragments are immutable, so this is a new
# event rather than an edit — and the orchestrator is being asked for a ruling,
# so it must rule on the corrected premise.

events:
  - packet_id: lane-branch-methodology-for-a-fleet-that-outruns-its-slowest-host
    event:
      type: note
      ts: "2026-08-23T16:15:00Z"
      agent_id: windows-esmeraldinha-opus5-20260823T152632Z
      host: windows
      summary: >
        THREE CORRECTIONS to this packet's own context, plus one piece of
        provenance stronger than anything it cited. Filed because the packet
        asks for a ruling and a ruling deserves the corrected premise.

        CORRECTION 1 — "a host may create a lane today" is right about the
        RULING and misleading about the EFFECT. The 2026-08-06 rescope does
        permit anonymous lane creation as interim containment while deletion
        waits for order 451. But order 502 BUNDLES seeding, lane-exit.sh and
        ledger-checkpoint.sh into one packet, so the sanctioned creation path
        ships no sooner than the deletion path — methodology says exactly this
        ("until it lands no agent/* or salvage/* refs are seeded, so this
        exclusion is vacuously satisfied"). What this host did was create a
        lane BY HAND, outside the bundle. That is permitted by the ruling and
        unblocked by the hook, but it is not "the shipped path arriving early";
        there is no shipped path. If the orchestrator wants lanes usable
        fleet-wide before 451, the cheapest route may be to UNBUNDLE 502 —
        seeding does not need the authenticated deletion transport, only
        lane-exit's final `git push origin :<lane>` does.

        CORRECTION 2 — enforcement is live, not merely documented. Rung 2's
        warn-only grammar is in code at images/git/entrypoint.sh:137, carrying
        the agent/*+salvage/* creation regex. So the namespace is enforced
        (warn-only) on the mirror path today. The packet under-claimed this by
        calling rung 2 merely "completed".

        CORRECTION 3 — "451 is two levels from claimable" overstates the
        remoteness. Of its five dependencies: 322, 579 and 606-9wqd are
        completed/done; 610-txvr's SOLE dependency (579) is completed, so it is
        dependency-clear and actionable NOW; and 606-bvnp reads `blocked` only
        because it was SPLIT into four slices (722-hthz implemented, 722-ft6g
        implemented, 722-uern ready, 722-ecne blocked), with 722-ecne itself
        split into six 749-* rungs that are three completed, two implemented,
        one ready. The identity arm runs three levels deep but its frontier is
        nearly-finished work awaiting verification, not unstarted work. 451 is
        closer than the packet implied, which weakens the "will not unblock
        soon" argument for an interim ruling — though it does not remove the
        need for one, since the fleet is contending now.

        PROVENANCE the packet did not have, and it is decisive:
        `git show e47f17b67^:plan/index.yaml` shows packet 502 with
        `status: ready` and a depends_on list WITHOUT 451, immediately before
        the 2026-08-06 compaction. So the security rescope is provably the
        event that added the 451 dependency and moved rung 3 from ready to
        pending — the stall has a single identifiable cause and a single
        identifiable reverser.

        Also worth carrying into any ruling: 503's stale `status: ready` has a
        mechanical cause, not an oversight. It carries no `blocked_on` field,
        so nothing ever flipped it when its dependency went pending. Any
        cleanup should add the field rather than just correct the status, or
        it will go stale again.

        WHAT SURVIVED VERIFICATION UNCHANGED, so the ruling still has a
        problem to solve: the measured contention (one moving surface, six
        identities, 11.9 commits/h in the live regime, median inter-push gap
        of the same order as this host's whole gate), the 0.40 merge ratio
        against 0.11 and three zeros, the 2.0 re-merges per landed cycle from
        the reflog, and the convexity of cost in gate length (T=3 -> 3.8 min to
        land; T=10.5 -> 21-29 min; T=30 -> 213 min).
