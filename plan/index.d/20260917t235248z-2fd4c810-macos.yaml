# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: log-query-language-has-zero-consumers
    field: status
    value: in_progress
    ts: "2026-09-17T23:52:48Z"
    host: macos

events:
  - packet_id: log-query-language-has-zero-consumers
    event:
      type: note
      ts: "2026-09-17T23:52:48Z"
      host: macos
      summary: |-
        claimed by macos-tlatoanis-macbook-air-claude-20260917t235248z: re-measure the orphan claim (a month old) and frame the wire-or-tombstone decision on current evidence
