# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-esmeralda-claude-fable-20260808t235000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# Cross-host coordination: Esmeralda (620-* low-end/login work) x the sibling
# windows host's 626-* first-launch cold-start findings. Operator-directed
# info sharing, 2026-08-09.
events:
  - packet_id: wsl-github-login-vault-misroute
    type: progress
    ts: "2026-08-09T06:55:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      Real-token login VERIFIED end-to-end on Esmeralda (authentication
      complete for 8007342; --list-cloud-projects returns the operator's real
      repositories; tray shows remote projects). Two hardening finds recorded
      in the deliverable: PowerShell-pipe UTF-8 BOM poisons --with-token
      stdin (43-byte token -> 401 Bad credentials; cmd.exe raw pipe + guest
      sed/tr scrub is the working runbook), and the git-identity preflight is
      a dead-end for operators (no git binary in the runtime distro;
      read_git_identity_defaults parses ~/.gitconfig directly, so writing the
      file by hand unblocks). CROSS-REF: the sibling 626 report's F2
      evidence (tray-menu click ran the injected wrapper; 0-byte
      github-login-last.log proves the silent preflight, not a wrapper
      failure) CLOSES this packet's tray-menu-wrapper exit criterion; the
      residual preflight-visibility work lives in
      first-run-long-work-is-silent-across-surfaces.
  - packet_id: first-run-long-work-is-silent-across-surfaces
    type: note
    ts: "2026-08-09T06:56:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      Input from Esmeralda (620-9xpg deliverable,
      plan/issues/wsl-github-login-vault-misroute-2026-08-09.md): when the
      silent-preflight surface is reworked, fold in the two --with-token
      stdin hardening asks recorded there — BOM/CR scrubbing in
      GH_LOGIN_STDIN_TOKEN_SCRIPT (PowerShell pipes BOM-prefix the token ->
      the same 401 a wrong token gives) and a non-interactive git-identity
      path (env/flags or tray identity propagation; the runtime distro has
      no git binary so the current "configure it" error is un-actionable).
      Both were field-hit during the successful Esmeralda login.
  - packet_id: tray-sign-in-state-unknown-until-observed
    type: note
    ts: "2026-08-09T06:57:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      Corroboration from Esmeralda: the same early-login-click pattern the
      626 report timelines (operator clicks GitHub Login within ~30-150s of
      subscription established) recurs across this host's 2026-08-08/09
      sessions in tray.log, including clicks during the not-yet-known window
      that led the operator to report the login as crashed. Strengthens the
      F1 case for the specified-but-lost Unknown variant; approval remains
      the blocking input per UX curation governance.
  - packet_id: low-end-no-local-inference-gate
    type: progress
    ts: "2026-08-09T06:58:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      First real-world lane-launch exercise imminent: operator is launching
      an opencode lane on Esmeralda with TILLANDSIAS_NO_LOCAL_INFERENCE=1
      set (user env, forwarded into the guest unit by injection). Expected:
      no tillandsias-inference container created and no ~2.1GB ollama
      self-install on this 16GB N100; verification via podman ps to follow
      as this packet's remaining lane-launch exit criterion.
