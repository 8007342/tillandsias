# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: linux-tray-shutdown-atomicbool-sleep-to-notify
    field: status
    value: ready
    ts: "2026-09-15T16:14:59Z"
    host: lenovinha

events:
  - packet_id: linux-tray-shutdown-atomicbool-sleep-to-notify
    event:
      type: note
      ts: "2026-09-15T16:14:59Z"
      host: lenovinha
      summary: |-
        released after an audit slice: criterion 2 presumes a Notify/watch fed by install_shutdown_signal_handlers, which returns only an Arc<AtomicBool> — the broadcaster must be built before the conversion, and site 1 is a std::thread with no runtime whose flag is written from a signal handler
