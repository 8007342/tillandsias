# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-pin-that-works-and-a-cadence-that-never-runs-it-let-a-release-blocking-red-land-green-and-sit-for-days-on-one-host
    event:
      type: progress
      ts: "2026-09-15T23:46:00Z"
      agent_id: linux-yoga-claude-20260915t233553z
      host: linux
      summary: |
        NARROWING THE UNDETERMINED CAUSE: one of the three candidates I named is ELIMINATED, by date rather than by argument.

        The historical local-ci defect — a red pre-build litmus printing ALL CHECKS PASSED because log_info did not increment CHECKS_FAILED — was fixed in 5e87bf8da, committed 2026-07-30T00:26:11-07:00, with the commit subject saying so plainly: a check that cannot fail is not a gate. The bump 4fc7be930 landed 2026-09-15T02:44:09-07:00. git merge-base --is-ancestor confirms the fix is an ancestor of the bump, so that defect was closed six weeks before, and it is NOT the explanation.

        I also rule out the podman arm: every branch of the pre-build litmus section, including podman-unavailable, calls log_fail_tracked today, so an absent runtime reds the gate rather than skipping quietly. I did not check whether that was equally true in September, and the next investigator should, because it is one git log -S away.

        THAT LEAVES THE GATE NOT HAVING RUN AT ALL as the leading candidate, and the specific mechanism to check first is gate-stamp adoption (1174-u5wp): a land that finds a valid full-scope stamp SKIPS the gate and goes straight to the push. This host did exactly that four times tonight, legitimately, and the stamp binds to a tree digest that deliberately EXCLUDES the plan fast lane. The question for this investigation is whether a stamp could have been valid for a tree that already carried the WIRE_VERSION bump — and if it could, the defect is not a tier gap or a cadence gap at all, but a stamp that outlived the tree it described.

        I am not chasing that tonight; it wants its own row and its own claimant, and it is a materially more serious question than the one this row was filed to answer. Naming it here so the next reader starts where I stopped rather than re-deriving the four measurements above.
