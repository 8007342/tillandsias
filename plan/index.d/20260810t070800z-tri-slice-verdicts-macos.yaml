# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# One guest session (post-cee23fb9 bring-up), three verdicts.

events:
  - packet_id: macos-v05-validation-bundle
    event:
      type: progress
      ts: "2026-08-10T07:08:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        M3 RESIDUAL CLOSED — ENVELOPE-BYTE-IDENTITY: PASS. With the
        635-kagg bring-up fix in, the maintenance forge container is
        created 8s after lane start; its Config.Env carries
        TILLANDSIAS_ACCEL_ENVELOPE byte-identical to the first line of
        in-guest `tillandsias-headless --capabilities`:
        accel_class=cpu-only accel_gpu=none accel_gpu_name=- accel_npu=none
        accel_npu_name=- accel_reason=- accel_cpu_cores=4 accel_ram_gb=4.
        The bundle is now M1/M2/M3/M4/M6 GREEN; the ONLY open item is
        M5's runtime half, operator-gated on a zero-repo GitHub account.

  - packet_id: macos-forge-config-trust-live-parity
    event:
      type: blocked
      ts: "2026-08-10T07:08:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      lease_id: macos-v05-drain-349-20260809t1628z
      summary: >
        Token probe EXECUTED against the live in-session enclave (the
        635-kagg fix unblocked it): GH-TOKEN:ABSENT — `vault kv get
        -field=token secret/github/token` (presence-only, name-only -e
        VAULT_TOKEN) finds nothing; the 2026-07-27 re-provision wiped the
        guest vault as suspected. Per the pre-committed fallback: BLOCKED
        ON OPERATOR — one interactive `tillandsias-tray --github-login`
        re-seeds the token, then the closing run (real mirror push + TLS
        parity sweep) can execute. Criteria 1+2 remain PASS from
        2026-07-16.

  - packet_id: macos-inference-tier-verification
    event:
      type: progress
      ts: "2026-08-10T07:08:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      lease_id: macos-v05-drain-401-20260809t1628z
      summary: >
        LOCAL INFERENCE IS ALIVE on this host for the first time: with
        the enclave up (egress via proxy), `podman start
        tillandsias-inference` retried the ollama self-install and it
        SUCCEEDED — /api/version answers {"version":"0.32.6"} in-guest.
        Combined with the earlier tier:cpu verdict, exit criterion 1 is
        fully evidenced and the substrate for the measurement half now
        exists. Remaining: pull the tiny model set (preload policy names
        qwen2.5:0.5b), run the order-393 ground-truth measurements on
        cpu-ollama, record the llama.cpp comparison decision (order 482
        engine slots), one Modelfile expert build.

  - packet_id: macos-forge-bash-lane-wedges-over-exec-wire
    event:
      type: progress
      ts: "2026-08-10T07:08:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Post-fix bring-up timing datum: maintenance container created 8s
        after lane start (was: never, pre-cee23fb9). Only the foreground
        attach output-silence remains on this packet; everything the
        attach blocks (M3, token probe, inference retry) is now reachable
        by catching the container mid-flight or using the enclave the
        bounded lane leaves behind.
