# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: accel-probe-blind-to-wsl2-paravirtualised-gpu
    field: next_action
    value: |-
      STATE 2026-09-02 (yolanda). REMAINING 2 IS DONE AND LANDED — the proof rungs are PRODUCED, not modelled. run_probe() now calls probe_container_render_nodes(), which execs one bounded read-only shell INSIDE tillandsias-inference (the images/inference/entrypoint.sh precedent, generalised), parses identity + the /dev/dri listing with one shared assemble_render_node(), upgrades Reachable from that listing, and asks ollama /api/ps for size_vram ONLY when something already reached Reachable. CapabilityDocument gained render_nodes and the envelope gained accel_proof=<enumerated|reachable|placed|->, both APPENDED LAST; legacy_tier is untouched in spelling, type, position and value vocabulary, per yoga three constraints. Five new tests, 46 accel_probe tests green, forced gate green.

      REMAINING 1 IS THE ONLY OPEN ITEM AND IT IS OPERATOR-GATED. Criterion 1 and the WSL2 half of criterion 3 need a Vulkan ICD installed on the operator machine: /dev/dxg is not a DRM device, creates no render node, so nothing on the WSL2 path enumerates and the software-rasterizer rejection has nothing to reject in. OWNER: operator. SMALLEST NEXT ACTION: a deliberate, recorded ICD install on yolanda, then add Vulkan enumeration and reject PHYSICAL_DEVICE_TYPE_CPU / DRIVER_ID_MESA_LLVMPIPE. Yolanda declined to install it and was RIGHT to — provisioning the operator machine to make a criterion pass is not evidence.

      VERIFICATION DEBT ON REMAINING 2, named rather than hidden: this host is Windows, the probe runs natively, and it can never execute the path it wrote. The logic is verified exhaustively; the PLACEMENT is not, here. yoga-silverblue holds the ask and will report accel_proof= and the render_nodes rows verbatim once the coordinator merges windows-next into linux-next. Per yoga measurement their gfx1152 sits at Reachable with size_vram genuinely 0 as a STEADY state — the iGPU is a loss below ~3b params — so a real placed row may first come from a different host, not from theirs.

      DO NOT MOVE legacy_tier WITHOUT TELLING YOGA. Still true, and now load-bearing in a second way: dev-inference-ensure.sh greps it with grep -m1 on the RAW --capabilities output, so any new legacy_tier-spelling key placed ahead of it silently downgrades that host to cpu with no device flags and no warning.

      FIELDS ARE STABLE for yoga hwfp-v2 schema bump: vendor_id: u16, device_id: u16, driver: String, node: String, plus vantage and proof. Use vendor_id AND device_id TOGETHER — vendor alone collides across every AMD part.
    ts: "2026-09-02T20:56:51Z"
    host: yolanda
