# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-23T22:35:05Z.
events:
  - packet_id: spec-expert-refusal-threshold-fleet-decision
    event:
      type: progress
      ts: "2026-08-23T22:35:05Z"
      agent_id: linux-macuahuitl-opus5-20260823T223400Z
      host: linux_mutable
      evidence_refs:
        - "scripts/refusal-calibration/results-relabelled-561-nomic-prefixed.tsv (index prefix=[search_document: ], queries prefixed search_query: )"
        - "stage-1 guard: build refused to be measured unless its log said reused=0 — it said reused=0 embed=21126 of 21126"
        - "band composition identical to every baseline: near=33 far=70 in=458"
        - "GPU: peak 74% util, peak non-ollama VRAM 386 MiB against an 800 MiB threshold, contaminated_samples=0"
      summary: >
        THE PREFIX CAVEAT IS CLOSED, AND EVERY PRIOR CONCLUSION SURVIVES IT.

        nomic-embed-text, the model whose documentation is most insistent that
        "search_document: " / "search_query: " are required, and the model that
        scored BEST in the size comparison — measured both ways on the same 561
        relabelled questions:

          nomic, no prefixes    band overlap 0.2058   margin 0.1104
          nomic, WITH prefixes  band overlap 0.2001   margin 0.0998

        Using the model correctly improves band overlap by 0.0057 — under 3%
        relative — and the bands remain massively inseparable. Both ends moved
        together rather than apart: in-floor 0.5620 -> 0.5950, near-ceiling
        0.7677 -> 0.7951. The prefix shifts the whole distribution up and barely
        narrows the gap, which is what a convention that helps the model
        interpret its INPUT would be expected to do; it does not change what the
        score is measuring.

        WHY THIS SETTLES IT FOR THE OTHER TWO MODELS WITHOUT 40 MORE MINUTES OF
        GPU, stated as an argument so it can be disputed rather than assumed:
        the effect of correct prefixing on nomic is 0.0057. The gap the caveat
        would have to explain away is the monotone size trend, nomic 0.2058 ->
        qwen3 0.2744, which is 0.0686 — TWELVE TIMES larger. A correction an
        order of magnitude smaller than the effect cannot reverse its sign.
        Anyone who disagrees can run it: the harness now supports prefixes on
        both halves and the cache key finally respects them (864-p2rk).

        SO THE THRESHOLD HYPOTHESIS IS DEAD AT EVERY GRANULARITY THIS CAMPAIGN
        CAN TEST:
          - one global cutoff                    NO (overlap 0.20-0.27)
          - per-corpus cutoffs                   NO (every corpus overlaps)
          - top1-minus-top2 margin               NO (0.10-0.12 everywhere)
          - across model scale, 137M to 4B       NO, and monotonically worse
          - with documented prefixes applied     NO, and it barely moves

        824-6qxh's decision — use the layer, not a threshold — is now backed by
        measurement across five independent axes rather than by argument. The
        layer's own numbers, for contrast: 0/33 false positives on unanswerable
        questions, and recall@5 double recall@1 with the near band still at 0.0%.
        The refusal question was never a geometry problem.

  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    event:
      type: progress
      ts: "2026-08-23T22:35:05Z"
      agent_id: linux-macuahuitl-opus5-20260823T223400Z
      host: linux_mutable
      next_action: >
        CAMPAIGN STATE 2026-08-23T22:35Z. FLEET STOOD DOWN: macuahuitl hourly
        and lenovinha (~2h) are the only loops. esmeraldinha 10h, yoga 10h,
        yolanda 7h, macbook 6h, forge 99h read SILENT and are NOT stalls. pirria
        never attested; operator is arming it to 4h. yoga rejoins tonight.

        THE REFUSAL-THRESHOLD CAMPAIGN IS FINISHED. Do not open it again without
        a new axis. Thresholds are refuted on five: one global cutoff,
        per-corpus cutoffs, top1-top2 margin, model scale 137M->4B (monotonically
        worse), and correct query/passage prefixing (moves the number by 0.0057,
        twelve times smaller than the size effect it would have to reverse).
        824-6qxh's answer is "use the layer, not a threshold", now measured
        rather than argued.

        NEXT, in order — this queue is now OFF the refusal campaign:
        (1) 846-idhn capabilities channel base representation. Seven hosts
            publish rows; this is the last piece of the routing story and it is
            plain work, not research.
        (2) 864-q7dm — background jobs writing inside the checkout block the
            host from pushing anything at all. Filed with a proven workaround
            (move the file mid-run; a redirect holds the inode). The packet is
            to make the refusal NAME the changed paths, because "re-run it" is
            actively wrong advice in this case.
        (3) 853-6gz3 Tier B remains the live expert-layer thread, and its open
            operator decision is fold-on-disagreement (conservative / majority /
            divergence-surfacing; macuahuitl recommends divergence-surfacing).

        TOOLING BUILT TODAY, use it rather than rebuilding it:
        - rebuild-index-with-embedder.sh — serialises loads, REFUSES on a dirty
          card, samples GPU throughout, writes .state. qwen3-embedding:4b needs
          batch 32; at 64 its runner dies mid-tokenise.
        - apply-relabelled-561.sh — projects a wide-579 run onto the corrected
          ground truth. MANDATORY before quoting any comparison: the only
          question FILE carries the discredited labels (near=243, 86% wrong).
        - TILLANDSIAS_EMBED_DOC_PREFIX / _QUERY_PREFIX now exist and the cache
          key respects them. Before 864-p2rk a "prefixed" build silently reused
          unprefixed vectors and its identity assertion agreed.

        STANDING COORDINATOR DUTY EVERY CYCLE: merge platform branches into
        linux-next; nobody else has it. Both ahead=0 at 22:34Z.

        NOT MINE: 856-s56y (yoga), 857-4kqv (yoga), 860-g798 (yolanda), low-end
        tier rows on pirria/esmeraldinha.
      summary: >
        Cycle 2026-08-23T22:34Z. Heartbeat clean. Closed the prefix caveat and
        with it the whole refusal-threshold campaign; queue moves to 846-idhn.
