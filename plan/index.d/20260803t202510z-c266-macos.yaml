# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260803
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: plan-append-event-blind-to-fragment-packets
    order: 600-c266
    status: ready
    desired_release: v0.5
    role: tooling
    pickup_role: any
    priority: p1
    tags: [crdt, plan-cli, fragments, cross-platform]
    title: >
      tillandsias-plan append-event refuses packets that exist only in
      plan/index.d/ fragments — the write path forgot the overlay the read
      path mandates.
    problem: >
      OBSERVED 2026-08-03 on macOS at 9e291df3: `tillandsias-plan status
      598-kibt` and `status macos-v05-validation-bundle` both resolve (the
      read fold sees fragments), but `append-event macos-v05-validation-bundle
      claim ...` fails with "packet_id 'macos-v05-validation-bundle' not
      found". Same for 599-4wzr. Every packet filed via a fragment is
      therefore un-claimable and un-annotatable through the CLI until
      compaction folds it into the base — exactly the window in which sibling
      hosts are told to claim it. 582-26mm's rule ("a reader that forgets
      fragments reports a stale ledger with total confidence") applies to the
      CLI's own validation path. Workaround used this cycle: hand-written
      events fragment per plan/index.d/README.md.
    exit_criteria:
      - >
        append-event resolves <id|order> against the FOLDED ledger (base +
        fragments) and appends its event as a new fragment.
      - >
        A regression test: file a packet via fragment, append-event to it,
        both visible in `status` without compaction.
events:
  - packet_id: macos-v05-validation-bundle
    event:
      type: claim
      ts: "2026-08-03T20:25:10Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260803
      host: macos
      lease_id: macos-v05-bundle-20260803T2025Z
      summary: >
        Claimed by the macOS orchestration cycle (operator-directed session).
        Sibling heads at claim: main=1526fb29,
        linux-next=windows-next=osx-next=9e291df3 (1:1 parity). Working M1-M6
        in order. OpenCode e2e smoke EXCLUDED this cycle per operator
        directive (OpenCode token budget low for ~24h; operator will smoke
        manually with other harnesses).
  - packet_id: macos-v05-validation-bundle
    event:
      type: progress
      ts: "2026-08-03T20:25:10Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260803
      host: macos
      summary: >
        M1 COMPLETE: RED reproduced exactly as filed — the 7 predicted
        accel_probe.rs lints (163 unused_mut, 164/165 unused_assignments, 242
        unused_variables, 408 unused_mut, 453/587 dead_code) plus the
        previously unenumerable remainder, which is exactly ONE more:
        main.rs:2967 needless_return (macOS-only detect_inference_tier arm).
        ALSO RED, same class: tillandsias-macos-tray under -D warnings — 11
        errors invisible to Linux (cfg-gated crate): 3x collapsible_if
        (action_host.rs:1923, action_host.rs:2492, pty_vsock_bridge.rs:191)
        + 8x doc_lazy_continuation (diagnose.rs:389-396). All fixed:
        cfg_attr allows per the vault_bootstrap.rs:275 idiom for the
        Linux-only mutation sites, let-chain collapses, doc list terminated
        with a blank /// line. ./build.sh --check now GREEN end to end on
        macOS (fmt + workspace check + strict clippy both variants + ledger
        checks). M5 CODE-COMPLETE: cloud_projects_loaded now set on any
        confirmed reply and reset on fresh logout in macos action_host.rs,
        mirroring Windows; pinned by new unit test
        apply_cloud_projects_empty_reply_marks_loaded_and_logout_resets.
        Empty-repo RUNTIME verification still requires a zero-repo GitHub
        account (operator/manual). M4 prereqs: GNU timeout 9.11 present,
        md5sum present (Darwin), jq/ruby present, yq ABSENT — the two
        md5sum-vacuous litmus tests are NOT vacuous on this host. Bonus find:
        gate-stamp.sh compute() could never record a stamp on macOS
        (symlink-to-directory skill links) — fixed, see
        plan/issues/gate-stamp-symlink-macos-2026-08-03.md.
  - packet_id: hooks-shipped-but-never-installed-audit
    event:
      type: progress
      ts: "2026-08-03T20:25:10Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260803
      host: macos
      summary: >
        macOS checkout report (exit criterion 3): .git/hooks/ was EMPTY on
        this host until this cycle — every shipped hook inert, matching the
        linux-mutable finding. ./build.sh --check (599-w5jd mechanism)
        installed pre-commit/pre-push/post-commit. ADDITIONALLY the installed
        gate could still never pass here: gate-stamp.sh compute() hashed
        THROUGH the committed symlink-to-directory skill links, sha256sum
        exited "Is a directory", pipefail ate the write — every push would
        have been refused stale:never-run despite a green gate. Fixed this
        cycle; evidence and Linux-side residuals in
        plan/issues/gate-stamp-symlink-macos-2026-08-03.md.
  - packet_id: release-gates-moved-to-local-hardware
    event:
      type: note
      ts: "2026-08-03T20:25:10Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260803
      host: macos
      summary: >
        Cross-platform follow-up: the stamp mechanism shipped here was
        unrecordable on macOS (symlink-to-directory hashing defect,
        plan/issues/gate-stamp-symlink-macos-2026-08-03.md). The 11-step
        litmus fixture repo contains no symlink-to-directory entries so it
        could not see the class; consider adding that negative control.
