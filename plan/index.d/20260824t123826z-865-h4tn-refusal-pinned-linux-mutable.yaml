# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-24T12:38:26Z.
status:
  - packet_id: the-groundtruth-harness-cannot-express-a-refusal-case
    field: status
    value: completed
    ts: "2026-08-24T12:38:26Z"
    host: linux_mutable

events:
  - packet_id: the-groundtruth-harness-cannot-express-a-refusal-case
    event:
      type: completed
      ts: "2026-08-24T12:38:26Z"
      agent_id: linux-macuahuitl-opus5-20260824T123400Z
      host: linux_mutable
      evidence_refs:
        - "crates/tillandsias-plan/src/groundtruth.rs — optional per-case `answer`, defaulting to the synthesised retrieval-only prose"
        - "openspec/litmus-tests/groundtruth/spec-rung1.yaml — spec-refuses-when-the-answer-uses-no-retrieved-chunk + its negative control"
        - "openspec/litmus-tests/groundtruth/query-vectors/out-of-corpus-refusal.json — 768-dim nomic, GPU 1% / 664 MiB at capture"
        - "grade: 5/5 pass (3 pre-existing unchanged)"
        - "MUTATION 1 (ignore case.answer) -> refusal case FAILS, 4/5"
        - "MUTATION 2 (envelope cites whatever was retrieved) -> refusal case FAILS, 4/5"
        - "restored -> 5/5"
      merge_affordance: merge-now
      merge_reason: >
        The refusal had no regression test on any host and the suite could not
        host one. Any host weakening the envelope's citation filter would have
        gone green. Test-and-harness only; no product behaviour changes.
      summary: >
        THE SUITE CAN NOW EXPRESS A REFUSAL, AND ONE IS PINNED.

        A case may supply its own `answer`. Omitted, the grader synthesises the
        retrieval-only prose exactly as before — which is what kept every
        existing case green and unchanged. Supplied, the case controls the one
        input the envelope's citation filter reads.

        That filter is the entire refusal: a chunk is kept only if the answer
        contains its key (spec.rs:1079), and an empty citation set becomes
        `unsupported` (spec.rs:1095). The synthesised prose lists every key by
        construction, so before this the check could never fail and no case
        could ever grade `unsupported` — whatever it asked.

        TWO CASES, SHARING A QUESTION AND A VECTOR, DIFFERING ONLY IN PROSE.
        The refusal case supplies an answer citing nothing and expects
        `unsupported` with zero citations. The negative control supplies no
        answer, so the grader's key-listing prose must still produce citations —
        without it, a harness that refused EVERYTHING would score green on the
        refusal case and look correct.

        MUTATION-CONTROLLED IN BOTH DIRECTIONS, and the second is the one the
        criterion actually names:
          ignore case.answer                     -> refusal case FAILS (4/5)
          envelope cites whatever was retrieved  -> refusal case FAILS (4/5)
          restored                               -> 5/5
        The second mutation is the literal regression 824-6qxh criterion 3 was
        written against: "if that check is ever weakened to cite whatever was
        retrieved". Nothing else in the suite notices it.

        WHAT THE GREEN ROW DOES NOT MEAN, stated in the file itself so it cannot
        be misread later: it pins KEY CONTAINMENT, not semantic support. The
        same question still retrieves three chunks at ~0.59, and before this
        landed the grader cited `fn basic_recipe()` in the VM layer as evidence
        about baking sourdough and called it a pass. Semantic refusal is
        853-6gz3's Tier B and remains untested.

  - packet_id: spec-expert-refusal-threshold-fleet-decision
    event:
      type: progress
      ts: "2026-08-24T12:38:26Z"
      agent_id: linux-macuahuitl-opus5-20260824T123400Z
      host: linux_mutable
      summary: >
        CRITERION 3 IS NOW MET, after four cycles and three wrong answers about
        it. A refusal case lands in spec-rung1.yaml expecting `unsupported`, it
        passes, and it goes red when the envelope's citation filter is weakened.

        Remaining for this row: criterion 1, which asks EACH HOST KIND for its
        measured bands. Only gpu-cuda has reported; the low-end tier has not,
        and that is 864-dvhk — not mine. Criterion 2 has been met since the
        five-axis refutation.

        So 824-6qxh is now blocked solely on a low-end-tier measurement, and no
        longer on anything this host can do.
