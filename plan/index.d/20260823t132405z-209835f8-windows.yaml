# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: build-bumps-version-then-the-hook-forbids-pushing-it
    field: next_action
    value: 'Wait for the 862-zhr2 ruling (A: untracked local counter — recommended; B: guard learns the mechanical-bump shape + LUB merges). Then implement the chosen resolution: build.sh + guard + litmus:versioning-shape re-pin + methodology/versioning.yaml reconciliation.'
    ts: "2026-08-23T13:24:05Z"
    host: windows
