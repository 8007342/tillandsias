# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by: windows operator-attended session (The Tlatoāni, windows/Yolanda),
# 2026-08-16. Records the SignPath Foundation signing decision on 722-w7a2
# and files its prerequisite chain.
packets:
  - packet_id: repo-osi-license-decision
    release_target: windows-authenticode-signing
    order: 776-it8u
    status: ready
    desired_release: v0.5
    kind: decision
    pickup_role: operator
    priority: p1
    capability_tags:
      - licensing
      - signing
      - governance
    title: |
      Choose and commit an OSI-approved LICENSE for the repo — the SignPath Foundation prerequisite.
    context: |
      The repo has NO LICENSE file. SignPath Foundation — the decided signing
      path for the GitHub-release channel (operator decision 2026-08-16,
      recorded on 722-w7a2) — requires an OSI-approved license, no proprietary
      components, and no commercial dual-licensing (signpath.org/terms). The
      license choice binds the legal terms of the whole project, so it is the
      operator's: pickup_role operator. Research:
      plan/issues/windows-signing-research-2026-08-16.md.
    exit_criteria:
      - |
          An OSI-approved LICENSE file is committed at the repo root, and the choice (who/when/which license) is recorded as a decision event on this packet.
      - |
          The SignPath constraints are confirmed against the chosen license and the tree: OSI-approved, no proprietary components, no commercial dual-licensing.
    events:
      - type: filed
        ts: "2026-08-16T21:05:00Z"
        agent_id: windows-fable5-attended-20260816
        host: windows
        summary: |
          Filed as the first rung of the SignPath prerequisite chain decided
          by the operator 2026-08-16 (decision event on 722-w7a2).

  - packet_id: signpath-foundation-application
    release_target: windows-authenticode-signing
    order: 776-t8hs
    status: ready
    desired_release: v0.5
    kind: implementation
    pickup_role: operator
    priority: p1
    depends_on:
      - repo-osi-license-decision
    capability_tags:
      - windows
      - signing
      - ci
    title: |
      Publish a code-signing policy page + attribution and apply to SignPath Foundation for the GitHub-release channel.
    context: |
      The decided path per the 2026-08-16 operator decision on 722-w7a2
      (SignPath Foundation over Azure Artifact Signing, which stays the
      recorded fallback). Expect a days-to-weeks lead time and PER-RELEASE
      manual approval thereafter; the publisher name on signed binaries will
      read "SignPath Foundation". Requires the OSI license decision first
      (depends_on repo-osi-license-decision, 776-it8u). Research and
      constraints: plan/issues/windows-signing-research-2026-08-16.md.
    exit_criteria:
      - |
          A code-signing policy page and the SignPath attribution are published where SignPath requires them.
      - |
          The application is submitted; its status (approved/pending/rejected, with dates) is recorded as events on this packet.
      - |
          On approval, wiring the release workflow to SignPath (including the per-release manual-approval step) is scoped as its own follow-up packet, not smuggled into this one.
    events:
      - type: filed
        ts: "2026-08-16T21:05:00Z"
        agent_id: windows-fable5-attended-20260816
        host: windows
        summary: |
          Filed as the second rung of the SignPath prerequisite chain decided
          by the operator 2026-08-16 (decision event on 722-w7a2).

  - packet_id: store-msix-free-signing-research
    release_target: windows-authenticode-signing
    order: 776-g6r3
    status: ready
    desired_release: v0.5
    kind: research+enhancement
    pickup_role: windows
    priority: p2
    capability_tags:
      - windows
      - signing
      - packaging
      - ci
    title: |
      Research/prototype the Microsoft Store MSIX channel — Store re-signing costs $0 and clears SmartScreen/SAC.
    context: |
      RESEARCH-RECOMMENDED, NOT YET OPERATOR-APPROVED — recorded explicitly
      per the 2026-08-16 session. Microsoft re-signs MSIX packages submitted
      through the Store ($0; no SmartScreen/SAC warnings; both Partner Center
      registration fees waived as of 2025-09-10 and 2026-05-07), and the
      operator already holds a Store listing. Scope: MSIX-package the tray
      (runFullTrust + startup task), drive the Store submission API from CI.
      Complementary to — not a replacement for — the SignPath GitHub-release
      channel. Research: plan/issues/windows-signing-research-2026-08-16.md.
    exit_criteria:
      - |
          A working MSIX packaging of the Windows tray (runFullTrust + a startup task) is demonstrated, or the concrete blocker is named.
      - |
          The Store submission API path from CI is mapped end to end (auth, artifact upload, submission, publish), with costs and lead times recorded.
      - |
          An operator decision event on this packet records whether the Store channel is adopted alongside the GitHub-release channel — until then this stays research, not direction.
    events:
      - type: filed
        ts: "2026-08-16T21:05:00Z"
        agent_id: windows-fable5-attended-20260816
        host: windows
        summary: |
          Filed from the 2026-08-16 signing research; flagged explicitly as
          recommended by research and NOT yet operator-approved.

events:
  - packet_id: windows-signing-identity-provisioned
    event:
      type: decision
      ts: "2026-08-16T21:05:00Z"
      agent_id: windows-fable5-attended-20260816
      host: windows
      summary: |
        OPERATOR DECISION (The Tlatoāni, 2026-08-16, attended session on
        windows/Yolanda): SignPath Foundation is the signing path for the
        GitHub-release channel. This packet is RESHAPED, not closed: the
        deliverable changes from "an Azure Trusted Signing account" to the
        SignPath Foundation chain (code-signing policy page + attribution,
        application with days-to-weeks lead, per-release manual approval;
        publisher name will read "SignPath Foundation"), with Azure Artifact
        Signing (Trusted Signing renamed, GA Jan 2026; Basic $9.99/mo; paid
        subscription required; individual validation US/CA only) as the
        recorded fallback. Prerequisite chain filed this session:
        repo-osi-license-decision (776-it8u, the repo has NO LICENSE file and
        SignPath requires an OSI-approved license) -> signpath-foundation-
        application (776-t8hs); plus store-msix-free-signing-research
        (776-g6r3, research-recommended, NOT yet operator-approved). Full
        research with provenance:
        plan/issues/windows-signing-research-2026-08-16.md.
