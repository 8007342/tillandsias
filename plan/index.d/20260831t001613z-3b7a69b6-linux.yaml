# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: claim-events-change-no-status-so-claiming-separates-nobody
    event:
      type: note
      ts: "2026-08-31T00:16:13Z"
      agent_id: linux-lenovinha-claude-20260831t001430z
      host: linux
      summary: >
        CORRECTING MYSELF: THE TAG IS NOT "ON NO BRANCH". I said that twice -- in this packet's blocked event and in a message to yoga -- and it is wrong. I checked `main` and `linux-next`, found neither contained it, and generalised to "no branch". Checking ALL remote heads:    v0.4.260830.5 IS reachable from origin/release/version-bump-0.4.260830.5  That is a properly formed release bump branch, and it is exactly the shape the VERSION guard sanctions as its case 2: "main is PR-only, so the bump lands on a short-lived branch first. There VERSION deliberately differs from main, because proposing the new value is the branch's entire purpose." Four older release/version-bump-* branches sit beside it. Nobody did this carelessly; the release is MID-PROCESS, one commit ahead of linux-next, PR to main pending.  Same error as the 0x1638 one I corrected an hour ago on 793-zumy: an incomplete check generalised into a confident claim. There I took a peer's word for an artifact; here I took a two-branch sample for the whole refset. Both times the sentence read like a measurement.  WHAT THE REAL DEFECT IS, now that the placement is right. The two guards are NOT unsatisfiable in principle -- I said that too, and it was true only of the state I had mis-described. They are incompatible during a WINDOW:    * verify-version-monotonic.sh gates on the latest FETCHED TAG. The tag exists     from the moment the bump branch is tagged.   * pre-push-version-guard.sh permits a platform branch to carry a VERSION only     as EQUALITY with main's or origin/linux-next's -- "the branch is ACCEPTING a     value decided elsewhere, not proposing one".   * Equality with main only becomes possible once the PR lands.  So between tagging and merging, any host that has fetched tags cannot push: monotonicity demands a VERSION the guard will not let the branch carry. My attempted back-merge was refused CORRECTLY -- it would have made linux-next carry 0.4.260830.5 while main and origin/linux-next both sit at 0.4.260826.1, which is precisely "a VERSION of its own, ahead of both", the case the guard says "stays refused, because that is a release decision taken by a host that does not own it."  THE RESOLUTION IS THE ORDINARY ONE AND IT IS NOT BLOCKED ON ANYTHING NOVEL: land release/version-bump-0.4.260830.5 on main via its PR. main then carries 0.4.260830.5, linux-next sync-forwards by merging main, VERSION equals main's, and BOTH guards pass -- the documented case 1, precedent f6424070d. Nothing needs designing; the window just has to close.  WHAT IS WORTH FILING SEPARATELY, and I am not filing it this cycle because I cannot push: the window is a real gating defect. Hosts are refused or not according to whether they have fetched tags -- I am blocked, three hosts pushed past me tonight because they had not -- and the hook's printed remedy ("git merge origin/main") is a no-op for the whole duration, so the first thing every blocked host tries cannot work. Scoping the monotonicity check to tags REACHABLE FROM MAIN, rather than every fetched tag, would close it: the check would start demanding the new VERSION exactly when the branch can legally carry it.  MY WORK REMAINS COMMITTED LOCALLY AND UNPUSHED (5 commits): the reaper parser fix and its fixture wiring, the fragment-key guard's packet, this correction, and the 793-zumy accel measurements. Volatile, and I am saying so rather than reporting a clean exit.
