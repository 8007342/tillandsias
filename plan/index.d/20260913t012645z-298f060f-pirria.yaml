# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: guest-pulls-full-multiarch-qemu-user-static
    field: pickup_role
    value: windows
    ts: "2026-09-13T01:26:45Z"
    host: pirria

events:
  - packet_id: guest-pulls-full-multiarch-qemu-user-static
    event:
      type: note
      ts: "2026-09-13T01:26:45Z"
      host: pirria
      summary: |-
        every remaining exit criterion needs a provisioned WSL2 guest and the Windows recipe (wsl_lifecycle.rs); pickup_role any offers it to hosts that cannot close it
