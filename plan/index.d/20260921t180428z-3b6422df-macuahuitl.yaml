# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-four-agent-tool-alias-trees-each-carry-their-own-copy-of-eleven-openspec-skills-in-three-different-generated-versions-and-gemini-has-none-of-them
    event:
      type: note
      ts: "2026-09-21T18:04:28Z"
      agent_id: linux-macuahuitl-fable51-20260921t180428z
      host: macuahuitl
      summary: |
        COORDINATOR NOTE (2026-09-21, relayed from yolanda-windows before anyone claims this row): two facts to carry. (1) A fourth generator version on a fifth locus: a forge container provisioned inside WSL2 on yolanda's box regenerated the 22 .claude/commands/opsx and .claude/skills/openspec-* files with openspec 1.13.1 at provision time (mtimes 17:22:55Z, before the session started), the bytes are durable at refs/heads/salvage/unknown/20260921-opsx-1.13.1-regen (2e392947c, 22 files, 566/163), and scripts/check-opsx-generated-dirt.sh answers ok:opsx-only on them — the "third harness locus repeats this" that 964-fwvh's Residual section predicted; the forge's own event carries the detail. (2) THE NPM PINS WERE REMOVED DELIBERATELY: plan/archive/packets-2026-07.yaml line 1733 records "Removed npm pins (opencode-ai@1.16.2, openspec@1.4.1, ...)". "Pin the CLI version" is therefore not a fix this row can take without first answering why the pins were removed; treating the floating version as an oversight re-opens a settled decision. The closure as written (one generator version across the trees, recorded where a gate can read it, a check that refuses on disagreement) is compatible with either answer and stands. Also observed on the way: the salvage ref came out under salvage/UNKNOWN/ rather than a host slug although agent-identity.sh answers in that regime — a salvage ref exists to be found and attributed by someone else, so that is a separate small row for whoever hits it next, not this one.
