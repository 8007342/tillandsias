# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-skew-line-cannot-see-a-behaviour-change-inside-an-existing-subcommand
    event:
      type: note
      ts: "2026-09-03T16:45:42Z"
      agent_id: linux-macuahuitl-fable5-20260903t164542z
      host: macuahuitl
      summary: |-
        THE DETECTOR AVOIDS THIS ORDER'S OWN DEFECT ARRIVING THROUGH ITS FIX, which is the design decision worth keeping. build.rs bakes the revision of the sources it compiles as build-id; source-revision computes what a rebuild from a checkout would produce. ONE hash function, include!d by build.rs and used by the crate, deliberately not two - because two implementations could drift, and a drifted comparison reports skew forever or never, which is this packet's defect one layer over. A binary that cannot report its identity reads unknown-currency, NEVER none: pre-fix sources hash 25380f5e846ea354 against HEAD's 61ec32cc16e974ca, and a pre-fix binary has no build-id at all, so it lands in the LOUD state rather than a green one. BOTH NEW VERDICTS GOT ADVICE ARMS because the existing fallback prints No capability skew - without them a non-green machine field would sit beside reassuring prose, which is the same defect in the text rather than the code. TWO BUGS OF THEIRS, NEITHER FOUND BY READING, and the second is the one for the record. source-revision read args.get(2) against already-shifted args, so it hashed the DEFAULT directory for every input - returning a valid, matching, plausible answer for a nonexistent path with rc=0. Caught only because pre-fix and HEAD returned the SAME hash, which cannot be true of trees differing by four files. AND THE LITMUS CAUGHT THE OTHER ONE, NOT THEM: their first version compared revisions whenever a crates/tillandsias-plan directory existed, which litmus:expert-capability-skew-honesty's MANIFEST-ONLY fixture satisfies while containing no sources, so it reported rebuild-required for a synthetic in-sync scenario. FIRST TIME THIS SESSION AN EXISTING TEST FOUND A FALSE POSITIVE IN SOMETHING JUST WRITTEN, rather than a test failing to find anything. Worth naming given how much of today was checks that could not fail.
