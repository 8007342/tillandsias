# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-linux-host-with-only-zram-swap-is-offered-a-silverblue-safe-disk-swapfile-behind-zram-sized-to-its-tier-and-diagnose-reports-the-swap-shape
    event:
      type: amendment
      ts: "2026-09-26T01:23:29Z"
      agent_id: linux-macuahuitl-claude-20260926t012328z
      host: macuahuitl
      summary: |
        CLOSURE AMENDED TO THE PER-LAUNCH MODEL (macuahuitl, 2026-09-26; the filed closure named a persistent /var/swap file and is superseded). Deliverable: a one-time root install (printed by the installer as the exact sudo command, e.g. sudo tillandsias-install-swap-service or sudo bash scripts/install-forge-swap-service.sh) of: /etc/systemd/system/tillandsias-swap@.service (Type=oneshot, RemainAfterExit=yes, EnvironmentFile=/etc/tillandsias/swap.conf, ExecStartPre=<helper> reap, ExecStart=<helper> start %i, ExecStop=<helper> stop %i, TimeoutStopSec=5min); a root-owned helper under /usr/local/libexec that accepts ONLY an instance matching [A-Za-z0-9-]{1,64}, takes size/dir/priority from the root-owned config (never from the instance string), creates /var/swap/tillandsias-<id> with btrfs filesystem mkswapfile on btrfs or fallocate+chmod 600+mkswap elsewhere, swapon -p 10 (below zram's 100), and on stop swapoff + rm; a polkit rule /etc/polkit-1/rules.d/50-tillandsias-swap.rules granting org.freedesktop.systemd1.manage-units to the installing user only when action.lookup("unit") matches ^tillandsias-swap@[A-Za-z0-9-]{1,64}\.service$ and action.lookup("verb") is start or stop; and tillandsias-swap-gc.timer (2 min) whose service stops every instance whose launch lease (/run/user/<uid>/tillandsias/swap-<id>.lease, held by flock from the tray's launch process) can be acquired, plus a reap of /var/swap/tillandsias-* files not present in /proc/swaps (crash or reboot leftovers). The tray runs systemctl start tillandsias-swap@<launch-id> before podman run and systemctl stop after podman rm.
        NEW CLOSURE: bash scripts/test-forge-swap-service.sh prints ok:forge-swap-service:<n> arms: systemd-analyze verify passes on the shipped units; the helper refuses instance strings "../x", "a b" and a 65-char id with refused:instance; the polkit rule evaluated by a JS interpreter over fixture (action, subject) pairs says YES for tillandsias-swap@abc-123.service start/stop and NO for restart, for tillandsias-swap@x.service by another user, and for any other unit; the gc logic stops an instance whose lease is not held. Live arm on the landing host after the sudo install: a forge launch makes swapon --show list /var/swap/tillandsias-<id> at pri 10; forge stop removes the swap and the file; kill -9 of the tray removes it within 3 minutes. Pre-fix result: FAILS: none of the units, the helper, the rule or the test exist, and swapon --show lists only zram0.
        VERIFIED HERE (macuahuitl, systemd 259, polkit 127): systemd-analyze verify accepts the template, gc service and timer as drafted (stand-in helper path); systemd passes "unit" and "verb" details for manage-units (src/core/dbus-util.c builds details[] = {"unit", id, "verb", verb}) and StartUnit/StopUnit use verbs "start"/"stop"; polkit 127 JS rules with action.lookup and subject.isInGroup are the shipped API (50-libvirt.rules uses them); btrfs filesystem mkswapfile works on a fresh file (unprivileged in $HOME; NODATACOW set); flock -n on the lease refuses while held and succeeds after the holder exits; swap usage returns to zero once the container's tmpfs and processes are gone (SwapFree back to its full value after the probe), so ExecStop's swapoff pages in nothing of ours. UNVERIFIED (needs root, measure on the landing host): swapoff wall time with another tenant's pages on the file; polkitd's actual evaluation of the rule (only the JS logic was exercised); ostree /usr/local -> /var/usrlocal for the helper path.
