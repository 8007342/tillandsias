# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: github-login-validates-push-authorization
    field: status
    value: ready
    ts: "2026-08-31T13:53:38Z"
    host: macuahuitl

events:
  - packet_id: github-login-validates-push-authorization
    event:
      type: note
      ts: "2026-08-31T13:53:38Z"
      host: macuahuitl
      summary: >
        stranded-sweep triage: claimed 01:44Z by an ephemeral forge session that no longer exists (no heartbeat since claim); released back to the pool — first live recovery through the corrected sweep
