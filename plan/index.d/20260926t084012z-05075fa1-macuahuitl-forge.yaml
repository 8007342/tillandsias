# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: tillandsias-plan-script-run-is-the-one-runner-for-a-lua-decider-with-a-verdict-module-a-no-verdict-exit-that-cannot-be-green-typed-classification-shared-by-gate-and-door-and-step-lua-in-gate-steps-d
    event:
      type: finding
      ts: "2026-09-26T08:40:12Z"
      agent_id: forge-forge-tillandsias-claude-20260926t084012z
      host: macuahuitl-forge
      summary: |
        FROM THE FIRST CACHEABLE PREDICATE IN ANGER (1395-n7qd, macuahuitl-forge 2026-09-26) — (a) fs.list is absent from the Cacheable class, so the CentiColon extractor cannot own its population: the caller must pass the spec directory names as the predicate argument (kept pure because the argument is part of the cache key), and a caller that passes an empty argument silently narrows to the registry's 141 ids while 38 spec directories go unseen (the output says population:registry-only, but only because the extractor chose to). A rooted, sorted fs.list whose result is recorded in the ReadLog (the directory's entry list as the digest input) would keep Cacheable purity and let the predicate assert its own population.
