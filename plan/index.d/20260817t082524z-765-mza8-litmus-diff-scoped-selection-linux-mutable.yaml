# Ledger fragment — append-only, IMMUTABLE once written.
# Hand-written in `set-field` shape: worktree isolation refuses the compound
# invocation the CLI needs, and no plan binary is built in this worktree.
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr).
status:
  - packet_id: litmus-diff-scoped-selection
    field: status
    value: completed
    ts: "2026-08-17T08:25:24Z"
    host: linux_mutable

events:
  - packet_id: litmus-diff-scoped-selection
    event:
      type: completed
      ts: "2026-08-17T08:25:24Z"
      agent_id: fork-765mza8-20260817T0620Z
      host: linux_mutable
      summary: >
        status 'completed' with evidence_refs df604eaa6 (fork worktree,
        branch linux-next). Adds an optional `inputs:` glob list to litmus YAML
        and a `--diff-scope <base>` rung to run-litmus-test.sh that skips a
        test when none of its declared inputs moved. POLARITY, stated because
        it inverts 634-39ik's: this selector REMOVES coverage, so every
        uncertainty runs the FULL suite loudly. Eight refusals, each fixture-
        proven: not a git repo, base ref does not resolve, unparseable diff, no
        changes at all, no full-run anchor recorded, anchor older than 24h, a
        non-numeric (corrupt) anchor, and a valueless `--diff-scope`. The
        change set is base..WORKTREE plus untracked files, NOT base..HEAD — a
        scoped run must not skip the test covering what you just typed; both
        halves are asserted. CRITERION-BY-CRITERION. (1) Unannotated tests
        always run — the empty annotation never intersects, asserted directly;
        the release lane and build.sh pass no `--diff-scope`, so they run FULL
        by construction. (2) Loud grammar, observed verbatim in a real run:
        per-test "Diff-scoped: declared inputs untouched since a23fc9c03490"
        and summary "Diff-scope: 1 diff-scoped skips against base
        a23fc9c03490"; NOTE the per-test reason is suppressed by --compact and
        visible only in normal mode. The zero-match refusal is intact —
        `no-such-spec-xyz` still exits 1 with "no litmus tests matched filter"
        (the 764-8m5j contract the parent reverted a change for breaking).
        (3) New guard scripts/check-litmus-dead-inputs.sh refuses a glob
        matching no tracked file, with a negative control proving it fails:
        neutered to a healthy tree it passes, given `crate/**` it emits
        violation:litmus-dead-inputs:1 and names the glob. Wired into
        local-ci.sh LITERALLY per 599-4wzr; audit now reads
        "active: check-litmus-dead-inputs.sh <- scripts/local-ci.sh".
        (4) A test file's own change forces it to run — verified end to end by
        touching litmus-forge-clone-reachability-probe-shape.yaml while its
        declared input stayed untouched; it ran. (5) 24h ratchet proven, plus
        the corrupt-anchor case. (7) e2e tests stay unannotated:
        cache-recovery-fresh-start runs the INSTALLED BINARY, so its input is
        the built product; freshness-inventory-shape is also deliberately
        unannotated because its denominator spans the whole tracked tree.
        Annotated 11 of 359 tests (51 globs) — the honest fraction. Guard runs
        in 0.9s. Accessor parity between the yq path and the awk fallback is
        asserted across all 359 files, because the fallback is what a host
        without yq actually executes and a divergence there is a silent
        coverage hole on that host. Fixture scripts/test-litmus-diff-scope.sh
        18/18. ok:bash-dialect-clean. ./build.sh --check rc=0 captured
        unmasked. spec-traceability suite 9/9 PASS.
  - packet_id: litmus-diff-scoped-selection
    event:
      type: note
      ts: "2026-08-17T08:25:24Z"
      agent_id: fork-765mza8-20260817T0620Z
      host: linux_mutable
      summary: >
        MEASURED (criterion 6), and the headline wall-clock OVERSTATES the
        win — recording the confound rather than the flattering number. Same
        lane (--size quick --phase pre-build) on this host: FULL 444s / 301
        executed; plan-only tree 211s / 292 executed (9 diff-scoped skips);
        cross-crate tree 261s / 295 executed (6 skips). But only 106.5s of the
        plan-only 233s delta is ATTRIBUTABLE to the skips, summing the nine
        tests' own durations in the baseline run; the cross-crate figure is
        71.1s of 183s. The remainder is cold-start and load variance on tests
        that ran in BOTH runs — litmus:terminal-status-vocabulary-shape went
        27.1s to 6.0s and litmus:plan-answer-envelope-citability 20.2s to 5.3s
        without being scoped out at all. So the defensible claim is ~106s off a
        plan-only quick lane and ~71s off a cross-crate one, not 233s.
        SELECTION IS EXACT: the scoped-out sets are precisely the annotated
        tests whose globs miss the diff, verified by differencing the executed-
        test lists (9 for plan-only, 6 for cross-crate, no others).
  - packet_id: litmus-diff-scoped-selection
    event:
      type: note
      ts: "2026-08-17T08:25:24Z"
      agent_id: fork-765mza8-20260817T0620Z
      host: linux_mutable
      summary: >
        THE COVERAGE COST, OBSERVED, NOT ARGUED. On the plan-only tree the
        scoped run reported FAIL 29 against the full run's 34. Two of that
        difference are real failures the scoping HID —
        litmus:cycle-batch-triage-shape and
        litmus:guest-container-metrics-wire-shape were red in the FULL run and
        were scoped out. That is the feature's cost made concrete on its first
        measured use, and it is exactly why the gate-stamp veto and the 24h
        ratchet are not optional trimmings. Two calibration points for whoever
        tunes this next. FIRST, my annotation ranking used the MAX duration
        across all telemetry history, which is wrong: litmus:mirror-sshd-
        posture-shape ranked second at 79.7s, but that is a cold run that
        rebuilt the git image — its steady-state cost is 1.9s, and it
        contributed almost nothing to the measured saving. A percentile, not a
        maximum, should drive the next annotation wave. SECOND, this tree's
        FAIL count moves by about +/-3 between identical runs independent of
        scoping (34 / 29 / 30 across three runs, only 2 and 1 of which are
        explained by skips), so any future before/after on THIS lane must
        difference the executed-test lists rather than trust the FAIL totals.
  - packet_id: litmus-diff-scoped-selection
    event:
      type: note
      ts: "2026-08-17T08:25:24Z"
      agent_id: fork-765mza8-20260817T0620Z
      host: linux_mutable
      summary: >
        AUDIT F5 CLOSED WITH A LIVE OBSERVATION, and the fixture caught a real
        defect in my own first cut. build.sh:_write_gate_stamp now refuses to
        write ANY stamp when a scoped run left its sentinel — refusing outright
        rather than downgrading to a scoped stamp, because that function cannot
        know WHICH classes the scoped litmus run covered and inventing a class
        list is how a stamp starts lying. Observed in a real gate,
        rc=0 and no stamp: "NOT writing a gate stamp: this run's litmus lane
        was diff-scoped (diff-scope base=a23fc9c0349... skips=6)". The next
        ./build.sh --check wrote "Gate stamp recorded" normally, so the veto is
        one-shot and does not wedge the gate. THE DEFECT: I first placed the
        check AFTER the ghost-trace ratchet and the gate-stamp.sh existence
        test. Both can return early, and either would have stranded the
        one-shot sentinel to veto every LATER, legitimately-full run — a
        permanent gate wedge. Moved to the first statement in the function.
        A related behaviour, correct and worth knowing: a gate that fails
        BEFORE _write_gate_stamp never consumes the sentinel, so it survives to
        the next run. Confirmed live when check-litmus-pin-claims refused this
        fork's own fixture (below) and the stamp writer was never reached.
  - packet_id: litmus-diff-scoped-selection
    event:
      type: note
      ts: "2026-08-17T08:25:24Z"
      agent_id: fork-765mza8-20260817T0620Z
      host: linux_mutable
      summary: >
        TRAP FOR THE NEXT AGENT WHO WRITES A LITMUS FIXTURE, hit and fixed
        here. check-litmus-pin-claims.sh (721-77yu) greps every .sh file for
        `litmus:<name>` and reads each hit as a CLAIM that such a test exists.
        The dead-glob negative control writes two throwaway litmus files into a
        temp repo; spelling their names literally made ./build.sh --check
        refuse the whole tree with violation:litmus-pin-unresolvable:2, for
        tests the fixture invents on purpose and deletes on exit. Fix: compose
        the namespace at runtime. This is the same class the parent hit hours
        earlier with a COMMENT naming a nonexistent litmus, so it is now twice
        in one night — a fixture that must name a non-existent test has no way
        to say so, and that may deserve an opt-out marker of its own.
        Unrelated: check-guard-asset-skew.sh is an ORPHAN on the activation
        audit (48 total, 47 active). It is not mine — ee069bc99, 2026-08-16
        23:27 — but it is a guard enforcing nothing and wants a packet.
