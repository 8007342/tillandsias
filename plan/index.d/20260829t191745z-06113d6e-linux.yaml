# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-test-and-runbook-tier-composes-with-shell-pipes-instead-of-steps
    field: next_action
    value: |-
      STATE 2026-08-29 (lenovinha). PORTABILITY DISCHARGED, MODEL CORRECTED, BATCH 1 LANDED. 411 SITES REMAIN AND THE CONVERSION IS NOW SAFE TO DO MECHANICALLY — WHICH IT WAS NOT THIS MORNING.

      READ THIS FIRST: the flag is part of the assertion. Use the variant matching the ORIGINAL grep flag, never mf_holds for everything:
          grep -q   -> mf_holds          (BRE)      321 sites
          grep -qE  -> mf_holds_ere      (ERE)       85
          grep -qF  -> mf_holds_literal  (fixed)     46
          grep -qx  -> mf_holds_line     (whole)     18
          grep -qv* -> mf_lacks*                      7
          grep -qi* -> no variant yet; ADD ONE rather than approximating (10 sites)
      Converting a `grep -q` through the ERE consumer changes what 74 of them mean and makes at least one ERROR. This is measured, not theoretical — see the 2026-08-29 implemented event.

      THE CONVERSION, worked example in litmus-ledger-fragment-convergence.yaml:
          BEFORE  command: "cargo test … 2>&1 | grep -q 'test result: ok' && echo 'ok: …'"
          AFTER   command: "mf_stage cargo 0 T -- cargo test … && mf_holds T 'test result: ok' && echo 'ok: …'"
      Drop the explicit `2>&1` — mf_run captures both streams by design.

      BATCHES, in the order I would take them:
        1. THE 115 SINGLE-PIPE SITES. Genuinely mechanical with the table above.
        2. THE ~300 MULTI-PIPE CHAINS. NOT mechanical: `a | b | grep -q c` needs the chain staged or its intermediate stages preserved. Read each.
        3. Add mf_holds_ignorecase before touching the 10 `-qi/-qiE` sites.

      THE DISCIPLINE, which is the same each time: convert a batch, re-run that spec, confirm VERDICTS UNCHANGED. A verdict that changes is a conversion bug, not a discovery — the model is designed to preserve outcomes, and its value is in what it makes possible LATER, not in what it turns red today.

      WHY IT IS WORTH DOING AT ALL, since the suite-wide measurement found zero masked failures today: batch 1's seven sites each ran `cargo test | grep -q 'test result: ok'` over output containing TWO `test result:` lines. One binary failing while another passes reports SUCCESS, and cargo's exit code is discarded. The masking is LATENT, not absent — it is waiting for the first failure, in the assertion that tells us whether our tests pass.

      STILL UNTOUCHED, from the packet's own inventory: 212 shell scripts with tee/head/tail pipelines, 57 SKILL.md runbook snippets, 39 PIPESTATUS sites, and `2>/dev/null` on probes. The 39 PIPESTATUS sites remain the highest value of those — each is a hand-rolled workaround for this defect and each is broken under zsh.
    ts: "2026-08-29T19:17:45Z"
    host: linux

events:
  - packet_id: the-test-and-runbook-tier-composes-with-shell-pipes-instead-of-steps
    event:
      type: note
      ts: "2026-08-29T19:17:45Z"
      host: linux
      summary: >
        model corrected for flag fidelity; batch 1 landed; 411 sites remain with a safe conversion table
