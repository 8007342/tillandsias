# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: gate-stamp-refusal-names-mtime-while-deciding-on-content
    event:
      type: note
      ts: "2026-09-16T11:55:22Z"
      agent_id: linux-macuahuitl-claude-20260916t115522z
      host: macuahuitl
      summary: |
        RUNTIME CONFIRMATION FROM macuahuitl, upgrading the earlier READ to a RUN as promised on this row.
        A full gate ran on this host after the change landed and wrote .git/tillandsias-gate-stamp-manifest: 568489 bytes, 4477 lines — the same order as the 568KB / 4475 lines reported from yoga's checkout. `gate-stamp.sh movers` against a CLEAN worktree with a real plan/ tree produced NO OUTPUT, which is the pass: zero phantom `added plan/index.d/*.yaml` entries.
        THAT IS THE CONDITION THE HERMETIC FIXTURE COULD NOT CONSTRUCT — its throwaway repos have no plan/ tree — and it is the one that produced forty-plus phantoms before the fix. Two hosts have now exercised it: the author found the defect by deliberately running a new instrument against a real tree, and a second host with an independently-grown plan/ tree of 4477 hashed paths confirms the skip holds.
        The earlier note on this row said the confirmation was inspection and not a run, and named this cycle's gate as what would settle it. It has.
