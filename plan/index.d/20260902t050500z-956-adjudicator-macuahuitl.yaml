# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: litmus-low-end-instrument
    event:
      type: progress
      ts: "2026-09-02T05:05:00Z"
      host: macuahuitl
      summary: >
        RUNG LANDED (not closure — the packet's verifiable_closure names four
        other rungs, all still open): the kill-time adjudicator now diffs the
        runner's OWN cgroup cpu.pressure `some total=` counter across the
        step's own window (captured at launch, read at kill) and says
        CONTENDED (stall >= LITMUS_STALL_CONTENDED_PCT, default 25% of wall
        time), NOT contended, or UNCLASSIFIED when no pressure file is
        exposed — with no fallback instrument, because none measures this
        step's contention. Retires the load1-vs-ncpus rule and its three
        defects (not namespaced: forge steps were judged by the host's
        runqueue; utilization is not starvation; the trailing 1-minute window
        measured the previous step). Verdict unchanged: a timed-out step still
        FAILS. evidence_refs: commits c6ec9a3cc (adjudicator + fixture + gate
        wiring) and dfa01f243 (own-cgroup path first, root fallback);
        scripts/test-litmus-kill-adjudicator.sh — three arms THROUGH the real
        runner in a temp root with counters injected via LITMUS_PSI_FILE,
        every arm also asserting the step reached TIMEOUT: "PASS: litmus
        kill-time adjudicator diffs cpu.pressure 4/4 arms (956-llei)" (6s);
        build.sh gate step "Checking the kill-time adjudicator diffs
        cpu.pressure (956-llei)" green in ./build.sh --check (105s);
        scripts/check-litmus-pin-claims.sh ok:84 (the probe's name is
        composed at runtime so a fixture-local probe is not read as a suite
        claim); instant pre-build sweep Status PASS on the edited runner.
        REGIME of the live instrument: Fedora 44 kernel 7.1.12, cgroup2,
        /proc/self/cgroup = a ptyxis scope; root `some total=` 7.7s vs own
        scope 2.2s (the root counts every unrelated process); a 1s two-core
        burst moved the own counter 228us on an idle 24-thread host. NOT yet
        observed on a real contended timeout nor inside a forge (cgroupns
        root `/` falls through to the same file by design) — the next
        saturated-host or in-forge timeout is the first field reading; the
        25% threshold is a first cut to be re-read against it. Still open on
        this packet: --diff-scope escalation executes phase:retired tests;
        the slowest-tests table reports kill times for timeouts (censored
        data); and the four verifiable_closure rungs.
