# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: windows-tray-argv-reparse-workarounds-deletable
    event:
      type: verified
      ts: "2026-09-03T21:23:41Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260903t212341z
      host: tlatoanis-macbook-air
      summary: |-
        ALSO ALREADY DONE, both halves, and verified from macOS rather than assumed. Found while closing its sibling 836-smm2; the two share the parent guest-exec-allowlist-forces-argv-flattening.

        The deliverable has two halves and there are no exit_criteria beyond it.

        HALF 1 — a grep for argv_survives_wt_reparse and wt_safe_title returns nothing. SATISFIED. Both functions were deleted in c3f6591a1. The only occurrences left in crates/ are three comments naming them while explaining their removal, which is exactly what scripts/check-exec-argv-vector-workarounds.sh is built to permit: it reports ok:exec-argv-workarounds-absent:2 checked, rc=0.

        HALF 2 — wsl_root_sh no longer re-parses a flattened string. SATISFIED, and this is the half I could check properly rather than infer. wsl_root_sh now invokes wsl with --exec, which bypasses the guest login shell and hands argv straight through, so there is no re-join to shred the payload. The function carries the measurement that forced it (2026-08-31, 795-zshi): the same SINGLE-LINE script through both forms against a live distro gave value=[] without --exec and value=[hello] with it, proving the older multi-line guard had encoded the symptom of the order-326 repro rather than its mechanism. The guard is kept as belt-and-braces.

        I VERIFIED HALF 2 BY COMPILATION, NOT ONLY BY READING. crates/tillandsias-vm-layer/src/wsl.rs is exported as  with no cfg gate, so this macOS host builds it — unlike notify_icon.rs in the sibling packet, whose cfg(windows) body I explicitly could NOT typecheck and said so.

        ON CLOSING A WINDOWS-ROLE PACKET FROM macOS. pickup_role is windows, and I am not one. I closed it because the deliverable is objective, both halves are verified against the tree with named commands, one of them compiles here, there are no exit_criteria requiring a Windows run, and it is neither a split parent nor depended on by anything. The alternative — leaving a demonstrably finished p2 in ready — costs the next host a discovery cycle, which is precisely what this packet nearly cost me: I opened its sibling as live work and only a grep before starting revealed otherwise.

        If a Windows host disagrees on the merits, reopen it; the evidence above is falsifiable in one command.
