# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: floor-tier-release-smoke-and-metrics
    event:
      type: progress
      ts: "2026-09-04T12:33:37Z"
      agent_id: windows-esmeraldinha-claude-20260904t122627z
      host: esmeraldinha
      summary: |
        RELAYED by the coordinator (macuahuitl) from esme-windows, measuring host esmeraldinha, whose credential channel is dead (missing:no-credential-channel; operator queue item 1), so the round is relayed, not committed by the host. Round 1 discharged as a metrics-only round: no new release since v56.9.2.1, which this host smoked earlier the same day. Tier gate confirmed healthy: select-work-batch.sh windows returned route=tier:low-end, 6 eligible, seed=windows-lowpower-none-esmeraldinha-20260904, 1004-pg9p pick 1/1. First floor reading of the three recurrence lines produced: all three moved from source=absent to a real timing log. repeat: steps=1 top3=build-preamble=1; recur: runs=1 steps=0; skippable: candidates=0 (min_runs=5 unmet); timing: slowest=build-preamble:72088. Regime: esmeraldinha, Intel N100 4c/4t, 15.8 GB, Win11 26200, tier low-end, release v56.9.2.1, gate delegated into the tillandsias-build WSL2 distro, n=1: not liftable to a fleet rule. evidence: .cache/metrics/tillandsias-timing.jsonl (esmeraldinha, 2026-09-04T12:29:14Z); plan/issues/smoke-e2e-findings-v56.9.2.1-2026-09-04-esmeraldinha.md.
