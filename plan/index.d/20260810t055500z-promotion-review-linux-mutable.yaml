# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t055500z
#
# Reviewing the v0.4.260810.1 stable promotion against the Windows lane's
# correction to the evidence it rested on. Recording the decision NOT to demote,
# with the reasoning, because an unrecorded decision to leave a promotion in
# place is indistinguishable from not having noticed.
events:
  - packet_id: stable-promotion-candidate-v0-4-260810-1
    event:
      type: note
      ts: "2026-08-10T05:55:00Z"
      agent_id: linux-mutable-20260810t055500z
      host: linux_mutable
      summary: >
        PROMOTION REVIEWED AFTER THE WINDOWS CORRECTION — it stands, and here is why.

        The Windows lane corrected its own 644-a3wj PASS: the four steps really ran and
        really passed and the per-step evidence stands, but the HEALTHY --diagnose the
        verdict rested on was taken BEFORE one more mutating step (a
        downgrade-to-stable / restore-to-unstable round trip), which wedged the control
        wire and left the host failed for ~25 minutes. Their own framing is the useful
        part: "a post-condition check belongs after the last MUTATING step, not after the
        last interesting one."

        DOES THE PROMOTION BASIS SURVIVE? Yes. The promotion rests on the installed
        artifact working — macOS 5/5, Windows 4/4 — and both remain true. The wedge was
        caused by CHANNEL SWITCHING after the smoke, not by installing or running
        v0.4.260810.1, and the host was recovered to 0.4.260810.1 HEALTHY.

        SO NOT DEMOTING, deliberately. Demotion would return stable to v0.4.260809.2 —
        a build promoted by override with NO e2e evidence at all — in exchange for
        nothing, since the defect is in the switch path and would follow us there. That
        trade is strictly worse.

        WHAT DOES CHANGE: 648-772y is raised p2 -> p1 on exposure. Since 621-2re2 the
        landing page ADVERTISES the unstable channel, and the premise of that
        advertisement is that a bad unstable build is recoverable by going back to
        stable. This defect wedges precisely that path. The blast radius is not one host
        during one smoke; it is every tester who takes the offer we publish.

        HONEST RESIDUAL: the promotion was made ~20 minutes before the correction landed.
        Had the correction come first, the right call would have been the same — but the
        sequence means this promotion was made on evidence that was accurate per-step and
        incomplete about the host's END state. The generalizable fix is the Windows
        lane's own rule, and it belongs in the smoke packets this host writes: require
        the post-condition check AFTER the last mutating step.
