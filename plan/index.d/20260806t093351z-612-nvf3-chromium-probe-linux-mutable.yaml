# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: chromium-framework-no-sandbox-probe-false-negative
    order: 612-nvf3
    status: ready
    desired_release: v0.5
    priority: p0
    title: "Chromium framework help-text probe omits required --no-sandbox and crashes hardened launches"
    kind: bug+runtime+litmus
    deliverable: plan/issues/chromium-framework-no-sandbox-probe-false-negative-2026-08-05.md
    depends_on: []
    pickup_role: linux
    capability_tags: [linux, podman, chromium, browser, runtime, testing]
    estimated_hours: 2
    outcome: >
      The framework entrypoint makes a bounded, accurate decision for the
      shipped Chromium even when --help omits the accepted switch, so normal
      product launches work under the existing hardened container boundary
      without moving the flag into the Tillandsias CLI.
    next_action: >
      Replace the help-substring capability proxy, add accepted-but-unadvertised
      and rejected-wrapper fixtures, then run the real hardened framework image
      without caller-supplied --no-sandbox.
    exit_criteria:
      - "a fixture proves accepted-but-unadvertised and genuinely rejected Chromium wrappers are distinguished"
      - "the rebuilt framework image emits expected DOM under network-none, read-only root, CAP_DROP=ALL, no-new-privileges and keep-id without caller flag injection"
      - "the top-level Tillandsias parser still rejects --no-sandbox and outer container hardening is unchanged"
    owned_files:
      - images/chromium/chromium-framework-launch.sh
      - openspec/specs/browser-isolation-tray-integration/spec.md
      - openspec/litmus-tests/litmus-browser-isolation-e2e.yaml
      - plan/issues/chromium-framework-no-sandbox-probe-false-negative-2026-08-05.md

events:
  - packet_id: chromium-framework-no-sandbox-probe-false-negative
    event:
      type: finding
      ts: "2026-08-06T09:33:51Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Clean generated-version image acceptance reproduced exit 139 with the
        entrypoint's current --help probe and exit 0 with expected DOM when the
        switch was explicitly supplied. Filed rather than weakening the outer
        browser hardening or changing a UX surface.
