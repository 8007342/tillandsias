# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: retire-host-src-checkout-behind-proven-transparent-push
    event:
      type: correction
      ts: "2026-09-13T01:31:06Z"
      agent_id: linux-lenovinha-opus5-20260913t013106z
      host: linux
      summary: |
        AMENDING CRITERION 1's OBSERVABILITY EXPECTATION, at the coordinator's
        instruction and on my own launch evidence.

        WHAT THE ROW SAYS NOW: confirm stderr shows '[tillandsias] cloud: ephemeral
        project resolved' / 'Ephemeral cloud project', and notes that ZERO host logs
        fleet-wide contain those strings.

        WHY THAT CANNOT STAND: I ran the cloud launch successfully on lenovinha
        (v56.9.12.1) and neither string appears anywhere in the capture. The zero was
        being read as "this path has never run"; it actually means the path does not
        print those strings. A criterion no working system can satisfy is not a gate, it
        is a trap — the next host either hunts for absent text or, worse, concludes the
        launch failed when it succeeded.

        'Ephemeral cloud project' DOES exist in the source but is behind `if debug`, so
        it cannot appear on a default-path launch at all. 'cloud: ephemeral project
        resolved' matches nothing in the tree.

        WHAT THE LAUNCH ACTUALLY PRINTS, and what the criterion should name instead —
        observed, in order, on an unadorned `tillandsias --cloud 8007342/tillandsias
        --bash`:

          1. "[tillandsias] no active lane containers; cleaning project + shared stack
              for <project> (lane-scoped: ...; keeping application-lifetime: ...)"
          2. the forge banner, which is the load-bearing evidence because it states the
             cloud posture explicitly:
               Credentials   none (git auth via mirror service)
               Code          cloned from git mirror (uncommitted work is ephemeral)
               RAM mounts    /home/forge/src/<project>   <- project source
          3. "Cloning into '/home/forge/src/<project>'..." followed by a completed
             object count — the clone that used to die with Permission denied.

        PROPOSED CRITERION TEXT, for whoever holds the cutover to adopt or amend:
          "the launch reaches the forge banner with Code = 'cloned from git mirror',
           the project mounted at /home/forge/src/<project>, and a COMPLETED clone into
           that path — and does NOT print 'could not create work tree dir
           /home/forge/src/<project>: Permission denied'."
        Every clause of that is on the default path, needs no --debug, and is falsifiable
        in both directions.

        NOT DONE UNILATERALLY: I have not rewritten exit_criteria on the row. This event
        is the evidence and the proposal; the edit belongs to whoever owns the cutover,
        and 440cde994's author may have intended strings that were later renamed, which
        is worth one look before the text is replaced.

        RELATED, now closed: 1119-w2rj. The same launch showed the cloud forge got NO
        mirror redirect (origin resolution exited 128 on the bare relative name), and a
        stray same-named directory would have supplied an UNRELATED repository's origin
        at rc 0. Both are fixed and pinned with a control. That matters to THIS row
        because criterion 1 is about a transparent push: a cloud forge without a working
        mirror redirect could not have performed one, so the launch-side half was not
        merely unobserved — until now it could not have passed.
