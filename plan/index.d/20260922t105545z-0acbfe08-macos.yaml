# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-skills-single-source-guard-asserts-link-shape-over-a-population-that-can-silently-shrink-so-a-runtime-that-does-not-exist-passes-and-deleting-the-tree-it-protects-reads-as-green
    field: verifiable_closure
    value: "a fixture whose arms are: the nonexistent-runtime case REFUSING (it returns\nok:1:16 pre-fix); a divergent-target, a dangling and an absolute-path link\neach refusing, with the absolute-path arm executed inside the builder\ntoolbox; a genuine second source refusing; and both the per-skill and the\ndirectory-symlink layouts passing. Plus a recorded disposition for\ncheck-skill-canonicalization.sh.\n\n\n[2026-09-22T10:55:45Z macos] \nDELIVERED ON work/1255-rvr7, PR #157. Criteria 1-4 changed; criterion 5 verified\nalready satisfied by a landed fix and NOT redone.\n\nCRITERION 1 — POPULATION PINNED. Lenovinha's case now REFUSES:\n  violation:runtime-missing:.nonexistent-runtime/skills — declared in\n  SKILLS_CHECK_RUNTIMES but nothing is tracked there.\nThe expected set is DECLARED (skills/RUNTIMES.txt when present, else the\nbuilt-in list, else the env seam) and the DECLARATION is what is asserted —\nthe shape the unscoreable asks for, so a checkout that legitimately lacks a\nruntime declares it rather than being refused by a fixed five.\n\nCRITERION 2 — RESOLUTION, one rule killing B, C and D: the target must resolve,\ninside this repo, to canonical skills/. Read from the INDEX via git cat-file, so\nthe verdict does not depend on this filesystem and an absolute target is caught\neven where it happens to resolve. THAT MAKES THE \"run the absolute arm inside\nthe builder toolbox\" REQUIREMENT MOOT RATHER THAN SKIPPED: the arm no longer\ndepends on any host's paths, which is strictly stronger than running it in one\nmore place.\n\nCRITERIA 3 AND 4 — the invariant still bites (case 11, a genuine second source\nstill refuses: the control on the FIX, not on the tree) and both layouts still\npass (cases 1 and 6).\n\nTWO THINGS MUTATION AND A DECIDER FOUND THAT I DID NOT:\n  * removing the per-skill resolution call reddened NOTHING — cases 8-10 all\n    plant a bad DIRECTORY link, so the per-skill path was untested. Case 12\n    plants a per-skill one. Without it holes B/C/D simply move between the two\n    supported layouts, which is the defect surviving its own fix.\n  * check-sigpipe-verdict-pipelines-added REFUSED my first draft:\n    `git ls-files ... | head -1 | grep -q .` lets SIGPIPE from the early-exiting\n    head decide the pipeline under pipefail, so A MATCH CAN SURFACE AS A FAILURE\n    — a runtime that IS present reading as missing, the exact inversion this row\n    exists to prevent. I wrote that bug INTO the fix for it. Captured first now.\n\nFixture 7/7 -> 14/14, count DERIVED: it printed \"(7/7)\" while thirteen cases\nran. Second instrument on this host today with a literal self-reported count.\n\nFour mutations, each redding its own arm. Eight relay deciders and cargo fmt\ngreen, read by exit code."
    ts: "2026-09-22T10:55:45Z"
    host: macos
