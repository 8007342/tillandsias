# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    event:
      type: investigated
      ts: "2026-09-13T12:13:01Z"
      agent_id: linux-yoga-claude-20260913t121301z
      host: yoga
      summary: |
        STALE-ROW VERIFICATION: THIS ROW DOES NOT CLOSE. Two of five criteria hold by
        execution against current trunk. Verified rather than recalled, and one of the
        things it caught is an overstatement of mine.

          C1  the failure path PRINTS $out2                          MET
              Present in scripts/test-archiver-ruby-could-not-run.sh at the arm-4 else
              branch ("what the archiver actually said"). Landed 6e7de8915.

          C2  the mechanism is NAMED for the rc=3 shape               PARTIAL — and I
              OVERSTATED THIS. Commit 9999e706a says "criterion 2 answered". That is
              too strong. What was established is that a cancelled gate SURVIVES in the
              toolbox and can run concurrently — reproduced deliberately on yoga,
              conmon-parented, SIGTERM inert — which is a mechanism that WOULD produce
              arm 4's rc=3 by racing a second archiver. What has NOT been established is
              that it produced THIS rc=3: no refusal has been caught with the print
              armed, so nothing links the named mechanism to the observed shape. Named
              and plausible is not named and confirmed.

          C3  same verdict in gate and standalone, on a host that     NOT MET
              reproduced the refusal
              No refusal has EVER been caught with the print armed — every refusal on
              record predates it. Checked the ledger for a captured
              "what the archiver actually said (rc=" and the only hit is MY OWN event
              prose describing the print, not a captured refusal. A matcher finding my
              own writing about a thing is not the thing.
              Runs on record since the print landed: 5/5 in-situ twice, 5/5 standalone,
              5/5 on the host and 5/5 inside the toolbox today. All clean.

          C4  arms 4 and 5 shown independent, or one causes the other NOT MET
              A shared cause is PROPOSED — one stray gate would explain both, arm 4 by
              racing the archiver and arm 5 by leaving scratch — and only arm 4's race
              has been exhibited. The row's own warning applies: a fix for one that
              silently closes the other would bury a real defect.

          C5  NEGATIVE CONTROL: the fixture still refuses a          MET
              genuinely absent ruby
              By execution on current trunk: PASS 5/5 (965-sxec), arms 1-3 green.

        ONE CANDIDATE REMOVED TONIGHT, none confirmed: lenovinha's grep -q/pipefail
        mechanism is refuted for this file by size — $out2 is 310 bytes over 3 lines,
        piped and captured both 0/30, and the producer is small in the FAILING arm too,
        so EPIPE is structurally unavailable rather than merely unobserved (74aed509f).

        WHY THE HEURISTIC FLAGGED THIS ROW, worth knowing at 94-of-510. Every commit
        citing 1132-r4mt is either a PLAN-ONLY progress record or a 1141-vf9w code
        commit that merely mentions this order in its body. A citing commit means
        somebody WROTE ABOUT the row, which on an investigative row is what progress
        looks like — the more honestly a row is worked, the more citing commits it
        accrues without closing. Citation is not completion, and rows that are
        investigated rather than implemented will be the heuristic's densest false
        positives.
