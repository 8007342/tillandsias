# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: amd-vulkan-userspace-missing-from-inference-image
    field: status
    value: ready
    ts: "2026-09-04T09:42:51Z"
    host: linux

events:
  - packet_id: amd-vulkan-userspace-missing-from-inference-image
    event:
      type: note
      ts: "2026-09-04T09:42:51Z"
      host: linux
      summary: |-
        lenovinha's vote and verification recorded (APPROVE both questions, with measurements); released for the remaining host kinds — this is a fleet decision and closes when each has voted, not on one host's evidence
