# Ledger fragment — append-only, IMMUTABLE once written.
packets:
  - packet_id: ci-full-is-red-on-linux-next-at-eef84d6af-and-blocks-the-daily-release
    order: 1022-y7kc
    status: ready
    kind: bug
    priority: p1
    desired_release: v0.5
    release_target: stable-milestone-v1
    pickup_role: any
    capability_tags: [release, ci, gate, litmus, nix, experts, fail-loud]
    estimated_hours: 4
    owned_files:
      - build.sh
      - scripts/check-ca-bind-migration.sh
      - scripts/check-ca-path-literals.sh
      - scripts/check-enclave-ca-consistency.sh
      - scripts/check-inference-container-name-agreement.sh
    title: |
      ./build.sh --ci-full is red on linux-next at eef84d6af (2026-09-04T13:11Z-13:30Z, macuahuitl): three checks, six causes, and the daily release is blocked until each is green or explicitly waived
    context: |
      MEASURED by the coordinator as the release gate for v56.9.4.1 (PR #105,
      linux-next -> main, left OPEN for the next cycle). Both known blockers
      (1006-jfwv, 803-49re) had landed; --check was green; --ci-full was not.
      Six causes, routed:
      1. tray-contract: foreground_git_mirror_lanes_revoke_issued_approle_
         accessors_on_return counted 6 run_cli_with_vault_credential_cleanup
         dispatches in main() against a pinned 5 — the sixth is the
         --ensure-enclave dispatch the coordinator added this morning
         (998-3z6g), in a crate feature set --check does not run (1003-444f's
         class, fourth instance today). FIXED in this cycle: pin bumped to 6
         naming the dispatch; test green.
      2. guard-activation: four guards shipped with no invoker on any
         activation surface — check-ca-bind-migration.sh, check-ca-path-
         literals.sh, check-enclave-ca-consistency.sh (998-3z6g work) and
         check-inference-container-name-agreement.sh. An orphaned guard is
         fixed by INVOKING it (865-n8vq): the static ones belong in
         ./build.sh --check, the runtime ones in a litmus lane or the
         diagnose surface; each must pass on the trunk once wired.
      3. litmus:guest-binary-embed-integrity (ci-release, step 2/2, the nix
         lane, 790-mbk9): fail:guest-binaries-nix-lane-fixture: blocked-rung-
         actually-reaches-cargo. Also red on yoga (35/36) this morning;
         green on this host in the INSTANT tier (187 steps), so it is the
         full-tier arm. Unowned.
      4. litmus:expert-groundtruth-harness step 30/31 (786-kjke): "committed
         glob not green: groundtruth-result: sets=4 total=33 pass=31 fail=2".
         The two misses are 917-6iwv's known reading (both cite a cheatsheet
         over the normative source, lenovinha 2026-09-04); the fixture
         requires the committed glob fully green, so 917-6iwv's residual now
         blocks the release. Decide there: fix the two or scope the fixture.
      5. litmus:expert-capability-skew-honesty step 27/29 (712-r5x8): the
         unset arm printed `expert_capability: now=none after_relaunch=none
         skew=none ... spec_index=present embed_endpoint=unset
         embed_model=unset` where the step expected "ok: embed-endpoint-
         discoverable"; which arm (unset / dead endpoint / stall) failed is
         not visible because the runner prints no rc (1018-5f5a).
      6. litmus:release-gates-run-locally step 13/20 (gate-stamp): red inside
         --ci-full, but scripts/test-gate-stamp.sh run directly afterwards
         passes (7 cases + 14 memoisation cases, ok:gate-stamp-memoization-
         fixture:14); inside the gate the step has a 120 s budget under full
         load, so a TIMEOUT is the likeliest cause and it is indistinguishable
         from a real red without the step's rc (1018-5f5a again).
      Evidence: the coordinator's scratchpad logs cifull-1237c.log,
      /tmp/litmus-pre-build.log, /tmp/tray-check.log, /tmp/guard-activation.log
      on macuahuitl at 13:30Z. Provenance: coordinator measured and files.
    exit_criteria:
      - "./build.sh --ci-full exits 0 on linux-next, or every remaining red is named here with an operator waiver; pre-fix result: FAILS (rc=1, three checks)"
      - "each of the four orphaned guards has an invoker on an activation surface and passes on the trunk; pre-fix result: FAILS"
      - "causes 3, 4, 5 each have an owner event on their own packet (or a new packet) naming the failing arm and its rc; pre-fix result: FAILS"
      - "cause 6 is re-run with the step's rc visible (after 1018-5f5a, or by running the fixture under the gate's timeout by hand) and classified as timeout or defect; pre-fix result: FAILS"
    unscoreable: |
      The closure is `./build.sh --ci-full` exit 0 on linux-next, which no litmus
      wraps (a litmus that ran the full gate would recurse). The per-cause pins
      are the listed litmus tests themselves, already bound.
