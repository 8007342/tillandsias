# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: no-yaml-reader-exists-in-every-environment-the-gates-run-in
    field: status
    value: in_progress
    ts: "2026-08-29T18:52:21Z"
    host: linux

events:
  - packet_id: no-yaml-reader-exists-in-every-environment-the-gates-run-in
    event:
      type: note
      ts: "2026-08-29T18:52:21Z"
      host: linux
      summary: >
        claimed by macuahuitl (coordinator working the queue directly per operator direction); starting with the rg survey of ruby/yq YAML reads in scripts/
