# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-macos-fixtures-cannot-say-which-binary-they-tested-so-a-skewed-run-raises-a-violation-about-the-wrong-subject-and-a-relocated-copy-skips-in-silence
    field: status
    value: in_progress
    ts: "2026-09-21T16:38:39Z"
    host: macbookair-macos

events:
  - packet_id: the-macos-fixtures-cannot-say-which-binary-they-tested-so-a-skewed-run-raises-a-violation-about-the-wrong-subject-and-a-relocated-copy-skips-in-silence
    event:
      type: note
      ts: "2026-09-21T16:38:39Z"
      host: macbookair-macos
      summary: |-
        first PR from this host; the fixtures are shell and run on any host, so it goes as a PR rather than an osx-next relay
