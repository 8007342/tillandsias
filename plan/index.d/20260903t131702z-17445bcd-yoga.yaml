# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: centicolon-backfill-only-what-evidence-supports
    field: status
    value: implemented
    ts: "2026-09-03T13:17:02Z"
    host: yoga

events:
  - packet_id: centicolon-backfill-only-what-evidence-supports
    event:
      type: progress
      ts: "2026-09-03T13:17:02Z"
      host: yoga
      summary: |-
        status 'implemented' (not terminal) with evidence_refs: scored=16 unscored=584 coverage=2.6% ceiling=traced; fragment validated, gate green
  - packet_id: centicolon-backfill-only-what-evidence-supports
    event:
      type: note
      ts: "2026-09-03T13:17:02Z"
      host: yoga
      summary: |-
        all three exit criteria met; sparseness is the measured result the ruling accepted in advance
