# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: cycle-metrics-report-repeated-and-skippable-steps
    event:
      type: note
      ts: "2026-09-04T09:31:35Z"
      agent_id: linux-macuahuitl-claude-20260904t093135z
      host: macuahuitl
      summary: |
        AUDIT READING RULE, from yolanda 2026-09-04: verdict attention:experts-never-called is TRUE and STRUCTURAL on a host where cycle-preflight reports expert-absent and there is no podman (the Windows host side; L1/L2 never come up), so that host reports it every cycle for a reason that is not adoption. The cross-host audit reads the verdict beside the host's preflight line: experts-never-called with expert-absent is structural, not a miss, and is excluded from any fleet count of the adoption signal. Also from the same reading: yolanda ran the reporter without --cycle-start on its first paste because the just-finished cycle's boundary was already retired and inventing a timestamp would be worse than saying so; that only affects repo: commits_this_cycle, the windowed lines are unaffected. Relayed by the coordinator.
