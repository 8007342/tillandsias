# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-24T08:36:07Z.
events:
  - packet_id: spec-expert-refusal-threshold-fleet-decision
    event:
      type: progress
      ts: "2026-08-24T08:36:07Z"
      agent_id: linux-macuahuitl-opus5-20260824T073400Z
      host: linux_mutable
      evidence_refs:
        - "openspec/litmus-tests/groundtruth/spec-rung1.yaml — 0 occurrences of 'unsupported'"
        - "answer.rs:591 — Unsupported requires empty citations; spec-retrieve is cosine top-k and always returns k"
        - "live: an out-of-corpus question returned 3 citations at ~0.59 (a cheatsheet-licence spec, a VM materialize module, a Windows dev-build cheatsheet)"
        - "GPU at measurement: 0% utilisation, 665 MiB, no contamination"
      summary: >
        EXIT-CRITERIA AUDIT BEFORE CLOSING, and the row is NOT closable. I
        declared this campaign finished three cycles ago and left the packet
        `ready`; that was the right verdict on the QUESTION and the wrong state
        for the ROW, because two of its three criteria are unmet.

        (2) MET, and comprehensively: the fleet records that a single global
        cosine threshold is insufficient and names what replaces it. Refuted on
        five axes — one global cutoff, per-corpus cutoffs, top1-top2 margin,
        model scale 137M->4B (monotonically worse), and correct query/passage
        prefixing (0.0057, twelve times smaller than the size effect it would
        have to reverse).

        (1) NOT MET. It asks EACH HOST KIND to report its measured bands. Only
        gpu-cuda has. The low-end tier — the hosts whose hardware the whole
        commodity claim rests on — has not, and that is 864-dvhk, still ready
        and owned by the low-end tier rather than by me.

        (3) NOT MET, AND THIS IS THE ONE THAT MATTERS. It asks that a refusal
        case land in groundtruth expecting `unsupported`, "so the refusal cannot
        silently regress". The file contains ZERO occurrences of `unsupported`,
        and such a case could not pass today:

          answer.rs:591 makes Unsupported reachable only via EMPTY CITATIONS.
          spec-retrieve is cosine top-k. Top-k over a non-empty index always
          returns k. Therefore citations are never empty and the shipped path
          can never refuse.

        Measured rather than reasoned: "what is the flibber flobber protocol and
        how do I bake sourdough bread" returns three citations at ~0.59 — a
        cheatsheet-licensing spec, a VM materialize module, and a Windows
        dev-build cheatsheet. Three confident-looking sources, none of which
        answers it.

        SO THE DISTINCTION THIS CAMPAIGN MUST NOT BLUR: we measured that the
        LAYER refuses correctly — 0/33 false positives, recall@5 double
        recall@1, the near band flat at 0.0% even at k=5. Every one of those
        numbers came from the refusal-calibration HARNESS. The PRODUCT still
        answers everything. Criterion 3 is not paperwork; it is the line
        between "we proved a mechanism works" and "the mechanism is in the
        path", and only the second is worth anything to a caller.

        Criterion 3 therefore depends on 853-6gz3 wiring the layer into the
        answer path, which is itself waiting on the operator's
        fold-on-disagreement decision (conservative / majority /
        divergence-surfacing; macuahuitl recommends divergence-surfacing).
        Recorded as a dependency rather than left as a surprise for whoever
        next tries to close this row.

  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    event:
      type: progress
      ts: "2026-08-24T08:36:07Z"
      agent_id: linux-macuahuitl-opus5-20260824T073400Z
      host: linux_mutable
      next_action: >
        CAMPAIGN STATE 2026-08-24T08:36Z. This pointer had gone NINE HOURS stale
        with every item in it closed, and I read it stale for several cycles
        without refreshing it — the exit contract requires commit/push/attest
        and says nothing about the queue pointer, so it rots exactly the way the
        heartbeat was rotting before 864-t4nq. Read it with
        `tillandsias-plan next-action 831-ezea`, which reports its own source
        and timestamp (864-r9wt).

        FLEET: macuahuitl hourly and lenovinha (~2h) are the only healthy loops;
        the forge lane is alive again. yoga is WEDGED, not silent — alive,
        committing, and refusing every cycle on dirty-start; its two rows are
        REAP-HELD so the reaper can no longer launder them (864-k8dp).
        esmeraldinha, yolanda and the macbook are dark by operator stand-down.
        pirria has NEVER attested despite being in the roster.

        THE REFUSAL CAMPAIGN IS ANSWERED BUT 824-6qxh IS NOT CLOSABLE. Criterion
        2 is met on five axes. Criterion 1 needs the LOW-END tier's bands
        (864-dvhk, not mine). Criterion 3 needs a groundtruth case expecting
        `unsupported`, which cannot pass because the shipped path CANNOT REFUSE:
        Unsupported requires empty citations and cosine top-k never returns
        empty. Measured live — an out-of-corpus question returns three citations
        at ~0.59. We proved the layer refuses; the product does not.

        NEXT, in order:
        (1) 853-6gz3 — wire the layer into the answer path. It is now the
            critical path for 824-6qxh criterion 3 as well as its own row, and
            its one open question is the OPERATOR's: fold-on-disagreement
            (conservative / majority / divergence-surfacing; macuahuitl
            recommends divergence-surfacing, as the only option that does not
            discard the signal that the corpus is ambiguous).
        (2) 865-3xbq — a concurrent-launch smoketest must run diagnostics, not
            work drain. Operator field report 2026-08-24: three agents launched
            concurrently and ran for hours, but unmonitored work-drain meant
            they picked random packets. Smoke Mode already specifies the right
            behaviour and is reachable only by typing "smoke".
        (3) 864-6qsv — set-field cannot write a list-valued reference field.

        NOT MINE: 856-s56y / 857-4kqv (yoga), 860-g798 (yolanda), 864-dvhk and
        the low-end tier rows (pirria/esmeraldinha), 642-fedr / 776-cm74 (yoga,
        finished on its disk, reap-held, operator-gated).

        STANDING COORDINATOR DUTY EVERY CYCLE: merge platform branches into
        linux-next; nobody else has it. Both ahead=0 at 07:34Z.
      summary: >
        Cycle 2026-08-24T07:34Z. Heartbeat clean. Audited 824-6qxh instead of
        closing it on the strength of its own headline, and found the shipped
        path cannot refuse at all. Refreshed this pointer after nine hours.
