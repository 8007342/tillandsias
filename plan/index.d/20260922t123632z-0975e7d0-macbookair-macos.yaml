# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-preflight-roster-enumerates-guards-by-three-rules-and-never-asserts-its-population-so-a-guard-outside-all-three-is-invisible-rather-than-reported-missing
    event:
      type: note
      ts: "2026-09-22T12:36:32Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260918t210700z
      host: macbookair-macos
      summary: |
        A SECOND INSTANCE ARRIVED WITHIN THE HOUR, unplanned, which is worth recording
        because it was not gathered to support this row — it happened to it.

        scripts/check-requirement-ids.sh (976-suab) refused PR #156 with
        violation:requirement-ids-missing:7. It is a ./build.sh --check step and is NOT
        in the preflight roster, so the author's pre-ready sweep could not see it and the
        refusal arrived from the relay. Same shape as
        scripts/test-bound-litmus-is-runnable.sh, which refused #158 from the same blind
        spot an hour earlier.

        TWO REFUSALS, TWO DIFFERENT GUARDS, BOTH OUTSIDE THE STEP EVERY AUTHOR IS NOW
        REQUIRED TO RUN, both against the same author on the same evening. That is not
        an argument that these two guards belong in the preflight — that is the taker's
        per-guard judgement and this row's unscoreable says so. It is evidence that the
        ROSTER'S SILENCE IS ROUTINE rather than exceptional: an author running the
        mandatory step correctly, twice, learned nothing about either guard.

        WORTH THE DISTINCTION, because it bears on how the fix is judged: neither
        refusal was a false positive. Both guards were right, both named their own
        remedy, and both were fixed in minutes once seen. The cost was not wrong
        verdicts — it was WHERE the verdict arrived. A guard that refuses at the relay
        instead of at the author's desk spends a relay pass to say what a second at the
        desk would have said, and the author who ran the required step has no way to
        know it was coming.
