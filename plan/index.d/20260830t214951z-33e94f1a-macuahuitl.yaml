# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: sni-windowid-type-wedges-shell
    event:
      type: note
      ts: "2026-08-30T21:49:51Z"
      agent_id: linux-macuahuitl-fable5-20260830t214951z
      host: macuahuitl
      summary: >
        ROOT CAUSE OF THE FREEZE FOUND AND FIXED — it was not WindowId (that caused the 99.5% CPU spin, real but survivable) and not IO (operator called that correctly: 'a regular process using the drive at full io shouldn't freeze a linux box'). THE FREEZE: about_to_show returned (bool,bool) — wire '(bb)' — where com.canonical.dbusmenu declares AboutToShow -> single 'b'. Operator expanded the ~/src/tillandsias submenu -> shell called AboutToShow -> malformed reply mid menu-grab -> Wayland session wedged, THIRD instance of the wire-signature class (Event '(ib)', WindowId 'u', now AboutToShow '(bb)'). eu-stack of the wedged shell captured (JS promise job, Array.shift on huge arrays). Both fixes built and installed (binary 14:49). OPERATOR DIRECTIVE ADDED TO SCOPE: the tray's tokio event streams need a RATE LIMITER on produced events — tray must be near-0% overhead at idle; 'crashing a system for showing a menu is a far cry from that'. New exit criterion: bound the emission rate of NewIcon/NewStatus/NewToolTip/LayoutUpdated (coalesce within a window), and extend the wire-audit to METHOD REPLY signatures — the property sweep missed methods, which is where two of the three instances lived. BUILD-CACHE ODDITY worth its own look: cargo reported the crate Fresh with source mtime NEWER than the binary, three runs in a row; only deleting the fingerprint forced the compile — on this host's btrfs, do not trust an incremental 'Finished in 0.1s' after an edit without seeing 'Compiling'.
