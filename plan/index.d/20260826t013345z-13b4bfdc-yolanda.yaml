# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: windows-credential-manager-retains-stale-unseal-share-across-guest-wipe
    field: next_action
    value: 'Part A (clear host credentials on every wipe path) landed in a8cd8cc32. RESIDUAL: the reconcile half — a delivered share that fails to authenticate must lose to the guest''s own unseal secret — tracked as 890-y72v, pickup_role linux, because DeliverCredentialsReply carries no accept/reject discriminator. Until it lands, a host that ALREADY holds a stale credential is unaffected by Part A and still needs the documented cmdkey recovery step.'
    ts: "2026-08-26T01:33:45Z"
    host: yolanda

events:
  - packet_id: windows-credential-manager-retains-stale-unseal-share-across-guest-wipe
    event:
      type: note
      ts: "2026-08-26T01:33:45Z"
      host: yolanda
      summary: >
        name the residual on the row so the next reader sees it
