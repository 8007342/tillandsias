# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: plan-only-lane-blames-the-ledger-when-the-plan-binary-cannot-run
    event:
      type: progress
      ts: "2026-09-05T07:43:14Z"
      agent_id: linux-yoga-claude-20260905t074314z
      host: yoga
      summary: |-
        Closed, landed 02db50523. Pre-fix FAILS is my own measurement: with the override pointed at a binary that dies on exec, three arms of test-pre-push-issue-capture-lane.sh went red with "plan-only lane: validation FAILED - tillandsias-plan check refused the folded ledger". The ledger was sound; the binary could not link. HOW IT GOT THERE, every part reasonable: resolve_plan_binary honours an explicit override on EXISTENCE alone by design (704-zcgi), and the lane set have_plan from "did the probe return a path", so a file that cannot execute became a validator and its non-zero exits became verdicts about the data. FIX: one exec at the point the claim is made, not exit-code matching at the three call sites, because a dynamic-link failure and a genuine refusal are both non-zero and only running the thing separates them. A binary that cannot answer capabilities is not a validator, so the lane treats it as ABSENT (routing into the existing fail-closed path) and the fail-closed message now says WHICH of the two it was, since "not available" reads as absent and an operator looking at the file in target/release will not believe it. THE PIN FIRST ARM DID NOT DISCRIMINATE AND THE MUTATION CONTROL SHOWED IT: against the pre-fix hook that arm PASSED while it only looked for "refused the folded ledger", because a dead binary fails validate-yaml FIRST and the lane says "is not valid YAML" - a different sentence, the same misattribution, and one that accuses the pushed fragment even more directly. The arm now rejects every data-blaming verdict; mutation control 2 passed 3 failed against the pre-fix hook, 5 of 5 after. An arm that pins one spelling of a class certifies the class is gone when only that spelling is. BOTH CONTROLS because arm 1 alone is satisfiable by never reporting a ledger fault at all: a genuinely malformed fragment must still be refused, and a real ledger fault must not be blamed on the instrument. scripts/test-lane-instrument-vs-ledger.sh wired into build.sh --check. TWO SIBLING FIXTURES FOLLOW FROM THE SAME RULE: test-pre-push-issue-capture-lane now exports TILLANDSIAS_PLAN_BIN only when the binary has been RUN (it exported target/release unconditionally, which is how an unlinkable file became a validator) and skips the whole file with a named reason when no validator exists, since its acceptance arms would otherwise fail on host tooling rather than lane behaviour - the shape that took macOS out of the landing path this morning, corrected before it could repeat. And the 1058-fenk pin arm 3 is NARROWED: it claimed the ledger arms were exercised in an environment with no runnable binary and no yq, where the lane must fail closed and every acceptance arm with it; that claim held only while the lane accepted an unrunnable binary as a validator, i.e. only while THIS defect was present. It now asserts the fixture does not go red for host tooling, and arm 4 with a real binary is where the arms-really-run claim lives, the only place it can.
