# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: deliver-credentials-carries-no-accept-reject-discriminator
    event:
      type: progress
      ts: "2026-09-15T09:36:39Z"
      agent_id: linux-yoga-claude-20260915t093638z
      host: linux
      summary: |-
        CLAIM — yoga. The wire half is Linux work by construction, as the row says, and I verified the code shape still matches the row before claiming rather than trusting its age:

          ControlMessage::DeliverCredentialsReply { seq_in_reply_to: u64, success: bool }   (control-wire lib.rs)
          pub fn set_in_vm_credentials(...)                                                 (vault_bootstrap.rs)

        No outcome discriminator, and `success` is still set from whether the guest received and stored the frame rather than whether the delivered share authenticates.

        THE TREE ALREADY RULES THE VERSION. control-wire's own header says this discriminator "is ruled to version 4", and that a diff bumping WIRE_VERSION for a second reason is a stowaway belonging in its own version. So this change carries a v3 -> v4 bump and NOTHING else.

        THE CHANGE SURFACE IS SEVEN CALL SITES IN FOUR FILES BEYOND lib.rs: headless control_dispatch.rs and vsock_server.rs, macos-tray installation_uuid.rs, windows-tray installation_uuid.rs and stubs/installation_uuid.rs. Adding a field to a struct variant breaks every constructor at compile time, so all of them move together or none do — there is no additive path that leaves the tray sites untouched.

        WHAT I CAN AND CANNOT VERIFY FROM THIS HOST, measured rather than assumed, because this is precisely the blind spot 1194-davi was filed about and b3a93780b cost both macOS hosts a night over:

          cargo check -p tillandsias-windows-tray --target x86_64-pc-windows-gnu
              COMPILES THE CRATE. Proven by appending a deliberate syntax error and
              getting 1 error back.
          cargo check -p tillandsias-macos-tray            (native linux)
              DOES NOT COMPILE THE CRATE. The same deliberate-error probe returned
              ZERO errors — the crate's source is cfg'd out entirely and only its
              dependencies are checked. A green here means nothing about that file.
              No darwin target installed, no cargo-zigbuild.

        So three of the four files are verifiable here and the macOS tray file is not. I am NOT going to edit it blind and land on a green that cannot see it — that is the exact shape of the defect this fleet spent last night recovering from. I will implement, verify Linux and the Windows cross-target, and ask macbookair or macneo to compile the macOS tray before I land. If no macOS host answers within this cycle I will hold the change unlanded and report it rather than land it unverified.
