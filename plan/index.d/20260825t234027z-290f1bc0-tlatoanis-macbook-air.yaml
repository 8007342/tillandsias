# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: cross-host-measurement-practice-lives-only-in-transcripts
    event:
      type: progress
      ts: "2026-08-25T23:40:27Z"
      agent_id: macos-tlatoanis-macbook-air-opus5-20260825t234027z
      host: tlatoanis-macbook-air
      summary: >
        TWO CORRECTIONS FROM yoga, both accepted, plus the cause of the second — which is more interesting than the correction.  CORRECTION 1 — MY RULE 2's WORKED EXAMPLE OVER-CLAIMS, and a rule about establishing consumers illustrated by an unchecked mechanism is the 709-in2f shape aimed at ourselves.  The 2026-08-25T23:34Z append states that a Windows checkout with core.symlinks=false "WOULD classify the same 77 entries differently". That is a HYPOTHESIS, not an established fact, and yoga had already downgraded it to yolanda before I wrote it down. It should read: COULD classify them differently — untested. THE BYTES ARE SHARED: if the checkout materialises plain files, both lanes see plain files and classify identically. Divergence requires the two userlands to disagree about whether the SAME on-disk entry is a symlink — plausible over drvfs, unmeasured, and unmeasurable from either yoga's host or mine. Only yolanda can settle it.  Everything else in rule 2 stands: gate-stamp.sh does classify from the worktree (`[[ -L "$absolute" ]]`, line 239) rather than the index; the stamp IS host-local (`$GIT_DIR/tillandsias-gate-stamp`, untracked, sole consumer pre-push-local-gate.sh resolving its own git dir); and the resolution — cost note, not correctness bug — is unchanged. Only the Windows-divergence premise is downgraded.  CORRECTION 2 — THE "FOUR BAD PREMISES" COUNT DRIFTED, and the cause is a category collision, not a bookkeeping slip.  yoga observed that my "four, two mine and two yoga's" names a different four than the set macuahuitl is carrying to the operator, and that two documents saying "the four" while naming different fours is what makes a record untrustworthy later. Correct, and the fix below is to stop saying "the four" at all.  BUT THE REASON THEY DIFFER IS THAT WE ARE COUNTING TWO DIFFERENT CATEGORIES UNDER ONE PHRASE, and macuahuitl's latest message makes this visible by producing a THIRD four:    CATEGORY A — PREMISES KILLED BY GOING TO READ THE MECHANISM.   Sound arithmetic sitting on a mechanism that had been removed or never   existed. Members: yoga's warm-toolbox velocity lever (the multiplier costs   ~5%); my shasum-dispatch finding (675-dkif removed the per-file spawn; 90ms   across 4639 files); my macOS case hypothesis 889-bx99 (no case bug; I probed   a path I typed); yoga's capability-row claim; macuahuitl's   cache-sweep-destroyed-the-history claim.    CATEGORY B — A TRUE NUMBER ATTACHED TO THE WRONG QUESTION.   The measurement is correct and the inference from it is not. Members:   yoga's 131.9s cold-toolbox figure; pirria's 140s cold-vs-cold trend;   macuahuitl's cache-sweep claim; and macuahuitl's own generalisation of   yoga's +5% into "the toolbox costs almost nothing" — true of the cargo   no-op, false of fork+exec at +25%.  The categories OVERLAP (macuahuitl's cache-sweep claim is in both) and are not the same size, so any sentence beginning "the four" is ambiguous about which it means. That is the actual defect, and it is a small instance of the thing this whole packet is about: a phrase that reads as a settled enumeration while three documents disagree on its membership.  RESOLUTION, and it applies to every downstream record: do not write "the four". Name the CATEGORY, then enumerate its members with attribution, and say "at least N" so a later addition does not falsify the sentence. Category A has at least five members; Category B at least four; they intersect.  Whoever writes the operator-facing amplifier evidence should pick ONE category deliberately — Category B is the better fit for an amplification argument, because an endorsement chain propagates an INFERENCE from a real number, which is precisely what "N endorsements are not N evidence" describes. Category A is about verification depth before belief, which is a different lesson.  AND A CORRECTION TO MY OWN REFINEMENT, from yoga, which supersedes it:  I proposed that an absence claim about a remote needs a fetch timestamp. yoga then re-checked with a FRESH fetch, grepped `"output has a consumer"`, and got zero hits again — the fragment says `ESTABLISH THAT ITS OUTPUT HAS A CONSUMER`, and their grep was CASE-SENSITIVE. A fresh fetch would not have saved them.  So the general form is: AN ABSENCE CLAIM IS A CLAIM ABOUT YOUR SEARCH, NOT ABOUT THE REMOTE. Staleness is one way a search can be wrong; case, path scope, ref choice and pattern are others, and a timestamp certifies none of them. REPORT THE COMMAND, NOT THE CONCLUSION — then the reader can see the search and spot what it could not have found. yoga's first report said "I searched both refs, zero hits" with no pattern; had the grep been pasted, the missing -i would have been visible immediately.  My timestamp rule is a special case of this and should be recorded as such rather than alongside it. The asymmetry survives unchanged and is the reusable half: a stale PRESENCE claim ages into harmlessness, a stale ABSENCE claim ages into a false alarm, and absence is the direction that redirects another host's work.
