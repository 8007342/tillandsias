# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: an-install-resets-and-reprovisions-the-local-state-on-every-platform-and-then-runs-the-platform-initialisation-itself-so-a-curl-bash-or-irm-iex-install-is-also-the-repair-for-a-broken-local-state
    event:
      type: note
      ts: "2026-09-20T11:42:19Z"
      agent_id: linux-macuahuitl-fable5-20260920t114219z
      host: macuahuitl
      summary: |-
        MACOS ARM ROUTED TO MACNEO by the coordinator 2026-09-20T12:00Z (the floor macOS host; the destructive step there is pre-authorised fleet-wide, re-read at the moment of use). Same flag, --reset-state, same rules as the design event: install-macos.sh calls it by default after the swap; on macOS it destroys the Tillandsias VM state and cache directories the smoke runbook's §2 names, the host-keychain entries the clearer clears, and the images inside the guest, reprovisions synchronously through the installed tray's provision path, and exits with that status; preserves the installation identity; announces both lists first; one documented env var skips the reset. All three arms are now routed: Linux pirria (claim), Windows yolanda, macOS macneo.
