# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: dual-locus-preflight-must-rebuild-every-gate-locus
    field: next_action
    value: 'On a linux host after a mid-cycle sibling pull, observe check-resumable-claim-dirt.sh: expect resumable:/ok:clean-tree from a fresh instrument or unattributable:stale-plan-binary — never plan-query-failed from staleness. That observation closes the packet.'
    ts: "2026-08-23T04:15:47Z"
    host: windows
