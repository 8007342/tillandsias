# Ledger fragment — append-only, IMMUTABLE once written.
packets:
  - packet_id: a-background-wrapper-reports-its-own-exit-0-for-a-child-that-exited-1-so-a-red-gate-reads-as-green-one-line-away-from-the-truth
    order: 1255-s4im
    status: ready
    kind: bug
    priority: p2
    desired_release: v0.5
    release_target: fail-loud-diagnosis-milestone
    pickup_role: any
    capability_tags: [tooling, agent-safety, gate, diagnosis, instrument-defect]
    estimated_hours: 2
    owned_files:
      - build.sh
      - scripts/gate-stamp.sh
    title: |
      a backgrounded `./build.sh --check` that exited 1 was reported as `[exited with code 0]` by the wrapper that ran it, so a RED gate read as green and the resulting stale-stamp refusal was misdiagnosed as a stamp race
    context: |
      FOUND BY macneo-macos 2026-09-18 while syncing osx-next, and the cost was
      a full wrong hypothesis reported to the coordinator before the correction.
      THE OBSERVATION, measured: the captured output of a backgrounded
      `timeout 3000 ./build.sh --check` ends with these two lines, in this
      order:
        EXIT=1
        [exited with code 0]
      The first is the gate's own status, echoed deliberately. The second is the
      WRAPPER's status — the shell that ran it, which succeeded at running a
      command that failed. They sit one line apart and the misleading one is
      LAST, which is exactly where a reader looks.
      WHAT IT COST. Reading the wrapper line, the gate was called green. The
      gate had in fact refused on
      `violation:missing-from-runtime:.gemini/skills/advance-work-from-plan`
      (631-wpkd) and therefore never reached "Writing the gate stamp...". The
      next push was refused with "the tree changed since ./build.sh --check
      last passed", which is TRUE and honest, but it was reported upstream as a
      stamp invalidated by a merge race. The real cause was a red gate that
      wrote no stamp at all. A green-looking gate turns the stamp mechanism's
      correct refusal into evidence for a bug that does not exist.
      WHY THE STAMP FILE DOES NOT RESCUE IT. `.git/tillandsias-gate-stamp`
      existed and was recent (13:37), because an EARLIER gate on this host had
      genuinely passed. A stamp from a previous run is byte-indistinguishable
      from a fresh one except by mtime, so "the stamp exists" reads as
      confirmation. Only comparing the stamp's mtime against the run's start
      time separates them, and nothing prompts the reader to do that.
      WHY THIS IS 1255-s4im's NEIGHBOURHOOD AND NOT A DUPLICATE: 1255-s4im is a
      checkout with no hooks installed, i.e. a check that never ran and said
      nothing. This is a check that DID run, DID fail, and whose failure was
      overwritten by a second instrument's success. Same family — an agent
      concluding from the wrong instrument — different mechanism, and the
      remedies differ: 1255-s4im wants hooks present, this wants the child's
      status to be the one that survives.
      NOT A CLAIM ABOUT ANY ONE HARNESS. Any wrapper that reports its own
      status for a child — a backgrounded runner, a `cmd | tail`, a `timeout`
      whose status is swallowed by a pipeline — has this shape. `./build.sh`
      cannot control its callers, but it CAN make its own verdict
      unmissable and last.
    exit_criteria:
      - '`./build.sh --check` ends its output with a single unambiguous terminal verdict line naming pass or fail (e.g. `gate:red:<first-violation>` / `gate:green:<stamp-digest>`), emitted AFTER every phase, so the last line of any captured log is the gate''s own answer and not a wrapper''s — pre-fix evidence: the last gate line was a phase message, with `EXIT=1` above it and `[exited with code 0]` below'
      - 'a RED `./build.sh --check` states, in that terminal line, that NO stamp was written and that any existing stamp is from an earlier run — today a red gate is silent about the stamp and a stale stamp file from a prior pass is left in place looking current'
      - 'the pre-push stale-stamp refusal distinguishes "no gate has run since the tree changed" from "the last gate ran and FAILED", and says which — today both produce the identical "the tree changed since ./build.sh --check last passed" text, which sent this incident down the wrong path'
      - 'a fixture asserts the above by running a deliberately-red gate, capturing its output through a wrapper that exits 0, and checking the captured last line still reads red; MUTANT — with the terminal verdict removed the same capture reads green, proving the line is what carries it'
    unscoreable: |
      unpinnable-until-the-guard-exists: the deliverable IS the test, so the
      litmus it would be scored against does not exist yet and pinning a name
      nothing defines would be refused by 885-92iu. The intended filename is
      scripts/test-gate-red-survives-a-wrapper.sh, to be written in the same
      commit as the terminal-verdict line it tests (1136-n8sh). Its shape is
      fixed by the last exit criterion: run a deliberately-red gate, capture it
      through a wrapper that exits 0, assert the captured LAST line still reads
      red, and mutate the verdict line away to prove the capture then reads
      green.
    events:
      - type: filed
        ts: "2026-09-18T20:54:46Z"
        agent_id: "macos-macneo-claude-20260918t205446z"
        host: tlatoanis-macbook-neo
        summary: |-
          Filed from a host that cannot gate: trunk at 51db2c14c reds 631-wpkd
          fleet-wide, so this fragment travels the stamp-free plan-only lane —
          the same lane arm 3b of test-pre-push-honours-a-live-freeze.sh
          confirmed on this host earlier today. The incident is recorded in the
          agent's own words because the misread was the agent's, not the
          tooling's: the tooling merely offered two statuses and made the wrong
          one easier to read.
