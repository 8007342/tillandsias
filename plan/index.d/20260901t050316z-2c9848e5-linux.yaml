# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: github-login-validates-push-authorization
    field: next_action
    value: 'EC1 and EC2 MET 2026-09-01 (lenovinha). The push-authorization gate runs inside the ephemeral login container between the gh-auth-status check and the Vault write: a token without .permissions.push on the derived repo is REFUSED, nothing is written to Vault, the message names Contents Read-and-write and the remediation, and the process exits non-zero. Both paste and stdin automation modes are gated, because the gate lives in run_provider_login AFTER the token_script — common to both input modes rather than added to either script. The token still never reaches argv, env, logs or host disk: the probe is a podman exec in the same container. EC3 REMAINS — the hermetic litmus with mocked gh/api responses, including the fine-grained-PAT-without-Contents-write shape. That is the follow-on macuahuitl scoped on release, and it is now much cheaper: github_owner_repo_from_origin, github_push_authorization_probe_args and github_push_authorization_verdict are pure and already unit-tested, so the litmus only has to fake the CONTAINER boundary (a stub gh binary early on the container PATH answering true/false/garbage) rather than the decision logic. Six unit tests cover the decision including two negative controls and a positive control, mutation-tested both directions. DO NOT re-derive the spans: the packet''s cited main.rs line numbers are stale and always will be; find run_provider_login, github_push_authorization_verdict and the GH_LOGIN token-script constants by symbol. ONE THING NOT VERIFIED HERE: no live GitHub call was made. The verdict logic is exercised against recorded answer shapes (true, false, null, a 404 message, empty), not against a real gh api response on this host. A host that runs a real github-login should confirm the probe''s actual stdout matches one of those shapes; 759-vsaj''s fleet propagation is where that gets exercised across all three platforms. NOTE ON THIS FIELD: an earlier write of it was CORRUPTED by backtick command substitution in a shell-passed string — gh help output was spliced into the text. Rewritten from a quoted heredoc. Never put backticks in a value passed through the shell.'
    ts: "2026-09-01T05:03:16Z"
    host: linux

events:
  - packet_id: github-login-validates-push-authorization
    event:
      type: note
      ts: "2026-09-01T05:03:16Z"
      host: linux
      summary: >
        rewriting next_action: the previous value was corrupted by backtick command substitution
