# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-macuahuitl-opus5-20260802
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
status:
  - packet_id: loop-status-crdt-fragments
    field: status
    value: completed
    ts: "2026-08-03T00:44:48Z"
    host: linux-mutable-macuahuitl-opus5-20260802
events:
  - packet_id: loop-status-crdt-fragments
    event:
      type: completed
      ts: "2026-08-03T00:44:48Z"
      agent_id: linux-mutable-macuahuitl-opus5-20260802
      host: linux_mutable
      summary: >
        The fragment overlay now applies to plan/loop_status.md. The CRDT was
        chosen PER SECTION, not assumed from the index overlay (the packet body
        demanded this): prose has no packet_id to key a G-Set on. `## Cycle
        <ts> (<host> — …)` is a G-SET keyed by its heading line (the heading
        embeds timestamp+host, so it IS the stable prose identity; the union of
        two hosts' appends keeps BOTH narratives); `## Direction` and
        `## ACTIVE RELEASE` are LWW-REGISTER, operator-writes-only — append
        refuses any fragment carrying them by name, and fold never touches
        them; every other section is base-only and fragments never carry it.
        The rendered document is a FOLD (base + every fragment's Cycle
        sections, newest first after the title, deduplicated by heading,
        idempotent, pure).

        New CLI, dispatched BEFORE the ledger load because loop_status is a
        different corpus: `loop-status` (the folded view, broken-pipe graceful
        so it survives `| head`), `loop-status-append` (stdin or --file ->
        ONE new fragment under plan/loop_status.d/, refusing any non-cycle
        heading by name, exit 1 on refusal or empty input),
        `loop-status-compact` (fold + delete EXACTLY the consumed fragments by
        name — never a glob; gated on nothing dropped / nothing lost /
        operator sections byte-identical / re-fold idempotent), and
        `loop-status-fragments` (drift verdict with eligibility).
        Fragment filenames are UTC-first `<utc>-<suffix>-<host>.md` (same
        lexical-sort contract as plan/index.d/). Loop-status write paths in
        meta-orchestration and coordinate-multihost-work now append via the
        overlay instead of rewriting the shared document, so two hosts writing
        status no longer conflict.

        13 loop_status unit tests green (commutative, idempotent, G-Set
        survival, gate refusals). Exit criteria all met: per-section CRDT
        justified; two-host fixture shows both narratives with no shared-write
        conflict; Direction cannot be clobbered (append + fold + gates refuse).
        litmus:loop-status-fragment-overlay bound to methodology-accountability
        (9 steps, all green through the runner). ./build.sh --check green
        (cargo fmt + clippy fixed), plan ledger check green, full workspace
        test suite green. Commit 9aeb1031 pushed to linux-next. Packet
        582-nqw5.
