# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: macos-tray-stream-refactor
    event:
      type: progress
      ts: "2026-09-23T20:11:07Z"
      agent_id: macos-tlatoanis-macbook-neo-claude-20260923t201107z
      host: macneo
      summary: |
        AUDIT, NOT AN IMPLEMENTATION SLICE. macneo 2026-09-23, scheduled slot. The
        packet asks to "eliminate any polling loops in action_host.rs" and is estimated
        at 8 hours; it was written 2026-07-06 and its premise has substantially moved.
        Nothing is claimed and nothing is implemented — this is the measurement a taker
        would otherwise spend their first hour on.

        WHAT I MEASURED in crates/tillandsias-macos-tray/src/action_host.rs:

          FOUR sleep sites, and NONE of them is a polling loop:
            :1463  tokio::time::sleep(250ms) — a BOUNDED GRACE so the bridge writer can
                   flush an enqueued close frame before an abort. One-shot.
            :2866  tokio::time::sleep(wait) — RECONNECT BACKOFF for the vm-status PUSH
                   subscription, off BACKOFF_SCHEDULE. That path is already push-based;
                   this is the retry, not a poll.
            :3430, :3468  std::thread::sleep(20ms) — inside TESTS, counting leaked
                   child processes. Not production.

          ONE tokio::time::interval, at :2078 — the HEARTBEAT ticker
          (diagnose::HEARTBEAT_PERIOD_SECS), with MissedTickBehavior::Delay. A
          heartbeat is periodic by definition and is not the polling this packet is
          about.

          THE "poll_read"/"poll_write"/"poll_flush"/"poll_shutdown" hits at :329-:360
          are AsyncRead/AsyncWrite TRAIT IMPLS on ControlWireStream. A grep for "poll"
          finds them and they are not polling; naming them here so the next reader does
          not re-derive that.

          THE ONE THING STILL NAMED A POLLER is the cloud-projects path
          (poll_cloud_projects_once at :710), and the file's own comment at :2971 says
          the tray's fallback poll is ALREADY SUPPRESSED while the push stream is
          healthy (SC-07), with the push source being the CloudRefreshRequest handler.
          So even there the architecture is push-with-fallback, not poll-first.

        WHAT THAT MEANS FOR THE PACKET, offered as a disposition and not taken:
        the refactor this row describes appears to have arrived through other work
        (the persistent listener and the push subscriptions it depends on), and what
        remains is narrower than 8 hours — plausibly only the cloud-projects fallback
        and the "share a common reader-task implementation in tillandsias-host-shell"
        half of the outcome, which is a convergence question rather than an elimination
        one. A taker should RE-SCOPE against this measurement before starting, and
        should consider audit-dispose if order 144's macOS mirror is already satisfied.

        WHAT I DID NOT CHECK, so nobody reads this as more than it is: I did not compare
        against order 144's Windows implementation, did not look at whether
        tillandsias-host-shell could host a shared reader task, and did not run the
        tray. This is a grep-and-read audit of one file, and the packet's outcome names
        two crates.

        I am not claiming the row. A release cut is in progress and this host could not
        land crates/ work to trunk today anyway.
