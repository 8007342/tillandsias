# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: github-login-validates-push-authorization
    field: next_action
    value: |-
      EC1 and EC2 MET 2026-09-01 (lenovinha). The push-authorization gate runs inside the ephemeral login container between 'gh auth status' and the Vault write: a token without .permissions.push on the derived repo is REFUSED, nothing is written to Vault, the message names Contents:Read-and-write and the remediation, and the process exits non-zero. Both paste and stdin automation modes are gated, because the gate is in run_provider_login AFTER the token_script — it is common to both input modes rather than added to either script. The token still never reaches argv, env, logs or host disk: the probe is a podman exec in the same container. EC3 REMAINS — the hermetic litmus with mocked gh/api responses, including the fine-grained-PAT-without-Contents:write shape. That is the follow-on macuahuitl scoped on release, and it is now much cheaper than it was: github_owner_repo_from_origin, github_push_authorization_probe_args and github_push_authorization_verdict are pure and already unit-tested, so the litmus only has to fake the CONTAINER boundary — a stub Work seamlessly with GitHub from the command line.

      USAGE
        gh <command> <subcommand> [flags]

      CORE COMMANDS
        auth:          Authenticate gh and git with GitHub
        browse:        Open repositories, issues, pull requests, and more in the browser
        codespace:     Connect to and manage codespaces
        discussion:    Work with GitHub Discussions (preview)
        gist:          Manage gists
        issue:         Manage issues
        org:           Manage organizations
        pr:            Manage pull requests
        project:       Work with GitHub Projects.
        release:       Manage releases
        repo:          Manage repositories
        skill:         Install and manage agent skills (preview)

      GITHUB ACTIONS COMMANDS
        cache:         Manage GitHub Actions caches
        run:           View details about workflow runs
        workflow:      View details about GitHub Actions workflows

      ALIAS COMMANDS
        co:            Alias for "pr checkout"

      ADDITIONAL COMMANDS
        agent-task:    Work with agent tasks (preview)
        alias:         Create command shortcuts
        api:           Make an authenticated GitHub API request
        attestation:   Work with artifact attestations
        completion:    Generate shell completion scripts
        config:        Manage configuration for gh
        copilot:       Run the GitHub Copilot CLI (preview)
        extension:     Manage gh extensions
        gpg-key:       Manage GPG keys
        label:         Manage labels
        licenses:      View third-party license information
        preview:       Execute previews for gh features
        ruleset:       View info about repo rulesets
        search:        Search for repositories, issues, and pull requests
        secret:        Manage GitHub secrets
        ssh-key:       Manage SSH keys
        status:        Print information about relevant issues, pull requests, and notifications across repositories
        variable:      Manage GitHub Actions variables

      HELP TOPICS
        accessibility: Learn about GitHub CLI's accessibility experiences
        actions:       Learn about working with GitHub Actions
        environment:   Environment variables that can be used with gh
        exit-codes:    Exit codes used by gh
        formatting:    Formatting options for JSON data exported from gh
        mintty:        Information about using gh with MinTTY
        reference:     A comprehensive reference of all gh commands
        telemetry:     Information about telemetry in gh

      FLAGS
        --help      Show help for command
        --version   Show gh version

      EXAMPLES
        $ gh issue create
        $ gh repo clone cli/cli
        $ gh pr checkout 321

      LEARN MORE
        Use `gh <command> <subcommand> --help` for more information about a command.
        Read the manual at https://cli.github.com/manual
        Learn about exit codes using `gh help exit-codes`
        Learn about accessibility experiences using `gh help accessibility` early on the container PATH answering true/false/garbage — rather than the decision logic. Six unit tests cover the decision including two negative controls and a positive control, mutation-tested both directions. DO NOT re-derive the spans: the packet's cited main.rs line numbers are stale and always will be; find run_provider_login, github_push_authorization_verdict and the GH_LOGIN_*_TOKEN_SCRIPT constants by symbol. ONE THING NOT VERIFIED HERE: no live GitHub call was made. The verdict logic is exercised against recorded answer shapes (true/false/null/404/empty), not against a real gh api response on this host. A host that runs a real --github-login should confirm the probe's actual stdout matches one of those shapes, and 759-vsaj's fleet propagation is where that gets exercised across all three platforms.
    ts: "2026-09-01T05:02:52Z"
    host: linux

events:
  - packet_id: github-login-validates-push-authorization
    event:
      type: note
      ts: "2026-09-01T05:02:52Z"
      host: linux
      summary: >
        EC1/EC2 landed; EC3 hermetic litmus remains and is now cheap because the decision logic is pure
