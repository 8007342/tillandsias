# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: headless-bin-suite-shares-process-global-state
    field: status
    value: ready
    ts: "2026-09-16T15:39:03Z"
    host: linux

events:
  - packet_id: headless-bin-suite-shares-process-global-state
    event:
      type: progress
      ts: "2026-09-16T15:39:03Z"
      host: linux
      summary: |-
        status 'ready' (not terminal) with evidence_refs: criteria 1 and 2 MET: ten consecutive parallel runs, 544 passed, identical set; state removed not serialised. Criterion 3 needs workspace-level evidence before the pin can drop
  - packet_id: headless-bin-suite-shares-process-global-state
    event:
      type: note
      ts: "2026-09-16T15:39:03Z"
      host: linux
      summary: |-
        claim released by yoga: the bin suite is deterministic; dropping the gate pin needs evidence this row does not have
