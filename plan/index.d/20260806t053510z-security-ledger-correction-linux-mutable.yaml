# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
status:
  - packet_id: authenticated-forge-write-transport-impl
    field: depends_on
    value:
      - mirror-authenticated-push-transport
      - mirror-repo-config-hardening-never-reaches-existing-volumes
      - git-mirror-cross-project-service-identity
      - git-mirror-egress-network-spec-drift
    ts: "2026-08-06T05:35:10Z"
    host: linux-mutable-root-codex-20260806t001750z
  - packet_id: authenticated-forge-write-transport-impl
    field: blocked_on
    value: >
      unsigned authenticated-transport decision plus existing-volume
      hardening, exact cross-project service/signer identity, and the mirror
      egress contract
    ts: "2026-08-06T05:35:10Z"
    host: linux-mutable-root-codex-20260806t001750z
  - packet_id: mirror-repo-config-hardening-never-reaches-existing-volumes
    field: exit_criteria
    value:
      - "repo receive.denyNonFastForwards=true, receive.denyDeletes=true, and receive.fsckObjects=true are applied on every start, including a pre-existing volume"
      - "pre-receive rejects every ref deletion and every non-fast-forward branch update before invoking the privileged upstream relay"
      - "a functional fixture proves rejected deletion and rewind transactions leave both the mirror and upstream refs byte-identical"
      - "the same fixture proves new refs and allowed fast-forwards still converge mirror and upstream, and restart hardening is idempotent"
      - "the active spec explicitly supersedes the former <=10 deletion relay semantics and records the lane-exit cleanup compatibility decision"
      - "focused git-mirror fixtures, the bound litmus, and the full local build gate pass"
    ts: "2026-08-06T05:35:10Z"
    host: linux-mutable-root-codex-20260806t001750z

events:
  - packet_id: authenticated-forge-write-transport-impl
    event:
      type: ledger-correction
      ts: "2026-08-06T05:35:10Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Reinstated the four real prerequisites from the zero-trust audit after
        the earlier compactor-workaround fragment restored only order 322 and
        left 579/606-bvnp/606-9wqd as non-enforced prose blocks. The text
        compactor now handles sequence-valued LWW fields, so order 451 cannot
        become claimable while containment, cross-project identity, or egress
        policy remains open.
  - packet_id: mirror-repo-config-hardening-never-reaches-existing-volumes
    event:
      type: ledger-correction
      ts: "2026-08-06T05:35:10Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      lease_id: mirror-existing-volume-579-20260806t053006z
      summary: >
        Corrected the first claim fragment's branch-only deletion wording.
        receive.denyDeletes applies to all refs, so pre-relay enforcement and
        the negative fixture must cover tags and other refs as well as branches.
        The previous <=10 deletion relay/override behavior is intentionally
        superseded rather than left as a bypass.
  - packet_id: git-daemon-receive-pack-regression
    event:
      type: ledger-correction
      ts: "2026-08-06T05:35:10Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        The 20260806T004500Z audit fragment accidentally routed its
        audit-correction event to nonexistent packet_id
        mirror-daemon-receive-pack-restored. This event attaches the fact to
        the real order-450 packet without editing the immutable source fragment.
