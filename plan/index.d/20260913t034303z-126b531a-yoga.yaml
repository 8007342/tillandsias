# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: a-dispatch-wrapper-that-execs-into-a-container-cannot-be-killed-so-a-cancelled-gate-keeps-running-in-the-same-checkout
    field: next_action
    value: 'implement the lock as: group live pids by TILLANDSIAS_WRAPPER_TOKEN, refuse only when a group has NO live host-side toolbox run AND a live worker that is itself a build.sh — conmon-only groups are normal residue and leaked test children are not contention'
    ts: "2026-09-13T03:43:03Z"
    host: yoga

events:
  - packet_id: a-dispatch-wrapper-that-execs-into-a-container-cannot-be-killed-so-a-cancelled-gate-keeps-running-in-the-same-checkout
    event:
      type: progress
      ts: "2026-09-13T03:43:03Z"
      host: yoga
      summary: |-
        status 'implement the lock as: group live pids by TILLANDSIAS_WRAPPER_TOKEN, refuse only when a group has NO live host-side toolbox run AND a live worker that is itself a build.sh — conmon-only groups are normal residue and leaked test children are not contention' (not terminal) with evidence_refs: measured on yoga: 1 live, 7 idle, 1 false-stray from leaked sleep 600 in tillandsias-podman
