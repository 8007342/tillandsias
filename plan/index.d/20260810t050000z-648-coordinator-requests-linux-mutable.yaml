# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t050000z
#
# Operator directive 2026-08-10: siblings loop /meta-orchestration until 07:00
# PDT, and this coordinator should request what it needs VIA PLAN TRIAGE.
# There is no cross-host trigger in this project — the ledger is the channel —
# so a request is a packet, not a message.
packets:

  - packet_id: coordinator-asks-overnight-2026-08-10
    order: 648-dvzd
    status: ready
    desired_release: v0.5
    release_target: stable-milestone-v1
    priority: p1
    title: "COORDINATOR ASKS: three things only the sibling hosts can answer before 07:00 PDT"
    kind: coordination
    deliverable: plan/issues/ (one report per answered ask)
    depends_on: []
    pickup_role: any
    capability_tags: [coordination, e2e, validation, release]
    context: >
      This host has been cycling since 19:11Z and has run out of questions it can answer
      alone. Everything below needs a machine this one is not. Listed in the order that
      unblocks the most.

      Read this as a queue of asks, not a claim: take the one your host can answer, work it
      inside your own batch, and record the answer on the NAMED packet rather than here.
    request: |
      1. macOS — 644-7w89 (p0, PINGED). The curl-install e2e smoke of the published
         v0.4.260810.1. It is the last blocker on 644-pnsp, the stable promotion, which
         requires `promote-stable.sh` to run WITHOUT --force. Stable currently serves
         v0.4.260809.2, promoted by operator override with NO e2e evidence; this is the
         chance to satisfy that gate rather than bypass it twice. Windows already
         delivered its headless half.

      2. Windows — 646-qde5. Does the bare `tillandsias-tray.exe` alias WORK standalone?
         Asked three times now and still open, correctly: it needs interactive GUI
         verification, which a headless cycle cannot do. It is a PRIMARY landing-page
         download that this host published and has never seen run. If it needs sibling
         files from the zip, that is a real defect in 621-2re2 — file it, do not work
         around it.

      3. EITHER host with working rootless container egress — 606-9wqd criteria 2-4.
         This host's runtime reproduction was INCONCLUSIVE: enclave-only and dual-homed
         both showed no HTTPS, but the CONTROL showed the DEFAULT podman network also
         fails while the host itself returns 200. Container egress is simply unavailable
         here. Run the two postures PLUS both controls; the enclave/egress comparison
         means nothing unless the default-network control returns 200. Full method and
         the near-miss are in
         plan/issues/git-mirror-egress-spec-divergence-audit-2026-08-10.md.
         This unblocks 451, the only `release-blocker-v0.5`.
    exit_criteria:
      - Each ask is answered on its OWN packet (644-7w89 / 646-qde5 / 606-9wqd), not here.
      - >
        A "cannot" is a valid answer and must be recorded as one, with the reason. An ask
        that is silently skipped looks identical to an ask nobody reached, and this host
        cannot tell those apart from the ledger.
    verifiable_closure: >
      All three named packets carry either a terminal status or a recorded refusal naming
      the blocker.
    notes: >
      Batches are now GREEDIER (budget 3 -> 6, operator directive) and the epics were
      regrouped to fill them: the stream/transport family now hangs off
      socket-audit-master (151) and four all-platforms audits off the new
      architecture-audit-epic (647-6c3g). Both sibling batches currently draw one p0 plus
      the FULL four-packet audit epic — a greedy chunk that is still a single theme.

      If your batch feels too large, take the urgent packet and as much of the epic as
      fits, then say so in your loop status. Budget is a ceiling, not a quota; a cycle
      that closes two packets well beats one that touches six.
    events:
      - ts: "2026-08-10T05:00:00Z"
        event: filed
        summary: >
          Filed because the operator asked this coordinator to request what it needs via
          plan triage while the siblings loop overnight. There is no trigger mechanism
          between hosts; a request is a packet.
