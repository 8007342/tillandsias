# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: build-check-unrunnable-in-forge-ruby-absent
    field: next_action
    value: 'Landed and gate-wired on yoga, but VERIFY IN A REAL FORGE — I reproduced the forge condition with stub ruby/toolbox on PATH, not by observing one. Two things a forge host should confirm: (1) scripts/archive-plan-packets.sh --check returns 3 with the ''no usable ruby in this locus'' verdict and NO ready-set claim; (2) whether the REST of ./build.sh --check then reaches a real verdict there, which is sibling territory (964-zedm) and untested by me. Putting ruby into the forge image was deliberately NOT done: the could-not-run channel is the correct answer for a lane that cannot run the instrument, and adding an interpreter to the image is a separate decision.'
    ts: "2026-09-02T23:19:24Z"
    host: yoga

events:
  - packet_id: build-check-unrunnable-in-forge-ruby-absent
    event:
      type: progress
      ts: "2026-09-02T23:19:24Z"
      host: yoga
      summary: >
        status 'Landed and gate-wired on yoga, but VERIFY IN A REAL FORGE — I reproduced the forge condition with stub ruby/toolbox on PATH, not by observing one. Two things a forge host should confirm: (1) scripts/archive-plan-packets.sh --check returns 3 with the 'no usable ruby in this locus' verdict and NO ready-set claim; (2) whether the REST of ./build.sh --check then reaches a real verdict there, which is sibling territory (964-zedm) and untested by me. Putting ruby into the forge image was deliberately NOT done: the could-not-run channel is the correct answer for a lane that cannot run the instrument, and adding an interpreter to the image is a separate decision.' (not terminal) with evidence_refs: scripts/test-archiver-ruby-could-not-run.sh 5/5; wired into build.sh --check
