# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-kill-time-adjudicator-turns-a-low-cpu-pressure-reading-into-a-claim-that-the-step-was-not-contended-and-is-genuinely-too-slow
    field: status
    value: done
    ts: "2026-09-22T15:49:22Z"
    host: macbookair-macos

events:
  - packet_id: the-kill-time-adjudicator-turns-a-low-cpu-pressure-reading-into-a-claim-that-the-step-was-not-contended-and-is-genuinely-too-slow
    event:
      type: completed
      ts: "2026-09-22T15:49:22Z"
      host: macbookair-macos
      summary: |-
        status 'done' with evidence_refs: work/1358-9wzz is an ancestor of origin/linux-next; the low-stall branch carries 'CPU starvation EXCLUDED' at that tree. The old verdict claimed 'step NOT contended at kill time; genuinely too slow for its 60s budget' about a step running bare in 4.98s on darwin and 5s on macuahuitl, passing through the runner alone at 6.5s — it reds only inside the full suite, so the cause is interference, which the sentence declared absent. The module's own comment already forbade it: the caller must say UNCLASSIFIED, never fall back to a number that measures something else. The asymmetry was the finding — the UNAVAILABLE path was already honest, so the path with partial data produced more confidence than the path with none. Arms 5 -> 7; arm 2 had been PINNING the false string and moved with the fix, and the two added arms assert the absences separately so a reworded-but-still-over-claiming verdict cannot satisfy a positive match. Mutation reds all three. Verified end to end: the proxy-container spec is PASS with the step at 5.1s.
  - packet_id: the-kill-time-adjudicator-turns-a-low-cpu-pressure-reading-into-a-claim-that-the-step-was-not-contended-and-is-genuinely-too-slow
    event:
      type: note
      ts: "2026-09-22T15:49:22Z"
      host: macbookair-macos
      summary: |-
        landed; a low cpu.pressure reading now excludes CPU starvation and nothing else
