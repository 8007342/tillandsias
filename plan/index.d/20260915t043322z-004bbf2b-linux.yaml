# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: trunk-is-red-on-macos-so-no-macos-host-can-land-code
    field: status
    value: completed
    ts: "2026-09-15T04:33:22Z"
    host: linux

events:
  - packet_id: trunk-is-red-on-macos-so-no-macos-host-can-land-code
    event:
      type: completed
      ts: "2026-09-15T04:33:22Z"
      host: linux
      summary: |-
        status 'completed' with evidence_refs: 660e092c8
  - packet_id: trunk-is-red-on-macos-so-no-macos-host-can-land-code
    event:
      type: note
      ts: "2026-09-15T04:33:22Z"
      host: linux
      summary: |-
        the gh auth status probe is bounded only when a bounding tool exists; without timeout(1)/gtimeout(1) the guard behaves exactly as before b3a93780b and reports the missing tool on stderr, leaving that report to the prover that owns it (988-7kxf). The prover row was NOT stale and its expectation was NOT edited: blocked:gh-cli-only is correct, because with timeout absent a credential channel exists and only the probe tool is missing. Verified against origin/linux-next's own copy under a curated no-timeout PATH: blocked:gh-cli-only. Arm 9 of the locked-keyring fixture reproduces the macOS shape on any platform and is falsified against b3a93780b; fixture 9/9. Landed 660e092c8, ok:land:660e092c8:attempt-1, gate 886s no violations. The CLASS is filed separately as 1194-davi and survives this fix
