# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: forge-delegation-concurrency-ramp
    field: next_action
    value: 'Criterion 2 is DONE: methodology/in-forge-delegation.yaml carries stage_lift_gates (G1-G5) with thresholds, an evaluation window of six cycles / four-with-a-delegate, and a dated fed: verdict per gate. THE RULE TO PRESERVE: an unfed metric is a RED gate, never a pass. NO LIFT IS POSSIBLE TODAY and the reason is measurement, not behaviour — only G2 (expert accuracy) is green. UNBLOCK IN THIS ORDER. (a) 943-7dn5: --emit-flow has no caller, so G1 overhead_ratio has never been computable; until it is wired the ramp''s headline metric is structurally absent. (b) G5''s mcp half reads source=absent while pointing at 682-m8ek, which is completed and archived — find out whether the instrumentation regressed or the reporter''s pointer is stale. (c) G3 is currently FAILING on a real expired claim (920-pxg6, stale since 2026-08-29T05:13:52Z); that is the gate working, not a bug. THEN criterion 1, which is the operational half and needs a delegate actually launched and registered through scripts/delegate-outcome.sh — G4 reads sweep:no-registered-runs today, and an empty registry is an unrun stage, not a pass. Criterion 3''s stage log has nothing to record until a lift can occur. DO NOT let the loop lift its own gate (bar_raise_governance): defining the gates is the loop''s half, approving a lift is the operator''s.'
    ts: "2026-08-30T07:18:29Z"
    host: linux

events:
  - packet_id: forge-delegation-concurrency-ramp
    event:
      type: note
      ts: "2026-08-30T07:18:29Z"
      host: linux
      summary: >
        criterion 2 landed; criteria 1 and 3 remain, and three of five gates are blocked on telemetry wiring
