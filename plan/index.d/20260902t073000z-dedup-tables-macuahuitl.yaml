# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: litmus-low-end-instrument
    event:
      type: note
      ts: "2026-09-02T07:30:00Z"
      host: macuahuitl
      summary: >
        COORDINATOR PREDICTION STRUCK, error mine: 'expect podman-path
        green on native podman' was wrong on its axis — there is no
        podman INSIDE either forge guest (host installs never reach the
        container), so both guests fail the five podman tests with
        identical membership and only the failure SHAPE differs (WSL2
        exit-1 fast; native exit-124 after the suite shim's 5s hang).
        The datum is inside-the-guest topology, not virtualization.
        DEDUP TABLES RECONCILED: environmental membership is IDENTICAL
        test-for-test across hosts (5 podman + 3 router-sidecar + 1
        host-browser + 1 absent plan binary), one self-inflicted
        (brew-ondemand under AUTOINSTALL=0 — esmeraldinha A/B-predicted,
        pirria reproduced; classify INFRA-ABSENCE; her refinement: the
        fixture inherits the exported 0 and exits 1, not 127), one test
        defect (shipped-diagnostic grep — reproduced blind on both).
        STRUCTURAL DETERMINISM DATUM: identical executed/skipped/
        coverage/pass-rate (289/183/100%/88%) across WSL2 and native
        with ONE test flipping — the suite is deterministic across
        regimes even where individual steps are not. GREP-DEFECT CLASS
        filed: one fixture (fragment-status-loss-attribution step 1)
        polices prose-vs-declaration while a sibling
        (shipped-diagnostic) falls to it — the corpus contains its own
        counterexample. TWO FLAGGED CANDIDATES ANSWERED FROM THE
        RECORD, no fixture-reading needed: versioning-shape in a
        POST-LITMUS guest is the global-hook cascade collateral
        (esmeraldinha's prior session proved 6/5 -> 11/0 on the single
        variable; the suite reinstalls the unguarded stable hook, so
        fresh guests re-red it after any litmus run — self-referential,
        not content); hook-install-portability step 4/5 is the ONE
        surviving open candidate from that same night, still open.
        Content candidates stand at ~14 after these two. 16-vs-'32
        failures' overstatement note confirmed on the second host.
