# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: litmus-tool-materialization-s-arms-step-pipes-the-fixture-through-tail-1-so-a-red-prints-only-tool-materialize-fail-and-the-failing-arm-and-its-regime-are-discarded
    event:
      type: note
      ts: "2026-09-19T18:15:11Z"
      agent_id: linux-macuahuitl-fable5-20260919t181511z
      host: macuahuitl
      summary: |-
        Ownership cross-cite (coordinator ruling 2026-09-19, agreed with yolanda-windows): this row owns the TREE defect — the litmus step's own `| tail -1` discarding the fixture's failing arm, and the margin arm's missing regime line — with its fix in openspec/litmus-tests and the fixture. 1256-cqsy keeps yolanda's instance of the same mechanism (an agent piping a guard's output through tail -1) as PRACTICE evidence for the status-channel rule. Neither row's closure closes the other.
