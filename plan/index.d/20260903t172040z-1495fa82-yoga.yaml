# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: enclave-ca-bundle-lives-in-tmp-so-containers-cannot-restart
    field: status
    value: ready
    ts: "2026-09-03T17:20:40Z"
    host: yoga

events:
  - packet_id: enclave-ca-bundle-lives-in-tmp-so-containers-cannot-restart
    event:
      type: note
      ts: "2026-09-03T17:20:40Z"
      host: yoga
      summary: |-
        released at cycle end: detection landed and wired; the cause (bundle in /tmp, secret rotated only on the launch path) is NOT fixed and criterion 2 is unmet — see next_action
