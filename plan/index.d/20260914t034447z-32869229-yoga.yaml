# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-plan-only-reintegrate-forces-a-full-re-gate-so-cheap-ledger-traffic-starves-out-code-lands
    event:
      type: investigated
      ts: "2026-09-14T03:44:47Z"
      agent_id: linux-yoga-claude-20260914t034447z
      host: yoga
      summary: |
        THE SECOND READING THE ROW ASKED FOR, taken from the primary evidence rather than from the quoted rule — and it CLEARS the change. Then the change, implemented.

        WHAT 1036-e5w9 ACTUALLY FORBIDS. The rule is quoted in gate-stamp.sh above gate_stamp_plan_digest as "THE FIX IS IN THE MEMO, NEVER IN THE DIGEST", citing a digest-side attempt (1034-ihxw) reverted at 61a56bb3b. I read that revert's diff rather than the summary of it. What 1034-ihxw added to `compute` was a PER-FILE WORKTREE MODE READ — a second exec-bit field sourced from `[[ -x "$absolute" ]]` instead of from the index. gate-stamp.sh records the consequence in its own body: "The first version tested `[[ -x "$absolute" ]]` and DEADLOCKED" on drvfs (889-8tcb). So the harm is a mechanism, not a location: a per-file worktree stat inside compute, on a filesystem where that is pathological.

        WHY THIS CHANGE IS NOT THAT, mechanically and not by analogy:
          * Adding a glob to compute's SKIP list strictly REMOVES reads. It cannot introduce a stat, a mode read, or any per-file worktree access; compute does strictly less work than before.
          * compute's mode source is unchanged and remains the INDEX (`git ls-files -s`), which is the half 1034-ihxw kept and the revert restored.
          * gate_stamp_plan_digest hashes CONTENT ONLY, NO MODES — its own comment says so, and the added glob is hashed exactly like the three already there.
        So the deadlock mechanism is unreachable from this change. The reading in the row holds, and the precise form of the rule the incident supports is: NEVER ADD A PER-FILE WORKTREE MODE READ TO COMPUTE. "Never touch the digest" is a broader restatement than the evidence carries, and it would forbid 930-i6x4 itself, which is the same shape and is the fast lane everything here depends on.

        THE COST QUESTION THE ROW DID NOT ASK, measured before relying on it: plan/issues is 594 files and 5.6 MB, and it now enters plan_digest, which runs on the MEMO path — so a wrong answer here would tax every cheap check. Measured on yoga: plan-digest 10ms today, and hashing plan/issues the same way 12ms. check-issue-citation-convention.sh, which now runs in the partial-memo arm, is 11ms. The memoised-plan arm costs seconds, so this is noise.

        IMPLEMENTED, all four bullets:
          * compute skips plan/issues/*.md, TOP LEVEL ONLY.
          * gate_stamp_plan_digest gains the same set via -maxdepth 1, paired in the same commit. Unpaired, the change would be invisible to BOTH digests, return a full ok:gate-fresh and silently retire the issue guard — 1127-waxf's hole one directory over, and that is now arm 3b's named failure message.
          * build.sh's ok:gate-fresh-except-plan arm runs check-issue-citation-convention.sh, whose subject is the directory just admitted.
          * Three arms in the existing fixture: 3b (a plan/issues-only change yields the partial verdict, and says by name if it went invisible instead), 3c (a SUBDIRECTORY still re-gates, which is what keeps the skip glob and the digest's -maxdepth covering the same set), 5b (the arm runs the issue guard). 9/9.

        TOP-LEVEL ONLY IS A DECISION, not an oversight: plan/issues/research/*.md and anything else nested keeps forcing a full gate. The two mechanisms must cover EXACTLY the same set or a path falls out of one without entering the other, and -maxdepth 1 was the existing shape for the other three globs.

        MUTATION-TESTED THREE WAYS, each with the diff printed and proven non-empty first: removing the plan_digest half reds arm 3b by the name of the hole; disabling the issue guard in build.sh reds 5b; widening the skip to every depth reds 3c. The pre-fix tree is green on all three, which is the point — none of these is observable without the arms.

        A FALSE PASS CAUGHT DURING THE WORK, recorded because it is the class this milestone is about: arm 3c passed on its first run while arm 3b failed, and its pass was worthless — arm 4 had already left a stamp with no plan_digest, so every later verdict was `stale:no-plan-digest-recorded` and 3c read the right word for the wrong reason. Both new arms now re-stamp first and create the directory they need. An arm that inherits a previous arm's broken state is not an arm.
