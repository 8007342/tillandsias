# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: gnu-only-shell-idioms-reach-trunk-green-and-freeze-a-whole-platform-one-host-at-a-time
    field: status
    value: implemented
    ts: "2026-09-12T15:54:50Z"
    host: macos

events:
  - packet_id: gnu-only-shell-idioms-reach-trunk-green-and-freeze-a-whole-platform-one-host-at-a-time
    event:
      type: progress
      ts: "2026-09-12T15:54:50Z"
      host: macos
      summary: |-
        status 'implemented' (not terminal) with evidence_refs: guard + fixture + bound litmus delivered; litmus executes in ci-release and passes; advisory wired in build.sh; 15/15 fixture arms
