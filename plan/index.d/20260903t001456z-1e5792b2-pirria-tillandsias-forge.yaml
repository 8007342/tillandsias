# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: build-check-unrunnable-in-forge-ruby-absent
    event:
      type: note
      ts: "2026-09-03T00:14:56Z"
      agent_id: forge-forge-tillandsias-claude-20260903t001456z
      host: pirria-tillandsias-forge
      summary: >
        CRITERION 2 VERIFIED BY OBSERVATION IN A REAL FORGE — BOTH HALVES, AND THE SECOND HALF FOUND SOMETHING. yoga reproduced the forge condition with stubs and said so plainly; this is the live run on the host the packet was filed from.  HALF ONE — THE ARCHIVER'S REFUSAL IS NOW HONEST. Ran `bash scripts/archive-plan-packets.sh --check` directly in the forge with TILLANDSIAS_HOST_KIND=forge. OBSERVED, exit code captured without a pipe so it is the archiver's own:   ARCHIVER_RC=3   stdout carries the token: `could-not-run:no-usable-ruby (965-sxec)`   and the refusal reads: 'Check FAILED: no usable ruby in this locus, so the archiver's ready-set and orphan-event invariants CANNOT BE EVALUATED. This says NOTHING about the ledger — the instrument is missing, not the data (923-ws3r). Inside a forge this is expected: the image ships no ruby and the brew shim cannot install one without a GitHub credential (965-sxec). Remedy: run this check on a host with ruby or a tillandsias-builder toolbox, or rebuild the forge image with ruby present.' So: exit 3, machine-readable token, canonical refusal, and a NAMED REMEDY — and crucially NOT the old false substantive verdict ('the plan archiver would CHANGE THE READY SET, orphan events, or leave archived rows unanswerable'), which was the actual defect. The claim about the ledger is gone and replaced by a claim about the instrument. Confirmed too that the probe is BY EXECUTION and not `command -v`: `which ruby` here resolves to /home/forge/.local/share/tillandsias/brew-shims/ruby, an executable shim that cannot run, so a command -v probe would have reported ruby PRESENT. The executable bit is a claim; running it is evidence.  HALF TWO — SOMETHING ELSE DID BLOCK BEHIND IT, WHICH IS WHAT NOBODY HAD TESTED. The first full `./build.sh --check` after merging trunk did NOT reach a verdict: it died earlier, at `scripts/test-pre-push-issue-capture-lane.sh` (889-twhe), 6 passed / 4 FAILED, with `fatal: ambiguous argument 'origin/linux-next': unknown revision` and `CONTROL: unexpected verdict for a plain fragment: plan-only lane: not applicable — remote base origin/linux-next is not present locally`.  ROOT CAUSE, AND IT IS THE THIRD INSTANCE OF ONE CLASS TODAY: the ambient global core.hooksPath again. A forge sets core.hooksPath=/home/forge/.cache/tillandsias/git-hooks in ~/.gitconfig, and git's rule is that a set core.hooksPath REPLACES the per-repo hooks directory outright. That fixture seeds its scratch repo with `git push -q -u origin linux-next`, and in a forge THAT PUSH RUNS THE REAL FORGE HOOKS against the scratch tree instead of the fixture's own copied guard. The push fails, refs/remotes/origin/linux-next is never created, and every arm needing a remote base then fails for a reason entirely outside the behaviour under test. PROVED, not inferred: rerunning the untouched fixture with core.hooksPath forced to .git/hooks turned 6/4 into 10 passed, 0 failed, in one command and with no edit.  FIXED the same way as its sibling: `git config core.hooksPath .git/hooks` pinned locally in the scratch repo right after `git remote add`, with the reasoning in a comment. Verified 10/10 BOTH under the forge's ambient config and with hooksPath forced — the two ambients that were previously giving different answers. This is the same defect I fixed in scripts/test-pre-push-empty-ref-list.sh earlier today, in a sibling fixture, and neither was found by reading; both were found by being blocked.  AND THEN THE GATE WENT GREEN. With that fixed, `./build.sh --check` completed in this forge: 'Gate stamp recorded (pre-push will accept this tree)', gate phases totalled 146s, 2m26s wall. THAT IS THE FIRST GREEN GATE IN THIS FORGE ALL SESSION, and it answers the packet's real question — the forge lane can now reach a verdict end to end. 965-sxec's fix was necessary and, once one more fixture of the same class was corrected, sufficient.  SO THE ANSWER TO 'DOES THE REST OF THE GATE REACH A REAL VERDICT': not at first, and the blocker was NOT 964-zedm territory as expected — it was a third core.hooksPath victim. It is now green. Anyone predicting what else blocks a forge should assume more of this class rather than more of the ruby class: two of the three instances today were fixtures that drive git, and the fleet has more of those.  GATE PHASE PROFILE from the green run, floor hardware, for whoever is costing the gate: 146s total; slowest phases 26.2s terminology dictionary (629-t6bx), 25.5s plan ledger tests, 10.2s image rebuild launch tag (747-knbp), 6.8s bash dialect (761-g36m), 6.4s kill-time adjudicator (956-llei), 5.3s plan ledger integrity, 5.1s the empty-ref-list fixture.
