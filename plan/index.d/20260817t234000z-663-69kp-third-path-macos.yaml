# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by macos (osx-next), 2026-08-17T23:40:00Z.
#
# 663-69kp's residual asked whether the --github-login variant was covered by
# 689-stig + 689-y2my, or whether a third path remained. A third path remained.
status:
  - packet_id: macos-oneshot-boot-hangs-unbounded-after-interrupted-teardown
    field: status
    value: completed
    ts: "2026-08-17T23:40:00Z"
    host: macos

events:
  - packet_id: macos-oneshot-boot-hangs-unbounded-after-interrupted-teardown
    event:
      type: completed
      ts: "2026-08-17T23:40:00Z"
      agent_id: macos-opus5-metal-20260817T232300Z
      host: macos
      summary: >
        THE RESIDUAL QUESTION IS ANSWERED: A THIRD PATH REMAINED, and it is now
        bounded. The residual read "confirm whether the --github-login variant of
        the symptom is fully covered by 689-stig plus 689-y2my or whether a third
        path remains." It was NOT covered. `prompt_line`
        (diagnose.rs) did an `std::io::stdin().read_line()` with NO
        is_terminal() guard and NO timeout — the same mistake 689-stig fixed one
        function away, in the function --github-login actually uses (three call
        sites: token, author name, author email).
        IT IS WORSE-PLACED THAN THE 689-stig READ, IN A WAY THAT CHANGES THE
        SIGNATURE. That read blocked BEFORE the VM existed, giving the
        pre-breadcrumb silence this packet was originally filed about. This one
        is called from inside the DynamicExpect response closures — AFTER the VM
        is booted and the control wire is open — so it wedges having already
        printed its progress lines, while HOLDING A LIVE VM. And per the comment
        on the expects list, the guest's 30s PTY heartbeat keeps resetting the
        exec idle deadline (689-y2my), so nothing upstream bounds it either. The
        07-29 prompt-ORDERING deadlock described in that same comment was a
        second, independent cause of the same 70-minute wedges; it was fixed by
        reordering, which is exactly why this one survived unnoticed.
        FIX, mirroring 689-stig's insight ("not a TTY" is not "a pipe that will
        EOF") without breaking attended use: an interactive stdin is read
        UNBOUNDED (a human reading a prompt and typing a token must never be
        timed out — bounding that would be the bug), a non-interactive stdin is
        read on a detached thread with a 30s bound, and on expiry it refuses
        loudly. Echo restoration was also moved onto EVERY exit including the
        timeout path: `stty -echo` outlives the process, so a hang used to leave
        the operator's terminal with echo disabled on top of the confusion.
        THE REFUSAL TEXT WAS WRONG ON FIRST DRAFT AND VERIFYING CAUGHT IT. I
        wrote "pass --with-token", from memory. main.rs:262-271 is the authority
        and says the opposite: --with-token is a tillandsias-headless GUEST flag
        with NO macOS host equivalent, the tray's unknown-flag arm refuses it,
        and order 663-acdw records that exact suggestion costing one session two
        blind credential runs. On macOS the host drives the guest login over the
        control wire, so PIPING is the supported unattended path — the very path
        this bounded read has to keep working, not merely tolerate. The refusal
        now prints the working printf pipeline in the guest's prompt order and
        names --with-token as NOT accepted. A stale personal memory asserting
        the wrong flag was corrected in the same pass.
        AUDIT COMPLETED RATHER THAN STOPPED AT THE FIRST HIT — there is no
        FOURTH path. Every remaining blocking read on a one-shot path is
        accounted for: the 689-stig read_to_end (detached thread, 5s bound); the
        interactive read_line (correctly unbounded); the piped read_line (this
        fix, 30s bound); pty_vsock_bridge's `rx.recv().await` (an async pump loop
        whose termination on channel close is correct, not a one-shot
        pre-breadcrumb path); its sibling at :497 (already inside a 2s timeout);
        and SingletonGuard::try_acquire (non-blocking by construction).
        TEST + FALSIFIABILITY. prompt_line_is_bounded_for_non_interactive_stdin
        pins the guard, the bound, the UNBOUNDED interactive branch, that
        restore_echo is reachable from all three exits, and that the refusal does
        not send an operator to --with-token. Falsified against two shapes:
        replacing recv_timeout with recv() fails it, and bounding BOTH branches
        (which would satisfy a naive "is it bounded?" check while breaking every
        attended login) fails it with the interactive-branch diagnostic.
        HONEST LIMIT, stated because it matters: this is a SOURCE-SHAPE test, not
        a behavioural one. It cannot prove the timeout fires — only that the
        shape is right. A behavioural test would have to boot a VM, which is far
        too heavy for a unit test; the property is pinned this way rather than
        not at all, and a green result means "the shape is right", not "measured".
      evidence_refs:
        - "crates/tillandsias-macos-tray/src/diagnose.rs — prompt_line: is_terminal guard + PROMPT_LINE_PIPED_TIMEOUT bound + restore_echo on every exit"
        - "crates/tillandsias-macos-tray/src/main.rs:262-271 — the authority that --with-token has no macOS host equivalent (663-acdw)"
        - "test prompt_line_is_bounded_for_non_interactive_stdin (source-shape, limits stated)"
        - "falsifiability: recv()->fails; bounding both branches->fails on the interactive-branch assertion"
        - "audit: no fourth unbounded one-shot path (689-stig read bounded; bridge recv is an async pump; try_acquire non-blocking)"
