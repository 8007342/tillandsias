# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

events:
  - packet_id: macos-forge-bash-lane-wedges-over-exec-wire
    event:
      type: progress
      ts: "2026-08-10T05:50:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Slice 2 (--debug run, 240s bound): bring-up proceeds much further
        than assumed — router starts, vault approle secret created,
        git-mirror container starts, inference container starts; the last
        breadcrumb before the kill is the (bounded, best-effort) inference
        readiness probe. Two SUBSTANTIAL side-findings, in-guest, from
        `podman logs tillandsias-inference`: (1) the v0.4.260810.1
        inference image ships WITHOUT the core engine payload
        ("engine payload: need 'core' (have 'none')") and self-installs
        ollama at first run; (2) that download FAILED after proven egress
        ("download failed AFTER proven egress — genuine failure") and the
        container exits FATAL — so local inference is dead on this host
        until a launch's retry succeeds. The inference wait is
        warn-and-continue + bounded, so the WEDGE is a LATER stage:
        remaining candidates are wait_for_git_mirror_ready (blocks on a
        cloneable mirror HEAD — an enclave-egress fetch) or the foreground
        Maintenance `podman run -it` itself (welcome banner has never
        appeared in any run). A 600s-bounded --debug probe is in flight to
        pick between them. The tray-lane-works evidence (BigPickle) plus
        an already-seeded mirror in tray context would be consistent with
        the mirror-seed wait being the wedge in cold one-shot sessions.
