# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-valid-mo-full-marker-is-not-evidence-the-ledger-was-written
    event:
      type: progress
      ts: "2026-09-03T05:50:46Z"
      agent_id: linux-lenovinha-opus5-20260903t054145z
      host: lenovinha
      summary: >
        IMPLEMENTED AND CLOSED at 840892c3b. Both exit criteria met, and the second one is met in a DIFFERENT SHAPE than it was written — the literal version is unsatisfiable, and I want that on the record rather than quietly substituted.  CRITERION 2 AS WRITTEN CANNOT BE IMPLEMENTED. "self should refuse to print COMPLETE when the ledger carries no line for the head it is attesting" refuses EVERY CORRECT CYCLE. The ledger line attests the WORK head; committing that line moves HEAD; the terminal marker is then re-derived one commit later, by design, per Finalization step 9. So at the moment self prints the final marker, the ledger will never contain a line naming the head being attested. That is exactly the auditor caveat you added to the packet after nearly tripping on it — the same trap, one level down, in the implementation of the fix for it.  WHAT I BUILT INSTEAD, and I think it is what the criterion meant: the check is against the CYCLE, not the head. `record` stamps the head it recorded; the boundary guard's `snapshot` clears that stamp at Start Of Cycle; and `self` refuses a COMPLETE marker unless HEAD descends from a head THIS cycle recorded. It reuses the exact pattern self already applies to the startup boundary (717-3bvv), so there is one mechanism rather than two, and it fails in the same loud direction — no marker, which every outer launcher already treats as failure.  THE CLEARING STEP IS LOAD-BEARING AND IS NOT OBVIOUS. Without it the stamp answers "has this checkout ever recorded" instead of "did this cycle record", so one successful record would license every later cycle's marker forever. That is the original defect with a longer fuse, and it would have passed a fixture that only tested one cycle. Case 6 exists for it.  BLOCKED IS EXEMPT, deliberately. A cycle reporting that it did not finish must still be able to say so, and requiring a record there would silence precisely the marker whose job is to report incompleteness. Criterion 2 says "refuse to print COMPLETE" and I read that narrowly on purpose.  CRITERION 1: scripts/test-mo-full-record-precedes-marker.sh builds a temp repo with a real bare remote and walks MY EXACT SEQUENCE — verify boundary, record refuses on unpushed HEAD, retry loop lands the work, marker must REFUSE, record succeeds and writes the ledger, ledger commit moves HEAD, terminal marker prints. Seven cases, wired into ./build.sh --check.  YOUR CAVEAT IS AN ASSERTION NOW, NOT A COMMENT. Case 5 asserts that the ledger does NOT name the terminal head and that the recorded head IS an ancestor of it. I nearly wrote that assertion the wrong way round — "the ledger names the head the marker names" — which would have failed on every correct cycle and looked like the bug reproducing. Your warning is the only reason I checked.  AND I RAN THE NEGATIVE CONTROL ON MY OWN GUARD, because the lesson of the last two days is that a guard which cannot fail is the same defect one level up. With the coupling disabled, the fixture reports exactly two regressions: the marker printing with no record, and a previous cycle's record licensing this one. Both the cases that matter, both caught.  ONE EXISTING FIXTURE NEEDED UPDATING AND I DID NOT PAPER OVER IT. `mo-full-attest.sh fixture`'s self-verified-boundary scenario began failing, because a COMPLETE marker now has two preconditions and that scenario only satisfies one. It tests the BOUNDARY precondition, so it now satisfies the record one by default and a new scenario, self-no-ledger-record, drives the new one directly — 15/15. Letting the boundary scenarios fail for a reason they were not written to test is how a fixture stops measuring the thing it names.  THE SKILL ALREADY CARRIED YOUR PROSE WARNING, written from my report four hours ago, and it is good — so I did not duplicate it. I added only that the rule is now ENFORCED rather than advised, and the sentence that matters for whoever reads it next: the guard catches a skipped record, it cannot make a retry loop call `record` for you. The ordering is still the agent's to get right.
