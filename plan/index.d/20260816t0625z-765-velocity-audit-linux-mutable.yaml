# Filed by linux_mutable (macuahuitl), 2026-08-16: the build-velocity audit's
# packet drafts (workflow wf_7ca75985-a53, operator velocity directive 2026-08-15).
# Full report: plan/issues/build-velocity-audit-2026-08-15.md
packets:
- packet_id: gate-per-step-telemetry
  order: 765-dfry
  status: ready
  desired_release: v0.5
  priority: p1
  kind: build-velocity+telemetry
  title: 'Per-step gate telemetry: preamble, per-guard, per-check, and per-litmus-test
    durations'
  deliverable: 'timing_emit instrumentation in build.sh (per --check step: fmt, clippy
    x2, plan check, plan-orders, each guard script, trace-coverage --gate, stamp write,
    plus the preamble record), a duration_ms field per check in local-ci''s check-log
    archive, and per-test elapsed in run-litmus-test.sh compact output'
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: Only three step names exist in /tmp/tillandsias-timing.jsonl;
    local-ci.sh:432-452 times only run_litmus_phase; the build.sh preamble (:357-:715)
    runs before the --check timer at :1081, so the sidecar-rebuild tax is invisible;
    the 254s quick lane cannot be ranked test-by-test. Prerequisite evidence substrate
    for approving or revoking every other velocity change.'
  exit_criteria:
  - Every --check step and every guard script emits a named timing record under the
    682-emvg grammar (best-effort, never alters wrapped exit codes; 693-tf79 rejection
    applies)
  - A --ci-full run's wall clock is attributable across quick-litmus / clippy-all-features
    / rust-tests / install from the records alone
  - The 55 pre-build quick litmus tests are rankable by measured duration (per-test
    elapsed in compact output), naming which of the 44 cargo-invoking tests dominate
    the ~254s lane
  - 'The post-VERSION-bump sidecar rebuild appears as a named slowest-step candidate
    in the timing: rolling line'
  - No gate exit code changes under fault injection (timing sink unwritable)
- packet_id: gate-stamp-scope-awareness
  order: 765-dt8h
  status: ready
  desired_release: v0.5
  priority: p1
  kind: build-velocity+gate-integrity
  title: 'Scoped gate stamps: record what the gate validated and make pre-push compare
    scope to the outgoing diff'
  deliverable: gate-stamp.sh write/verify extended with a scope field (full | class-set);
    pre-push-local-gate.sh verify path comparing stamp scope against the outgoing
    diff's change classes, refusing with a loud message naming the missing classes
    and the exact rerun command
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: gate-stamp.sh content-hashes the whole tree but records
    THAT a gate passed, not WHICH (verified: no scope concept in the script); _write_gate_stamp
    (build.sh:825-859) writes identically from --check/--ci/--ci-full; pre-push accepts
    any fresh stamp. Without this, any scoped run converts every selector skip into
    trunk exposure — the highest silent-green risk in the whole design.'
  exit_criteria:
  - A stamp written by an unscoped (full) gate behaves exactly as today (byte-format
    change is backward-compatible or migrates loudly)
  - A scoped stamp is accepted only when the outgoing diff's classes are a subset
    of the stamped classes; any parse ambiguity or unknown scope token is treated
    as stale (fail closed) with the full-gate rerun command printed
  - The 668-2xeh plan-only lane is preserved intact as the narrowest instance and
    its fail-closed refusal grammar is reused
  - 'Negative litmus: a stamp scoped to {plan-ledger} plus an outgoing diff touching
    crates/ is refused at pre-push'
  - No scoped stamp can be produced by any gate until this packet lands (ordering
    constraint on the selector packets, recorded in their depends_on)
- packet_id: litmus-diff-scoped-selection
  order: 765-mza8
  status: ready
  desired_release: v0.5
  priority: p1
  kind: build-velocity+litmus-tooling
  title: 'Diff-scoped litmus selection: inputs metadata plus a fail-closed --diff-scope
    rung in the runner'
  deliverable: 'An optional inputs: glob-list field in litmus test YAML (mirroring
    the phase:/size:/host_kind: pattern at run-litmus-test.sh:342-448); a --diff-scope
    <base> runner flag inserting a per-test SKIP after the size check, active only
    when the test declares inputs, the diff misses them all, and the test file itself
    is unchanged; annotations for the slowest pre-build quick tests first (per the
    telemetry packet''s ranking); a new added-inputs guard in the 634-39ik/698-7n6q
    family refusing dead globs'
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: The quick lane costs 251.6-268.9s per --ci-full and
    pays full price when red (local-ci.sh:60 set +e); selection is phase/size/host-kind
    only; 236 instant + 55 quick pre-build tests run for a plan-fragment-only diff.
    Precedents: 634-39ik diff-scoping (polarity inverted: this REMOVES coverage so
    it fails closed), 668-2xeh loud per-file acceptance. Depends on the scoped-stamp
    packet.'
  exit_criteria:
  - Tests without inputs always run; --ci-full's in-loop, rate-limited invocation
    may pass --diff-scope, but the release lane (merge-to-main-and-release), the daily
    full anchor, and any run where the base ref is missing or the diff unparseable
    run FULL, loudly
  - Every diff-scoped skip uses the loud per-test SKIP grammar naming the base SHA;
    an all-skipped phase reports 'N diff-scoped skips against base <sha>' — never
    a vacuous green (explicit-filter zero-match failure at run-litmus-test.sh:1443-1463
    preserved)
  - 'A NEWLY ADDED inputs: field whose globs match no tracked file is refused by the
    added-inputs guard (a dead glob is an always-skip)'
  - A test file's own change always forces the test to run regardless of inputs
  - A 24h full-run ratchet refuses to skip (forces FULL, loudly) when the last recorded
    full quick-tier run is older than 24h
  - 'Measured: in-loop --ci-full pre-build litmus drops from ~254s to a diff-proportional
    cost on a plan-only tree, with the skip ledger naming every scoped-out test'
  - e2e/runtime tests remain unannotated (their input is the built product)
- packet_id: build-check-stamp-memoization
  order: 765-tkq2
  status: ready
  desired_release: v0.5
  priority: p2
  kind: build-velocity+gate-tooling
  title: 'Stamp-based --check memoization: loud early exit when tree bytes are identical
    to the last green gate'
  deliverable: 'An early check at the top of build.sh''s --check block: when gate-stamp
    verify returns ok:gate-fresh (with toolchain versions folded into the stamp),
    print the memoized verdict with the stamp timestamp and the TILLANDSIAS_FORCE_CHECK=1
    override, and exit 0'
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: gate-stamp.sh already proves ''the gate validated these
    bytes'' (~60ms content hash over 4,241 files) and pre-push already trusts it —
    the same trust extended to the gate''s own entry point. --check runs 2-5x per
    cycle at p50 8.1s green; telemetry shows clusters minutes apart on identical trees.
    Risk (environmental drift the hash can''t see) is bounded by the toolchain fold
    and the force knob.'
  exit_criteria:
  - rustc -V and cargo clippy -V are folded into the stamp so a toolchain swap invalidates
    memoization
  - The early exit prints one attributable line every time — 'ok:gate-fresh (stamped
    <ts>; TILLANDSIAS_FORCE_CHECK=1 to re-run)' — never a silent green
  - TILLANDSIAS_FORCE_CHECK=1 bypasses memoization unconditionally
  - The 2nd-5th --check of a cycle on an unchanged tree (post-rebase no-op, finalization
    after stamp-covered edits) measures ~0.3s instead of 8-12s in the timing records
  - Memoization applies to --check only; --ci/--ci-full/--release never memoize
  - 'Negative litmus: editing any tracked or untracked byte after a green gate forces
    a full re-run'
- packet_id: change-class-gate-selector
  order: 765-xpct
  status: ready
  desired_release: v0.5
  priority: p2
  kind: build-velocity+gate-tooling
  title: Change-class selector (change-class.sh) with pilot scoping of the two heavy
    plan/litmus guards
  deliverable: 'A sourced scripts/change-class.sh computing the change-class set once
    per gate run (diff vs merge-base with origin/<platform>-next, plus worktree-dirty
    and untracked, mirroring 634-39ik''s shape); a selector matrix in build.sh keying
    gates to declared input classes; pilot: check-fragment-status-loss (key: plan-ledger
    ∪ plan-core incl. crates/tillandsias-plan) and check-groundtruth-mutable-status-pins
    (key: spec+litmus ∪ plan-ledger); the keep-unconditional list embedded as data
    with per-entry incident citations'
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: Investigator-measured: fragment-status-loss 2,381ms
    + groundtruth-mutable-status-pins 1,083ms of the ~7s warm --check are pure functions
    of narrow path families (verified: FSL reads plan/index.d + the tillandsias-plan
    fold; GMS reads openspec/litmus-tests/groundtruth + the plan binary). Precedent
    shape: 634-39ik (loud diff-scoping) + 668-2xeh (fail-closed class keying), polarity
    inverted because this consumer removes coverage.'
  exit_criteria:
  - Unknown path → FULL; missing/unfetchable base ref → FULL; build-tooling class
    (build.sh, local-ci.sh, run-litmus-test.sh, the guard's own script) → FULL; last
    full run older than 24h → selector refuses to skip — all four fail-closed paths
    covered by negative litmus tests
  - Every skip prints one line naming the gate, the intersecting-class evidence, the
    diff base SHA, and the last FULL run timestamp
  - The selector writes only scoped stamps (depends on the scoped-stamp packet; a
    scoped run can never write scope=full)
  - The keep-unconditional set (gate-stamp verify, release-preflight, ghost-trace
    ratchet, version-bump isolation + monotonicity, guard-activation audit, python/base64
    bans, plan-schema-divergence, mo-full-attestations, podman-sync-budgets, script-exec-bits,
    the three added-* guards) is data with a comment per entry naming its incident
    (599-4wzr, dd8fd63f, 668-2xeh) — removing an entry is a reviewable diff
  - Operator (Tlatoani) approval recorded before activation, as the symmetric twin
    of bar_raise_governance (methodology/convergence.yaml:410) — scope reduction is
    a bar decision the loop must not self-enact
  - 'Pilot measured: a code-only cycle drops ~3.5s per --check with both pilot guards''
    skip lines present in the log'
- packet_id: tray-fingerprint-stability-non-artifact-lanes
  order: 765-evbt
  status: ready
  desired_release: v0.5
  priority: p2
  kind: build-velocity+build-hygiene
  title: Stabilize tray-crate build fingerprints in non-artifact lanes
  deliverable: 'macos-tray build.rs: constant TILLANDSIAS_BUILD_TIME fallback unless
    SOURCE_DATE_EPOCH is set, plus a TILLANDSIAS_GIT_SHA_OVERRIDE honored via rerun-if-env-changed
    that suppresses all .git rerun directives when set; windows-tray build.rs: the
    existing BUILD_COMMIT_SHA_OVERRIDE (:94) likewise suppressing its .git tracking
    when set; loop wiring exporting the overrides for --check and the pre-build CI
    phase ONLY; a build.sh guard refusing the overrides inside --install and --release
    dispatches'
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: macos-tray build.rs:31-32 stamps date -u on every rerun
    and :55-56 tracks .git/HEAD+index; windows-tray build.rs:82-92 tracks refs/heads/<branch>+packed-refs
    (~3 commits/hour) with the override hook already present but the directives unconditional.
    Both crates recompile under every one of the ~5 per-cycle compile variants after
    any git activity; post-commit checks measure 9-12s vs 5.5-7s clean. The .git/index
    quick win lands first; this packet finishes the job.'
  exit_criteria:
  - With overrides exported, two consecutive commits produce zero tray-crate recompiles
    in check/clippy/test lanes (verified via cargo build-plan or fingerprint inspection)
  - build.sh --install and --release hard-fail loudly if either override is set in
    the environment — a staged artifact can never carry false provenance
  - The e2e embedded-SHA freshness gate still fails loud on an override-built binary
    (the safe direction, verified by negative test)
  - Nix/release lanes are untouched (exact provenance preserved)
  - SOURCE_DATE_EPOCH behavior for reproducible release builds is unchanged
- packet_id: sidecar-staleness-evaluation-stamp
  order: 765-5efu
  status: ready
  desired_release: v0.5
  priority: p2
  kind: build-velocity+build-hygiene
  title: 'Sidecar staging staleness redesign: evaluation stamp + dev-lane thin-LTO
    profile'
  deliverable: build-sidecar.sh keeping a gitignored evaluation stamp (e.g. images/router/.sidecar.stamp)
    so is_stale's find -newer keys on the last evaluation rather than the staged binary's
    mtime (eliminating the perpetual re-stale state the byte-compare quick win leaves
    after VERSION bumps); optionally a [profile.release-sidecar] (inherits release,
    lto=thin, codegen-units=16) for the local dev lane only
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: build-sidecar.sh:49-63 is_stale keys find -newer on
    the staged binary; VERSION is in the input set deliberately (710-w9kc) but the
    binary embeds only WIRE_VERSION (main.rs:136-141, build_version: None), so bump-triggered
    rebuilds are byte-identical; the cascade into tillandsias-headless''s asset tracking
    is the multi-variant cost. The byte-compare quick win stops the cascade; this
    packet stops the repeated warm rebuilds too.'
  exit_criteria:
  - After a VERSION bump with unchanged sidecar sources, the SECOND and later build.sh
    invocations skip the sidecar cargo build entirely (stamp newer than VERSION),
    verified by the preamble timing record
  - The find -newer input set (3 crates + Cargo.toml + Cargo.lock + VERSION) is unchanged
    — only the reference file moves, so false negatives are impossible by construction
    (a genuinely newer source always trips the stamp)
  - The 723-b9cn ELF-format assert and wrong-artifact removal are untouched
  - release.yml and the nix derivation keep fat LTO; only the local dev lane may use
    the thin profile, and order 710-w9kc's version-matched intent for install/release
    lanes is preserved and documented in the script header
  - 'Negative test: editing a sidecar source file after the stamp forces a rebuild
    and a restage'
- packet_id: crane-deps-stability-and-binary-cache
  order: 765-8hc3
  status: ready
  desired_release: v0.5
  priority: p1
  kind: build-velocity+nix
  title: Crane deps-derivation stability and a fleet binary cache (extends 606-um5s)
  deliverable: 'flake.nix: a depsOnlySrc (sourceByRegex or equivalent covering Cargo.toml/Cargo.lock/crate
    manifests, letting crane''s mkDummySrc stub the rest) passed to all four buildDepsOnly
    calls while buildPackage keeps craneSrc; a deps-.drv stability assertion in release.yml
    extending the existing cache-regression warning; a binary cache (cachix or LAN
    attic) pushed from release.yml and substituted by build-guest-binaries.sh''s local
    nix lane'
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: flake.nix:43-59 feeds the full tree as src to commonCraneArgs;
    all four buildDepsOnly calls (:93/:109/:125/:160) inherit it, so any file change
    invalidates every dep derivation; build-guest-binaries.sh:187-200 prefers nix
    locally (nix on PATH) and is invoked by every --ci-full --install (build.sh:875-895/:927);
    release.yml caches only the nix store keyed on flake.lock. This is the largest
    event-scoped compute sink in the loop — plausibly 10-30 min per cold event.'
  exit_criteria:
  - Two consecutive source-only commits (Cargo.lock unchanged) leave all four deps
    derivation paths byte-identical, verified by nix build --dry-run in CI
  - release.yml warns loudly when a deps .drv changes while Cargo.lock is unchanged
    (extension of the :181-183 regression warning)
  - An over-narrow source filter fails LOUD at buildDepsOnly (missing manifest = evaluation
    error), never silently — confirmed by deliberately dropping a crate manifest in
    a test evaluation
  - A second host (or a clean checkout) substitutes dependency artifacts from the
    cache instead of cold-compiling ~1,000 crates for two musl targets
  - 'Measured: --ci-full --install guest-binary preparation after an ordinary commit
    no longer cold-rebuilds dependency derivations'
- packet_id: thin-lto-dev-install-lane
  order: 765-rm6a
  status: ready
  desired_release: v0.5
  priority: p3
  kind: build-velocity+build-hygiene
  title: Thin-LTO dev-install lane for cycle-end installs
  deliverable: A dev-install profile or CARGO_PROFILE_RELEASE_LTO=thin export in the
    meta-orchestration cycle-end install invocation only; release.yml, tagged releases,
    and flake.nix stay on fat LTO
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: Cargo.toml:105-108 (lto=true, codegen-units=1, strip=true)
    applies to every local install; the in-forge cycle contract mandates a cycle-end
    install; warm install measured 1m04s (plan/loop_status.d/20260812t235600z-32de7f7f-linux-mutable.md),
    dominated by the single-threaded fat-LTO link. Gated p3 by the PSK same-lane verification,
    which must land first.'
  exit_criteria:
  - Cycle-end TILLANDSIAS_SKIP_VERSION_BUMP=1 ./build.sh --install wall clock drops
    measurably (baseline 1m04s warm, fat-LTO-link dominated), recorded via the timing
    records
  - 'The secure-channel PSK derivation (runtime hash of the running binary) is verified
    compatible: guest and host binaries used within one cycle are proven to come from
    the same build lane before enabling, with a loud refusal when lanes are mixed'
  - Any litmus asserting binary size or byte-equality with the nix artifact is identified
    up front and either passes or the packet is rescoped — no silent divergence
  - Tagged/release artifacts are byte-for-byte governed by the unchanged fat-LTO lanes
- packet_id: worktree-shared-cache-and-target-gc
  order: 765-eqq3
  status: ready
  desired_release: v0.5
  priority: p3
  kind: build-velocity+infra
  title: Worktree-agent build cache and bounded target GC
  deliverable: sccache installed with a capped local disk cache, RUSTC_WRAPPER=sccache
    + CARGO_INCREMENTAL=0 exported in the worktree-agent entry path ONLY (never the
    main checkout's hot loop); a periodic bounded sweep (cargo-sweep --time 14 or
    equivalent) of the 74GB target/ that never runs concurrently with a build
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  - gates
  - telemetry
  context: 'Filed from plan/issues/build-velocity-audit-2026-08-15.md (operator velocity
    directive 2026-08-15; fail-loud constraints F11/F12 of the report bind every selector/skip
    proposal). Draft evidence: sccache not installed, no RUSTC_WRAPPER, no shared
    target-dir (.cargo/config.toml verified); target/ = 74GB; the live agent worktree
    carries its own separate 281MB partial target — zero artifact sharing, so each
    spawned worktree pays a cold dep build. Disk headroom is 261GB today, making this
    hygiene (p3) rather than emergency.'
  exit_criteria:
  - A fresh .claude/worktrees agent's first cargo check hits the shared compile cache
    for dependency crates instead of cold-building (measured before/after)
  - The main checkout's loop env is proven free of RUSTC_WRAPPER (incremental compilation
    wins there; sccache cannot cache incremental units) — enforced by an assertion
    in the loop's env setup, not by memory
  - target/ size is bounded (14-day artifact age) with the sweep serialized against
    builds via an explicit lock, and a loud log line per sweep naming bytes reclaimed
  - A corrupt cache entry fails the compile loudly (rustc error) — documented failure
    mode, no silent fallback
- packet_id: build-check-quick-win-wave
  order: 765-uti9
  status: ready
  desired_release: v0.5
  priority: p1
  kind: optimization
  title: Apply the seven endorsed quick wins from the 2026-08-15 build-velocity audit
    (~40s/cycle combined)
  deliverable: 'each quick win from the report appendix applied as its own verified
    change: drop redundant cargo check --workspace; skip podman/dev-proxy preamble
    on check-only dispatches; byte-stable sidecar restage; macos-tray git-index fingerprint
    fix; content-conditional bump-version rewrites; local-ci clippy lane aligned to
    --all-targets; build-preamble timing record'
  pickup_role: linux
  release_target: convergence-velocity-milestone
  capability_tags:
  - build
  - velocity
  estimated_hours: 3
  context: The machine-readable list with exact edits and per-item risk lives in the
    appendix of plan/issues/build-velocity-audit-2026-08-15.md. Items are independent;
    apply one at a time with ./build.sh --check green between items — the 2026-08-16
    with-tillandsias-builder RETURN-trap incident is the cautionary tale for batching
    build-system edits.
  exit_criteria:
  - each applied win keeps ./build.sh --check green and its stated coverage-neutrality
    argument holds under review
  - 'timing: line shows a lower build-check average over the following cycles, cited
    in the closure evidence'
  - any win found unsafe is dropped with a reasoned-declined note rather than forced
