# Ledger fragment — append-only, IMMUTABLE once written.
events:
  - packet_id: floor-tier-release-smoke-and-metrics
    event:
      type: note
      ts: "2026-09-06T06:48:25Z"
      host: macuahuitl
      summary: |-
        Closed on pirria's report; criteria met. AUTHORISED destructive floor
        smoke ran on pirria against published v56.9.5.1: install_exit=0 with
        version asserted, reset_exit=0 (containers/volumes/images empty),
        init_exit=0 from pristine, opencode_exit=0 with the forge lane at
        2h42m00s, health_rc=0 with vault sealed:false and proxy alive. Release
        v56.9.5.1 RECORDED AS TESTED. 1026-ps4n criterion 3 is discharged the
        OTHER way, by a clean run with a real record: the stamp was written with
        a live writer pid, survived 2h42m, timing_commit emitted and cleared it,
        and the §0 reap was correctly silent because no orphan existed.

        THREE THINGS THIS CLOSE DOES NOT SAY, at pirria's explicit request and
        because each would be an upgrade of the evidence. The supervisor-kill
        path is NOT verified — it remains unobserved in the field; the third
        harness kill took pirria's background waiter while the detached runner
        survived, which confirms setsid nohup and discharges nothing. There were
        TWO timing records, not five, and five was never reachable: the runbook
        instruments only §4 and §4c, so §1-§3 emit nothing and the three
        "missing" records were never achievable — an instrumentation packet
        against §1-§3 is the honest follow-up. And the 2h42m lane is NOT a
        regression against the 71m30s baseline: the in-forge agent did strictly
        more work, so the comparison is not like-for-like and no per-step delta
        was isolated.

        THE OPERATOR AUTHORISATION COVERED ONE RUN AND IS DISCHARGED. It is not
        standing; a further smoke needs fresh operator authorisation (1004-vsh2).
        Findings from the run are filed as separate ready packets rather than
        folded into this close, since each is claimable by a different host:
        1096-p3tn, 1097-mw6d, 1098-q7bk, 1099-t4xr.
