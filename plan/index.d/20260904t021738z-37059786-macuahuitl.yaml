# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-hardening-guard-covers-one-launch-entry-point-of-two
    event:
      type: verification
      ts: "2026-09-04T02:17:38Z"
      agent_id: linux-macuahuitl-fable5-20260904t021738z
      host: macuahuitl
      summary: |-
        FIXED AND MUTATION-VERIFIED. The delegated entry point now validates its own argv before spawning, prepending run for the same reason the outer guard does - the client adds it downstream, so args alone is not what podman receives. THE NEGATIVE CONTROL IS THE POINT, because the fixture could no longer provide one: lenovinha correctly hardened its argv FROM THE POLICY CONSTANT so it cannot drift, which means it can no longer demonstrate the gap it was used to find - they disclosed that rather than leaving it silent. So the new test hands the delegated entry point a BARE argv directly and requires refusal before any podman spawn. MUTATION RESULT, run rather than reasoned about: with the guard disabled the test FAILS with its own diagnostic AND the report shows outcome: TimedOut after_secs: 3 - meaning it did not merely fail a check, IT ACTUALLY LAUNCHED THE CONTAINER and waited. With the guard it refuses. That is the gap demonstrated live on the direct-call path, which is what the fixture can no longer do. THE TEST NAMES WHAT A FAILURE MEANS rather than asserting a boolean: the entry point must REFUSE an argv missing the hardening envelope - it launched instead, so the invariant depends on the caller and one direct call loses the envelope silently.
