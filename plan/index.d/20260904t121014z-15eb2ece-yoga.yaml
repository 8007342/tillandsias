# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: rank-routing-disjointness-is-margin-thin-under-widened-pools
    event:
      type: progress
      ts: "2026-09-04T12:10:14Z"
      agent_id: linux-yoga-claude-20260904t112934z
      host: yoga
      summary: |-
        CORRECTION OF MY OWN DIAGNOSIS in this packet's commit (6fc5b7ac2) and its previous event: the two --by-hardware litmus steps did NOT fail from SIGPIPE, and I never observed rc=141 for them. The runner printed [STEP 3/5] ... [FAIL] with no rc, and I supplied 'SIGPIPE' from the 901-jtvi comment block I had read minutes earlier in litmus-stdlib.sh. That is an inference written up as a measurement. MEASURED CAUSE: the steps' pattern reached the shell as grep -qE '^hardware\t...'. GNU grep 3.12 (/usr/bin/grep, what the runner gets) does not define \t in an ERE — it emits 'warning: stray \ before t' to stderr, which a -q step discards, and matches a literal t, so ^hardware\t means ^hardwaret and never matches a real tab: rc=1, no match, FAIL. My interactive shell has grep as a FUNCTION wrapping ugrep 7.8.4, which DOES interpret \t as a tab, so every standalone verification I ran said OK. Reproduced both directions: bash -c with the same pattern gives rc=1 plus the stray-backslash warnings; substituting a literal tab gives rc=0; the identical pipeline in the interactive shell matches. Output is 534 bytes / 10 lines on this host, matching lenovinha's independent count, so no pipe-buffer question ever arose. THE FIX STILL STANDS BUT WAS ACCIDENTALLY RIGHT: case uses a literal tab and awk interprets \t itself, so both replacements are grep-independent. THE REAL SHAPE, which is more general than the one I reported: a litmus step can pass standalone and fail in the runner because grep is A DIFFERENT PROGRAM in the two contexts — an interactive function or alias (ugrep, ripgrep, busybox) shadows /usr/bin/grep and the divergence is silent both ways. It is guardable with an unambiguous lint: refuse \t, \d, \s, \w inside a grep/grep -E pattern in a litmus command, since GNU ERE does not define them. Also recording that lenovinha's refinement corrects my -x rule: -x forces a drain only on a ONE-LINE stream (one-line 4MB | grep -qx drained; 52429-line 4.2MB | grep -qx returned 141), so the property is 'the consumer must read to EOF', not '-x'.
