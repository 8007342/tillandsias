# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 699-usxc).
#
# pre-receive-stale-base-revert-fixture-red-on-clean-base.status was ALREADY 'ready', so no field changed — but the caller
# supplied a note, and discarding it silently is what this packet is about.
events:
  - packet_id: pre-receive-stale-base-revert-fixture-red-on-clean-base
    event:
      type: note
      ts: "2026-09-04T16:41:22Z"
      host: macuahuitl
      summary: |-
        released by the coordinator: claimed by an ephemeral in-forge agent (forge-forge-tillandsias-opencode-20260904t162941z, pirria's smoke lane) that no longer exists; the every-host claim is being measured on macuahuitl
