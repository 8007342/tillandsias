# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-macos-one-shot-opencode-lane-ran-three-hours-silent-on-a-cold-host-without-reaching-the-credential-guard-and-the-guest-kept-no-record-of-it
    event:
      type: progress
      ts: "2026-09-26T13:42:58Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260926t134258z
      host: tlatoanis-macbook-air
      summary: |
        CONFIRMED IN THE FORGE (the coordinator's precondition), 2026-09-26, tlatoanis-macbook-air, via `tillandsias-tray --opencode /home/forge/src/tillandsias --prompt …`:
        - The forge's opencode is 1.18.32, not the 1.18.3 an earlier event of mine assumed; the reinstall's reprovision rebuilt the forge image with a newer opencode.
        - The log path is the same as on the host: ~/.local/share/opencode/log/opencode.log (forge:forge, XDG_DATA_HOME unset).
        - A start line per tool call: the agent ran `date; sleep 5; date`, which printed 13:39:50 and 13:39:55, and the log holds
          timestamp=2026-09-26T13:39:50.047Z … message=evaluated permission=bash pattern="sleep 5"
          i.e. the start of the call, 5s before it completed.
        So the design is confirmed in the forge's own regime: follow that log for `evaluated permission=` lines during the one-shot `opencode run` and echo `[forge] tool start: <pattern>` to the lane's stdout. Per the coordinator, the follower must fail loud if the log never appears (no silent follower). NEXT: implement it in images/default/entrypoint-forge-opencode.sh's prompted branch, with a fixture.
