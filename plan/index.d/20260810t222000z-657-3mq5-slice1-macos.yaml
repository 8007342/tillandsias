# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

events:
  - packet_id: aarch64-guest-cpu-inference-tuning
    event:
      type: progress
      ts: "2026-08-10T22:20:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Slice 1 SMASHED the prediction band: guest /proc/cpuinfo shows
        asimddp + i8mm + FULL SME/SME2 (M4-class; no SVE — precisely the
        combo the prebuilt linux-arm64 variants cannot use). Native
        in-guest build (GGML_NATIVE=ON; detected +dotprod+i8mm+sme2p1+...)
        compiled in 65s and benches qwen2.5-0.5b Q4_0 at pp512=1194.5 t/s,
        tg128=190.0 tok/s on 4 threads — ~3.65x the order-401 cpu-ollama
        52 tok/s decode baseline (caveat: llama-bench raw vs ollama HTTP
        path; the pin-bench comparison is a remaining criterion). Exit
        criteria 1 evidenced, 2 evidenced-with-caveat; recipe recorded in
        the 393 decision doc. Remaining: quant A/B (Q4_K_M/Q8_0), ollama
        version pin-bench, ground-truth quality spot-check. The build and
        GGUF persist on the guest rootfs at /root/llama.cpp and
        /root/qwen05-q4_0.gguf for the next slice.
