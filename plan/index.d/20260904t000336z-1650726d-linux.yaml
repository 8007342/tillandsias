# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-login-poll-gates-on-token-presence-and-never-on-validity
    field: status
    value: verified
    ts: "2026-09-04T00:03:36Z"
    host: linux

events:
  - packet_id: the-login-poll-gates-on-token-presence-and-never-on-validity
    event:
      type: completed
      ts: "2026-09-04T00:03:36Z"
      host: linux
      summary: |-
        status 'verified' with evidence_refs: 7007d2172: build.sh --check green (208s); 652/652 tray+vsock tests; credential-verdict fixture 7/7 wired into --check; four negative controls red without the fix. Criterion 1 (live token revocation) NOT performed - see events.
