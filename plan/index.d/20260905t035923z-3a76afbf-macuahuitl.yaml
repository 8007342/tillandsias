# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: floor-tier-release-smoke-and-metrics
    event:
      type: progress
      ts: "2026-09-05T03:59:23Z"
      agent_id: linux-macuahuitl-claude-20260905t035924z
      host: macuahuitl
      summary: |
        VALIDATION LANE 3, Windows floor (esmeraldinha, 03:5xZ), PASS with findings on the published tag; the lane that closes the tested-release record for v56.9.5.1: §1 curl-install PASS install_exit=0 sha256 7305c983…b515; §1 version == tag PASS tillandsias-tray 56.9.5.1 (b528680d2); §2 unregister + purge PASS with tillandsias-build preserved; §2 host vault credentials cleared PASS, decisively this round (present beforehand, deleted, whole-store read confirms both gone, vm-uuid preserved); §3 cold provision PASS provision_exit=0 in 129 s (117 s for v56.9.2.1 on the same host; the v56.9.4.1 round could not produce this number because 1043-c73v killed it partway); §3 fresh rootfs not a survivor PASS; §3 wire Ready at exit PASS phase=Ready podman_ready=True wire_version=3; §3 final --diagnose PASS version=56.9.5.1 guest_version=56.9.5.1 distro_running=true; §4/§4b not applicable on Windows. Two FAIL rows, both known: §0.2b ledger row absent on windows-next/main (1045-8yyw persists, read from linux-next); §2 runbook's credential predicate reports both credentials present after a verified delete (804-ckst, third confirmation). Not checked, named: 1032-utne (no health, age or verdict key in --diagnose --json; a tray GUI line the lane cannot click), 1043-kvvn, 1031-q4pb, 873-vgyg, 972-umik. Report file: plan/issues/smoke-e2e-findings-v56.9.5.1-2026-09-05-esmeraldinha.md, relayed because the host's gate is red pending 6169b531d reaching linux-next. With this lane the tag is validated on Linux §1 (macuahuitl), macOS (macbookair, curl-install skill, /Applications branch) and the Windows floor full round.
