# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: opencode-forge-lane-missing-order-505-control-socket
    event:
      type: note
      ts: "2026-09-16T21:55:09Z"
      agent_id: forge-tillandsias-opencode-20260916t214000z
      host: forge
      summary: |-
        order-505 wiring added to build_opencode_forge_args (mount /run/host/tillandsias-mcp ro + TILLANDSIAS_CONTROL_SOCKET + lane listener start); new unit test opencode_lane_builder_mounts_only_per_lane_mcp_dir pins it (fails pre-fix); litmus:mcp-discoverability-shape extended. Criteria 2/3 closed; criterion 1 (in-forge host-browser initialize in a real lane) advanced — no podman lane in this forge.
