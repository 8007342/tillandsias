# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: forge-first-launch-tax
    event:
      type: note
      ts: "2026-09-01T20:00:00Z"
      host: macuahuitl
      summary: >
        SECOND INSTRUMENT ONLINE — pirria (N150/CachyOS/NATIVE podman,
        forge at stable ad4a84773) completes the bracket's third corner:
        first-launch expert build 233s TWO-INSTRUMENT-VERIFIED (cargo
        self-report 3m53s vs wall-clock state-mtime delta 234s) under a
        COLD crates.io registry (244 crates downloaded) AND a concurrent
        199.3MiB antigravity vendor extraction. Identical headline to
        esmeraldinha's N100/WSL2 warm-registry idle 233s — pirria matched
        while paying two costs she did not, evidence that native podman
        (plus N150>N100, confound named) is materially faster than WSL2 on
        this workload. All three bracket corners now measured across two
        hosts. NEW RECURRING TAX filed here: the forge's global post-commit
        hook fires `timeout 600 cargo build --release -p tillandsias-plan`
        on ANY commit touching the plan crate — a 233s-class job per
        qualifying commit on floor hardware; the prebake fix should feed
        this hook's fast path too. Gate discipline held: pirria read
        /dev/shm/tillandsias-experts/state=ready before running anything,
        and deferred litmus while their own evaluation workflow held 2 of
        4 cores (self-subtraction applied prospectively).

  - packet_id: forge-guest-hygiene
    event:
      type: note
      ts: "2026-09-01T20:00:00Z"
      host: macuahuitl
      summary: >
        Pirria corroborations on a second host OS: (1) boot-time global
        hooks dir matches esmeraldinha's launch forensics exactly — two
        hooks only (post-commit expert-refresh, prepare-commit-msg
        trailer), both benign in foreign repos, checksums banked as a
        PRE-litmus baseline for the cleanest possible post-litmus
        contamination diff (incoming). (2) DISCORDANT PAIR for the
        gitconfig ordering race: pirria's native clone WON the race
        (healthy mirror insteadOf redirect, no FAULT comment) where
        esmeraldinha's WSL2 clone latched the permanent FAULT — consistent
        with the pre-clone-transient hypothesis, now evidenced by a
        concordant/discordant pair instead of one case. (3) 805-yzhw
        antigravity-growth claim RETRACTED BY ITS AUTHOR before filing:
        code-first read shows the 199.3MiB payload stages in
        $HOME/.cache/antigravity/staging on the overlay and NEVER touches
        /tmp — only the 7KB installer script uses mktemp. Sound
        arithmetic, false premise. WHAT THE SAME READ FOUND INSTEAD:
        lib-common require_antigravity passes ANTIGRAVITY_BIN=/usr/local/
        bin/agy, an env var the vendor installer contains ZERO references
        to (it honors only --dir, defaults ~/.local/bin) — the binary
        lands on the OVERLAY, dies with every container, and the forge
        re-downloads and re-extracts 199.3MiB PER FRESH LAUNCH, concurrent
        with the expert build. That is the structural cause of the
        measured first-launch contention, a recurring sibling of the
        953-fflt tax. The fix needs a placement DECISION (image-bake or
        persistent tool-cache — /usr/local/bin is also overlay), not just
        the one-token --dir; retry loop worst case adds 276s
        (3x curl --max-time 90 + backoff). (4) Guest provenance gap second
        confirmation: no tillandsias binary on PATH in the guest.

  - packet_id: litmus-low-end-instrument
    event:
      type: note
      ts: "2026-09-01T20:00:00Z"
      host: macuahuitl
      summary: >
        host-browser MCP CONNECTION_CLOSED is now TWO-HOST (esmeraldinha
        WSL2 + pirria native CachyOS, both fresh stable-installed guests)
        — the precondition-hole arm's leaked litmus executes-and-fails on
        an absence two independent harnesses confirm at session start.
        Also noted for the onboarding doc: an empty /opt/tillandsias/
        spec-index on a fresh install is the 712-r5x8 typed refusal
        (no-embed-endpoint), not breakage — agents that skip the boot log
        misdiagnose it.
