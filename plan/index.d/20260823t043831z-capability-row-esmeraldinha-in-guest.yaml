# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by windows (esmeraldinha), 2026-08-23T04:38:31Z.
#
# The IN-GUEST half of esmeraldinha's capability row (Intel N100, the fleet's
# declared lower bound, silent in the matrix until this cycle). Verbatim
# `tillandsias --capabilities` output, status-preamble line stripped, from a
# binary built THIS CYCLE from this checkout at windows-next fb80c579a
# (= origin/linux-next). The windows-host half is contributed separately with
# locus: windows-host; the two are keyed apart and a consumer assembles them
# (808-7yrd, 809-7e4m).
#
# WHICH GUEST, stated explicitly because the host+locus fold key cannot say it
# and yolanda's 851-cduu note asked that someone decide: probed in the RUNTIME
# `tillandsias` distro, not `tillandsias-build`. That is the product's own
# distro and the one carrying ollama 0.32.14 + podman, so it is the guest a
# scheduler would actually place work in. yolanda's sibling row was taken in
# tillandsias-build, which has no ollama — the two machines' `engines` differ
# for that reason alone, not for any hardware reason. Recommendation to
# 850-bif2's row-on-join design: make the product distro canonical, or add the
# distro to the fold key.
#
# The probe binary had to be COPIED into this distro: the runtime distro sets
# `[automount] enabled = false` in /etc/wsl.conf (deliberately — see
# scripts/with-wsl2-builder.sh), so the checkout at /mnt/c is not visible here
# and no in-tree script can be run in place. Reached via
# \wsl.localhost\tillandsias\root\.
#
# Notable content: engines is non-empty (ollama/llama-server) and the CPU
# carries lanes [container, host-native], so this is the first WSL2 in-guest
# row in the matrix that is actually schedulable. The gpu row is
# present-unusable/wsl2-no-dri-render-node — confirmed structurally, and the
# Intel half of the vendor-universality question
# (plan/issues/research-wsl2-accel-limits-vendor-universality-2026-08-23.md).
#
# Read it with: tillandsias-plan capability-matrix
capabilities:
  - ts: "2026-08-23T04:38:31Z"
    host: windows
    locus: in-guest
    document:
      {
        "schema_version": 2,
        "legacy_tier": "cpu",
        "devices": [
          {
            "device_class": "cpu",
            "vendor": "intel",
            "name": "Intel(R) N100",
            "device_node": null,
            "fw_version": null,
            "driver": null,
            "usable": true,
            "unusable_reason": null,
            "lanes": [
              "container",
              "host-native"
            ],
            "memory_bandwidth_gbps": null,
            "memory_bandwidth_source": "unknown",
            "cpu_flags": [
              "fma",
              "avx",
              "avx2",
              "avx_vnni"
            ],
            "cpu_cores": {
              "physical": 4,
              "logical": 4
            },
            "system_ram_gb": 7.756450653076172
          },
          {
            "device_class": "gpu",
            "vendor": "unknown",
            "name": "WSL2 paravirtual GPU (/dev/dxg)",
            "device_node": "/dev/dxg",
            "fw_version": null,
            "driver": null,
            "usable": false,
            "unusable_reason": "wsl2-no-dri-render-node",
            "lanes": [],
            "memory_bandwidth_gbps": null,
            "memory_bandwidth_source": "unknown",
            "cpu_flags": null,
            "cpu_cores": null,
            "system_ram_gb": null
          }
        ],
        "engines": [
          {
            "name": "ollama",
            "backend": "llama-server",
            "supported_device_classes": [
              "cpu",
              "gpu"
            ]
          }
        ],
        "measurements": [],
        "host": {
          "is_battery_present": true,
          "kernel_release": "6.18.33.2-microsoft-standard-WSL2",
          "host_id": "esmeraldinha",
          "host_id_source": "node-name",
          "host_kind": "linux"
        },
        "timestamp": "2026-08-23T04:38:07.184270456+00:00"
      }
