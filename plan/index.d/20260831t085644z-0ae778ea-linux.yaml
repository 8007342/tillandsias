# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: network-architecture-audit
    field: next_action
    value: 'P4 IS DONE and this field previously said it was the one open item — verify before implementing. The platform transport matrix is normative in openspec/specs/vsock-transport/spec.md (added 2026-08-30 from 245 §5 P4). Sections 2, 4 and 7 are now symbol-anchored (§2 by L5 2026-08-31; §4 and §7 held one citation each, §7''s already under cite-ok). REMAINING, in order: (1) 39 line citations survive OUTSIDE §2/§4/§7 — the same 881-29me treatment, and L1/L2/L3/L5 are the worked pattern; (2) §2''s operation table was RE-ANCHORED BUT NOT RE-VERIFIED — the image-count claims (10 images for --init, EIGHT for status check, 5-image opencode ensure) were not checked against the actual arrays, and that is now cheap because the symbols resolve; (3) P1 still needs the git-mirror-egress verdict (plan/issues/git-mirror-egress-spec-divergence-audit-2026-08-10.md) and P6 still needs the :3129 wire-or-delete decision — both OPERATOR calls, not worker items. CLOSURE: multi_cycle with verification_required, phase review — three named agents owe verified-by events; no worker may flip this to completed.'
    ts: "2026-08-31T08:56:44Z"
    host: linux

events:
  - packet_id: network-architecture-audit
    event:
      type: note
      ts: "2026-08-31T08:56:44Z"
      host: linux
      summary: >
        P4 confirmed already landed; §2 re-anchored this cycle (L5); remaining work re-scoped
