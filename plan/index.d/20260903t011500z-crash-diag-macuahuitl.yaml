# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: builder-container-serial-killer
    event:
      type: note
      ts: "2026-09-03T01:15:00Z"
      host: macuahuitl
      summary: >
        CROSS-HOST CRASH DIAGNOSIS (lenovinha + macuahuitl, operator-asked
        'are these the same incident'): ANSWER — NOT the same, and
        macuahuitl's 'crashes' are not crashes. Two-instrument split:
        (1) coredumpctl on macuahuitl is ALL DELIBERATE FIXTURES — every
        recent bash SIGSEGV is test-harness-supervisor.sh's `kill -SEGV
        $$` with its launchesN marker, the squid SIGABRT is
        test-squid-supervisor.sh; ZERO real tillandsias/rustc/opencode
        tray dumps (the 4 non-launches hits are old in-forge fixture
        SIGABRTs from 2026-08-28, not host crashes). lenovinha confirmed
        the identical fixtures on its side (~120 bash SIGSEGV in 5-bursts)
        and flagged them as noise-to-exclude FIRST — correct, they would
        dominate any false correlation. (2) The REAL macuahuitl symptom
        is IO THRASHING, not a crash: /proc/pressure/io full avg300 hit
        ~50% under unthrottled ci-full on the 86%-full budget NVMe,
        dropping to 0.00 idle and <2% after nice-19+ionice-3 throttling
        (memory saved: builds-throttled-on-operator-desktop). lenovinha's
        WD_BLACK SN7100 holds io/memory PSI at 0.00 even under build with
        zero kernel I/O errors — the fast NVMe hides what the budget one
        surfaces, exactly the operator's read. Incidents unrelated;
        macuahuitl's fix is throttle+scheduling, not a crash hunt.
        CROSS-LINK for 667-se87 (lenovinha): that packet's pids-exhaustion
        mechanism was never proven (pids.peak read 199, below the 512
        ceiling it blamed) — and 959-fpc5's two-host measurement now shows
        builds reach only 3.5-6.6% of the ceiling, so the rustc
        jobserver/lost-codegen panics 667-se87 attributed to pids are
        ALSO consistent with severe IO/memory stall under parallel build
        — the same thrashing class. Recorded as a live alternative
        explanation on 667-se87's coredump branch (host-bound: the 94.1M
        dump is macuahuitl-side).
