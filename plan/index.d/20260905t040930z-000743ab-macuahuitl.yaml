# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: memoise-the-terminology-dictionary-check-on-a-digest-of-its-inputs
    field: status
    value: in_progress
    ts: "2026-09-05T04:09:30Z"
    host: macuahuitl

events:
  - packet_id: memoise-the-terminology-dictionary-check-on-a-digest-of-its-inputs
    event:
      type: note
      ts: "2026-09-05T04:09:30Z"
      host: macuahuitl
      summary: |-
        claimed by the coordinator on pirria's behalf: pirria's gate runs in the toolbox now and it asked for this packet, but it cannot push a claim while its credential channel is missing; its work relays through the coordinator until the grant reset
