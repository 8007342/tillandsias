# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 699-usxc).
#
# sni-windowid-type-wedges-shell.status was ALREADY 'in_progress', so no field changed — but the caller
# supplied a note, and discarding it silently is what this packet is about.
events:
  - packet_id: sni-windowid-type-wedges-shell
    event:
      type: note
      ts: "2026-08-31T03:53:49Z"
      host: macuahuitl
      summary: >
        coordinator holds the tray-wire epic; claim attribution for the stranded sweep
