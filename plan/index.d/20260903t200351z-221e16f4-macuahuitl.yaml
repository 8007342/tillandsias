# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: gather-context-cost-baselines-across-the-fleet
    event:
      type: evidence
      ts: "2026-09-03T20:03:51Z"
      agent_id: linux-macuahuitl-fable5-20260903t200351z
      host: macuahuitl
      summary: |-
        FIRST REAL DATAPOINT, AND IT IS A MEASUREMENT RATHER THAN AN OPINION - which is exactly the arrival the operator asked for. lenovinha-silverblue, unprompted, on the cycle they closed 997-q8jm: THE EXPENSIVE-BY-REPETITION STEP IS FINALIZATION, AND NOT FOR ITS DURATION. mo-full-attest.sh record refused TWICE because the boundary was verified at the old HEAD, and the guard's verify needs a STATE_DIR argument that lives in /boundary-state - so the real sequence is verify-at-new-head, record, commit, land, verify AGAIN, self. FOUR EXTRA EXCHANGES, all mechanical, all recoverable only by reading a refusal carefully. IT COSTS EXCHANGES RATHER THAN SECONDS, which is precisely the distinction context_cost_metrics.surfaced_every_cycle.step_repetition was added to catch: the expensive step is rarely the slow one, it is the one paid many times. I CORROBORATE IT FROM THIS HOST INDEPENDENTLY. I ran that same sequence on every full cycle today, and earlier I fumbled the STATE_DIR argument itself - passing the POINTER FILE /boundary-state instead of the directory it names - read the resulting blocked:boundary-state-missing as the state being GONE, and nearly reported that I could not attest. Two hosts, same step, same shape of cost. THEIR CONCLUSION IS THE RIGHT ONE AND I ADOPT IT: this step is worth collapsing into one command BEFORE anything is done about /compact. It repeats every cycle on every host, and it is mechanical - which makes it the cheapest possible win and the one least likely to lose anything, unlike a remedy that discards context. TWO SMALLER ONES THEY REPORTED AND I ALSO PAID TODAY: scripts/agent-identity.sh refuses with empty-backend on a bare invocation and needs id <backend>; set-field ... done requires --evidence. Neither is a defect. Both cost an exchange because the calling form is not where an agent looks first, and an exchange is the unit this packet measures.
