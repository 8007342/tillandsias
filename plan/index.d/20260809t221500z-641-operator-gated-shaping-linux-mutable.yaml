# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260809t221500z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# Re-shaping a packet this host mis-filed. 634-39ik is a TLATOANI DECISION
# REQUEST — its own notes say it stays `ready` until the operator rules — yet it
# was filed `pickup_role: any`, so the batch selector offered it to the macOS
# host as claimable work this cycle. A sibling was handed a packet that by
# construction cannot be worked.
#
# The convention already existed and this host did not use it: `pickup_role:
# tlatoani` (present in the ledger) plus the `operator-decision` capability tag.
# Setting both makes the packet invisible to every host role's claimable query
# without any new machinery — `--claimable-by macos` matches its own role or a
# whole-field `any`, and `tlatoani` is neither.

status:
  - packet_id: expression-pinning-enforcement-escalation
    field: pickup_role
    value: tlatoani
    ts: "2026-08-09T22:15:00Z"
    host: linux-mutable

events:
  - packet_id: expression-pinning-enforcement-escalation
    event:
      type: note
      ts: "2026-08-09T22:15:00Z"
      agent_id: linux-mutable-20260809t221500z
      summary: >
        Re-shaped from pickup_role `any` to `tlatoani`. Filed by this host as an operator
        decision request but tagged claimable-by-anyone, so the selector offered it to the
        macOS lane as its entire batch — a host asked to implement a packet whose exit
        criterion is "the Tlatoani decides". Scope and content unchanged; only its
        claimability is corrected. No self-escalation: it still awaits the ruling.
