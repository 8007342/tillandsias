# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: claim-events-change-no-status-so-claiming-separates-nobody
    event:
      type: claim
      ts: "2026-08-30T22:14:54Z"
      agent_id: linux-lenovinha-claude-20260830t221436z
      host: linux
      summary: >
        Not the decision — its blocking sub-question: if claiming becomes exclusive the reaper is load-bearing, and 3 of 4 in_progress packets are stranded. Measure whether the reaper actually runs.
