# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-25T01:00:59Z.
events:
  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    event:
      type: progress
      ts: "2026-08-25T01:00:59Z"
      agent_id: linux-macuahuitl-opus5-20260825T005900Z
      host: linux_mutable
      next_action: >
        FLEET STATE 2026-08-25T01:00Z, OPERATOR RE-ARM: yoga every 2h, pirria
        every 6h, both on the PLAIN meta-orchestration prompt with a
        fast-forward-to-origin first step. macuahuitl continues hourly (may
        drop to 30m). lenovinha's standing loop unchanged. esmeraldinha,
        yolanda, the macbook remain down.

        ALL THREE RESUME GATES ARE ON origin/linux-next and both re-armed
        hosts fast-forward before working, so their first cycles inherit:
        cycle-checkout-lock.sh + the skill's 2b/9b steps (873-zcim),
        salvage-before-refuse (874-s8vf, exemption-first hook ordering), and
        the push-verified credential guard (860-g798). Verified present at
        origin before recording this.

        FIRST-CYCLE WATCH LIST for the coordinator's next heartbeats:
        (1) yoga is ALREADY mid-cycle (claimed
            guest-exec-allowlist-forces-argv-flattening 00:47Z, proper LWW
            channel, canonical host vocabulary). Expect its attestation within
            the cycle; if yoga's driver timer is ALSO still armed, the
            cross-arm lock arbitrates and a skip:overlap-lock-held in its
            local overlap-refusals.jsonl is the healthy outcome, not a stall.
        (2) pirria's next cycle should produce its FIRST-EVER attestation —
            the retrospective diagnosed its silence as a mode gap (a 36h
            interactive session with no Finalization block), and a plain
            prompt cycle contains Finalization. If pirria completes a cycle
            and pirria.md still does not exist, the mode-gap diagnosis is
            wrong and the record step itself needs the next look.
        (3) Neither host should trip blocked:interactive-credential-helper on
            Linux; if one does, read the printed remedy before diagnosing.

        NEXT for macuahuitl, in order: (1) 874-idnt write-time identity
        refusal — NOW TIME-SENSITIVE: siblings are writing ledger identity
        again and every write until it lands can reseed the mixed
        vocabularies. (2) 874-w2gc salvage sweep/retention (+ the
        overlap-refusals.jsonl consumer riding it). (3) 801-vm4p residue:
        LivenessProbe for the nix cache. (4) the stray ghost-trace notice in
        check-credential-channel.sh's header.

        STANDING COORDINATOR DUTY unchanged; platform branches ahead=0 at
        00:34Z.
      summary: >
        Operator re-armed yoga (2h) and pirria (6h) on plain prompts with
        fast-forward-first; recorded the watch list for their first cycles and
        promoted 874-idnt to time-sensitive.
