# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: inference-enclave-ca-not-trusted-by-engine
    field: next_action
    value: 'ALL THREE EXIT CRITERIA NOW MET, one of them differently than the 2026-07-29 event proposed. EC1/EC2 closed then (SSL_CERT_FILE+SSL_CERT_DIR exported, the three ''|| true''s collapsed into one guarded block reporting rc once, verified empirically as uid 1000). EC3 closed 2026-08-31: the engine classifies its own pull failures into pull-failed-tls-untrusted / -unreachable / -not-found / -other and says, on the trust case, that no-models will misreport it; the vocabulary comment and no-models detail string name ''podman logs tillandsias-inference | grep reason=pull-failed'' as the procedure. Fixture scripts/test-inference-pull-failure-classification.sh, 12 cases, in build.sh --check, mutation-tested both directions. WHY NOT A NEW VOCABULARY TOKEN, which the earlier event proposed: lib-inference-state.sh runs in the FORGE and probes plain HTTP; the trust failure is on the inference container''s OUTBOUND path, and no channel carries it across. A reason nothing can set is a vocabulary entry with no producer. IF SOMEONE WANTS THE PROBE ITSELF TO REPORT IT — needed only if an AGENT rather than an operator must branch on the distinction automatically — that is a new packet for a cross-container channel, not a change here. Mint an order rather than reopening this one. NOT VERIFIED ON A LIVE ENCLAVE: the classifier is pinned against recorded x509/ollama output shapes, not against a pull that actually failed through squid''s bump on this host; a host with a running enclave could confirm the real string matches, and lenovinha''s experts-probe reports l1=unreachable so it could not.'
    ts: "2026-08-31T22:59:35Z"
    host: linux

events:
  - packet_id: inference-enclave-ca-not-trusted-by-engine
    event:
      type: note
      ts: "2026-08-31T22:59:35Z"
      host: linux
      summary: >
        EC3 closed via engine-side classification; recording why the proposed vocabulary token was refused and what remains unverified
