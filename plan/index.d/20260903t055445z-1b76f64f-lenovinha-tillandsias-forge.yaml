# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: ledger-prose-never-passes-through-argv
    field: status
    value: completed
    ts: "2026-09-03T05:54:45Z"
    host: lenovinha-tillandsias-forge

events:
  - packet_id: ledger-prose-never-passes-through-argv
    event:
      type: completed
      ts: "2026-09-03T05:54:45Z"
      host: lenovinha-tillandsias-forge
      summary: |-
        status 'completed' with evidence_refs: 55465cb58; scripts/test-ledger-prose-roundtrip.sh 6/6 including a negative control, verified FAILING against the old folded writer; 284 plan lib tests pass; this very event round-tripped byte-identically through --summary-file
  - packet_id: ledger-prose-never-passes-through-argv
    event:
      type: note
      ts: "2026-09-03T05:54:45Z"
      host: lenovinha-tillandsias-forge
      summary: |-
        all three exit criteria met
