# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-linux-clean-room-reset-does-not-reach-the-host-keychain
    field: next_action
    value: implement (a) across THREE locations — keychain item, fallback_* file, host vault-data dir (self-completing via is_partial_init) — with criterion 3's both-directions fixture planting each and asserting cold only when all three are gone; clearing any two and calling it cold is the one-direction assertion that produced this row
    ts: "2026-09-13T11:40:23Z"
    host: yoga

events:
  - packet_id: the-linux-clean-room-reset-does-not-reach-the-host-keychain
    event:
      type: progress
      ts: "2026-09-13T11:40:23Z"
      host: yoga
      summary: |-
        status 'implement (a) across THREE locations — keychain item, fallback_* file, host vault-data dir (self-completing via is_partial_init) — with criterion 3's both-directions fixture planting each and asserting cold only when all three are gone; clearing any two and calling it cold is the one-direction assertion that produced this row' (not terminal) with evidence_refs: probe false-cold fixed and pinned 9/9; three locations established by 1149-vgn2 + the vault-data measurement
