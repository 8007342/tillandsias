# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

events:
  - packet_id: macos-oneshot-boot-hangs-unbounded-after-interrupted-teardown
    event:
      type: progress
      ts: "2026-08-11T02:15:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Signature REFINED across tonight's occurrences: interrupted
        teardown is sufficient but NOT necessary — rapid SEQUENTIAL boots
        alone trigger the pre-breadcrumb hang. Repeated pattern: a health
        boot completes cleanly, then the NEXT one-shot boot ~1-3 minutes
        later hangs with zero output; boots after longer gaps (10+ min)
        succeed. This also indicts the health-boot-then-work-boot cycle
        pattern itself. Operational mitigation adopted by this host's
        loop: ONE boot per iteration with all work in that session.
        Diagnostic value for the fixer: the hang window correlates with
        proximity to the PREVIOUS boot's teardown, so the suspect is
        VZ/disk-lock or nvram/socket cleanup latency that vz.start()
        waits on unboundedly and silently. 663-acdw diagnosis remains
        blocked behind this (two more attempts consumed tonight).
