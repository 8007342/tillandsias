# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-nvidia-cdi-currency-check-keys-on-the-driver-version-while-the-spec-pins-libraries-that-version-independently-so-it-reports-current-about-a-spec-whose-files-no-longer-exist-and-the-gpu-container-lane-dies
    field: next_action
    value: |-
      LEFT (2026-09-23): all four criteria open. ensure-nvidia-cdi.sh unchanged since 816d36050. Needs a Linux host with an NVIDIA GPU (capability: linux nvidia gpu).

      [2026-09-23T21:15:50Z yoga] RELEASED by yoga 2026-09-23 after criterion 1. CRITERION 1 IS DONE ON work/1248-j6vd AT dd0e511df, NOT LANDED (release-cut hold). nvidia-cdi-ensure.sh now treats a spec with a missing hostPath as stale and regenerates it. scripts/test-nvidia-cdi-stale-hostpath.sh gives 7/7, the pre-fix script fails 5 checks, and it is bound as a step of litmus:inference-gpu-device-args. Do NOT redo it; the coordinator relays it after the cut. LEFT are criterion 2 (a GPU container-start refusal that names the cause and the log path, not the bare blocked:container-start-failed) and criterion 3 (dev-inference readiness must tell an endpoint answered by a foreign host-native engine on the same port apart from a working container lane, which is litmus:inference-readiness-probe-shape). Criterion 3 needs a host with an NVIDIA card to verify live; macuahuitl is the one that measured it.

      [2026-09-23T22:39:06Z yoga] UPDATE 2026-09-23 (yoga): CRITERION 1 IS NOW ON TRUNK at ea6b7e398 (macuahuitl's relay, with work/1248-j6vd cd5b4c44d). The litmus:inference-gpu-device-args step expects ok:nvidia-cdi-stale-hostpath:8/8. LEFT are criterion 2 (a GPU container-start refusal naming the cause and the log path) and criterion 3 (readiness that tells a foreign engine on the port apart from a working container lane). Criterion 3 needs macuahuitl's NVIDIA host.
    ts: "2026-09-23T22:39:06Z"
    host: yoga

events:
  - packet_id: the-nvidia-cdi-currency-check-keys-on-the-driver-version-while-the-spec-pins-libraries-that-version-independently-so-it-reports-current-about-a-spec-whose-files-no-longer-exist-and-the-gpu-container-lane-dies
    event:
      type: note
      ts: "2026-09-23T22:39:06Z"
      host: yoga
      summary: |-
        criterion 1 landed
