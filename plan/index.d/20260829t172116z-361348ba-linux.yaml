# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-smallest-cited-synthesis-model-exceeds-the-quick-tier-budget
    field: status
    value: ready
    ts: "2026-08-29T17:21:16Z"
    host: linux

events:
  - packet_id: the-smallest-cited-synthesis-model-exceeds-the-quick-tier-budget
    event:
      type: note
      ts: "2026-08-29T17:21:16Z"
      host: linux
      summary: >
        returned to ready — decision recorded and the mechanism landed with the CPU-floor half measured; the slow-model half of verifiable_closure needs a host with a 7b, which lenovinha does not have
