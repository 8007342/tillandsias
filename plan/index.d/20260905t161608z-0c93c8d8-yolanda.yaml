# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-wsl-dispatch-drops-every-tillandsias-control-flag-at-the-boundary
    event:
      type: claim
      ts: "2026-09-05T16:16:08Z"
      agent_id: windows-yolanda-opus5-20260905t161608z
      host: yolanda
      summary: |
        claimed for cycle by yolanda. This is the host that filed it and the host the defect lands on, and I nearly walked into it again this week: the gate's own output prints "TILLANDSIAS_FORCE_CHECK=1 to re-run" on every ok:gate-fresh line I have read all day, and on this host that instruction is silently false.

        Taking the packet's own warning seriously before writing anything: do NOT hardcode a second flag into _ENV_PREFIX. That is precisely what made the current state look intentional -- one flag crosses, so the boundary appears handled. Reading scripts/with-tillandsias-builder.sh:232-235 first, because it already solved namespace forwarding once and a second bespoke solution is how the two boundaries diverged.

        Four criteria, and the third and fourth are the ones that make this more than a one-liner: a flag NOT set on the host must not appear set inside WSL (a forward must not MANUFACTURE values), and the two dispatches must share the logic or a check must prove they agree, so a third boundary cannot diverge silently the way this one did. I will build the demonstration as a forced run after an unforced one, measured on this host, rather than argued.
