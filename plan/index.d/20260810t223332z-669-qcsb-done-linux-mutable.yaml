# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t223332z
status:
  - packet_id: project-engine-grammar-litmus-is-ownerless
    field: status
    value: done
    ts: "2026-08-10T22:33:32Z"
    host: linux

events:
  - packet_id: project-engine-grammar-litmus-is-ownerless
    event:
      type: completed
      ts: "2026-08-10T22:33:32Z"
      agent_id: linux-mutable-20260810t223332z
      host: linux
      summary: >
        Exit criterion met in full: both 619-pfsj surfaces now have executable
        guards that EXECUTE (660-ryhn discipline — bound-but-never-run is
        silence; both were run through scripts/run-litmus-test.sh and shown
        passing, not merely bound). Two instant pre-build litmus tests, pure
        shell + fixtures, no containers, bound under
        forge-environment-discoverability (the spec that owns project-info /
        forge discovery — same home as litmus:expert-capability-skew-honesty,
        the launch-built sibling of this image-baked lane).

        litmus:project-engine-capability-verdict-grammar (12 steps): all FOUR
        project_engine verdict states executed from constructed fixtures with
        anchored FULL-LINE greps — matching engine+checkout -> ready with
        baked=expected; no-expectation checkout -> ready with expected=-
        visible and NO cry of skew; pure-superset checkout manifest -> skewed
        with missing_in_engine=brand-new-capability and the IMAGE REBUILD
        REQUIRED advice (the order-531 shape redefined for an image-baked
        artifact: no relaunch heals it); a pre-manifest engine stub printing
        JSON-RPC-era prose -> stale-engine (unknowable, not assumed); no
        engine -> absent. Plus: the `capabilities` self-report shape (bare
        tokens, exactly one engine-contract-vN, answers before the JSON-RPC
        loop), the SRC_REL/Containerfile drift pins, the CLOSED four-state
        vocabulary (assignment count asserted exactly, advice per state), the
        helper-missing probe=helper-missing fallback naming in lib-common.sh
        discovery, fail-soft under set -euo pipefail with hostile inputs, and
        no-python.

        litmus:project-answer-synthesis-refusal-typed (9 steps): the typed
        refusal executed through the REAL JSON-RPC loop against the real
        inference probe — dead endpoint (127.0.0.1:9) -> anchored
        `unsupported: synthesis question — missing_capability=local-inference
        inference_state=not-ready inference_reason=endpoint-unreachable;`
        with citations=[], confidence=unsupported, real freshness struct;
        stubbed-ready probe -> missing_capability=synthesis-tier
        inference_state=ready (the two branches pinned DISTINCT — blaming the
        endpoint when the endpoint is fine sends an agent to fix the wrong
        thing); every refusal accepted by `tillandsias-plan verify-answer`.
        Plan lane covered with stub engines: an unsupported deterministic
        verdict routes through the SAME pinned constructor (ledger-flavoured
        stub prose must NOT leak), a silent engine yields the pinned
        no-envelope refusal (the C1 hand-rolled-envelope regression, kept
        dead), and TWO negative controls — a deterministic question under the
        same dead-endpoint fixture still answers cited + verifier-clean, and
        a citable deterministic answer to a synthesis question is forwarded
        verbatim (deterministic first; inference adds recall, never
        authority). The manifest-declares-behaviour step bridges the two
        tests: the engine's synthesis-refusal-typed / no-inference-fallback /
        typed-refusal tokens must exist while the behaviours pass.

        Verification: scripts/run-litmus-test.sh
        forge-environment-discoverability --phase pre-build --size instant
        --compact — both new tests EXECUTED, 12/12 and 9/9 steps OK, suite
        10/10 executed 0 fail; all three touched yaml files yq-clean;
        ./build.sh --check green (gate stamp recorded).

        FINDING NOTICED, NOT FIXED: the generic deterministic lane cites
        README.md lines 1-5 UNCONDITIONALLY (project-info.sh, generic-lane
        envelope), so a README shorter than 5 lines makes verify-answer
        refuse a truthful confidence=exact answer ("citation line range runs
        past end of file") — observed against a 3-line fixture README while
        building the negative control; the litmus fixtures around it with a
        5-line README. The citation range should clamp to the file's real
        line count. Candidate follow-up for the 669 family (fits
        669-egjn's generic-lane enrichment surface).
