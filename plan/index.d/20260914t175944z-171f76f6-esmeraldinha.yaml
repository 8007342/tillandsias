# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: three-fixtures-assert-properties-of-the-environment-they-run-in-so-a-correct-refusal-scores-as-a-failure
    event:
      type: progress
      ts: "2026-09-14T17:59:44Z"
      agent_id: linux-esmeraldinha-opus5-20260914t175944z
      host: esmeraldinha
      summary: |
        esmeraldinha classified group C at the AT-RISK locus (Windows/Git Bash, MINGW64_NT-10.0-26200). TWO OF THREE ARE GROUP A, each proven with the locus as the single variable; the third is NOT classifiable without a build and is left open rather than guessed. FIRST, WHY ALL THREE WERE SILENT, which is itself the finding: none is silent by accident. plan-answer-envelope-citability ends its failing branch in a bare `exit 1` inside a for loop; citation-frame-and-caller-relation has exactly one silent exit (`grep -q FAILED && exit 1`) while its other failure path echoes a count; project-answer-synthesis-refusal-typed chains grep && jq -e && echo so any broken link yields nothing. "Empty output" was never evidence about the system under test — it was a property of the fixtures. (1) plan-answer-envelope-citability 6/21 — GROUP A. The surviving envelope /tmp/.litmus-394b-env.json (written 17:33Z, inside the run window) shows a cited packet_id reported ABSENT from the answer text while being VISIBLY PRESENT in it. Bytes: jq emits CRLF on MSYS, raw output ends "envelope \r \n"; \r is not in the default IFS, so word-splitting leaves it attached to every field except the last, where command substitution strips it. Loop item 1 len=68 tail "...ity \r" vs the same id assigned directly via $(...) len=67 which greps MATCH. So the first citation is compared as "...ephemerality\r" and cannot match; the second, being last, is clean. THE PRODUCT AND THE ENVELOPE ARE CORRECT — the answer does name every packet it cites. This is the same root as the two CRLF arms already in group A, making THREE, and it is one mechanism rather than three coincidences: on this locus text arriving through a pipe carries \r, and any fixture that word-splits or string-compares it is asserting a property of the host. (2) project-answer-synthesis-refusal-typed 3/10 — GROUP A. The saved /tmp/.litmus-669r/refusal.json satisfies every part of the arm's predicate except one: confidence=unsupported OK, citations length 0 OK, freshness carries source_commit and indexed_at OK, but inference_reason=endpoint-timeout where the arm pins endpoint-unreachable. images/default/lib-inference-state.sh maps curl 6/7 to unreachable and curl 28 to timeout, probing with --max-time 1. Same command, only the locus varying: Windows/Git Bash `curl -fsS --max-time 1 http://127.0.0.1:9/api/tags` exits 28; the tillandsias-build distro exits 7. A FIRST HYPOTHESIS WAS WRONG AND IS RECORDED AS WRONG: "curl's dead-port exit differs by platform" is false — a plain curl to that port exits 7 on BOTH loci. The variable is the DEADLINE: a connect attempt to a dead local port takes about 2s on the Windows stack and about 0s in the distro, so the one-second probe deadline expires first. The product is right and its refusal reason is ACCURATE for this host; the fixture pins the vocabulary one locus happens to produce. (3) citation-frame-and-caller-relation 8/8 — NOT CLASSIFIED. Reading the control flow narrows it without a re-run: the ONLY silent exit is `grep -q FAILED && exit 1`, since the other failure path echoes "FAIL: only $n frame unit pins ran" and a compile error would leave n=0 and take that echoing path. So the empty output establishes that `cargo test -p tillandsias-plan` output contained the literal FAILED — real failing tests, not a missing fixture and not a build break. WHICH tests, and whether they fail for a Windows reason or a genuine one, needs the cargo test run itself, which this host's standing selection rule excludes (release build / long gate). Routing it to a capable host is the right move rather than running it here badly. WARNING FOR WHOEVER TAKES IT: this arm reports 314.0s, the slowest in its spec, so a re-run on a floor-tier host could produce a TIMEOUT instead of the FAILED it actually hit — a different red of the same colour, which is exactly the group A / group B confusion this packet exists to prevent. NET: group A grows from 6 to 8, the CRLF mechanism accounts for three of them, one arm remains open. Nothing was edited. Full evidence: plan/issues/litmus-1109-t8kw-group-c-classified-2026-09-14.md on origin/windows-next at 2635f6cb3.
        TXT
        out="$(./target/debug/tillandsias-plan append-event 1109-t8kw progress "$SUM" --ts "$(date -u +%FT%TZ)" --agent "$(bash scripts/agent-identity.sh id opus5 2>/dev/null)" --host esmeraldinha 2>&1)"; rc=$?
        echo "APPEND_RC=$rc"; printf '%s\n' "$out" | head -5
