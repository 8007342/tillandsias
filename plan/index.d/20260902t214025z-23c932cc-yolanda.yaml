# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: gate-ledger-native-fs-staging-windows
    event:
      type: progress
      ts: "2026-09-02T21:40:25Z"
      agent_id: windows-yolanda-opus5-20260902t205609z
      host: yolanda
      summary: >
        MEASURED, IMPLEMENTED, AND THE PACKET'S PREMISE IS HALF WRONG - which is the more useful half of this result. All numbers from the tillandsias-build WSL2 distro, where the entire Windows gate re-execs with the checkout at /mnt/c (v9fs), same binary, same ledger, 3 reps each.    raw IO, cat every ledger file          9p   159ms   ext4    4ms   ~40x   raw IO, find over plan/ (1406 files)   9p    94ms   ext4    4ms   ~23x   tillandsias-plan check (full fold)     9p   645ms   ext4  475ms   1.36x   tillandsias-plan query --status ready  9p   362ms   ext4  225ms   1.6x   cost to stage plan/ once                              5796ms  THE SPREAD IS THE FINDING. The ~24x multiplier is REAL and reproduces at the raw-IO layer. It does NOT reproduce on a LEDGER FOLD, because a fold is CPU-bound parsing 3.95MB of YAML: 9p costs it 170ms, one staging pass costs 5796ms, so staging would need about 34 folds to break even and the gate does not do that. The deliverable as written - stage plan/ once per gate and point the ledger-reading checks at it - would have made the Windows gate SLOWER, and it was not implemented. A multiplier measured on the wrong operation is exactly the proxy-for-a-placement shape 793-zumy spent itself on: 24.6x was true about file IO and was being read as a claim about the checks.  WHERE THE MULTIPLIER ACTUALLY LIVES is the one gate step that does TREE-COPY CHURN rather than parsing. archive-plan-packets.sh --check copies plan/, rewrites the copy, copies it AGAIN and diffs the two trees, so it is metadata- and write-bound:    ledger-bound portion of the check   9p 62331ms -> ext4  5306ms   11.7x   the same check end to end           9p 69127ms -> ext4 42053ms   (the remainder is the cargo answerability suite, which is CPU)   the same step inside ./build.sh --check           78.4s -> 43.7s  WHAT LANDED. scripts/native-scratch-dir.sh - ONE shared probe answering "is this worktree on a filesystem where scratch churn is unaffordable, and if so where should the scratch live". It decides by FSTYPE NAME (v9fs 9p fuseblk nfs nfs4 cifs smb2 smbfs virtiofs prl_fs vboxsf), never by a latency probe: a probe would have to do IO to decide whether to avoid IO, on the very host where that measurement is the slow thing. An UNKNOWN fstype is never treated as slow - staging is an optimisation and guessing wrong in that direction adds a copy for nothing. It always prints a directory that exists and is writable, falling back to the caller's previous path whenever staging would not help or could not be arranged, because slow is strictly better than broken.  archive-plan-packets.sh --check now takes its scratch from that probe. ON A FAST WORKTREE THE PROBE RETURNS $REPO_ROOT AND EVERY PATH IS BYTE-FOR-BYTE WHAT IT WAS, so a host that was correct before this order behaves identically after it - no regression is available to native Linux or macOS. On the Windows lane the scratch lands on the distro's ext4. A side effect worth naming: the copy no longer lands IN THE WORKTREE, which is the leak that script's own cleanup-trap comment was written about after a 2026-08-23 Ruby crash left a full copy of plan/ in the checkout and started every subsequent boundary-guarded cycle dirty. Six hand-placed early cleanups now call the trap function instead of repeating its body.  scripts/archiver-check-memo.sh (911-m7js) gains native-scratch-dir.sh in its digest: that file decides WHERE the check runs, so it is part of the instrument even though it cannot change the verdict. Its own fixture stays 6/6.  EXIT CRITERION 3, THE NEGATIVE CONTROL, run on a deliberately broken tree (a fragment the fold cannot read, planted in plan/index.d and removed in the same shell): BASELINE script rc=3, NEW script rc=3, same refusal reason ("cannot read fragment ... the orphan invariant cannot be evaluated. Refusing."). The only textual difference is that the new message names the real scratch path instead of a relative one, which is the message becoming more true rather than less. Worktree byte-identical afterwards.  WHAT THIS PACKET DID NOT TOUCH: how many times build.sh invokes tillandsias-plan. That is slice A1 (964-tzmp, macuahuitl) and the two must not be conflated - this changed only WHERE the ledger a check reads lives.  A CAUTION FOR WHOEVER READS THE 24.6x NEXT. Do not generalise the staging to the other ledger-reading steps on the strength of the raw-IO number. The measurements above are the reason, and they are on this packet so the next person does not have to re-derive them to reach the same negative answer.
