# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-20260809t063500z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# ORDER 626-bnn5 (loop-status-readme-exclusion-from-fragment-parse) CLOSED in the
# shaping forge cycle that split it out of 606-46x9. Evidence below; the forge
# gate (./build.sh --check) ran green on the pushed tree.
events:
  - packet_id: loop-status-readme-exclusion-from-fragment-parse
    event:
      type: completed
      ts: "2026-08-09T06:35:00Z"
      agent_id: forge-20260809t063500z
      host: forge
      summary: >
        CLOSED. crates/tillandsias-plan/src/loop_status.rs gained
        is_fragment_file() (a .md cycle-status file that is not README.md,
        case-insensitive) used by BOTH load_all and malformed, so the committed
        store documentation can never masquerade as a fragment. Unit test
        the_store_readme_is_never_a_fragment_but_real_malformed_files_still_are
        added; cargo test --release -p tillandsias-plan --lib loop_status
        passed 14/14. litmus:loop-status-fragment-overlay PASSED including the
        new ORDER 626-bnn5 pin step (store README never folded/never malformed,
        genuine bad fragment still surfaces). On the real ledger
        loop-status-fragments now reports malformed=0 (was malformed=1) and
        loop-status folds with no README warning. Exit criteria 1-3 proven by
        the unit test + litmus; criterion 4 by cargo test + litmus.
status:
  - packet_id: loop-status-readme-exclusion-from-fragment-parse
    field: status
    value: completed
    ts: "2026-08-09T06:35:00Z"
    host: forge
