# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-linux-clean-room-reset-does-not-reach-the-host-keychain
    field: next_action
    value: wire clear-vault-host-credentials.sh into the documented reset when the operator confirms (a); if (b), document it as deliberately not called. The mechanism is landed and INERT, both directions pinned 6/6 and mutation-verified, gate step binds the fixture never the clearer
    ts: "2026-09-13T15:58:11Z"
    host: yoga

events:
  - packet_id: the-linux-clean-room-reset-does-not-reach-the-host-keychain
    event:
      type: progress
      ts: "2026-09-13T15:58:11Z"
      host: yoga
      summary: |-
        status 'wire clear-vault-host-credentials.sh into the documented reset when the operator confirms (a); if (b), document it as deliberately not called. The mechanism is landed and INERT, both directions pinned 6/6 and mutation-verified, gate step binds the fixture never the clearer' (not terminal) with evidence_refs: b721b880e
