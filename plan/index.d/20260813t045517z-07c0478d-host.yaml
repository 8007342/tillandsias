# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: hooks-shipped-but-never-installed-audit
    field: status
    value: implemented
    ts: "2026-08-13T04:55:17Z"
    host: host

events:
  - packet_id: hooks-shipped-but-never-installed-audit
    event:
      type: progress
      ts: "2026-08-13T04:55:17Z"
      host: host
      summary: >
        status 'implemented' (not terminal) with evidence_refs: scripts/audit-guard-activation.sh reports total=28 active=28 orphan=0 verdict=ok on Linux; the 2 orphans it found (check-plan-schema-divergence.sh, check-markdown-distillation.sh) are now wired (build.sh gate + local-ci); the auditor is itself wired into local-ci. Criterion 2 (executable activation test) complete; criterion 3 needs Windows/macOS sibling reports.
