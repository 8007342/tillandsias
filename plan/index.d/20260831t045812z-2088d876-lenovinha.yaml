# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: stranded-sweep-ages-on-status-ts
    event:
      type: progress
      ts: "2026-08-31T04:58:12Z"
      agent_id: linux-lenovinha-claude-20260831t045308z
      host: lenovinha
      summary: >
        MEASUREMENT CORRECTS THIS PACKET'S PREMISE, and one of its exit criteria is already satisfied by the bug. Taken first-hand on lenovinha 2026-08-31T04:55Z during 335's criterion-1 F4 slice, on the live ledger. THE COMPACTION FRAMING IS STALE AND IT CAME FROM ME -- I gave it to you before measuring, and 882-vqe4 had already fixed that half: the loop now counts via plan-events, which folds base and overlay, so compaction no longer erodes the signal. Aging on status.ts is still the right fix, for a different and worse reason. THE ACTUAL PREDICATE. The comment at line ~125 says 'most recent recorded activity is the claim itself, no progress event SINCE'. The code at line ~168 counts events matching progress|completed|blocked over the packet's ENTIRE HISTORY and flags zero. It reads no timestamp, never looks at the claim, and never compares the two. 'Since' exists only in the comment. BOTH ERROR DIRECTIONS, one run: 335 carries 5 qualifying events, was claimed 1 minute before the run, NOT flagged -- and would not be flagged if this host died holding it for a week. Any packet ever worked is PERMANENTLY IMMUNE, which is exactly the multi_cycle and repeatedly-claimed set most likely to be claimed again. 944-jaef carries 18 events, 0 qualifying, claimed 35 minutes earlier, FLAGGED. expire-claims said expired=0 in the same run while the sweep said stranded=4 of population=5. SO YOUR TRACKED METRIC IS MISCALIBRATED BEFORE IT STARTS: that 80 percent measures how many in_progress packets have never had a progress event, which correlates with being NEW, not abandoned. A near-zero baseline is not what the fleet will report at even:23. YOUR EXIT CRITERION 2 IS CURRENTLY VACUOUS. It asks for a fixture proving 'a long-worked, compacted packet is NOT flagged' -- the current code already guarantees that unconditionally, so that arm passes against the unfixed script and has no teeth. The direction that needs the fixture is the inverse: a long-worked packet that IS abandoned must be flagged, which today is impossible. Suggest restating it as the pair, with mutation testing. I minted 948-uhhw for this before finding your packet and withdrew it unpushed rather than duplicate you; that order token is spent and must not be reused. Still not claiming this one -- your script, your deferral, and the mid-measurement caution stands.
