# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260812t030500z
events:
  - packet_id: status-ladder-enforcement-wave-2
    event:
      type: progress
      ts: "2026-08-12T03:05:00Z"
      agent_id: linux-mutable-night-20260811
      host: linux_mutable
      summary: >
        Criterion 1 (rank-aware fold merge) IMPLEMENTED + unit-verified. The
        status field's LWW resolution in crates/tillandsias-plan/src/fragments.rs
        is now a monotone lattice over the 650-dq6u closure ladder
        (implemented<completed<verified<done): a higher rung wins regardless of
        (ts,host) arrival order; equal rung falls back to plain LWW; a move DOWN
        the ladder — a lower rung, or a working state replacing a rung — is
        accepted ONLY when the same fragment carries a `falsified` event for
        that packet (fragment_falsifies + status_entry_wins helpers). obsoleted
        and failed are lateral terminals decided by plain LWW; climbing UP from
        a working state onto the ladder always wins (recording verification does
        not need a newer clock). Mirrors the set-field write gate's is_downgrade
        so fold and write-time refusal agree. 6 new unit tests cover both
        arrival orders, the stale-in_progress-vs-done and completed-vs-verified
        clobber cases, the falsified exemption, equal-rung/working LWW, lateral
        terminals, and an end-to-end commutativity check (verified survives a
        later stale ready in either fragment order). 122 crate tests green incl.
        the pre-existing fold-commutativity property. Real ledger folds
        unchanged (634-39ik verified, 650-dq6u verified, 322 completed).
        REMAINING (packet stays ready): criterion 2 (invisible-block report in
        check/plan_status), criterion 3 (new-fragment evidence gate), criterion
        4 (plan_next verification pickups).
