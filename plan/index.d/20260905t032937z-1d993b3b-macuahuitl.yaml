# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: floor-tier-release-smoke-and-metrics
    event:
      type: progress
      ts: "2026-09-05T03:29:37Z"
      agent_id: linux-macuahuitl-claude-20260905t032937z
      host: macuahuitl
      summary: |-
        TESTED RELEASE v56.9.5.1 (daily, prerelease), the meta-orchestration duty's record: cut 02:19Z-02:23Z by the 01:47Z coordination fire after the 00:09Z meta cycle deferred it on three host fixes (all landed: yoga's one-sided secure-wire flip reverted at 08a7d3cc7, macbookair's --replace removed from both forge builders and the tray binary-name collision fixed at 21745a1fd, both proven on forced full gates), from the merged trunk with union litmus 291/291 and the tool's gate; PR #107 merged, bump merge b528680d2 (PR #108), tag at that SHA verified unmoved after the back-merge (c3b64f56d on linux-next); release.yml run 33938905320 dispatched 02:22:50Z, SUCCESS 03:24:33Z (61m42s; Windows assets verified and uploaded). VALIDATION LANE 1, Linux §1 on macuahuitl (non-smoke host, §2 opted out): pinned curl install from the tag's download base exit 0 in 222 s (the installer ran init), `tillandsias --version` = "Tillandsias v56.9.5.1" exact, binary sha256 361e40d3e5be4fad…, artifact HTTP 200; §2b read the README row because the row was written before the smoke this time. Lanes 2 and 3 (macbookair macOS §1; esmeraldinha's floor round) dispatched at publication; their reports land on this packet. WHAT THE DAILY CARRIES (README row): tray honesty on all three platforms (980-ja2m slice (a), 1032-utne), the LocalProjects wire surface removed at WIRE_VERSION 3 with every discriminant pinned (997-e4v2 step 3, 1029-5wvd), the client-side wire-version refusal tested (1032-62rx), order 505's label validation failing closed (1031-q4pb, security), forge launch collisions handled by state with a bounded wait and no --replace (873-vgyg), the secure-wire readers on one parser with a ratchet (972-umik, default unchanged), the tray bin-name collision guard (1043-kvvn), and the gate fixes (1022-px54, 1038-d7vw, 1028-3eiz, 1009-gccx, 1036-jamx). NOT in it: the 972-umik flip (lands with the last reader), 1042-svey (the builder's openssl, landed after the cut), 1051-f7e2 (the stress ratio guard, in progress). STABLE CANDIDATE per the operator's answer: this daily once its three lanes pass, plus the flip's daily; the Windows Authenticode question stays the operator's.
