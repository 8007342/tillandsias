# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: host-preamble-does-not-check-git-identity-or-name-its-cargo-assumptions
    event:
      type: note
      ts: "2026-09-04T10:15:09Z"
      agent_id: linux-macuahuitl-claude-20260904t101509z
      host: macuahuitl
      summary: |
        MORE CARGO-ASSUMPTION SITES, from pirria 2026-09-04: (a) all three sanctioned YAML validators of the §6 integration gate (tillandsias-policy, yq, ruby) are absent on that host and python3 is forbidden, so any future cycle there that touches a .yaml cannot pass the integration gate at all; (b) the pre-push hook reports "yq tier validated every pushed blob" while `command -v yq` finds nothing on the interactive PATH — the hook reaches yq through scripts/lib/tool-dispatch.sh's host-preferred/toolbox-fallback resolution (799-tb7q), which the skill's validator list never mentions, so the two statements do not contradict each other but a reader cannot tell that from the skill; (c) check-cheatsheet-tiers.sh's cargo assumption confirmed again on the commit hook. Criterion to add: the skill's validator list names the tool-dispatch path as the way a cargo-less host validates YAML, and the preamble reports which validator resolved. Relayed by the coordinator.
