# Ledger fragment — append-only, IMMUTABLE once written.
# Hand-written in set-field shape (worktree isolation refuses the compound CLI
# invocation); validated with closure-evidence-check before commit.
status:
  - packet_id: build-step-timing-is-banner-to-banner-not-per-step
    field: status
    value: completed
    ts: "2026-08-17T04:10:00Z"
    host: linux_mutable

events:
  - packet_id: build-step-timing-is-banner-to-banner-not-per-step
    event:
      type: completed
      ts: "2026-08-17T04:10:00Z"
      agent_id: "fork-785ibu9-20260817"
      host: linux_mutable
      summary: >
        status 'completed' with evidence_refs: OPTION (a) implemented — the emitted
        record now measures the named step's OWN work, not the banner-to-banner span.
        Seam: build.sh's `_run`, which is where a step's real work happens, so the fix
        cost ZERO per-site edits and preserves 765-dfry's whole reason for existing
        (a fix requiring ~40 annotated call sites would rot). `_run` accumulates its
        elapsed time into _PHASE_WORK_MS and sets _PHASE_RAN_WORK; `_phase_close` logs
        span<TAB>work<TAB>name; `_phase_emit_timing` emits work when measured and the
        span only as a labelled fallback. Provenance is explicit in the record's
        `phase` field — "build" = attributable to the named step, "build-span" =
        wall clock between banners — and cycle-metrics suffixes a span-provenance
        slowest= with "~span" so the one number a cycle reads first cannot be
        mistaken for a step cost. 682-emvg contract preserved by construction:
        `|| _rc=$?` keeps the command tested so errexit cannot abort before the
        accounting, and the original status is returned unchanged (fixture asserts
        exit 0 and exit 7 both propagate); a stuck clock degrades to 0 and never
        fails the emit. Batched single-spawn emit and the finest-grain slowest=
        preference both retained; 693-tf79 bounds still apply.
        FIXTURES: scripts/test-build-step-timing-attribution.sh 9/9 (fast named step
        followed by slow UNNAMED work reports 1ms, not the adjacent second; the
        genuinely slow step still reports 1004ms so the fix does not just shrink
        every number; provenance labels asserted both ways; span fallback keeps its
        wall-clock number; all phases still emitted under the step: family so
        slowest= sees them; _run exit-code preservation; stuck-clock degradation)
        and scripts/test-slowest-span-marker.sh 3/3 (span marked, measured unmarked,
        pre-785 records with no phase field unchanged).
        MUTATION CONTROL, non-vacuous: reverting the emit to the span reproduces the
        defect verbatim — the ~0ms step reports 1007ms — and exactly the two
        assertions this fix owns go red, nothing else.
        litmus:build-test-timing-telemetry-shape extended 8 -> 10 steps, both new
        steps behavioural (fixture-driven, not source greps, so the 634-39ik ratchet
        is satisfied); PASS via the canonical runner. bash-dialect-clean.
        ./build.sh --check rc=0 verified unmasked (72s, isolated timing log).
  - packet_id: build-step-timing-is-banner-to-banner-not-per-step
    event:
      type: note
      ts: "2026-08-17T04:10:00Z"
      agent_id: "fork-785ibu9-20260817"
      host: linux_mutable
      summary: >
        MEASURED, and it refines this packet's own premise rather than merely
        confirming it. On a real --check run at this tree, every guard phase in the
        gate reports phase=build with span and work in agreement — e.g. the
        fragment-status step records 10363ms of measured work against a 10.4s span,
        so THAT reading (the one 783-xyk5 was filed on) was sound on this host; the
        fork's 5ms-vs-1.5s observation came from its own worktree, where the guard
        early-exited on an empty fragment set while _run overhead and the surrounding
        block still elapsed. What the fix actually exposes is a different and larger
        offender: step:staging-router-sidecar-build-artifact-not-committed reports
        14300ms with phase=build-span — the third-largest number in the gate, running
        no measurable command, previously indistinguishable from an attributable step
        cost. Two consequences worth carrying forward: (1) the gate's unattributed
        wall clock is concentrated in the few phases that shell out without _run, not
        smeared across the guards; (2) `slowest=` on this host currently names
        step:running-clippy-strict-includes-the-workspace-type-check:17456 with no
        marker, i.e. a genuinely attributable target. The _phase_report gap
        annotation (span minus work, when both exist) printed nothing this run,
        which is the correct negative control for the same finding.
