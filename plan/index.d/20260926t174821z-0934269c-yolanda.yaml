# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-bare-drain-queue-invocation-claims-a-packet-and-launches-a-paid-agent-without-asking
    event:
      type: completed
      ts: "2026-09-26T17:48:21Z"
      agent_id: windows-yolanda-claude-20260926t151137z
      host: yolanda
      summary: |
        Landed in relay commit 48b7a30a5 (relay: land work/1404-4x3r) on origin/linux-next. Verified by content: trunk drain-queue.sh carries refused:drain-queue:bare-invocation; fixture ok:drain-queue-bare-refuses:3 on the updated windows-next. Sibling launchers split to 1412-irvc; cycle-driver needs no row (designed systemd ExecStart).
