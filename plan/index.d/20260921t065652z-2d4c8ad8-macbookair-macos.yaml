# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: three-macos-fixtures-spell-their-could-not-run-unsupported-which-the-litmus-runner-scores-as-a-failure-rather-than-a-skip
    field: status
    value: done
    ts: "2026-09-21T06:56:52Z"
    host: macbookair-macos

events:
  - packet_id: three-macos-fixtures-spell-their-could-not-run-unsupported-which-the-litmus-runner-scores-as-a-failure-rather-than-a-skip
    event:
      type: completed
      ts: "2026-09-21T06:56:52Z"
      host: macbookair-macos
      summary: |-
        status 'done' with evidence_refs: Pushed on osx-next at 61b3f785d; gate BUILD_RC=0, ok:test-baseline:ran=2476 new-red=0. BEFORE: [FAIL] litmus:macos-vz-orphan-diagnosis 'Check implementation', 0% pass rate. AFTER: Step Verdicts skipped=1, Status [PASS]. The STEP scores SKIPPED and leaves the rate; the TEST scores PASS on its host-independent parse step, stated that way rather than left as an unqualified 'scores SKIPPED'. Healthy state still ok:macos-vz-orphan-diagnosis:4. Both litmus bindings widened — their grep -E '^(ok|violation|unsupported):' would have eaten the new terminal line, so a source-only fix would have read correct and changed nothing. The relocated-script case was proposed for cannot_run and REFUSED: it becomes refused:...:root-is-not-a-tillandsias-checkout on the follow-up slice, because a skip there would hide a broken invocation.
  - packet_id: three-macos-fixtures-spell-their-could-not-run-unsupported-which-the-litmus-runner-scores-as-a-failure-rather-than-a-skip
    event:
      type: note
      ts: "2026-09-21T06:56:52Z"
      host: macbookair-macos
      summary: |-
        the three fixtures end a could-not-run on a line the runner scores; before/after measured on the same command
