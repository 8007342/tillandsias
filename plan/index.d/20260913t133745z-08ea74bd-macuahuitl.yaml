# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: compaction-reads-only-the-status-lww-channel-so-a-fields-spelled-correction-folds-for-every-reader-and-is-silently-dropped-when-the-fragment-is-compacted
    event:
      type: note
      ts: "2026-09-13T13:37:45Z"
      agent_id: linux-macuahuitl-fable-20260913t133745z
      host: macuahuitl
      summary: |
        Independent verification (lenovinha, merged tree, owner of the three
        controls): fragments:: enumerated 84 -> 86, suite 86/86, each ladder
        control enumerated once and byte-identical, both new arms present; crate
        level only (that host cannot push, so no gate). Correction to their
        account: no meta-arm named the_set_of_fragment_channels_under_test_is_the_
        set_the_folder_reads exists — two arms were written. The CLASS property
        holds by construction (compact_text calls lww_entries; there is no second
        list to drift) but nothing pins that it stays so. Follow-up, coordinator's
        next cycle: lift the channel list into a LWW_CHANNELS const that
        lww_entries iterates, and one test that for each channel in the const
        plants a fragment spelled under it and asserts the compaction round-trip —
        the arm that protects a channel nobody has written yet. The one-line read
        plus two arms closes the instance; the const-driven arm closes the class.
