# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: ghost-trace-ephemeral-guarantee-spec-and-litmus-closure
    field: status
    value: in_progress
    ts: "2026-09-08T22:55:18Z"
    host: macuahuitl

events:
  - packet_id: ghost-trace-ephemeral-guarantee-spec-and-litmus-closure
    event:
      type: falsified
      ts: "2026-09-08T22:55:18Z"
      host: macuahuitl
      summary: |-
        falsified 'completed' claim; evidence: af7529a265ee0ec3b61f1f6fb7d3537cf01874a3; ./build.sh --ci-full pre-build: ephemeral-guarantee has no @trace annotations in code — status set to 'in_progress', new attempt epoch
  - packet_id: ghost-trace-ephemeral-guarantee-spec-and-litmus-closure
    event:
      type: note
      ts: "2026-09-08T22:55:18Z"
      host: macuahuitl
      summary: |-
        Reopened after CI exposed missing code trace on the activated spec.
