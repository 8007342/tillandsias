# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260812t030000z
#
# Two packets: (1) a valid re-declaration of macOS's 697-s3by, which is
# INVISIBLE in the fold because its origin fragment
# (plan/index.d/20260812t013937z-697-timing-absent-macos.yaml) has an unquoted
# `colon-space` inside its `deliverable:` scalar and does not parse; (2) the
# class finding — a malformed fragment silently drops its packet AND passes the
# gate. macOS should DELETE its malformed fragment (this re-declaration is now
# the canonical 697-s3by), NOT re-file it, to avoid a duplicate packet_id.
packets:

  - packet_id: build-check-emits-no-timing-record
    order: 697-s3by
    status: ready
    desired_release: v0.5
    priority: p2
    title: "build.sh --check emits no timing record, so the cycle's slowest step is unmeasured on every host and the timing: line reads source=absent forever"
    kind: bug
    deliverable: "one duration record per heavy step, and a non-absent timing: line naming a real slowest step"
    depends_on: []
    pickup_role: any
    capability_tags: [telemetry, cycle-time, build, instrumentation]
    estimated_hours: 3
    context: >
      RE-DECLARED on linux-next 2026-08-12 to rescue the packet from macOS's
      malformed fragment (see 697-sy8w). Original macOS finding preserved:
      observed on macOS after `./build.sh --check` ran FIVE times to completion
      in one session — /tmp/tillandsias-timing.jsonl never existed and
      cycle-metrics.sh reported `timing: steps=0 ... source=absent` every cycle.
      This is the metric meta-orchestration introduces with "where does the
      cycle's wall-clock go?" and calls building/testing "the most likely
      bottleneck"; slowest is meant to name the one step to attack first. While
      it reads absent, the most expensive part of every cycle on every host is
      unmeasured and 682-emvg's deliverable is not in effect where it matters
      most. THE WIRING LOOKS CORRECT (filed as research, not a one-line fix):
      build.sh sources scripts/timing-log.sh (no-op stubs if that fails),
      captures start, traps EXIT, emits; local-ci.sh emits per phase;
      timing-log.sh shells out to cycle-metrics.sh --emit-timing. TWO
      HYPOTHESES ALREADY REFUTED — do not re-chase: (1) the emitter is broken
      (no — called directly it works); (2) bash 3.2 can't parse timing-log.sh
      so macOS gets no-op stubs (no — bash -n parses clean and 3.2 defines the
      real function). Next probe: one trace at the timing_emit call site during
      a gate run distinguishes "never called" from "called, shell-out failed
      silently" (timing_emit swallows every error by design, which is exactly
      why it failed invisibly). Full write-up:
      plan/issues/research-build-check-emits-no-timing-record-2026-08-12.md.
      NOTE: this connects to 693 (timing: line emits overflow garbage on linux)
      — the same telemetry surface is broken two different ways across hosts.
    exit_criteria:
      - "./build.sh --check appends exactly one build-check duration record per run, on macOS and Linux"
      - "cycle-metrics.sh reports a non-absent timing: line naming a real slowest step"
      - "a NEGATIVE CONTROL asserts a FAILING step still emits its record with a non-zero exit= — instrumentation that only records successes hides the slow path that matters most"
    events:
      - type: filed
        ts: "2026-08-12T01:39:37Z"
        agent_id: macos-tlatoanis-macbook-air-fable5-20260812t013937z
        host: macos
        summary: "Original filing (malformed fragment). Re-declared valid on linux-next 2026-08-12 (697-sy8w)."
      - type: progress
        ts: "2026-08-12T03:00:00Z"
        agent_id: linux-mutable-night-20260811
        host: linux_mutable
        summary: "Rescued into a parseable fragment; content preserved verbatim from the malformed macOS original. macOS should delete the malformed file, not re-file."

  - packet_id: malformed-fragment-passes-the-gate-silently
    order: 697-sy8w
    status: ready
    desired_release: v0.5
    priority: p1
    title: "A malformed plan fragment is SKIPPED at fold time and still passes the gate — filed work vanishes with exit 0"
    kind: fix
    deliverable: "tillandsias-plan check (and build.sh --check) FAILS when any plan/index.d fragment does not parse, naming the file"
    depends_on: []
    pickup_role: linux
    capability_tags: [plan, ledger, gate, robustness, work-loss]
    context: >
      Found 2026-08-12 merging osx-next: fragment
      plan/index.d/20260812t013937z-697-timing-absent-macos.yaml does not parse
      (an unquoted `colon-space` — 'timing: line' — inside its `deliverable:`
      scalar, YAML line 12), so packet 697-s3by was SKIPPED from the fold and
      INVISIBLE to every reader and selector. Critically, `tillandsias-plan
      check` printed the SKIP warning to stderr but STILL EXITED 0 — verified
      live — so the malformed fragment passed the pre-push gate and shipped.
      A filed packet silently disappearing with a green gate is the exact
      work-loss class the fold's malformed-skip was meant to make visible
      (fragments::malformed) but never made FATAL. The write path that produced
      it (an agent hand-authoring a fragment with a colon in an unquoted scalar)
      will recur; set-field/append-event avoid it, but hand-authored `packets:`
      fragments do not go through them. FIX: check must exit non-zero on any
      malformed fragment, name the file and the parse error, and the build.sh
      gate must surface it as a failure — a lost packet must never pass CI.
      Consider also a commit-time lint so the author sees it before push.
    exit_criteria:
      - "tillandsias-plan check exits non-zero and names the file when any plan/index.d/*.yaml fragment does not parse"
      - "build.sh --check fails on a malformed fragment (a fixture with a deliberately broken fragment reds the gate; a NEGATIVE CONTROL proves a well-formed fragment passes)"
      - "the trigger file 20260812t013937z-697-timing-absent-macos.yaml is removed once 697-s3by's re-declaration is confirmed canonical"
    events:
      - type: filed
        ts: "2026-08-12T03:00:00Z"
        agent_id: linux-mutable-night-20260811
        host: linux_mutable
        summary: "Filed from the fold-time SKIP of a macOS fragment that still passed the gate; work-loss hazard, p1."
