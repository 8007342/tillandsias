# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: forge-pids-ceiling-512
    event:
      type: note
      ts: "2026-09-01T23:45:00Z"
      host: macuahuitl
      summary: >
        Attribution TIGHTENED with a free control (esmeraldinha): her
        verification workflow spawned 20 AGENTS and produced ZERO new
        denials, while one brew bootstrap produced 100 — agent fan-out is
        NOT what reaches the ceiling; the installer's fork storm is. Her
        live step-1 litmus run under AUTOINSTALL=0 shows pids.current 68
        with events still exactly 100 (no new denials mid-suite). Census
        corrected per her own retraction: her Cellar holds 7 formulae /
        299MB (the RUBY DEPENDENCY CHAIN — and NO target tool: her
        2-minute wrapper killed the hyperfine install after the full
        bootstrap was paid, before the tool arrived). The two halves of
        the evidence now read: pirria paid 364s and got one tool;
        esmeraldinha paid 299MB and 100 denials and got NOTHING —
        expensive-and-empty is the sharper failure mode and the better
        960-tpop example. TRAP SURFACES ARE NOT COMMON-MODE: Cellars
        diverge (tokei-only vs ruby-chain), so tokei is the pair's one
        known-divergent tool, EXCLUDED BY NAME from comparisons; the
        other 63 shims 127 symmetrically under AUTOINSTALL=0 and
        subtract. GATE BYPASS verified in shim-exec: an
        ALREADY-INSTALLED tool execs normally above the AUTOINSTALL=0
        gate — the 127 fires only for absent tools, so same variable,
        same command, opposite outcomes across hosts with divergent
        Cellars, for reasons unrelated to virtualization or chip.
        Logged, not explained: both linuxbrew trees were created ONE
        SECOND apart on different hosts (19:56:42/19:56:43) — almost
        certainly parallel sessions reaching tool probes together.
