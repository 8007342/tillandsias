# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-20260809t104000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# ORDER 598-znuv (local-gate-obligation-missing-from-the-loop) CLOSED with
# RED/GREEN-proven litmus evidence. The loop skill now names ./build.sh --check
# as a Finalization obligation with the command; AGENTS.md no longer cites push
# CI as a safety net (GEMINI.md and .github/copilot-instructions.md inherit via
# symlink); litmus:github-actions-budget carries a step that is RED without the
# skill edit and GREEN with it; and the query_packets clippy failure is recorded
# as the evidence case.
events:
  - packet_id: local-gate-obligation-missing-from-the-loop
    event:
      type: completed
      ts: "2026-08-09T10:40:00Z"
      agent_id: forge-20260809t104000z
      host: forge
      summary: >
        CLOSED, all four exit criteria PROVEN. (EC1) skills/meta-orchestration/
        SKILL.md Finalization step 4 now reads 'Run the local pre-push gate:
        ./build.sh --check and fix what it reports ... Do not push past a red
        gate (evidence case: plan/issues/local-gate-evidence-query-packets-
        clippy-2026-08-09.md)' — placed between the YAML-validation step (3)
        and the commit step (5), an obligation with a command, not an advisory.
        (EC2) AGENTS.md no longer contains the false 'CI workflow will mark it
        red' claim; it now states push CI exists on no working branch and the
        local gate is the only trunk protection. GEMINI.md and .github/
        copilot-instructions.md are symlinks to AGENTS.md and inherit the
        correction (verified by readlink). (EC3) litmus:github-actions-budget
        gained the step 'the loop skill names the local gate in Finalization as
        an obligation with the command' (sed-scoped to /^## Finalization/,$p so
        the --ci freshness advisory elsewhere cannot satisfy it). RED proven by
        running the identical grep against a Finalization block without the
        gate (no match); GREEN proven by the real skill after the edit (match)
        plus a direct step run reporting 'ok: loop skill names the gate'. (EC4)
        plan/issues/local-gate-evidence-query-packets-clippy-2026-08-09.md
        records the always-true `limit == 0 || true` at
        crates/tillandsias-plan/src/main.rs:327 that clippy denies — the first
        concrete instance where ./build.sh --check was the only thing between an
        agent and a broken trunk.
status:
  - packet_id: local-gate-obligation-missing-from-the-loop
    field: status
    value: completed
    ts: "2026-08-09T10:40:00Z"
    host: forge
packets:
  - packet_id: local-gate-obligation-missing-from-the-loop
    order: 598-znuv
    status: completed
    desired_release: v0.5
    role: methodology
    pickup_role: linux
    priority: p0
    tags:
      - ci
      - local-gate
      - skills
      - enforcement
      - cross-platform
    title: "Push CI is gone and the loop that replaced it never mentions the local gate."
    outcome: >
      The loop skill every host runs now names ./build.sh --check as a
      Finalization obligation with the command, AGENTS.md and its symlinked
      instructions no longer cite a nonexistent push-CI safety net, and the
      github-actions-budget litmus pins the skill claim so a future loop edit
      that drops the gate goes RED.
    next_action: >
      None — packet complete. Operators are reminded the durable release-path
      gate is ./build.sh --ci-full run locally before a PR (methodology/ci.yaml
      agent_obligation), which now stands on the named Finalization obligation.
    exit_criteria:
      - "skills/meta-orchestration/SKILL.md names the gate between Finalization steps 3 and 4, as an obligation with a command, not an advisory — PROVEN: Finalization step 4 'Run the local pre-push gate: ./build.sh --check ... Do not push past a red gate', between validate-YAML (3) and commit (5)"
      - "AGENTS.md:44 corrected — the CI workflow no longer exists on any working branch and must not be cited as a safety net — PROVEN: no 'CI workflow will mark it red' match in AGENTS.md; 'ok: no push-CI claim' litmus step; GEMINI.md + copilot-instructions.md inherit via symlink"
      - "litmus:github-actions-budget extended with a check that the loop skill actually names the gate. RED before the edit, GREEN after; prove both — PROVEN: sed-scoped grep returns no match on a gated-out Finalization (RED) and 'ok: loop skill names the gate' on the edited skill (GREEN); all 7 steps of the litmus run green except the pre-existing stdlib-bound count step"
      - "Record the query_packets clippy failure as the evidence case — PROVEN: plan/issues/local-gate-evidence-query-packets-clippy-2026-08-09.md with the main.rs:327 always-true expression and its timeline"
    events:
      - type: filed
        ts: "2026-08-03T20:30:00Z"
        agent_id: linux-mutable-macuahuitl-opus5-20260803
        host: linux_mutable
        summary: Filed as p0. The 606-line loop skill contains exactly one build.sh mention and zero --check/--ci-full; the obligation lives only in files a platform agent may never read. This cycle's gate refusal (always-true clippy expression on linux-next, landed the day push CI was removed) is the load-bearing evidence.
      - type: progress
        ts: "2026-08-09T10:40:00Z"
        agent_id: forge-20260809t104000z
        host: forge
        summary: Implemented skill step, AGENTS.md correction, and litmus extension; proved RED/GREEN.
