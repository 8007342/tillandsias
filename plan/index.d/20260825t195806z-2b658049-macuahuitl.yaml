# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: macos-unbuffered-bulk-io-paths
    field: pickup_role
    value: macos
    ts: "2026-08-25T19:58:06Z"
    host: macuahuitl

events:
  - packet_id: macos-unbuffered-bulk-io-paths
    event:
      type: note
      ts: "2026-08-25T19:58:06Z"
      host: macuahuitl
      summary: >
        every exit criterion needs a Darwin host: three sites run only on cold macOS provision, the fourth is vz.rs boot-path code, and criterion 2 forbids unmeasured claims
