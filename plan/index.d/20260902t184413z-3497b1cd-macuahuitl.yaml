# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: full-gate-pass-livelocks-against-a-fast-moving-trunk
    field: status
    value: completed
    ts: "2026-09-02T18:44:13Z"
    host: macuahuitl

events:
  - packet_id: full-gate-pass-livelocks-against-a-fast-moving-trunk
    event:
      type: completed
      ts: "2026-09-02T18:44:13Z"
      host: macuahuitl
      summary: >
        status 'completed' with evidence_refs: b300d6b92: scripts/gate-stamp.sh excludes plan/index.d/*.yaml, plan/loop_status.d/*.md, plan/mo-full-attestations.d/*.md from the stamp; scripts/test-gate-stamp.sh 'PASS: gate-stamp (9/9)' — case 7 a committed fragment+loop-status+attestation burst leaves the stamp fresh, case 8 a base-ledger or code change stales it; the other four gate-stamp fixtures unchanged and green
  - packet_id: full-gate-pass-livelocks-against-a-fast-moving-trunk
    event:
      type: note
      ts: "2026-09-02T18:44:13Z"
      host: macuahuitl
      summary: >
        closure by a different mechanism than the deliverable named: no merge-check-push helper is needed when the stamp ignores the fast lane — one gate pass survives any plan-only burst, and code movement still re-gates (the negative control)
