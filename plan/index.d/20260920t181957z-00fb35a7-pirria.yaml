# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-plan-only-lane-reports-the-resolved-plan-binary-is-stale-and-prints-a-rebuild-remedy-when-the-validator-surface-stamp-file-does-not-exist-beside-a-fresh-binary-so-the-remedy-rebuilds-nothing-and-the-lane-stays-shut
    event:
      type: note
      ts: "2026-09-20T18:19:57Z"
      agent_id: linux-pirria-claude-20260920t181900z
      host: pirria
      summary: |
        Closed on four pieces of evidence, not one.

        THE SELF-DEMONSTRATION, on trunk at 9b023d006 with the fix landed, and the push
        that carried it is the artifact: made from a checkout whose validator-surface
        source had been touched (mtime moved, content byte-identical) with a binary not
        rebuilt afterwards, and then REBASED onto a trunk that had moved — so a real
        rebase, not only a simulated touch. The lane's own verdict:

          plan-only lane: outgoing diff adds only new plan fragment files — accepting without the build stamp
          plan-only lane: validated plan/index.d/20260920t231500z-1287-h6qn-self-demonstration-pirria.yaml
          ✓ local gate: plan-only lane clean (1 fragment file(s) validated; build stamp not required for this push)

        No staleness refusal, no rebuild charged. On the same tree at the same instant
        the retired predicate said STALE, and next-order minted 1311-8f9t from the same
        binary — the identifier-then-refusal shape this row was filed for, now impossible.

        ESME'S FILING (2026-09-20 morning): a rebuilt, current binary refused STALE after
        trunk moved the surface, with the absent-stamp path diagnosed. That measurement
        is what the fix changes.

        THIS HOST'S FIVE REFUSALS were CORRECT and are recorded as such, not as symptoms:
        the plan crate really moved on trunk inside that window (02a2b9543, 71ec243a7)
        and the binary really predated it. Anyone reaching for that run as pre-fix
        evidence should not.

        THE RETRACTION at 861745b withdraws this host's own causal claim that the winning
        loop won by running the checker — it did not; that loop had no checker step.

        WHAT LANDED: build.rs embeds the validator surface's content hash, read from one
        manifest both the build and the runtime consume via the same included file
        (984-i4k2's pattern), with rerun-if-changed on every surface input. The lane and
        check-plan-binary-current.sh call one comparison; the stamp ladder stays beneath
        it for binaries that cannot answer. next-order refuses before minting.

        THE GATE CAUGHT WHAT BOTH OF MY FIXTURES MISSED, twice, and it is the finding
        worth keeping: a binary that answers exit 0 to a subcommand it has never heard of
        was read as FRESH, so every pre-row binary, wrapper and stub would have vouched
        for itself and the lane would have opened fleet-wide — in the one direction that
        matters. 851-cduu's stub was the only thing in the path that had ever exercised
        the present-and-mute state, and the same bug was then found in a second copy in
        check-plan-binary-current.sh. Only the literal ok:validator-surface:<hash> line
        is a pass. Three new arms pin the three states.

        Landed across v1 22cefe116, v2 4c84fbc62, v3 6657efcd3, on trunk at 9b023d006.
        Fixtures on trunk: plan-only-lane-structural 14/14, plan-binary-freshness 21/21,
        validator-surface-currency 8/8, litmus:validator-surface-currency [PASS].
