# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-20260809t092200z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# ORDER 626-zmhz (folded-loop-status-renderer-and-count-gate) CLOSED in the
# forge cycle that implemented the renderer/check. The gate now runs every
# coordinator cycle and the operator-owned release prose on plan/loop_status.md
# was corrected to the folded truth. Note the stale-number correction: the
# 2026-08-09T06:30Z split fragment's next_action quoted "254 total, 188
# nonterminal" from plan/issues/v05-refactor-dependency-freshness-audit-2026-08-05.md;
# today's FOLDED v0.5 count is 298 total / 217 nonterminal (178 ready, 79
# completed, 21 pending, 15 in_progress, 2 needs_clarification, 2 obsoleted,
# 1 blocked). The packet prose is immutable; this fragment records the
# correction as the completion evidence.
events:
  - packet_id: folded-loop-status-renderer-and-count-gate
    event:
      type: completed
      ts: "2026-08-09T09:22:00Z"
      agent_id: forge-20260809t092200z
      host: forge
      summary: >
        CLOSED. All three exit criteria proven on the real ledger.
        (1) loop-status-verify reads the FOLDED loop_status prose: exactly one
        ## ACTIVE RELEASE heading, exactly one `— ACTIVE`-labeled release
        bullet, and that bullet IS the active release. (2) The active release's
        (N open / M total tagged) count is derived from count_release() over the
        FOLDED ledger (base plus index.d fragments), not hand-maintained prose.
        (3) The fixture test a_packet_fragment_changes_the_rendered_release_count
        proves a fragment-born packet changes the rendered count and stale
        output fails. Parser robustness: the release-bullet matcher rejects
        prose bullets like `- **v0.5 work seeded**` (a version token must be
        followed by `— ACTIVE` or a count parenthetical) and `v0.9x` (version
        must be followed by whitespace). On the real ledger the stale prose
        (v0.4 labeled ACTIVE, v0.5 committed 87/94) failed the gate; after
        fixing plan/loop_status.md to `v0.5 — ACTIVE (217 open / 298 total
        tagged)` and removing the stale ACTIVE label from v0.4 the gate
        reports verdict=ok. cargo test -p tillandsias-plan passes 116 lib + 8
        usage/doc; litmus:loop-status-fragment-overlay gained three ORDER
        626-zmhz pins (consistent fixture verdict=ok, stale count fails loud,
        stale ACTIVE label fails loud). Loop-status-verify is a READ-ONLY
        command, so a coordinator runs it every cycle; --render prints the
        canonical count line for the splice.
status:
  - packet_id: folded-loop-status-renderer-and-count-gate
    field: status
    value: completed
    ts: "2026-08-09T09:22:00Z"
    host: forge
packets:
  - packet_id: folded-loop-status-renderer-and-count-gate
    order: 626-zmhz
    status: completed
    desired_release: v0.5
    priority: p0
    title: "Deterministic folded-loop-status renderer/check for ACTIVE RELEASE and v0.5 counts"
    kind: plan-tooling+standing-gate
    parent_order: 606-46x9
    depends_on:
      - loop-status-readme-exclusion-from-fragment-parse
    pickup_role: any
    capability_tags:
      - plan
      - release
      - loop-status
      - fragments
      - metrics
      - testing
    estimated_hours: 2
    outcome: >
      Every orchestration cycle reports one unambiguous active release and v0.5
      totals/status counts derived from the FOLDED ledger (base plus fragments),
      never hand-maintained prose. Stale numbers fail before they can guide
      packet selection. Implemented as the read-only `loop-status-verify`
      command on tillandsias-plan: it folds the loop_status base plus its
      fragments, asserts exactly one ACTIVE RELEASE heading, exactly one
      `— ACTIVE`-labeled release bullet naming that release, and re-derives the
      active release's `(N open / M total tagged)` from count_release() over
      the folded plan ledger. The operator-owned release section of
      plan/loop_status.md was corrected to the folded truth (v0.5 — ACTIVE,
      217 open / 298 total tagged; v0.4 ACTIVE label removed).
    next_action: >
      None — packet complete. The gate is READ-ONLY and safe to run on every
      coordinator cycle; plan/loop_status.md release bullets must carry the
      count parenthetical rendered by `tillandsias-plan loop-status-verify
      --render` and the `— ACTIVE` label must sit on the active release's own
      bullet.
    exit_criteria:
      - "loop status has exactly one ACTIVE RELEASE declaration and no contradictory active-release prose — PROVEN: loop-status-verify on the real ledger reports headings=1 actives=1 verdict=ok; the stale v0.4 — ACTIVE label failed before the fix"
      - "v0.5 totals/status counts match tillandsias-plan's folded ledger on every coordinator cycle — PROVEN: count gate re-derives (217 open / 298 total tagged) from the folded ledger; stale 87/94 failed"
      - "a fixture adding a packet fragment changes the rendered count and makes stale output fail — PROVEN: a_packet_fragment_changes_the_rendered_release_count + litmus fixture changes (1,2)→(2,3) and the stale count fails loud"
    events:
      - type: filed
        ts: "2026-08-09T06:30:00Z"
        agent_id: forge-20260809t063000z
        host: forge
        summary: >
          Shaping child of 606-46x9 holding exit criteria 1, 2 and 4 (the
          deterministic ACTIVE RELEASE + v0.5 count renderer/check). Split at a
          verification boundary from the README-exclusion slice (626-bnn5) so
          the forge cycle budget could close one criterion now and leave the
          rest as a ready, dependency-clear packet.
      - type: progress
        ts: "2026-08-09T09:22:00Z"
        agent_id: forge-20260809t092200z
        host: forge
        summary: >
          Implemented. crates/tillandsias-plan: loop_status.rs gained
          ReleaseBullet, release_bullets(), active_release_headings(),
          render_count_line() and verify_active_release() plus seven tests;
          lib.rs gained count_release(ledger, release) with the criterion-3
          fixture; main.rs wired the read-only `loop-status-verify` subcommand
          (--ledger override, --render splice helper); capabilities.txt
          declares loop-status-verify. FOLDED v0.5 truth corrected from the
          stale 254/188 quoted at split time to 298 total / 217 nonterminal
          (the 2026-08-05 audit predates packets filed since).
      - type: completed
        ts: "2026-08-09T09:22:00Z"
        agent_id: forge-20260809t092200z
        host: forge
        summary: >
          Verified end-to-end. cargo test -p tillandsias-plan: 116 lib + 8
          usage/doc green. Real-ledger run: before the prose fix
          loop-status-verify reported headings=1 actives=1 count_ok=no counts=
          \"(217 open / 298 total tagged)\" verdict=fail (v0.5 bullet not
          labeled ACTIVE, v0.4 stale ACTIVE, count stale 87/94); after editing
          plan/loop_status.md to the folded truth it reports count_ok=yes
          verdict=ok. litmus:loop-status-fragment-overlay pinned with three
          ORDER 626-zmhz steps (consistent fixture verdict=ok; stale count
          fails loud with count_ok=no; stale ACTIVE label fails loud).
