# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-guest-reports-metal-from-a-compile-time-cfg-that-cannot-hold-in-a-linux-aarch64-guest
    field: title
    value: Guest reports Metal from a compile-time cfg that cannot hold in a linux-aarch64 guest
    ts: "2026-09-12T23:23:30Z"
    host: macos

events:
  - packet_id: the-guest-reports-metal-from-a-compile-time-cfg-that-cannot-hold-in-a-linux-aarch64-guest
    event:
      type: note
      ts: "2026-09-12T23:23:30Z"
      host: macos
      summary: |-
        check --strict-fragments emitted 'missing required field title' for this packet; adding it clears one of three schema-drift advisories
