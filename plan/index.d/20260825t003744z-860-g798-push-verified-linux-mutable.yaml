# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-25T00:37:44Z.
status:
  - packet_id: credential-guard-passes-green-while-git-push-blocks-on-an-interactive-helper
    field: status
    value: completed
    ts: "2026-08-25T00:37:44Z"
    host: linux_mutable

events:
  - packet_id: credential-guard-passes-green-while-git-push-blocks-on-an-interactive-helper
    event:
      type: completed
      ts: "2026-08-25T00:37:44Z"
      agent_id: linux-macuahuitl-opus5-20260825T003400Z
      host: linux_mutable
      evidence_refs:
        - "scripts/check-credential-channel.sh — the gh arm now runs a bounded non-interactive `git push --dry-run` (GIT_TERMINAL_PROMPT=0, GCM_INTERACTIVE=never, GIT_ASKPASS=/bin/false, timeout 45)"
        - "verdicts: ok:gh-keyring-push-verified | blocked:interactive-credential-helper (remedy printed, the measured 18s esmeraldinha fix) | blocked:gh-cli-only"
        - "scripts/test-check-credential-channel.sh — 6/6 hermetic: incident shape blocked+named, distinct verdict, verified ok, store-arm regression, MUTATION (pre-fix arm passes the incident shape — the suite has teeth)"
        - "LIVE fresh-clone rehearsal: shallow clone from GitHub, no .gh-credentials, no helper -> ok:gh-keyring-push-verified via a real authenticated dry-run push"
        - "skills/meta-orchestration/SKILL.md — Credential Channel Guard section exports GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=never around every push (EC3)"
      merge_affordance: merge-now
      merge_reason: >
        Every resumed sibling enters through a fresh clone, which is exactly the
        population this guard lied to. The last named day-one resume risk.
      summary: >
        NOTE ON OWNERSHIP: this row was yolanda's beat (filed from esmeraldinha,
        Windows family). Both hosts are stopped by the operator, who directed
        the sole remaining host to take over open work — absorbed on that
        licence, not silently.

        THE GUARD NOW PROVES THE THING IT CLAIMED. `gh auth status` proves the
        gh CLI holds a token; it says nothing about git's helper chain, and on
        a fresh clone that chain resolves to the interactive Git Credential
        Manager — so the guard said "channel healthy" and the first push hung
        >10 minutes on a prompt no unattended session can answer. The fix runs
        a bounded, non-interactive, dry-run PUSH — the actual operation the
        cycle will need — and only then says ok, with a new verdict name so no
        consumer can mistake the old semantics for the new
        (ok:gh-keyring-push-verified).

        When the probe fails, the failure is legible THE FIRST TIME: an
        interactive-only helper is named (blocked:interactive-credential-helper)
        with the measured remedy printed — seed the repo-local store from
        `gh auth token`, add the EMPTY helper entry that drops the system
        manager (the subtlety that cost esmeraldinha its second diagnosis
        pass), then the store helper. An unexplained probe failure gets its own
        verdict (blocked:gh-cli-only) rather than a bare ok.

        All four exit criteria measured: probe not config-inspection (EC1),
        helper named with remedy (EC2), the skill closes the interactive escape
        hatches around every push so credential failures are fast and loud
        (EC3), and the hermetic fixture's incident shape — gh green +
        interactive helper — is blocked, with a MUTATION reconstruction proving
        the pre-fix arm passes that same shape (EC4).

        LIVE REHEARSAL of the exact day-one path: a fresh shallow clone from
        GitHub on this host, no .gh-credentials, no helper configured — the
        probe ran a real authenticated dry-run push and returned the verified
        ok. On a GCM host the same probe fails in seconds instead of hanging
        for ten minutes; that asymmetry is the entire fix.
