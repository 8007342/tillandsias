# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by windows (yolanda), 2026-08-23T02:00:50Z.
packets:
  - order: 851-cduu
    packet_id: dual-locus-preflight-must-rebuild-every-gate-locus
    desired_release: v0.5
    title: "cycle-preflight rebuilds the native instrument while the gate tests the WSL-cached one — a 6-day-stale destroyer passed for HEAD"
    status: ready
    kind: bug
    pickup_role: windows
    priority: p2
    capability_tags: [windows, wsl2, build, preflight, ledger, probe]
    deliverable: scripts/cycle-preflight.sh
    depends_on: []
    estimated_hours: 3
    context: >
      Measured 2026-08-23 on yolanda, first ./build.sh --check after
      fast-forwarding windows-next 177 commits to origin/linux-next:
      compaction-coverage went red 6/23 because the gate runs inside WSL
      where plan-binary-probe (correctly, 783-jdeh) resolves
      CARGO_TARGET_DIR=/root/.cache/tillandsias-wsl2-target/tillandsias —
      a tillandsias-plan built 2026-08-16, six days behind HEAD, predating
      843-624y. cycle-preflight had just reported ok:rebuilt, but it rebuilt
      only the native Windows ./target lane: "rebuilt" is locus-relative and
      nothing says so. The stale binary's compact ignores --help/--dry-run/
      unknown args and deletes every fragment it LOADED; invoked once during
      diagnosis with the repo as cwd it deleted all 16 live plan/index.d/
      fragments (restored from HEAD, zero loss, nothing pushed). Details:
      plan/issues/enhancement-dual-locus-preflight-stale-wsl-instrument-2026-08-23.md
    exit_criteria: >
      (1) After cycle-preflight on a dual-locus host, the binary
      plan-binary-probe resolves INSIDE the gate's WSL execution context is
      built from the current checkout (verifiable handshake, e.g.
      plan_binary_has on a subcommand introduced at that head, or a
      source-vintage probe). (2) A stale resolved binary produces a distinct
      stale-instrument verdict, never a behaviour-fixture red — 702-68zj
      names the conflation. (3) A hermetic fixture pins the stale-lane shape
      so it cannot regress silently.

events:
  - packet_id: joining-host-must-publish-a-capability-row
    event:
      type: note
      ts: "2026-08-23T02:00:50Z"
      agent_id: windows-yolanda-fable5-20260823T014638Z
      host: windows
      summary: >
        yolanda refreshed BOTH its capability rows at probe v0.4.260817.1
        (fragments 20260823t015456z-49df4525-yolanda.yaml windows-host,
        20260823t015759z-61d6612c-yolanda.yaml in-guest), and answered the
        coordinator's open question: the npu wsl2-npu-not-exposed and gpu
        wsl2-no-dri-render-node limits are WSL2-universal, NOT
        Intel-specific — yolanda is itself AMD (Ryzen AI 7 350, XDNA NPU
        VEN_1022&DEV_17F0, Radeon 860M VEN_1002); the guest PCI bus carries
        only Microsoft 0x1414 3D-controller paravirtual functions, no DRM
        driver binds (no /dev/dri for any vendor), and no PCI passthrough
        exists (no /dev/accel* possible). The fleet's Intel N100 is
        esmeraldinha, still silent; its future row confirms the Intel half.
        Full evidence:
        plan/issues/research-wsl2-accel-limits-vendor-universality-2026-08-23.md.
        Caveat for this packet's design: in-guest rows are DISTRO-dependent
        (ollama present in the 08-18 probe context, absent in
        tillandsias-build today, so yolanda's in-guest schedulable set
        dropped to none) and the host+locus fold key cannot express which
        guest was probed — decide the canonical guest or key the distro.
