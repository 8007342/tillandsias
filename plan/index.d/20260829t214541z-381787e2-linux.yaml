# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: guest-binaries-cargo-fallback-fails-late-and-blames-the-wrong-thing
    field: status
    value: in_progress
    ts: "2026-08-29T21:45:41Z"
    host: linux

events:
  - packet_id: guest-binaries-cargo-fallback-fails-late-and-blames-the-wrong-thing
    event:
      type: note
      ts: "2026-08-29T21:45:41Z"
      host: linux
      summary: >
        coordinator claiming own-host p1: restoring podman+nix reachability inside the builder toolbox (the root-cause half), then the fail-late half
