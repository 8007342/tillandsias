# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: windows-lane-sends-a-verbatim-argv-and-drops-its-escaping-workarounds
    event:
      type: progress
      ts: "2026-09-04T20:19:34Z"
      agent_id: windows-yolanda-opus5-20260904t201934z
      host: yolanda
      summary: |-
        CRITERION 1 MET, CRITERION 2 NOT OBSERVED — and I am recording the three observables that FAILED so the next agent does not spend a window rediscovering them. Packet stays OPEN and the claim is released.

        THE CLOSURE HAS TWO HALVES: "a source scan finds no argv_survives_wt_reparse and no wt_safe_title, AND a Windows lane launch is OBSERVED opening in Windows Terminal rather than conhost."

        CRITERION 1: MET. A workspace-wide scan finds no CODE bearing either name. The three surviving mentions are all comments recording the deletion — pty/mod.rs:382, notify_icon.rs:3932 and :4219 — not call sites. Deleted in c3f6591a1 ("delete argv_survives_wt_reparse and wt_safe_title (836-smm2)"). The tray now routes every non-login lane through spawn_wsl_terminal (wt.exe) unconditionally; only PtyIntent::GithubLogin takes the plain-console path, which is a deliberate credential-safety decision from the 2026-08-09 Esmeralda crashes, not a fallback.

        CRITERION 2: NOT OBSERVED. Three observables tried, all wrong, each for a different reason:

          (a) COUNT WindowsTerminal PROCESSES BEFORE AND AFTER. Wrong: Windows Terminal REUSES ONE PROCESS. A new window is a tab inside the existing pid, so the count is unchanged whether the lane opened in wt or not. Before=1, after=1, and that number is compatible with both outcomes.

          (b) INSPECT wsl.exe PARENTAGE. Wrong twice. The two wsl.exe processes with a plausible-looking parent traced back to claude.exe -> cmd.exe — they were MY OWN tool calls, not the tray's lane. And the tray's lane left no wsl.exe at all, because a `--forge <p> --shell` into a project that does not exist opens and exits immediately.

          (c) READ THE TRAY LOG. Wrong: %LOCALAPPDATA%\tillandsias\logs\tray.log has no entry after 14:51Z, so the headless --forge path does not write it. No record of the spawn decision exists to read.

        WHAT WOULD ACTUALLY OBSERVE IT: a human clicking a project lane in the tray menu and seeing which terminal appears, or an instrumented build that logs the chosen spawn path. Neither is available to a headless session on this host. That is the honest state of criterion 2, and it is why the packet is not closed: the source half is verifiable and the observation half needs an instrument this session does not have.

        I ALMOST CLOSED IT ON (a). A count that does not move reads as "nothing happened", and "no new conhost either" would have felt like corroboration. Both readings are worthless because the instrument cannot distinguish the outcomes — the same proxy-read-as-the-thing shape the fleet has hit repeatedly today, this time in my own verification rather than in a test.

        INCIDENTAL, AND DELIBERATELY NOT CLAIMED AS EVIDENCE. To make the lane persist I created /home/forge/src/smoke-observe in the guest; the tray QUARANTINED it, renaming it to smoke-observe.invalid-1788553078 in the host's ~/src. That is the 997-e4v2 quarantine path firing on the shipped release. It says NOTHING about whether the three-state classifier fixed it: the installed v56.9.2.1 (d6d3e3ed9) PREDATES e8a995a41, and my directory was an empty dir which is TRUTHFULLY not a checkout, so the fixed classifier would quarantine it too. The observation cannot discriminate and is recorded as context only. Residue removed; the host's ~/src is empty and was empty before, which is itself consistent with the operator's ruling that it is abandoned.

        NEXT ACTION for whoever takes this: criterion 1 needs no further work. For criterion 2, either get an operator to click a lane and report the terminal, or add a one-line log at the spawn_wsl_terminal / spawn_wsl_console branch so the decision is readable from the log — the latter is small, permanent, and makes this criterion checkable by any host forever instead of once by a human.
