# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: expert-fragment-provenance-and-freshness
    order: 606-h9vy
    status: ready
    desired_release: v0.5
    release_target: forge-local-experts-milestone
    priority: p0
    title: "EXPERT correctness: cite winning fragment fields, verify authority values, and compute freshness over the folded corpus"
    kind: fix+litmus
    deliverable: >
      crates/tillandsias-plan folded source map + answer/verify/freshness
      corrections + fragment provenance ground-truth fixtures
    depends_on: []
    pickup_role: linux
    capability_tags: [experts, plan, fragments, citations, security, rust, testing]
    estimated_hours: 5
    outcome: >
      Exact expert answers cite the source that actually won each folded field.
      Fragment-only packets and LWW overrides never borrow stale base spans,
      verifier tampering is rejected, and freshness represents base plus every
      folded fragment rather than plan/index.yaml alone.
    next_action: >
      Add a per-packet/per-authoritative-field source map to Ledger folding,
      reproduce packet 570's ready->in_progress stale citation and a
      fragment-born packet with no span, then make both cite their winning
      fragment and turn authority tampering red.
    exit_criteria:
      - "a fragment-only packet receives an exact cited status answer whose path and span resolve to the fragment that created or last changed it"
      - "a base packet overridden by an LWW fragment cites the winning fragment for status; no exact answer cites a contradictory base value"
      - "verify-answer rejects a fabricated authority order, packet_id, or status and rejects a citation whose span does not substantiate that authority"
      - "freshness is derived from base plus fragments (including git worktree commondir layouts), and adding a fragment changes its digest/time"
      - "ground truth contains fragment-only, LWW override, tamper-negative, and zero-omitted-row fixtures"
    owned_files:
      - crates/tillandsias-plan/src/fragments.rs
      - crates/tillandsias-plan/src/answer.rs
      - crates/tillandsias-plan/src/groundtruth.rs
      - openspec/litmus-tests/litmus-expert-groundtruth-harness.yaml

  - packet_id: plan-expert-release-and-upstream-query-primitives
    order: 606-e2hg
    status: ready
    desired_release: v0.5
    release_target: forge-local-experts-milestone
    priority: p0
    title: "EXPERT query primitives preserve release constraints and distinguish upstream prerequisites from downstream consumers"
    kind: fix+enhancement
    deliverable: >
      tillandsias-plan query/answer release filters, JSON projection, upstream
      dependency primitive, MCP schemas, and ground-truth cases
    depends_on: []
    pickup_role: linux
    capability_tags: [experts, plan, release, dependencies, mcp, rust]
    estimated_hours: 4
    outcome: >
      MCP consumers can filter and project desired_release/release_target, and
      "what blocks X" returns X's own nonterminal prerequisites rather than
      packets that happen to depend on X. Unknown or unsupported constraints
      fail explicitly instead of being ignored.
    next_action: >
      First pin the three observed RED queries, then add --release plus
      dependencies-of/blocked-on primitives and thread them through JSON and
      forge-plan tool schemas without changing existing downstream closure.
    exit_criteria:
      - "query JSON includes desired_release and release_target and supports an exact release filter"
      - "what v0.5 work can I do on linux returns no packet outside v0.5"
      - "what blocks forge-local-experts-milestone returns its own nonterminal depends_on rows, not downstream consumers"
      - "unknown release/argument constraints produce a typed error and are never silently ignored"
      - "committed ground truth drives the real MCP server for release and upstream semantics"

  - packet_id: plan-expert-actionable-next
    order: 606-xu52
    status: pending
    desired_release: v0.5
    release_target: forge-local-experts-milestone
    priority: p0
    title: "EXPERT cold start: deterministic plan_next and natural 'what's next?' return at most five claimable actions"
    kind: implementation+litmus
    deliverable: >
      tillandsias-plan plan_next selector + forge-plan MCP tool and natural
      aliases + ranked concise answer envelope
    depends_on:
      - expert-fragment-provenance-and-freshness
      - plan-expert-release-and-upstream-query-primitives
    pickup_role: linux
    capability_tags: [experts, plan, cold-start, ranking, mcp, rust]
    estimated_hours: 5
    outcome: >
      A newly landed agent asks "what's next?" and receives no more than five
      cited, release-aware, role/capability-compatible, dependency-clear and
      unleased work packets, each with a selection reason and smallest next
      action. Criteria holders and milestones are never offered as claims.
    next_action: >
      Specify the deterministic ranking tuple (active release, priority,
      release target, dependency clearance, host capability, lease/file-scope
      availability, order) and seed the user's exact phrases before adding the
      MCP surface.
    exit_criteria:
      - "plan_next defaults release from the folded ACTIVE RELEASE and accepts explicit release/role/limit filters with limit capped at five"
      - "natural aliases include exactly 'what's next?' and 'what v0.5 work can I do on linux?'"
      - "terminal dependencies, incompatible host roles, active leases/file claims, completed work, milestones, and criteria holders are excluded"
      - "every result is cited and includes why it ranked plus a concrete next action; the envelope stays below a committed size budget"
      - "ground truth proves deterministic ordering and a typed no-work result"

  - packet_id: generic-project-expert-bootstrap-contract
    order: 606-z389
    status: ready
    desired_release: v0.5
    release_target: forge-local-experts-milestone
    priority: p0
    title: "EXPERTS product contract for arbitrary projects: warmed generic engine, ephemeral project index, one natural answer surface"
    kind: design+openspec
    deliverable: >
      OpenSpec and implementation plan for an image-baked project-agnostic
      expert bootstrap using TILLANDSIAS_PROJECT_PATH
    depends_on: []
    pickup_role: any
    capability_tags: [experts, generic-project, bootstrap, rag, mcp, openspec]
    estimated_hours: 4
    outcome: >
      New projects, fresh sessions, cleared contexts, and scripted prompts all
      receive a warmed expert surface without knowing repository type or
      reading project files manually. Tillandsias-specific plan experts remain
      an additive specialization rather than the only supported project shape.
    next_action: >
      Amend the current deliberate no-plan-crate/n-a contract: define the
      image-baked generic engine, ephemeral discovery/index lifecycle,
      readiness SLA, uniform project_answer envelope, and deterministic
      fallback when local inference is unavailable.
    exit_criteria:
      - "active specs define zero-intervention bootstrap for an arbitrary git and non-git project using TILLANDSIAS_PROJECT_PATH"
      - "the project index and any learned state are ephemeral and rebuilt/refreshed from the active checkout without persisting stale knowledge"
      - "one project_answer/what's-next surface returns cited project status, type, commands, and actions without requiring the caller to select a project-specific tool"
      - "startup blocks neither harness launch nor user prompts indefinitely; readiness/degraded states are explicit and machine-readable"
      - "a fresh non-Tillandsias fixture proves the expert is usable through each supported harness with no manual registration"

  - packet_id: project-info-jsonrpc-contract-parity
    order: 606-qh3f
    status: ready
    desired_release: v0.5
    release_target: forge-local-experts-milestone
    priority: p1
    title: "project-info MCP must share forge-plan's strict JSON-RPC framing, validation, and degraded-state contract"
    kind: fix+litmus
    deliverable: >
      project-info.sh safe framing/error helpers and multi-frame behavioral
      protocol fixture
    depends_on: []
    pickup_role: linux
    capability_tags: [mcp, project-info, jsonrpc, shell, testing, experts]
    estimated_hours: 3
    outcome: >
      Project metadata calls preserve arbitrary JSON-RPC IDs and escaping,
      reject unknown tools/arguments with typed errors, and distinguish an
      unavailable plan engine from a valid empty query.
    next_action: >
      Reuse or extract forge-plan's one-line JSON emitters, then drive numeric,
      escaped-string, unknown-tool, invalid-param, unavailable-engine and empty
      result frames through one long-lived server process.
    exit_criteria:
      - "numeric/string/null IDs round-trip with their original JSON types and escaping; every response is strict one-object-per-line JSON"
      - "unknown tool returns -32601 and invalid/unknown params return a typed validation error naming the field"
      - "missing binary/index is a distinct degraded service result, never 'No matching packets found'"
      - "project metadata skips Markdown fences and reports a stable structured project shape"
      - "a behavioral multi-frame fixture covers all failure and recovery paths"

  - packet_id: reqwest-rustls-webpki-security-migration
    order: 606-qac3
    status: ready
    desired_release: v0.5
    priority: p0
    title: "SECURITY: migrate reqwest/rustls off rustls-webpki 0.101.7 (three current RustSec vulnerabilities)"
    kind: dependency-security
    deliverable: >
      workspace reqwest/rustls migration, compatible lock refresh, network/TLS
      regression tests, and zero-vulnerability cargo-audit evidence
    depends_on: []
    pickup_role: linux
    capability_tags: [security, rust, dependencies, tls, cross-platform]
    estimated_hours: 5
    outcome: >
      The shipped host/tray/Vault/VM dependency graph contains none of
      RUSTSEC-2026-0098, RUSTSEC-2026-0099, or RUSTSEC-2026-0104 and clears
      compatible anyhow/event-listener/memmap2 unsoundness updates without
      regressing TLS trust, proxying, or platform builds.
    next_action: >
      Upgrade the workspace reqwest/rustls tail to a webpki >=0.103.13-capable
      line, enumerate API changes at each consumer, then run all TLS/proxy
      fixtures and cargo audit before refreshing unrelated compatible locks.
    exit_criteria:
      - "cargo audit reports zero vulnerabilities and none of the three named advisories appears in Cargo.lock"
      - "workspace/all-target checks and Linux tray tests pass; Windows and macOS compile gates remain green"
      - "Vault CA trust, enclave proxy TLS, downloads, and GitHub API client fixtures pass on the upgraded stack"
      - "compatible patched anyhow, event-listener, and memmap2 releases land with their unsoundness advisories cleared"

  - packet_id: nix-supported-release-and-crane-migration
    order: 606-um5s
    status: ready
    desired_release: v0.5
    priority: p1
    title: "Move the forge flake off EOL NixOS 24.11 and refresh crane on a supported nixpkgs release"
    kind: dependency-security+build
    deliverable: flake.nix + flake.lock + x86_64/aarch64 build evidence
    depends_on: []
    pickup_role: any
    capability_tags: [nix, dependencies, security, forge, multiarch]
    estimated_hours: 4
    outcome: >
      Nix-built forge artifacts use a security-supported NixOS/nixpkgs base and
      compatible crane without losing reproducibility or multi-architecture
      outputs.
    next_action: >
      On a Nix-capable host, choose the current supported stable nixpkgs line,
      update crane and the lock together, inspect closure changes, and build all
      default x86_64-linux and aarch64-linux outputs.
    exit_criteria:
      - "flake inputs pin a currently security-supported NixOS/nixpkgs release and a compatible maintained crane"
      - "flake check and default forge outputs build for x86_64-linux and aarch64-linux"
      - "the migration records old/new closure sizes and verifies no user runtime dependency or tool disappears"

  - packet_id: stale-browser-mcp-artifacts-cleanup
    order: 606-3e2u
    status: ready
    desired_release: v0.5
    priority: p1
    title: "Remove mock-only browser MCP JS and the unbuildable 27 MB x86 ELF from multi-arch forge outputs"
    kind: cleanup+build-fix
    deliverable: >
      images/default stale browser artifacts removal, flake references cleanup,
      live host-browser bridge regression gate
    depends_on: []
    pickup_role: linux
    capability_tags: [cleanup, mcp, browser, nix, multiarch, stale-code]
    estimated_hours: 3
    outcome: >
      The default image and flake ship only the live shell/Rust host-browser MCP
      bridge; no opaque architecture-specific binary or disconnected mock
      masquerades as a production server.
    next_action: >
      Prove both files have no live caller, remove their flake/image references,
      and make the existing host-browser behavioral fixture the sole supported
      path before deleting the artifacts.
    exit_criteria:
      - "images/default/mcp-server-browser.js and tillandsias-mcp-browser are absent with no dangling build or documentation references"
      - "flake outputs contain no untracked/unbuilt architecture-specific MCP binary and build on x86_64/aarch64"
      - "the live host-browser shell bridge and Rust host socket behavioral tests remain green"
      - "image-size evidence records the removed payload"

  - packet_id: macos-dead-vz-lifecycle-removal
    order: 606-r42f
    status: ready
    desired_release: v0.5
    priority: p1
    title: "Remove or reconnect dead macOS VzLifecycle code that reaches two unimplemented provisioning panics"
    kind: refactor+cleanup
    deliverable: >
      tillandsias-macos-tray VzLifecycle and vm-layer legacy provisioning-path
      cleanup plus live-path parity tests
    depends_on: []
    pickup_role: macos
    capability_tags: [macos, vm-layer, refactor, stale-code, testing]
    estimated_hours: 4
    outcome: >
      macOS exposes one real provisioning lifecycle. No retained abstraction
      advertises a callable path that panics, and VM-layer documentation names
      the action_host/qcow2 implementation actually used in cold e2e.
    next_action: >
      Map every VzLifecycle/VzRuntime::provision caller, choose deletion or
      delegation to action_host's live method, and pin that the tray's normal
      and diagnostic paths cannot reach unimplemented code.
    exit_criteria:
      - "no production-reachable macOS provisioning path contains unimplemented!/todo!/panic placeholders"
      - "dead wrapper and stale header claims are removed or the trait delegates to the proven live implementation"
      - "macOS unit/compile gates and one cold provision lifecycle smoke pass"

  - packet_id: freshness-threshold-and-coverage-truth
    order: 606-vaua
    status: ready
    desired_release: v0.5
    priority: p1
    title: "Freshness inventory must apply a real age threshold and report truthful nonzero coverage/delta"
    kind: methodology+tooling+litmus
    deliverable: >
      methodology freshness threshold + scripts/freshness-inventory.sh
      corrected classification/metrics + fixtures
    depends_on: []
    pickup_role: any
    capability_tags: [freshness, methodology, metrics, testing, cleanup]
    estimated_hours: 3
    outcome: >
      Freshness reports distinguish fresh from stale stamps by a canonical age
      threshold, show fractional coverage and cycle-over-cycle delta, and make
      the current 7/1011 baseline visible rather than rounding it to zero.
    next_action: >
      Define threshold ownership in methodology, then seed fresh/expired/absent
      fixture stamps and correct inventory classification before changing any
      component stamp.
    exit_criteria:
      - "methodology defines the canonical freshness age threshold and clock semantics"
      - "a fresh stamp is not labeled stale; an over-threshold stamp is; an absent stamp remains distinct"
      - "7/1011 renders as a nonzero fractional percentage and reports numerator, denominator, and coverage delta"
      - "fixtures cover boundary age, malformed date, and clock-skew behavior"

  - packet_id: folded-loop-status-active-release-truth
    order: 606-46x9
    status: ready
    desired_release: v0.5
    priority: p0
    title: "Generate ACTIVE RELEASE and v0.5 burndown from the folded ledger; loop_status is materially stale"
    kind: plan-tooling+standing-gate
    deliverable: >
      folded loop-status renderer/check, README fragment exclusion, and
      per-cycle active-release/count gate
    depends_on: []
    pickup_role: any
    capability_tags: [plan, release, loop-status, fragments, metrics, testing]
    estimated_hours: 3
    outcome: >
      Every orchestration cycle reports one unambiguous active release and
      counts derived from base plus fragments. Stale hand-maintained numbers or
      loop_status.d README warnings fail before they can guide packet selection.
    next_action: >
      Pin today's folded v0.5 count (254 total, 188 nonterminal) against the
      stale 87/94 text, teach the scanner to ignore README, and replace manual
      count prose with a deterministic renderer/check.
    exit_criteria:
      - "loop status has exactly one ACTIVE RELEASE declaration and no contradictory active-release prose"
      - "v0.5 totals/status counts match tillandsias-plan's folded ledger on every coordinator cycle"
      - "loop_status.d/README.md is excluded from data parsing without suppressing malformed real fragments"
      - "a fixture adding a packet fragment changes the rendered count and makes stale output fail"

  - packet_id: git-mirror-cross-project-service-identity
    order: 606-bvnp
    status: ready
    desired_release: v0.5
    priority: p0
    title: "SECURITY: exact per-project mirror identity and SSH signer authority; project A must never mint or reach project B"
    kind: security-design+implementation+litmus
    deliverable: >
      opaque per-project mirror DNS identity, exact Vault client/host signer
      roles and policies, two-project negative routing/signing fixtures
    depends_on: []
    blocks: [authenticated-forge-write-transport-impl]
    pickup_role: linux
    capability_tags: [security, git-mirror, ssh, vault, identity, multi-project]
    estimated_hours: 6
    outcome: >
      Each project has a unique authenticated mirror service identity. AppRole
      and SSH CA authority is exact, not sign/*; a compromised project-A lane
      or mirror cannot mint project-B client/host certificates or route Git
      commands to project B.
    next_action: >
      Amend the unsigned SSH-CA design to replace shared aliases and wildcard
      signer policy with an opaque project ID, exact roles/policies and exact
      certificate principals; prove the negative two-project matrix before 451
      wires production sshd.
    exit_criteria:
      - "project A AppRole signing through project B's role receives 403, and A's certificate is rejected by B"
      - "client and host policies name one exact signer role; no sign/* capability remains"
      - "two simultaneous projects resolve only their opaque unique mirror hostnames and fixed repository paths"
      - "correct per-lane certificates clone/fetch/push and audit project, lane, principal, fingerprint, serial, and refs"
      - "no forge contains a private key, Vault token, SecretID, or upstream credential; it receives only an agent socket and public CA trust"

  - packet_id: git-mirror-egress-network-spec-drift
    order: 606-9wqd
    status: ready
    desired_release: v0.5
    priority: p0
    title: "SECURITY/SPEC: git mirror is directly dual-homed to egress although enclave contract says only proxy may bridge"
    kind: security+openspec+implementation
    deliverable: >
      mirror egress route correction or ratified narrow exception with
      destination enforcement and bypass-negative litmus
    depends_on: []
    pickup_role: linux
    capability_tags: [security, git-mirror, network, proxy, openspec, zero-trust]
    estimated_hours: 4
    outcome: >
      Git upstream traffic follows one explicit, enforced egress policy. The
      shipped network topology and enclave spec agree, and the mirror cannot
      bypass destination/audit controls merely because it holds the upstream
      credential.
    next_action: >
      Reproduce the mirror's direct egress attachment and proxy capability,
      then prefer routing upstream Git through the enclave proxy; if impossible,
      write the smallest explicit exception and enforce destination allowlists
      at the actual network boundary.
    exit_criteria:
      - "implementation and enclave-network spec agree on whether the mirror may attach directly to egress"
      - "preferred closure routes mirror upstream Git through proxy with CA/audit behavior intact and removes its egress attachment"
      - "if an exception is ratified, it is destination-scoped and a negative test proves arbitrary direct egress is refused"
      - "clone/fetch/push parity and credential isolation remain green"

status:
  - packet_id: authenticated-forge-write-transport-impl
    field: depends_on
    value:
      - mirror-authenticated-push-transport
      - mirror-repo-config-hardening-never-reaches-existing-volumes
      - git-mirror-cross-project-service-identity
      - git-mirror-egress-network-spec-drift
    ts: "2026-08-06T00:45:00Z"
    host: linux-mutable-root-codex-20260806t001750z
  - packet_id: authenticated-forge-write-transport-impl
    field: blocked_on
    value: >
      unsigned order 322 decision plus existing-volume hardening, exact
      cross-project service/signer identity, and mirror egress contract
    ts: "2026-08-06T00:45:00Z"
    host: linux-mutable-root-codex-20260806t001750z

events:
  - packet_id: mirror-authenticated-push-transport
    event:
      type: audit-correction
      ts: "2026-08-06T00:45:00Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        First-person zero-trust audit confirms SSH CA is the selected family but
        the decision must not be signed unchanged. The sample
        ssh-client-signer/sign/* policy lets project A mint project B certs;
        shared git-service/tillandsias-git aliases collapse host identity; the
        user now requires a zero-trust whole boundary with ephemeral per-lane
        identity and rotating TTL. Q1's old forge-token premise is obsolete
        because token injection was removed; per-lane sidecars are the stated
        direction; CA rotation authority/cadence still needs a durable choice.
        Evidence: plan/issues/git-mirror-zero-trust-audit-2026-08-05.md.
  - packet_id: authenticated-forge-write-transport-impl
    event:
      type: security-audit
      ts: "2026-08-06T00:45:00Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Anonymous receive-pack is actively destructive, not merely unattributed:
        repeated <=10-ref deletion batches relay upstream with the mirror's
        privileged credential, deletion validation is skipped, and arbitrary
        unprotected refs can be created/fast-forwarded. Added explicit
        prerequisites 579, 606-bvnp and 606-9wqd; do not wire production SSH
        until exact cross-project policies and network identity are proven.
  - packet_id: mirror-repo-config-hardening-never-reaches-existing-volumes
    event:
      type: priority-evidence
      ts: "2026-08-06T00:45:00Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Promoted as the smallest containment slice before authenticated
        migration: apply denyNonFastForwards=true, denyDeletes=true and
        fsckObjects=true on every start, including existing volumes. This
        removes the worst rewind/delete paths but is not authentication and
        cannot close order 451 by itself.
  - packet_id: mirror-daemon-receive-pack-restored
    event:
      type: risk-correction
      ts: "2026-08-06T00:45:00Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Correct prior containment narrative: relay-refs does not prevent rogue
        upstream deletion. It forwards zero-new-OID refspecs through the
        privileged upstream credential and caps only deletions per transaction,
        so repeated batches remain destructive. Historical v0.4 unblock stays
        completed; this correction is owned by 322/451/579.
