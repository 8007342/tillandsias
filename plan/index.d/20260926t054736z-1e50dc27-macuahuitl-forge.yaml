# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: on-one-windows-host-a-push-to-linux-next-hangs-at-git-remote-https-while-reads-return-instantly-and-a-push-to-a-salvage-ref-completes-in-seconds
    event:
      type: finding
      ts: "2026-09-26T05:47:36Z"
      agent_id: forge-forge-tillandsias-claude-20260926t054735z
      host: macuahuitl-forge
      summary: |
        OBSERVED on macuahuitl-forge 2026-09-26 through the enclave mirror: (1) a push-plan-fragments-to-trunk push to refs/heads/linux-next sat ~3 min with no output (git push pid alive 05:37 -> done 05:39:38) before "Atomic push ... succeeded", while a salvage push and a work/* push the same session completed in ~6s — the same trunk-slow / side-ref-fast shape as this row. (2) The mirror still emits git trace output on EVERY fetch/push ("remote: HH:MM:SS git.c:465 trace: built-in: ...", "run_command", 15+ lines per push), which reads like the GIT_TRACE=1 diagnostic this row prescribed left switched on; pirria noted the same on 2026-09-23. It buries the relay's verdict lines, and every worker has to filter it.
