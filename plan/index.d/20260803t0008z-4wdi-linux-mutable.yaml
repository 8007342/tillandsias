# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-macuahuitl-opus5-20260802
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
status:
  - packet_id: format-preserving-ledger-compaction
    field: status
    value: completed
    ts: "2026-08-03T00:08:08Z"
    host: linux-mutable-macuahuitl-opus5-20260802
events:
  - packet_id: format-preserving-ledger-compaction
    event:
      type: completed
      ts: "2026-08-03T00:08:08Z"
      agent_id: linux-mutable-macuahuitl-opus5-20260802
      host: linux
      summary: >
        Compaction is now TEXT-LEVEL and format-preserving: the base is never
        re-serialized, so its ~120 operator comment lines and the four-space
        item prefix edit::append_event locates survive BY CONSTRUCTION.
        fragments.rs compact_text() emits appended packets field-by-field
        (keys at 6, list items at 8, multi-line strings as literal blocks),
        routes new events through the new edit::push_event() (bottom-append,
        complement of append_event), and re-applies LWW status wins as targeted
        line edits. All four compaction_text_tests green, including
        compaction_on_the_real_ledger_preserves_every_comment_and_item, which
        compacts a fixture copy of the REAL plan/index.yaml + real fragments
        and asserts every comment, item order, and fold-state survives. The
        fail-closed serde_yaml round-trip guard is REPLACED by a parse +
        integrity gate (Ledger::parse + check_integrity) before the candidate
        may replace the base. Live gate fix: the candidate is validated with
        the same archived-id set as `tillandsias-plan check` — an empty set
        wrongly refused depends_on that resolves via plan/archive/
        (agent-login-flows-research, encrypted-control-channel-research).
        New litmus litmus:plan-compaction-format-preservation (8 steps,
        including archive-aware positive control); litmus-ledger-fragment-
        convergence updated to pin format-preservation instead of the removed
        fail-closed guard. 10/10 methodology-accountability litmus green,
        ./build.sh --check green, full workspace test suite green. Verified on
        a live copy: compact folded 24/24 fragments, byte-identical on the
        second pass. Packet 582-4wdi. Production ledger NOT compacted here —
        that is the compaction step's deliberate act.
