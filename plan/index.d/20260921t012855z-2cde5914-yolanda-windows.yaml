# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-windows-installers-capability-probe-asks-help-whether-a-flag-exists-so-it-is-defeated-by-exactly-the-defect-it-was-written-to-survive-a-help-that-advertises-a-flag-the-parser-rejects
    event:
      type: claim
      ts: "2026-09-21T01:28:55Z"
      agent_id: windows-yolanda-claude-20260921t012854z
      host: yolanda-windows
      summary: |-
        yolanda-windows takes 1323-5taw: replacing the --help capability probe in install-windows.ps1 with an attempt under TILLANDSIAS_DESTRUCTIVE_RESET_OK=0, plus the fixture and litmus the unscoreable block named
