# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: every-apple-silicon-mac-in-the-fleet-produces-the-same-cpu-and-gpu-fingerprint-strings
    field: status
    value: implemented
    ts: "2026-09-13T06:53:30Z"
    host: macos

events:
  - packet_id: every-apple-silicon-mac-in-the-fleet-produces-the-same-cpu-and-gpu-fingerprint-strings
    event:
      type: note
      ts: "2026-09-13T06:53:30Z"
      host: macos
      summary: |-
        name_source provenance field replaces the deny-list; macOS reads machdep.cpu.brand_string; two guards falsified; fingerprint moved hw2-d1ec0bba -> hw2-487ff08e on this host
