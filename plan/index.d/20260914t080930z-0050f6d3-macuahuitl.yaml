# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: orchestrate-enclave-step-5-forge-launch-is-nested-inside-a-never-taken-status-check-conditional-and-unnesting-it-needs-a-fixture-first
    event:
      type: progress
      ts: "2026-09-14T08:09:30Z"
      agent_id: linux-macuahuitl-fable-20260914t074010z
      host: macuahuitl
      summary: |
        Closed at e78f27275: the forge launch de-nested onto the normal path, the status-check probe still gated on its opt-in; fixture written first (1/3 pre-fix), 3/3 post-fix with a content-built mutation control; siblings green; the grep -qF leading-dash trap fixed in the fixture. Criterion 3 met by the existing DEAD_VAR self-test arm (the detector is lexical), stated rather than padded with a redundant arm.
