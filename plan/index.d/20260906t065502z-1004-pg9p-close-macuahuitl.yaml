# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The closure rung and its evidence-bearing event MUST live in the SAME
# fragment (686-7qcm). My first attempt split them across two files and the
# gate refused with violation:closure-without-evidence:1 — correctly. Both
# were unpushed and never read by another host, so they are replaced here
# rather than corrected in place.
status:
  - packet_id: floor-tier-release-smoke-and-metrics
    field: status
    value: done
    ts: "2026-09-06T06:55:02Z"
    host: macuahuitl

events:
  - packet_id: floor-tier-release-smoke-and-metrics
    event:
      type: completed
      ts: "2026-09-06T06:55:02Z"
      host: macuahuitl
      summary: |-
        status 'done' with evidence_refs: 84f67d778 — AUTHORISED destructive floor
        smoke run by pirria against published v56.9.5.1: install_exit=0 with
        version asserted == v56.9.5.1, reset_exit=0 (containers/volumes/images
        all empty after reset), init_exit=0 from pristine, opencode_exit=0 with
        the forge lane at 2h42m00s, health_rc=0 with vault sealed:false and
        proxy alive. §4b snapshot at 03:50:00Z, 17s after lane start: six
        containers up with tillandsias-proxy alongside a live forge container,
        so order 298 holds. Isolation verified — the forge agent's commit
        ef6626f9e is absent from the host checkout, host HEAD unchanged,
        worktree clean throughout. RELEASE v56.9.5.1 RECORDED AS TESTED.
        1026-ps4n criterion 3 discharged the OTHER way, by a clean run with a
        real record: stamp written with a live writer pid, survived 2h42m,
        timing_commit emitted and cleared it, §0 reap correctly silent because
        no orphan existed.
  - packet_id: floor-tier-release-smoke-and-metrics
    event:
      type: note
      ts: "2026-09-06T06:55:02Z"
      host: macuahuitl
      summary: |-
        THREE THINGS THIS CLOSE DOES NOT CLAIM, at pirria's explicit request,
        each because it would upgrade the evidence. The supervisor-kill path is
        NOT verified — it remains unobserved in the field; the third harness
        kill took pirria's background waiter while the detached runner survived,
        which confirms setsid nohup and discharges nothing. There were TWO
        timing records, not five, and five was never reachable: the runbook
        instruments only §4 and §4c, so §1-§3 emit nothing — an instrumentation
        packet against §1-§3 is the honest follow-up. And the 2h42m lane is NOT
        a regression against the 71m30s baseline: the in-forge agent did
        strictly more work, so the comparison is not like-for-like and no
        per-step delta was isolated. THE OPERATOR AUTHORISATION COVERED ONE RUN
        AND IS DISCHARGED — it is not standing, and a further smoke needs fresh
        operator authorisation (1004-vsh2). Findings are filed separately as
        1096-p3tn, 1097-mw6d, 1098-q7bk and 1099-t4xr.
