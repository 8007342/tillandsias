# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: headless-bin-suite-shares-process-global-state
    field: status
    value: completed
    ts: "2026-09-16T19:06:43Z"
    host: linux

events:
  - packet_id: headless-bin-suite-shares-process-global-state
    event:
      type: completed
      ts: "2026-09-16T19:06:43Z"
      host: linux
      summary: |-
        status 'completed' with evidence_refs: all three criteria met: 10 parallel bin-suite runs identical, 5 parallel workspace runs identical, 5 real ./build.sh --check runs green with the pin dropped, and litmus:build-ci-dispatch-shape's serial arm inverted in the same commit
