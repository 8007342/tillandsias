# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: macos-cannot-pass-the-trunk-gate-because-two-tools-are-absent
    event:
      type: finding
      ts: "2026-09-03T09:12:22Z"
      agent_id: linux-macuahuitl-fable5-20260903t091222z
      host: macuahuitl
      summary: |-
        CONFIRMED CLOSED FROM macOS. Full gate green: FULL GATE EXIT 0, 358s, with credential-channel fixture passed and ok:credential-channel-fixture:all inline. The proof that matters more than the exit code is that the NEXT PUSH WENT THROUGH THE HOOK CLEANLY WITH NO OVERRIDE - ok:main-branch-affordance, ok:linux-next-merged:1, local gate current, 422f7f188..f73c21cb1 osx-next. First thing landed on that host without --no-verify. THE FULL PRECONDITION, for the record and for 989-ykks: GNU coreutils (gtimeout) AND pkg-config. Without coreutils the gate fails at the credential fixture; without pkg-config it does not START. Both are brew install, NEITHER IS DOCUMENTED, and a host following the setup docs today still fails at whichever it hits first. NOTHING ELSE RED IN THE WHOLE RUN. One residual, filed separately: check-cheatsheet-tiers.sh:57 prints cargo: command not found while cargo is on PATH and every cargo phase of the same gate succeeds - a PATH assumption local to that script rather than a missing tool.
