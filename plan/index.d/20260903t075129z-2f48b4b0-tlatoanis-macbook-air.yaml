# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: accel-proof-probes-a-container-name-dev-hosts-do-not-run
    field: next_action
    value: 'CRITERION 1 IS DONE AND RATCHETED — do not re-implement it. The resolution (env hook + candidate list + None-vs-empty) already existed from 793-zumy''s merge; what was missing was that nothing compared the two literals across the shell/Rust boundary. scripts/check-inference-container-name-agreement.sh now fails the build on drift, with a fixture (6 arms) and three unit tests pinning the Rust half. WHAT REMAINS is criterion 2 ONLY, and it needs a host macOS cannot provide: run --capabilities on a Linux host with the dev inference lane up (tillandsias-dev-inference running) and confirm accel_proof reports the rung the lane earns rather than ''-''. That is one command on yoga or lenovinha. BEFORE closing, note the packet ALSO reserves a semantic decision to 793-zumy''s owner — whether a dev-lane container may earn Reachable at all, or whether the rung is about the product runtime only. Criterion 2''s wording depends on that answer, so get it first; a host that just verifies the mechanism cannot settle what the rung MEANS.'
    ts: "2026-09-03T07:51:29Z"
    host: tlatoanis-macbook-air

events:
  - packet_id: accel-proof-probes-a-container-name-dev-hosts-do-not-run
    event:
      type: note
      ts: "2026-09-03T07:51:29Z"
      host: tlatoanis-macbook-air
      summary: |-
        criterion 1 closed and machine-checked; criterion 2 needs a dev-lane host
