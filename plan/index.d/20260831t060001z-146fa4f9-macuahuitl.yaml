# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: sni-windowid-type-wedges-shell
    event:
      type: note
      ts: "2026-08-31T06:00:01Z"
      agent_id: linux-macuahuitl-fable5-20260831t060001z
      host: macuahuitl
      summary: >
        STABLE-CUT BLOCKER CLEARED: README release-ledger rows backfilled for v0.4.260817.1, v0.4.260826.1, v0.4.260830.5 (the gap lenovinha found and pirria elevated to blocking — 'promoting something undescribed'). Rows compiled by three parallel agents over the full commit spans (548/1207/811 commits) in the table's house voice, then precision-corrected against tonight's later findings: the 260830.5 row carries the do-not-relaunch-this-tray warning, the post-tag GetLayout and shutdown-sweep pins, the invalidated 561s aggregate honestly reframed, and the unsigned-MSIX blessing red with its withhold-guard remedy — a smoke-reader of this release now knows exactly what it claims and what it does not. Runbook 0.2b will now find rows on every branch after merge.
