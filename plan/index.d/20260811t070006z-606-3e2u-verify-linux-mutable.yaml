# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260811t070006z
events:
  - packet_id: stale-browser-mcp-artifacts-cleanup
    event:
      type: progress
      ts: "2026-08-11T07:00:06Z"
      agent_id: linux-mutable-20260811t070006z
      host: linux_mutable
      summary: >
        VERIFY + EVIDENCE pass over the four exit criteria (no deletion needed;
        the source removal was already done). Findings by criterion:

        (1) SOURCE CLEANUP CONFIRMED COMPLETE. Both stale artifacts are absent:
        `images/default/mcp-server-browser.js` is not on disk and not
        git-tracked, and `tillandsias-mcp-browser` is not git-tracked (it exists
        only as an untracked build-cache ELF under target/, out of scope).
        flake.nix has zero references to mcp-server-browser /
        tillandsias-mcp-browser / forgeMcpBrowser. A focused BUILD-surface grep
        over flake.nix + all Containerfiles + images/** + scripts/** returned NO
        hits. The only live-tree mention of the names is the negative absence
        fixture itself, openspec/litmus-tests/litmus-host-browser-mcp-shape.yaml
        (the proof-of-absence litmus, not a dangling ref). Every other mention
        lives in the EXCLUDED historical-provenance buckets (docs/audits/,
        openspec/changes/archive/*, plan/). So NO real dangling build or doc
        reference exists; no tracked file was changed and no fix was required.

        (3) LIVE BRIDGE + RUST HOST-SOCKET TESTS GREEN. `cargo test -p
        tillandsias-browser-mcp` = 103 passed / 0 failed / 0 ignored:
        72 lib unit tests (src/lib.rs) + 25 (tests/allowlist_enforcement_
        integration.rs) + 6 (tests/window_timeout_enforcement.rs) + 0 doc-tests.

        (4) SOURCE-SIDE PAYLOAD DELTA RECORDED = exactly 27,249,383 bytes removed
        (27,243,632-byte x86 ELF + 5,751-byte mock JS), per the
        2026-08-06 handoff. The BUILT-OUTPUT byte size and `nix path-info -Sh`
        closure size are NOT measurable on this host right now — blocked with (2)
        below — and are deliberately kept separate from the source-side sum per
        the packet next_action.

        (2) BLOCKED on this host: nix daemon is unavailable. `nix build
        .#forge-image` failed with "cannot connect to socket at
        '/nix/var/nix/daemon-socket/socket': Connection refused"; nix-daemon.
        service and nix-daemon.socket are both `inactive`, no nix-daemon process
        is running, and passwordless sudo is refused so the daemon cannot be
        started from here. The store is a multi-user install (/nix/store =
        root:nixbld), so even a pure `nix eval .#forge-image.drvPath` fails on the
        same socket. This is the same "daemon socket path present but connection
        refused" condition recorded in the 2026-08-06T10:47:11Z progress event —
        it persists. Therefore BOTH the x86_64 build (+ built-output/closure
        sizes) AND the aarch64 build remain. aarch64 additionally CANNOT run on
        this x86_64 host regardless of the daemon.

        REMAINING WORK (packet stays ready):
        - x86_64-linux: run `nix build .#forge-image`, record built-output byte
          size + `nix path-info -Sh` closure size — needs a daemon-capable Linux
          host (linux_mutable's daemon is currently down).
        - aarch64-linux: build `.#forge-image` and record the same sizes — needs
          an arm/macOS host (EXPLICITLY REMAINING; not fakeable from x86_64).
      partial_artifact_refs:
        - flake.nix
        - openspec/litmus-tests/litmus-host-browser-mcp-shape.yaml
        - crates/tillandsias-browser-mcp
        - plan/issues/stale-browser-mcp-artifacts-removal-handoff-2026-08-06.md
      criteria_status:
        artifacts_absent_no_dangling_refs: confirmed_complete
        flake_no_arch_specific_binary_and_builds: blocked_x86_daemon_down__aarch64_remaining
        live_bridge_and_rust_socket_tests_green: green
        image_size_evidence: source_delta_recorded__built_output_delta_blocked
      evidence:
        source_bytes_removed_total: 27249383
        source_bytes_removed_breakdown: "27,243,632-byte x86 ELF + 5,751-byte mock JS"
        cargo_test_browser_mcp: "103 passed; 0 failed; 0 ignored (72 lib + 25 allowlist + 6 window-timeout + 0 doctests)"
        nix_build_forge_image: "FAILED: cannot connect to socket '/nix/var/nix/daemon-socket/socket': Connection refused (nix-daemon.service + .socket inactive; no passwordless sudo)"
        dangling_build_reference_found: false
