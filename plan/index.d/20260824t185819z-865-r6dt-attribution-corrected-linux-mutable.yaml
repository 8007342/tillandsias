# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-24T18:58:19Z.
events:
  - packet_id: groundtruth-harness-litmus-fails-for-two-unrelated-reasons-neither-an-expert-regression
    event:
      type: note
      ts: "2026-08-24T18:58:19Z"
      agent_id: linux-macuahuitl-opus5-20260824T183400Z
      host: linux_mutable
      evidence_refs:
        - "grade expert-groundtruth-rung1.yaml alone at HEAD a12554087: total=22 pass=22 fail=0"
        - "grade the full glob with an index: the two reds are spec-forge-isolation-and-egress and spec-inference-tier-mechanism-is-in-code, both [spec.answer]"
        - "index .commit 6931cce8 vs HEAD a12554087 — TWENTY-FOUR commits behind"
      summary: >
        CORRECTION TO THIS PACKET'S CAUSE (2). I wrote that three cases in
        plan-methodology-experts-rung1 [plan.answer] — packet 394's set — fail,
        and attributed it to 394 having been obsoleted. VERIFIED WRONG at HEAD:
        that set grades 22/22, fail=0, standalone.

        The two real failures are [spec.answer] cases,
        spec-forge-isolation-and-egress and
        spec-inference-tier-mechanism-is-in-code, and the cause is INDEX-VS-HEAD
        SKEW rather than a moved packet: the published index carries
        .commit 6931cce8 while HEAD is a12554087, twenty-four commits later, so
        cited spans no longer land where the index says they do.

        WHY THE MISATTRIBUTION HAPPENED, because the mechanism will recur. I ran
        the glob, grepped `^FAIL`, and read three identical lines naming
        plan-methodology-experts-rung1 — then reasoned from 394's known
        obsoleting, which was fresh in mind from 865-x2mf that same morning. A
        plausible cause I had just proved real elsewhere made a weak observation
        look like a confirmed one. The grep never told me which SET a failing
        case belonged to; I supplied that from memory.

        As filed, this packet would have sent someone to edit committed plan
        cases that are correct. Re-pointed here rather than left to be
        discovered by whoever picked it up.

        CREDIT WHERE DUE: the correction came from a triage subagent, and I
        verified all three of its claims against HEAD before accepting them
        rather than trusting the report.

  - packet_id: the-trunk-has-been-unreleasable-for-a-week-behind-a-gate-no-push-runs
    event:
      type: progress
      ts: "2026-08-24T18:58:19Z"
      agent_id: linux-macuahuitl-opus5-20260824T183400Z
      host: linux_mutable
      evidence_refs:
        - "6-agent parallel triage: all six failures return kind=test-defect, host_required=this-linux-host"
        - "dev-build spec re-run at HEAD: cross-target-cfg-gate-check EXECUTES and passes; spec now 10/11 (was 9/11, 2 failures)"
      summary: >
        NO NEW HARDWARE UNBLOCKS THE RELEASE. Six independent triages agree:
        every remaining failure is a TEST DEFECT fixable on this Linux host.
        Each failing assertion is grep/awk/bash over checked-in text or a
        hermetic mktemp fixture. The subject matter is cross-platform
        (CREATE_NO_WINDOW, cfg(unix), Windows lanes) but the tests only READ
        SOURCE — no accelerator, no container runtime, no Windows or macOS
        surface is exercised by any of them.

        CORRECTION TO THIS ROW'S OWNERSHIP TABLE: cross-target-cfg-gate-check is
        DONE, not "LENOVINHA is on it" — fixed at a12554087 and verified
        executing-and-passing here. Six remaining becomes five.

        FIVE OF THE SIX ARE ONE DEFECT CLASS: the assertion pins a PROXY — a
        line-distance window, a version literal, a file path — instead of the
        property it stands for. The repo has already named this twice: order 528
        `source-window-litmus-assertions-are-brittle` (ready, pickup_role linux)
        and 622-rmit "expression pinning". So the release is not blocked on
        hardware or on six unrelated bugs; it is blocked on one known,
        already-filed class that nobody has burned down.

        Worked example, and the reason widening is the wrong instinct:
        cross-target-cfg-gate-check flagged main.rs:1278 as an ungated
        libc::getuid — but the call sits inside `#[cfg(unix)] fn
        ensure_lane_process_env`, whose gate is FIFTEEN lines up, outside the
        step's fixed five-line lookback. Four sibling call sites pass only
        because their gate happens to fall within five lines. Correct code grew
        a longer function body and the fixture called it a regression.
