# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: precommit-openspec-scan-exceeds-ten-minutes-on-large-files
    field: next_action
    value: 'LINUX HALF DONE (yoga, 440233994): the sweep no longer walks build output, 237ms -> 93ms median here, answer-preserving and pinned. WHAT REMAINS IS A MEASUREMENT ONLY MACBOOKAIR CAN MAKE: re-run the pre-commit hook and report zero_trace_check''s elapsed ms against the 2500ms budget. Yoga was UNDER budget both before and after, so this host cannot verify the fix. If still over, the next candidates are the awk pass over 170 spec.md files and the per-spec loop, both bounded by spec count not repo size. Do NOT widen the budget to match a measurement.'
    ts: "2026-09-05T11:05:59Z"
    host: yoga

events:
  - packet_id: precommit-openspec-scan-exceeds-ten-minutes-on-large-files
    event:
      type: progress
      ts: "2026-09-05T11:05:59Z"
      host: yoga
      summary: |-
        status 'LINUX HALF DONE (yoga, 440233994): the sweep no longer walks build output, 237ms -> 93ms median here, answer-preserving and pinned. WHAT REMAINS IS A MEASUREMENT ONLY MACBOOKAIR CAN MAKE: re-run the pre-commit hook and report zero_trace_check's elapsed ms against the 2500ms budget. Yoga was UNDER budget both before and after, so this host cannot verify the fix. If still over, the next candidates are the awk pass over 170 spec.md files and the per-spec loop, both bounded by spec count not repo size. Do NOT widen the budget to match a measurement.' (not terminal) with evidence_refs: 440233994
  - packet_id: precommit-openspec-scan-exceeds-ten-minutes-on-large-files
    event:
      type: note
      ts: "2026-09-05T11:05:59Z"
      host: yoga
      summary: |-
        linux half landed; only macOS can verify the budget
