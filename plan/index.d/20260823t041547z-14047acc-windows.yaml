# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: dual-locus-preflight-must-rebuild-every-gate-locus
    field: status
    value: implemented
    ts: "2026-08-23T04:15:47Z"
    host: windows

events:
  - packet_id: dual-locus-preflight-must-rebuild-every-gate-locus
    event:
      type: note
      ts: "2026-08-23T04:15:47Z"
      host: windows
      summary: >
        All three exit criteria have hermetic + this-host evidence; field evidence from a Linux mid-cycle pull is the residual
