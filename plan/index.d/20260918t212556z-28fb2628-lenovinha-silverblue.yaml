# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-nvidia-cdi-currency-check-keys-on-the-driver-version-while-the-spec-pins-libraries-that-version-independently-so-it-reports-current-about-a-spec-whose-files-no-longer-exist-and-the-gpu-container-lane-dies
    field: title
    value: the NVIDIA CDI currency check keys on the driver version while the spec pins libraries that version independently, so it reports current about a spec whose files no longer exist — and the gpu container lane does NOT die, it comes up healthy carrying 33 zero-byte libraries at the dead soname, a compliant-silence defect that nothing announces
    ts: "2026-09-18T21:25:56Z"
    host: lenovinha-silverblue

events:
  - packet_id: the-nvidia-cdi-currency-check-keys-on-the-driver-version-while-the-spec-pins-libraries-that-version-independently-so-it-reports-current-about-a-spec-whose-files-no-longer-exist-and-the-gpu-container-lane-dies
    event:
      type: note
      ts: "2026-09-18T21:25:56Z"
      host: lenovinha-silverblue
      summary: |-
        REPAIR: my earlier invocation put the value after the flags, so the parser took --host's argument as the value and the title on trunk became the literal string lenovinha-silverblue. Restoring the intended corrected title.
