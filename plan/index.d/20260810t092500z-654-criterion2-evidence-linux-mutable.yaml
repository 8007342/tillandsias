# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t092500z
events:
  - packet_id: git-mirror-egress-network-spec-drift
    event:
      type: verified
      ts: "2026-08-10T09:25:00Z"
      agent_id: linux-mutable-20260810t092500z
      host: linux
      summary: >
        CRITERION 2 TESTED AT RUNTIME AND VIABLE — the packet's central decision is
        now made on evidence, and the criterion-3 fallback is unnecessary.

        Probe squid (the real tillandsias-proxy image on the spec-sanctioned
        dual-homed posture); client container on tillandsias-enclave ONLY, no egress
        leg, http_proxy=http://proxy:3128:

          git ls-remote https://github.com/8007342/tillandsias.git HEAD -> exit 0,
                                                              real SHA returned
          https://github.com/      (allowlisted)      -> 200
          https://example.com/     (NOT allowlisted)  -> 000 denied
          https://wikipedia.org/   (NOT allowlisted)  -> 000 denied
          https://bbc.co.uk/       (NOT allowlisted)  -> 000 denied

        1. The mirror does not need its egress leg: its real operation works through
           the proxy from enclave-only. The old "failed on clean rootless" note was a
           DNS resolution failure, as the static audit suspected.
        2. The proxy ENFORCES destination scoping (allowlist + deny all;
           .github.com already on the list at line 108). That is precisely what
           tillandsias-egress lacks, so routing the mirror behind it converts "has
           the internet" into "has the allowlist" — the scoping criterion 3 would
           have had to build from scratch.

        ssl_bump splice all means non-release-asset traffic is tunneled, not
        decrypted, so TLS stays end-to-end and git credential handling is untouched
        (bears on criterion 4).

        CAVEAT, stated: this tested ls-remote (authenticated read), not an
        authenticated PUSH, and used a probe proxy rather than the product's
        ensure_proxy path. Push parity remains 654-7ur4's to close.

        Also: the first negative control used gitlab.com, which IS allowlisted
        (line 111). It returned a real SHA and read as "the proxy does not enforce
        its allowlist" — a dramatic, entirely wrong finding, caught only by checking
        the list before writing the claim. Third invalid instrument in three cycles
        on this packet. Controls above were chosen by verifying absence first.

  - packet_id: route-mirror-and-login-egress-through-the-proxy-chokepoint
    event:
      type: progress
      ts: "2026-08-10T09:25:00Z"
      agent_id: linux-mutable-20260810t092500z
      host: linux
      summary: >
        Unblocked with a decided direction: take criterion 2, drop the five
        unsanctioned egress legs, do NOT ratify an exception. The proxy is proven to
        carry git from an enclave-only container AND to deny non-allowlisted
        destinations, so the fix is a re-attachment plus an allowlist check, not new
        scoping infrastructure.

        Remaining to prove: authenticated PUSH parity through the proxy (this test
        covered ls-remote), and the same treatment or a scoped exception for
        run_provider_login. The bypass-negative litmus this packet requires now has a
        known-good fixture: enclave-only + proxy must reach github.com and must NOT
        reach example.com.
