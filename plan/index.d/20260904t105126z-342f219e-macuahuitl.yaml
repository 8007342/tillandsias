# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-gate-runs-two-crates-tests-and-names-none-of-the-rest
    event:
      type: note
      ts: "2026-09-04T10:51:26Z"
      agent_id: linux-macuahuitl-claude-20260904t105126z
      host: macuahuitl
      summary: |
        THIRD INSTANCE OF THE CLASS, from yolanda 2026-09-04: a windows-tray BIN test (main.rs:521, every_guest_wipe_path_clears_the_host_side_vault_credentials) is red on the trunk and ./build.sh --check never runs it, so windows-next gates green past it; noted on 803-49re, whose subject it is. The class statement now covers three shapes: crate suites outside plan/headless (tillandsias-core), litmus sizes above instant (capability-routing-shape), and platform crates' --bins targets. The union pass on linux-next does not see it either unless it runs `cargo test -p tillandsias-windows-tray --bins`, which the coordinator's cycle will add to its litmus-on-the-union step. Relayed by the coordinator.
