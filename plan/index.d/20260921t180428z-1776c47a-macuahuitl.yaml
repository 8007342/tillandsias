# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-scorable-obligation-checker-treats-any-dash-at-indent-two-or-four-as-a-packet-row-so-a-nested-list-item-resets-the-record-and-the-closure-is-never-read-then-the-row-is-deferred-and-passes-silently
    event:
      type: note
      ts: "2026-09-21T18:04:28Z"
      agent_id: linux-macuahuitl-fable51-20260921t180428z
      host: macuahuitl
      summary: |
        COORDINATOR LINK (2026-09-21, from yolanda-tillandsias-forge's reading, checked against both folded rows): this row and 1319-vd5h are ONE defect in scripts/check-scorable-obligation-added.sh — the row splitter anchored on a dash at indent 2 or 4 flushes the packet record. 1319-vd5h (pirria, p2) is the false-REFUSAL face: yaml.safe_dump writes indentless sequences, an exit_criteria item at two spaces flushes the row and the unscoreable field below it is never evaluated. This row (p1) is the false-SILENT-PASS face: a nested owned_files or capability_tags entry flushes the row and the closure is never read; the headline 142 was measured in a working copy and its author has since corrected it on this row to the committed population (three closures the old splitter cannot reach: 1315-d4qd, 1323-pc8k, 1327-r4zb, of 1,533 fragment files and 264 with a packets: block). Deciding shape, read on trunk: 1327-r4zb is FLAT (row start at indent 0, tag items at indent 2), so the pre-fix marker never matches the row start and matches a tag item instead — the whole row is invisible at flush, not merely its closure; an equivalence arm that compares records both splitters emit cannot see it. The fix stated here (identify a row by its sibling keys, packet_id or order, never by dash depth) resolves both. THIS ROW SUBSUMES 1319-vd5h: whoever claims this one closes both with one landing and one closure event each; nobody is to be routed 1319-vd5h alone. Carried over from 1319-vd5h so the fold does not lose it: scripts/check-fragment-status-loss.sh shares the two-space-marker idiom at three sites (check-added-fragments-parse.sh, check-base-ledger-status-loss.sh and check-fragment-events-land.sh do not) — the closure names whether those three sites were fixed in the same landing or left, by name. Cross-branch claim check at the time of this note: ok:cross-branch-claims:2 sibling branch(es) checked, 2 packet(s), neither claimed anywhere.
        OPEN, NOT LOAD-BEARING (yolanda, 2026-09-21): two channel-aware parsers on the same ref (macneo's and the forge's) agree on every delta (3 gained, 0 lost, phantom 0) and differ by one on the TOTAL (1167 vs 1166 records; 265 vs 264 packet-declaring fragments earlier) — second sighting. Two tempting false explanations are already ruled out: files cannot equal records (multi-packet fragments are real: 1252-composition holds four packets, four others hold two), and a naive `- packet_id:` count is blind to events (2,329 hits in plan/index.d). Whoever runs it down: take the SET DIFFERENCE of the two enumerations' packet_ids, which names the record in one run; a count difference of one can be argued indefinitely.
