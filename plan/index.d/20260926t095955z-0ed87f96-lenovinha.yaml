# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: bsd-awk-rejects-a-newline-in-an-awk-v-value-so-a-variable-fed-awk-v-silently-empties-on-macos-and-check-bash-dialect-has-no-rule-for-it
    field: status
    value: in_progress
    ts: "2026-09-26T09:59:55Z"
    host: lenovinha

events:
  - packet_id: bsd-awk-rejects-a-newline-in-an-awk-v-value-so-a-variable-fed-awk-v-silently-empties-on-macos-and-check-bash-dialect-has-no-rule-for-it
    event:
      type: note
      ts: "2026-09-26T09:59:55Z"
      host: lenovinha
      summary: |-
        claimed by lenovinha-silverblue at coordinator priority (before 1375-2x4e); dialect rule for awk -v multiline plus the masking shape
