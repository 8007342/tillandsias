# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-20260809t230500z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# Closes 635-qpx8. Three pickup_role values named a lane only as a follower
# (replicate later / verify / support), and `query --role` matches pickup_role as
# a case-insensitive SUBSTRING, so those follower lanes were offered the packets
# as claimable work. 632-39p3 settled the semantics this depends on: `--role`
# stays a substring filter on the field and `--claimable-by` answers claimability
# — under BOTH readings a follower lane is still not a claimant, so normalizing
# the field is required either way.
#
# The sequencing information is NOT deleted. It moves to an event on each packet,
# where a reader still finds it and no matcher reads it. That was an explicit
# exit criterion: a reader must still be able to tell that Windows and macOS
# replicate 513 after linux lands the harness.
#
# The correction travels on the LWW channel (`status:` with `field: pickup_role`)
# rather than a direct edit of plan/index.yaml, which 627-c9c2 forbids: the base
# is a write-write concurrency block AND the mirror's pre-receive Psych gate
# rejects any push touching it. The channel is keyed by (packet_id, field) and
# applies to ANY field, not only `status` — see the finding filed alongside this
# fragment about how its name and README wording obscure that.

status:
  - packet_id: dev-end-user-gating-litmus
    field: pickup_role
    value: linux
    ts: "2026-08-09T23:05:00Z"
    host: windows

  - packet_id: forge-seed-branch-clone-fix
    field: pickup_role
    value: linux
    ts: "2026-08-09T23:05:00Z"
    host: windows

  # Genuine co-ownership is preserved: windows and macos each own a half. Only
  # the false linux claim is dropped — linux SUPPORTS the control wire here, and
  # a supporting lane is not a claimant. Co-owned multi-lane values are legal and
  # scripts/check-pickup-role-grammar.sh does not flag them.
  - packet_id: dev-control-vsock-crossplatform
    field: pickup_role
    value: windows + macos
    ts: "2026-08-09T23:05:00Z"
    host: windows

events:
  - packet_id: dev-end-user-gating-litmus
    event:
      type: progress
      ts: "2026-08-09T23:05:00Z"
      agent_id: windows-20260809t230500z
      summary: >
        pickup_role normalized from "linux (litmus harness); Windows/macOS
        replicate later" to "linux" (635-qpx8). SEQUENCING PRESERVED HERE: linux
        owns and lands the litmus harness first; Windows and macOS replicate it
        afterwards. Until linux lands it, this packet is not claimable Windows or
        macOS work — which is precisely what the old field value caused, since
        `query --role windows` matched the word "Windows" in the prose and
        offered a linux-owned p0 to the Windows lane.

  - packet_id: forge-seed-branch-clone-fix
    event:
      type: progress
      ts: "2026-08-09T23:05:00Z"
      agent_id: windows-20260809t230500z
      summary: >
        pickup_role normalized from "linux (Rust launcher + lib-common.sh);
        Windows/macOS verify" to "linux" (635-qpx8). SEQUENCING PRESERVED HERE:
        linux implements the Rust launcher and lib-common.sh changes; Windows and
        macOS VERIFY afterwards. A verifying lane is not a claimant, so neither
        should have been offered this packet as claimable work.

  - packet_id: dev-control-vsock-crossplatform
    event:
      type: progress
      ts: "2026-08-09T23:05:00Z"
      agent_id: windows-20260809t230500z
      summary: >
        pickup_role normalized from "windows/macos with linux control-wire
        support" to "windows + macos" (635-qpx8). Co-ownership is intact — both
        lanes genuinely own a half. Only the false linux claim is dropped: linux
        SUPPORTS the control wire for this packet and a supporting lane is not a
        claimant, but the substring matcher was offering it to linux as work.

  - packet_id: pickup-role-sequenced-prose-normalization
    event:
      type: progress
      ts: "2026-08-09T23:05:00Z"
      agent_id: windows-20260809t230500z
      summary: >
        All three sequenced values normalized through the LWW channel, with the
        sequencing moved to per-packet events rather than deleted.
        scripts/check-pickup-role-grammar.sh now reports sequenced=0
        verdict=ok:canonical-roles and exits 0; the three co-owned values remain
        and are correctly not flagged. Unblocked by 632-39p3, which linux
        prioritized specifically because this lane reported itself blocked on it.
