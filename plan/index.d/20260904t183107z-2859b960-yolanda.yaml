# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: project-lanes-always-fall-back-to-legacy-conhost-losing-paste-and-clickable-links
    event:
      type: done
      ts: "2026-09-04T18:31:07Z"
      agent_id: windows-yolanda-opus5-20260904t183107z
      host: yolanda
      summary: |-
        CLOSED as obsolete: the MECHANISM this packet specified is dead and was never built, and the INTENT it existed for is met by a different fix and pinned by a test that provably catches its regression. Evidence for both halves, per the obsolete_mechanism_live_intent disposition.

        THE MECHANISM IS DEAD, and this packet's own ledger said so before I got here. Its stated deliverable was "project lanes launch through an INJECTED guest wrapper taking bare, quote-free arguments ... so argv_survives_wt_reparse passes". A prior event on this packet already recorded that this treats the predicate as a fixed constraint when it is not: argv_survives_wt_reparse existed ONLY to decide whether a FLATTENED `bash -lc "<script>"` string survives wt.exe's re-parse, and the flattening existed only because the guest exec allowlist admitted nothing but `/bin/bash -l|-lc <string>` (795-zshi). That constraint was already gone guest-side. The wrapper was never written, and should not be.

        THE INTENT IS MET, by 823-u5zf + 836-smm2 rather than by this packet. In crates/tillandsias-windows-tray/src/notify_icon.rs today: all non-login lanes go through spawn_wsl_terminal (wt.exe) unconditionally; only PtyIntent::GithubLogin deliberately takes the plain-console path, and that is a credential-safety decision from the 2026-08-09 Esmeralda field crashes, not a fallback. argv_survives_wt_reparse itself is DELETED — c3f6591a1, "delete argv_survives_wt_reparse and wt_safe_title (836-smm2)" — because it always returned true for the shapes launch_spec actually emits. So the reported symptom, "every project lane opens in a legacy conhost window", no longer has a mechanism.

        THE PIN, AND I SABOTAGE-TESTED IT RATHER THAN CITING IT GREEN. notify_icon.rs::project_lane_argv_is_wt_safe_and_login_bypasses_wt drives the REAL builder across five lanes (shell, claude, codex, opencode, antigravity) and two project shapes. Its own comment records that an earlier revision asserted wt-safety over a HAND-WRITTEN vector while nothing called launch_spec — so a regression to the flattened shape would have left it green while every lane fell back to conhost. That is the same proxy-read-as-the-thing failure the fleet has hit six times today, already caught and fixed here by someone else.
          CONTROL RUN: I injected a regression into host-shell's launch_spec returning the exact flattened shape this packet reports — ["bash", "-lc", "cd /home/forge/src/x && exec bash -l"]. The test went RED with "lane shell on project myapp builds a wt-unsafe argv, so it falls back to conhost and loses paste". Restored, tree clean, test green again.
          A first attempt at that control did NOT compile (I guessed the return type name), which proves nothing, so it is not offered as evidence — only the second, compiling run is.

        WHY THIS IS A CLOSE AND NOT A REWRITE. The disposition for a dead mechanism with a LIVE intent is REWRITE, never close, because closing silently drops the obligation. That is not this case: the obligation is DISCHARGED, not dropped. Project lanes open in Windows Terminal with working paste and clickable links, which is exactly what the deliverable asked for, by a better route than the one it named. What would have been wrong is closing it on the strength of 823-u5zf's existence without checking the symptom was actually gone and that something would notice if it came back.

        RESIDUAL: none for this packet. 823-u5zf remains open on its own terms for whatever else it covers; nothing here depends on it staying open.
