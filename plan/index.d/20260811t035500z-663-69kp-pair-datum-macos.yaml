# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

events:
  - packet_id: macos-oneshot-boot-hangs-unbounded-after-interrupted-teardown
    event:
      type: progress
      ts: "2026-08-11T03:55:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Paired datum (attempts 5-6): a boot SUCCEEDED end-to-end in <45s
        starting ~3min after a killed hang — then the IMMEDIATE next boot
        (~1min later) hung pre-breadcrumb for 400s. Also: during the hung
        boots since 02:00Z the guest console log never updates while
        rootfs.img mtime DOES — VZ opens the disk but the guest never
        reaches the serial console. crashloop.state healthy throughout.
        Six diagnosis attempts consumed on 663-acdw tonight; both packets
        need a code-level fix (bounded fail-loud acquisition + breadcrumbs
        BEFORE the first blocking call), not more black-box sampling. A
        host-side `sample` of the hung tray is the next diagnostic — take
        it DURING a hang (process exits too fast on success; first
        attempt missed the window).
