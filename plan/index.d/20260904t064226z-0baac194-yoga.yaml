# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: ollama-rocm-asset-never-fetched
    event:
      type: progress
      ts: "2026-09-04T06:42:26Z"
      agent_id: linux-yoga-claude-20260904t063730z
      host: yoga
      summary: |-
        MEASURED on yoga (gfx1152 / AMD Radeon 840M, RADV KRACKAN1), 2026-09-04. DECISION: Vulkan is the AMD lane; the 1,047,683,394-byte ollama ROCm asset is NOT fetched and is NOT needed. Evidence, all from one container startup on a freshly built image: ollama device discovery printed 'inference compute id=0 library=Vulkan name=Vulkan0 description="AMD Radeon 840M Graphics (RADV KRACKAN1)" libdirs=ollama,vulkan type=iGPU total=7.9 GiB available=7.5 GiB' — a real radv device, not the lvp software rasterizer — and qwen2.5:0.5b then loaded with size_vram=572228893 == size, i.e. fully resident. That is placement, not a label. WHAT THE OLD IMAGE HID: the pre-917-zkge image (v0.4.260830.5) was still running on this host and reproduced the original bug exactly — libggml-vulkan.so present, manifest core+vulkan, tier gpu-rocm announced, and NO Vulkan userspace anywhere in the container (no libvulkan.so, no /usr/share/vulkan/icd.d, no mesa packages), so the backend could not enumerate and every model ran CPU-only. 917-zkge's Containerfile fix was in-tree but unmeasured on AMD until now; rebuilding the image is what turned the lane on. ALSO CORRECTED: accel_probe.rs's finding that this container ran with 'HostConfig.Devices == []' is a podman inspect artifact, not proof of absence — that field reads empty while /dev/kfd and /dev/dri are both statted inside the container, which is the check that actually discriminates. FILED SEPARATELY: 1001-7fd9 — engine-tuning.sh reads VRAM from nvidia-smi only, so the same startup that enumerated 7.9 GiB reported vram_mb=0; the sysfs carve-out is measured at 512 MiB and rejected as a substitute source.
