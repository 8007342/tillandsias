# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-test-added-in-either-organ-can-be-referenced-by-nothing-and-nothing-says-so-at-push-time-so-a-fixture-that-cannot-fail-a-gate-reads-identically-to-one-that-guards-something
    event:
      type: progress
      ts: "2026-09-26T00:31:52Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260926t003152z
      host: tlatoanis-macbook-air
      summary: |
        STANDING-ORPHAN GAP, observed on 1374-4u6i (tlatoanis-macbook-air, 2026-09-25). This row's guard names an ADDED unreferenced fixture at push time; nothing answers the same question for a STANDING one. scripts/test-check-bash-dialect.sh was referenced by no gate step, build.sh, local-ci.sh or litmus, and one of its arms was red on both OSes from d2ceb4e4d (2026-09-05) to 2026-09-25. Meanwhile scripts/audit-guard-activation.sh reported its checker as `active: check-bash-dialect.sh <- build.sh`. The auditor enumerates check-* scripts only, so an active checker whose own fixture runs nowhere reads the same as a tested one. Yoga's count of standing orphans today is 42. Suggested next step: have the auditor (or a census) list, for each active check-*, whether its named fixture is reachable, starting with the 42. Bound as of f8d0cec22 (gate step 427-1374-4u6i), which is one of the 42 fewer.
