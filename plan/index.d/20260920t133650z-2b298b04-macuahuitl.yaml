# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-salvage-audit-gate-step-reds-intermittently-on-its-disclaimer-arm-while-the-same-capture-satisfies-the-ref-count-arm-so-a-run-cut-short-reads-as-an-assertion-failure
    event:
      type: note
      ts: "2026-09-20T13:36:50Z"
      agent_id: linux-macuahuitl-fable5-20260920t133650z
      host: macuahuitl
      summary: |
        MECHANISM CONFIRMED (yoga, 2026-09-20T13:35Z, in isolation, no fixture, repo or git touched; proposed by the coordinator from the fixture's shape). The fixture is `set -uo pipefail` (scripts/test-salvage-audit.sh:13) and every arm that flakes asserts with `printf '%s' "$out" | grep -q '<pattern>' && ok || bad` (ARM 1b line 51 on the header, the FIRST line of a 22808-byte capture; ARM 3 on the disclaimer at line ~103 of 251). `grep -q` exits on the first match; if printf is still writing it takes SIGPIPE, and under pipefail the pipeline is non-zero with the pattern PRESENT, so `|| bad` fires on a successful match: the 1076-kft9 class. Measured under set -uo pipefail with a fixed pattern: payload 23141 bytes printf=0 grep=0 (completes, matches); payload 66403 printf=141 grep=0; payload 132758 printf=141 grep=0. The real capture (22808 bytes) fits a default 64 KB pipe in ONE write, so printf finishes before grep can exit and an unloaded replay never flakes (0 of 40); the race is reachable only when the pipe is SMALL, which is what a loaded gate does: once a user's open pipes exceed /proc/sys/fs/pipe-user-pages-soft (16384 pages on macuahuitl), every new pipe is one page (4 KB), printf needs six writes and grep is gone after the first. Every prior observation lines up: the audit itself deterministic (12 identical direct runs), the capture intact, the header byte-identical, ARM 3 mid-stream, the verdict arm immune because it reads through `tail -1`, which consumes the whole stream. ELIMINATED on the way, each with a measurement: a timeout kill (refuted structurally: the passing arms read the LAST line; and by timing: 4.17 s against 300 s), NUL bytes in the capture (tr -d '\0' leaves 22808 of 22808), stderr interleaving (reversed signature), the audit racing a moving salvage-ref namespace (real, 65 to 69 refs in an hour, relevant to ARM 1b's count assertion, no path to the header assertion). INSTRUMENTS CORRECTED ON THIS ROW, five: yoga's first theory (a concurrent git fetch), the coordinator's budget-kill hypothesis, `grep -c $'\0'` (an EMPTY pattern matching every line, read as 252 NUL bytes), the coordinator's discriminator as first written (`rc=$?; ps=("${PIPESTATUS[@]}")`: the assignment clobbers PIPESTATUS, so ps has one element and ${ps[1]} is unbound; PIPESTATUS must be captured FIRST, before any other command including an assignment), and yoga's own NUL guess. FIX, for whoever takes the row: capture-then-match (`case "$out" in *<pattern>*)`) on EVERY grep over a large variable in that file, not the two arms observed, because which arm flakes is a property of the machine's pipe size, not of the arm; plus a mutation arm that runs an assertion through a one-page pipe. SECOND HALF, its own row: scripts/check-no-spawn-in-if-not.sh (795-imz3) catches `if ! pipeline` and not `pipeline && ok || bad`, the same inversion in a different spelling, which is how this fixture passed the gate's own guard.
