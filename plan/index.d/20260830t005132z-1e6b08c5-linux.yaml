# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: twin-tier-7b-experts-behind-an-openai-endpoint
    event:
      type: note
      ts: "2026-08-30T00:51:32Z"
      agent_id: linux-yoga-opus5-20260830t005132z
      host: linux
      summary: >
        MECHANISM FOR THE ~124s ROWS, found by yolanda and confirmed by yoga in the source. pipeline.rs:695-703 dispatches synthesis as tokio::task::spawn_blocking and enforces the tier budget with tokio::time::timeout around the resulting JoinHandle. Dropping a JoinHandle DETACHES the blocking task; tokio cannot cancel one, and a synchronous socket read has no cancellation point. So an overrun frees the CALLER (correctly degrading to the named retrieval-only fallback) while the blocking thread runs on to the socket budget at pipeline.rs:87, Duration::from_secs(120). Evidence: yolanda's four sequential requests on a fresh server alternate 12452 / 123259 / 25979 / 123361 ms — the two slow rows 102 ms apart, 0.08%, which contention cannot reproduce; yoga's default cell carries 124292 and 124567 ms on a host with ~9 GB free and no VM split, killing the memory-pressure story. YOGA'S REFINEMENT to the open queuing question: it is not tokio's blocking pool (default 512 threads, two leaks cannot exhaust it) but ollama's single runner slot — the container starts with OLLAMA_NUM_PARALLEL:1 and runner.parallel=1, so the leaked generation holds the only slot and the NEXT request queues at the inference server. That predicts alternation rather than degradation, which matches. FALSIFICATION TEST, not yet run: set OLLAMA_NUM_PARALLEL=2 — the alternation should vanish while the 123s leak remains. CONSEQUENCE: the tier budget is a promise to the caller, and the user is not the caller. The 60s clamp bounds synthesis and does not bound the request. This is a defect in the fail-loud surface itself (920-pxg6), not a tuning result — NOT fixed here; a cancellation redesign mid-experiment is out of scope and is being filed with yolanda's reproduction.
