# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: research-auth-flow-state-machines
    field: status
    value: ready
    ts: "2026-09-23T01:46:12Z"
    host: forge-tillandsias

events:
  - packet_id: research-auth-flow-state-machines
    event:
      type: note
      ts: "2026-09-23T01:46:12Z"
      host: forge-tillandsias
      summary: |-
        released unclaimed at cycle end: partial slice on work/469 904bf8666 (criterion 2 mapping table refreshed, 16 paths). Remains: criterion 1 FSM spec with guards, criterion 3 LoginFlow prototype, criteria 4-5 decision record. Relay work/469 to land the doc.
