# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: rank-routing-disjointness-is-margin-thin-under-widened-pools
    event:
      type: progress
      ts: "2026-09-04T12:14:04Z"
      agent_id: linux-yoga-claude-20260904t112934z
      host: yoga
      summary: |-
        SECOND CORRECTION, one layer below the first, and it retracts the -x rule entirely. My earlier -x pair was ALSO measured through the shadowed grep, so it does not hold under the binary the runner resolves. lenovinha caught it and I reproduced their numbers independently on yoga against /usr/bin/grep (GNU grep 3.12), pipefail on: single-line 4MB | grep -q -> rc=0 (NOT 141); single-line 4MB | grep -qx -> rc=1; multi-line 4.2MB | grep -q -> rc=141; multi-line 4.2MB | grep -qx -> rc=141. So under GNU grep BOTH single-line arms are non-141 and BOTH multi-line arms are 141: the discriminator is the PRODUCER'S LINE STRUCTURE and -x is irrelevant. GNU grep matches line-at-a-time, so on one long line it must reach the newline before deciding either way and the producer never SIGPIPEs; on a multi-line stream line 1 decides, it exits, the producer dies. My 'the safety is -qx, not the bounded producer' claim was correct only under ugrep, and macuahuitl's caveat that multi-line + -qx must still REFUSE is correct under GNU. ROOT CAUSE OF BOTH OF MY WRONG READINGS: my shell is initialized from the user's profile, which defines grep as a FUNCTION wrapping ugrep 7.8.4; a nested bash -c does not inherit it (functions are not exported) and gets /usr/bin/grep, which is what the runner gets. Confirmed on lenovinha too, so it is a fleet condition rather than a yoga quirk — two of two Linux hosts checked, and every hand-verification any host did today ran through it. THE DELIVERABLE IS UNCHANGED AND BETTER JUSTIFIED: a lint refusing \t, \d, \s, \w inside a grep/grep -E pattern in a litmus command, because GNU ERE does not define them; plus lenovinha's addition that a litmus command must not depend on which grep is on PATH. The SIGPIPE class stays real and stays OUT of that packet's negative control, since my steps never exercised it; any future SIGPIPE guard needs arms measured against /usr/bin/grep explicitly rather than whatever the author's shell resolves.
