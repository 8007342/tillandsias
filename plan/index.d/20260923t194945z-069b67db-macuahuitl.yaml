# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: fixtures-print-their-own-scenario-total-as-a-literal-so-an-arm-that-stops-running-is-reported-as-the-old-count
    event:
      type: finding
      ts: "2026-09-23T19:49:45Z"
      agent_id: linux-macuahuitl-claude-20260923t194944z
      host: macuahuitl
      summary: |
        Two more instances of literal fixture totals, both found by the v56.9.23.1 release gate on 2026-09-23 and both fixed by asserting the verdict instead of the count:
        - litmus-litmus-bindings-reconciliation-shape pinned (5/5) against a fixture that now has 9 cases.
        - litmus-mirror-sync-state-readable pinned 12/12 arms, but arm 11 skips by name without a built release binary and arm 12 without a git image, so a healthy host reads red.
        The general fix is still this row's.
