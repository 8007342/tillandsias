# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: reopening-a-terminally-closed-packet-reds-the-gate-so-the-sanctioned-reopen-path-cannot-be-landed
    event:
      type: progress
      ts: "2026-09-13T03:19:59Z"
      agent_id: linux-lenovinha-opus5-20260913t031959z
      host: linux
      summary: |
        CLAIM — lenovinha. plan_next's top claimable that is actually actionable here:
        829-dkuc is multi_cycle/12h and its own next_action asks for an operator-paired
        session; 900-z3kv is mine with its (a)/(b) decision fenced for the operator; so
        this is the top row I can finish.

        Scope: scripts/check-fragment-status-loss.sh and the fixture this row's
        `unscoreable` prescribes.

        THE FIX I INTEND, stated before writing it so it can be checked against the row
        rather than against itself: thread the TIMESTAMPS into the join. A terminal
        event OLDER than the current status transition is HISTORY; one NEWER than it, or
        with no transition at all, is the loss this guard exists to catch. The row says
        `declared_events` carries packet_ids only and that FoldProvenance::lww_wins
        already keys (packet id, field) to fragment/ts/host — so the discriminator
        exists in the ledger and merely is not reaching the guard.

        WHAT I WILL NOT DO, because the row names it as the attractive wrong turn:
        widen the accepted-status set. Accepting `in_progress` against a `completed`
        event would delete the guard's actual subject — a closure whose transition never
        landed, 11 of 21 such packets on 2026-08-09. The whole value of this guard is
        that case, and a fix that scores green by removing it is worse than the bug.

        THE MIDDLE ARM IS THE ONE THAT MATTERS. Three arms: reopen-accepted,
        unapplied-closure-STILL-refused, newer-terminal-refused. The row says outright
        that without the middle one, DELETING THE CHECK scores two green arms. I will
        verify each arm reds on the pre-fix tree for the right reason, not just that the
        suite goes green — three times this session a verification of mine ran and
        proved something adjacent to what I claimed, so the arms get scored
        individually.

        I have run this guard on every land this cycle, so I know its green path well;
        that is familiarity with its OUTPUT, not with its join, and I will read the join
        rather than assume it.
