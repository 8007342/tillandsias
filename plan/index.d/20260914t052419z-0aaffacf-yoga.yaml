# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: deleting-a-salvage-ref-refuses-every-in-flight-gate-whose-trunk-snapshot-predates-the-deleted-marker-because-the-ledger-checker-reads-only-the-local-copy
    field: verifiable_closure
    value: bash scripts/test-salvage-refs-ledger.sh — a line absent from origin and unmarked locally but MARKED on trunk yields ok:salvage-refs-ledger:<n>:marker-on-trunk:<ref> at exit 0 with a merge-trunk line on stderr; NEGATIVE CONTROL, absent and unmarked in BOTH copies still refuses violation:salvage-refs-ledger; an unreadable trunk copy refuses rather than forgiving (git show writes zero bytes on a missing ref); and sweep-salvage-refs.sh carries the one-pass-after-the-marker deletion rule. Bound at scripts/gate-steps.d/325-1173-a5ng.step.
    ts: "2026-09-14T05:24:19Z"
    host: yoga

events:
  - packet_id: deleting-a-salvage-ref-refuses-every-in-flight-gate-whose-trunk-snapshot-predates-the-deleted-marker-because-the-ledger-checker-reads-only-the-local-copy
    event:
      type: note
      ts: "2026-09-14T05:24:19Z"
      host: yoga
      summary: |-
        guard exists; moving the text out of unscoreable per 1136-n8sh
