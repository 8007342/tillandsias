# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260812t151500z
events:
  - packet_id: in-forge-delegate-disposition-unobservable
    event:
      type: progress
      ts: "2026-08-12T15:15:00Z"
      agent_id: linux-mutable-session-20260812
      host: linux_mutable
      summary: >
        CONFIRMED LIVE 2026-08-12, cleanly separated from the DOA. With the
        clone race fixed (fresh --init image), a BigPickle delegate launched,
        cloned, and ran an agent healthily — then ended leaving NO recoverable
        host-side disposition: no 682-z5h8 ledger event, no pushed branch, no
        readable launch log, forge torn down, opencode process gone. So
        stage-0 delegation now WORKS (clone unblocked) but the host still
        cannot tell what a delegate concluded. This is the gating blocker for
        the 689-zwzm ramp: a durable delegate-outcome channel (a marker under
        the boundary tmp dir, a required ledger event, or a forge-side status
        the tray exposes) must exist before scaling beyond one delegate. The
        operator can read the ptyxis window, but the LOOP cannot.
