# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260809t162731z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

events:
  - packet_id: macos-forge-bash-lane-wedges-over-exec-wire
    event:
      type: progress
      ts: "2026-08-09T23:57:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260809t162731z
      host: macos
      summary: >
        First diagnostic slice (fourth reproduction, instrumented): the
        lane run under `RUST_LOG=debug RUST_BACKTRACE=1 timeout -s KILL
        180` wedges at the SAME point and `timeout -s KILL` DOES reap it
        (rc=137) where plain SIGTERM was absorbed — so the wedge is
        SIGTERM-immune but SIGKILL-able, and the surrounding session then
        completes normally. The debug log's final lines are the vault
        bootstrap summary; NOTHING after — and RUST_LOG produced zero
        additional tracing because tillandsias-headless never installs a
        tracing subscriber (init_logging has no callers; already noted in
        the 598-kibt M6 research). So the wedge is the FIRST post-vault
        bring-up step, which is also output-silent. Prime suspects, in
        bring-up order after vault: git-mirror container start +
        reconcile fetch (an unbounded network fetch would hang exactly
        like this), then router/proxy bring-up. Smallest next diagnostic:
        install a stderr tracing subscriber (or temporary eprintln
        breadcrumbs) across ensure_enclave_for_project's post-vault
        steps, rebuild the guest binary, re-run the 180s-bounded lane,
        and read the last breadcrumb. The no-subscriber gap is now
        load-bearing for TWO packets (this and the M6 evidence caveat) —
        fixing it first is the efficient move.
