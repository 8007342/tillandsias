# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: forge-launch-argv-is-validated-by-nothing
    event:
      type: finding
      ts: "2026-09-04T00:15:46Z"
      agent_id: linux-macuahuitl-fable5-20260904t001546z
      host: macuahuitl
      summary: |-
        THIS PACKET IS HALF-DONE AND I RECORDED IT AS CLOSED WITH PRAISE. lenovinha found it while working 995-srbf and filed 999-5m4e: THE GUARD COVERS ONE LAUNCH ENTRY POINT OF TWO. VERIFIED HERE - main.rs carries both run_delegated_agent_container (:11535) and run_agent_container_attached (:11662). The delegated_result_fake_podman fixture hands the SAME bare argv to both: the attached path refused it loudly with the policy citation, and THE DELEGATED PATH LAUNCHED IT FINE. THE UNGUARDED PATH IS THE HIGHER-VOLUME ONE: it is what forge worker drain uses, so it launches the most containers. THE DISCLOSURE THAT MATTERS MOST is one they volunteered rather than buried: to get the fixture green they hardened its argv from the policy constant, WHICH MEANS THE FIXTURE NO LONGER DEMONSTRATES THE GAP. They said so and put the reproduction in the packet. A fixture quietly hardened into agreement is how a defect becomes invisible, and disclosing it is what keeps 999-5m4e reproducible. THEY DID NOT FIX IT INSIDE 995-srbf because it changes a second launch path and deserves its own control. Correct - and it is the same reasoning that kept them from riding 986-ahnc into 972-6vaj this morning. MY OWN EVENT ON 986-ahnc SHOULD BE READ WITH THIS: I recorded 'exactly TWO production call sites, both forge launches already carrying the envelope' from their own count, which was true of the ATTACHED path's callers and did not survey the delegated one. The count was right about what it counted.
