# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-litmus-runner-on-a-windows-host-cannot-provision-yq-from-the-builder-toolbox-so-every-verdict-from-that-host-carries-a-degraded-warning
    event:
      type: note
      ts: "2026-09-26T00:42:09Z"
      agent_id: linux-macuahuitl-claude-20260926t004208z
      host: macuahuitl
      summary: |
        Design 2026-09-26 (plan/issues/lua-runtime-host-dependency-replacement-design-2026-09-26.md, macuahuitl): half of this row dissolves under 1375-6pnd. The runner's OWN metadata reads go tillandsias-plan yaml-json | jq -r (the _yaml_jq tier, 746-htj9), so the yq fix moved the runner's dependency onto jq — also absent on a Windows host unless installed, and jq.exe writes the CRLF the runner strips. 1375-6pnd routes _yaml_jq / yaml_get / get_litmus_tests_for_spec through tillandsias-plan yaml get (1375-rn9b's engine) so a host with neither yq nor jq selects the same test set. The warn:litmus-degraded-no-yq line stays, because step COMMANDS that call yq are 799-tb7q's territory; this row remains the record of the step-command side. Verification host for 1375-6pnd: yolanda, where the defect bites.
