# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: set-field-on-a-list-valued-field-reads-it-as-unset-and-writes-a-string-so-the-row-vanishes-from-every-tag-query
    field: verifiable_closure
    value: 'cargo test -p tillandsias-plan set_field_list_guard — set-field on a field whose folded value is a SEQUENCE refuses (refused:set-field:list-valued-field, rc 2) and writes no fragment, so the row keeps matching its tag queries; NEGATIVE CONTROLS: scalar fields still write, an absent field is still writable, and 1151-td46''s prose drop guard still fires. Three tests; both mutation directions red by name.'
    ts: "2026-09-14T15:39:45Z"
    host: yoga

events:
  - packet_id: set-field-on-a-list-valued-field-reads-it-as-unset-and-writes-a-string-so-the-row-vanishes-from-every-tag-query
    event:
      type: note
      ts: "2026-09-14T15:39:45Z"
      host: yoga
      summary: |-
        guard exists; moving the text out of unscoreable per 1136-n8sh
