# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: builder-toolbox-init-set-too-small-for-the-dispatch-pattern
    field: next_action
    value: 'INIT-SET SLICE IS DONE (lenovinha 2026-08-23, +8s init cost, jq/yq/rg/openssl verified in-toolbox). What remains is the SWEEP, and it needs one decision made first: NOT EVERY NAMED CALLER CAN TAKE THE TOOLBOX FALLBACK. tray-diagnose.sh and diagnose-macos-provision.sh run on end-user machines with no tillandsias-builder toolbox, where the documented fallback degrades from a clear host-install instruction to a confusing toolbox error. Split the ~50 callers into DEV-HOST scripts (take the fallback) and SHIPPED-DIAGNOSTIC scripts (need a different answer), then sweep the first group only. The two nix capability probes judging by host binary alone are independent of that decision and can go first.'
    ts: "2026-08-23T17:49:45Z"
    host: lenovinha
