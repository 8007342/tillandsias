# Ledger fragment — append-only, IMMUTABLE once written.
status:
  - packet_id: selector-drop-host-tool-dependency
    field: status
    value: done
    ts: "2026-08-13T17:30:00Z"
    host: windows

events:
  - packet_id: selector-drop-host-tool-dependency
    event:
      type: progress
      ts: "2026-08-13T17:28:00Z"
      host: windows
      summary: >
        RUNG 2, closing the packet. scripts/select-work-batch.sh now takes its
        entire projection from `tillandsias-plan select-rows` and
        `blocking-counts`. Nineteen jq calls are gone, and so is the jq
        preflight that guarded them — the dependency is deleted rather than
        satisfied per host, which was the packet's stated rationale (satisfying
        it repeats the yq/ruby exposure recorded 2026-08-03).
        `blocking-counts` is deliberately NOT folded into `select-rows`.
        select-rows answers "what may I pick" and is filtered to
        dependency-clear and unleased; the graph weight must count edges from
        EVERY ready packet, including the ones select-rows excludes, because a
        packet in another column is frequently the thing this one waits on.
        Folding them would score the frontier against a filtered subset —
        silently, and in the direction that looks fine.
  - packet_id: selector-drop-host-tool-dependency
    event:
      type: verified
      ts: "2026-08-13T17:29:00Z"
      host: windows
      summary: >
        Closure as the packet states it, both halves.
        (1) `litmus:cycle-batch-triage-shape` green — 14/14, 100%. Its step 11
        was INVERTED rather than deleted: it used to assert that an absent jq
        produced `refused:missing-tool`, which is a refusal the script can no
        longer produce, and a test that keeps passing while describing a program
        that no longer exists is worse than no test. It now asserts that with
        TILLANDSIAS_JQ naming a nonexistent binary the selector EMITS A BATCH.
        (2) Run on a PATH with jq shadowed out entirely (a temp dir of symlinks
        to bash/awk/sed/grep/… and no jq): `batch:` and `triage: eligible=45`
        emitted normally.
        PARITY MEASURED, not assumed: batch/triage/frontier output for roles
        windows, any and linux is BYTE-IDENTICAL to the pre-change baseline
        captured before touching the script. Same epics, same scores, same
        probabilities, zero jq.
        THE NEGATIVE CONTROL CAUGHT ME. The first draft collapsed the empty-pool
        checks into one refusal, so a plan binary that exits 0 and prints
        nothing produced `refused:no-eligible-work` — a counterfeit drained
        ledger, which is the exact defect this packet exists to remove,
        reintroduced by its own fix. Output volume cannot separate
        empty-because-drained from empty-because-broken, so the script now asks
        a different question: `capabilities` (order 569) is the binary's
        compile-time capability set, so a binary too old to project rows says so
        about itself. Verified live: a stand-in binary now yields
        `refused:stale-plan-binary`, not a drained ledger.
        The retired assertion's CLASS is retained as its own step — an
        environment fault must still be refused on its own token — now exercised
        through the stale-binary path, which is the live instance of that class
        precisely because this change added a subcommand every host's binary
        predates until it rebuilds.
        cargo test -p tillandsias-plan = 129 + 15 passed.
        KNOWN CONSEQUENCE, stated rather than discovered later: every host must
        rebuild tillandsias-plan before its next cycle. This checkout's Windows
        .exe is stale right now and refuses with the exact actionable message
        (`does not support select-rows … rebuild with cargo build --release -p
        tillandsias-plan`); the WSL-built binary is current. That refusal is the
        guard working, not a regression.
