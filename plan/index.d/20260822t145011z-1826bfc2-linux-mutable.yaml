# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    field: next_action
    value: 'CAMPAIGN STATE 2026-08-22T14:49Z, single host (macuahuitl), and this is the FINAL cycle of the hourly loop. b measured 0.33 steady-state (4 rows filed / 12 closures) or 0.075 over the whole window; ready 410 (08-19) -> 386 (08-22), net -24 against a pre-campaign +26/day. Routing rule holding at 36:1 events-to-rows. DO NOT compare 0.33 to the old 1.80 — that was five hosts doing feature work, this is one host doing closure and repair. NEXT, in order: (1) 552 delta re-embed — priced at ~490s per full rebuild vs 0.6% of chunks actually changing (~325x waste); part 2 of 4 (content-hash stale detection) ALREADY WORKS; the hazard is that the embed loop is line-positional and its per-batch arity check is structurally blind to a mis-joined cache hit, so delta needs its own per-chunk identity assertion plus a negative control — written into 552 next_action, do not skip it. (2) 846-idhn p3 — the capabilities channel has no base representation, which is why three fragments can never be folded and the overlay cannot fully drain; size concern, not loss. (3) 821-73es — BLOCKED ON THE OPERATOR, needs a decision not an implementation: a single global cosine threshold cannot work (in-corpus floor 0.6818 vs near-miss ceiling 0.6889, bands OVERLAP by 0.007), so choose per-corpus thresholds or a top1-vs-top2 margin test. DONE SINCE THE LAST ROADMAP, both were listed above as open: 843-624y (compaction safety, closed) and the embed-cut chunks (now 0 chunks / 0 chars over, was 2 / 11,191). COMPACT IS NO LONGER A P0 HAZARD but read this first: as of today compact refuses unfoldable fragments (10/10) AND no longer mutates on --help or an unknown flag (23/23) — before today `compact --help` performed the compaction, which is how it was found. Use `compact --dry-run` or `fragments` to inspect. FLEET REJOIN: R1 claiming and R4 seed separation both MEASURED satisfied 2026-08-22; second Linux host first and it must pass --seed <hostname> explicitly, because the default seed is host KIND + date and two Linux boxes collide by construction. Measure b over its first three days before adding macOS.'
    ts: "2026-08-22T14:50:11Z"
    host: linux_mutable

events:
  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    event:
      type: note
      ts: "2026-08-22T14:50:11Z"
      host: linux_mutable
      summary: >
        roadmap refresh: 2 of 4 items closed; compact prohibition narrowed; rejoin bar measured
