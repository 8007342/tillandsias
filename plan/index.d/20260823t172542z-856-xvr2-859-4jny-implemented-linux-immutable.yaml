# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_immutable (pirria), 2026-08-23T17:25:42Z.
events:
  - packet_id: capability-document-does-not-survive-a-serde-json-roundtrip
    event:
      type: implemented
      ts: "2026-08-23T17:25:42Z"
      agent_id: linux-pirria-opus5-20260823T170000Z
      host: linux_immutable
      summary: >
        FIXED, with the workspace-wide risk actually measured rather than
        asserted. `serde_json = "1"` -> `{ version = "1", features =
        ["float_roundtrip"] }` (Cargo.toml:56).
        accel_probe::tests::test_serialization_roundtrip now PASSES on this
        host; it failed here on an unmodified tree because system_ram_gb
        (15.349456787109375) drifted 1 ULP through serde_json's default
        fast-but-lossy float parser.

        THE REASON I DEFERRED IT TWICE WAS THAT THIS IS A WORKSPACE-WIDE
        DEPENDENCY CHANGE, so the deferral is only discharged by measuring it:
        `cargo test --workspace --bins --lib` after the change is 93 passed,
        0 failed, 0 panics. Nothing depended on the lossy parse.

        EXIT CRITERION 2 IS NOT CLOSED BY THIS and should not be marked so: it
        asks for a DECISION on whether workspace unit tests run in any gate a
        joining host executes. They still do not — ./build.sh --check is
        `cargo check --workspace` (build.sh:402), which COMPILES the tests and
        never RUNS them, which is why this one sat red behind a green gate for
        an unknown period. That decision is a fleet call, not this host's.

  - packet_id: the-local-gate-is-slower-than-the-fleets-push-interval-and-misreports-stale-instruments
    event:
      type: implemented
      ts: "2026-08-23T17:25:42Z"
      agent_id: linux-pirria-opus5-20260823T170000Z
      host: linux_immutable
      summary: >
        BOTH HALVES CLOSED, and the first was closed by someone else — verified
        rather than reimplemented.

        CRITERION 1 (a stale plan binary is reported AS staleness) is already
        satisfied by 851-cduu's `ensure_fresh_plan_binary`
        (scripts/plan-binary-probe.sh), which mtime-checks the resolved binary
        against crates/tillandsias-plan + Cargo.lock, rebuilds in-locus when
        stale, and requires callers to refuse loudly on rc=2. Verified
        EMPIRICALLY here, not by reading: touching a crate source and running
        the gate now yields

          fail:set-field-yaml-shapes:stale-plan-binary — the resolved binary
          predates this tree and could not be rebuilt in this locus

        instead of the "set-field can write an unparseable ledger fragment"
        this packet was filed about. Timeline worth keeping: the helper landed
        21:16:42, my misreported failure was 21:36:12, and
        test-set-field-yaml-shapes.sh ADOPTED the helper at 22:27:43 — so the
        mechanism existed 20 minutes before the failure and that gate had not
        yet been wired to it.

        A SILVERBLUE NUANCE THE HELPER'S DESIGN DID NOT ANTICIPATE: its
        self-healing arm calls `cargo build` directly, and on an immutable host
        cargo is not on the host PATH — it lives in the tillandsias-builder
        toolbox. So on Silverblue the helper DETECTS staleness and always takes
        the rc=2 "could not be rebuilt in this locus" path. That is the safe
        direction (refuse rather than consume) and needs no fix, but any future
        "rebuild it for the caller" work should know the rebuild never fires
        here.

        CRITERION 2 (a supported land path) delivered as
        scripts/land-on-platform-branch.sh: fetch, rebase, gate, push, then
        PROVE the commit landed with `git merge-base --is-ancestor HEAD
        origin/<branch>` against a freshly fetched ref. It carries the two bugs
        the ad-hoc version had, documented in its header so the next host does
        not reacquire them: a `git push | tee | tail` pipeline yields TAIL's
        exit status, and grepping output for "<branch> -> <branch>" also matches
        `! [rejected]  <branch> -> <branch> (fetch first)`. Together those
        reported LANDED for a refused push. Neither a zero exit status nor a
        ref-update line is sufficient evidence; only the remote is.
