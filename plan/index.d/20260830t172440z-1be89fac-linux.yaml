# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: network-architecture-audit
    field: next_action
    value: 'Nine slices landed. THE NA-05 PATCH LIST HAS EXACTLY ONE OPEN WORK ITEM: P4''s vsock-transport half of the platform matrix (host-guest-transport/spec.md names hvsock; vsock-transport/spec.md has two matches across the whole hvsock/virtio-vsock/unix-socket vocabulary — add the section-4 matrix as normative text there). Everything else is done, refuted, blocked on a dependency, or an operator decision: P1 needs the git-mirror-egress verdict (plan/issues/git-mirror-egress-spec-divergence-audit-2026-08-10.md); P6''s remainder is wire-builds-through-:3129-or-delete-the-port. ALSO OPEN, ordinary: sections 2, 4 and 7 carry line citations — mechanical, but the expensive half is re-verifying each claim, not re-anchoring it. THE ACTUAL COMPLETION GATE is unchanged and is not a worker''s to satisfy: opencode-bigpickle, antigravity-gemini and codex-gpt55-highthink still owe verified-by events on NA-01..NA-06. A verifier should know that four of the six criteria now have artifacts that did not exist at the 2026-07-14 verification: openspec/specs/network-scenarios/spec.md (NA-02, new), the section-3 rewrite (NA-03), the enclave-network and proxy-container and headless-mode spec rewrites (NA-05), and section 6 + section Observation (NA-06). FOR THE PACKET OWNER, ninth request: NA-05''s exit criterion mandates ''specific file:line references'' and now contradicts eight slices, four spec rewrites, one new spec and a cheatsheet.'
    ts: "2026-08-30T17:24:40Z"
    host: linux

events:
  - packet_id: network-architecture-audit
    event:
      type: note
      ts: "2026-08-30T17:24:40Z"
      host: linux
      summary: >
        P5 done; one open work item left on the patch list
