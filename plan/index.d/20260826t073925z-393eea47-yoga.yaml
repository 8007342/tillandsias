# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: hash-image-sources-git-normalized
    field: next_action
    value: 'Windows verification is the only remaining criterion. yolanda has a live RED baseline (FAIL: forge hash depends on checkout location) and has agreed to run scripts/test-hash-image-sources.sh on both Git Bash and WSL and report raw output including a failure. Close only on that evidence. Do NOT close on a Linux-only PASS. Note the exec-bit scenario no longer needs the core.fileMode gate criterion 2 asks for — index-based hashing made it platform-independent via git update-index --chmod=+x — so criterion 2 should be restated rather than treated as unmet.'
    ts: "2026-08-26T07:39:25Z"
    host: yoga

events:
  - packet_id: hash-image-sources-git-normalized
    event:
      type: progress
      ts: "2026-08-26T07:39:25Z"
      host: yoga
      summary: >
        status 'Windows verification is the only remaining criterion. yolanda has a live RED baseline (FAIL: forge hash depends on checkout location) and has agreed to run scripts/test-hash-image-sources.sh on both Git Bash and WSL and report raw output including a failure. Close only on that evidence. Do NOT close on a Linux-only PASS. Note the exec-bit scenario no longer needs the core.fileMode gate criterion 2 asks for — index-based hashing made it platform-independent via git update-index --chmod=+x — so criterion 2 should be restated rather than treated as unmet.' (not terminal) with evidence_refs: Linux fixture PASS incl. new CRLF-invariance arm; mutation-tested; yolanda verified index stability across both userlands
