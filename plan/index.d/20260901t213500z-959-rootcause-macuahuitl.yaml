# Ledger fragment — append-only, IMMUTABLE once written.

status:
  - packet_id: forge-pids-ceiling-512
    field: status
    value: in_progress
    ts: "2026-09-01T21:35:00Z"
    host: macuahuitl

events:
  - packet_id: forge-pids-ceiling-512
    event:
      type: note
      ts: "2026-09-01T21:35:00Z"
      host: macuahuitl
      summary: >
        ROOT-CAUSED by pirria, five-way fingerprint, both hypotheses
        killed: the live 512 is crates/tillandsias-headless/src/tray/
        mod.rs:2157 .pids_limit(512) in build_launch_spec — the site that
        launches every tray-minted forge. 65fe4e766 (667-se87) was a
        2-OF-5-SITES PARTIAL FIX: it raised main.rs:3236 and :6510 to
        4096 with the rationale comment, and missed tray/mod.rs:2157,
        container_profile.rs:275/295/318/336 (all four profiles), and
        orchestrate-enclave.sh:322/404 — the comment traveled only with
        the edited sites, making the missed ones read as intentional.
        container_profile.rs:1052 pids_limits_match_container_roles PINS
        512 with a comment naming the exact workload 667-se87 proved 512
        cannot hold — value and test must move together. Hypotheses
        killed: the shipped stable binary CONTAINS 65fe4e766 (ancestor of
        every v56 tag — the fix never touched the launch site); no
        containers.conf exists anywhere in the guest (podman's own
        default would read 2048). Counters: 240 kernel-counted denials
        across both floor hosts, both peaks pinned at exactly 512.
        COORDINATOR RULING on the pair's step (d): HELD on both hosts —
        a workspace build under this ceiling is the recorded 667-se87
        silent-session-death shape, the reproductions it would buy are
        already two-host evidenced, and the timing it would produce is a
        number about the ceiling, not the floor. FIX CLAIMED by
        macuahuitl, next slot: all five remaining sites to 4096 with the
        rationale comment ON EACH, the pinning test moved with the value,
        tray rebuild+relaunch here. BLOCKED-EXIT recorded for 667-se87's
        own criterion (peak>512 sampled in-guest is unreachable while the
        guest caps at 512); post-fix a fresh 4096 guest samples it, one
        piece of evidence closing both orders.
