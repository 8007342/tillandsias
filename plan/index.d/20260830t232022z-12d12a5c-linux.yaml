# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: claim-events-change-no-status-so-claiming-separates-nobody
    event:
      type: progress
      ts: "2026-08-30T23:20:22Z"
      agent_id: linux-lenovinha-claude-20260830t231435z
      host: linux
      summary: >
        THE DEFERRED NIT WAS WORSE THAN I RECORDED: two of the three argument forms HUNG, they never reached a wrong value.  Last cycle I wrote that reclaim-stranded-claims.sh "silently yields 4 rather than erroring" when --ttl-hours is passed without a value, and left it. Tested this cycle:   * `--ttl-hours` bare -> HANG until killed.   * `--now` bare -> HANG until killed.   * `--ttl-hours abc` -> refused, but the machine-readable verdict said     `mode=refused-expire-claims-failed`, blaming the TOOL for the CALLER's bad     argument; the real reason appeared only on the following stderr line.  THE MECHANISM. `--ttl-hours) TTL_HOURS="${2:-4}"; shift 2` with the flag passed bare has one argument to consume. Under `set -uo pipefail` (no `-e`) the `shift 2` FAILS, `$#` is unchanged, and the while loop re-reads the same argument forever. The `${2:-4}` default is never reached -- which is why my "silently yields 4" note was wrong. Had the shift succeeded it would have been worse in a different way: reaping at 4 HOURS instead of the documented 24, returning live claims six times early, in a reaper the meta-orchestration skill already warns "launders finished work into lost work" (833-fpe7). A typo must not be able to choose a shorter TTL.  FIXED. Every value-taking flag now requires its value and refuses by name (`mode=refused-missing-value:--ttl-hours`). TTL is validated in the WRAPPER so a bad argument reports its own cause (`mode=refused-bad-ttl:abc`) instead of implicating expire-claims. Zero is refused separately and explicitly, because `--ttl-hours 0` would reclaim every live claim -- the laundering hazard reached by a typo.  THREE FIXTURE CASES ADDED to scripts/test-reclaim-stranded-claims.sh (3/3 -> 6/6): bare flags refuse rather than hang (with a timeout, because a regression here is a HANG and a hung fixture in a gate is indistinguishable from a slow one); a non-positive-integer TTL refuses with its own cause; and a POSITIVE control, because the first two arms are both satisfied by a script that refuses everything. Mutation-tested: restoring the original parser makes case 4 fail with "bare --ttl-hours HUNG (the pre-943-unii defect)".  AND THE FOURTH INSTANCE OF THIS WEEK'S SHAPE, which is the part worth carrying: that fixture had NO CALLER either. Nothing ran it -- not build.sh, not any litmus. So the state was a reaper nothing invokes, guarded by a fixture nothing invokes, both correct and both inert. It is now wired into ./build.sh --check, which is how I know the fix holds.  Running total of this shape found in four cycles: --emit-flow (943-7dn5), the delegate liveness probe (943-3xyf), reclaim-stranded-claims.sh itself (last cycle), and now its fixture. Four tools that pass their own tests and that nothing calls. That is not four coincidences; it is what happens when "the capability exists" is the thing checked and "something invokes it" is not.  STILL NOT THE DECISION. This packet's choice -- claiming as mutex versus claiming as audit record -- remains the operator's/coordinator's. What changed is that if the exclusive branch is ever taken, the reaper it depends on no longer hangs on a typo and is now actually exercised by the gate.
