# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: jq-loop-callers-need-restructuring-not-dispatch-conversion
    field: status
    value: ready
    ts: "2026-09-26T03:27:03Z"
    host: linux

events:
  - packet_id: jq-loop-callers-need-restructuring-not-dispatch-conversion
    event:
      type: note
      ts: "2026-09-26T03:27:03Z"
      host: linux
      summary: |-
        released unclaimed: HELD by the coordinator pending the jq-retirement design, now 1375-2x4e, which absorbs it. The quoting fix is on origin/work/914-ahsy at 5bc53b357 (unquoted JQ at 115 sites plus test-jq-multiword-prefix.sh, 3/3 with the fix and 2/3 without). Not landed.
