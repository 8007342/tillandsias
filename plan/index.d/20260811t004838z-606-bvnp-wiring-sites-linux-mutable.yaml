# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260811t004838z
events:
  - packet_id: git-mirror-cross-project-service-identity
    event:
      type: progress
      ts: "2026-08-11T00:48:38Z"
      agent_id: linux-mutable-20260811t004838z
      host: linux_mutable
      summary: >
        Identity provisioning wired into ALL remaining mirror-CREATE sites
        (finding 2 of the vault-rungs fragment closed); packet stays ready —
        transport rungs T3-T13 still untouched.

        All four production build_git_run_args callers now pair with
        ensure_mirror_identity_provisioned, provision-before-mint, create
        path only: status-check (run_status_check), opencode CLI
        (run_opencode_mode), opencode web (run_opencode_web_mode), plus the
        already-landed forge-launch create branch
        (ensure_shared_git_and_inference_for_launch). The three new sites are
        create-by-construction — cleanup_stack_containers removes the
        per-project mirror and the lanes recreate it unconditionally
        (--replace); none has a running-mirror reuse branch, so the
        cheap-on-reuse property is the provision's own one-kv-read fast path.

        Failure semantics follow each site's existing vault-dependency
        posture: both opencode lanes hard-fail (`?`) — ensure_vault_running
        precedes and the relay-credential mint below already fails loud
        (windows-260716-2); status-check tolerates-but-LOUD (eprintln
        WARNING, not debug-gated), matching its credential-less-mirror
        tolerance — the status lane must keep diagnosing hosts whose vault
        is down, and nothing consumes the identity yet (sshd wiring is T4+).
        Note the status lane provisions under its fixed pseudo-project name
        tillandsias-status-check: one idempotent kv entry + role/policy pair,
        forward-correct for when T4+ makes mirror sshd need an identity.

        Coverage pin: new source-window test
        every_mirror_create_lane_provisions_the_service_identity asserts,
        per lane window, (a) a provision call exists, (b) it precedes the
        mint, (c) exactly-once — which pins the forge-launch reuse branch
        (same window) never gaining a Vault touch. cargo test -p
        tillandsias-headless 325 passed (was 324); ./build.sh --check green.

        REMAINS (packet stays ready): T3-T13 as listed in the vault-rungs
        fragment — sshd in the git image, host cert at mirror start,
        sshd_config/receive wrapper, repo hardening, sidecar + AppRole
        ssh-lane-signer-<mirror-id>, forge wiring, negative matrix M0-M7,
        audit volume T9, 659-8faj DNS swap. Design still UNSIGNED; Q1/Q2
        operator-gated. Noticed, not fixed (pre-existing): non-vault builds
        (--no-default-features) hard-fail both opencode lanes at the mint
        already; the new provision call fails one step earlier with the
        read_and_handover_root_token stub's message — same lane outcome,
        different first error string.
