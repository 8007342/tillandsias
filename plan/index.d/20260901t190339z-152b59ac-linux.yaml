# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: nvidia-driver-update-strands-inference-container
    field: next_action
    value: 'EC4 only: write a durable host-side launch-failure breadcrumb on Linux (greenfield; anchor at tillandsias_core::config::state_dir(), mirror the Windows launch-failure-diagnostics.json shape) naming the failed service and the podman inspect command. EC1/EC2/EC3 are done or were already satisfied — see the 2026-09-01 progress event for the two corrected premises, in particular that --replace has recreated the container since 2026-07-12 and that this failure exits 127 not 125.'
    ts: "2026-09-01T19:03:39Z"
    host: linux
