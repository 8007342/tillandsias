# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
events:
  - packet_id: expert-fragment-provenance-and-freshness
    event:
      type: ledger-correction
      ts: "2026-08-06T01:31:00Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Clarification for the wording in the 01:03 correction fragment:
        whole-field LWW values may be sequences or block strings, and the
        text-preserving compactor now supports those shapes. The two order-451
        audit updates were invalid semantically because they replaced canonical
        dependency/blocker fields with additive audit sequencing, and they also
        exposed the then-missing non-scalar text-compaction implementation; a
        non-scalar YAML shape alone is not schema-invalid.
