# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: per-phase-accelerator-routing-policy
    field: next_action
    value: 'Wire route_phase() into the inference call path (nothing but the envelope renderer calls it today — order-480 shape), teach scripts/bench-accel-lane.sh to send model/model_params_b through --record-measurement, and run the bench on a unified-memory host so some machine has a crossover other than Unmeasured. Start with the bench run: it is cheapest and unblocks the other two.'
    ts: "2026-09-02T21:01:21Z"
    host: lenovinha

events:
  - packet_id: per-phase-accelerator-routing-policy
    event:
      type: note
      ts: "2026-09-02T21:01:21Z"
      host: lenovinha
      summary: >
        residual after 2ecac8fda
