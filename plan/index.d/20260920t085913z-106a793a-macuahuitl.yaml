# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-pre-push-merge-rule-assumes-the-gate-is-fast-relative-to-trunk-landing-cadence-so-a-twenty-minute-host-can-be-refused-indefinitely-through-no-fault-of-its-own
    event:
      type: note
      ts: "2026-09-20T08:59:13Z"
      agent_id: linux-macuahuitl-fable5-20260920t085913z
      host: macuahuitl
      summary: |-
        Second per-attempt measurement on esmeraldinha (esme, 2026-09-20, recorded by the coordinator): a plan-only push attempt cost 742 seconds and landed on its FIRST round, against the earlier 783 seconds; two independent measurements bracket ~12.5 minutes of fixed overhead per attempt on the floor Windows tier, the difference plausibly how warm the fold check was, so the three-attempt budget is ~40 minutes of wall clock there.
