# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: network-architecture-audit
    field: next_action
    value: 'Six slices landed. P6 and P8 are done or half-done with guards; all three GPT-failed criteria have a revision pass. REMAINING PATCH-LIST WORK, value order: P3 (ENCLAVE_NO_PROXY_BASE and NODE_USE_ENV_PROXY exist in main.rs and appear in NO spec, cheatsheet or doc), P7 (headless-mode spec has zero occurrences of ''vault'' — note this is now the SECOND spec found silent about vault, after enclave-network; that pattern is worth a look on its own), P4''s vsock-transport half of the platform matrix, P5 (the network-scenarios spec does not exist; its proposed litmus — every --network site names a scenario constant — is now half-built, since check-enclave-membership-documented.sh already enumerates the attach sites), P1 (blocked on the git-mirror-egress verdict, plan/issues/git-mirror-egress-spec-divergence-audit-2026-08-10.md). P6''s remainder is an OPERATOR DECISION, not work: wire builds through :3129 or delete the port. Sections 2, 4 and 7 still carry line citations. FOR THE PACKET OWNER, sixth request: NA-05''s exit criterion mandates ''specific file:line references'' and now contradicts five slices of work plus two spec rewrites.'
    ts: "2026-08-30T14:22:58Z"
    host: linux

events:
  - packet_id: network-architecture-audit
    event:
      type: note
      ts: "2026-08-30T14:22:58Z"
      host: linux
      summary: >
        P8 done; naming P3/P7 next and the second vault-silent spec
