# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: capabilities-serves-a-cached-envelope-indistinguishably-from-a-measurement
    field: unscoreable
    value: ''
    ts: "2026-09-13T21:54:00Z"
    host: macuahuitl

events:
  - packet_id: capabilities-serves-a-cached-envelope-indistinguishably-from-a-measurement
    event:
      type: note
      ts: "2026-09-13T21:54:00Z"
      host: macuahuitl
      summary: |-
        cleared: the closure it deferred now lives in verifiable_closure, bound to litmus:capabilities-envelope-names-its-source
