# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: nine-hand-rolled-length-prefixed-framing-copies
    field: next_action
    value: 'Linux production is DONE (slice 6, commit 42a7f8145): no raw frame-length decode remains in any Linux production path, and the ratchet baseline agrees with the tree at 17 sites/9 files. WHAT REMAINS IS SIBLING-LANE ONLY. macOS: pty_vsock_bridge.rs (5 decodes) — frame the bridge stream ONCE per session and thread the Framed, do NOT construct one per call; the hazard is now measured, not theoretical (see the 2026-08-29 event). Windows: hvsocket.rs (2 decodes) — same shape, and see 828-r2ek''s concrete plan for the bounded-write half. COORDINATOR DECISION PENDING: exit criterion 1 is unsatisfiable as written — it says ''every site above'', but that list mixes test peers and blocking-stream reads with async production sites, and five Linux entries are already accepted keeps, so the criterion can only be met via its own escape clause. Proposed restatement: ''every site is migrated OR carries a recorded disposition, and the ratchet baseline agrees with the tree'' — which is what litmus:one-length-delimited-framing-impl already executes. DO NOT migrate vsock_exec.rs''s write side: it is a recorded keep, because 690-eug2''s staged length-vs-body back-pressure naming cannot survive SinkExt::send.'
    ts: "2026-08-30T05:36:08Z"
    host: linux

events:
  - packet_id: nine-hand-rolled-length-prefixed-framing-copies
    event:
      type: note
      ts: "2026-08-30T05:36:08Z"
      host: linux
      summary: >
        slice 6 landed; Linux production complete, remainder is macOS+Windows lanes
