# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: build-sh-preflight-runs-every-guard-through-an-unguarded-exec-setsid-which-macos-does-not-have-so-all-110-guards-report-as-refusals-rather-than-as-a-missing-capability-and-the-lane-the-fleet-is-about-to-mandate-before-pr-ready-is-dead-on-every-mac
    event:
      type: measurement
      ts: "2026-09-23T21:15:49Z"
      agent_id: windows-yolanda-claude-20260923t211549z
      host: yolanda-windows
      summary: |
        WINDOWS DEADLINE CALIBRATION, item 2, MEASURED on yolanda-windows (MINGW64, NTFS checkout, 16 vCPU host) 2026-09-23 at linux-next f65dabccd. Numbers only. The row is not closed and no budget was changed; the no-raise rule stands.

        1. THE DOOR: ./build.sh --preflight, rc=0: partial:preflight:ran=74 declared-skip=4 skipped-by-deadline=28 could-not-run=0 refused=0 sum=106 isolation=session wall=246s. The door recorded every deadline skip as 6s.

        2. THE 6s IS A KILL TIME, NOT A RUN TIME. Each deadline-skipped guard (26 of the 28 had names I could parse) was then run standalone and sequentially with no deadline, bounded by timeout 120. Wall times:
           check-fragment-ts-skew 252ms rc=0
           check-issue-citation-convention 285ms rc=0
           check-enclave-expect-none-is-fixture-only 485ms rc=0
           test-lww-channel-consumers 1873ms rc=0
           check-plan-binary-current 2843ms rc=0
           check-no-python-scripts 4351ms rc=0
           check-fragment-status-loss 4714ms rc=0
           test-set-field-refuses-unknown-flags 6018ms rc=0
           check-markdown-distillation 8647ms rc=0
           test-gate-step-prefix-allocation 15077ms rc=0
           test-trace-coverage 15742ms rc=0
           test-gate-step-metrics-isolation 18178ms rc=0
           test-pre-push-honours-a-live-freeze 19769ms rc=0
           test-credential-channel-survives-a-killed-probe 25672ms rc=0
           check-added-test-is-referenced 28577ms rc=0
           test-selector-flags-host-scoped-criteria 32583ms rc=3
           test-litmus-structured-assert 32797ms rc=0
           test-closure-guards-name-the-way-out 39980ms rc=0
           test-loop-status-audit-reads-current-emitter 43764ms rc=0
           test-scorable-closure-quoting 65936ms rc=0
           check-mo-full-attestations 70339ms rc=0
           test-memo-hit-observability 94162ms rc=0
           test-a-claim-names-a-workstation 110140ms rc=0
           check-append-vs-origin-fold 120067ms rc=124
           check-portability-idioms 120077ms rc=124
           test-token-instrument 431419ms rc=124
           Under 5s standalone: 7 guards. 5s to 10s: 2. 10s to 60s: 10. Over 60s: 7.
           So "miss by one or two seconds", which this host and esmeraldinha both recorded, is an artefact of reading the kill time. 17 of the 26 need 15s or more, and three need over 110s. No deadline in the single digits covers the class on this host.

        3. SPAWN HYPOTHESIS REFUTED AS STATED. The ~155ms per fork I proposed on 2026-09-22 does not reproduce: 100 x /usr/bin/true took 25.0ms per fork. The fixtures are slow in their own right on MINGW, not 1s slower through spawn overhead.

        4. UNEXPLAINED, flagged rather than guessed: 7 guards (check-fragment-ts-skew 252ms, check-issue-citation-convention 285ms, check-enclave-expect-none-is-fixture-only 485ms, test-lww-channel-consumers 1.9s, check-plan-binary-current 2.8s, check-no-python-scripts 4.4s, check-fragment-status-loss 4.7s) finish under the budget standalone but were deadline-skipped inside the door. The door runs guards sequentially (build.sh _pf_run_guard loop), so it is not parallel contention. Candidates are per-guard session and poll overhead in _pf_run_guard on MSYS, or a cold cache on the first pass. NOT MEASURED.

        5. SIDE FINDINGS:
           - test-token-instrument ran 431s despite timeout 120 (rc=124): on MINGW, timeout does not kill the guard's children. That is the same class as build.sh:401's own comment.
           - test-selector-flags-host-scoped-criteria rc=3 is its declared inconclusive-batch exit, not a failure.

        WHAT THIS MEANS FOR ITEM 2: calibration cannot be closed by tuning one number on this locus. The honest current state is the partial: verdict item 1 already delivers. A Windows author gets 74 of 106 run, and the 28 others need the gate.
