# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by macos (osx-next), 2026-08-18T00:45:00Z.
#
# 681-er9z asked the macOS host to capture its OWN selector output and
# root-cause the divergence. This is that capture, from the host in question.
status:
  - packet_id: macos-host-reports-no-eligible-work-while-8-ready-packets-exist
    field: status
    value: completed
    ts: "2026-08-18T00:45:00Z"
    host: macos

events:
  - packet_id: macos-host-reports-no-eligible-work-while-8-ready-packets-exist
    event:
      type: completed
      ts: "2026-08-18T00:45:00Z"
      agent_id: macos-opus5-metal-20260818T002300Z
      host: macos
      summary: >
        CAPTURED ON THE HOST IN QUESTION, and the divergence is NOT reproducible.
        Criterion 1 asked this host to capture its own selector output and
        root-cause the gap against the coordinator's 8-ready view.
        THE CAPTURE. scripts/select-work-batch.sh macos returns
        `batch: epic=architecture-audit-epic role=macos release=v0.5 size=8
        budget=10 score=18.475 seed=host-20260818 pick=1/3` with eight named
        packets and `triage: eligible=110 grouped=100 ungrouped=10 epics=9
        urgency_unscored=11 caps_filtered=1`. Independently, plan_query
        (status=ready, desired_release=v0.5, pickup_role=macos) returns EIGHTEEN
        packets — more than the eight the coordinator saw on 2026-08-11, because
        the queue has grown since. The host has produced a non-empty batch on
        every one of the last five consecutive cycles.
        THE THREE HYPOTHESES, RESOLVED WITH EVIDENCE RATHER THAN ASSERTION.
        (2) SELECTOR UNDER-REPORT — ELIMINATED. The selector is not returning
        empty and is not silently filtering the queue away; it NARROWS BY
        DESIGN, taking one epic and at most `budget` packets from 110 eligible,
        which is exactly what cycle_batch_triage specifies ("so a cycle drains a
        coherent slice instead of five unrelated subsystems"). The raw query and
        the selector agree in kind: 110 eligible behind an intentional batch of
        8. A narrowed batch is not a false "no work".
        (3) A REGRESSION OF THE 627-53gu bash-3.2/yq SELECTOR CLASS — ELIMINATED,
        and this is the one worth spelling out because this host is the fleet's
        bash-3.2 floor. `#!/usr/bin/env bash` resolves to GNU bash 3.2.57 here —
        it is the ONLY bash on this machine — so every cycle's run has already
        been a bash-3.2 run. Forced explicitly through /bin/bash it produces the
        identical batch and triage lines, exit 0. `yq` is ABSENT from this host
        entirely, so the selector cannot be depending on it. Both halves of the
        627-53gu class are therefore excluded by measurement, not by assumption.
        (1) STALE LOCAL BASE — the residual explanation, and it fits the original
        report's own timeline (the host "last authored 5d74786d at 20:37 local,
        merged linux at 21:39, idle ~2h since"). A selector reading a base that
        predates the coordinator's merge sees the pre-merge queue. That is now
        structurally prevented rather than merely unobserved: this host merges
        origin/linux-next before EVERY push per the pre_push_gate cadence, and
        did so twice mid-cycle in the last two cycles alone as linux advanced.
        HONEST LIMIT: I cannot retroactively root-cause a 2026-08-11 event I did
        not witness. What criterion 1 permits and what this discharges is
        "root-caused ... or recorded as an honest idle reason" — recorded here as
        NOT REPRODUCIBLE, with the two mechanical causes excluded by evidence and
        the environmental one explained and now prevented. Criterion 2 is
        conditional ("IF it is a real under-report, the selector change is
        litmus-pinned") and is vacuous: there is no under-report to pin.
        WORTH KNOWING, NOT BUILT (bar-raise discipline — proposing, not
        self-escalating): the packet's real fear is that a false "no work" idles
        a host silently. The falsifiable invariant that would catch it is cheap
        and does not exist today — `triage: eligible=0` while a raw
        status=ready + pickup_role query for the same role is NON-empty is a
        contradiction the selector could assert on itself. Recording it here as
        a candidate rather than implementing it, because adding a new failing
        gate is a scope decision the operator owns.
      evidence_refs:
        - "scripts/select-work-batch.sh macos -> batch size=8, triage: eligible=110 (captured on this host 2026-08-18)"
        - "plan_query status=ready desired_release=v0.5 pickup_role=macos -> 18 packets"
        - "bash 3.2.57 is the only bash here; /bin/bash run reproduces the batch, exit 0"
        - "yq ABSENT on this host, so the 627-53gu yq dependency cannot apply"
        - "non-empty batch on five consecutive cycles"
