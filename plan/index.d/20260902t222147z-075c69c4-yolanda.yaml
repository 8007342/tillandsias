# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: build-check-unrunnable-in-forge-ruby-absent
    field: next_action
    value: 'Fixed and pinned on windows-next: ruby-absence now exits 3 (could-not-run) so build.sh reports the instrument, and a forge skips that ONE tokenised cause loudly rather than asserting a ready-set claim it never tested. REMAINING, and it is not mine to run: exit criterion 2 needs a live forge. Someone on a forge (pirria, or lenovinha-tillandsias-forge) runs ./build.sh --check and reports whether it now reaches a real verdict instead of stopping at the archiver. The FAILURE path is measured 4/4 here; the SKIP path is exercised only at source level, because this host has no forge.'
    ts: "2026-09-02T22:21:47Z"
    host: yolanda
