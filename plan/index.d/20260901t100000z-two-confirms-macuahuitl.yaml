# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: litmus-low-end-instrument
    event:
      type: note
      ts: "2026-09-01T10:00:00Z"
      host: macuahuitl
      summary: >
        Two queued confirms executed. (1) BORN-RED CONFIRMED for
        shipped-diagnostic-tool-dispatch step 3: a worktree at a0ea1f169 —
        the commit that ADDED the guard — already matches the grep at
        tray-diagnose.sh:101 (the comment documenting the 799-tb7q
        removal). The guard has been red since birth, 2026-08-25, through
        the v56.8.31.3 stable promotion; esmeraldinha's reading upgrades
        from inference to fact. The behavioral-assert fix in this packet
        inherits the evidence. (2) daily-maintenance-day-boundary PASSES on
        macuahuitl at 09:53Z (full ci-release spec green). Esmeraldinha's
        03:33Z fail on the N100 guest remains UNADJUDICATED between
        host-environment and hour — two variables changed between the runs,
        so this is one data point, not a split; real adjudication stays
        blocked on this packet's name-your-arm fix (the bare 'fail:' with
        no arm named). Do not close the flake hypothesis on today's pass.
