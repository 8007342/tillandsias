# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: macos-cannot-pass-the-trunk-gate-because-two-tools-are-absent
    event:
      type: finding
      ts: "2026-09-03T09:03:39Z"
      agent_id: linux-macuahuitl-fable5-20260903t090339z
      host: macuahuitl
      summary: |-
        VERIFIED ON macOS BOTH WAYS by macneo, and the fix is CONDITIONAL rather than complete. WITHOUT GNU coreutils (timeout and gtimeout both absent), against b376b7179: fixture exit 1, FAIL count 22 - UP FROM 12 - verdict blocked:gh-cli-only instead of blocked:credential-unretrievable-no-keyring-service. So on a bare Mac the fix does not unblock; it moves the failure EARLIER and WIDENS it, because _ccc_timeout correctly refuses to probe unbounded and that refusal cascades into arms that previously got far enough to fail differently. That is an improvement in HONESTY - a refusal naming its reason beats a keyring-shaped lie - but anyone reading the FAIL COUNT rather than the verdict would conclude it regressed. WITH brew install coreutils, same tree same host: fixture exit 0, FAIL count 0, ok:credential-channel-fixture:all. COMPLETE PASS. SO THE FIX DOES NOT MAKE THE GUARD PLATFORM-INDEPENDENT. It makes the guard HONEST ABOUT A DEPENDENCY IT ALWAYS HAD, and it adds GNU coreutils as a macOS PREREQUISITE OF THE GATE. That is a defensible resolution and it MUST BE STATED, because a macOS host following the setup docs still cannot pass and gets no instruction saying why. Remedy line added to the refusal in this commit, matching how every other refusal in this guard names its own. MY GAP, NOT macneo: I wrote a refusal that names its reason and forgot to name its remedy, in a guard whose entire discipline is that refusals carry remedies.
