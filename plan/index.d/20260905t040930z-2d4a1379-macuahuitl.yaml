# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: floor-tier-parallelism-sweep
    field: status
    value: completed
    ts: "2026-09-05T04:09:30Z"
    host: macuahuitl

events:
  - packet_id: floor-tier-parallelism-sweep
    event:
      type: completed
      ts: "2026-09-05T04:09:30Z"
      host: macuahuitl
      summary: |-
        status 'completed' with evidence_refs: pirria's three-arm table relayed in this commit's event; esmeraldinha's row on 1047-h88p; floor row set at jobs=2 from both winners with regimes named
  - packet_id: floor-tier-parallelism-sweep
    event:
      type: note
      ts: "2026-09-05T04:09:30Z"
      host: macuahuitl
      summary: |-
        exit criterion met: the floor row is set from the two floor hosts' winners with the regime named; recorded by the coordinator because pirria has no credential channel
