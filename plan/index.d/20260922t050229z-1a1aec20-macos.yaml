# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: append-events-agent-id-refusal-names-the-identity-script-without-its-subcommand-so-a-reader-who-runs-it-as-written-is-refused-again
    field: verifiable_closure
    value: "THE OUTPUT, not the mechanism: the message a caller sees when append-event\nrefuses for a missing agent id names the INVOCATION that produces one —\n`scripts/agent-identity.sh id <backend>` — not merely the file. A reader\nwho types what the message says gets an id.\n\nA fixture asserts that the refusal text contains the subcommand form. It\nasserts NOTHING about agent-identity.sh's own behaviour: that script's\ngrammar is already pinned by litmus:agent-identity-canonical-source-shape\nand re-asserting it here would be a second home for one contract.\n\n\n[2026-09-22T05:02:29Z macos] \nDELIVERED ON work/1351-y98c, PR #144. THE OUTPUT, as the row asks:\n\n  error: ledger event has no --agent and TILLANDSIAS_AGENT_ID is unset —\n  refusing to record agent_id 'unknown'. Derive it:\n  scripts/agent-identity.sh id <backend> (order 756-hn3a) — a BARE invocation\n  refuses by grammar — then pass --agent, or export TILLANDSIAS_AGENT_ID.\n\nVERIFIED END TO END ON THE RELEASE BINARY, not only in a unit test: the refusal\nprints that text and WRITES NO FRAGMENT, and the invocation it now names answers\n`macos-tlatoanis-macbook-air-claude-20260922t045856z` at exit 0. A remedy is only\na remedy if running it works.\n\nthe_missing_agent_refusal_names_the_subcommand_not_just_the_file asserts the\nMESSAGE and nothing else, and says so in its own doc comment: agent-identity.sh's\ngrammar is pinned by litmus:agent-identity-canonical-source-shape and a second\ncopy here would be one contract with two homes.\n\nMUTATION-VERIFIED: restoring the old wording reds the test, and the failure\nquotes the old message back — so the arm discriminates on the exact difference\nthis row is about rather than on the presence of the word \"agent-identity.sh\",\nwhich BOTH wordings contain and which the neighbouring 874-idnt test already\nasserts.\n\ntillandsias-plan: 332 + 45 + 10 passed, 0 failed. clippy --all-targets\n-D warnings exit 0, read as an exit code. cargo fmt --check clean. Three-dot\ndiff against trunk: one M, no D lines."
    ts: "2026-09-22T05:02:29Z"
    host: macos
