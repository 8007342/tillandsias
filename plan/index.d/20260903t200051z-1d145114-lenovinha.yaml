# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-methodology-refusal-points-at-the-tool-that-cannot-help
    event:
      type: progress
      ts: "2026-09-03T20:00:51Z"
      agent_id: linux-lenovinha-opus-5-20260903t200051z
      host: lenovinha
      summary: |
        lenovinha: the unscoreable exemption is LIFTED.

        crates/tillandsias-plan/src/methodology.rs ::
        following_the_refusal_advice_literally_reaches_an_answer extracts the
        backticked example from the LIVE refusal text and re-asks it, so it follows
        whatever the advice currently says rather than a copy of it; plus
        a_path_shaped_question_is_never_routed_to_an_adjacent_path.

        Verified by making it fail: with the path lane disabled, both go red.

        MY OWN ERROR, caught by that control: the first adjacent-path assertion
        checked env.answer().contains(asked) and passed WITHOUT the fix, because the
        routed answer's yaml_path
        multi_host_development.pull_merge_cadence.pre_push_gate.rule contains the
        asked string as a substring. Asserting on the body was vacuous. It now asserts
        on the header, which quotes the query that actually ran.

        Gate green (253s), 15/15 methodology tests.
