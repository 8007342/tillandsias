# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: git-mirror-expert-refresh-post-receive
    field: status
    value: ready
    ts: "2026-08-12T08:01:21Z"
    host: host

events:
  - packet_id: git-mirror-expert-refresh-post-receive
    event:
      type: note
      ts: "2026-08-12T08:01:21Z"
      host: host
      summary: >
        Dependency host-expert-refresh-on-commit-env-gated (685-yidq) is now verified/terminal, so this git-mirror post-receive leg is unblocked. pending -> ready.
