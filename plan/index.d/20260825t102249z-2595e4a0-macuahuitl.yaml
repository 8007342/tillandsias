# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    field: next_action
    value: 'COORDINATOR QUEUE @ 2026-08-25T10:25Z (macuahuitl hourly). FLEET: yoga 2h + pirria 6h on cadence; lenovinha operator-stopped. STANDING DUTY unchanged + NEW CAUTION: do NOT run ./build.sh --ci-full while relying on the live enclave — 880-tdwn (a ci-full run kills the whole stack, twice-measured; the acting arm and the gate currently fight, and vault/proxy restart caps on this host are at 2/3 — a flapping verdict here may be 880-tdwn, not a crash). 865-n8vq: ALL known blockers fixed and pushed (SIGPIPE exit 141, guard-audit false orphan, 879-gidx, expert_accuracy pin, sidecar stub); the closure needs one clean full --ci-full run — ideally AFTER 880-tdwn so the gate stops killing the enclave it runs beside; yoga''s claim TTLs ~19:33Z. NEXT for macuahuitl: (1) 880-tdwn — identify the teardown escape with podman-events/test-timeline correlation, close it, deny-by-default guard. (2) the clean --ci-full for 865-n8vq. (3) 873-b1nx prototype (scoping events filed: rustc closure already in the served store). DONE THIS ARC adds the SIGPIPE runner fix, the guard-audit surface fix, the resumable-claim budget fix, 880-tdwn filed.'
    ts: "2026-08-25T10:22:49Z"
    host: macuahuitl

events:
  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    event:
      type: note
      ts: "2026-08-25T10:22:49Z"
      host: macuahuitl
      summary: >
        queue refresh: 880-tdwn filed, ci-full caution
