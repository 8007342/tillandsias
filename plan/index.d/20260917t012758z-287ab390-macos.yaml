# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: guest-vm-memory-ceiling-is-8gib-regardless-of-host-ram-operator-ruling
    field: status
    value: ready
    ts: "2026-09-17T01:27:58Z"
    host: macos

events:
  - packet_id: guest-vm-memory-ceiling-is-8gib-regardless-of-host-ram-operator-ruling
    event:
      type: note
      ts: "2026-09-17T01:27:58Z"
      host: macos
      summary: |-
        cycle end; gated green on refs/heads/work/1235-kjdf, awaiting a trunk host's merge. Not complete until it reaches a published app update.
