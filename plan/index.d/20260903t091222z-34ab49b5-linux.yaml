# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: macos-cannot-pass-the-trunk-gate-because-two-tools-are-absent
    field: status
    value: done
    ts: "2026-09-03T09:12:22Z"
    host: linux

events:
  - packet_id: macos-cannot-pass-the-trunk-gate-because-two-tools-are-absent
    event:
      type: completed
      ts: "2026-09-03T09:12:22Z"
      host: linux
      summary: |-
        status 'done' with evidence_refs: Full gate green on macOS (FULL GATE EXIT 0, 358s, ok:credential-channel-fixture:all) and the next push cleared the pre-push hook with no override (422f7f188..f73c21cb1 osx-next). Verified independently on a second macOS host by macbook with the variable isolated: timeout present -> ok:gh-keyring-push-verified, timeout absent -> blocked:gh-cli-only. Non-regressing on linux (ok:credential-channel-fixture:all). Guard now reports indeterminate with a stated reason where it cannot inspect the store, refuses rather than probing unbounded, and names brew install coreutils as the remedy. Full-gate sweep found one residual, filed as 990-u3xy. Remaining prerequisite work carried by 989-ykks.
