# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 699-usxc).
#
# cloud-only-migration-interrupted-local-src-still-load-bearing.status was ALREADY 'ready', so no field changed — but the caller
# supplied a note, and discarding it silently is what this packet is about.
events:
  - packet_id: cloud-only-migration-interrupted-local-src-still-load-bearing
    event:
      type: note
      ts: "2026-09-04T05:15:26Z"
      host: yolanda
      summary: >
        operator ruled; unblocked and scoped for the next cycle
