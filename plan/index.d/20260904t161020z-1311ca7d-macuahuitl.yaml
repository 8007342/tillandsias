# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: smoke-runbook-assumes-bash-and-has-no-windows-lane
    field: status
    value: completed
    ts: "2026-09-04T16:10:20Z"
    host: macuahuitl

events:
  - packet_id: smoke-runbook-assumes-bash-and-has-no-windows-lane
    event:
      type: completed
      ts: "2026-09-04T16:10:20Z"
      host: macuahuitl
      summary: |-
        status 'completed' with evidence_refs: ae85ee471 (guard + Windows blocks), e8ca29d20 (yolanda's two real-host fixes, landed on windows-next), pirria's zsh probes: guard rc=2 under zsh, old shape passes on an empty capture; yolanda's first Windows triple: install_exit=0 provision_exit=0 status ok, tillandsias-build survived
