# Ledger fragment — append-only, IMMUTABLE once written.
events:
  - packet_id: the-guest-adopts-a-delivered-shamir-share-without-ever-asking-the-vault-whether-it-opens-so-the-new-accept-reject-discriminator-can-never-answer-rejected-for-the-case-that-produced-it
    event:
      type: corrected
      ts: "2026-09-26T11:19:02Z"
      host: yoga
      summary: |
        SUPERSEDES the 2026-09-26T11:05 progress event on this row in two claims, after an
        independent security review of 756e30a90 found a DATA-LOSS PATH.
        (1) "a share that DIFFERS from the own secret is Rejected ... not stored" is WITHDRAWN.
            launch_vault_container wipes vault-data when has_shamir_share_in_keyring is false, and
            in a guest that predicate sees only the fallback share file, which that rejection
            skipped writing: a guest with a missing share file would WIPE a healthy vault on its
            next launch. It was also judged against a stand-in (the podman secret, the wrong key in
            the documented dummy-key state): 888-miiy's class. Now such a share is stored as before
            and one line is logged (no secret material). Rejecting on LIVE evidence is noted on
            1400-b7h4.
        (2) The closure test named there as rejecting for authentication no longer exists. The
            tests now are: delivered_share_check_decides_every_branch;
            a_mismatched_share_keeps_the_wipe_predicate_true (the review's required test: missing
            share file + mismatched share -> the guest's predicate stays true; it FAILS on
            756e30a90: Rejected vs Accepted); a_malformed_share_is_rejected_and_not_stored;
            a_malformed_rejection_leaves_the_wipe_predicate_as_it_was;
            a_matching_share_and_an_unverifiable_share_are_accepted_and_stored.
        STILL TRUE: a MALFORMED share is rejected before anything is stored. That opens no wipe
        path, because the predicate only counts a file decoding to exactly 32 bytes. Review minors
        done: no decoder error text in the reason; the decoded share and the own secret are
        zeroized; the test block moved off 701-se6x's doc comment. The predicate's fallback half is
        split into fallback_share_counts so the test is hermetic on a host whose keyring holds a
        share (its premise check caught exactly that). Vault tests 75/75, three consecutive runs.
