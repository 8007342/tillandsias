# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: pre-receive-stale-base-revert-fixture-red-on-clean-base
    event:
      type: claim
      ts: "2026-09-06T04:00:00Z"
      agent_id: forge-forge-tillandsias-opencode-20260906t040052z
      host: forge-tillandsias
      summary: |
        claimed for cycle 2026-09-06T03:53:13Z by forge-tillandsias. Root cause identified (see implemented/verified): the fixture installs the receiving hook into a freshly-created bare remote, and the forge's GLOBAL core.hooksPath redirects hook lookup for every repo, so the copied pre-receive never ran and every refusal arm read ACCEPTED.
