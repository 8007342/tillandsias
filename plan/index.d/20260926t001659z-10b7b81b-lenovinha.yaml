# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: gate-shared-ledger-parse
    event:
      type: claim
      ts: "2026-09-26T00:16:59Z"
      agent_id: linux-lenovinha-claude-20260926t001659z
      host: lenovinha
      summary: |
        Claim TAKEOVER by lenovinha-silverblue, at the coordinator's request (macuahuitl, 2026-09-25). The operator's --cloud forge on lenovinha (forge-tillandsias) wrote 8125b5c2c and 201c57566 on work/964-tzmp and held the live claim. lenovinha continues on that same ref: it will make fingerprint() mix in binary identity (the validator-surface hash) and add a stale-snapshot fail-closed test. The forge's commits are kept, not rewritten.
