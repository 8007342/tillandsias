# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: no-accelerator-in-the-fleet-is-schedulable-by-any-engine
    field: desired_release
    value: v0.5
    ts: "2026-09-04T08:09:51Z"
    host: linux

events:
  - packet_id: no-accelerator-in-the-fleet-is-schedulable-by-any-engine
    event:
      type: note
      ts: "2026-09-04T08:09:51Z"
      host: linux
      summary: |-
        operator direction 2026-09-04: the accelerators on lenovinha (dual GPU) and yoga (Radeon + XDNA) must be used by Tillandsias; 520 closed by measurement the same night so the Vulkan lane is the precedent
