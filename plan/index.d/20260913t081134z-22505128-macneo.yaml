# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-proxy-permissive-port-guard-puts-include-after-the-double-dash-so-the-filter-is-inert-and-it-scans-binaries-reporting-a-staged-guest-as-a-consumer
    event:
      type: progress
      ts: "2026-09-13T08:11:34Z"
      agent_id: macos-tlatoanis-macbook-neo-claude-20260913t081042z
      host: macneo
      summary: |
        macneo: VERIFIED LANDED, closing a ready-but-landed row rather than reimplementing it. The fix is e357f3f87 (fix(1125-wi4d, 1126-w8rq, 1127-apa8): three Windows-lane defects, each falsified both ways), and the diff is exactly this packet's title: scripts/check-proxy-permissive-port-routing.sh:59 moved `--include='*.rs'` from AFTER the `--` to BEFORE it. Before: grep -rlF -- '3129' --include='*.rs' "$CONSUMER_ROOT" — with the option after the double dash, grep treats it as a PATH operand, the filter never applies, and the scan walks whatever the root holds including staged binaries, which is how a staged guest binary was reported as a source consumer. After: grep -rlF --include='*.rs' -- '3129' "$CONSUMER_ROOT". Both halves of the title are addressed by that single reordering: the inert filter and the binary scan are the same defect, because the binaries were only reachable while the filter was inert.
        VERIFIED ON THIS HOST, not inferred from the commit message: scripts/check-proxy-permissive-port-routing.sh returns ok:proxy-permissive-port:unrouted-and-documented, rc 0. POSITIVE CONTROL for the zero-match result, because a guard that finds nothing and a guard that cannot look are indistinguishable from the verdict alone: the literal 3129 IS present in the tree (build.sh, images/proxy/entrypoint.sh, plan/index.yaml and two linux plan fragments), and the .rs-filtered scan under CONSUMER_ROOT=crates returns zero because no Rust consumer exists — which is the guard's expected unrouted verdict, not a broken search. Without that control the ok would be unfalsifiable.
        NO CODE CHANGE THIS CYCLE. The packet was open while its fix was already on the branch, so the correct output is a status transition with evidence, not a second implementation. This is an instance of the class 1080-4deb's live-lane sweep measures (46 ready-but-landed of 383 ready): the landing commit subject opens with fix(1127-apa8), so that sweeper's anchored ^((fix|close|feat)\(ORDER\)) reader would have rung this row, and it is offered here as a worked confirmation that the sweep's hits are real closures rather than false positives.
