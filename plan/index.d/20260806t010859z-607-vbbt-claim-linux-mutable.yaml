# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: container-image-final-layer-squashing
    order: 607-vbbt
    status: in_progress
    desired_release: v0.5
    priority: p1
    title: "Squash final Containerfile instruction layers without duplicating shared forge/chromium bases"
    kind: performance+build+litmus
    deliverable: >
      Podman --squash policy across compiled init, compiled on-demand and shell
      builders; cache identity ratchet; active spec and layer-count evidence
    depends_on: []
    pickup_role: linux
    capability_tags: [performance, podman, images, overlay, cache, rust, shell, testing]
    estimated_hours: 3
    outcome: >
      Every image produced from a Tillandsias Containerfile collapses the
      layers created by that Containerfile into one final layer, reducing
      overlay depth while retaining inherited forge-base/chromium-core sharing
      and Podman's intermediate build cache. Existing unsquashed images cannot
      pass the new content-identity check.
    next_action: >
      Thread Podman's squash-new policy through all three builders, include it
      in Rust/shell identities and an OCI label, pin argv/cache behavior, then
      record clean-room RootFS layer counts and runtime parity.
    exit_criteria:
      - "compiled init, compiled on-demand, and scripts/build-image.sh pass exactly one --squash for final Containerfile images and never --squash-all"
      - "Rust and shell content identities include squash-new, change once at migration, and emit io.tillandsias.image.layer-policy=squash-new"
      - "a built final image has at most its inherited base layer count plus one while forge-base/chromium-core remain independently reusable"
      - "focused tests, full build gate, and clean-room init/start smoke pass with image metadata and readiness unchanged"
      - "the report preserves the operator benchmark conclusion, the live pre-change layer baseline, and the reason inherited bases are not squash-all flattened"
    owned_files:
      - crates/tillandsias-core/src/image_builder.rs
      - crates/tillandsias-podman/src/client.rs
      - crates/tillandsias-headless/src/main.rs
      - scripts/build-image.sh
      - scripts/hash-image-sources.sh
      - openspec/specs/init-incremental-builds/spec.md
      - plan/issues/container-image-final-layer-squashing-2026-08-05.md

events:
  - packet_id: container-image-final-layer-squashing
    event:
      type: claim
      ts: "2026-08-06T01:08:59Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      lease_id: image-squash-607-vbbt-20260806t010859z
      expires_at: "2026-08-06T05:08:59Z"
      summary: >
        Claimed directly from the operator's laptop benchmark intake after a
        folded-ledger/history dedupe found no existing OCI image-layer packet.
        Starting at b49a2c6c on linux-next; sibling heads observed:
        main=7491caf2, linux-next=b49a2c6c, windows-next=osx-next=936c1899.
        Live forge is 65 layers while forge-base is 3 layers/3.126 GB, so the
        bounded implementation is Podman --squash (new layers), not
        --squash-all (which would duplicate inherited shared bases).
