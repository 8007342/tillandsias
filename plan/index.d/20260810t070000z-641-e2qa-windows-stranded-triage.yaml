# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-20260810t070000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# Windows lane's share of the 641-e2qa stranded-in_progress leak. Three packets
# flagged by scripts/check-stranded-in-progress.sh as in_progress with zero
# events. Each is resolved to a state a reader can act on — NOT batch-closed.
#
# The two directions are deliberately different. 620-9xpg is closed because
# evidence exists that its last open criterion was met. 620-ca7g and 620-cine go
# back to READY because no evidence exists that anyone started them — returning
# unstarted work to the claimable pool is the conservative move, and asserting
# completion without evidence is the very thing 635-i6vm and 641-e2qa exist to
# stop.

status:
  - packet_id: wsl-github-login-vault-misroute
    field: status
    value: completed
    ts: "2026-08-10T07:00:00Z"
    host: windows

  - packet_id: low-end-no-local-inference-gate
    field: status
    value: ready
    ts: "2026-08-10T07:00:00Z"
    host: windows

  - packet_id: low-end-bridge-eof-diagnosability
    field: status
    value: ready
    ts: "2026-08-10T07:00:00Z"
    host: windows

events:
  - packet_id: wsl-github-login-vault-misroute
    event:
      type: completed
      ts: "2026-08-10T07:00:00Z"
      agent_id: windows-20260810t070000z
      summary: >
        CLOSED on live field evidence from tonight's operator session.

        The packet's `--with-token` criterion was already verified on a second
        Windows host (claudia) in 525e2b3a. That commit ALSO recorded, precisely,
        what was still open: "The tray-menu lane is the broken one, and it is the
        criterion still open on 620-9xpg" — because `provider_login_exec_args`
        adds `--tty` only for LoginInputMode::Terminal, so the stdin-token lane
        never touched the SIGTTIN/SIGTTOU trap that broke the tray lane.

        That trap was fixed in 9a244471 (627-sgtt/627-wtrp, "the GitHub Login
        terminal is kernel-STOPPED, not slow").

        TONIGHT the operator ran the TRAY-MENU lane on this host, on
        v0.4.260810.1, and it completed end to end:
          04:58:26 tray menu click menu_id=github-login
                   -> /usr/local/lib/tillandsias/github-login.sh
          05:00:34 github sign-in state resolved signed-out -> signed-in
        Guest log: "Logged in as 8007342", git protocol configured, git identity
        stored (verified on disk: name=Tlatoani, email=bulloncito@gmail.com).

        So the last open criterion — the tray-menu lane reaching a real
        authenticated state — is met, by an operator-driven run rather than a
        synthetic one.

        RESIDUAL, filed separately as 648-e5pf (p2), NOT swept in here: the flow
        prompted for a token, silently failed to authenticate with it
        (04:58:59 signing-in -> signed-out), and completed via device flow 95s
        later. That is a UX and credential-hygiene defect in the same lane, but it
        is not this packet's criterion — the lane authenticates, which is what
        620-9xpg asked. Closing it with the residual visible beats either leaving
        it stranded or quietly folding an unrelated defect into a completion.

  - packet_id: low-end-no-local-inference-gate
    event:
      type: progress
      ts: "2026-08-10T07:00:00Z"
      agent_id: windows-20260810t070000z
      summary: >
        RETURNED TO READY as part of the 641-e2qa stranded triage. Flagged
        in_progress with zero events; the only commit mentioning it is 74ad23cd,
        the original filing from the Esmeralda N100 field session. No evidence
        anyone started it, so the claim is stale rather than the work unfinished.

        Not closed, and deliberately not: there is no evidence of completion
        either. Returning it to the claimable pool is the conservative
        resolution — a stranded packet is invisible in both directions, and
        `ready` at least makes it visible in one.

  - packet_id: low-end-bridge-eof-diagnosability
    event:
      type: progress
      ts: "2026-08-10T07:00:00Z"
      agent_id: windows-20260810t070000z
      summary: >
        RETURNED TO READY as part of the 641-e2qa stranded triage. Same evidence
        as 620-ca7g: in_progress with zero events, only the 74ad23cd filing
        commit mentions it, no sign work began. Stale claim, not unfinished work.

  - packet_id: stranded-in-progress-packets-are-invisible-both-ways
    event:
      type: progress
      ts: "2026-08-10T07:00:00Z"
      agent_id: windows-20260810t070000z
      summary: >
        WINDOWS LANE'S SHARE RETIRED. scripts/check-stranded-in-progress.sh
        flagged three windows-owned packets; all three are now resolved —
        620-9xpg completed on field evidence, 620-ca7g and 620-cine returned to
        ready as stale claims.

        Cross-host confirmation of the tooling: the checker and its grammar
        (`stranded\t<order>\t<role>\t<packet_id>` + `summary: in_progress=N
        stranded=N threshold_events=N`) ran unmodified on Windows and produced
        actionable output, which is a second-host validation this lane can offer
        for free.

        Method note for whoever takes the remaining linux/unassigned ones: the
        useful discriminator was `git log --all --grep=<order>`. A packet whose
        only mention is its filing commit was never started (-> ready); a packet
        with implementation commits needs its exit criteria re-read against them
        (-> completed, or ready with the residual named). Batch-closing the whole
        set would have destroyed exactly the distinction that makes the number
        actionable.
