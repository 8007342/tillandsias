# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260809t234500z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# COORDINATOR ACTION. The macOS close-out fragment
# (20260809t232000z-macos-drain-closeout-macos.yaml) recorded `type: completed`
# and `type: blocked` EVENTS but carried no `status:` LWW block, so every packet
# it reported on still folds at its old status. 624-q4jj came back ALL-PASS on
# five of five steps with a full evidence file and was still being offered as
# claimable work.
#
# This is the 641-e2qa "never declared" leak reproducing on a THIRD host, hours
# after it was documented — which is the strongest available evidence that the
# WRITE PATH is at fault rather than any agent. Windows independently filed the
# same shape today as 642-fedr (the LWW channel is named `status:` but corrects
# any field, so nobody finds it when they need it). 636-9m79 already proposes
# the durable fix: refuse the no-op at authoring time.
#
# Applying macOS's OWN recorded verdicts here — not this host's inference. Each
# value below mirrors the event type macOS wrote, and 349 stays blocked because
# that is what they reported.

status:
  - packet_id: macos-unstable-channel-installer-validation
    field: status
    value: completed
    ts: "2026-08-09T23:45:00Z"
    host: linux-mutable
  - packet_id: host-pty-slave-retention-eof-teardown
    field: status
    value: completed
    ts: "2026-08-09T23:45:00Z"
    host: linux-mutable
  - packet_id: macos-forge-config-trust-live-parity
    field: status
    value: blocked
    ts: "2026-08-09T23:45:00Z"
    host: linux-mutable

events:
  - packet_id: macos-unstable-channel-installer-validation
    event:
      type: note
      ts: "2026-08-09T23:45:00Z"
      agent_id: linux-mutable-20260809t234500z
      summary: >
        Status applied by the coordinator from the macOS lane's own `type: completed`
        event. macOS reported 5/5 PASS with evidence
        (plan/issues/macos-unstable-channel-validation-2026-08-09.md): unstable curl
        one-liner, channel-resolution probes, SHA256SUMS-macos integrity green under both
        verifiers, stable DMG e2e, and version proof via CFBundleVersion+SHA. Step 4 also
        DISCHARGES the order-455 macOS smoke for v0.4.260809.2. The macOS half of the
        621-2re2 unstable channel — designed and verified entirely on Linux — is now
        confirmed on Apple hardware.
  - packet_id: host-pty-slave-retention-eof-teardown
    event:
      type: note
      ts: "2026-08-09T23:45:00Z"
      agent_id: linux-mutable-20260809t234500z
      summary: >
        Status applied by the coordinator from the macOS lane's own `type: completed`
        event (order 492, PTY slave dropped at split so EOF reaps the guest child).
  - packet_id: macos-forge-config-trust-live-parity
    event:
      type: note
      ts: "2026-08-09T23:45:00Z"
      agent_id: linux-mutable-20260809t234500z
      summary: >
        Status applied by the coordinator from the macOS lane's own `type: blocked` event:
        the closing run is blocked by 635-kagg, the --bash lane wedging post-vault.
        Recorded as blocked rather than left ready, so it stops being offered as claimable
        work while its blocker is open.
