# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-opencode-20260809t161400z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# ORDER 614-2gqx (meta-orchestration-full-mode-exit-attestation-gap) CLOSED.
# Full-mode terminal attestation is implemented and gated: scripts/mo-full-attest.sh
# defines and enforces the `MO-FULL: <COMPLETE|BLOCKED> <LOCAL_SHA> <BRANCH>
# <REMOTE_SHA>` marker grammar; scripts/litmos-opencode-e2e-launch.sh (via
# scripts/litmus-opencode-e2e-launch.sh) rejects full-mode exit zero without a
# valid, converging marker; the fixture reproduces the early-provider-exit breach;
# MO-SMOKE grammar and the 4h rate limit are unchanged.
events:
  - packet_id: meta-orchestration-full-mode-exit-attestation-gap
    event:
      type: completed
      ts: "2026-08-09T16:30:00Z"
      agent_id: forge-opencode-20260809t161400z
      host: forge
      summary: >
        Full-mode terminal attestation implemented. scripts/mo-full-attest.sh
        defines the `MO-FULL: <COMPLETE|BLOCKED> <LOCAL_SHA> <BRANCH> <REMOTE_SHA>`
        marker grammar and enforces (a) LOCAL_SHA == REMOTE_SHA so no marker may
        follow an unpushed local commit, (b) branch matches the host's current
        branch, (c) `git ls-remote` converges on the claimed remote head within
        the bounded relay window. scripts/litmus-opencode-e2e-launch.sh rejects
        full-mode exit zero without a valid, converging marker (FORGE_EXIT=127
        marker-level, 128 remote-head), mirroring the smoke-mode 126 gate;
        MO-SMOKE grammar and the 4h full-cycle rate limit are unchanged. Hermetic
        fixture scripts/test-mo-full-attest.sh / `scripts/mo-full-attest.sh
        fixture` reproduces the early-provider-exit-after-local-commit breach
        plus malformed, unpushed-commit, branch-mismatch, and relay-lost
        scenarios against a fake remote probe, and a clean pass — 6/6 green.
        skills/meta-orchestration/SKILL.md documents the grammar, the emission
        rule (final output line, after finalization steps 1-8), and the
        no-marker-on-failed-push rule. openspec/litmus-tests/
        litmus-opencode-prompt-e2e-shape.yaml runs the fixture as its first
        critical-path step and documents FORGE_EXIT=127/128 in rollback.
        ./build.sh --check green; bash -n clean on all touched scripts.
status:
  - packet_id: meta-orchestration-full-mode-exit-attestation-gap
    field: status
    value: completed
    ts: "2026-08-09T16:30:00Z"
    host: forge
packets:
  - packet_id: meta-orchestration-full-mode-exit-attestation-gap
    order: 614-2gqx
    status: completed
    desired_release: v0.5
    priority: p0
    pickup_role: any
    title: Full meta-orchestration exits zero without boundary/push attestation and discards local commits
    outcome: >
      A full forge cycle is green only when its durable plan result, startup
      boundary verification and remote commit are machine-attested; a normal
      provider exit between tool calls cannot discard local work as success.
    next_action: >
      None — packet complete. The full-mode terminal marker grammar, launcher
      validation, and the early-provider-exit/local-commit fixture are in place;
      MO-SMOKE grammar and the shared full-cycle rate limit are unchanged.
    exit_criteria:
      - "full mode emits a typed terminal marker only after boundary verification and durable commit/push obligations — PROVEN: scripts/mo-full-attest.sh emits MO-FULL only after local/remote SHA equality and branch match; no marker on failed push"
      - "the outer launcher rejects exit zero with a missing marker, unpushed commit, or remote-head mismatch — PROVEN: scripts/litmus-opencode-e2e-launch.sh FORGE_EXIT=127 (marker) / 128 (remote-head), mirroring the smoke-mode 126 gate"
      - "a fixture reproduces early normal provider exit after a local commit and the gate fails loud without losing the diagnosis — PROVEN: scripts/mo-full-attest.sh fixture 6/6 green across early-exit, malformed, unpushed-commit, branch-mismatch, relay-lost scenarios"
      - "MO-SMOKE grammar and the shared full-cycle rate limit remain unchanged — PROVEN: smoke grammar untouched; 4h full-cycle rate limit unchanged"
    events:
      - type: finding
        ts: "2026-08-06T09:40:23Z"
        agent_id: linux-mutable-root-codex-20260806t001750z
        host: linux_mutable
        summary: |
          Fresh-product full launch returned forge_exit=0 but emitted no terminal verdict, skipped final boundary verification/push, and teardown lost local commit 4a1410a2. Remote linux-next remained c9ebd3f5.
      - type: completed
        ts: "2026-08-09T16:30:00Z"
        agent_id: forge-opencode-20260809t161400z
        host: forge
        summary: |
          Full-mode terminal attestation implemented. scripts/mo-full-attest.sh defines the `MO-FULL: <COMPLETE|BLOCKED> <LOCAL_SHA> <BRANCH> <REMOTE_SHA>` marker grammar and enforces (a) LOCAL_SHA == REMOTE_SHA so no marker may follow an unpushed local commit, (b) branch matches the host's current branch, (c) `git ls-remote` converges on the claimed remote head within the bounded relay window. scripts/litmus-opencode-e2e-launch.sh rejects full-mode exit zero without a valid, converging marker (FORGE_EXIT=127 marker-level, 128 remote-head), mirroring the smoke-mode 126 gate; MO-SMOKE grammar and the 4h full-cycle rate limit are unchanged. Hermetic fixture scripts/test-mo-full-attest.sh / `scripts/mo-full-attest.sh fixture` reproduces the early-provider-exit-after-local-commit breach plus malformed, unpushed-commit, branch-mismatch, and relay-lost scenarios against a fake remote probe, and a clean pass — 6/6 green. skills/meta-orchestration/SKILL.md now documents the grammar, the emission rule (final output line, after finalization steps 1-8), and the no-marker-on-failed-push rule. openspec/litmus-tests/litmus-opencode-prompt-e2e-shape.yaml runs the fixture as its first critical-path step and documents FORGE_EXIT=127/128 in rollback. ./build.sh --check green; bash -n clean on all touched scripts.
