# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

status:
  - packet_id: macos-v05-validation-bundle
    field: next_action
    value: |
      M1/M2/M3/M4/M6 are GREEN (events on this packet, 2026-08-09/10). The
      ONLY open item is M5's runtime half: verify the cloud submenu renders
      "(no repos)" with a ZERO-repo GitHub account — operator-gated (needs
      such an account attended). Do not re-run M1-M4/M6; their evidence
      stands at the recorded commits.
    ts: "2026-08-11T02:45:00Z"
    host: macos-tlatoanis-macbook-air-fable5-20260810t043031z

events:
  - packet_id: macos-oneshot-boot-hangs-unbounded-after-interrupted-teardown
    event:
      type: progress
      ts: "2026-08-11T02:45:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Datum: pre-breadcrumb hang reproduced 15s after the TRAY's own
        clean quit (not an interrupted teardown, not a rapid one-shot
        sequence) — the hazard window follows ANY VM teardown and the
        safe-gap threshold is racy (10s sometimes works, 15s failed here,
        10+ min reliably works). 663-acdw diagnosis attempt #4 consumed;
        that packet stays blocked behind this one.
