# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: two-independent-mutexes-guard-one-process-global-podman-bin-so-the-stub-tests-still-race-and-the-gate-reds-on-an-unrelated-head
    field: status
    value: done
    ts: "2026-09-21T06:56:56Z"
    host: macbookair-macos

events:
  - packet_id: two-independent-mutexes-guard-one-process-global-podman-bin-so-the-stub-tests-still-race-and-the-gate-reds-on-an-unrelated-head
    event:
      type: completed
      ts: "2026-09-21T06:56:56Z"
      host: macbookair-macos
      summary: |-
        status 'done' with evidence_refs: Pushed on osx-next at 61b3f785d; gate BUILD_RC=0, ok:test-baseline:ran=2476 new-red=0, clippy --all-targets -D warnings exit 0, fmt clean, crate 177/177. One lock: PODMAN_BIN_ENV_LOCK (tokio) in lib.rs; env_lock() delegates via blocking_lock, backend.rs takes .lock().await, its private mutex removed. THE FIRST ATTEMPT USED A STD MUTEX AND THE GATE REFUSED IT (BUILD_RC=101, clippy::await_holding_lock): holding it across the await is SOUND under the current-thread test runtime, and soundness was never the binding constraint — backend.rs's instinct to reach for a tokio mutex was right, declaring a SECOND one was the error, and my first fix repeated that error in the mirror. exactly_one_mutex_guards_the_process_global_podman_bin asserts one declaration by SHAPE because the defect is an interleaving; MUTATION-VERIFIED against the tokio-typed second lock by file and line, with needles assembled via concat! after it matched itself. Re-running that arm after the type change also caught the guard detecting only starts_with('static ') while the real declaration is pub(crate) static — as written it would have missed the only lock and passed a two-lock tree. a_prompt_podman_still_succeeds_under_the_same_budget stays live and out of test-known-red.txt; 177/177 is NOT offered as evidence the race is gone.
  - packet_id: two-independent-mutexes-guard-one-process-global-podman-bin-so-the-stub-tests-still-race-and-the-gate-reds-on-an-unrelated-head
    event:
      type: note
      ts: "2026-09-21T06:56:56Z"
      host: macbookair-macos
      summary: |-
        one tokio mutex guards the process-global; the shape test is mutation-verified and the racing test stays live
