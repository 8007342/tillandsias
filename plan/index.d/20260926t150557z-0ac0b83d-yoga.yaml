# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: amd-vulkan-userspace-missing-from-inference-image
    field: next_action
    value: '2026-09-26 yoga (not claimed — a decision packet is the coordinator''s to dispose): BOTH gaps this row names are closed on trunk. (1) USERSPACE: images/inference/Containerfile installs vulkan-loader + mesa-vulkan-drivers unconditionally (32dc4278c, feat(917-zkge)). (2) PASSTHROUGH: scripts/dev-inference-ensure.sh maps legacy_tier gpu-rocm to --device=/dev/kfd --device=/dev/dri (937-68n4, lines ~208-220) and sets OLLAMA_IGPU_ENABLE=1 for gpu-rocm|gpu-vulkan. MEASURED on yoga 2026-09-04 after rebuilding the image: ollama enumerated library=Vulkan "AMD Radeon 840M (RADV KRACKAN1)" and qwen2.5:0.5b loaded with size_vram == size. The windows-side concern (c6df04f17: OLLAMA_VULKAN defaulting on moved a host) is the open question for the fleet decision; the Linux half is implemented and measured. Suggest disposing this row as decided-by-implementation (917-zkge) unless a host rejects.'
    ts: "2026-09-26T15:05:57Z"
    host: yoga

events:
  - packet_id: amd-vulkan-userspace-missing-from-inference-image
    event:
      type: progress
      ts: "2026-09-26T15:05:57Z"
      host: yoga
      summary: |-
        status '2026-09-26 yoga (not claimed — a decision packet is the coordinator's to dispose): BOTH gaps this row names are closed on trunk. (1) USERSPACE: images/inference/Containerfile installs vulkan-loader + mesa-vulkan-drivers unconditionally (32dc4278c, feat(917-zkge)). (2) PASSTHROUGH: scripts/dev-inference-ensure.sh maps legacy_tier gpu-rocm to --device=/dev/kfd --device=/dev/dri (937-68n4, lines ~208-220) and sets OLLAMA_IGPU_ENABLE=1 for gpu-rocm|gpu-vulkan. MEASURED on yoga 2026-09-04 after rebuilding the image: ollama enumerated library=Vulkan "AMD Radeon 840M (RADV KRACKAN1)" and qwen2.5:0.5b loaded with size_vram == size. The windows-side concern (c6df04f17: OLLAMA_VULKAN defaulting on moved a host) is the open question for the fleet decision; the Linux half is implemented and measured. Suggest disposing this row as decided-by-implementation (917-zkge) unless a host rejects.' (not terminal) with evidence_refs: 32dc4278c
  - packet_id: amd-vulkan-userspace-missing-from-inference-image
    event:
      type: note
      ts: "2026-09-26T15:05:57Z"
      host: yoga
      summary: |-
        evidence: both named gaps closed on trunk; GPU measured placing on yoga
