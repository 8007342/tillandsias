# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: centicolon-scorer-calls-the-model
    field: status
    value: implemented
    ts: "2026-09-03T11:26:47Z"
    host: yoga

events:
  - packet_id: centicolon-scorer-calls-the-model
    event:
      type: progress
      ts: "2026-09-03T11:26:47Z"
      host: yoga
      summary: |-
        status 'implemented' (not terminal) with evidence_refs: local-ci --phase runtime wrote 140/140/0 via the model; no-fallback verified by stub mutation (blocked:local-ci:scorer-refused rc=2)
  - packet_id: centicolon-scorer-calls-the-model
    event:
      type: note
      ts: "2026-09-03T11:26:47Z"
      host: yoga
      summary: |-
        all three exit criteria met; numbers unchanged by design, which is the faithfulness evidence
