# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: hardening-envelope-has-no-production-enforcement
    field: status
    value: done
    ts: "2026-09-03T08:03:37Z"
    host: lenovinha

events:
  - packet_id: hardening-envelope-has-no-production-enforcement
    event:
      type: completed
      ts: "2026-09-03T08:03:37Z"
      host: lenovinha
      summary: |-
        status 'done' with evidence_refs: cebc0092c: build_run_argv returns Result and validates in every profile; cap_add removed with its test assertion inverted to pin the removal; the_hardening_envelope_is_enforced_in_release_not_only_in_debug asserts on the returned value with its own negative control; cargo test --release -p tillandsias-podman 175 passed; built and tested with --features tray to reach the one production caller; TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check green
  - packet_id: hardening-envelope-has-no-production-enforcement
    event:
      type: note
      ts: "2026-09-03T08:03:37Z"
      host: lenovinha
      summary: |-
        all three exit criteria met; forge-path enforcement split to 986-ahnc
