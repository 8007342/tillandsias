# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-claude-fable-metaorch-20260803
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: windows-runtime-guest-reprovision-and-lane-verification
    order: 599-3b9h
    status: ready
    desired_release: v0.5
    role: platform-validation
    pickup_role: windows
    priority: p1
    tags: [windows, v0.5, wsl2, provenance, accel, follow-up]
    title: >
      Reprovision the Windows runtime distro with the 0.4.260802.1 guest, then
      complete the two lane reads 598-yhu5 deferred.
    context: >
      598-yhu5 W5 measured the installed guest at v0.4.260728.1 — it predates
      the token-removal fix 82160687 (2026-08-01) and still runs order-359
      behaviour. Fresh, version-correct binaries for both arches are staged in
      target-guest/ and the tray assets/ copy is refreshed (verified: statically
      linked ELF x86-64 carrying 0.4.260802.1; note the cargo fallback links
      static non-PIE where Nix produces static-pie). The staging-path defect
      that had silently staged a 2026-07-15 artifact is fixed in
      scripts/build-guest-binaries.sh (stage from CARGO_TARGET_DIR when set).
    exit_criteria:
      - >
        Runtime distro runs a guest reporting Tillandsias v0.4.260802.1 (or
        newer), installed through a sanctioned path: the destructive local-build
        e2e (preferred; preflight said `eligible` on 2026-08-03), or the tray's
        fetch-headless flow. If via e2e: the OpenCode leg
        (litmus:opencode-prompt-e2e-shape) is operator-embargoed until
        2026-08-04T19:00Z — run the gate WITHOUT that leg before then, and
        record the exclusion in the e2e report. Watch for a Smart App Control
        block on the freshly built unsigned tray exe (os error 4551) — an
        interactive allowance may be needed; report it distinctly, not as a
        provisioning failure.
      - >
        Deferred W4 half: inside a forge lane on the NEW guest, first line of
        $TILLANDSIAS_ACCEL_ENVELOPE — expected accel_class=cpu-only (hardware
        answer already recorded: /dev/dri absent, /sys/class/accel absent, no
        nvidia-smi, 16 cpus). Any GPU claim on this box is a probe defect.
      - >
        Deferred W5 half: grep the RUNNING forge lane's env for
        GH_TOKEN/GITHUB_TOKEN/HOMEBREW_GITHUB_API_TOKEN. PASS = nothing.
        This is the first measurement of the post-82160687 contract on Windows.
status:
  - packet_id: windows-v05-validation-bundle
    field: status
    value: completed
    ts: "2026-08-03T19:30:00Z"
    host: windows-claude-fable-metaorch-20260803
events:
  - packet_id: windows-v05-validation-bundle
    event:
      type: claimed
      ts: "2026-08-03T19:10:00Z"
      agent_id: windows-claude-fable-metaorch-20260803
      host: windows
      summary: >
        Claimed via claim-ledger-node.sh on windows-next e81819f0 (= linux-next,
        fast-forwarded 127 commits at cycle start). Guards: credential channel
        ok:gh-keyring, branch ok:branch-windows-next, boundary snapshot clean.
  - packet_id: windows-v05-validation-bundle
    event:
      type: completed
      ts: "2026-08-03T19:30:00Z"
      agent_id: windows-claude-fable-metaorch-20260803
      host: windows
      summary: >
        All six checks produced verdicts on the hardware. W1 PASS (check exit 0;
        86/0/8 tests after restaging — the single first-run red was the
        order-282 staleness litmus correctly refusing a v0.4.260728 embedded
        guest). W2 PASS (all build.sh --check steps green in the WSL builder;
        none of the three named fail modes). W3 PASS after fixing a real defect
        it surfaced: the cargo fallback staged from hardcoded $ROOT/target,
        ignoring the with-wsl2-builder CARGO_TARGET_DIR redirect, and installed
        a stale 2026-07-15 v0.3.260715.6 artifact as current — only
        verify_binaries caught it; fixed in scripts/build-guest-binaries.sh and
        both arches restaged at 0.4.260802.1 (x86_64 statically linked ELF,
        non-PIE via cargo fallback vs Nix static-pie — recorded delta). W4:
        cpu-only is the only reachable verdict — /dev/dri and /sys/class/accel
        absent, no nvidia-smi, 16 cpus; the GPU-PV false-positive trigger does
        not exist on this box; envelope read deferred to 599-3b9h (measuring the
        stale guest would validate retired code). W5 FAIL as suspected:
        installed guest v0.4.260728.1 predates 82160687; configured container
        env clean of tokens; live-lane grep deferred to 599-3b9h. W6:
        tools recorded (timeout+md5sum resolve natively, jq/yq/ruby/policy do
        not); bare pre-build/quick litmus 181 pass / 55 fail / 114 skip (76%) —
        every step-context-captured fail is environmental (15x accel_probe
        E0381 native-compile, Linux-substrate fixtures), zero Windows
        regressions demonstrated; two ci-release shape tests suspected stale
        post-purge, routed to Linux via
        plan/issues/litmus-corpus-not-host-aware-windows-2026-08-03.md. E2E
        skipped with dated reasons (OpenCode leg
        operator-embargoed until 2026-08-04T19:00Z; destructive gate cleared
        only mid-cycle; SAC launch-block risk) — see loop status 2026-08-03.
