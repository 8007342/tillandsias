# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: hardening-envelope-has-no-production-enforcement
    event:
      type: finding
      ts: "2026-09-03T08:15:38Z"
      agent_id: linux-macuahuitl-fable5-20260903t081538z
      host: macuahuitl
      summary: |-
        THE COMPILER WAS THE SILENT INSTRUMENT, and this is the sharpest of the four instrument defects the fleet found today. lenovinha changed the signature of build_run_argv and IT COMPILED CLEANLY WHEN IT SHOULD NOT HAVE. Cause: the only production caller is tray/mod.rs:2481, and mod tray sits behind a cfg feature gate the DEFAULT BUILD EXCLUDES. VERIFIED HERE: crates/tillandsias-headless/src/main.rs:14585 reads cfg(feature = tray) immediately above mod tray, and Cargo.toml line 98 reads default = [vault], with no tray. They rebuilt with --features tray, got the expected E0308, and fixed it at the call site. THE GENERAL RULE: A CHANGE THAT COMPILES IS NOT EVIDENCE WHEN THE CODE IS BEHIND A FEATURE NOBODY ENABLES. A green build is a measurement of what was built, not of what exists, and the difference is invisible unless someone asks. Same family as memory.peak counting page cache and a litmus asserting a pure helper. CORRECTION 2, NARROWING RATHER THAN WIDENING, because they went looking for the setters instead of trusting the packet sentence: the four flags are NOT settable false from outside the crate - fields are private, new() sets all four true, and no builder disables any. The exposure is REGRESSION RISK INSIDE THE CRATE, not a live bypass. The packet context overstated it and the correction is on the record.
