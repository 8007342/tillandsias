# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: forge-launch-argv-is-validated-by-nothing
    field: status
    value: done
    ts: "2026-09-03T12:14:43Z"
    host: lenovinha

events:
  - packet_id: forge-launch-argv-is-validated-by-nothing
    event:
      type: completed
      ts: "2026-09-03T12:14:43Z"
      host: lenovinha
      summary: |-
        status 'done' with evidence_refs: 386bc9ea6: validation at run_agent_container_attached (main.rs:11498) prepending the run subcommand; refusal proven on a REAL launch — tillandsias . --opencode refused before podman with rc=1, no container, flag named — after removing BOTH occurrences of --cap-drop=ALL from the forge argv; the_live_forge_argv_satisfies_the_hardening_envelope passes against the real builders with its own negative control; source restored with 101 additions and 0 removals; TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check green
  - packet_id: forge-launch-argv-is-validated-by-nothing
    event:
      type: note
      ts: "2026-09-03T12:14:43Z"
      host: lenovinha
      summary: |-
        all four exit criteria met; refuse-vs-log decided and recorded
