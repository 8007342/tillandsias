# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-macos-one-shot-opencode-lane-ran-three-hours-silent-on-a-cold-host-without-reaching-the-credential-guard-and-the-guest-kept-no-record-of-it
    event:
      type: progress
      ts: "2026-09-26T13:46:36Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260926t134636z
      host: tlatoanis-macbook-air
      summary: |
        CRITERION 1 IMPLEMENTED on work/1385-h6uz (23f8d1500), not landed: images/default/opencode-tool-start-follower.sh follows opencode's own log for 'evaluated permission=' lines (written at tool START; confirmed in the forge on opencode 1.18.32) and prints '[forge] tool start: <permission>: <pattern>' on the prompted lane's stdout, which streams to the host live. It is fail-loud: exit 3 with a named WARN if the log never appears within 30s. It is started before the prompted branch's exec in entrypoint-forge-opencode.sh and installed by the Containerfile. Fixture scripts/test-opencode-tool-start-follower.sh: 7/7 x3 on macOS; 6/7 against the pre-fix entrypoint. Gate step 510-1385-h6uz (darwin second regime). OPEN: a live run with a forge image rebuilt from this ref, showing the start line on the host during a long call; then criteria 2 and 3 (name the guard mechanism from that record; the cold-host lane ends at blocked:upstream-no-credential).
