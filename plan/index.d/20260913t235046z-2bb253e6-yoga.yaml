# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    field: next_action
    value: capture ps by name at the fixture step on every gate, refusals and passes; the discriminator is whether the host-side podman exec still exists, not conmon parentage; criterion 2 (the rc=3 mechanism for arm 4) is the only one still open
    ts: "2026-09-13T23:50:46Z"
    host: yoga

events:
  - packet_id: the-archiver-ruby-fixture-refuses-inside-the-gate-and-passes-standalone-on-the-same-tree-on-two-hosts-with-opposite-ruby-layouts
    event:
      type: note
      ts: "2026-09-13T23:50:46Z"
      host: yoga
      summary: |-
        arm 5 exhibited directly; refinement 3 corrected
