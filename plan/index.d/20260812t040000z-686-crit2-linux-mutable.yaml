# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260812t040000z
events:
  - packet_id: status-ladder-enforcement-wave-2
    event:
      type: progress
      ts: "2026-08-12T04:00:00Z"
      agent_id: linux-mutable-night-20260811
      host: linux_mutable
      summary: >
        Criterion 2 (invisible-block report) IMPLEMENTED + verified as-wired.
        New is_parked_status() (implemented/needs_clarification/blocked/failed —
        non-terminal, non-progressing) and Ledger::parked_blocks() /
        parked_dependencies_of() surface every dependent stuck behind a parked
        packet with the dependency's status and first next_action line. Wired
        into three surfaces: `check` emits a `parked-block:` advisory per edge
        and counts them in its ok line; `status <id>` prints `blocked-on-parked:`
        for the packet's own parked deps (verified live: 348 shows it waits on
        blocked 349); and a new `parked-blocks [id]` subcommand (TSV, whole
        ledger or per-packet) added to DISPATCH_ARMS + capabilities.txt +
        usage, consistency tests green. 2 unit tests (positive: implemented dep
        surfaced with outstanding action; negative: ready and done deps
        excluded) + litmus:parked-blocks-visibility-shape bound to ci-release,
        4/4 green as-wired (positive + two negative controls + check-count).
        124 crate tests green. This closes the 690-2kwd/627-sgtt visibility
        gap — an implemented packet's waiting dependents are now reported, not
        silently stuck. REMAINING (packet stays ready): criterion 3
        (new-fragment closure evidence gate) and criterion 4 (plan_next
        verification pickups). Criteria 1 and 2 of 4 now done.
