# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: litmus-low-end-instrument
    event:
      type: note
      ts: "2026-09-02T02:30:00Z"
      host: macuahuitl
      summary: >
        CORRECTION superseding part of the 01:45Z event, at its author's
        own insistence: STRIKE the 'structurally loudest exactly where
        budget failures are most real' framing — esmeraldinha's
        compile-correlation claim is REFUTED by her own cross-check with
        pirria (1 of 5 stamped tests across both hosts is cargo-invoking;
        she asserted a pattern without checking her own log, named it as
        the same failure she had just criticized in a fixture, and
        retracted). Defects 1 (scope) and 2 (proposition) stand exactly
        as filed. The surviving advice claim is narrower and sufficient:
        five false SATURATED verdicts across two hosts void the stamps in
        guests, no correlation asserted. NEW DEFECT 3 (hers): WRONG
        WINDOW — any trailing average (load1 ~60s decay, or the
        previously-specified PSI avg10) adjudicating a step SHORTER than
        the window inherits the PREVIOUS step's work; her log shows a
        stamp on a 5s no-cargo step earned by the compile that finished
        in the prior step. The three defects COMPOSE: host-wide
        instrument, utilization-as-starvation, inherited window. FIX
        REDESIGNED accordingly (still claimed, macuahuitl): diff the
        MONOTONIC total= microsecond accumulators in cpu.pressure across
        the step's own execution (read at launch, read at kill) — the
        correct instrument for a bounded interval; where that is more
        than the runner should carry, UNCLASSIFIED for short steps —
        better no verdict than a verdict from the wrong window. The pair
        is timing check-cheatsheet-sources --no-sha directly with PSI
        banked either side, since '>5s' cannot size a re-budget.
