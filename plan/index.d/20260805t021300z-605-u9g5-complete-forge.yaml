# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-opencode-20260805t021300z (forge host, linux-next)
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
status:
  - packet_id: codex-mcp-config-merge-fixture
    field: status
    value: completed
    ts: "2026-08-05T02:32:24Z"
    host: forge
  - packet_id: codex-mcp-expert-validation
    field: status
    value: pending
    ts: "2026-08-05T02:32:24Z"
    host: forge
events:
  - packet_id: codex-mcp-config-merge-fixture
    event:
      type: claim
      ts: "2026-08-05T02:14:00Z"
      agent_id: forge-opencode-20260805t021300z
      host: forge
      lease_id: 605-u9g5-forge-mcp-registration
      expires_at: "2026-08-05T06:14:00Z"
      summary: >
        Claimed on linux-next after the 2026-08-05T02:06Z meta-orchestration
        start (credential ok:forge-git-mirror, branch ok:branch-linux-next,
        clean startup boundary). Release-targeted (harness-mcp-expert-validation)
        and freshly shaped by the 2026-08-04 Codex forge; verified unclaimed and
        unimplemented before this drain.
  - packet_id: codex-mcp-config-merge-fixture
    event:
      type: completed
      ts: "2026-08-05T02:32:24Z"
      agent_id: forge-opencode-20260805t021300z
      host: forge
      summary: >
        Implemented and verified. images/default/config-overlay/codex/
        register-experts.sh registers forge-plan and project-info as Codex
        stdio MCP servers through the supported `codex mcp add <name> -- <cmd>`
        interface (verified live against codex-cli 0.146.0: writes
        [mcp_servers.<name>] into $CODEX_HOME/config.toml, replaces the same
        name, never touches auth.json). The helper is CODEX_BIN/CODEX_HOME/
        TILLANDSIAS_CONFIG_OVERLAY_ROOT injectable, pre-checks `codex mcp list
        --json` so an already-registered server is not churned, and creates a
        missing $CODEX_HOME up front (codex mcp list refuses to run when the
        root is absent, verified empirically). Containerfile COPYs it to
        /usr/local/bin/register-codex-experts and chmods it; entrypoint-forge-
        codex.sh invokes it after require_codex and before either launch path,
        wrapped fail-soft into /tmp/forge-lifecycle.log so a registration
        defect can never block a forge launch.
        Fixture: scripts/test-codex-mcp-registration.sh (empty + pre-populated
        CODEX_HOME, double application each, exact-once entry per server,
        byte-preserved auth.json, preserved unrelated config keys + unrelated
        custom MCP server, and wire-level ordering asserts). It runs against
        the REAL codex CLI when available (this forge: 0.146.0) and the
        committed hermetic model scripts/fixtures/codex-mcp-stub.sh otherwise;
        both legs PASS. Verified real-CLI semantics were encoded in the stub
        (map-form mcp_servers tables, replace-same-name, list --json shape).
        Exit-criterion audit: (1) image COPY + chmod + entrypoint invocation
        after require_codex/before launch — pinned; (2) helper registers both
        servers via the supported CLI and a double application leaves exactly
        one entry each — pinned by fixture against real codex AND stub;
        (3) empty + pre-populated fixtures prove auth.json byte-preservation
        and unrelated key/MCP preservation — pinned; (4) discoverability
        litmus extended (now 13/13, incl. the fixture + two wire steps), bash
        -n clean on all touched scripts, ./build.sh --check exit 0.
        Litmus:forge-experts-discoverability-shape PASS 13/13.
