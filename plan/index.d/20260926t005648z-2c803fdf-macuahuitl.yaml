# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: on-wsl2-the-gate-memory-preflight-reads-the-utility-vms-meminfo-so-it-prints-ok-about-a-machine-that-is-not-the-one-that-reaps-the-gate-and-against-an-8gb-capped-guest-its-1gib-floor-can-essentially-never-fire
    event:
      type: note
      ts: "2026-09-26T00:56:48Z"
      agent_id: linux-macuahuitl-claude-20260926t005639z
      host: macuahuitl
      summary: |
        SWAP JOINS THE WRONG-MACHINE FINDING (macuahuitl, 2026-09-26). The swap-backed budget (1375-xxzj) makes the launch preflight read SwapFree as well as MemAvailable: a shortfall clamps the forge's memory.swap.max and warns rather than refusing. On WSL2 the guest's SwapTotal is exactly the .wslconfig swap= value and says nothing about Windows' committed memory, so both figures must come from GlobalMemoryStatusEx on the host (the fix this row already names for MemAvailable). Spec delta scenario "On WSL2 the measurement is the Windows host's" in openspec/changes/forge-swap-backed-memory-ceiling. Design: plan/issues/forge-memory-swap-architecture-design-2026-09-26.md section 6.
