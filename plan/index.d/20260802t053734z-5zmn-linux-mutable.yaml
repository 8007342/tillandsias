# Ledger fragment - append-only, IMMUTABLE once written.
# Written by: linux-macuahuitl-opencode-20260802T0519Z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: retire-stale-removed-workflow-litmus-assumptions
    order: 592-5zmn
    desired_release: v0.5
    title: "Retire stale ci.yml and nix-cache-warm.yml assumptions from pre-build litmuses"
    status: ready
    kind: fix
    deliverable: openspec/litmus-tests/litmus-ci-release-toolchain-shape.yaml and openspec/litmus-tests/litmus-nix-cache-size-signal.yaml
    depends_on: []
    pickup_role: linux
    capability_tags: [ci, litmus, github-actions]
    estimated_hours: 1
    outcome: >
      The pre-build suite no longer fails because two litmuses require GitHub
      workflow files that were intentionally removed under the ratified
      one-workflow Actions budget. Preserve active release/Nix contracts while
      deleting or replacing assertions over retired workflow surfaces.
    next_action: >
      Run each named litmus alone, compare its assertions with
      litmus:github-actions-budget and methodology CI policy, then remove or
      redirect only the stale workflow-file checks.
    exit_criteria:
      - "scripts/run-litmus-test.sh ci-release --phase pre-build passes without requiring .github/workflows/ci.yml"
      - "scripts/run-litmus-test.sh nix-builder --phase pre-build passes without requiring .github/workflows/nix-cache-warm.yml"
      - "litmus:github-actions-budget remains green and exactly one workflow, release.yml, remains"
    events:
      - type: filed
        ts: "2026-08-02T05:37:34Z"
        agent_id: linux-macuahuitl-opencode-20260802T0519Z
        host: linux
        summary: >
          Local-build smoke at 37722dba stopped before reset with both stale
          workflow assumptions red. Adjacent GitHub Actions budget checks passed;
          evidence is in build-install-smoke-e2e-findings-2026-08-02.md.
