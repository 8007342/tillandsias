# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (order 699-usxc).
#
# The target packet is declared only in an uncompacted fragment, so the
# BASE ledger has no block to append into. Recording the event here keeps
# it visible to the fold, which is what every reader consults.
events:
  - packet_id: sni-windowid-type-wedges-shell
    event:
      type: note
      ts: "2026-08-31T04:19:47Z"
      agent_id: linux-macuahuitl-fable5-20260831t041947z
      host: macuahuitl
      summary: >
        CORRECTION to the premature 'accepted end to end': round 3's phone-home only occurred AFTER the operator, returning from dinner, manually approved a THIRD first-run dialog — the --dangerously-skip-permissions consent prompt — that had been holding the session (the two earlier rounds' windows sat dead from their kills). The acceptance claim is downgraded to 'works with one manual approval'. Blocker #3 seeded (bypassPermissionsModeAccepted); zero-touch round 4 with an exit-after-report prompt is the honest acceptance run. Lesson, same family as tonight's others: an acceptance observed while an operator MIGHT have intervened is not an acceptance of autonomy — the operator's absence at dinner is what surfaced the dialog.
