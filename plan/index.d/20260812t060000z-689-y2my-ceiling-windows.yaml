# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-claude-opus5-20260811t215500z

events:
  - packet_id: pty-heartbeat-defeats-exec-progress-deadline
    event:
      type: progress
      ts: "2026-08-12T06:00:00Z"
      agent_id: "windows-claude-opus5-20260811t215500z"
      host: windows
      summary: >
        Remaining item (a) ANSWERED and implemented; criterion 1 left refuted,
        as instructed. The macOS scope correction asked whether any backstop is
        wanted at all, and if so that it be a generous configurable wall-clock
        ceiling explicitly justified against order 332.
        DECISION: yes, one is wanted, for exactly one property the
        observational fix cannot provide — TERMINATION. The heartbeat reports
        shipped in the safe half make a wedge legible within 30 seconds to
        anyone watching; a ceiling is what happens when nobody is, which is the
        normal case for an unattended recurring cycle that wedges at 02:00.
        It is NOT the refuted progress deadline. It never asks whether output
        means progress. It is a single wall-clock bound of 14400s (4h) against
        a measured 1290s worst-case legitimate first-run silence — a ~11x
        margin — configurable via
        TILLANDSIAS_VSOCK_EXEC_WALL_CLOCK_CEILING_SECS, with an explicit 0 to
        disable and a 300s floor so a host cannot quietly reintroduce a bound
        tight enough to kill working work.
        The breach message carries what the exec was waiting for and how many
        bytes the guest had produced, and says the wire was ALIVE — a ceiling
        breach is a wedged guest, not a stale connection, and reporting it as
        the latter is the misdirection that made the 70-minute wedge
        unreadable.
        Checking the ceiling at the top of the read loop is sufficient because
        the heartbeat wakes that loop every 30s; a guest that stops
        heartbeating is the other failure and the idle deadline already owns
        it.
        Tests: tillandsias-vm-layer vsock_exec 15 passed / 0 failed, including
        BOTH order-332 pins unchanged
        (streaming_exec_heartbeats_survive_total_silence_beyond_idle_deadline,
        streaming_exec_times_out_when_slow_guest_sends_no_frames). Three added:
        the parser contract (default / explicit-disable / floor refusal), the
        breach message with a negative control that an empty pending slot does
        NOT invent a label, and a guard asserting the ceiling stays at least
        10x the measured worst case — which is the test that should fail if a
        later edit tightens it back toward the idle deadline.
        REMAINING: item (b), the protocol-level fix — a guest BLOCKED ON INPUT
        saying so in its heartbeat, which distinguishes "working" from
        "waiting for me" exactly instead of guessing from elapsed time. Not
        started here: it is a wire-format change across control-wire, the guest
        pty handler and the host exec client, needs capability negotiation
        (CAP_PTY_HEARTBEAT_V2) so a version-skewed guest keeps working, and
        does not fit one recurring-loop cycle. That is where the value is; this
        packet should stay open for it.
