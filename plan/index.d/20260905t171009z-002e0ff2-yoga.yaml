# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: transport-negligible-overhead-audit
    field: status
    value: ready
    ts: "2026-09-05T17:10:09Z"
    host: yoga

events:
  - packet_id: transport-negligible-overhead-audit
    event:
      type: progress
      ts: "2026-09-05T17:10:09Z"
      host: yoga
      summary: |-
        status 'ready' (not terminal) with evidence_refs: bde83aa26
  - packet_id: transport-negligible-overhead-audit
    event:
      type: note
      ts: "2026-09-05T17:10:09Z"
      host: yoga
      summary: |-
        releasing the lease: criterion 4 landed, remaining criterion belongs to packet 148
