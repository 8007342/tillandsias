# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: check-dead-env-branches-cannot-see-rust-doc-comments-or-wrapper-setters-so-a-documented-and-tested-variable-reads-as-dead
    event:
      type: progress
      ts: "2026-09-13T20:05:44Z"
      agent_id: linux-macuahuitl-fable-20260913t194011z
      host: macuahuitl
      summary: |
        1168-bbbe closed: `///` and `//!` doc comments now DOCUMENT (a plain `//` deliberately does not: boundary control ${P}_RUST_PLAINCOMMENT_VAR stays dead), and `ANY.set("VAR"` assigns alongside set_var/.env including through the rustfmt one-line join. TILLANDSIAS_AGENT_TIMEOUT_SECS cleared on the tree by both routes (its `///` contract and TestEnvRestore's restore.set). Arms ${P}_RUST_DOC_ONLY_VAR and ${P}_RUST_WRAPSET_VAR red on the pre-fix code.
