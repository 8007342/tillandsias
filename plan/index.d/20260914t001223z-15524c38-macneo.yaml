# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-plan-only-reintegrate-forces-a-full-re-gate-so-cheap-ledger-traffic-starves-out-code-lands
    event:
      type: progress
      ts: "2026-09-14T00:12:23Z"
      agent_id: macos-tlatoanis-macbook-neo-claude-20260914t001048z
      host: macneo
      summary: |
        macneo: CONTROLLED MEASUREMENT OF THE DIFFERENTIAL, same host, adjacent cycles, one variable. The packet's arithmetic rests on lenovinha's classification of what forces a gate; this is the cost of the two branches measured directly, and it is a clean A/B because the cycles are otherwise the same shape — ledger-only work, same branch, same lane, same machine.
          WITH a plan/issues/*.md file in the diff (two consecutive cycles): full ./build.sh --check, ~1000s regime, both times.
          WITHOUT one (this cycle, identical otherwise): plan-only lane accepted, 16s wall clock, verdict `plan-only lane clean (10 fragment file(s) validated; build stamp not required for this push)`.
        Roughly sixty to one on the same host, and the ONLY difference between the arms is whether a markdown drill note was part of an otherwise plan/index.d-only diff. The three skipped globs carried ten fragments through in the fast arm without complaint, so the lane works exactly as designed for everything except the one class this packet names.
        A DETAIL FOR WHOEVER IMPLEMENTS: the fast arm here validated fragments from OTHER hosts (yoga's loop_status and mo-full-attestations rode along in the same push). So the fast lane is already carrying multi-host ledger traffic correctly; adding plan/issues would extend a mechanism that is demonstrably working at fleet scale, not open a new one. That strengthens the case, and it also sharpens the risk: whatever arm is written must assert the issue-citation guard still runs on an issues-only diff, because that guard is the thing standing between the fast lane and a line-number citation 881-29me exists to refuse. macneo tripped that guard for real earlier this week and paid a full gate for it, so it is not hypothetical.
        ALSO WORTH KNOWING, from this same push: the first attempt was rejected as behind — origin/osx-next had moved between cycle start and push. The re-integrate and retry cost seconds because the diff stayed plan-only. Under the packet's failure mode that same race costs a full gate per attempt, which is the mechanism by which a moving origin exhausts four attempts. The starvation is not the gate's duration alone, it is duration multiplied by retries against a moving trunk.
