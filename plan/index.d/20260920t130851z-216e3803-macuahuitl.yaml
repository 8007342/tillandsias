# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: an-install-resets-and-reprovisions-the-local-state-on-every-platform-and-then-runs-the-platform-initialisation-itself-so-a-curl-bash-or-irm-iex-install-is-also-the-repair-for-a-broken-local-state
    event:
      type: note
      ts: "2026-09-20T13:08:51Z"
      agent_id: linux-macuahuitl-fable5-20260920t130850z
      host: macuahuitl
      summary: |
        DESIGN REFINED by measurement (yolanda, 2026-09-20T12:50Z; ruling by the coordinator the same hour): "one binary flag, three platforms" is ONE CONTRACT WITH A PER-PLATFORM BODY, not one code path. On Windows the tray's --reset-guest (crates/tillandsias-windows-tray/src/notify_icon.rs, reset_guest_once) ALREADY has the ruling's semantics: wipe_guest (graceful stop, wsl --unregister of the distro deleting the VHDX, the import-complete marker), clear_guest_vault_credentials (vault-shamir-share-v1 and vault-root-token-v1 cleared, tillandsias-vm-uuid deliberately preserved per installation_uuid.rs), provision_via_recipe synchronously, exit 0 on Ready and 1 on failure. pirria's reason for a new flag (--reset-guest preserves images and never touches the keychain) is a property of the Linux headless binary, not of the fleet. So the Windows arm is installer wiring plus four gaps: (a) the canonical name --reset-state added on the tray over the existing path with --reset-guest kept as an alias; (b) A DEFECT ON ITS OWN, pre-fix measured RUN: zero occurrences of TILLANDSIAS_DESTRUCTIVE_RESET_OK in the tray crate, so the Windows reset destroys unconditionally on the platform whose smoke runbook names that variable as the only supported opt-out, fixed in the arm as the guard's first installer caller; (c) the announcement reshaped to name what is preserved (tillandsias-vm-uuid first) beside what is destroyed BEFORE destroying; (d) the download cache purged with wsl-build excluded (1295-b4i8's enumeration: cache/, logs/, state/, wsl/, tray-windows.lock). install-windows.ps1 calls tillandsias-tray.exe --reset-state synchronously after 1258-8wfb's health check and exits with its status; the Windows binary carrying the flag MUST be the tray, because the headless binary refuses that lane by design (1276-2hc6's litmus). THE CONTRACT, shared by all three bodies: the name; the semantics (local state destroyed, installation identity preserved, reprovision through the platform's own init, synchronous); the announcement shape; the TILLANDSIAS_DESTRUCTIVE_RESET_OK guard with ONE skipped-case string on every platform (pirria names the exact string; the others copy it); exit 1 on failure. Bodies: Linux in headless (pirria: parse, dispatch, announcement printer, run_reset_state), Windows on the tray (yolanda), macOS on the macOS tray (macneo, who first measures what the tray's existing reset already does, step by step with file and line, and whether it reads the guard at all, before adding anything).
