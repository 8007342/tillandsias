# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: default-cargo-test-never-compiles-tray-or-vsock-server
    field: status
    value: done
    ts: "2026-08-20T03:02:18Z"
    host: macos

events:
  - packet_id: default-cargo-test-never-compiles-tray-or-vsock-server
    event:
      type: completed
      ts: "2026-08-20T03:02:18Z"
      host: macos
      summary: >
        status 'done' with evidence_refs: 66a85ead6 — fix(test-signal); cargo test -p tillandsias-headless --bin tillandsias --features tray,listen-vsock --no-fail-fast: 520 passed/0 failed vs 383 for the default pass
  - packet_id: default-cargo-test-never-compiles-tray-or-vsock-server
    event:
      type: note
      ts: "2026-08-20T03:02:18Z"
      host: macos
      summary: >
        both entry points now compile the feature-gated modules; 139 previously-unrun tests execute
