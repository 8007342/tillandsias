# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t203500z
# RECOVERY PROVENANCE: the two fragments below were authored in-forge by
# linux-bigpickle-opencode-20260810t2011z (commit 1a01228a in its ephemeral
# checkout, 2 fragments, 130 lines) and were LOST when the container exited:
# the pre-push gate demands ./build.sh --check in-checkout, the session's
# BUILD-FREE directive forbade it, the agent correctly refused --no-verify,
# asked for coordinator disposition, and the teardown deleted the checkout.
# Reconstructed by the coordinator from the journald transcript (container
# tillandsias-tillandsias-forge-bigpickle-valid-04f32576..., 13:07-13:11 PDT).
# Coordinator files this ONE fragment carrying both payloads; original
# filenames and the agent's own wording preserved where the journal held them.
packets:

  - packet_id: loop-status-count-goes-stale-on-every-filed-packet
    order: 668-9z9h
    status: ready
    desired_release: v0.5
    priority: p3
    title: "loop-status-verify was RED at HEAD again 25 minutes after being fixed — the committed count is invalidated by every filed packet"
    kind: bugfix
    deliverable: fold-time count refresh or a verify that tolerates post-fold fragments; the check must stop being a treadmill
    depends_on: []
    pickup_role: linux
    capability_tags: [plan-tooling, loop-status, verify]
    estimated_hours: 2
    context: >
      Found in-forge by linux-bigpickle-opencode-20260810t2011z during the
      round-2 validation. At its HEAD (61705906), loop-status-verify was RED:
      committed (233 open / 363 total tagged) vs live (235 open / 366 total
      tagged). Its root-cause analysis, quoted: "base 233/363 -1 (659 done)
      +3 (665/666-qbjd/667-se87 ready) = 235/366, exactly what verify
      demands — but the fold (980b34b4) predates 667-se87, so the committed
      count is stale," contradicting the 20:05Z handoff fragment's
      verdict=ok, which was TRUE when written. The structural defect: the
      committed count is a snapshot, every subsequently filed release-tagged
      packet invalidates it, and the coordinator fixed this exact count at
      12:23 only for it to be stale again by 12:51. A check that is red
      within half an hour of every green is a treadmill, not a control.
    exit_criteria:
      - filing a release-tagged packet no longer flips loop-status-verify red on its own — either the fold/verify derives the count live from the folded ledger, or verify treats the committed count as a floor annotated with its fold ref
      - the coordinator cycle that lands this change leaves verify green and a subsequently filed test packet keeps it green
    events:
      - ts: "2026-08-10T20:11:00Z"
        event: filed
        agent_id: linux-bigpickle-opencode-20260810t2011z
        summary: >
          Filed in-forge during build-free validation round 2; original
          fragment lost with the checkout, reconstructed by the coordinator
          from journald.

  - packet_id: in-forge-plan-only-commits-cannot-pass-the-push-gate
    order: 668-2xeh
    status: ready
    desired_release: v0.5
    priority: p2
    title: "An in-forge agent that only files plan fragments cannot push them without a full workspace build"
    kind: enhancement
    deliverable: a plan-only fast lane in the pre-push gate (YAML/schema/marker validation without cargo) or an explicit relay path for validation-only sessions
    depends_on: []
    pickup_role: linux
    capability_tags: [gate, forge, plan-tooling, push]
    estimated_hours: 3
    context: >
      Round 2's ending, verbatim from the session: "Push blocked by the
      pre-push gate, and I can't clear it without violating the BUILD-FREE
      directive: the hook requires ./build.sh --check to have run in this
      checkout." The agent behaved exactly right — refused --no-verify,
      reported, exited — and its two fragments died with the container,
      needing journald archaeology to recover. The gate is the only trunk
      protection and must stay; but a diff that touches ONLY plan/index.d/
      and plan/loop_status.d/ fragments has a verifiable closure (YAML
      parse, fragment schema, marker grammar) that does not require
      compiling the workspace. Without a plan-only lane, every lightweight
      in-forge validation/reporting session either burns a full build
      (667-se87 shows in-forge workspace builds are currently crashing
      rustc), pushes nothing, or is tempted toward --no-verify — the worst
      of the three.
    exit_criteria:
      - a diff touching only plan fragment paths passes the pre-push gate through an explicit fast lane that still validates YAML, fragment schema, and forbidden-pattern checks, recorded in the gate stamp as plan-only
      - any non-fragment path in the diff disqualifies the fast lane (fail closed to the full gate)
      - litmus pins both the acceptance and the disqualification
    events:
      - ts: "2026-08-10T20:35:00Z"
        event: filed
        summary: >
          Filed by the coordinator from the round-2 session's terminal
          blocker; the agent's refusal to bypass was correct and is the
          behavior to keep.

events:
  - packet_id: mirror-dns-identity-is-shared-across-projects
    event:
      type: verified-by
      ts: "2026-08-10T20:11:00Z"
      agent_id: linux-bigpickle-opencode-20260810t2011z
      host: forge
      summary: >
        Independent in-forge verification (round 2): the 659-8faj derivation
        function confirmed at main.rs:2966 with its tests; no
        tillandsias-git/git-service alias assignment remains in the launch
        path. Reconstructed from journald by the coordinator.
  - packet_id: mirror-identity-transitional-residue-cleanup
    event:
      type: progress
      ts: "2026-08-10T20:11:00Z"
      agent_id: linux-bigpickle-opencode-20260810t2011z
      host: forge
      summary: >
        Round-2 spot-check: orchestrate-enclave.sh:158 still carries an
        old-alias residue site — dead legacy, already inside this packet's
        scope; listed here so the cleanup does not miss it.
  - packet_id: tray-control-wire-has-no-launch-verb
    event:
      type: progress
      ts: "2026-08-10T20:11:00Z"
      agent_id: linux-bigpickle-opencode-20260810t2011z
      host: forge
      summary: >
        Round-2 spot-check confirmed 664-7qzz and 664-yw8r exist and fold
        correctly at its HEAD (61705906).
  - packet_id: opencode-segfault-reproduced-with-a-host-side-coredump
    event:
      type: progress
      ts: "2026-08-10T20:35:00Z"
      agent_id: linux-mutable-20260810t203500z
      host: linux_mutable
      summary: >
        CONTROL DATA for the resource-ceiling hypothesis (667-se87): the
        round-2 session in the SAME image, same instance class, ran 19
        minutes doing litmus + repo reads WITHOUT cargo builds and exited
        cleanly — zero new coredumps (coredumpctl checked at session end).
        First session crashed 9 minutes in while ./build.sh --check
        compiled the workspace. Consistent with the build being the
        pressure source; not yet proof.
  - packet_id: validation-round-2-verdict
    event:
      type: filed
      ts: "2026-08-10T20:12:00Z"
      agent_id: linux-bigpickle-opencode-20260810t2011z
      host: forge
      summary: >
        VALIDATION VERDICT (round 2, build-free): NOT FULLY GREEN — one
        genuine discrepancy: loop-status-verify RED at HEAD (filed as
        668-9z9h); every other spot-check reconciled (659-8faj derivation +
        tests, 664 packets, 666-cmr7 residue scope). Litmus instant
        pre-build: 6 FAILs, all classified environmental in-forge, none a
        code regression — 4 podman-ENV (podman unresponsive inside the
        forge: default-image-containerfile-shape,
        forge-standalone-runtime-shape, podman-path-availability,
        runtime-diagnostics-stream-shape) and 2 environment-shaped
        (podman-egress-control-shape trips on the forge's by-design
        http_proxy=http://proxy:3128; hook-install-portability-shape trips
        on the shared external hooks dir). These six want host_kind /
        precondition declarations per the 661-emqi mechanism so an in-forge
        run reports SKIP instead of FAIL. Original verdict fragment lost
        with the checkout; reconstructed from journald.
