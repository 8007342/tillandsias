# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-test-added-in-either-organ-can-be-referenced-by-nothing-and-nothing-says-so-at-push-time-so-a-fixture-that-cannot-fail-a-gate-reads-identically-to-one-that-guards-something
    event:
      type: verified
      ts: "2026-09-21T08:08:30Z"
      agent_id: linux-pirria-claude-20260921t050924z
      host: pirria
      summary: |
        Promoted to verified: the push-time half is now observed AS WIRED, which is what
        next_action named as the outstanding condition.

        Both paths were watched on a dry-run push against a scratch ref that was never
        created on origin — the hook fires on --dry-run, but ONLY when refs actually
        reach it. An earlier attempt produced no output and I nearly recorded that as
        "the hook does not name it": the hook had exited at "no refs on stdin — git is
        pushing nothing", because HEAD already matched the remote. Silence from a step
        that never ran is not a negative result, which is this packet's subject wearing
        another coat.

        OBSERVED, shell organ, as wired:
          unreferenced added test present — the hook prints "this push adds a test
          nothing can fail because of (1325-ygq5)", the verdict warn:added-test-
          unreferenced:1 standing=41 surfaces=6, and NAMES the file.
          clean tree — the hook prints ok:added-test-referenced:2 checked standing=40
          surfaces=6.

        The litmus organ's push-time naming is covered hermetically rather than as
        wired: it is the same code path with a different needle, and arms 4 and 5 of
        the fixture exercise it. Said plainly so nobody reads this rung as covering
        both organs in the field.

        WHAT THE PROMOTION COST, and it belongs on the row because it is the packet's
        own sentence for the third time. The first wired version was SILENT on success,
        so it read identically whether wired or not. The second rendered its own warning
        under a GREEN CHECK: it captured the guard 2>&1 into one blob and prefix-matched
        for warn:, but the guard writes detail to stderr and the verdict to stdout, so
        the blob began "UNREFERENCED TEST ADDED" and the warn branch was unreachable. A
        verdict channel is only a channel when nothing else is mixed into it.

        THE GAP THAT LET BOTH THROUGH, for whoever takes the enforcement flip: the
        fixture proves the GUARD and knew nothing about either defect, because both
        lived in the HOOK's rendering of the guard's answer. A hook-level arm is
        missing, and it is the arm that would have caught a warning painted green.
