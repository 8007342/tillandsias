# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: a-killed-tray-orphans-the-virtualization-helper-and-every-later-vm-start-fails-as-an-invalid-boot-loader
    field: status
    value: done
    ts: "2026-09-21T04:27:42Z"
    host: macos

events:
  - packet_id: a-killed-tray-orphans-the-virtualization-helper-and-every-later-vm-start-fails-as-an-invalid-boot-loader
    event:
      type: completed
      ts: "2026-09-21T04:27:42Z"
      host: macos
      summary: |-
        status 'done' with evidence_refs: CLOSED ON HALF 1. Half 2 is recorded as NOT REPRODUCED, not as fixed.  Half 1, measured on tlatoanis-macbook-air 2026-09-20 against 56.9.20.1: with a VM holding nvram.bin, a second start reported "pid 31650 still has .../nvram.bin open — a live VM already running on this image" and "DO NOT --reset-guest", instead of relaying "The boot loader is invalid". I then ran the `kill 31650` the message itself printed and the identical command that had just failed SUCCEEDED — the remedy was executed, not reasoned about.  Half 2: on 56.9.20.1, TERM/INT/KILL mid-`--exec-guest`, each with the in-guest command confirmed running first, had the helper reaped every time. The probe is not blind — lsof showed pid 31650 holding nvram.bin during a live run. The leak was measured twice on 56.9.13.1 (pids 22546, 23586). What changed between those versions is unidentified and is NOT claimed as deliberate. Reopen by specimen, with the version.  Fixture: ok:macos-vz-orphan-diagnosis:4 in ~21s, bound as litmus:macos-vz-orphan-diagnosis under vm-provisioning-lifecycle (post-build). The MUTATION ARM is arm 2: it leaves a live VM holding nvram and requires the same second-start assertion to RED and the failure text to name the holder and refuse --reset-guest. Without it the fixture would only prove a VM started twice on a quiet host.  Landed SHA: see the relay of osx-next carrying this commit.
  - packet_id: a-killed-tray-orphans-the-virtualization-helper-and-every-later-vm-start-fails-as-an-invalid-boot-loader
    event:
      type: note
      ts: "2026-09-21T04:27:42Z"
      host: macos
      summary: |-
        half 1 landed and measured end to end; half 2 has no specimen on this build and is recorded as not reproduced
