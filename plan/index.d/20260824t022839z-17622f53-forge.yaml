# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: podman-sync-wait-bounded-busy-polls-two-detached-threads
    field: next_action
    value: On Windows and macOS, fetch the linux-next commit containing e3e4ee8ee, run cargo test -p tillandsias-podman plus the two exact-PID timeout tests, and append verified evidence before promoting beyond implemented.
    ts: "2026-08-24T02:28:39Z"
    host: forge

events:
  - packet_id: podman-sync-wait-bounded-busy-polls-two-detached-threads
    event:
      type: note
      ts: "2026-08-24T02:28:39Z"
      host: forge
      summary: >
        all-platforms packet needs native confirmation of wait-timeout's platform implementations
