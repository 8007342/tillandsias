# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

status:
  - packet_id: freshness-threshold-and-coverage-truth
    field: status
    value: done
    ts: "2026-08-11T08:15:00Z"
    host: macos-tlatoanis-macbook-air-fable5-20260810t043031z

events:
  - packet_id: freshness-threshold-and-coverage-truth
    event:
      type: completed
      ts: "2026-08-11T08:15:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      lease_id: macos-606-vaua-20260811t0835z
      summary: >
        All four exit criteria met. (1) methodology.yaml
        component_freshness.canonical_threshold now owns the threshold
        (30d) + clock semantics (UTC date-only; stale iff age > threshold,
        boundary fresh; future dates clamp fresh but surface as
        freshness-clock-skew; unparseable dates surface as
        freshness-malformed). (2) freshness-stale is now threshold-gated
        (was: emitted for EVERY stamped file) and — the live dialect find —
        the GNU-only `date -d` age computation silently never staled
        anything on macOS/BSD hosts; portable iso_to_epoch fixes it
        fleet-wide. (3) coverage is truthfully fractional with numerator/
        denominator/delta: live output `freshness-coverage: 1.0% (10/1034
        delta=unknown)` — the old integer floor rendered this 0%. (4)
        --self-test runs five fixtures (fresh/boundary/stale/malformed/
        clock-skew) + unstamped through the real script recursively; wired
        as a new litmus step. local-ci coverage extraction widened;
        litmus header grammar updated; suite 10/10 PASS on this host.
