# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: floor-tier-parallelism-sweep
    event:
      type: finding
      ts: "2026-09-05T03:02:23Z"
      agent_id: windows-esmeraldinha-claude-20260905t030223z
      host: esmeraldinha
      summary: |
        FLOOR SWEEP COMPLETE (esmeraldinha, N100 4c, 7942 MB inside the distro, drvfs; three valid arms plus the zero-compile control; every arm forced, touch crates/tillandsias-core/src/lib.rs (the widest reverse-dependency set, 9 crates, checked), fail:test-baseline:ran=2202 with compiling_lines=14 and arm_verdict=valid, samples verified inside each arm's own window, idle host before each): A0 zero-compile control: io_some 11.40, io_full 11.27, cpu_some 1.07, used 685 MB, rustc 0; A (unset, 4 jobs): 415 s, io_some 48.75, io_full 45.64, cpu_some 24.11, mem_full 0.14, used 1329 MB, rustc_max 4; B (jobs=2): 427 s (+2.9%), io_some 16.71, io_full 15.99, cpu_some 9.87, used 1243 MB, rustc_max 2; C (jobs=1): 493 s (+18.8%), io_some 42.80, io_full 42.45, cpu_some 1.64, used 858 MB, rustc_max 1. rustc_max 4/2/1 is direct proof each cap crossed the boundary (via the WSLENV workaround). AGAINST THE FALSIFIER STATED FIRST: B QUALIFIES (+2.9% wall inside the 10% band; io-full peak down 65%); C FAILS BOTH (+18.8% breaks the band; peak only 7% below A and nearly three times B's). So on the floor parallelism IS a lever, non-monotonic, with an INTERIOR OPTIMUM at nproc/2, not a less-is-better gradient; capping harder is worse on time and barely better on stall. UNEXPLAINED AND NOT DRESSED UP: jobs=1 has almost no CPU contention (1.64) yet its I/O stall stays at 42.45; the plausible story (one job exposes every I/O wait with nothing to overlap it, two let one compute through the other's wait, four re-introduce contention) is untested, and an interior optimum deserves a second host before a table is built on it (yolanda after 1042-svey). REGIME: the zero-compile control is the anchor, a gate that compiles nothing peaks at 11.27 io-full, the drvfs floor present before any compiler runs; every arm sits above it and the best is 42% above it, so the I/O regime is the dominant term and the job cap stops making it worse rather than escaping it; both findings hold together (macuahuitl's row: the compiler is not the freeze driver, the test phase is). CONTAMINATION INCIDENT, two arms lost: a chained multi-arm driver reported killed (handle status killed; a pattern-based ps grep returned 0) was never dead; it sat in wait_idle, timed out because of the manual arm, and launched its own arm B concurrently with the manual arm C, overwriting B's samples and racing C's build (C reported compiling_lines=0 and was VOIDED by the compile-work guard on its first outing); the process check was an instrument that could not see the answer (Get-CimInstance Win32_Process with a CommandLine filter found it in one query), the fifth confident negative of the day that had no way to be earned. Added to the protocol for both tiers: a per-arm sample-window check (first and last sample inside the arm's own start/end), and no chained multi-arm drivers on this harness (arms run individually, or the driver takes a stop file). The first clean manual B (429 s) was overwritten before it could be preserved and was re-run as B2 (427 s), agreeing within 0.5%, which also establishes wall time on this host as reproducible to under a percent, so the 12 s A-B gap is real. Nothing committed or pushed.
