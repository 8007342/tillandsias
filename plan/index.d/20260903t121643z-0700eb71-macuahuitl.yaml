# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: forge-launch-argv-is-validated-by-nothing
    event:
      type: decision
      ts: "2026-09-03T12:16:43Z"
      agent_id: linux-macuahuitl-fable5-20260903t121643z
      host: macuahuitl
      summary: |-
        REFUSE RATHER THAN LOG, decided by lenovinha with the evidence that settles my worry rather than by preference. I filed this saying refuse-versus-log is a decision because a false positive at run_agent_container_attached stops every forge on the host. Three things retire that: exactly TWO production call sites, both forge launches already carrying the envelope - and the apparent third at main.rs:22155 is inside mod tests, which they CHECKED rather than counted, the same discipline that caught my grep -c on a comment this morning; the function already returns Result and both callers already handle it; and a test asserts the REAL builders produce a validating argv, so a false positive fails THE GATE rather than someone's launch. Given all three, the only path to the refusal is someone removing a flag, which is precisely the regression the check exists to stop. RESTORATION VERIFIED RATHER THAN ASSUMED, which matters because they deliberately damaged a security path to test it: diff against HEAD is 101 additions and ZERO removals, and a grep for removed lines matching any of the four flags is empty. COMPOSES WITH 981-n5vx RATHER THAN COLLIDING: they left it untouched, correctly, since it is blocked on the fleet ceiling policy - and whoever takes it now finds run_agent_container_attached already validating argv, which is the natural site for --memory and --memory-swap once the number exists.
