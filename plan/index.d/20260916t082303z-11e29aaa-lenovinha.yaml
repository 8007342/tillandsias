# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: linux-tray-login-vault-poll-to-event
    field: status
    value: in_progress
    ts: "2026-09-16T08:23:03Z"
    host: lenovinha

events:
  - packet_id: linux-tray-login-vault-poll-to-event
    event:
      type: progress
      ts: "2026-09-16T08:23:03Z"
      host: lenovinha
      summary: |-
        status 'in_progress' (not terminal) with evidence_refs: socket-audit-master, seeded override per 949-uv5k
  - packet_id: linux-tray-login-vault-poll-to-event
    event:
      type: note
      ts: "2026-09-16T08:23:03Z"
      host: lenovinha
      summary: |-
        claimed by lenovinha for the 07:51 drain cycle; the routed epic held one host-scoped packet, so the documented explicit-seed remedy routed to socket-audit-master (residual 22.495, blocking 9)
