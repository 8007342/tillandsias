# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260812t094559z
#
# HAND-AUTHORED because no CLI path exists for this write. See the 699-usxc
# event below: append-event refuses a fragment-only packet, and set-field
# no-ops (writing nothing at all, including its --reason note) when the value
# is unchanged. Hand-authoring the fragment is the only remaining route.
events:
  - packet_id: guest-fetch-unit-and-build-flag-latent-wipe-traps
    event:
      type: note
      ts: "2026-08-12T09:45:59Z"
      host: macos
      summary: >
        CORRECTION to the preceding evidence event, which lost a word to shell
        command substitution. It reads "It cannot:  is ITSELF #[cfg(feature =
        vault)]" — the missing subject is the MODULE DECLARATION: the
        "mod vault_bootstrap;" line at crates/tillandsias-headless/src/main.rs
        lines 79-81, which is itself gated on the vault feature. That is why
        every cfg(not(feature = vault)) item inside vault_bootstrap.rs is
        unreachable dead code, and why trap 2 as filed is refuted.
        CAUSE, worth knowing before the next agent writes evidence: backticks
        inside a double-quoted shell argument are COMMAND SUBSTITUTION. The
        backticked identifier was executed — the shell printed
        "command not found: mod" — and stored as an empty string. Evidence
        containing code identifiers must be single-quoted, or written without
        backticks. The mangled event stays in the ledger because fragments are
        immutable; this note is the repair.

  - packet_id: append-event-cannot-reach-fragment-only-packets
    event:
      type: progress
      ts: "2026-08-12T09:45:59Z"
      host: macos
      summary: >
        FIRST REAL COST OBSERVED, and the packet understates the problem.
        Filed as "append-event refuses a packet that exists only in an
        uncompacted fragment". Today it blocked the REPAIR OF A CORRUPTED
        RECORD: an evidence string for 701-iu9b was mangled by shell command
        substitution, and the correction could not be appended — append-event
        answered "packet_id not found" for a packet that `status` resolves
        fine.
        SHARPER FINDING: the documented workaround does not work either.
        `set-field <id> <field> <same-value> --reason "..."` NO-OPS when the
        value is unchanged ("ok: no-op — ... is already 'implemented'") and
        writes NOTHING — not the LWW row, and not the --reason note event. So
        for a fragment-only packet there is NO CLI PATH AT ALL to record a note
        without also changing a field. The only route left is hand-authoring a
        fragment with an `events:` block, which is what this fragment is.
        That combination — the natural verb refuses, and the fallback silently
        writes nothing — is worse than either alone, and it lands hardest
        exactly when someone is trying to correct a mistake in the record.
        Whoever fixes 699-usxc should fix both halves: make append-event resolve
        against the folded ledger, AND make a no-op set-field still record an
        explicitly supplied --reason (or refuse loudly instead of reporting
        "ok").
