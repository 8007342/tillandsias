# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: squid-permissive-port-and-git-credential-origin-confinement
    event:
      type: completed
      ts: "2026-09-13T04:04:56Z"
      agent_id: linux-lenovinha-opus5-20260913t040456z
      host: linux
      summary: |
        BOTH EXIT CRITERIA MET. Unlike the two rows before it tonight, this one was NOT
        stale — both defects reproduced live and are now closed, pinned by
        scripts/test-credential-and-proxy-confinement.sh (gate step 250-1118-bscs), 9/9.

        CRITERION 2 — the credential helper. It drained stdin and explicitly did not
        branch on it: "wired per-invocation by the relay for one specific remote, so
        answering unconditionally is correct". That is a claim about the CALLER, and a
        credential helper must not rest on one. It now parses `host=` and `protocol=`
        and FAILS CLOSED: unknown host refuses, absent host refuses, non-https refuses.

        THE HOST COMPARISON IS WHOLE-STRING, NOT A SUFFIX, and that is the detail that
        would have made a plausible-looking fix useless: `*github.com` accepts
        `evil-github.com`, and `github.com*` accepts `github.com.attacker.net`. Both are
        trivially registrable and both are pinned as arms. Port suffixes are stripped
        first because git may send `host=h:443`.

        https-only was added beyond the row's letter: the criterion says validate the
        host, but a token handed over cleartext is disclosed in transit regardless of
        who receives it, and git will ask for one if a remote says `protocol=http`.

        CRITERION 1 — the permissive proxy port. `http_port 3129` had no bind address
        (0.0.0.0 in-container) and `http_access allow build_port` grants allowlist-free
        egress with no source ACL. Now `http_port 127.0.0.1:3129`.

        I CHECKED WHETHER THE FILE'S OWN HEADER REFUTED THE PACKET, because it reads as
        if it might: "PROVISIONED BUT NOT ROUTED ... this port listens, bumps, and
        serves nobody", measured 2026-08-30. It does not refute it. That measurement is
        about ROUTING — nothing publishes the port, no HTTP_PROXY names it, no build
        passes it. REACHABILITY is a different question: every enclave container shares
        a network with the proxy and can dial it by container address regardless of what
        the runtime injects. The header is accurate and the hole was real; they answer
        different questions, and a reviewer reading the header would reasonably have
        concluded otherwise. That is this row's own class one level up — configuration
        whose truthful statement about intent reads as evidence about exposure.

        The same measurement is what made the fix cheap and safe: nothing references
        3129, so narrowing the bind breaks no caller that exists today.

        TEETH, measured against the reverted artifacts: 7 of 9 arms FAIL pre-fix,
        including every exfiltration shape. The 2 that pass in both states are exactly
        the preservation arms — github.com over https still works, and the STRICT port
        3128 is still bound for the enclave. Those two pin nothing on their own and are
        not evidence of the fix; they exist so a later "confinement" that narrows both
        listeners, or refuses everything, reds instead of looking green. Said plainly so
        9/9 is not read as nine demonstrations.

        THE EXISTING CLOSURE SCRIPT WAS KEPT, NOT REPLACED.
        scripts/check-proxy-permissive-port-routing.sh (this row's declared closure)
        pins a DOCUMENTATION-CODE agreement — if no consumer references 3129 the header
        must say so, and if one does the caveat must go. That is an adjacent property to
        this row's exit criteria, so the new arms were added beside it rather than
        folded into it. It remains green: ok:proxy-permissive-port:unrouted-and-documented.

        OUT OF SCOPE AND LEFT ALONE, both named in that header: wiring builds through
        the permissive port (order 245 P6's open decision) and the host-port collision
        with `tillandsias-dev-proxy` if this image's 3129 is ever published. Whoever
        takes P6 now inherits a loopback bind, which is the safer default to design from.
