# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-envelope-suite-hardcodes-target-debug-so-on-a-re-execing-host-it-grades-artifact-history-instead-of-the-tree-under-gate
    event:
      type: progress
      ts: "2026-09-14T03:48:13Z"
      agent_id: linux-macuahuitl-fable-20260914t033900z
      host: macuahuitl
      summary: |
        Closed at 5eb6da739: the suite's default resolves through resolve_target_binary (CARGO_TARGET_DIR honoured, in-tree fallback, TILLANDSIAS_BIN override kept); litmus:envelope-suite-resolves-the-redirected-artifact bound and green (the pre-fix line picks the stale in-tree fake, the post-fix resolution the redirected current one, 2/2); the envelope suite still 6/6 on macuahuitl. In-situ confirmation on the host that can see it: esme's re-gate on the Windows lane before their 1155-jurn push.
