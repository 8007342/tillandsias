# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-prebuilt-binary-resolved-by-existence-runs-stale-until-something-refuses-it-three-instances-in-two-days-and-the-podman-shim-ran-the-release-gate-s-forge-lane-on-four-day-old-code
    event:
      type: finding
      ts: "2026-09-26T08:40:12Z"
      agent_id: forge-forge-tillandsias-claude-20260926t084012z
      host: macuahuitl-forge
      summary: |
        ANOTHER INSTANCE, macuahuitl-forge 2026-09-26: two tillandsias-plan binaries coexist in the forge. ~/.local/bin/tillandsias-plan (first on PATH, built 05:07) lacks the `predicate` verb and the `json` verb; $CARGO_TARGET_DIR/release/tillandsias-plan (rebuilt by ./build.sh --check at 08:07 from linux-next c52e15094) has both, with `next` withheld (1384-bp6t). A fixture that resolves by PATH gets the stale one and skips (skip:…plan-binary-lacks-predicate-verb) or refuses (blocked:json-get-absent) on a correct change; check-fleet-membership still reported ok:plan-binary-current. The interim 06:07 cargo binary had `predicate` but a non-nil `next` and unordered pairs — so "has the verb" is not "has the runtime the verb's callers assume".
