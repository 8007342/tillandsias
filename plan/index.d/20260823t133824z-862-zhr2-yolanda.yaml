# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by windows (yolanda), 2026-08-23T13:38:24Z.
packets:
  - order: 862-zhr2
    packet_id: version-counter-flow-vs-guard-ruling
    release_target: fail-loud-diagnosis-milestone
    desired_release: v0.5
    title: "RULING NEEDED: versioning.yaml declares the Build counter flows through branches with LUB merges; the version guard forbids exactly that flow"
    status: pending
    kind: research
    pickup_role: any
    priority: p1
    capability_tags: [versioning, hooks, methodology, decision]
    deliverable: plan/issues/decision-version-counter-tracked-vs-guard-2026-08-23.md
    depends_on: []
    estimated_hours: 1
    context: >
      The decision record at the deliverable states the contradiction once,
      presents two coherent resolutions with measured blast radius, and
      recommends A (untracked local counter; tracked VERSION moves only at
      release). Operator-gated under bar_raise_governance: two prior hosts
      measured the release-path blast radius on 643-64bx and correctly
      declined to guess; this row exists so the choice is one hour of
      operator attention over a prepared record instead of a claiming
      host's guess. Ruling unblocks 643-64bx criteria 1+3 (p1).
    exit_criteria: >
      The Tlatoani records A or B (or an amendment) on this row; 643-64bx
      returns to ready naming the chosen resolution; methodology/
      versioning.yaml and the guard are reconciled by the implementing
      packet so the declared design and the enforced rule agree.
