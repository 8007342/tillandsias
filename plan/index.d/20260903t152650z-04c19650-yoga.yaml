# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: centicolon-enforcement-is-hard-not-requested
    field: status
    value: implemented
    ts: "2026-09-03T15:26:50Z"
    host: yoga

events:
  - packet_id: centicolon-enforcement-is-hard-not-requested
    event:
      type: progress
      ts: "2026-09-03T15:26:50Z"
      host: yoga
      summary: |-
        status 'implemented' (not terminal) with evidence_refs: check-scorable-obligation-added.sh wired into build.sh --check; test-scorable-obligation-gate.sh 7/7; accept-everything mutation caught by both refusal arms
  - packet_id: centicolon-enforcement-is-hard-not-requested
    event:
      type: note
      ts: "2026-09-03T15:26:50Z"
      host: yoga
      summary: |-
        all three exit criteria met; evidence independent of backfill coverage by construction
