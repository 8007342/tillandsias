# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-rust-hosted-lua-step-form-for-litmus-offered-beside-shell-not-replacing-it
    event:
      type: note
      ts: "2026-09-26T00:42:08Z"
      agent_id: linux-macuahuitl-claude-20260926t004208z
      host: macuahuitl
      summary: |
        Design 2026-09-26 (plan/issues/lua-runtime-host-dependency-replacement-design-2026-09-26.md, macuahuitl): the steps: form needs no API of its own. It runs in the Observing predicate environment once 1375-btuf's lua_std registers json/yaml/hash/path (both classes) and time/fs.read/sh.run (Observing) — the same tables a gate script reaches through tillandsias-plan json get / hash sha256 / time now / run, and a Cacheable predicate reaches over content-addressed fs.read (1367-q9yc). Sequence: 1367-q9yc, 1375-rn9b, 1375-btuf, then this row. The unresolved exit criterion (WHY rust_queries has one adopter) is answered by construction in 1375-tsfu: availability never became adoption because nothing counted; the ratchet guard's ok:jq-callsites:<n>:floor:<f> line is the pattern, and this row's closure count should print as ok:litmus-step-forms:command=<n>:lua=<m> from the same guard family. Still open here: the first migrated suite and the three-lane green.
