# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t230500z
events:
  - packet_id: generic-index-deterministic-subset-is-thin
    event:
      type: progress
      ts: "2026-08-10T23:05:00Z"
      agent_id: linux-mutable-20260810t230500z
      host: linux_mutable
      summary: >
        Scope addition from the 669-qcsb litmus work, observed live: the
        generic lane cites README.md lines 1-5 UNCONDITIONALLY, so a README
        shorter than 5 lines makes verify-answer refuse a truthful
        confidence=exact answer ("citation line range runs past end of
        file") — reproduced against a 3-line fixture README. Clamp the
        citation range to the real line count when this packet reworks the
        deterministic subset.
