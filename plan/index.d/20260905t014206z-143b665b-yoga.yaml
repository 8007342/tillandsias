# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: control-wire-ships-unauthenticated-by-default
    field: status
    value: ready
    ts: "2026-09-05T01:42:06Z"
    host: yoga

events:
  - packet_id: control-wire-ships-unauthenticated-by-default
    event:
      type: progress
      ts: "2026-09-05T01:42:06Z"
      host: yoga
      summary: |-
        status 'ready' (not terminal) with evidence_refs: 310051bf2: 3 of 6 converted and verified; ratchet at 3; flip belongs to the last converter
  - packet_id: control-wire-ships-unauthenticated-by-default
    event:
      type: note
      ts: "2026-09-05T01:42:06Z"
      host: yoga
      summary: |-
        released: the three readers this host can compile are converted; the remaining three are cfg-gated to macOS and Windows and are routed to those hosts
