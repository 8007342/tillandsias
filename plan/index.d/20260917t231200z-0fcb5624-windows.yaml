# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: wire-local-experts-mode-through-grounded-pipeline
    event:
      type: progress
      ts: "2026-09-17T23:12:00Z"
      agent_id: windows-yolanda-claude-20260917t231151z
      host: windows
      summary: |-
        TASK 4.5 MSYS HALF: GREEN, and the C toolchain question is answered by artifact rather than by exit code.

        FORCED REBUILD, not a cached green: cargo clean -p mlua-sys removed 302 files / 111.0MiB, then cargo build -p tillandsias-plan --lib exited 0 with BOTH "Compiling mlua-sys v0.6.8" and "Compiling mlua v0.10.5" present in the verbose log. Then cargo test -p tillandsias-plan --lib lua_runtime: 7 passed, 0 failed, 322 filtered, rc=0 -- the same 7/7 the darwin half reported today.

        THE EVIDENCE AN EXIT CODE DOES NOT GIVE YOU. "vendored" builds Lua from C SOURCE, so the real criterion is whether a working C toolchain exists on this platform. 32 Lua .c sources compiled (lapi.c, lauxlib.c, lcode.c, ldo.c, lgc.c, llex.c, lvm.c, ...) into out/lua-build/lib/lua5.4.lib and liblua5.4.a, 2601110 bytes. The objects are x86-64 COFF carrying .drectve and .debug$S sections -- MSVC cl.exe output, not gcc.

        THE HOST DETAIL THAT MATTERS FOR ANYONE REPRODUCING THIS, and it narrows the claim. The shell is MSYS/MINGW64 (MSYSTEM=MINGW64, MINGW64_NT-10.0-26200) but rustc's host triple is x86_64-pc-windows-msvc, so this lane compiles vendored Lua through Visual Studio 2022, NOT through a MinGW gcc. cc, gcc and clang are ALL absent from this shell's PATH and the build still succeeded, because cc-rs discovers cl.exe via vswhere rather than via PATH. So "msys" here names the SHELL, not the C toolchain. A host with the MSYS shell and no Visual Studio is UNTESTED by this run and is the case most likely to fail; do not read this as "any MSYS host builds mlua-vendored".

        TOOLCHAIN: cargo 1.98.0 (797e8a9bc 2026-08-05), rustc 1.98.0 (88d9e12ae 2026-08-18), mlua-sys 0.6.8, mlua 0.10.5, Lua 5.4.

        NOT CLAIMED, because the row misleads on it. Task 4.5 cites 902-5bf9's criterion as its standard and they are two different obligations: 902-5bf9 needs the litmus wiring verified green on linux, darwin AND msys BEFORE IT IS WIRED INTO --check, and the thing to be wired does not exist. Verified on this host rather than accepted on relay: scripts/run-litmus-test.sh is 2390 readable lines with grep -c "steps:" = 0 and grep -ci lua = 0. This msys evidence completes task 4.5's portability question and does NOT advance 902-5bf9.
