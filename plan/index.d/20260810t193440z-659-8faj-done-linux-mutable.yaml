# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t193440z
status:
  - packet_id: mirror-dns-identity-is-shared-across-projects
    field: status
    value: done
    ts: "2026-08-10T19:34:40Z"
    host: linux

events:
  - packet_id: mirror-dns-identity-is-shared-across-projects
    event:
      type: completed
      ts: "2026-08-10T19:34:40Z"
      agent_id: linux-mutable-20260810t193440z
      host: linux
      summary: >
        All three exit criteria met. Each mirror now carries a project-unique
        DNS identity: build_git_run_args assigns ONE per-project alias
        (git-{sanitized-project}, same string as the existing --hostname) and
        the shared git-service / tillandsias-git aliases are no longer
        assigned anywhere. Derivation lives in exactly one function,
        git_mirror_service_identity(project), doc-commented as the single
        swap point for the opaque/salted upgrade the SSH-CA design under
        606-bvnp will decide.

        The plumbing was reused, not invented, per the change-surface
        handover: TILLANDSIAS_GIT_SERVICE now carries the per-project name at
        all three injection sites, the generated gitconfig insteadOf redirect
        and the forge readiness probe derive from the same function, and
        lib-common.sh routes all seven client URL sites through a new
        git_mirror_host() helper (injected env first, git-${TILLANDSIAS_PROJECT}
        fallback for host-mount lanes). clone-fresh (sh + fish) addresses
        git-<requested-project> — with per-project identities, cloning
        project X must reach X's own mirror, not this project's.

        Sequencing: the 12:25Z correction event was honored — NO_PROXY
        (surface B) was treated as tidiness, landed in the same commit as the
        aliases; the retired names are simply removed from all five literals
        with a comment recording why the per-project name is deliberately
        absent (git:// never consults http_proxy).

        Negative fixture encoded AND run: scripts/test-git-mirror-dns-identity.sh
        starts two containers with the product's exact --name/--hostname/
        --network-alias args and asserts via getent ahostsv4 (never nslookup,
        per the audit's instrument note): git-dnsfixa -> 10.89.1.2 only,
        git-dnsfixb -> 10.89.1.3 only, tillandsias-git and git-service both
        NXDOMAIN. PASS. The fixture detects a pre-659 stale mirror still
        holding the shared alias on tillandsias-enclave (this host had one
        live) and measures on a dedicated network instead of failing on
        environment noise. litmus:git-mirror-dns-identity (instant,
        pre-build) pins the code shape and is BOUND under git-mirror-service
        (660-ryhn discipline: verified it EXECUTED, 4/4 steps, suite 17/17).

        Residual, deliberately not closed here: (a) 606-bvnp's Vault
        AppRole/SSH-CA criteria untouched — this removes only criterion 3;
        (b) a HOST-MOUNT lane whose launcher-injected gitconfig is absent
        falls back to git-${TILLANDSIAS_PROJECT} (unsanitized shell
        approximation of the Rust derivation) — exact only for simple project
        names; (c) check-credential-channel still accepts
        git://tillandsias-git/* for pre-rename configs, removable once no
        live forge predates 659-8faj; (d) a RUNNING mirror launched by a
        pre-659 binary keeps its shared alias until relaunched — forges built
        from the new image cannot reach it by the old name until the stack is
        recreated (same-version-recreate caveat as every alias change).
