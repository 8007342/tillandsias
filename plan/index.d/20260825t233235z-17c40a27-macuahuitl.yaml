# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: dev-inference-container-exits-2-mid-workload-with-no-error
    field: next_action
    value: 'Root-cause the exit(2) itself: needs a host that can RUN the inference container under sustained embedding load. The diagnosis half is landed (3807164f4) — the verdict now distinguishes absent/timeout/died-mid-request/unreachable/refused, so whoever takes this gets a typed signal instead of a wrong one.'
    ts: "2026-08-25T23:32:35Z"
    host: macuahuitl
