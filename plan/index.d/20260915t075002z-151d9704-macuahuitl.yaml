# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: a-release-freeze-is-announced-in-ledger-prose-and-enforced-by-nothing-so-the-pre-push-hook-will-push-code-to-a-frozen-branch-and-a-long-gate-makes-the-window-wide
    field: next_action
    value: 'Nothing is left to build. What remains is the LIVE confirmation, which only the next cut can give: the runbook''s `release-freeze.sh set` at gate start should print ok:freeze-set:<ref>, any host attempting a code push to linux-next during that window should be refused with the marker named, and `clear` before the back-merge push should print ok:freeze-cleared:1. Paste those three lines onto this row at the next cut. A refused:freeze:unreachable at set time is a STOP for the cut, not a warning — an origin the cut cannot reach is an origin it cannot publish to. THE OPEN TRADE THE FILING ROW LEFT IS DECIDED, in the hook''s own comment: the check FAILS OPEN on a fetch failure, loudly, because it queries the same remote the push is about to contact seconds later, so failing closed adds no protection in the case it exists for while making every offline push impossible; the residual is the narrow window where ls-remote fails transiently and the push then succeeds, which is bounded and recoverable, and strictly smaller than stranding work on a host that cannot push at all (872-c9nd). Revisit only with a measurement showing that window actually occurring.'
    ts: "2026-09-15T07:50:02Z"
    host: macuahuitl

events:
  - packet_id: a-release-freeze-is-announced-in-ledger-prose-and-enforced-by-nothing-so-the-pre-push-hook-will-push-code-to-a-frozen-branch-and-a-long-gate-makes-the-window-wide
    event:
      type: note
      ts: "2026-09-15T07:50:02Z"
      host: macuahuitl
      summary: |-
        implemented; the live confirmation is the next cut
