# Ledger fragment — append-only, IMMUTABLE once written.
# Hand-written in `set-field` shape: worktree isolation refuses the compound
# CLI invocation, and `closure-evidence-check` validates the result.
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: guards-that-spawn-per-item-go-flaky-under-fleet-load
    field: status
    value: completed
    ts: "2026-08-17T05:47:48Z"
    host: linux_mutable

events:
  - packet_id: guards-that-spawn-per-item-go-flaky-under-fleet-load
    event:
      type: completed
      ts: "2026-08-17T05:47:48Z"
      agent_id: fork-792ksr8-20260817T054748Z
      host: linux_mutable
      summary: >
        SWEEP COMPLETE and the recurrence question ANSWERED; both halves of the
        residual discharged. EVIDENCE — mechanism reproduced deterministically
        with its controls (scripts/test-sigpipe-verdict-pipelines.sh, 11/11):
        a 200k-line producer into `grep -qx` for the FIRST line fails 20/20
        under pipefail despite MATCHING; the same match at the LAST line fails
        0/20; the same early match with pipefail off fails 0/20; the
        recommended here-string rewrite fails 0/20 under identical conditions.
        MEASURED SIZE DISCRIMINATOR on this host, which is what makes the
        classification defensible rather than a guess — 0 false failures below
        ~6 KB of producer output, mixed 8-14 KB, essentially certain above
        ~19 KB. SWEEP — 355 pipelines feed an early-exiting consumer; ~50 sit
        in a verdict context; classified by producer, TWO are EXPLOITABLE and
        both are fixed here (check-windows-tray-diagnose-surface.sh, 4 sites,
        and check-windows-only-sources-verified.sh, 1 site — each piping a
        whole `cargo test` transcript, tens of KB, into `grep -q` under
        pipefail; the dangerous direction, since a false "absent" makes the
        toolchain-blocked and no-test-run detectors MISS and stamp a
        verification that never happened). ~48 are BENIGN on measured
        producer size (17 printf + 5 echo of short vars; `rustup target list
        --installed` = 144 bytes and `git config --get-all <key>` = 18 bytes,
        both measured here; single-line sed/file/nc producers), and were
        deliberately NOT churned. One was ALREADY-FIXED (check-litmus-pin-
        claims.sh, last night). RECURRENCE — a whole-repo gate is REFUSED with
        evidence: producer SIZE decides the defect and is not statically
        decidable, and the sweep proved every proxy leaks both ways — a
        producer-name heuristic flagged four legacy sites whose real output is
        144 and 18 bytes, while MISSING both genuinely exploitable sites
        because their producer is `printf '%s' "$var"`. A corpus-wide gate
        would cry wolf on safe code and miss the real cases, which is the
        741-2izr false-signal failure. Shipped instead as a DIFF-SCOPED gate
        following the 634-39ik precedent (check-sigpipe-verdict-pipelines-
        added.sh, wired into build.sh): the legacy corpus is never scanned so
        there is no standing false-positive burden, new code gets the safe
        idiom for free or records a one-line `# sigpipe-ok: <reason>`
        exemption, and an unavailable base ref SKIPS because this guard only
        ever ADDS enforcement. Pinned by litmus:sigpipe-verdict-pipeline-shape
        (4 behavioural steps incl. a legacy-corpus-not-refused negative
        control), PASS via the canonical runner. MUTATION CONTROL non-vacuous:
        neutering the violation counter reds exactly the added-pipeline case
        and leaves the other six green. The fixture also caught a real
        testability defect in my own gate mid-work — it resolved its root from
        the script's location, so four fixture cases were passing VACUOUSLY
        until a root-override seam was added.
  - packet_id: guards-that-spawn-per-item-go-flaky-under-fleet-load
    event:
      type: note
      ts: "2026-08-17T05:47:48Z"
      agent_id: fork-792ksr8-20260817T054748Z
      host: linux_mutable
      summary: >
        On the deliverable's second clause — "audit for `if ! cmd` idioms that
        conflate 'said no' with 'could not run'". That clause was written from
        the ORIGINAL inferred mechanism (a spawn failing to fork under load),
        which a sibling fork later falsified 0/400 and which the SIGPIPE
        measurement replaced. The distinction still matters in principle, but
        it did not manifest: across the ~50 verdict-context pipelines swept,
        every observed false verdict traced to SIGPIPE-under-pipefail, not to
        an exec failure. Recording that rather than quietly dropping the
        clause — the audit was done, and it came back negative, which is a
        result and not an omission. Residual for a future reader: `grep` exits
        >1 on a genuine ERROR (unreadable file, bad regex) and `if !` still
        cannot distinguish that from a clean no-match. No live instance was
        found, so no packet is filed; if one ever bites, this is the note that
        predicted it.
