# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-macuahuitl-opus5-20260803
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
status:
  - packet_id: main-branch-protection-blocks-v05-release
    field: status
    value: completed
    ts: "2026-08-03T21:30:00Z"
    host: linux-mutable-macuahuitl-opus5-20260803
  - packet_id: github-actions-still-scheduled-from-main
    field: status
    value: completed
    ts: "2026-08-03T21:30:00Z"
    host: linux-mutable-macuahuitl-opus5-20260803
events:
  - packet_id: main-branch-protection-blocks-v05-release
    event:
      type: completed
      ts: "2026-08-03T21:30:00Z"
      agent_id: linux-mutable-macuahuitl-opus5-20260803
      host: linux_mutable
      summary: >
        Operator chose to clear the required contexts rather than re-add cloud CI
        or weaken admin enforcement. Applied:
        required_status_checks.contexts [3 stale names] -> [], strict stays false,
        enforce_admins stays TRUE, the pull-request requirement is UNCHANGED. The
        prior state was captured before the change so it is reversible.
        PROVEN, not assumed: PR #86 (linux-next-independent, cut from main) reached
        mergeable=MERGEABLE, mergeStateStatus=CLEAN with 0 status checks, and
        merged. main is mergeable again and v0.5 is no longer blocked at the
        release path.
        The gate is now ./build.sh --ci-full run locally before the PR. That
        obligation is only durable once local-gate-obligation-missing-from-the-loop
        (598-znuv) is closed — until then it depends on the agent already knowing.
  - packet_id: github-actions-still-scheduled-from-main
    event:
      type: completed
      ts: "2026-08-03T21:30:00Z"
      agent_id: linux-mutable-macuahuitl-opus5-20260803
      host: linux_mutable
      summary: >
        PR #86 removed ci.yml and nix-cache-warm.yml from main. Final state,
        verified with `git ls-tree origin/<branch> -- .github/workflows/` on all
        four branches: main, linux-next, windows-next, and osx-next each carry
        release.yml and nothing else.
        The sibling branches were cleaned by fast-forwarding them to linux-next
        earlier in this cycle (both 0 ahead, verified ancestors via
        `git merge-base --is-ancestor`, so nothing was lost). main was cleaned by
        PR, since it is PR-only.
        The weekly `0 4 * * 0` schedule that fired 2026-08-02T06:35 AFTER the
        original purge can no longer fire: GitHub schedules cron from the default
        branch, and the file is gone from it.
        Residual, deliberately left to 598-u97y's exit criterion rather than
        claimed here: litmus:github-actions-budget still only inspects the
        checkout's own branch. It passed on linux-next throughout the window in
        which main was still scheduling work — it verified where it was written,
        not where it runs. That is the same failure class this project keeps
        naming, and it is now the only thing standing between a future workflow
        re-appearing on main and nobody noticing.
