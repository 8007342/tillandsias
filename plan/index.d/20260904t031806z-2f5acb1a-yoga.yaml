# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: ca-bundle-moves-to-a-durable-directory
    field: next_action
    value: 'Migration detector landed and verified against a real stale bind (scripts/check-ca-bind-migration.sh). THE VALUE IS UNCHANGED and the remaining work is a DECISION, not an edit. The durable location must survive a reboot on a Linux HOST and also be valid INSIDE a macOS guest, because proxy_exec_preamble is exec-d in the guest — one string, two filesystems, consistently meaning "where tillandsias-headless runs". A per-user path ($HOME/.local/state/tillandsias/ca) satisfies both but is a DERIVATION, not the static literal ca-path.txt holds today; a system path (/var/lib/tillandsias/ca) is static but needs privileges this lane lacks. So either ca-path.txt gains a value the readers expand, or ca_path::ca_dir() becomes a function over an env/HOME base with the file holding the leaf — both change the contract 998-qrwu established. DECIDE THAT FIRST. Also open: macbook-macos was asked to confirm the interpolated preamble is accepted by a real guest; 44/44 here only proves the string builds, since the crate is cfg-gated to a stub off Darwin.'
    ts: "2026-09-04T03:18:06Z"
    host: yoga

events:
  - packet_id: ca-bundle-moves-to-a-durable-directory
    event:
      type: progress
      ts: "2026-09-04T03:18:06Z"
      host: yoga
      summary: |-
        status 'Migration detector landed and verified against a real stale bind (scripts/check-ca-bind-migration.sh). THE VALUE IS UNCHANGED and the remaining work is a DECISION, not an edit. The durable location must survive a reboot on a Linux HOST and also be valid INSIDE a macOS guest, because proxy_exec_preamble is exec-d in the guest — one string, two filesystems, consistently meaning "where tillandsias-headless runs". A per-user path ($HOME/.local/state/tillandsias/ca) satisfies both but is a DERIVATION, not the static literal ca-path.txt holds today; a system path (/var/lib/tillandsias/ca) is static but needs privileges this lane lacks. So either ca-path.txt gains a value the readers expand, or ca_path::ca_dir() becomes a function over an env/HOME base with the file holding the leaf — both change the contract 998-qrwu established. DECIDE THAT FIRST. Also open: macbook-macos was asked to confirm the interpolated preamble is accepted by a real guest; 44/44 here only proves the string builds, since the crate is cfg-gated to a stub off Darwin.' (not terminal) with evidence_refs: scripts/check-ca-bind-migration.sh; migrate:ca-bind-stale:1 on a synthetic stale bind
