# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: periodic-deslopification-sweep
    field: next_action
    value: 'BLOCKER WITHDRAWN 2026-08-21 — `plan compact` is safe again. 843-624y landed the coverage check: compaction now consumes ONLY fragments whose records reached the base and REFUSES the rest with a printed reason (fixture scripts/test-compaction-coverage.sh, 10/10, wired into build.sh --check; mutant deletes the unfoldable fragment and reds 7/10). Expect compaction to shrink the base LESS than before — the difference is exactly what the old implementation was destroying. THE ARCHIVE SWEEP IS ALSO UNBLOCKED (4c221e731 made plan/archive answerable; check-archive-answerability.sh proves 204/204 after archiving 664 packets with the ready set unchanged). So the next sweep can run both. STILL TRUE: obsoleted stays exempt from archiving (2026-08-19 ruling c) and the tombstone form is owed before retraction scales. Cadence unchanged: event-counted, >=200 orders, 48h floor, no calendar ceiling; check-deslop-due.sh reports it.'
    ts: "2026-08-21T22:56:55Z"
    host: linux
