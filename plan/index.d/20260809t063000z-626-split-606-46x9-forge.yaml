# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-20260809t063000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# ORDER 626-bnn5 / 626-zmhz: split 606-46x9 (folded-loop-status-active-release-truth)
# into two verifiable children. The forge cycle budget
# (methodology/distributed-work.yaml -> forge_cycle_budget, order 264) cannot fit the
# full ~3h packet in the 600s launch envelope, so the README-exclusion slice (child A,
# implemented this same cycle) is split at a verification boundary from the
# renderer/count-gate work (child B). Shaping is itself a complete reduction step.
events:
  - packet_id: folded-loop-status-active-release-truth
    event:
      type: split
      ts: "2026-08-09T06:30:00Z"
      agent_id: forge-20260809t063000z
      host: forge
      summary: >
        Split the folded-loop-status truth packet into two children at a
        verification boundary. Child A (626-bnn5,
        loop-status-readme-exclusion-from-fragment-parse) closes exit criterion
        3: the committed plan/loop_status.d/README.md is documentation, not a
        fragment — it was reported malformed (malformed=1) on every fold and
        drift run and every loop-status invocation warned it was SKIPPED. Child
        B (626-zmhz, folded-loop-status-renderer-and-count-gate) keeps exit
        criteria 1, 2 and 4: the deterministic ACTIVE RELEASE + v0.5 count
        renderer/check over the folded ledger. Child A was implemented and
        closed in the same cycle; child B is left a ready, dependency-clear
        packet for the next capable host.
status:
  - packet_id: folded-loop-status-active-release-truth
    field: split_into
    value:
      - loop-status-readme-exclusion-from-fragment-parse
      - folded-loop-status-renderer-and-count-gate
    ts: "2026-08-09T06:30:00Z"
    host: forge
packets:
  - packet_id: loop-status-readme-exclusion-from-fragment-parse
    order: 626-bnn5
    status: in_progress
    desired_release: v0.5
    priority: p0
    title: "Exclude loop_status.d/README.md from fragment parse and malformed reporting"
    kind: fix
    parent_order: 606-46x9
    depends_on: []
    pickup_role: forge
    capability_tags:
      - plan
      - loop-status
      - fragments
      - testing
    estimated_hours: 1
    deliverable: crates/tillandsias-plan/src/loop_status.rs
    outcome: >
      plan/loop_status.d/README.md is the store's committed documentation, not a
      cycle-status fragment. The prose overlay reads .md fragments, so the README
      was parsed on every fold and drift report and surfaced as a malformed
      fragment (malformed=1, 'SKIPPED' warning on loop-status) even though
      nothing was wrong. Exclude it by name from load_all and malformed so the
      store doc never masquerades as a fragment, while a genuinely malformed
      cycle-status file still surfaces.
    next_action: >
      Rebuild the crate and confirm loop-status-fragments reports malformed=0 and
      loop-status folds without the README warning on the real ledger.
    exit_criteria:
      - "plan/loop_status.d/README.md is never folded into the loop_status view"
      - "plan/loop_status.d/README.md is never reported malformed by loop-status-fragments"
      - "a genuinely malformed cycle-status fragment still surfaces as malformed"
      - "cargo test -p tillandsias-plan --lib loop_status is green and litmus:loop-status-fragment-overlay passes"
    events:
      - type: filed
        ts: "2026-08-09T06:30:00Z"
        agent_id: forge-20260809t063000z
        host: forge
        summary: >
          Shaping child of 606-46x9. The live defect was reproduced: every
          tillandsias-plan loop-status / loop-status-fragments run warned
          'plan/loop_status.d/README.md is malformed and was SKIPPED'. This is
          the smallest verifiable slice of the parent and was implemented in the
          same forge cycle.
  - packet_id: folded-loop-status-renderer-and-count-gate
    order: 626-zmhz
    status: ready
    desired_release: v0.5
    priority: p0
    title: "Deterministic folded-loop-status renderer/check for ACTIVE RELEASE and v0.5 counts"
    kind: plan-tooling+standing-gate
    parent_order: 606-46x9
    depends_on:
      - loop-status-readme-exclusion-from-fragment-parse
    pickup_role: any
    capability_tags:
      - plan
      - release
      - loop-status
      - fragments
      - metrics
      - testing
    estimated_hours: 2
    outcome: >
      Every orchestration cycle reports one unambiguous active release and v0.5
      totals/status counts derived from the FOLDED ledger (base plus fragments),
      never hand-maintained prose. Stale numbers fail before they can guide
      packet selection.
    next_action: >
      Pin today's folded v0.5 count (254 total, 188 nonterminal) against the
      stale 87/94 loop_status.md text; build a deterministic renderer/check over
      the folded ledger; add the fixture proving a new packet fragment changes
      the rendered count and stale output fails.
    exit_criteria:
      - "loop status has exactly one ACTIVE RELEASE declaration and no contradictory active-release prose"
      - "v0.5 totals/status counts match tillandsias-plan's folded ledger on every coordinator cycle"
      - "a fixture adding a packet fragment changes the rendered count and makes stale output fail"
    events:
      - type: filed
        ts: "2026-08-09T06:30:00Z"
        agent_id: forge-20260809t063000z
        host: forge
        summary: >
          Shaping child of 606-46x9 holding exit criteria 1, 2 and 4 (the
          deterministic ACTIVE RELEASE + v0.5 count renderer/check). Split at a
          verification boundary from the README-exclusion slice (626-bnn5) so
          the forge cycle budget could close one criterion now and leave the
          rest as a ready, dependency-clear packet.
