# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-esmeralda-claude-fable-20260808t235000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
events:
  - packet_id: low-end-no-local-inference-gate
    type: progress
    ts: "2026-08-09T08:30:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      Lane-launch exit criterion VERIFIED on Esmeralda: operator's first
      opencode forge launch + one relaunch with
      TILLANDSIAS_NO_LOCAL_INFERENCE=1 — podman ps sampled every 60-90s
      across the whole bring-up; tillandsias-tillandsias-forge reached Up
      with ZERO tillandsias-inference containers ever created. GAP FOUND in
      the same run: the inference IMAGE still gets built ("building missing
      image inference (localhost/tillandsias-inference:v0.4.260809.2)" in
      the operator's terminal) — the gate covers container start sites and
      the readiness wait but not the lane's ensure-images list, wasting
      minutes of N100 CPU per version bump. next_action: gate the image
      ensure/build list too, then mark completed.
  - packet_id: first-run-long-work-is-silent-across-surfaces
    type: note
    ts: "2026-08-09T08:32:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      Third silent-surface instance, same field session: after "cloud: clone
      complete", the forge's clone-from-mirror retry loop printed eleven bare
      "Cloning into ... / warning: You appear to have cloned an empty
      repository." pairs across the multi-minute mirror-seed window (104MB
      seed into /srv/git/tillandsias on the slow NVMe), with no indication
      of what was being waited on; the operator read it as a failure and
      relaunched. Also the readiness family the 626 report's F4 asked to
      fold in: the order-452 mirror-ready gate apparently passed while the
      mirror still served an empty repo (refs landing, HEAD unset?) — the
      empty-clone loop then ran in the forge entrypoint. Retry surfaces need
      to state what they await + attempt count; the mirror gate needs an
      emptiness check. Mirror verified fully seeded afterward (complete ref
      set served) and the operator's relaunch proceeded normally.
  - packet_id: wsl-github-login-vault-misroute
    type: note
    ts: "2026-08-09T08:34:00Z"
    agent_id: windows-esmeralda-claude-fable-20260808t235000z
    host: windows
    summary: >
      Housekeeping observation for the login flow's owner: two
      tillandsias-github-login-<pid> helper containers remained Up 50-60+
      minutes after the login completed successfully on Esmeralda. If these
      are one-shot helpers they are not being reaped; on low-end hosts each
      lingering container costs memory. Triage whether lifetime is intended
      (e.g. status-probe reuse) before filing a fix.
