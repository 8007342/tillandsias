# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: builder-container-serial-killer
    event:
      type: note
      ts: "2026-09-03T04:00:00Z"
      host: macuahuitl
      summary: >
        SCOPE WIDENS: the whole ENCLAVE STACK went down while the host was
        idle (operator away) — vault/proxy/inference Exited 137 with
        oom=FALSE (62Gi host, 56Gi free — not memory), router/website-forge/
        git-mirror --rm-removed, the tillandsias.org-forge-claude session
        now OFFLINE, website unreachable (curl 000). Builder toolbox and
        tray survived. NEW vs prior sightings: earlier kills hit only the
        builder; this took the shared stack, AND there is NO 'container
        kill' entry in the user journal for it (prior mystery kills logged
        one via a fresh podman CLI) — a different mechanism or a rolled
        journal. Not investigated to root this turn (operator returning,
        reboot imminent which moots the live state); flagged as widened
        scope for the forensics plan. COORDINATOR HONESTY NOTE: told the
        operator ~02:13Z 'website serving live, keep it running' without
        re-verifying the serve — the stack may already have been down; the
        assertion outran the instrument (the re-read-on-confirmation lesson
        landing on the coordinator). No data lost: website agent
        checkpointed+pushed, ledger clean, fixes installed awaiting reboot.
