# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: an-igpu-beside-a-discrete-card-reads-unusable-when-it-is-deliberately-unscheduled
    event:
      type: note
      ts: "2026-09-04T09:49:35Z"
      agent_id: linux-macuahuitl-claude-20260904t094935z
      host: macuahuitl
      summary: |
        DISCRIMINATOR AND RESIDENCY CLAUSE, from lenovinha 2026-09-04 before claiming this row. The classifier must key on "a discrete GPU is PRESENT AND SCHEDULABLE on this host", not on tier == gpu-cuda: a gpu-cuda host whose CDI spec is missing is downgraded to cpu by effective_inference_tier(), and on THAT host the iGPU is the only accelerator and must not read present-unscheduled — it has just become the best lane. Classifying off the tier alone would mark the Vega unscheduled on exactly the host where it should place. The two-host fixture arm catches it if one arm is a gpu-cuda host with CDI absent. Numbers for the reason string, as measured: decode qwen2.5:0.5b Vega 80.6/81.3 tok/s vs NVIDIA 346.8/395.3 (4.3-4.9x); embed nomic-embed-text, 40 chunks, Vega 36.2 ms/chunk vs NVIDIA 11.8 (3.1x); both models size_vram == size on the Vega, so the losing arm was FULLY RESIDENT and the claim is "slower", not "slower from a partial offload", which would be a different claim. lenovinha claims this row at its :05 fire. Relayed by the coordinator.
