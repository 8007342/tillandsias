# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: plan-archiver-check-is-thirty-percent-of-every-gate
    event:
      type: progress
      ts: "2026-09-02T07:29:00Z"
      host: macuahuitl
      summary: >
        MEASUREMENT RUNG DONE on a native-filesystem host (macuahuitl, btrfs on
        NVMe, host-native toolchain, Fedora 44): the archiver ready-set check
        (831-ezea) cost 16.7-19.6s of a 105-140s --check with 211 fragments
        overlaying a base of ~620 packets; after this cycle's sweep (149
        packets archived) and compaction (211 fragments folded) it costs 6.8s
        of an 82s gate. Same tree, same host, same toolchain — the only
        variables were fragment count and base size, so the cost is
        LEDGER-BOUND, as the closure asked: the check archives every completed
        packet into a temp copy (892 in the run that measured it) and grades
        273 answerability questions, both proportional to the ledger. The
        fragment-backlog advisory (941-trcf) names the other half: every heavy
        plan load re-overlays all fragments, ~88 loads per gate. The 105s host
        (WSL, /mnt/c) pays the same shape 3x over. NEXT RUNG (not taken;
        released to ready): make the duration stop scaling on an UNCHANGED
        plan/ — cache the check's verdict by a content hash of plan/index.yaml
        + plan/archive + plan/index.d, and re-run only when it moves; the gate
        memo (765-tkq2) already skips whole gates on an unchanged tree, but
        plan/ changes every cycle by construction, so the cache has to be
        scoped to the ledger, not the tree. evidence_refs: gate logs from
        commits 041f610c1/7f501806a (before) and ea2b7b5c1 (after) on this
        host, TILLANDSIAS_GATE_PROFILE=1; compaction 1fb8e142b.
  - packet_id: cross-host-measurement-practice-lives-only-in-transcripts
    event:
      type: completed
      ts: "2026-09-02T07:29:00Z"
      host: macuahuitl
      summary: >
        DONE (ea2b7b5c1). The four rules ratified 2026-08-25, the 886-yizb
        review rule and probe protocol v1 live in methodology/verification.yaml
        under measurement_practice, each carrying the error that earned it;
        mechanical rules name their enforcer, judgment rules say they are
        judgment. Rule 1 is enforced at the source: scripts/cycle-metrics.sh
        emits build_check_mix=mixed:forced=N,memoised=M whenever the timing
        log holds any memoised gate run and omits it only when every run was
        forced (the mean already excluded memoised records — the label is
        what a reader lacked). On this host's real log it reads
        mixed:forced=345,memoised=50. evidence_refs:
        scripts/test-cycle-metrics-mix-label.sh "PASS: build_check_mix label
        2/2 (890-nkdz)" — RED against the previous reporter (no label on a
        mixed log), green after; litmus:cycle-metrics-answer-rate-shape
        Status PASS through the runner (scripts/litmus-run-one.sh) with the
        new leg; rule 2's enforcer scripts/check-bash-dialect.sh already
        wired into --check. The methodology-presence step was NOT added as a
        litmus leg: the 634-39ik gate refused it as an expression pin with no
        negative control, and it was right — the property is the behaviour,
        which the fixture asserts.
  - packet_id: ghost-trace-gate-is-blind-to-yaml-and-markdown-annotations
    event:
      type: completed
      ts: "2026-09-02T07:29:00Z"
      host: macuahuitl
      summary: >
        DONE (ea2b7b5c1, second rung; yaml rung lenovinha 2026-08-24).
        scripts/trace-coverage.sh now scans markdown with the same positional
        rule as yaml (an annotation begins its line, optionally inside an HTML
        comment opener; mid-line is prose); plan/ (ledger data) and
        openspec/changes/ (proposal prose naming its own future specs — 14
        of the 34 markdown ghost ids vanish when it is excluded) are out of
        scope by design. 1,092 anchored markdown annotations across 193 ids
        entered the gate; 14 ids with no spec directory and no change
        directory entered the ratchet baseline exactly once, reason recorded,
        may-only-shrink from here. evidence_refs: scripts/trace-coverage.sh
        --gate "ok:ghost-trace-gate baseline=18 current=18";
        "trace_coverage: specs=202 traced=184 ghost=18 annotations=4963
        files=1585" (annotations were ~1876 before the yaml rung, ~3871
        before this one); ./build.sh --check green (82s). Finding filed
        alongside: two clickable-trace-index litmus tests fail for
        pre-existing reasons (a retired generator; a live-stack precondition)
        — plan/issues/enhancement-clickable-trace-index-litmus-pins-retired-
        generator-2026-09-02.md.
