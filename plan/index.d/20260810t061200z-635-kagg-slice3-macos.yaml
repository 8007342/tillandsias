# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

events:
  - packet_id: macos-forge-bash-lane-wedges-over-exec-wire
    event:
      type: progress
      ts: "2026-08-10T06:12:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Slice 3 — ROOT CAUSE FIXED for the bring-up wedge (commit
        cee23fb9): the first inference readiness probe (`podman exec -i
        ... curl`) never returned — podman's conmon attach hung on the
        launcher's null stdin. All six no-stdin launcher execs dropped
        -i; pinned by bringup_execs_never_attach_stdin. Verified live:
        bring-up now runs inference wait -> 'git mirror seeded on attempt
        1/1200' -> maintenance container launch in ~20s and fails LOUDLY
        on real errors (stale-name collision produced a clean status-125
        refusal with guidance). REMAINING on this packet, narrower: the
        attached foreground `podman run` of the maintenance lane emits no
        output over the exec-wire PTY (run -i attach negotiation with the
        vsock PTY suspected; unlike the probes, a shell lane genuinely
        needs stdin, so the fix differs — candidates: pre-create +
        start/attach split, or conmon attach over a socketpair instead of
        the inherited PTY). The TRAY lane is unaffected (operator's
        BigPickle cycle completed through it). Cleanup breadcrumb for
        whoever continues: the lane container is named
        tillandsias-<project>-forge-maintenance (NOT -forge-terminal);
        stale ones from wedged runs must be podman rm -f'd or the launch
        refuses on name collision.
