# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: macos-terminal-attach-renders-corrupt-from-launch
    field: next_action
    value: 'D3 AND D4 CLOSED 2026-09-01 (lenovinha, linux lane), each falsified by reverting its own fix per the fifth criterion. D3: build_forge_agent_run_args_with_vault now pins TERM/COLORTERM for every interactive lane — the 2026-07-27 OpenCode fix had been applied to one caller of a shared builder and never carried to the other four. Test asserts all five modes, mutation-tested, plus a negative control that a prompt-driven Codex run claims no TTY and therefore pins no terminal identity. D4: the packet named entrypoint-forge-claude.sh; FIVE entrypoints carried it (claude, codex, opencode, opencode-web, antigravity — ten sites), all now 2>&1, guarded by scripts/check-backgrounded-jobs-redirect-stderr.sh in build.sh --check with a scope control so foreground fd1-only redirects are not flagged. REMAINING, both best taken by the macOS lane: D1 — the attach client never clears the screen or homes the cursor; the fix is ED (\e[2J) + CUP (\e[H) added to MODE_RESET in crates/tillandsias-host-shell/src/pty/attach_client.rs, which is CROSS-HOST SHARED scope so any host may write it, BUT the criterion verifies by launching a lane and confirming the session begins at row 1 with no banner above it, which needs a real macOS Terminal.app window. A linux host can make the edit and cannot honestly close the criterion. D2 — the 10s attach-client wait births the PTY at a 24x80 fallback a real window contradicts; action_host.rs is macOS-owned, and the packet already records the distinguishing signature (D2 self-corrects on the first window resize, D1 does not) plus the required negative control (a genuinely headless launch with no attach client must still succeed rather than hang). DO NOT re-derive the three REFUTED hypotheses recorded in the context — PtyOpen is not hardcoded 24x80 on the interactive path, TERM is not dumb there, and raw mode is established before any guest byte. The packet says the refutations are the expensive part; they are still valid.'
    ts: "2026-09-01T01:00:52Z"
    host: linux

events:
  - packet_id: macos-terminal-attach-renders-corrupt-from-launch
    event:
      type: note
      ts: "2026-09-01T01:00:52Z"
      host: linux
      summary: >
        D3/D4 landed and falsified; D1/D2 scoped to the macOS lane with the reason each needs it
