# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-macuahuitl-opus5-20260803
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
status:
  - packet_id: mcp-and-expert-read-paths-use-the-fragment-overlay
    field: status
    value: completed
    ts: "2026-08-03T01:18:00Z"
    host: linux-mutable-macuahuitl-opus5-20260803
events:
  - packet_id: mcp-and-expert-read-paths-use-the-fragment-overlay
    event:
      type: completed
      ts: "2026-08-03T01:18:00Z"
      agent_id: linux-mutable-macuahuitl-opus5-20260803
      host: linux_mutable
      summary: >
        Every content reader of plan/index.yaml is now overlay-aware or
        base-only-with-reason, and the two base-only readers were rerouted
        through the compiled CLI. Exit criterion 1 — the enumeration, with
        verdicts:
        OVERLAY-AWARE (route through the compiled tillandsias-plan CLI, which
        folds base + plan/index.d/): forge-plan.sh (wraps the CLI; ORDER 569
        capability_gap refuses pre-569 binaries); the crate itself
        (main/lib/fragments/loop_status/answer/groundtruth — Ledger loads base
        plus fragments).
        REROUTED THIS PACKET, from base-only: project-info.sh plan_query (was
        yq+jq direct read; now resolves the CLI via $TILLANDSIAS_PLAN_BIN /
        $HOME/.local/bin / /usr/local/bin / /usr/bin / cargo target/release and
        runs `query --status/--role/--tag(s)/--limit/--json`, returning the
        same 9-key projection); scripts/drain-queue.sh extract_ready_packets
        (was awk; now `query --status ready --limit 0` — the explicit limit-0
        is load-bearing, a default 20 would silently truncate the queue).
        BASE-ONLY-WITH-REASON: scripts/check-plan-schema-divergence.sh (reads
        only the default_status_values schema vocabulary, which fragments never
        carry); crates/tillandsias-policy plan-orders (legacy duplicate-order
        gate, superseded by the fragment-aware `tillandsias-plan check`; no
        live caller).
        NOT READERS (existence check / comments / @trace / legacy-dead):
        post-commit-expert-refresh.sh, check-committable-branch.sh,
        claim-ledger-node.sh, container-teardown-straggler-probe.sh,
        test-pre-receive-yaml-gate.sh, headless/tests/common/mod.rs,
        archive-plan-packets.rb (dead in forge); Observatorium reads no ledger.

        Exit criterion 2 — a fragment-only packet is visible through the MCP
        tools, proven by querying, not by reading code: 582-26mm itself exists
        ONLY in plan/index.d/ (0 occurrences in the base). `query --status
        ready --role linux --tag crdt --json` returns it; drain-queue --dry-run
        --tag crdt lists it; verified end-to-end through the rebuilt binary.

        Exit criterion 3 — a litmus fails if a new direct reader appears:
        litmus:no-unprotected-plan-ledger-readers (8 steps) is bound to
        methodology-accountability. The negative control writes a probe reader
        containing `yq` into scripts/ and asserts scripts/check-plan-ledger-
        readers.sh exits non-zero naming it. The behavioural steps build a tmp
        fixture (empty base + a fragment-only packet) and assert query --json
        sees it while a base-only yq read does not. 12/12 methodology-
        accountability litmus green through the runner; ./build.sh --check
        green (cargo fmt + clippy fixed); full workspace test suite green.
        Query filter-semantics unit test green. Packet 582-26mm.
