# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: expire-claims-ages-a-claim-by-the-newest-event-on-the-row-and-attributes-it-to-that-events-host-so-a-fresh-claim-reads-as-two-weeks-old-and-belongs-to-the-wrong-host
    field: verifiable_closure
    value: 'litmus:expire-claims-reads-the-status-write (scripts/test-expire-claims-reads-the-status-write.sh, pre-build, size instant, 5/5): on scratch ledgers driven through --index at a FIXED --now-epoch, a recent status write beside a two-week-old event is NOT an expire candidate (arm 1); a genuinely old claim IS listed and is attributed to the host that WROTE the status rather than the host of the newest event (arm 2); the deliberate event fallback for a base-born in_progress, which carries no claimant, still ages and attributes the row (arm 3); and a fresh claim whose host matches no event is neither expired nor unknown-age, because setting a claim is itself activity (arm 4). Arms 1 and 2 are the behavioural discriminators and fail on the pre-fix code by OUTPUT rather than by source text, so the fixture needs no mutant; arm 5 is labelled traceability and explicitly not a control. The litmus also pins that both readers of one claim — live_claims and expire_claim_candidates — consult status_lease_of, which is the disagreement that caused this.'
    ts: "2026-09-15T11:43:24Z"
    host: macuahuitl

events:
  - packet_id: expire-claims-ages-a-claim-by-the-newest-event-on-the-row-and-attributes-it-to-that-events-host-so-a-fresh-claim-reads-as-two-weeks-old-and-belongs-to-the-wrong-host
    event:
      type: note
      ts: "2026-09-15T11:43:24Z"
      host: macuahuitl
      summary: |-
        discharged the unscoreable into the litmus that now exists (1136-n8sh shape)
