# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-22T03:00:00Z.
#
# macuahuitl's contribution to the refusal-threshold fleet decision, plus the
# row the test-known-red seed has owed since it was written.
packets:
  - order: 845-7a88
    packet_id: proxy-cache-policy-bumped-origin-tls-test-is-the-standing-known-red
    desired_release: v0.5
    title: "proxy_cache_policy::bumped_origin_tls_and_signed_url_logs_fail_closed is Linux's only standing test failure and has no filed issue"
    status: ready
    kind: fix
    pickup_role: linux
    priority: p3
    capability_tags: [testing, proxy, tls, known-red, gate]
    deliverable: crates/tillandsias-headless/tests/proxy_cache_policy.rs
    depends_on: []
    context: >
      THE ENTRY EXISTS BECAUSE THE PRECEDENT DEMANDS ONE. scripts/
      test-known-red.txt seeds exactly one tolerated failure, and the file it is
      modelled on — scripts/windows-only-known-red.txt — states the rule in its
      own header: "Each entry needs a filed issue; an unexplained name here is
      how a stamp stops meaning anything." The seed was written with an in-line
      admission that no issue existed, which is honest but is not a filed issue.
      The agent that added it flagged this against itself. This row closes that.

      THE FAILURE. `cargo test -p tillandsias-headless --test proxy_cache_policy`
      -> bumped_origin_tls_and_signed_url_logs_fail_closed FAILED, panicking at
      tests/proxy_cache_policy.rs:112. Measured red on linux-next at 81d2315f5
      (2026-08-20), again at f9ee195a9 (2026-08-21), and it survived the archive
      sweep revert, so it is independent of that cycle's work.

      WHY IT MATTERS MORE THAN A SINGLE RED TEST. Until 28553c3db this one
      failure returned 101 from `_run cargo test --workspace --no-fail-fast`,
      `set -e` aborted the whole --test dispatch, and the feature-gated pass
      (546 tests, tray + listen-vsock) NEVER RAN. One un-triaged failure was
      silently costing the fleet an entire test lane. The ratchet made --test
      green again by tolerating it; that is a stopgap, and this row is the
      un-stopgapping.

      Linux's pre-existing failure set under --no-fail-fast is EXACTLY ONE. Any
      second entry in test-known-red.txt should be treated as a regression until
      proven otherwise.
    exit_criteria:
      - "The test passes, or it is deleted with a recorded reason why the property it asserted is no longer worth asserting. `bumped origin TLS and signed-url logs fail closed` is a security-shaped property, so deletion needs an argument, not a shrug."
      - "scripts/test-known-red.txt has ZERO entries afterwards, and the ratchet's stale-entry direction proves it: a listed test that now passes must red the gate, so removing the fix without removing the entry cannot pass silently."
      - "If it cannot be fixed now, this row's number replaces the `NEEDS A FILED ISSUE` line in test-known-red.txt so the entry cites its issue as the precedent requires."

status:
  - packet_id: proxy-cache-policy-bumped-origin-tls-test-is-the-standing-known-red
    field: status
    value: ready
    ts: "2026-08-22T03:00:00Z"
    host: linux_mutable

events:
  - packet_id: spec-expert-refusal-threshold-fleet-decision
    event:
      type: progress
      ts: "2026-08-22T03:00:00Z"
      agent_id: linux-macuahuitl-opus5-20260822T024300Z
      host: linux_mutable
      summary: |
        macuahuitl REPORTS ITS MEASURED BANDS, which is what this packet asks of
        each host kind before a number is fixed.

        HOST: macuahuitl, linux_mutable, bare metal Fedora. Corpus: 172 specs
        plus cheatsheets/methodology/scripts/code, index 198 MB (chunks.jsonl +
        vectors.jsonl). Embedder: nomic-embed-text via the local ollama at
        /v1/embeddings. Retrieval: `spec-retrieve --k 1`, top-1 cosine.

        THREE BANDS, not two, and the third is the one that decides the number:

          IN-CORPUS        0.7196 - 0.9102   (6 questions, real spec subjects)
          NEAR-MISS        0.5821 - 0.6889   (6 plausible infra questions this
                                              corpus has no spec for)
          FAR-FIELD        0.4756 - 0.5647   (6 unrelated: cooking, astronomy,
                                              football, veterinary, music,
                                              metallurgy)

        MARGIN AGAINST FAR-FIELD: 0.7196 - 0.5647 = 0.155. Comfortable, and
        MISLEADING — it measures the case nobody was worried about.

        MARGIN AGAINST NEAR-MISS: 0.7196 - 0.6889 = 0.031.

        THAT INDEPENDENTLY CORROBORATES THIS PACKET'S 0.027, on a different host
        with a different corpus build. Two measurements, one from the packet and
        one from here, land within 0.004 of each other on the hard case. The
        narrow margin is real and is not an artefact of one host.

        SO A SINGLE GLOBAL THRESHOLD IS VIABLE BUT TIGHT: anything in
        0.69 - 0.72 separates in-corpus from near-miss today, with ~3 points of
        headroom. Recommend 0.70 if a number must be chosen, and recommend
        AGAINST treating that as settled without at least one more host kind,
        because 3 points is inside the range a corpus rebuild could move.

        THE WORST NEAR-MISS IS WORTH NAMING: "what is the rollback procedure for
        a failed Helm chart upgrade" retrieved
        cheatsheets/concurrent-git/git-mirror-managed-alternatives.md at 0.6889.
        A question about Helm, confidently answered from a git-mirror
        cheatsheet. That single point is what sets the whole margin.

        AND A FINDING A THRESHOLD CANNOT FIX, which the fleet decision should
        not be read as covering. One IN-CORPUS question — "how are agent
        cheatsheets staged into the image" — retrieved
        cheatsheets/languages/html.md at 0.7669. That is ABOVE the in-corpus
        floor, so no refusal threshold would have caught it: the corpus does
        contain the answer and retrieval returned the wrong chunk with high
        confidence. The threshold addresses OUT-OF-CORPUS refusal (821-73es); it
        does nothing for IN-CORPUS mis-retrieval, and the two failures look
        identical to a caller reading `confidence=retrieved`. Worth a separate
        row if it recurs; recorded here rather than filed, per the routing rule.
  - packet_id: dev-host-spec-rag-index-never-built
    event:
      type: note
      ts: "2026-08-22T03:01:00Z"
      agent_id: linux-macuahuitl-opus5-20260822T024300Z
      host: linux_mutable
      summary: |
        THIS PACKET'S PREMISE IS STALE ON macuahuitl — the index EXISTS. And the
        thing that made it look absent is worth more than the correction.

        MEASURED: the index is a podman volume at
        .../volumes/tillandsias-spec-index-tillandsias/_data/8f7c918a...,
        198 MB, carrying chunks.jsonl and vectors.jsonl, and it serves — top-1
        retrieval returns real spec chunks with plausible cosines (see the
        824-6qxh event in this fragment for the full band measurement).

        WHY IT LOOKED MISSING: `spec-index-ensure.sh --where` reported
          spec-index:serving=/mnt/c/Users/bullo/claudia/tillandsias/target/spec-index
        A WINDOWS PATH, on a Fedora host, naming a directory that does not
        exist. Cause: FORGE_SPEC_INDEX_DIR is exported into THIS AGENT SESSION
        with a stale value. That is 789-nc2s exactly, and that packet is
        `completed` — it removed the value from the tracked config in 2b1f8d188
        — but its own note says every session started before that commit keeps
        the stale value until it restarts. This session is one of them.
        `env -u FORGE_SPEC_INDEX_DIR` resolves correctly to the local volume.

        THE GAP THAT REMAINS, and it is not 789-nc2s's: the BUILD path refuses
        an unwritable destination loudly (`blocked:spec-index:
        destination-unwritable`, with a message that names FORGE_SPEC_INDEX_DIR
        and 789-nc2s). The QUERY path does not. `--where` printed a confident
        `serving=` line for a path that does not exist, with no warning and exit
        0 — so a reader asking "where is the index" gets an answer that is wrong
        in the one way that matters. Same shape as every other "reports a
        verdict it could not compute" instance this week: `--where` should stat
        the path it names and say so when it is absent.

        RECOMMEND: re-scope this packet from "never built" to that `--where`
        gap, or close it and file the gap, once someone confirms the index also
        exists on the OTHER dev hosts — this event establishes it for macuahuitl
        only, and the packet is fleet-shaped.
