# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-guest-provisioning-script-can-abort-partway-while-cloud-init-reports-success
    event:
      type: progress
      ts: "2026-09-05T04:56:05Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260905t004510z
      host: tlatoanis-macbook-air
      summary: |-
        MY FIX IS DELIVERED AND WORKING, AND IT DID NOT FIX THE SYMPTOM. Recording that against my own packet rather than leaving it implied. MEASURED after rebuilding and booting with the fix: the instance-id now carries the user-data hash — 'tillandsias-vm-secure-off-20a3335d...-e0356c5584a1687a' — so the id DOES move with the provisioning script, which is what the packet's fix set out to do. The instance dir for that new id EXISTS, the config_scripts_user semaphore is PRESENT, the delivered user-data.txt.i CARRIES the new line, and cloud-init finished cleanly. AND /usr/local/lib/tillandsias/fetch-headless.sh IS STILL FROM 01:03:59 WITHOUT THE LINE. So the per-instance-semantics hypothesis is FALSIFIED: the id changed, cloud-init recorded running the user script for it, and the write still did not happen. THE SURVIVING LEAD, and the strongest signal I have: modules:final — the stage that runs scripts-user — COMPLETES IN 0.01 SECONDS. 'running modules:final at 04:55:00. Up 4.59 seconds' then 'finished ... Up 4.60 seconds', and again 5.38 -> 5.39 on the next boot. A script that writes a dozen files and installs systemd units cannot run in ten milliseconds. The user script is not executing at all, or is aborting almost immediately, which returns to the original set -euo pipefail candidate I had demoted. WHAT THIS MEANS FOR THE PACKET: the instance-id fix removes a REAL and separate staleness hazard — a host-side script change genuinely could not move the id before, and now does — so it should stay. But the packet's title claims provisioning aborts partway while cloud-init reports success, and THAT claim is still unproven in its mechanism and is now the whole remaining question. I am not closing it on a fix that demonstrably does not change the observed behaviour. NEXT STEP for whoever continues: instrument the user script's own first line (a marker written before anything can fail) and re-boot; if the marker is absent, scripts-user never invokes it and the fault is in delivery or module config, not in the script's body.
