# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: build-sh-collapses-a-could-not-run-exit-into-a-fragment-events-violation
    event:
      type: progress
      ts: "2026-09-04T16:26:31Z"
      agent_id: forge-forge-tillandsias-opencode-20260904t162631z
      host: forge
      summary: |-
        IMPLEMENTED + isolated-green in a forge. build.sh's check-fragment-events-land call site no longer collapses the script's could-not-run exit 2 into the exit-1 violation text: it captures _events_land_rc and names the exit-2 case with the script's own token + remedy (cycle-preflight) before the violation branch (follows the archiver's 923-ws3r exit-code discipline / 965-sxec 'never assert what a check that could not run would have found'). New fixture scripts/test-fragment-events-verdict.sh drives the real check with an unresolvable TILLANDSIAS_PLAN_BIN -> asserts exit 2 + blocked:fragment-events-land:no-fresh-plan-binary token + NO substantive orphans claim (4/4), and greps build.sh for the exit-2 routing + cycle-preflight remedy; wired into the gate after the call site. bash -n build.sh ok; fixture PASS 4/4; the fragment-events step PASSED in the full ./build.sh --check run. NOT closing: exit criterion 4 (esmeraldinha's Windows distro binary-freshness) needs a Windows host; and the trunk at HEAD is RED for two PRE-EXISTING unrelated failures this packet does not own (see findings): tillandsias-headless proxy_cache_policy::bumped_origin_tls (entrypoint now launches via squid-supervisor, commit 7292b097b 767-es4w, test still asserts literal 'exec squid -N') and pre-receive stale-base revert guard 3/6 (1001-i5ux's receiving-half fixture red on clean base).
