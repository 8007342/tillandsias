# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: an-idiomatic-command-executor-in-rust-argv-not-shell-strings-with-separately-captured-fds-concurrent-draining-and-a-run-identity-so-a-stale-marker-cannot-read-as-a-fresh-result
    field: next_action
    value: 'LEFT (2026-09-23): needs a ruling: either accept the Rust tests as the closure (poc rewritten by the test) or add poc/01-demonstrate-inversion.sh and assert the shell rc=141 5/5.'
    ts: "2026-09-23T03:48:50Z"
    host: macuahuitl
