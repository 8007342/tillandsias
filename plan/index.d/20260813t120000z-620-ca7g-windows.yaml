# Ledger fragment — append-only, IMMUTABLE once written.
status:
  - packet_id: low-end-no-local-inference-gate
    field: status
    value: implemented
    ts: "2026-08-13T12:00:00Z"
    host: windows

events:
  - packet_id: low-end-no-local-inference-gate
    event:
      type: progress
      ts: "2026-08-13T11:58:00Z"
      host: windows
      summary: >
        This cycle found the CODE already implemented — the 2026-08-08 Esmeralda
        field session landed the pure `inference_disable_flag`, the
        `local_inference_disabled()` reader, gates at all four lane start sites
        plus the readiness wait, and the tray forwarding. The packet was
        returned to `ready` by the 641-e2qa stranded triage on the correct
        grounds that there was no evidence of COMPLETION either. So the work
        here was verification, not implementation, and the gap worth closing was
        that the strongest existing evidence — the 2026-08-09 cross-host note
        confirming all five gate sites — was someone READING the code. That is a
        fact about that day, not a property of the tree.
        Criterion 3 (truth table) was already green and still is.
        Criteria 1 and 2 are about a fifth site never appearing, as much as
        about the four that exist: the failure mode is an ADDED lane, and no
        unit test of the flag parser can see one, because all four existing
        gates keep passing while the new one starts a 2.1GB ollama on exactly
        the hosts the switch was built for.
        Added `every_inference_start_site_is_behind_the_kill_switch`: it scans
        for `run_container_observed(... "tillandsias-inference" ...)` and
        requires `local_inference_disabled()` in the enclosing guard window,
        naming the LINE of any site that is not. It also asserts it found
        several sites, so a rename of the helper or the container cannot make it
        pass by scanning nothing — with
        `the_kill_switch_scan_can_actually_fail` as the negative control,
        running the same scan over a fixture containing one deliberately
        unguarded site.
        Evidence: cargo test -p tillandsias-headless --bin tillandsias inference
        = 8 passed, 0 failed. Hand-falsification recorded: replacing the
        status-check gate condition with an unrelated test makes the scan FAIL
        and name line 7186 — it points at the site, not merely at itself.
  - packet_id: low-end-no-local-inference-gate
    event:
      type: note
      ts: "2026-08-13T11:59:00Z"
      host: windows
      summary: >
        Status set to `implemented`, deliberately not `ready` and not
        `completed`. Criteria 1 and 2 name a LIVE lane launch — "no
        tillandsias-inference container created at forge/opencode/opencode-web/
        status-check launch" with the flag set, and unchanged behaviour with it
        unset. That is podman-hosted field evidence and this cycle did not
        produce it; the packet's own `next_action` says "verify on the field
        host", and the field host is the Intel N100 it was filed from.
        Leaving it `ready` would invite a third host to re-derive the same
        analysis from scratch, which is what the last two cycles of this
        packet's history already are. `implemented` records that the code and
        its structural guarantees are in place and that exactly one thing is
        outstanding: a launch on a real host, flag set, showing no inference
        container, and one with it unset showing the container present. The
        2026-08-09 note already reports the unset half observed live on a normal
        Windows host, so the remaining evidence is the flag-SET launch.
