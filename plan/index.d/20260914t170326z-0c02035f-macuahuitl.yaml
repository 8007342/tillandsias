# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-claim-or-closure-recorded-on-a-platform-branch-is-invisible-to-plan-next-on-every-other-host-until-the-next-relay-so-two-hosts-can-work-one-row
    event:
      type: completed
      ts: "2026-09-14T17:03:26Z"
      agent_id: linux-macuahuitl-fable-20260914t154054z
      host: macuahuitl
      summary: |
        Platform-regime half of criterion 2 measured by macbookair (2026-09-14T16:36Z–16:50Z) from a real osx-next checkout with that host's own hook and binary, after the plan/archive fix landed (ed669a32e): verdict `ok:fragments-on-trunk:638022bd9e46cecd6c60cee54c7b0cf0efdc5395:1` (true rc 0, captured unpiped); hook lines verbatim — `plan-only lane: outgoing diff adds only new plan fragment files / append-only attestation-ledger records — accepting without the build stamp`, `plan-only lane: validated plan/index.d/20260914t163612z-103e8dd8-macos.yaml`, `local gate: plan-only lane clean (1 fragment file(s) validated; build stamp not required for this push)`; 638022bd9 confirmed an ancestor of origin/linux-next after a fresh fetch and the fragment resolves there; HEAD unchanged (reflog shows no entry from the relay), porcelain 0 before and after. The first re-run answered skip:nothing-new because the coordinator's relay had already carried every fragment; the precondition was supplied with real content (the progress event recording the run), not manufactured. The `next macos | grep -c` control was deliberately not answered: macbookair releases claims at cycle end, so a 0 would now read as a fault; the mechanism is content-agnostic and arm 1 of the fixture pins an in_progress fragment reaching a trunk reader's fold while held. Before this, macbookair found the helper's trunk fold omitted plan/archive (96 unresolved referents; every relay refused) — fixed with a real-ledger arm and its control, 27/27.
