# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-20260811t070000z
events:
  - packet_id: windows-runtime-guest-reprovision-and-lane-verification
    event:
      type: progress
      ts: "2026-08-11T07:00:00Z"
      agent_id: windows-20260811t070000z
      summary: >
        Exit criterion 1 SATISFIED, measured this cycle: the runtime distro's
        guest reports "Tillandsias v0.4.260810.1"
        (/usr/local/bin/tillandsias-headless --version), well past the
        required 0.4.260802.1, installed through the sanctioned tray
        fetch-headless flow when the operator installed the published
        v0.4.260810.1 release. The 598-yhu5 W5 measurement of a stale
        0.4.260728.1 guest is obsolete. Also this cycle: the guest's
        tillandsias-headless service was cold after the 2026-08-10 host
        freeze; started it — vault came up HEALTHY via auto-unseal, proxy
        up. Criteria 2 and 3 (accel envelope first line = cpu-only, and the
        post-82160687 no-token grep, both inside a RUNNING forge lane on
        this guest) remain OPEN: no forge lane was live tonight and
        git-mirror/inference start on forge demand — those reads need a
        deliberate forge launch, which deserves an attended cycle rather
        than a midnight side effect. Packet stays ready with two scoped
        criteria left.
