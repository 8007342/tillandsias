# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-root-codex-20260806t001750z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: browser-compiled-launch-hardening-contract-drift
    order: 615-x3b8
    status: ready
    desired_release: v0.5
    priority: p0
    title: "Reconcile the compiled project-browser boundary with active hardening specs"
    kind: architecture+bug+runtime+litmus
    deliverable: plan/issues/browser-compiled-launch-hardening-contract-drift-2026-08-06.md
    depends_on: [chromium-framework-no-sandbox-probe-false-negative]
    pickup_role: linux
    capability_tags: [linux, podman, chromium, browser, runtime, networking, testing]
    estimated_hours: 4
    outcome: >
      The real compiled project-browser launch reaches its one intended local
      route and graphical display through a specified least-authority boundary,
      with filesystem/capability behavior matching the active browser specs.
    next_action: >
      After 612-nvf3 closes, choose and ratify the project-router connectivity
      mechanism, redirect crashpad/profile writes into bounded mounts, then pin
      the exact compiled ContainerSpec and negative reachability evidence.
    exit_criteria:
      - "active browser specs and the compiled ContainerSpec name the same network, filesystem, capability, userns and writable-mount boundary"
      - "the product path reaches its intended project route while an unrelated host listener and an external address remain unreachable"
      - "real X11 or Wayland launch evidence emits expected DOM without a writable root or ambient host network"
      - "no user-visible UX surface changes"
    owned_files:
      - crates/tillandsias-headless/src/main.rs
      - openspec/specs/browser-isolation-framework/spec.md
      - openspec/specs/browser-isolation-tray-integration/spec.md
      - openspec/litmus-tests/litmus-browser-isolation-e2e.yaml
      - plan/issues/browser-compiled-launch-hardening-contract-drift-2026-08-06.md

events:
  - packet_id: browser-compiled-launch-hardening-contract-drift
    event:
      type: filed
      ts: "2026-08-06T10:08:13Z"
      agent_id: linux-mutable-root-codex-20260806t001750z
      host: linux_mutable
      summary: >
        Independent read-only review of 612-nvf3 found the direct hardened
        image test is not the compiled product boundary: main.rs still requests
        host networking, SYS_CHROOT and a writable root while active specs say
        no external network, reduced capabilities and read-only root. Filed as
        a separate operator-visible v0.5 packet; no UX or runtime flags changed.
      artifact_refs:
        - plan/issues/browser-compiled-launch-hardening-contract-drift-2026-08-06.md
