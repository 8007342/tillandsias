# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: builder-toolbox-init-set-too-small-for-the-dispatch-pattern
    field: next_action
    value: 'RESCOPED 2026-08-24 (lenovinha), the sweep is now SMALLER. Init set landed 2026-08-23 (+8s). LITMUS CALLERS ARE SOLVED WITHOUT CONVERSION: run-litmus-test.sh now materialises the toolbox''s yq once into target/litmus-runtime/bin (verified before trust; deleted if it will not run; grep fallbacks unchanged otherwise), so all 121 bare yq calls in litmus steps resolve a real yq, AND the runner''s own yaml_get/get_litmus_tests_for_spec stop reading test metadata through grep approximations. The false FAIL on litmus:skills-canonical-and-mcp-first-shape STEP 6 is gone. WHAT REMAINS is the ~50 SCRIPT callers, split as before: dev-host scripts take the documented dispatch; shipped diagnostics (tray-diagnose.sh, diagnose-macos-provision.sh) run on end-user machines with no builder toolbox and need a different answer — decide that before sweeping. NOTE the extraction must run BEFORE the runtime bin joins PATH: that dir holds the runner''s podman wrapper and toolbox shells out to podman, so the reverse order fails silently.'
    ts: "2026-08-24T02:04:58Z"
    host: lenovinha
