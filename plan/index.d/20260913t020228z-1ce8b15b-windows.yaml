# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-cheatsheet-tier-check-protects-neither-windows-host-by-two-independent-routes
    field: next_action
    value: 'Land the reorder in resolve_target_binary (extensionless before .exe, matching resolve_plan_binary). Note the corrected blast radius: the defect is only reachable when CARGO_TARGET_DIR points at a mixed-artefact directory, which the sanctioned build.sh re-exec avoids by construction — so this is a latent trap to close, not a live break for anyone on the documented path.'
    ts: "2026-09-13T02:02:28Z"
    host: windows

events:
  - packet_id: the-cheatsheet-tier-check-protects-neither-windows-host-by-two-independent-routes
    event:
      type: note
      ts: "2026-09-13T02:02:28Z"
      host: windows
      summary: |-
        reachability corrected: the esme run bypassed with-wsl2-builder and used the drvfs target dir holding both artefacts
