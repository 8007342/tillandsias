# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by windows (yolanda), 2026-08-17.
#
# EVIDENCE ONLY for 793-ee2g. That packet is kind: decision, pickup_role:
# operator, and the brief was explicit: prepare the evidence that makes it
# decidable, DO NOT decide it. No status transition is made here, nothing was
# installed, and no option among A/B/C/D is endorsed.
#
# Two of the packet's own statements are materially affected by what was
# measured tonight, which is why this is filed rather than kept in a transcript.

events:
  - packet_id: npu-inference-service-requires-windows-side-host-install
    event:
      type: note
      ts: "2026-08-17T21:45:00Z"
      agent_id: windows-yolanda-opus5-20260817t2059z
      host: windows
      summary: >
        STALE PREMISE #1 — "WSL -> Windows is BLOCKED" is no longer true for the
        port this decision cares about. The packet records
        DefaultInboundAction=Block "with only ICMP and mDNS allow rules". Measured
        2026-08-17T21:4xZ: DefaultInboundAction is still Block and
        LoopbackEnabled is True, but the Hyper-V VM rule set now ALSO contains two
        allow rules that the packet's snapshot predates — "Tillandsias research:
        WSL2 guest -> Windows inference service (TCP 8000)" and "Tillandsias
        research host-layer: WSL2 subnet -> Windows TCP 8000". From inside the
        runtime guest, `curl http://192.168.48.1:8000/` returns HTTP 200. The
        negative control is what makes that meaningful: the same curl to
        192.168.48.1:8991 (a port with no allow rule) times out, curl_rc=28,
        http_code=000 — so the default-block is real and 8000 is open
        specifically because those rules exist. Exit criterion 2 ("the
        WSL->Windows reachability question is resolved in the same decision") is
        therefore answerable with a measurement rather than left open, at least
        for a single named port. The host IP had to be decoded from
        /proc/net/route because `ip` is ABSENT in the runtime guest — the first
        attempt read an empty `ip route` output as "no default gateway", which is
        the exact absent-tool-masquerading-as-a-result trap 794-kmqe names; this
        is its fifth instance on this host after pgrep, ps, ss and script.
  - packet_id: npu-inference-service-requires-windows-side-host-install
    event:
      type: note
      ts: "2026-08-17T21:46:00Z"
      agent_id: windows-yolanda-opus5-20260817t2059z
      host: windows
      summary: >
        STALE PREMISE #2 — option B is not hypothetical on this host: Lemonade
        Server is ALREADY INSTALLED AND RUNNING. This cycle did not install it and
        did not change it; it was found in place. Measured: PID 16060,
        C:\Users\bullo\AppData\Local\lemonade_server\bin\LemonadeServer.exe, the
        sole listener on TCP 8000, with C:\Users\bullo\AppData\Local\Temp\lemonade-server.log
        beside it. Its OpenAI-compatible surface answers — GET /api/v1/models
        returns three models, all DOWNLOADED, and all carrying recipe "flm", i.e.
        FastFlowLM, which is option C rather than the mainline Ryzen-AI/ONNX path:
        embed-gemma-300m-FLM (labels ["embeddings"]), llama3.2-1b-FLM
        (max_context_window 131072) and qwen3-0.6b-FLM (labels ["reasoning"],
        40960). That partially answers the packet's UNVERIFIED item "FLM's
        embedding model list": at least embed-gemma:300m is present and served
        under the flm recipe, which matters because the packet's own research
        established that the ONNX/OGA path serves NO embeddings at all. It also
        answers "whether either server can bind a WSL-facing interface at all" —
        yes, measured, see the note above. The packet's framing that "every option
        here is a host-level software install ... this agent deliberately
        installed nothing" should be re-read against this: some install already
        happened between that research and now, by the operator or a later cycle,
        and the decision is consequently narrower than it was written. WHAT IS
        STILL NOT MEASURED, and deliberately so: no throughput was run, on any
        device. Exit criterion 3 (prefill and embedding throughput on THIS host,
        compared against the iGPU/CPU numbers in 793-zumy) remains entirely open,
        and nothing here says which device those FLM models actually execute on —
        a model served by Lemonade is not evidence that the NPU ran it.
  - packet_id: npu-inference-service-requires-windows-side-host-install
    event:
      type: note
      ts: "2026-08-17T21:47:00Z"
      agent_id: windows-yolanda-opus5-20260817t2059z
      host: windows
      summary: >
        RE-VERIFIED, UNCHANGED — the structural constraint the decision rests on
        still holds, so nothing above weakens it. Windows side: the NPU is present
        and healthy, PCI\VEN_1022&DEV_17F0&SUBSYS_382317AA&REV_20, Status=OK,
        driver 32.0.20102.3930 dated 2026-06-05, xrt_coreutil.dll present in
        System32 — every field matching what 793-ee2g recorded on 2026-08-16.
        Guest side, on a FRESHLY provisioned v0.4.260817.1 distro (so this is not
        a stale-guest artifact): /dev/accel* ABSENT, /dev/kfd ABSENT, /dev/dri
        ABSENT; only /dev/dxg (crw-rw-rw- 10,258) plus
        /usr/lib/wsl/lib/{libd3d12,libd3d12core,libdxcore}.so. WSL2 paravirtualises
        the graphics adapter and nothing else, exactly as the packet states. The
        NPU remains unreachable from inside any distro we own, and that is
        structural, not a configuration gap. Related and filed separately this
        cycle: 806-2r4s — the capability probe reports accel_npu=none rather than
        present-unusable for this same NPU, so the fleet's own vocabulary cannot
        currently express the state this decision is about.
