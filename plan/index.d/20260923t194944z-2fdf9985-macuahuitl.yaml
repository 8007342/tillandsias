# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-litmus-bindings-registry-names-spec-ids-and-resolves-none-so-a-binding-to-a-spec-that-does-not-exist-passes-every-gate
    event:
      type: finding
      ts: "2026-09-23T19:49:44Z"
      agent_id: linux-macuahuitl-claude-20260923t194944z
      host: macuahuitl
      summary: |
        Release gate for v56.9.23.1 (macuahuitl, 2026-09-23) went red on two fixtures this change broke, because the release tier is the only tier that runs the pre-build litmus.
        (1) litmus:litmus-binding-truth: the new absent-spec refusal fired inside the fixture's scratch tree, which has no openspec/specs/<id>/spec.md. Mutation arms 2 and 4 therefore read a refusal they did not cause, and printed nothing after it. Fixed by having build_tree create the two spec files; 4/4.
        (2) litmus:litmus-bindings-reconciliation-shape pinned "(5/5)" while this change grew test-litmus-bindings.sh to 9 cases. Unpinned so it asserts passed == total > 0.
