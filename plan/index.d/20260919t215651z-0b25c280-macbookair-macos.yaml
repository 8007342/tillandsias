# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: on-bsd-date-the-timing-clock-has-one-second-resolution-so-a-real-sub-second-measurement-emits-the-same-zero-the-script-already-treats-as-no-measurement
    field: next_action
    value: |-
      Reproduce first; it is one command and needs no build. On any macOS host: `date +%s%3N` prints a trailing literal `N`, and `. scripts/timing-log.sh; timing_now_ms` returns a value ending in three zeros. Then decide between the two shapes in the deliverable rather than implementing whichever is nearer the cursor — and if you take (2), first establish who READS duration_ms, because the value is written by one printf in cycle-metrics.sh and a null changes the contract for every consumer. KEEP THE NEGATIVE CONTROL IN THE FIXTURE: a multi-second step must still record its real value, because macneo's contrast run is the only reason we know the clock itself is sound. Do NOT "fix" this by removing the `_t0 -eq 0` skip in timing_emit — that skip is load-bearing for the path-skew stub it was written for (693-tf79), and deleting it would let absolute epoch-ms values through as ~56-year durations.


      [2026-09-19T21:56:51Z macbookair-macos] CLOSED. The fix is on trunk at dd27a9e82 and needs nothing further; what follows is for anyone who touches this clock again. THE MECHANISM, because 'BSD date does not support %3N' is the wrong sentence and sends readers to the wrong place: BSD date does not REJECT %3N, it EMITS THE LITERAL CHARACTERS — `date +%s%3N` returns 17898506753N — so timing_now_ms's non-digit guard fires correctly and the fallback then staples three zeros onto whole seconds. The guard was never broken; it cannot invent resolution the source never had. SHAPE OF THE FIX: timing_now_ms tries date, then perl, then whole seconds; timing_clock_resolution reports which arm actually answered. date stays FIRST so the Linux lanes keep their existing path and are unaffected. DO NOT REACH FOR THE OTHER OBVIOUS INTERPRETER — 1087-h2z9 bars that runtime anywhere in the harness and the scan matches the TOKEN, so it refuses a comment that merely names it; this file was refused for exactly that. A Rust helper, which the policy nudges toward, does not fit either: this is a shell library sourced BY the build, so it cannot assume a built binary exists. NO SHARED STATE between the value and its resolution, and that is not style: every caller invokes timing_now_ms through $( ), a subshell, so a global set inside it is discarded — a first cut did exactly that and would have reported an empty resolution forever. GUARD: scripts/test-timing-clock-resolution.sh -> ok:timing-clock-resolution:4. Arm 2 is macneo's negative control and must not be dropped — their contrast run (42000 and 0 on one clock in one run) is the only reason we know the clock is sound and only its resolution was wrong. Arm 3 guards the tempting wrong fix: deleting the _t0=0 skip in timing_emit would 'stop losing' sub-second records and let absolute epoch-ms through as ~56-year durations (693-tf79). Arm 4 pins the subshell defect above.
    ts: "2026-09-19T21:56:51Z"
    host: macbookair-macos
