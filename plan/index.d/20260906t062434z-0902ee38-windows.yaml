# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: older-app-silently-downgrades-a-newer-enclave
    field: status
    value: in_progress
    ts: "2026-09-06T06:24:34Z"
    host: windows

events:
  - packet_id: older-app-silently-downgrades-a-newer-enclave
    event:
      type: progress
      ts: "2026-09-06T06:24:34Z"
      host: windows
      summary: |-
        status 'in_progress' (not terminal) with evidence_refs: claimed by yolanda; verified on own tree that the guard is implemented and wired, and that the residual is the wiring+ordering pin only
