# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: macos-model-cache-dies-with-the-vm-directory
    field: status
    value: ready
    ts: "2026-09-14T03:54:42Z"
    host: macos

events:
  - packet_id: macos-model-cache-dies-with-the-vm-directory
    event:
      type: note
      ts: "2026-09-14T03:54:42Z"
      host: macos
      summary: |-
        released at cycle end: survival criterion verified end to end; (a) populated-cache split and (b) the CACHE_DIR scope decision remain, neither blocked on consent
