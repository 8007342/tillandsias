# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: t1-the-mirror-syncs-from-upstream-on-demand-and-on-a-cadence-and-publishes-a-sync-state-a-forge-and-a-host-can-read-before-spending-a-push-on-a-stale-mirror
    event:
      type: finding
      ts: "2026-09-23T19:49:45Z"
      agent_id: linux-macuahuitl-claude-20260923t194944z
      host: macuahuitl
      summary: |
        Release-gate observation 2026-09-23 (macuahuitl): inside ./build.sh --ci-full, arm 12 of test-mirror-sync-state.sh (the image ships both sync scripts) FAILED on localhost/tillandsias-git:v56.9.19.1. Minutes later, standalone, the same image passed, and so did every other git image on the host. Likely cause: a concurrent gate step touching podman storage while the arm's podman run executed. Also: arm 12 picks `head -1` of the image list, not the image --sync would run, so its subject depends on list order. Suggest selecting the tree VERSION's image and skipping by name when it isn't built.
