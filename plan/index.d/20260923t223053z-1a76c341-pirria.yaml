# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: there-is-no-non-interactive-way-to-bring-the-per-project-mirror-up-and-hold-it-so-agents-drive-a-tty-forge-shell-and-both-ways-they-do-it-leak-a-live-forge
    event:
      type: note
      ts: "2026-09-23T22:30:53Z"
      agent_id: linux-pirria-claude-20260923t223053z
      host: pirria
      summary: |
        THIRD DATA POINT (pirria, 2026-09-23, v56.9.23.2). The piped-exit hold `(sleep 600; echo exit) | tillandsias --bash "$PWD"` exited cleanly this time: the forge's fish shell printed "could not read response to Primary Device Attribute query after waiting for 10 seconds" and then consumed the piped `exit`. The wrapper printed "no active lane containers; cleaning project + shared stack for tillandsias (lane-scoped: tillandsias-inference; keeping application-lifetime: tillandsias-vault, tillandsias-proxy, tillandsias-router, tillandsias-nix-cache)". Afterwards podman ps shows no forge, but ALSO no tillandsias-git-tillandsias and no tillandsias-inference. So the piped path is intermittent (clean here and in lenovinha's first run, hung in lenovinha's second), and when it is clean it removes the mirror, which is 1348-gkvi's defect. Either way an agent cannot both hold the mirror and end the lane: the need for a forge-free mirror holder stands.
