# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-20260810t140500z
packets:
  - packet_id: pickup-role-checker-one-spawn-per-test
    order: 662-inzz
    status: completed
    desired_release: v0.5
    priority: p2
    title: "check-pickup-role-grammar.sh spawned ~1000 greps: 100.1s, blowing its own litmus timeout the moment it was bound"
    kind: optimization
    deliverable: scripts/check-pickup-role-grammar.sh
    depends_on: []
    pickup_role: windows
    capability_tags: [performance, litmus, cross-platform, triage]
    context: >
      Found by binding this litmus (order 661) and running it for the first
      time. Step 2, the live-ledger grammar check, TIMED OUT at 60s.

      Measured: the checker took 100,128 ms, of which the tillandsias-plan query
      was 333 ms. The other 99.8 seconds were shell process spawns - a canonical
      grep per value plus up to six more per prose value, over 626 values.

      THE IRONY IS THE POINT. This is the same one-spawn-per-item pattern this
      lane had fixed in scripts/freshness-inventory.sh ONE HOUR EARLIER
      (126.8s to 4.7s, order 661-2wsz). I diagnosed it in someone else's script
      and shipped it in my own - and could, because until the test was bound
      nothing ever ran it and the cost was invisible. That is order 660's thesis
      demonstrated on its own reporter.
    outcome: >
      shopt -s nocasematch makes the [[ ]] tests case-insensitive, so every
      canonical check and host-presence test becomes a shell builtin with no
      process at all. One tr per MULTI-LANE value remains (4 of 626) where a byte
      offset is needed; the bash 4 lowercase expansion was avoided because macOS
      ships bash 3.2.

      MEASURED: 100,128 ms to 2,027 ms, a 49x speedup. Behaviour identical - the
      live verdict is unchanged (total=659 canonical=632 prose=27 multi_host=4
      sequenced=0 verdict=ok:canonical-roles) and all four fixtures
      (clean, sequenced, co-owned, missing-input) return their expected verdicts
      and exit codes.

      litmus:pickup-role-grammar-shape now PASSES on Windows.
    verifiable_closure: >
      litmus:pickup-role-grammar-shape passes on Windows; fixture verdicts and
      exit codes unchanged.
    events:
      - ts: "2026-08-10T14:05:00Z"
        event: completed
        summary: >
          Same defect class as 661-2wsz, in this lane's own script, found only
          because the test was finally bound and executed.
