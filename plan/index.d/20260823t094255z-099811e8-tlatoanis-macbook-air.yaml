# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: sidecar-arch-hardcoded-x86-64-vs-aarch64-guest
    field: next_action
    value: 'On a cycle with the macOS guest running (or the local-build e2e): exec the staged aarch64 sidecar inside the guest''s router container and record the criterion-1 verdict (expected: execs; ENOEXEC would reopen); also observe the entrypoint guard verdict line in the router container log'
    ts: "2026-08-23T09:42:55Z"
    host: tlatoanis-macbook-air

events:
  - packet_id: sidecar-arch-hardcoded-x86-64-vs-aarch64-guest
    event:
      type: note
      ts: "2026-08-23T09:42:55Z"
      host: tlatoanis-macbook-air
      summary: >
        carry-forward
