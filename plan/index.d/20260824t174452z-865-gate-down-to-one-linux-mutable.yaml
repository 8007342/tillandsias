# Ledger fragment — append-only, IMMUTABLE once written.
# Filed by linux_mutable (macuahuitl), 2026-08-24T17:44:52Z.
packets:
  - order: 865-r6dt
    packet_id: groundtruth-harness-litmus-fails-for-two-unrelated-reasons-neither-an-expert-regression
    desired_release: v0.5
    title: "the corpus-declaration fixture never supplies an index, and three cases are keyed to a packet that was obsoleted"
    status: ready
    kind: defect
    pickup_role: any
    priority: p1
    capability_tags: [litmus, groundtruth, ci, release]
    deliverable: scripts/test-groundtruth-corpus-declaration.sh
    depends_on: []
    estimated_hours: 4
    discovered_by: "865-n8vq — the last check still failing ./build.sh --ci-full"
    context: >
      litmus:expert-groundtruth-harness is red for TWO INDEPENDENT REASONS and
      neither is an expert regression, which is precisely the class 680-zphp
      exists to keep out of this harness.

      (1) THE FIXTURE NEVER SUPPLIES AN INDEX. Step 6 of
      scripts/test-groundtruth-corpus-declaration.sh runs

        "$PLAN" grade "$ROOT"/openspec/litmus-tests/groundtruth/*.yaml

      with no TILLANDSIAS_SPEC_INDEX_DIR. The glob now includes spec-rung1.yaml,
      whose cases use engine spec.answer, and spec.answer needs a built index —
      so the command dies with `HARNESS ERROR: spec.answer needs a built index`
      and the step reports `committed glob not green:` with an EMPTY verdict,
      because it captured a last line that never came.

      (2) WITH AN INDEX SUPPLIED, THREE CASES STILL FAIL, all in
      plan-methodology-experts-rung1 [plan.answer] — packet 394's set. 394 was
      obsoleted on 2026-08-23 (06d859499). That is the SAME ROOT CAUSE as
      865-x2mf, which I fixed this morning: a fixture keyed to a live ledger
      row, breaking when the row legitimately moved. The guard for that class
      (check-groundtruth-mutable-status-pins.sh) only inspects `status:` PINS;
      these cases depend on 394 in other ways it cannot see.

        with index:  sets=4 total=33 pass=31 fail=2  (3 FAIL lines, 2 distinct)
        without:     HARNESS ERROR, no verdict at all

      A THIRD THING FOUND ON THE WAY, ENVIRONMENTAL AND NOT THE CAUSE:
      this host's session carries

        FORGE_SPEC_INDEX_DIR=/mnt/c/Users/bullo/claudia/tillandsias/target/spec-index

      a WSL path belonging to a DIFFERENT HOST, leaked into macuahuitl's
      environment and silently redirecting index lookups to a directory that
      cannot exist here. Unsetting it does not fix the litmus — measured, so it
      is recorded as a separate hazard rather than a suspected cause. It is the
      849-tz8g shape: an environmental fact that makes a failure look like a
      code defect.
    exit_criteria: >
      (1) The fixture supplies an index for spec.answer sets, or skips them with
      a stated reason. A step whose verdict variable can be EMPTY must report
      that as an error, not as a failed comparison — the current message shows
      `not green: ` with nothing after it and reads like a value mismatch.
      (2) The three plan-methodology-experts-rung1 cases stop depending on a
      live ledger row, the way 865-x2mf's controls now supply their own.
      (3) litmus:expert-groundtruth-harness green, and ./build.sh --ci-full
      re-run to confirm what it unblocks rather than assuming.

events:
  - packet_id: groundtruth-harness-litmus-fails-for-two-unrelated-reasons-neither-an-expert-regression
    event:
      type: filed
      ts: "2026-08-24T17:44:52Z"
      agent_id: linux-macuahuitl-opus5-20260824T173400Z
      host: linux_mutable
      summary: >
        Filed rather than fixed: (1) is a small fixture change, but (2) means
        editing committed groundtruth cases whose correct new form depends on
        what 394 was asserting, and the last four cycles have shown how easily a
        confident edit to this harness certifies the wrong thing.

        Explicitly checked and NOT the cause: my 865-h4tn change. Both cases I
        added this morning PASS in the run above.

  - packet_id: the-trunk-has-been-unreleasable-for-a-week-behind-a-gate-no-push-runs
    event:
      type: progress
      ts: "2026-08-24T17:44:52Z"
      agent_id: linux-macuahuitl-opus5-20260824T173400Z
      host: linux_mutable
      evidence_refs:
        - "./build.sh --ci-full re-run after the three fixes: 3 failing checks -> 1"
        - "guard-activation and markdown-distillation now green; litmus-pre-build remains"
        - "litmus 317 pass / 7 fail (was 316/8) — the status-pin repair is the one that moved"
      summary: >
        THE GATE IS DOWN FROM THREE FAILING CHECKS TO ONE, and the caution filed
        with the last cycle was right: three blockers clearing individually was
        not a green gate.

        Remaining seven litmus failures, with ownership:
          cross-target-cfg-gate-check          LENOVINHA is on it (attested "cross-target gate")
          sidecar-staleness-stamp-shape        unclaimed
          expert-capability-skew-honesty       unclaimed
          expert-groundtruth-harness           filed as 865-r6dt above
          inference-container-implementation-shape  unclaimed
          pre-push-linux-next-merged-shape     unclaimed
          no-terminal-flicker-shape            unclaimed

        None is claimed by a wedged or dark host, so none is stalled — they are
        simply unworked. The trunk stays unreleasable until they are.
