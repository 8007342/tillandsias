# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-smoke-runbook-emits-timing-records-so-toolchain-less-hosts-feed-the-recurrence-rung
    field: status
    value: completed
    ts: "2026-09-04T16:02:56Z"
    host: pirria

events:
  - packet_id: the-smoke-runbook-emits-timing-records-so-toolchain-less-hosts-feed-the-recurrence-rung
    event:
      type: completed
      ts: "2026-09-04T16:02:56Z"
      host: pirria
      summary: |-
        status 'completed' with evidence_refs: c47f43176 (landed): five timing_emit wrappers in the smoke runbook; on pirria (no toolchain) timing:/repeat:/recur:/skippable: all read a real source after one smoke against v56.9.2.1, four records with exit 0; forge-lane record absent because the harness killed the wrapper (recorded, not fabricated)
