# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-claim-or-closure-recorded-on-a-platform-branch-is-invisible-to-plan-next-on-every-other-host-until-the-next-relay-so-two-hosts-can-work-one-row
    event:
      type: progress
      ts: "2026-09-14T16:37:07Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260914t163707z
      host: macos
      summary: |
        PLAN-ONLY LANE MEASURED FROM A NON-TRUNK CHECKOUT — the measurement 1153-j2nm
        was missing. Taken on macbookair (osx-next), against the fixed relay.

          verdict: ok:fragments-on-trunk:638022bd9e46cecd6c60cee54c7b0cf0efdc5395:1
          true rc: 0 (captured unpiped)

          hook lines, verbatim:
            plan-only lane: outgoing diff adds only new plan fragment files / append-only attestation-ledger records — accepting without the build stamp
            plan-only lane: validated plan/index.d/20260914t163612z-103e8dd8-macos.yaml
            local gate: plan-only lane clean (1 fragment file(s) validated; build stamp not required for this push)

        So the lane does what it claims from a platform-branch checkout: it accepts a
        fragment-only push WITHOUT a build stamp, validates the fragment, and lands it
        on trunk. This checkout had no current build stamp for the pushed tree, which is
        the regime the lane exists for.

        CONFIRMED ON TRUNK, by ancestry after a fresh fetch rather than by exit status:
        638022bd9 is an ancestor of origin/linux-next, and
        plan/index.d/20260914t163612z-103e8dd8-macos.yaml resolves there.

        INVARIANTS HELD. git reflog shows the last HEAD movement was this host's own
        commit; the relay itself added no reflog entry and left HEAD at
        8920b04ef30de7f731bbfbf3306bf4bff2717c69 with porcelain 0. Worktree, index and
        branch untouched, as designed.

        ONE REQUESTED CHECK IS NOT MEANINGFUL AS WRITTEN, so it is reported rather than
        answered with a misleading number. The ask was `tillandsias-plan next macos |
        grep -c 1183-j9dk` -> 0 from a trunk reader, which would demonstrate that a
        CLAIM relayed to trunk stops other hosts being offered the row. This host
        released 1183-j9dk back to `ready` at cycle end (the claim is deliberately not
        held across cycles), so a trunk reader SHOULD now be offered it, and a 0 would
        mean something is wrong rather than right. The claim-invisibility property
        wants measuring while a claim is actually held — a single cycle that claims,
        relays and then checks from trunk BEFORE releasing.
