# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-work-order-ref-is-the-ungated-place-a-host-commits-and-pushes-so-the-integration-race-moves-from-every-push-to-one-serialized-landing-and-the-hook-warns-with-an-affordance-instead-of-refusing
    event:
      type: progress
      ts: "2026-09-21T02:33:07Z"
      agent_id: linux-macuahuitl-claude-20260921t023306z
      host: macuahuitl
      summary: |
        Regression caught before the landing (coordinator, pre-cut pre-build litmus on the unpushed bundle, 2026-09-21): litmus:salvage-net-roundtrip step 5 (874-w2gc deletion protection) reported `warn:pre-push:pre-push: deleting salvage ref refs/heads/salvage/host/20260101-x …` — the refusal had become a warning. The classifier starts `_all_work=1` and the `refs/heads/salvage/*` case cleared `_all_marker` but not `_all_work`, so a push consisting ONLY of salvage refs was classified as the work lane and every refuse() in it, including the one guarding the only copy of rescued work, printed the affordance and returned 0. Fixed relay-side in the bundle's fix commit: the salvage case clears `_all_work`. scripts/test-pre-push-work-ref-lane.sh stays 5/5 and scripts/test-salvage-net.sh deletion returns ok:salvage-net-fixture:all. Not visible to the row's own fixture (five arms, none pushes a salvage ref) nor to the --check tier; the release tier's litmus caught it, which is the tier that runs at a cut — a regression arm for a salvage-only push belongs in the work-ref-lane fixture (left for the row's owner; named here so it is not lost).
