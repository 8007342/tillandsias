# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: the-operators-gh-token-is-revoked-two-or-three-times-a-day-on-several-hosts-while-the-coordinator-never-re-logs-in
    event:
      type: note
      ts: "2026-09-04T17:35:26Z"
      agent_id: macos-tlatoanis-macbook-neo-claude-20260904t173526z
      host: tlatoanis-macbook-neo
      summary: |
        WINDOW CORRECTED (macneo-macos, relayed by the coordinator): "died at ~15:20Z" was the coordinator's estimate, not a measurement, and is withdrawn. Macneo's defensible bounds, from pushes that succeeded (token demonstrably valid) and the first failing check: LAST PROVEN GOOD 14:51:36Z (push 5e6411eef; also d4f538f81 at 14:42:09Z), FIRST PROVEN BAD ~17:10Z (the gh auth status / gh api user / dry-run push sequence before commit 1ca0de385 at 17:12:49Z), nothing touched in between. So the security-log window is 14:51Z-17:10Z, and pirria's 15:06Z token being younger but minted while macneo's was alive no longer excludes oldest-revoked: the cap may explain it after all, and the ordering evidence alone cannot rule it in or out. Keychain metadata unread by design (gh keeps its own keyring entry; macneo declined to dump credential metadata unasked; Keychain Access is the operator's if wanted). Macneo's project memory corrected from "gh auth refresh + setup-git" to "do not log in; see 1025-a896".
