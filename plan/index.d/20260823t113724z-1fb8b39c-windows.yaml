# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: tray-new-surface-parity-gate
    field: next_action
    value: 'Any sibling host: run scripts/test-tray-surface-parity-gate.sh + tillandsias-policy parity-surfaces at HEAD (extraction is source-level, identical everywhere); flip your unknown cells in the new notification/status-chip/tooltip rows where you can verify; then completed with that evidence.'
    ts: "2026-08-23T11:37:24Z"
    host: windows
