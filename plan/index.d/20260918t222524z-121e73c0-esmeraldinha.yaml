# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: accel-probe-blind-to-wsl2-paravirtualised-gpu
    event:
      type: note
      ts: "2026-09-18T22:25:24Z"
      agent_id: windows-esme-claude-20260918t201100z
      host: esmeraldinha
      summary: |-
        CORRECTION TO MY OWN NOTE OF 20:38Z TODAY, made the same session, because the version I wrote teaches the wrong lesson. I claimed that "neither the Windows nor the macOS gate can see this code at all — both compile it out — so a green gate on two of three platforms means nothing here". That is wrong about the gate that actually runs on esmeraldinha. `./build.sh --check` here delegates into the WSL2 builder distro and its transcript reads `Running workspace tests (cargo test --workspace, all targets)` at 475.6s, with 75 test-result blocks including the whole accel_probe suite. So this gate COMPILES AND RUNS the Linux-gated arms, and it caught a clippy error in my new test code on the first pass.

        WHY THE DIFFERENCE MATTERS AND IS NOT PEDANTRY. The vkDestroyInstance segfault would have been caught by a normal `./build.sh --check` on this host, because libtest runs each test on a spawned thread and that is the trigger. I found it earlier only because I ran the accel_probe suite directly before gating. My original wording would have told the next person that the gates cannot see this code and that manual runs are the only protection — which would invite someone to skip the gate and trust a hand-run, the exact inversion of what is true.

        WHAT REMAINS TRUE FROM THAT SENTENCE, narrowed to what was measured: a NATIVE Windows compile cannot see this code, and the same gate reports `stale:sources-drifted:windows-only:4` and `stale:sources-drifted:macos-only:1` for sources it did not read. So platform-gated code is still only as verified as the platform the gate runs ON — the claim needed to be about the compile target, not about the gate.

        METHOD NOTE, since this is the second time today the same shape bit me: I asserted the gate's coverage from what the cfg attributes implied instead of reading the gate's own transcript. The transcript was available the whole time. Name what the instrument reads before theorising about what it cannot.
