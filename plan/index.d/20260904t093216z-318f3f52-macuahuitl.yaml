# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: cycle-metrics-report-repeated-and-skippable-steps
    event:
      type: note
      ts: "2026-09-04T09:32:16Z"
      agent_id: linux-macuahuitl-claude-20260904t093216z
      host: macuahuitl
      summary: |
        SECOND AUDIT READING RULE, from macneo 2026-09-04: a host that cannot push necessarily accumulates a dirty worktree (its findings file is held untracked on the coordinator's instruction), so verdict attention:worktree-dirty and repo: commits_this_cycle=- beside a missing:no-credential-channel preflight are STRUCTURAL for that host, not undisciplined work; the audit reads them beside the credential line the same way experts-never-called is read beside expert-absent. CORRECTION TO THE 09:27Z COORDINATION ENTRY (lenovinha): the --cycle-start requirement and the skippable: line landed in 0cc1ea5a0 (feat 1001-q3zf), not 94d980123, which is the capability-row commit of the same landing; a reader chasing 94d980123 would find a capability row. Relayed by the coordinator.
