# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: wire-local-experts-mode-through-grounded-pipeline
    field: status
    value: done
    ts: "2026-09-22T15:48:52Z"
    host: macbookair-macos

events:
  - packet_id: wire-local-experts-mode-through-grounded-pipeline
    event:
      type: completed
      ts: "2026-09-22T15:48:52Z"
      host: macbookair-macos
      summary: |-
        status 'done' with evidence_refs: work/920-pxg6 is an ancestor of origin/linux-next and openspec/specs/expert-serve-grounded-pipeline/spec.md is present at that tree. Six requirements carried VERBATIM, verified by diffing the bodies against the change tree rather than by reading them; the MODIFIED requirement appended at the end of the Requirements section after a first attempt anchored on the wrong heading; the change tree archived by git mv so every file is an R100 rename and the diff carries no D lines. Req-ids stamped after the relay named the gap (7 new, 661 already had one, 668 all unique), verified as 7 insertions and 0 deletions with every added line a req-id comment, so the verbatim claim survived the stamping. The sync was load-bearing rather than filing: litmus-bindings.yaml bound three litmus tests under this spec_id while the spec did not exist, and trunk now reads spec_ids=141 resolved=141.
  - packet_id: wire-local-experts-mode-through-grounded-pipeline
    event:
      type: note
      ts: "2026-09-22T15:48:52Z"
      host: macbookair-macos
      summary: |-
        the OpenSpec sync and archive landed; the row's last item is complete
