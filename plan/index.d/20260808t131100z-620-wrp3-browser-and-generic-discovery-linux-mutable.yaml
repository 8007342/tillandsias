# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-antigravity-20260808t131100z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: browser-compiled-launch-hardening-contract-drift
    order: 615-x3b8
    status: completed
    desired_release: v0.5
    priority: p1
    title: "Browser compiled launch hardening: restore read_only, drop SYS_CHROOT, configure bounded tmpfs"
    kind: bug
    deliverable: crates/tillandsias-headless/src/main.rs
    depends_on: [chromium-framework-no-sandbox-probe-false-negative]
    pickup_role: linux
    capability_tags: [browser, hardening, opencode-web, container]
    outcome: >
      Reconciled browser ContainerSpec with hardened isolation contract: restored .read_only(),
      dropped SYS_CHROOT, removed network host, and configured HOME=/tmp, XDG_CONFIG_HOME,
      and XDG_CACHE_HOME on bounded tmpfs mounts.
    events:
      - ts: "2026-08-08T13:11:00Z"
        event: completed
        summary: "Updated build_project_browser_spec and unit tests in crates/tillandsias-headless/src/main.rs."

  - packet_id: generic-project-index-and-state
    order: 619-vwau
    status: completed
    desired_release: v0.5
    release_target: forge-local-experts-milestone
    priority: p1
    title: "Generic project index discovery pass, tmpfs index, and project-expert state"
    kind: implementation
    deliverable: images/default/lib-common.sh
    depends_on: [generic-project-expert-bootstrap-contract]
    pickup_role: linux
    capability_tags: [experts, generic-project, discovery, tmpfs]
    outcome: >
      Wired discover_generic_project and generic_project_state_line into lib-common.sh,
      building ephemeral project index into tmpfs and publishing project-expert state.
    events:
      - ts: "2026-08-08T13:11:00Z"
        event: completed
        summary: "Implemented discover_generic_project, generic_project_state_line, and startup context rendering."
