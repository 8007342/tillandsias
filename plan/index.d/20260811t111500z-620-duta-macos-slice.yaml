# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

events:
  - packet_id: portable-zero-dependency-runtime-audit
    event:
      type: progress
      ts: "2026-08-11T11:15:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        Partial slice — the macOS HALF of exit criterion 3 is done:
        litmus:macos-tray-import-surface pins the tray binary's linked
        libraries to OS-shipped paths only (/usr/lib, /System/Library) —
        verified baseline is libSystem/libiconv/libobjc + six system
        frameworks, zero Homebrew//opt//@rpath imports, so the curl+tar
        portable promise is measurable on this platform. Bound into
        litmus-bindings.yaml under macos-native-tray (suite 7/7 PASS).
        partial_artifact_refs:
        openspec/litmus-tests/litmus-macos-tray-import-surface.yaml.
        REMAINING (windows-centric, unclaimed): criterion 1 diagnose JSON
        guest_wiring/reconcile fields, criterion 2 vsock_loopback
        preflight log, criterion 3's WINDOWS import-surface half
        (dumpbin/objdump equivalent), criterion 4 spec distillation.
