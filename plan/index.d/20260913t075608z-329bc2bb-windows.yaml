# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: accel-probe-blind-to-wsl2-paravirtualised-gpu
    field: next_action
    value: 'THE OPERATOR GATE NO LONGER BINDS: esmeraldinha already carries a Vulkan loader and the stock Fedora mesa-vulkan-drivers ICD set, unprovisioned, and enumerates Microsoft Direct3D12 (Intel UHD Graphics), INTEGRATED_GPU via DRIVER_ID_MESA_DOZEN, over /dev/dxg with /dev/dri absent — re-verified 2026-09-13. Criterion 1 and the WSL2 half of criterion 3 are satisfiable by observation there rather than by an ICD install on yolanda. Both arms of criterion 2 are live on that one host: llvmpipe enumerates beside the real part as the CPU device that must be rejected, and the sibling distro is dxg-present-loader-absent. SMALLEST NEXT ACTION: make wsl2_paravirtual_gpu_reason() a detection instead of the unconditional string literal it is today (enumerate_gpus assigns it to every dxg device; nothing reads icd.d, libvulkan or an enumeration), then run the probe on esmeraldinha. Needs Rust plus a fixture, so it is beyond the floor host compile-free lane. Any probe run must move ~/.cache/tillandsias/capabilities.json aside first — see 1139-xe5m, --capabilities serves that cache silently.'
    ts: "2026-09-13T07:56:08Z"
    host: windows

events:
  - packet_id: accel-probe-blind-to-wsl2-paravirtualised-gpu
    event:
      type: note
      ts: "2026-09-13T07:56:08Z"
      host: windows
      summary: |-
        evidence from esmeraldinha: a host with the ICD exists unprovisioned, so the operator gate is no longer the only route; prior next_action named an ICD install yolanda had declined
