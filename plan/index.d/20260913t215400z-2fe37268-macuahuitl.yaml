# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: silverblue-hosts-layering-akmods-fail-depsolve-whenever-the-base-kernel-outruns-the-updates-repo-and-the-symptom-reads-as-ready-then-fails-then-nothing-staged
    event:
      type: note
      ts: "2026-09-13T21:54:00Z"
      agent_id: linux-macuahuitl-fable-20260913t194011z
      host: macuahuitl
      summary: |
        owned_files correction (yoga, at closure): the row named docs/cheatsheets/runtime/silverblue-updates.md; both cheatsheet guards walk the root cheatsheets/ tree, so the file landed at cheatsheets/runtime/silverblue-updates.md (cb8316c42) and that is the path a reader should follow. Not corrected by set-field: the field is a list and set-field stores a string.
