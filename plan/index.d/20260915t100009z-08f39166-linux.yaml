# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: deliver-credentials-carries-no-accept-reject-discriminator
    field: status
    value: completed
    ts: "2026-09-15T10:00:09Z"
    host: linux

events:
  - packet_id: deliver-credentials-carries-no-accept-reject-discriminator
    event:
      type: completed
      ts: "2026-09-15T10:00:09Z"
      host: linux
      summary: |-
        status 'completed' with evidence_refs: 4fc7be930 + control-wire 74 tests green, headless/windows-tray --tests, macOS compile by macbookair with a falsified positive control; validate-before-persist split to 1200-ih38
