# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: windows-20260810t095000z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
#
# Second round of the 641-e2qa stranded triage. Two MORE windows-owned packets
# crossed the checker's staleness threshold after round one.

status:
  - packet_id: wsl-guest-forge-user-src-ownership
    field: status
    value: completed
    ts: "2026-08-10T09:50:00Z"
    host: windows

  - packet_id: windows-inforge-meta-orchestration-transparent-push
    field: status
    value: ready
    ts: "2026-08-10T09:50:00Z"
    host: windows

events:
  - packet_id: wsl-guest-forge-user-src-ownership
    event:
      type: completed
      ts: "2026-08-10T09:50:00Z"
      agent_id: windows-20260810t095000z
      summary: >
        CLOSED on evidence this lane generated by accident a few hours earlier.

        The packet was implementation-complete with one residual. 9cbe7849
        recorded it exactly: "implementation-complete, live-healed; e2e
        residual", on top of two implementation commits — ea5b9e47 (provisioning
        creates the forge user and hands it /home/forge/src) and f32e84f9
        (deliver the setup script via stdin; arg-delivered scripts arrive
        shredded).

        The residual was e2e proof from a genuinely cold provision. Tonight's
        644-a3wj curl smoke destroyed the distro (`wsl --unregister tillandsias`)
        and re-provisioned it from nothing, and the tray log recorded, at
        04:32:05:

          forge user + /home/forge/src ownership ensured (order 326)

        That is the residual satisfied, from a real cold start on a real Windows
        host, by a run whose purpose was something else entirely. Evidence is
        evidence; recording it beats re-running the smoke to manufacture it.

  - packet_id: windows-inforge-meta-orchestration-transparent-push
    event:
      type: progress
      ts: "2026-08-10T09:50:00Z"
      agent_id: windows-20260810t095000z
      summary: >
        RETURNED TO READY. `git log --all --grep` finds no implementation commit
        for this packet — the single near-hit (90f371f5) is about
        windows-260716-2 and moves a blocker to order 383, not about this work.

        Stale claim, not unfinished work. NOT closed: absence of implementation
        evidence is not evidence of completion, and asserting otherwise is the
        failure 635-i6vm and 641-e2qa exist to prevent. `ready` at least makes it
        visible to a claimant.

  - packet_id: stranded-in-progress-packets-are-invisible-both-ways
    event:
      type: progress
      ts: "2026-08-10T09:50:00Z"
      agent_id: windows-20260810t095000z
      summary: >
        WINDOWS SHARE RETIRED AGAIN — round two. Two more windows-owned packets
        (326, 594-pm5h) crossed the staleness threshold after round one cleared
        the 620-* group; both are now resolved. Zero windows-owned packets remain
        stranded.

        Worth noting for the packet itself: the set is not static. Round one
        cleared three, and two more appeared within two hours as their claims
        aged past the threshold. A one-time sweep will therefore always look
        complete and always be wrong shortly after — the leak needs the recurring
        check that is already wired into ./build.sh, not a cleanup pass.

        The remaining 7 are linux (6) and unassigned (1). Deliberately NOT
        touched from this host: the discriminating evidence for several of them
        is whether their exit criteria are met, which needs someone who can read
        the subsystem, not just the commit log. The method that worked here is
        recorded on the round-one event.
