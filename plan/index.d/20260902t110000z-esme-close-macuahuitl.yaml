# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: forge-guest-hygiene
    event:
      type: note
      ts: "2026-09-02T11:00:00Z"
      host: macuahuitl
      summary: >
        955 REPRODUCED LIVE WITH FINGERPRINT (esmeraldinha, warm rerun):
        litmus:forge-git-index-materialization fails with 'bash:
        /tmp/tmp.WJwhk1GjFV/present/scripts/hooks/pre-commit-openspec.sh:
        No such file or directory' — the installed v2 hook resolving into
        a scratch fixture repo, exactly the install-hooks-bakes-path
        issue's shape. THE ACCEPTANCE PATH IS DEMONSTRATED END-TO-END:
        litmus run 1 installs the defective hook, litmus run 2 exhibits
        the defect in 18 tests with no fixture written — the fixer
        inherits a reproduction AND a detector for free. METHOD LIMIT
        for the ledger: THE SUITE SELF-CONTAMINATES — her warm run was
        1.52x faster and failed MORE (33->49; the 18 new fails are
        git/hook-shaped), so repeat-run figures on one guest are not
        comparable unless the hook dir is reset first; this bounds the
        pair's own repeat-run instrument retroactively.

  - packet_id: litmus-low-end-instrument
    event:
      type: note
      ts: "2026-09-02T11:00:00Z"
      host: macuahuitl
      summary: >
        CLOSING REPORT (esmeraldinha). THE BUDGET CLASS IS RESURRECTED
        WITH DIFFERENT MEMBERSHIP by the warm-rerun class split: cold-
        failed/warm-passed = cache-caused (cheatsheet-source — PIRRIA'S
        EVICTION HYPOTHESIS CONFIRMED, closing the arc's one remaining
        mystery: the suite's ~5GB compile evicts page cache and the
        no-cargo script reads cold; external-logs = hidden-work cured
        warm); cold-and-warm-timeouts = cache-independent legitimate
        budget-class members: enclave-service-health (10s) and
        freshness-inventory (15s), found empirically — plus
        forge-policy-binary-discoverability, CACHE-IMMUNE BY DESIGN
        (mktemp target, builds from scratch forever — a wasteful-step
        design finding in its own right). Her original retraction
        STANDS IN FULL (wrong reasoning and wrong membership); the
        class survives on independent evidence. HANDOFF QUESTION for
        the fresh session: did enclave-service-health and
        freshness-inventory red on N150 warm? (pirria's first
        contended run had enclave-service-health among its
        genuinely-too-slow three.) Her timeout-masking hypothesis
        self-killed by arithmetic (6 timeouts cannot make 18 fails) —
        stated before checking, caught, filed. ORPHAN HYPOTHESIS
        REFUTED ON BOTH HOSTS (0.2s sampler across a full suite: no
        rustc survives a step boundary). FINAL 959 NUMBERS, full-suite
        scope: maxrustc=4 (cargo DOES reach nproc width at suite
        scope, refining pirria's single-spec maxrustc=2), maxprocs=34
        = 6.6% of the 512 ceiling, DENIAL_DELTA=0 both runs. Fresh-
        session note upgraded: re-check the ~14 candidates on a
        HOOK-RESET guest — some may be contamination, not content.
        Both scouts closed; zero repo writes throughout on both.
