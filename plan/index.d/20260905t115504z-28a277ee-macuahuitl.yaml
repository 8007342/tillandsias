# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: precommit-openspec-scan-exceeds-ten-minutes-on-large-files
    event:
      type: note
      ts: "2026-09-05T11:55:04Z"
      agent_id: linux-macuahuitl-claude-20260905t115504z
      host: macuahuitl
      summary: |
        FIX LANDED (yoga, 440233994), lease released to ready with the one remaining measurement named. WHAT THE SCAN GREW WITH: `grep -r` with --include still WALKS every directory, --include only decides which files it opens, so the sweep descended the whole build output (141,414 entries under target/ on yoga against 12,352 everywhere else) to read 928 source files; it grew with BUILDS, not with source, which is why the budget held for months and then did not, why a host that never built pays nothing, and why the macOS host saw it. Before/after on yoga (GNU grep, five runs each, sorted): 204 222 237 245 263 ms → 79 84 93 100 112 ms, median 237 → 93; the sweep alone 135/133/126 → 20/15/19 ms. NOT CLAIMED: that this fixes macbookair; if the 2.5x carries, 10373 ms becomes about 4100 ms, still over the 2500 ms budget, and yoga was under budget both before and after so it structurally cannot verify the fix; only macbookair can, and it has been asked for that single measurement. THE BUDGET STAYS AT 2500 ms, not widened to match a measurement: it states what the phase is worth at a commit boundary, and a scan that quietly costs more than it is worth is how a cycle loses staged work at its exit contract; if it cannot be met after the next round (the awk pass over 170 spec.md files and the per-spec loop, both bounded by spec count), re-scope the phase. The exclusion is answer-preserving, verified BEFORE it was written because the risk is worse than the slowness (a spec referenced only from build output would become a false zero-trace accusation): 218 tokens both ways, set difference empty in both directions, pinned on the real repo with a non-vacuity arm and an arm that the hook actually carries the exclusion. MEASUREMENT TRAP for the fleet: an interactive shell in this project shadows grep with a ugrep function that reports 2 to 6 ms for the same sweep; anyone timing a hook here must use /usr/bin/grep. SWEEP DEFECT found on the way: a packet reopened through set-field --reopen-evidence carries a lease the stranded-claim sweep cannot see, because the reopen's reason text does not carry the "claimed for cycle <ts> by <agent>" phrase; not specific to this packet.
         THE ONE REMAINING MEASUREMENT, taken (macbookair, after merging trunk with 440233994 an ancestor of HEAD): BEFORE 10373 ms; AFTER 187, 197, 199 ms over three runs; budget 2500 ms; about 52x faster and an order of magnitude under budget, so yoga's diagnosis holds on macOS, where target/ is large, which is why the same defect cost 10 s there and 237 ms on Linux. METHOD: grep is a shell function in that host's tool environment too, so every timing was taken under `env -i PATH=/usr/bin:/bin:/usr/sbin:/sbin` with `type -a grep` reporting /usr/bin/grep. A CORRECTION THAT MATTERS FOR HOW THE DRIFT SIGNAL IS READ: the host nearly reported "no warning printed, therefore under budget"; run_phase warns only when elapsed > budget x 4, i.e. over 10000 ms, so absence of the warning proves under ten seconds and says nothing about the 2500 ms budget; the original 10373 ms surfaced only because it was barely past 4x, and a 9000 ms scan would have been silently four times over budget. The drift signal has a BLIND BAND between 1x and 4x budget; the numbers above came from a throwaway copy of the hook patched to print every phase unconditionally, which is how anyone should re-measure. The awk-pass candidate is not needed at 199 ms.
