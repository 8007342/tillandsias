# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: macos-tlatoanis-macbook-air-fable5-20260810t043031z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

status:
  - packet_id: tray-sign-in-state-unknown-until-observed
    field: status
    value: done
    ts: "2026-08-11T09:15:00Z"
    host: macos-tlatoanis-macbook-air-fable5-20260810t043031z

events:
  - packet_id: tray-sign-in-state-unknown-until-observed
    event:
      type: completed
      ts: "2026-08-11T09:15:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      lease_id: macos-626-r7kq-takeover-20260811t0915z
      summary: >
        Taken over from the Windows host's expired 2026-08-09 lease (per
        the operator's stale-lease directive) and CLOSED. The Windows host
        had already IMPLEMENTED the approved Option-B fix in the shared
        layer — GithubLoginState::Unknown as initial() with the approved
        disabled "Checking your account…" row (menu_state.rs:276-279,
        :377, :528-539) plus unit tests (:940+) — but the completion never
        landed and the packet folded ready. macOS drain-cycle
        contribution: the exit-criterion-3 exhaustiveness follow-through
        for the macOS tray — the cfg(macos) test
        apply_login_state_reports_rebuild_only_on_change still encoded
        the pre-Unknown initial state and FAILED at merged HEAD
        (invisible to the implementing host); fixed so the first
        confirmed LoggedOut resolves Unknown with a rebuild. Criteria:
        (1) unit tests on build() cover the unobserved state (menu_state
        19/19); (2) first confirmed observation resolves both directions,
        LoggingIn path unchanged; (3) shared-layer fix inherited by both
        trays — macOS follow-through fixed here, Windows' own in their
        implementation, Linux native tray recorded out of scope in the
        2026-08-09 claim; (4) the field tray.log interval evidence is in
        the packet's own filing (Ready 06:10:33 -> project click 06:12:26
        with an accepted login click in between). Credit: implementation
        windows-claude-fable-metaorch-20260809; closure + macOS
        exhaustiveness this host.
  - packet_id: nix-supported-release-and-crane-migration
    event:
      type: note
      ts: "2026-08-11T09:15:00Z"
      agent_id: macos-tlatoanis-macbook-air-fable5-20260810t043031z
      host: macos
      summary: >
        CANNOT from this host, recorded so the skip is visible (648-dvzd
        discipline): `command -v nix` finds nothing on the macOS builder;
        the packet needs a Nix-capable host.
