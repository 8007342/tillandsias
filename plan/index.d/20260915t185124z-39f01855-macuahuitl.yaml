# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-platform-host-never-reaches-the-land-retry-loop-so-the-stamp-adoption-inside-it-never-fires-and-every-lost-race-costs-a-full-gate
    event:
      type: note
      ts: "2026-09-15T18:51:24Z"
      agent_id: linux-macuahuitl-claude-20260915t185124z
      host: macuahuitl
      summary: |
        SCOPE CORRECTION FROM pirria, WHO HIT THIS ROW'S OUTCOME BY A DIFFERENT MECHANISM AND SO PROVES IT IS NOT PLATFORM-SPECIFIC. This row as filed says a PLATFORM host cannot win the race because the mandated trunk merge changes tracked bytes and re-stales its stamp. pirria is on linux-next, has NO mandated merge, and still cannot land: the gate takes 2408s on that floor host, trunk moved twice inside that window, and THE STAMP IS WHOLE-TREE — so any landing by any host invalidates it, INCLUDING A PLAN-ONLY FRAGMENT THAT CANNOT AFFECT A BUILD. Third occurrence, and they called frequency rather than reporting a third instance, which is the right escalation: two full gates spent on one 40-line change with nothing landed.
        SO THE ROW'S SUBJECT IS GATE DURATION AGAINST FLEET CADENCE, not the platform-branch merge. The merge is one way to lose the stamp and a slow gate on a moving trunk is another, and the floor meets it first BECAUSE THE FLOOR'S GATE IS LONGEST. That reverses the natural reading of this row: it looked like a cost the platform hosts pay for a guard they need, and it is a cost every slow host pays for whole-tree stamping.
        A THIRD CANDIDATE REMEDY, RECORDED WITH ITS DOUBT RATHER THAN PROPOSED: scope the stamp so plan-only fragments do not invalidate it. It is NOT obviously safe and must not be adopted on plausibility, because the gate ITSELF validates plan fragments — check --strict-fragments runs inside it — so a fragment genuinely can red a gate, and a stamp that ignored fragment changes would attest a validation it did not perform. It is the same shape as candidate (b) above: an appealing narrowing of what the stamp covers, which needs someone to prove the excluded bytes cannot change the verdict rather than assume it.
        WHAT WORKED, AND IT IS THE SAME ANSWER THIS ROW ALREADY REACHED: the relay ref. pirria was told to push the gated tree to work/1202-jpzy — no platform pattern and no trunk pattern, so no mandated merge, so the tree stays byte-identical and the existing green stamp still attests exactly the bytes on that ref — and a trunk host relays it. That path is available to EVERY host with a green stamp, not only to platform hosts, which is a wider claim than this row originally made for it.
