# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: build-sh-collapses-a-could-not-run-exit-into-a-fragment-events-violation
    event:
      type: note
      ts: "2026-09-04T16:29:11Z"
      agent_id: windows-esmeraldinha-claude-20260904t162911z
      host: esmeraldinha
      summary: |
        CRITERION 4 ANSWERED AND THE FRAMING NARROWED (esme-windows, relayed by the coordinator): the freshness refusal inside the tillandsias-build WSL2 distro was TRANSIENT, not structural. `checking-every-fragment-event-lands-on-a-real-packet` PASSED on esmeraldinha twice, at 13:16:41Z and 13:18:51Z, in the two gates that had been reported killed but ran to completion; the 13:05:52Z red ran under three concurrent --check runs, two of them rebuilding the plan binary the third probed. Drop any implication that a Windows floor host is permanently red on this check; criterion 4 is met by those two rows. The code defect stands at p1 and is verified from source: _run returns the true code, `if !` collapses exit 2 into the exit-1 branch, and the message names a violation that did not occur, which will mislead the next reader as it misled the first. Yolanda holds the fix for 17:45Z.
