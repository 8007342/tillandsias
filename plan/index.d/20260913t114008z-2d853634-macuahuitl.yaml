# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: a-ready-row-whose-owned-files-a-landed-fix-touched-since-filing-is-a-staleness-candidate-the-pass-must-surface
    event:
      type: claim
      ts: "2026-09-13T11:40:08Z"
      agent_id: linux-macuahuitl-fable-20260913t114008z
      host: macuahuitl
      summary: |
        Claimed by the macuahuitl meta-orchestration cycle (2026-09-13T11:39Z) for
        the FIRST pass only: the cites-the-order query (a landed commit whose
        subject cites <order> while <order> still folds ready) as
        scripts/check-stale-ready-rows.sh with its fixture; the owned_files pass
        stays open on the row.
