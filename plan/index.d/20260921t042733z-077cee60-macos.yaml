# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: headless-reads-the-vsock-forwarder-target-the-tray-pushed-over-the-control-wire-after-ready-and-no-capability-bit-is-needed-because-the-psk-binds-the-build-version
    field: status
    value: done
    ts: "2026-09-21T04:27:33Z"
    host: macos

events:
  - packet_id: headless-reads-the-vsock-forwarder-target-the-tray-pushed-over-the-control-wire-after-ready-and-no-capability-bit-is-needed-because-the-psk-binds-the-build-version
    event:
      type: completed
      ts: "2026-09-21T04:27:33Z"
      host: macos
      summary: |-
        status 'done' with evidence_refs: Landed on origin/linux-next at 0fbffa6ce (land47), which I verified carries the change rather than taking the SHA on report: SetVsockForwardTarget appears at BOTH send sites in action_host.rs at that tree (the push listener's connect path and the poll fallback), and 81fe8e9be is an ancestor of it.  Gate on the work ref: BUILD_RC=0, 1658s, stamp fresh.  Closure: ok:macos-vsock-inference-closure:5, with NOTHING written by hand in the guest — LIVE target-from-tray-after-5s=[2:42421], forwarder=running, inference=absent, body=[{"version":"host-native-closure-probe"}]; DEAD (same forwarder, dead host port) body=[].  THE ROW'S OWN PREDICTION IS SUPERSEDED, NOT SATISFIED, and that note stands on the row (evidence event 2026-09-21T03:56:20Z): it said the closure was "the same probe minus its one line", but a fixture that merely stops seeding the target would pass whether or not the tray can configure the guest. ARM 0 deletes the target, waits for the tray, and asserts it arrived. That arm is what makes this a measurement of the writer.
  - packet_id: headless-reads-the-vsock-forwarder-target-the-tray-pushed-over-the-control-wire-after-ready-and-no-capability-bit-is-needed-because-the-psk-binds-the-build-version
    event:
      type: note
      ts: "2026-09-21T04:27:33Z"
      host: macos
      summary: |-
        the tray configures the guest's forwarder target over the control wire; closure re-run with nothing hand-written in the guest
