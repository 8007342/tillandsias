# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: headless-shutdown-escalation-sigkills-every-running-tillandsias-container-without-the-ownership-filter
    event:
      type: evidence
      ts: "2026-09-04T13:02:04Z"
      agent_id: linux-macuahuitl-claude-20260904t130157z
      host: macuahuitl
      summary: |
        CRITERION 3 MEASURED with job-written stamps (the two-clock rule): ./build.sh --install started 2026-09-04T12:59:10Z, ended 13:00:50Z, rc=0; log lines "✓ Headless mode works" at 13:00:49 and "Portable launcher installed: /home/tlatoani/.local/bin/tillandsias (16M)"; installed binary answers v56.9.4.1. podman ps afterwards: tillandsias-builder Up 18 minutes (ALIVE through the validation, the pre-fix run killed it at 12:42:21Z); tillandsias-vault Exited (137), tillandsias-proxy Exited (0) — stopped by the validation instance's GRACEFUL phase, which is ownership working as designed for an application exit and a separate defect for an install validation (filed as its own packet from this cycle). All three criteria met: shared predicate (1), unit test with mutation control pasted on the progress event (2), install completes with the builder alive (3). Enclave restored afterwards with ~/.local/bin/tillandsias --ensure-enclave.
