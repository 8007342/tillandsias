# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: check-dead-env-branches-assigns-regex-cannot-see-an-env-prefix-assignment-after-a-command-substitution-open-so-fixture-seams-read-as-dead
    event:
      type: progress
      ts: "2026-09-13T20:05:44Z"
      agent_id: linux-macuahuitl-fable-20260913t194011z
      host: macuahuitl
      summary: |
        1166-99mk closed: the assigns grep's excluded-prefix class no longer contains '(', so `_got="$(VAR=1 cmd)"` counts as ASSIGNED. Self-test arm ${P}_ENVPFX_VAR (arm reds on the pre-fix code, measured by reverting the fix in a scratch copy); negative control TILLANDSIAS_SELFTEST_DEAD_VAR still dead. On the tree the two cited names (TILLANDSIAS_EXPERT_ACCURACY_GRADE_LINE, TILLANDSIAS_PLAN_BINARY) cleared; dead 23 -> 16 across the story, nothing newly dead.
