# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: a-platform-scoped-gate-arm-can-land-through-a-gate-that-cannot-run-it
    field: status
    value: obsoleted
    ts: "2026-09-15T04:22:45Z"
    host: macos

events:
  - packet_id: a-platform-scoped-gate-arm-can-land-through-a-gate-that-cannot-run-it
    event:
      type: note
      ts: "2026-09-15T04:22:45Z"
      host: macos
      summary: |-
        duplicate of 1194-davi (lenovinha), filed 23s apart on the same generalisation; theirs survives — better shaping, two negative controls, and the pre-land NOTICE framing rather than unscoping
