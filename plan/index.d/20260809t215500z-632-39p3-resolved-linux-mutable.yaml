# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260809t215500z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.

status:
  - packet_id: query-and-next-disagree-on-role-eligibility
    field: status
    value: completed
    ts: "2026-08-09T21:55:00Z"
    host: linux-mutable

events:
  - packet_id: query-and-next-disagree-on-role-eligibility
    event:
      type: progress
      ts: "2026-08-09T21:55:00Z"
      agent_id: linux-mutable-20260809t215500z
      summary: >
        RESOLVED — and prioritized this cycle because the WINDOWS lane reported itself
        blocked on it ("632-39p3 (p0, linux) unblocks 635-qpx8"). A p0 owned by this host
        was gating a sibling; that is the coordinator's job before any local drain.

        DECISION: the two readers were never answering the same question, so neither was
        wrong — they were being USED interchangeably.
          `--role X`         substring filter ON THE FIELD. Both MCP servers document it
                             exactly this way ("pickup_role substring matching"), and
                             drain-queue plus plan_query depend on it.
          `--claimable-by X` NEW. What a host of role X may CLAIM: its own role OR a
                             whole-field `any`.

        Resolved by ADDING the missing question rather than redefining the existing one.
        Silently widening `--role` would have changed what plan_query reports to every
        other surface — the same unobserved-change class this packet exists to close, and
        exactly what 635-qpx8's notes warned against.

        `any` is matched as the WHOLE trimmed field, not a substring: a pickup_role of
        "company-lane" contains "any" and is emphatically not claimable by everyone.
        Verified: --claimable-by with a nonexistent role returns 0 non-`any` packets.

        Measured on the v0.5 ready set: `--role macos` 11, `--claimable-by macos` 53,
        `next macos` 33 eligible (smaller than 53 because next also requires deps-clear and
        unleased — consistent, not contradictory). Specifying both flags is a typed
        refusal rather than a silent precedence rule.

        scripts/select-work-batch.sh now calls `--claimable-by` and the two-query jq union
        that stood in while this was undecided is retired.

        UNBLOCKS: 635-qpx8 (windows, pickup_role prose normalization).
