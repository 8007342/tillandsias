# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: a-dangling-symlink-inside-the-repository-root-lets-the-lua-fs-write-sandbox-create-a-file-outside-the-root
    field: status
    value: completed
    ts: "2026-09-26T17:53:27Z"
    host: macos

events:
  - packet_id: a-dangling-symlink-inside-the-repository-root-lets-the-lua-fs-write-sandbox-create-a-file-outside-the-root
    event:
      type: completed
      ts: "2026-09-26T17:53:27Z"
      host: macos
      summary: |-
        status 'completed' with evidence_refs: f99b42eea (land45, on origin/linux-next under b2fe36e88; carries 049d9cdf2) + lua_std a_dangling_in_root_symlink_cannot_be_used_to_write_outside_the_root (the REAL fs.write/fs.mkdir through the plan binary: refused naming 'cannot be resolved', outside/ stays empty, link untouched) and the unit test covering all four write verbs; negative control: against the pre-fix sandbox the e2e test FAILS at fs.write(x) not refused; macOS tillandsias-plan 441/0; verified again at the land45 bundle tip b17f6a8a3 (lua_std 16/16)
  - packet_id: a-dangling-symlink-inside-the-repository-root-lets-the-lua-fs-write-sandbox-create-a-file-outside-the-root
    event:
      type: note
      ts: "2026-09-26T17:53:27Z"
      host: macos
      summary: |-
        closed after landing: the Lua fs sandbox refuses a path it cannot resolve (dangling in-root symlink) instead of comparing it lexically; no fail-open fallback remains
