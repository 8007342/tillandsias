# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: hash-image-sources-git-normalized
    field: status
    value: completed
    ts: "2026-08-26T07:44:54Z"
    host: yoga

events:
  - packet_id: hash-image-sources-git-normalized
    event:
      type: completed
      ts: "2026-08-26T07:44:54Z"
      host: yoga
      summary: >
        status 'completed' with evidence_refs: cross-host hash identical on 3 checkouts / 2 OSes / 3 autocrlf-fileMode regimes at ef244da05; yolanda PASS both userlands with arms verified executed (3 markers each), flipped from a live FAIL baseline
