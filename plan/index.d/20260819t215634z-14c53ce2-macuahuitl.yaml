# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    field: next_action
    value: 'Wave 2: (1) seed select-work-batch.sh on hostname -s, not TILLANDSIAS_HOST_KIND (:232 falls back to the literal ''host'' on every bare-metal box); (2) add a packet-level neglect term so the 100 no-priority rows in answer.rs:1470-1478 rank-9 can ever be selected; (3) give archive-plan-packets.sh its one call site, --check first. REFUTED ALREADY: reseeding alone does nothing — three distinct seeds produced a byte-identical batch, because urgent= heads the batch and is a packet property.'
    ts: "2026-08-19T21:56:34Z"
    host: macuahuitl

events:
  - packet_id: three-horizon-cycle-model-arrival-control-by-routing
    event:
      type: note
      ts: "2026-08-19T21:56:34Z"
      host: macuahuitl
      summary: >
        831-ezea wave 2 handoff
