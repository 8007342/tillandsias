# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: the-test-and-runbook-tier-composes-with-shell-pipes-instead-of-steps
    field: status
    value: completed
    ts: "2026-09-04T13:45:51Z"
    host: linux

events:
  - packet_id: the-test-and-runbook-tier-composes-with-shell-pipes-instead-of-steps
    event:
      type: completed
      ts: "2026-09-04T13:45:51Z"
      host: linux
      summary: |-
        status 'completed' with evidence_refs: 5326cb97d: ./build.sh --check green 229s exit 0; ok:test-litmus-grep-escapes:8 (8/8, incl. the real pre-fix file refused at lines 55 and 64, and four false-positive arms: printf idiom, -F, -P, GNU \s/\w); ok:litmus-grep-escapes:421 checked on the committed corpus
  - packet_id: the-test-and-runbook-tier-composes-with-shell-pipes-instead-of-steps
    event:
      type: note
      ts: "2026-09-04T13:45:51Z"
      host: linux
      summary: |-
        grep-escape lint shipped full-corpus; allowlist corrected by measurement against /usr/bin/grep (\s and \w are GNU extensions, not violations); SIGPIPE left to 792-ksr8; scripts/*.sh widening named as follow-up
