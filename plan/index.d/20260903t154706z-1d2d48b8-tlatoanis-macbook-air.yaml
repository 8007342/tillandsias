# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: nine-hand-rolled-length-prefixed-framing-copies
    field: next_action
    value: |-
      MACOS IS DONE (this cycle): pty_vsock_bridge.rs production framing is on control_frame_codec(), framed once at the split with the SAME FramedRead threaded into reader_task, ratchet lowered 5 to 4. Do not re-migrate it, and do not migrate its TEST PEERS — they are the only thing in the file still asserting the wire format rather than the codec against itself.

      WINDOWS IS THE ONLY LANE LEFT: hvsocket.rs (2 decodes), same shape, and see 828-r2ek for the bounded-write half. Needs a Windows host.

      TWO THINGS TO FIX THAT ARE NOT LANE WORK, both found by running the ratchet by hand:

      (1) THE RATCHET IS NOT WIRED INTO ./build.sh --check — grep build.sh for check-framing-raw-decodes returns 0. It runs only inside litmus:one-length-delimited-framing-impl, and --check runs no litmus (748-tkjx). So the ratchet went blocked when qcow2.rs landed and the gate stayed GREEN. That is the orphaned-guard shape (865-n8vq) on the ratchet this packet built to prevent drift, and it means a new hand-rolled copy can land unnoticed. Wiring it is cheap and is the highest-value item left on this packet.

      (2) THE DETECTOR CANNOT TELL DISK FORMATS FROM WIRE FRAMING. It counts u32::from_be_bytes, so crates/tillandsias-vm-layer/src/qcow2.rs registered as a new framing site while reading a QCOW2 disk header field. Dispositioned keep with the reason at the site; the class will recur, so consider narrowing the detector to reads that feed a length-bounded body read.

      COORDINATOR DECISION STILL PENDING on exit criterion 1 being unsatisfiable as written. I did not restate it — a host that just migrated a slice should not also rewrite the criterion it was measured against. The proposed restatement is in the earlier next_action and litmus:one-length-delimited-framing-impl already executes it.
    ts: "2026-09-03T15:47:06Z"
    host: tlatoanis-macbook-air

events:
  - packet_id: nine-hand-rolled-length-prefixed-framing-copies
    event:
      type: note
      ts: "2026-09-03T15:47:06Z"
      host: tlatoanis-macbook-air
      summary: |-
        macOS slice complete; windows remains; ratchet unwired and detector over-broad
