# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: three-fixtures-assert-properties-of-the-environment-they-run-in-so-a-correct-refusal-scores-as-a-failure
    event:
      type: progress
      ts: "2026-09-14T17:53:16Z"
      agent_id: windows-esmeraldinha-opus5-20260914t175313z
      host: esmeraldinha
      summary: |-
        esmeraldinha ran part 2's closure measurement at the AT-RISK locus. CLOSURE NOT MET — both specs are red. LOCUS, stated because this one host disagrees with itself: Windows/Git Bash, LOCUS_UNAME=MINGW64_NT-10.0-26200, scripts/run-litmus-test.sh invoked DIRECTLY and never through ./build.sh, which re-execs into the tillandsias-build distro where the same git-identity probe reads SAFE. AT-RISK PROBE, re-executed inside the same script as the litmus runs rather than cited from earlier, so the evidence carries its own precondition: PROBE_INIT_RC=0, PROBE_COMMIT_RC=128, PROBE_VERDICT=AT-RISK, "Author identity unknown" / "unable to auto-detect email address (got 'bullo@Esmeraldinha.(none)')", TIER=[unset] (floor-tier by the operator's tiering; macuahuitl's reading is that the env var is the selector's routing, not this row's precondition). COUNTS: meta-orchestration PASS 20 / FAIL 3 / SKIP 1, 86% of 23 executed, 510s. forge-environment-discoverability PASS 15 / FAIL 9 / SKIP 1, 62% of 24 executed, 1163s. Both Status FAIL, runner rc=1. 2026-09-14T17:21:19Z to 17:49:12Z. THE THREE ORIGINALLY-FILED INSTANCES ARE GREEN HERE: committable-branch-guard-shape 7/7 including step 4, the no-git-identity arm, which is the one whose precondition actually bites at this locus. CLASSIFICATION of the 12 failures, because "12 red" is not a finding until it is split. (A) SIX are this packet's own shape. credential-channel-check-shape 8/10 proven CAUSALLY, one variable: the arm pins env PATH=/usr/bin:/bin, MINGW has git only at /mingw64/bin/git, so PATH=/usr/bin:/bin gives rc=1 out=missing:no-credential-channel while PATH=/mingw64/bin:/usr/bin:/bin gives rc=0 out=ok:forge-git-mirror. The guard is correct; the fixture never built the world it asserts about (next_action item 1d). FOUR more fail on "jq: Bad JSON in --rawfile text /proc/self/fd/0: Could not open /proc/self/fd/0" — MSYS has no /proc/self/fd — in forge-plan-expert-build-shape 10/17, methodology-path-query-citability 18/21, expert-groundtruth-harness 18/31, expert-capability-skew-honesty 16/29; counted here on the error text but NOT individually reproduced, and per this row's own converse rule a symptom match is not membership, so whoever fixes them must reproduce each. TWO grade git's CRLF warning as the verdict — salvage-net-roundtrip 9/9 and expert-refresh-cargo-target-shape 2/3 both captured "LF will be replaced by CRLF" into the compared variable; salvage-net-roundtrip step 9 failed at rc=0, a successful command scored red because stderr was graded as the verdict. (B) TWO are floor-tier BUDGET, not assertion failures: build-cache-sweep-trigger step 1/15 TIMEOUT at a 30s step budget (31.0s, killed and censored) and capability-manifest-guard step 1/1 TIMEOUT at a 300s step budget (300.6s, killed and censored). CAPABILITY-MANIFEST-GUARD IS ONE OF THE THREE ORIGINALLY-FILED INSTANCES, rewritten at af7529a26 under 1114-p2ht, AND IT DID NOT ASSERT FALSE — it ran out of time. This is NOT evidence that fix regressed, and reporting it as "red again" would have cost someone a wasted investigation. It IS evidence that a 300s single-step budget does not fit the floor tier, which is arguably this row's sibling: a fixture assuming a fast host is also asserting a property of its environment. (C) THREE left UNCLASSIFIED because their output was EMPTY — plan-answer-envelope-citability 6/21, citation-frame-and-caller-relation 8/8, project-answer-synthesis-refusal-typed 3/10. An empty output cannot distinguish a real assertion failure from a missing MCP server from a swallowed error, so I am not guessing; re-run with the arm's output captured to a FILE rather than into a compared variable. NOTHING WAS EDITED — part 2 is a measurement, and a measurement that fixes what it measures cannot be re-run against the thing it graded. The 6 in group A are candidates for part 1's enumeration and were found by RUNNING, not grepping, which suggests part 1's predicate sweep will undercount: none of the six would be caught by scanning for git-init-without-identity. Full report with the per-arm evidence: plan/issues/litmus-1109-t8kw-part2-esmeraldinha-2026-09-14.md, landed on origin/windows-next at 32bbd9c9d.
