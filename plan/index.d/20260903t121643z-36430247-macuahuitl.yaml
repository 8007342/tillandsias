# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: forge-launch-argv-is-validated-by-nothing
    event:
      type: finding
      ts: "2026-09-03T12:16:43Z"
      agent_id: linux-macuahuitl-fable5-20260903t121643z
      host: macuahuitl
      summary: |-
        THE FLAG APPEARS TWICE IN THE FORGE ARGV AND NOBODY KNEW, and only a live negative control could have found it. lenovinha removed --cap-drop=ALL from build_stack_common_args, rebuilt, and ran a real tillandsias . --opencode - AND IT LAUNCHED. For about a minute that read as their guard being dead. It was the guard being RIGHT: the flag is emitted TWICE, and the occurrence in build_opencode_forge_args still carried it, so the argv was compliant and there was nothing to refuse. Confirmed on the running container, which reported the full expanded CapDrop set. BOTH WRONG CONCLUSIONS WERE AVAILABLE AT THAT MOMENT. Stopping at removed a flag, launched anyway, guard is broken would have filed a FALSE DEFECT against their own working code. Stopping at the passing unit test would never have revealed the duplication exists at all. Only the live control separated them. With BOTH occurrences removed a real launch refused before podman was spawned, rc=1, no container created, the missing flag named. THAT IS MY CRITERION 3 EARNING ITS KEEP BY FAILING INFORMATIVELY FIRST - I wrote verify by making it fail on a real path, and the first attempt at that produced a result that looked like refutation and was actually a discovery. A SECOND DETAIL THAT WOULD HAVE REFUSED EVERY LAUNCH: run is prepended DOWNSTREAM by run_container_attached_observed, so the args this function receives are not what podman gets. Validating them directly returns MissingRunSubcommand for every correct launch. In their words, a check that refuses everything looks exactly like a check that works, until you run it - which is the inverse of a check that passes vacuously and just as invisible from the test suite.
