# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: strong-law-invoked-over-a-sequence-the-design-makes-dependent
    event:
      type: note
      ts: "2026-09-03T06:15:40Z"
      agent_id: linux-macuahuitl-claude-20260903t061539z
      host: macuahuitl
      summary: |-
        LAST CRITERION DISCHARGED, and the author closed it better than it was written. The criterion said "the explainer author is told so the published footnote can be re-pinned". They were told, and they DECIDED NOT TO RE-PIN YET — deliberately, not as a delay, and the reasoning is worth more than the action would have been.
        THEIR ARGUMENT: the fix is on trunk and not in a release. The entire value of pinning the page to v56.9.2.1 is that a reader clicking a line number lands on the line the sentence quotes. Re-pinning to a moving branch trades that for a freshness the page does not need, and every line range starts drifting with the next commit. So the quoted passage is now DELIBERATELY the pre-fix text, and the page's path-to-green says so outright rather than leaving a reader to wonder why a citation still shows the defect. At the next release carrying this fix they re-pin wholesale and re-verify all 105 ranges in one pass — which is the moment tag-pinning discipline exists for.
        That is the correct reading of what the criterion was FOR. I wrote it meaning "do not leave the public page contradicting the tree"; the page does not contradict the tree, it quotes a tagged version accurately and says the tree has moved. Marking this done.
        THE POINTER STAYS, and they argued for it independently before I asked. My flagged judgement was whether to leave a note in `principle` naming the original wording, or let the text simply read correctly. Their argument, which I am adopting as the general rule rather than a one-off: an authored principle carrying `authority: "The Tlatoāni, 2026-08-13"` that silently comes to say something different IS AN INSTRUMENT THAT STOPPED MEASURING — the same family the fleet catalogued all night, applied to a document instead of a test. A reader who remembers the old sentence and cannot find out what happened to it has been handed a tree that quietly disagrees with their memory, and the honest response to that is to stop trusting the tree. Tidiness is not worth an audit trail. The operator can still overrule with a one-line deletion; nobody should do it silently.
        WHAT THE PAGE NOW CARRIES from this exchange: that the claim was withdrawn at BOTH sites and withdrawn rather than weakened (they had only ever quoted the detailed block and would have called the fix complete without knowing `principle` carried the same claim in the key a reader meets first), and the blast-radius sweep — nothing machine-reads the block, so the remedy is documentation-only. Their reason for wanting the second: a reader assessing a red flag deserves to know whether its remedy was a one-line doc edit or something with consumers to break.
