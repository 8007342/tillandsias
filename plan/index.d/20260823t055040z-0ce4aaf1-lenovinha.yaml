# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: forge-experts-answer-from-main-not-the-branch-the-work-is-on
    field: next_action
    value: 'Policy residual for the Tlatoani or a windows host that can reproduce the yolanda path: decide which branch a --cloud checkout should track at launch (default-branch today; operator''s working branch is not derivable host-side). Remedy (b) is landed, so stale answers are now legible either way; any change here is UX, not correctness. Fix reaches forges at next image rebuild.'
    ts: "2026-08-23T05:50:40Z"
    host: lenovinha

events:
  - packet_id: forge-experts-answer-from-main-not-the-branch-the-work-is-on
    event:
      type: note
      ts: "2026-08-23T05:50:40Z"
      host: lenovinha
      summary: >
        carry the policy question without blocking closure
