# Ledger fragment — append-only, IMMUTABLE once written.

events:
  - packet_id: plan-archiver-check-is-thirty-percent-of-every-gate
    event:
      type: completed
      ts: "2026-09-02T18:44:00Z"
      host: macuahuitl
      summary: >
        DONE (b300d6b92). The ready-set check (831-ezea) is memoised on a
        digest of exactly its inputs — plan/index.yaml, plan/archive,
        plan/index.d, the archiver, its checker and the plan binary (resolved
        through plan-binary-probe.sh) — so on an unchanged ledger it is not
        re-run at all, which is the closure's "stops scaling" in its strongest
        form; any byte of ledger or instrument is a miss and runs it. Measured
        this cycle: first gate ran it (miss:no-memo), second gate hit
        (ok:archiver-check-memoized) and the whole --check took 88s. Earlier
        measurement stands: 16.7-19.6s -> 6.8s after sweep + compaction,
        ledger-bound. evidence_refs: scripts/archiver-check-memo.sh;
        scripts/test-archiver-check-memo.sh "PASS: archiver-check memo 6/6
        (911-m7js)" wired into ./build.sh --check; gate log 2026-09-02
        ~19:00Z on macuahuitl.
  - packet_id: fourteen-ghost-traces-are-only-reported-in-files-a-commit-touches
    event:
      type: completed
      ts: "2026-09-02T18:44:00Z"
      host: macuahuitl
      summary: >
        DONE (b300d6b92). Criteria 1 and 4 were met by the ratchet (584-2qq2,
        lenovinha 2026-08-23); criterion 3 landed here: with markdown in the
        gate the baseline stood at 18 ids, and every trace to a spec that does
        not exist was re-pointed to the spec that owns the contract
        (agent-source-of-truth -> knowledge-source-of-truth x15,
        chromium-browser-isolation -> browser-isolation-core x5,
        container-health -> podman-idiomatic-patterns x4, litmus evidence
        lines to each test's own bound spec, docs examples to real specs) or
        dropped where it was decoration (forge-container, forge-skills-mapping,
        enclave-startup-sequencing). Baseline 18 -> 1: ephemeral-guarantee
        remains because three litmus tests declare it as their OWN spec and
        bind under it — the spec file is owed, a spec decision recorded in the
        baseline, not a cleanup. Criterion 2 (the two 100-day-old
        openspec/changes ghosts) is the operator's: promote or archive
        macos-tray-build-and-release and windows-wsl-runtime — openspec/changes
        is excluded from the scan as proposal prose, so they no longer inflate
        the count. evidence_refs: scripts/trace-coverage.sh --gate
        "ok:ghost-trace-gate baseline=1 current=1"; "trace_coverage: specs=185
        traced=184 ghost=1 annotations=4958 files=1585"; negative control: a
        fabricated @trace in a script made the gate name it (2026-09-02);
        mirror regenerated via scripts/stage-image-cheatsheets.sh (ok
        files=243); litmus:cheatsheet-host-image-sync Status PASS.
  - packet_id: full-gate-pass-livelocks-against-a-fast-moving-trunk
    event:
      type: completed
      ts: "2026-09-02T18:44:00Z"
      host: macuahuitl
      summary: >
        DONE (b300d6b92), by a simpler mechanism than the deliverable named.
        The deliverable asked for a merge-check-push helper with a plan-only
        retry arm; the gap it identified was that a stale stamp is judged
        against the whole tree rather than against what moved. Judged at the
        stamp instead: scripts/gate-stamp.sh no longer hashes the plan fast
        lane (plan/index.d/*.yaml, plan/loop_status.d/*.md,
        plan/mo-full-attestations.d/*.md) — the append-only ledger traffic the
        plan-only push lane already accepts without a stamp (668-2xeh) — so a
        sibling burst rebased into a checkout cannot stale a stamp that
        remains true of every byte of code, and no retry arm is needed. The
        compacted base plan/index.yaml and everything else still hash.
        Exit criterion 1 (one gate pass through a plan-only burst): case 7 of
        the fixture commits a fragment + loop-status + attestation burst on
        top of a stamp and verify stays ok:gate-fresh. Criterion 2 (NEGATIVE
        CONTROL — code movement still re-gates): case 8, a plan/index.yaml or
        code change reads stale:tree-changed-since-gate. Lived cost that
        motivated it today: three merge-check-push rounds on one batch.
        evidence_refs: scripts/gate-stamp.sh compute (930-i6x4 block);
        scripts/test-gate-stamp.sh "PASS: gate-stamp (9/9)"; the other four
        gate-stamp fixtures unchanged and green; ./build.sh --check green.
