# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: linux-mutable-20260810t193000z
events:
  - packet_id: git-mirror-cross-project-service-identity
    event:
      type: progress
      ts: "2026-08-10T19:30:00Z"
      agent_id: linux-mutable-20260810t193000z
      host: linux_mutable
      summary: >
        Design half of the packet's next_action executed: amended the UNSIGNED
        SSH-CA design (plan/issues/ssh-ca-forge-mirror-push-design-2026-07-31.md)
        to remove every shared-identity and wildcard-authority artifact the
        zero-trust audit's corrections 1-2 name. Per section: D3 principals are
        now til:forge-push:<mirror-id> (opaque, with the project<->mirror-id
        ledger join for readability); D9 host certs certify exactly one
        per-project hostname git-<mirror-id>, never the shared aliases; D12
        signer identities are per-project with exact sign paths, policies
        MINTED at provision (no static wildcard file ships); NEW D13 defines
        the opaque mirror-id scheme (12-byte CSPRNG random - never derived from
        the guessable project name - persisted once in Vault kv
        secret/mirror-identity/<project>, distributed only through 659-8faj's
        single derivation function; DNS opacity is defense-in-depth, certs are
        the control); section 2.3's role JSON now creates client role
        <mirror-id> and per-project host role host-<mirror-id> with
        allowed_domains git-<mirror-id> (shared roles/mirror-host withdrawn);
        the sign/* policy sample is withdrawn and replaced with exact minted
        pairs; the enclave-wide source-address is explicitly demoted to
        defense-in-depth (it never separated projects); ladder rungs
        T1/T2/T4/T5/T8/T10/T11 updated to match; NEW section 4a enumerates the
        negative two-project matrix M0-M7 (DNS uniqueness, cross-sign 403,
        cross-cert rejection, host-cert exactness, fixed repo path, audit
        completeness incl. the V12 T9 blocker, identity confidentiality) as
        the executable gate before 451 flips any lane. Doc remains UNSIGNED;
        Q1 and Q2 remain open and operator-gated (Q2 re-raised by the operator
        2026-08-10). Residual for this packet: the implementation itself -
        vault ssh mounts + per-project roles/policies minting + sshd + the
        matrix fixtures; criterion 3's DNS half is in flight as 659-8faj.
