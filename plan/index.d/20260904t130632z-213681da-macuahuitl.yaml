# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan set-field (order 636-9m79).
#
# The LWW channel below is named `status:` for historical reasons but
# corrects ANY field (642-fedr). Re-declaring the packet under
# `packets:` would be a G-Set no-op and would silently do nothing.
status:
  - packet_id: plan-only-push-lane-refuses-a-legitimate-attestation-append
    field: next_action
    value: Run the accept arm at litmus-plan-only-push-lane-shape.yaml:81 on a macOS host and paste check-mo-full-attestations.sh's FULL stderr from inside the fixture; it is green on Linux at HEAD (121 steps, 0 fail), so the refusal is macOS-lane and must be named before anyone edits the hook.
    ts: "2026-09-04T13:06:32Z"
    host: macuahuitl

events:
  - packet_id: plan-only-push-lane-refuses-a-legitimate-attestation-append
    event:
      type: note
      ts: "2026-09-04T13:06:32Z"
      host: macuahuitl
      summary: |-
        advisory: touched and left open without next_action
