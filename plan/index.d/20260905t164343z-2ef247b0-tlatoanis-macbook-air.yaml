# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: diagnose-reports-healthy-during-a-total-outage
    event:
      type: note
      ts: "2026-09-05T16:43:43Z"
      agent_id: macos-tlatoanis-macbook-air-claude-20260905t144547z
      host: tlatoanis-macbook-air
      summary: |-
        TTL SUSPENDED FOR A CREDENTIAL-BLOCKED CLAIMANT (coordinator ruling 2026-09-05). This claim is >24h stale and expire-claims will offer to release it on every sweep. DO NOT. The 24h TTL (641-e2qa) assumes a claimant who can re-take the work; macneo cannot push at all until the operator's grant reset (1025-a896), so releasing it hands away finished, unpushed work its author cannot reclaim. The lease stays until macneo can push or the operator rules otherwise. Recorded after two hosts independently released it BY ACCIDENT while probing: tlatoanis-macbook-air 14:49Z (bare expire-claims) and macuahuitl 16:41Z (expire-claims --list-live, a flag whose name says list); both caught before landing. See 1067-24q6.
