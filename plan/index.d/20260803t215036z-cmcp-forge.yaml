# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: forge-tillandsias-codex-20260803t214004z
# Read transparently by tillandsias-plan; folded into plan/index.yaml by compaction.
# Do not edit this file. To change something, write a NEW fragment.
packets:
  - packet_id: codex-mcp-config-registration
    order: 600-xrqk
    status: ready
    desired_release: v0.5
    release_target: harness-mcp-expert-validation
    title: "Register forge-plan and project-info with Codex at forge launch"
    kind: implementation
    deliverable: images/default Codex entrypoint/config overlay + launch fixture
    depends_on: []
    blocks: [codex-mcp-expert-validation]
    pickup_role: linux
    capability_tags: [forge, codex, mcp, experts, config-overlay]
    estimated_hours: 2
    outcome: >
      A pristine Codex forge discovers the same local expert MCP servers that
      OpenCode and Claude are configured to discover, without a user running
      `codex mcp add` by hand and without overwriting Codex auth or user state.
    next_action: >
      Add an idempotent Codex config merge in entrypoint-forge-codex.sh (or a
      Codex config-overlay template) for forge-plan.sh and project-info.sh, then
      exercise it against an empty and a pre-populated Codex config fixture.
    exit_criteria:
      - "a pristine Codex forge reports forge-plan and project-info in `codex mcp list` at launch"
      - "the running Codex tool surface exposes plan_answer, methodology_ask, spec_answer, and project-info tools without a manual config mutation"
      - "registration is idempotent and preserves auth.json plus unrelated user MCP/config entries"
      - "a behavioural fixture starts from no MCP entries, runs the launch merge twice, and proves both servers appear exactly once"
      - "a fresh order-558 validation records launch-time tool calls; a manual post-launch rebuild or `codex mcp add` does not count"
    owned_files:
      - images/default/entrypoint-forge-codex.sh
      - images/default/config-overlay/codex/
      - openspec/litmus-tests/

status:
  - packet_id: codex-mcp-expert-validation
    field: status
    value: ready
    ts: "2026-08-03T21:50:36Z"
    host: forge-tillandsias-codex-20260803t214004z

events:
  - packet_id: codex-mcp-config-registration
    event:
      type: filed
      ts: "2026-08-03T21:50:36Z"
      agent_id: forge-tillandsias-codex-20260803t214004z
      host: forge
      summary: >
        Filed from order 558 launch evidence: `codex mcp list` reports no
        configured servers and the Codex config has no MCP registration, while
        direct stdio `tools/list` proves forge-plan itself is live and exposes
        the expected tools. This child owns registration only; order 570 owns
        the separate TILLANDSIAS_AGENT routing signal.
  - packet_id: codex-mcp-expert-validation
    event:
      type: progress
      ts: "2026-08-03T21:50:36Z"
      agent_id: forge-tillandsias-codex-20260803t214004z
      host: forge
      lease_id: 010b3273f8f8
      summary: >
        Codex launch validation RED and recorded in
        plan/issues/codex-mcp-expert-validation-2026-08-03.md. Criterion 1:
        Codex has no MCP servers configured. Criteria 2/3: the raw forge-plan
        server advertises the tools, but the launch-installed pre-manifest
        binary returns honest verified unsupported envelopes with
        skew=relaunch-required. Shaped missing Codex registration as 600-xrqk.
        Packet returns to ready for a fresh post-fix Codex launch; no manual
        rebuild was used as false launch evidence.
      partial_artifact_refs:
        - plan/issues/codex-mcp-expert-validation-2026-08-03.md
        - codex-mcp-config-registration
  - packet_id: hooks-shipped-but-never-installed-audit
    event:
      type: progress
      ts: "2026-08-03T21:50:36Z"
      agent_id: forge-tillandsias-codex-20260803t214004z
      host: forge
      summary: >
        Linux/forge activation slice completed while enforcing the order-558
        push gate: install-hooks.sh now writes to Git's effective core.hooksPath,
        gate-stamp hashes tracked directory symlinks without following them,
        and litmus:release-gates-run-locally passes 15/15. Packet stays ready:
        enumerate remaining controls and collect Windows/macOS checkout evidence.
  - packet_id: release-gates-moved-to-local-hardware
    event:
      type: progress
      ts: "2026-08-03T21:50:36Z"
      agent_id: forge-tillandsias-codex-20260803t214004z
      host: forge
      summary: >
        Post-completion falsifier repaired: the first gate stamp followed the
        tracked skill symlinks into directories, so `write` failed on every
        real Tillandsias checkout despite the regular-file fixture passing.
        New behavioural symlink fixture reproduces the real shape and passes.
