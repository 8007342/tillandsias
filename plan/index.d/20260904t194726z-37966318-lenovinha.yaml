# Ledger fragment — append-only, IMMUTABLE once written.
# Written by: tillandsias-plan append-event (orders 699-usxc, 966-7umc).
#
# Events go to the OVERLAY, never to plan/index.yaml: a base write falls
# outside the plan-only push lane and would oblige the writer to pass a full
# ./build.sh --check, which is precisely the gate a floor host or a forge may
# not be able to run. The fold reads events from fragments, so every reader
# sees this exactly as it would a base append; `compact` folds it into the
# base later, on a host that can afford the gate.
events:
  - packet_id: enclave-health-check-satisfied-by-podman-rm
    event:
      type: evidence
      ts: "2026-09-04T19:47:26Z"
      agent_id: linux-lenovinha-claude-20260904t193514z
      host: lenovinha
      summary: |
        FIXED AND LANDED d08566639 (verified an ancestor of origin/linux-next before this event was written). `podman rm` no longer satisfies the enclave health check.

        QUESTION (1) SETTLED FIRST, because the packet says it changes the fix: it was the BARE SCRIPT, not cycle-preflight. Reproduced LIVE on this host before any edit — the proxy is still absent here from this morning's orchestrate-enclave.sh teardown, and `env -u TILLANDSIAS_ENCLAVE_EXPECTED_SERVICES scripts/check-enclave-service-health.sh` printed `ok:enclave-service-health:services=5:up=5:down=0:dead=0:absent=0` while images/default/enclave-services.txt declares tillandsias-proxy required. So the packet's description is exact and this is 994-8r3w's residual, not a failure of its wiring.

        QUESTION (2), the vault anchor, moved into the check WITH the default — and moving only the default would have re-created the same caller-dependent split one layer down. A never-provisioned laptop has no vault, is missing nothing, and must still read ok; a check that fires on correctly-configured hosts gets switched off, which is how the original gap survived. Arm (c) exists for exactly that and is as load-bearing as the defect arm.

        FIXTURE FIRST, RED BEFORE GREEN, in that order as required.
          PRE-FIX  arm (b) FAILED verbatim: "1004-inkc(b) REMOVED READ AS HEALTHY — 'podman rm' satisfied the check: 'ok:enclave-service-health:services=1:up=1:down=0:dead=0:absent=0'"
          POST-FIX 23 passed, 0 failed.
        Arm (b) passes NO --expect on purpose. Scenario 6 already proved the absent loop works when a declaration is PASSED IN, and scenario 6 passed throughout the entire period this defect was live — that is precisely why it did not catch it. An arm that only exercises the supplied-declaration path cannot see a defect in the default path.

        WHAT CHANGED IN THE CHECK: it reads images/default/enclave-services.txt itself (path from its own root), applies the vault-anchor rule, counts `services=` as the expected set plus extras so a deleted member cannot SHRINK the total, and prints the computed absent on the ok line instead of a literal 0. The shrink was half of why the two readings looked like an improvement — 6 down to 5 reads as "one fewer thing to worry about" rather than "one thing vanished". The literal 0 is always 0 on that path, but a verdict asserting something it never measured helps nobody who later changes the absent logic.

        A NEW EXPLICIT OPT-OUT, `--expect none`, and the reason is worth recording. Once empty means "use the default", there is no way left to ask for pure enumeration — a caller wanting the old semantics would have had to pass a fake service name. The legacy fixtures need it: they model a vault WITHOUT a proxy while testing exit codes, origin labels and stale-healthy, and without the opt-out they began reporting a proxy absence they were never written to model. Three of them went red on my first post-fix run for exactly that reason, and coupling every unrelated arm to the fleet's declared set would have been a worse outcome than the defect.

        THE FAKE PODMAN gains `container exists`, modelled AGAINST PS_OUT rather than stubbed: answering yes unconditionally would make the never-provisioned arm (c) pass for the wrong reason, and answering no unconditionally would silently disable the default expected set in every scenario — the fixture would then be green while testing nothing, which is the shape this fleet has spent the day removing.

        VERIFIED ON THE REAL HOST, not only in the fixture:
          before  ok:enclave-service-health:services=5:up=5:down=0:dead=0:absent=0
          after   degraded:enclave-service-health:services=6:up=5:down=0:dead=0:absent=1
                  fail:enclave-service-absent:service=tillandsias-proxy:state=absent:...
          preflight consuming that verdict: ok:cycle-preflight:rebuilt+expert-current+services-down0-absent1
        Note the count went 5 -> 6 rather than shrinking, which is the second half of the fix visible in one line.

        CYCLE-PREFLIGHT now CONSUMES the verdict: its expected-set derivation and anchor block are deleted, and it passes nothing deliberately — an explicit --expect there would restore the caller-supplied behaviour this order removes, one layer up.

        litmus:enclave-service-health-shape green (4/4, pre-build instant). Gate green 269s.

        ONE OBSERVATION I COULD NOT EXPLAIN, recorded rather than dropped: my first run of the fixture after adding the arm reported "21 passed, 2 failed" with only ONE FAIL line printed. Four consecutive re-runs before touching the check reported 22/1 with only my arm failing, and I could not reproduce a second failure. The extra failure is unidentified. It is not the three legacy arms that failed later — those failed only after the check change, for the expected-set reason above. Flagged in case another host sees an intermittent in this suite; I am not asserting a flake from one observation.
