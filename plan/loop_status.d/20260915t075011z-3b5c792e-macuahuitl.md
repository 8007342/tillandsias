## Cycle 2026-09-15T07:39:00Z — macuahuitl, 4h meta cron (full): 1176-9vqn — the release freeze gets a mechanism

daily-maintenance: was due:stale after the host upgrade and reboot; body run and stamped ok:daily-maintenance-stamped:2026-09-15
(post-restart: mcp-experts source_commit==HEAD, clock synced, hooks installed, capability row current, containers.conf proxy
absent; daily: nix-toolbox 13/13, nix-deps-stable, delegate sweep no-registered-runs, scratchpad 32G/1%). Credential:
ok:gh-keyring-push-verified-refstate-refused. Build-cache sweep reads due at 48 GiB and is deferred to the END of the cycle
per build_cache_hygiene.
Drained 1176-9vqn — coordination-shaped by construction: the freeze is the coordinator's own declaration and nothing enforced
it. Built scripts/release-freeze.sh (set|clear|status minting refs/tillandsias/freeze/<branch>/<host>/<epoch>, the ref NAME
carrying who and when so one ls-remote answers), the hook's section 2b consulting it AT the push for CODE pushes only, the
19-arm fixture with the real hook and a content-built mutant, litmus:release-freeze-holds-code, gate step 075-1176-9vqn, and
the cut runbook's set-at-gate-start / clear-before-the-back-merge-push. TWO REAL DEFECTS FOUND WHILE BUILDING IT: the hook
gated the freeze tool's own push (a freeze that needs a green gate on the host mid-cut is not a mechanism — refs/tillandsias/*
is now an exempt marker namespace), and the cut's own back-merge push is a code push to the frozen branch, so clear must
precede it. The open fetch-failure trade the filing row left is DECIDED in the hook's comment: fail OPEN, loudly, because the
check queries the same remote the push is about to contact — failing closed adds no protection where it matters and strands
offline hosts. Six existing hook fixtures re-verified green after the ref-classification change.
Fleet in the window: lenovinha closed 1196-5hva (blocked_by takes capability AND host tokens, the operator's "use both: CRDT
style"); nothing to relay, drift zero.
tokens: cycle=mo-full-20260915T0739Z main_ctx=115000 subagent_tokens=0 agents=0 by_model=none.
land: scripts/land-on-platform-branch.sh linux-next 4 (full gate: the hook, a new script, a fixture and a gate step).
