## Cycle 2026-09-20T13:30Z→2026-09-20T16:00Z (linux — macuahuitl, coordinator: the installer reset lands as one contract with three bodies; yolanda's Windows push returns after three per-file costs; the relay lane learns six one-second refusals and runs them first; two rulings held against peers' fixes)

Direction ("We're giving forge agents local EXPERTS"): served by keeping trunk integrating through six lands and every level, and by rows that make the gate say in a second what it said at minute twenty (1305-udgs with its front door, 1307-ermc, 1306-ifhv).

DONE, verified on origin by content: LAND 27 319c1e7e0 (yolanda's 1276-2hc6 litmus in awk with the case idiom; macbookair's Rust seccomp derivation in headless with the enclave-membership bullet, the python-in-guest script retired; yolanda's Windows reset arm v2 with its exit criterion RUN: a broken distro, --reset-state exit 0 in 68 s cold, diagnose Ready after, credentials cleared, tillandsias-vm-uuid kept, the installer probing the tray's --help for the flag by content). LAND 28 ef0446001 (yoga's 1285-vz27: set-field declares its flag vocabulary and refuses an unknown flag by name; seven cycles on yoga, none on the parser, relayed because a 19-minute gate cannot win a 1-in-4 race). LAND 29 69f368e08 (pirria's tillandsias_core::reset_state: the guard, the printer, RESET_NO_REPROVISION_PATH and the one skipped-case line as constants every body imports; two green gates on pirria arrived late, the third killed cleanly). LAND 31b b91b4f353 (yolanda's 1307-kic6 hook enumeration 18 min→2.3 s and gate-stamp movers ~80 min→7 s, proven byte-identical by an equivalence fixture that caught MSYS binary-mode digests on its first run; a first `find .` fix removed the spawn and still cost 132 s, rejected by the output-shaped closure). LAND 32b c73d796f2 (yolanda's Windows tray imports the contract; its look-alike skipped-case line deleted; one attributed whitespace fixup). LAND 33b 07f397fd5 (attempt 2 adopting the stamp over pirria's plan-only push: pirria's Linux --reset-state body v3, its unshare fallback through podman_cmd_sync().status_bounded after v1 was refused by the cross-target compile and v2 by the bounded-podman check; pirria's install.sh calling the flag behind a pre-flight guard, with the seven-step litmus arm bound and rewritten to the runner's contract after the first version was found never to have executed, the runner's [PASS] line quoted; yolanda's status-loss checker batched to three per-file plan-binary calls with a clippy needless_borrow fixup attributed on this side, 400+ s to 9 s on their host). Plan-only: macneo's macOS survey, yolanda's rows 1307-kic6 (the push returns) and 1308-9ej7 (build-guest-binaries cannot run on Windows: no Nix, no strings, no aarch64 musl gcc; a staged binary lacking listen-vsock refused at staging time), 1276-2hc6 CLOSED by the tool by order (a hand-written closure with a truncated packet_id had declared a phantom packet and refused land 31), 1300-k7wq (esme), 1303-vt3y (macbookair), the 1305-udgs front door, pirria's 1309-fhxb (the ci-release runner reports FAIL for an advisory verdict and for a named skip:no-cargo, both exit 0 and assert nothing; unscoreable on remedy: two success_patterns or one shared runner grammar) and 1309-q4kr (the smoke runbook sends the operator to cargo after §2 destroys the builder; recreate it by one named command, and refuse cargo inside the forge when it is typed, citing 1042-svey), yolanda's 1308 cross-toolchain note (the builder lacks CROSS toolchains, not toolchains: no aarch64 musl gcc, no mingw). All three branches equal at every pass end.

RULINGS: 1286-4437 is ONE CONTRACT, PER-PLATFORM BODIES (yolanda measured the Windows tray's --reset-guest already had the semantics; macneo measured the macOS tray's credential clearing has no production caller and announces it anyway, 1260-2qgi family): the guard and the line live in core and are imported, never agreed (pirria; corrects my "one host's text wins"); TILLANDSIAS_DESTRUCTIVE_RESET_OK=0 is the ONLY skip (pirria withdrew the variable all three arms and I had agreed); no litmus asserts "--reset-state does strictly more than --reset-guest" (true on Linux, false on Windows); installers call the flag only after the binary is in place; --reset-state fails the install on exit 1 only. NO LITMUS BUDGET IS RAISED: macneo's 120 s on the reconciliation fixture (a 30 s budget between a 3 s skip path and a 36 s corpus sweep, red on exactly the changes it checks, 5/5 assertions green both times) becomes "exclude the sweep from the fixture's subject" with both numbers beside the budget. lenovinha's finding 2 (the ssh lane with its host-CA cache absent falls back to the anonymous git:// push) is its own p1 row and lenovinha fixes it fail-closed in the same pass; finding 1 (the cache is a plain file read; nothing touches the keyring on the push path) holds the one-read-per-boot premise. macbookair's tray-side writer is a wire-protocol change, not a file write: closure test first, the variant as its own row. yoga's 1303-2d5g/1304-wbb2 to esme; 1304's rule corrected by esme's audit (membership, at least once; 69 misbound incl. a tray platform-swap pair, 16 unbound files never run, one binding mine from land 21); the reconciliation guard's red is the measured cause (macneo), repaired first. yolanda's status-loss checker: batch all three plan-binary calls with PER-FILE reporting, never collapse a verdict (theirs; landed in land 33b).

THE RELAY LANE CHANGED, from six refusals at minute twenty in one day: lands 25 (python3 in a litmus command), 26 (if-not pipeline guards), 26b (a missing enclave-membership bullet), 30 (the cross-target compile: a cfg-gated fn called unconditionally, an unguarded libc::getuid), 31 (plan-orders: a phantom packet from a hand-typed id), 32 (a direct podman Command, 714-4r6w), 33 (clippy 1.98 needless_borrow, in the fast tier so only a minute). scripts drafts-macuahuitl/relay-one.sh and relay-many.sh now run the gate's own deciders on the merged tree before any gate: no-Python, if-not, sigpipe, litmus parse, added fragments, cheatsheet refs and frontmatter, enclave membership, scorable obligations, podman-sync budgets, rustfmt, the cross-target compile for refs touching crates/, plan-orders and the plan check for refs adding fragments. Two relay-side fixups, each its own attributed commit, each mechanical (a blank line; three double borrows); yolanda then found the builder's toolchain reachable from their host through the build distro and runs the gate's rustfmt and clippy there from now, leaving only the mingw cross-compile to this host. A salvage ref is a SNAPSHOT: containment is checked by content against trunk, never by ancestry, and a plan-only fold copies fragments by path when the snapshot also carries code. 1305-udgs gains the front door (`./build.sh --preflight` running the fast tier alone), so the lane and every host read one list.

CORRECTIONS OF MINE, each recorded where it happened: the fallback hypothesis for yolanda's third push cost (refuted: the batched fold works; the cost was three calls per fragment); the budget-kill hypothesis for 1306-ifhv (refuted structurally by yoga: the passing arms read the LAST line; then CONFIRMED as the 1076-kft9 class by the fixture's own shape, `printf "$out" | grep -q` under pipefail, proven in isolation with a size threshold, 23 KB clean / 66 KB SIGPIPE, reachable only when gate load shrinks pipes to one page); the line numbers I relayed for land 30 (the warnings' addresses in the gate's filtered summary, not the errors'); "a headless unit test depends on host CA state" (pirria had run it inside the forge image, which has no openssl by the tree's own ruling; green in the builder and in this gate twice; the row is the runbook's: a destructive reset leaves no builder and cargo in the forge is not refused); "yolanda's string wins" (import the constant). Peers' corrections of their own, on their rows: yolanda's "the push hangs" and "hang after the verdict" (a slow step: 155 ms per spawn), pirria's "the gate is not catching a red test", macneo's three near-misreadings of the budget straddle, yolanda's "cannot run the gate's rustfmt" (the builder's was reachable).

FLEET STATE: macuahuitl: after land 33b, land 34 (lenovinha's 1309-qc95 from work/1309-qc95: the ssh lane fails closed when enabled and unwired) and then macneo's macOS body once its reconciliation fixture asserts the rule on a throwaway corpus (esme measured the floor tier: the sweep-skipped path alone costs ~90 s against the 30 s budget, so the sweep exclusion stays red there; a remedy has a regime too). yolanda: close 1307-kic6 by order (the push returns in 50 s with a correct named refusal), then 1308-9ej7; preflight in the builder from now. pirria: both rows filed (1309-fhxb, 1309-q4kr); next the green run of litmus:installer-reprovisions-on-install on trunk after land 33b, then two events on 1278-qbia (a new remote branch has no base for the plan lane) and 1287-h6qn (the stale plan binary refusing a pure fragment push it had itself validated minutes earlier). macneo: the macOS body with the sweep excluded and the allowlist line deleted, SHA pending. esme: 1303-2d5g, 1304-wbb2 (guard first, reconciliation repaired first, corpus cleanup second with per-file decisions and an explicit opt-out marker), 1258-8wfb to close. lenovinha: the fail-closed row and fix, then the ssh lane on lenovinha only, then arm 1. yoga: close 1285-vz27, then 1299-s2sv (survey first). macbookair: 830-xsk2's closure test, then the protocol-variant row. Unclaimed: 1305-udgs (a Linux builder's), 1306-ifhv, 1307-ermc, 1301-ie39, 1302-7j8p, 1300-k7wq.

OPERATOR: nothing is blocked. On the next cut, `curl | bash` and `irm | iex` reprovision and reset the local state on install on Linux and Windows (macOS lands with macneo's body), preserving the installation identity, with TILLANDSIAS_DESTRUCTIVE_RESET_OK=0 as the only opt-out; the Windows tray previously ignored that variable entirely and the macOS tray never cleared the credentials it announced clearing. A trunk push from the Windows host returns now; it never did. Six one-second gate checks that each cost a twenty-minute relay today now run first on this host and are filed to run first in the gate.

tokens: subagent_tokens=0 agents=0 (no sub-agents this cycle)
