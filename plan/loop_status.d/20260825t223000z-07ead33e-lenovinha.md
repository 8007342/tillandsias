## Cycle 2026-08-25T22:30:00Z — lenovinha (linux_immutable, linux-next, coordinator-directed)

Coordinator-directed work, not a batch drain. macuahuitl is orchestrating an operator-directed
release and asked for the promote-stable evidence gate to be made platform-aware BEFORE the
promotion. Took it; landed it. Also standing as backup Linux validator (yoga primary) — no
duplicate curl-install e2e run.

### 888-m75r — the promotion evidence gate was platform-blind AND passing on junk

Two defects the coordinator measured, both reproduced here against the live ledger before any
code changed:

1. It asked only whether SOME file under `plan/` asserted a PASS for the version. We ship three
   platforms; one platform's PASS satisfied the gate for all three.
2. For v0.4.260817.1 the two files satisfying it were `plan/index.yaml` — whose packet body
   DESCRIBES this gate's own past defect, so "smoke", "PASS" and the version co-occur while
   describing a bug — and a freshness-audit line in one of my own loop_status entries. The real
   Windows report was reachable only via a filename fallback that never ran.

**The two were jointly concealing a fact that changed the night's plan**: v0.4.260817.1 has
evidence for exactly ONE platform (windows). No linux, no macos. Surfaced that to the
coordinator before finishing the implementation, because it moved their critical path — they
cut a fresh daily instead of promoting it, and had already reached the same conclusion by an
independent route (804-bpke unfixed in any published release).

Fix: per-platform requirement (`TILLANDSIAS_REQUIRED_PLATFORMS`, default all three), refusal
names the missing platforms, ledgers excluded outright, and platform read ONLY from filename +
first heading. Whole-file platform detection was not an option — the genuine Windows report
contains "that lane is Linux/Podman", which would award it linux coverage. That is defect (2)
one level down, deciding COVERAGE instead of existence.

**Not requested, found while verifying, and the sharpest of the three.** The legacy second pass
accepted any file with e2e|smoke + PASS + version on one line. Measured on the real
`plan/issues/windows-next-work-queue-2026-07.md`, it accepted a line reading *"run PASS
end-to-end ... but promotion verdict for the tag UNCHANGED (morning FAIL)"*. A line stating the
promotion verdict is FAIL was counting as promotion evidence. With report identity required
that pass is a strict subset of the first, so it was removed rather than narrowed — one rule
instead of two. This narrowed a pinned test case (case 5), which I flagged explicitly to the
coordinator rather than changing quietly.

### Coordinator ruling honoured, and a follow-up filed

A report can assert PASS while disclaiming promotion clearance ("run-scoped; does NOT clear
promotion"). Ruling: the matcher counts a PASS as a PASS — chasing unbounded prose rots, and
eventually fails dangerously when new phrasing reads as clearance — but it must WARN and name
the report. Implemented. 888-79x3 filed for the machine-readable clearance field that removes
the residual properly; deliberately not built tonight.

### 889-bx99 warning channel — done, and one thing deliberately not done

macbook found the macOS smoke's destruction step targeting `.../tillandsias` while the app
writes `Tillandsias`: on a case-sensitive volume it matches nothing, exits 0, and the smoke runs
against a pre-existing image while reporting a clean room. A false PASS on a PRECONDITION, on
the path this gate consumes as evidence.

The accept path now names which report carries each platform, with the caveat that a PASS proves
the run completed and not that its preconditions held. Negative control: a refusal prints no
evidence list, or a refusal reads as though something was accepted.

I did NOT correlate evidence dates against known precondition defects. It needs a
hand-maintained registry of defect windows, and a registry nobody updates goes quiet exactly
when it matters — reading as "no known defects" when it means "nobody wrote one down". That is
the same failure shape as the gate being fixed.

### Verification

25/25 (was 10). New negative controls: each platform alone refused naming the other two;
two-of-three refused naming the third; ledger co-occurrence not evidence; the work-queue FAIL
line not evidence; a windows report whose body mentions Linux does not gain linux coverage;
--force still overrides and names what it overrode; the disclaimer warning fires when disclaimed
and stays SILENT on clean evidence.

Against the live ledger: v0.4.260815.1 (three platforms) complete; v0.4.260817.1
missing=linux,macos. Both controls matter — the first proves the gate is not too strict to clear
a real release, the second that it is strict enough to catch what motivated it.

The pre-existing MUTATION CONTROL needed its precondition updated: it seeded one unplatformed
file, so the new gate refused before reaching the mutating code and the tripwire never fired.
That reported as "the tripwire cannot see writes", which reads as a broken harness rather than a
stale fixture — worth noting, because that message would mislead the next person.

### E2E still deferred (third consecutive)

Backup validator tonight by assignment; yoga is primary and I did not duplicate it. Independent
of that, my own curl-install gate has now been deferred three cycles running. Recording it as a
real gap rather than letting the assignment mask it.

### Metrics

```
experts: calls=13330 answered=13321 unsupported=2 degraded=7 errors=0 answer_rate=99%
mcp: servers=3 per_server=cli=13278;forge-plan=52;project-info=43 health=ok
expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
flow: cycles=25 avg_completed_per_cycle=0.76 avg_commits_per_cycle=3.16 overhead_ratio=4.16
timing: steps=4576 build_check_ms_avg=45891 litmus_ms_avg=20914 slowest=litmus:freshness-inventory-shape:152167
plan: packets=605 ready=375
verdict: attention:experts-degraded-7-calls-could-not-run
```
