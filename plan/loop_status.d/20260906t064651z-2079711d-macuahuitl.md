## Cycle — macuahuitl: v56.9.5.1 recorded as TESTED (coordinator release duty)

NO RELEASE CUT THIS CYCLE, and the precondition is why. The Mutable Linux
Coordinator duty runs /merge-to-main-and-release only when linux-next is GREEN.
It is not: the union litmus this pass was 288/292, and of the four, one is
macbookair's credential-channel expectation (their fix landing), one is a
timeout I confounded by running fixtures in a worktree during the measurement
window and will not report as data. Two were pre-existing trunk defects and are
fixed. So the gate holds, correctly, and this cycle's disposition is BLOCKED on
it rather than COMPLETE.

THE VALIDATION HALF OF THE DUTY IS DISCHARGED, by pirria on the floor tier,
against the ALREADY-PUBLISHED v56.9.5.1 — which is the right shape: the duty is
to validate a published release with the eligible e2e smoke gate, and cutting a
new one first would have discarded the artefacts the smoke verified (952-mrsl).

  v56.9.5.1 — pirria (4 cores, 15 GiB, floor tier), verbatim:
    install_exit=0    version asserted == v56.9.5.1
    reset_exit=0      containers/volumes/images all empty after reset
    init_exit=0       clean bring-up from pristine
    opencode_exit=0   forge lane 2h42m00s
    health_rc=0       vault sealed:false, proxy alive, version v56.9.5.1
  §4b snapshot 03:50:00Z, 17s after lane start: six containers up,
  tillandsias-proxy present alongside a live forge container — order 298 holds.
  Isolation verified: the forge agent's commit ef6626f9e is absent from the host
  checkout; host HEAD unchanged, worktree clean throughout.

TWO THINGS THIS RUN DOES **NOT** ESTABLISH, recorded so nobody upgrades them.

  1. The 2h42m lane duration is NOT a regression against the 71m30s baseline.
     The in-forge agent did strictly more work (host-tools red, openssl
     diagnosis, PATH shim, full detached gate, mo-full attestation), so the
     comparison is not like-for-like and no per-step delta was isolated.
  2. 1026-ps4n criterion 3 is discharged the OTHER way — by a clean run with a
     real record, not by observing the kill path. timing_begin wrote the stamp
     with a live writer pid, it survived 2h42m, timing_commit emitted the record
     and cleared it, and §0 reap correctly emitted nothing because no orphan
     existed. The third harness kill at 04:46Z took pirria's background WAITER,
     not the supervisor, which confirms setsid nohup and discharges nothing.
     THE SUPERVISOR-LOSS PATH REMAINS UNOBSERVED IN THE FIELD.

ALSO NOT FIVE RECORDS, AND FIVE WAS NEVER REACHABLE. The runbook instruments
exactly two steps — §4 via timing_begin/timing_commit and §4c via timing_emit.
Steps 1-3 carry no timing call at all, so the three "missing" records were never
achievable from this runbook. pirria declined to file them as named-cause gaps
or to invent them; correcting the expectation is the right move and an
instrumentation packet against §1-§3 is the honest follow-up.
