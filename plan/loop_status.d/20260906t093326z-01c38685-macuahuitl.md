## Cycle — macuahuitl meta-orchestration full mode (cron lane, 4h)

LOCK AND BOUNDARY, both this cycle's own. Lock acquired with the documented
recipe on the harness pid: ok:checkout-lock:acquired:prompt:8519, from
ok:checkout-lock:free. A FRESH boundary snapshotted at Start Of Cycle against a
clean tree and verified — the previous pointer was the 04:46Z cycle's, and
reusing it would have been the late-snapshot defect macneo measured, where a
boundary taken after the work manufactures a green. Daily maintenance gate:
ok:daily-maintenance-current:2026-09-06, body skipped.

MUTABLE LINUX COORDINATOR DUTY: RELEASE ATTEMPTED, GATE REFUSED, NO MERGE.
Preconditions checked rather than assumed — last release v56.9.5.1 was ~30h ago
so the daily cadence was DUE; no release workflow in flight (last completed
2026-09-05T02:22Z, success); plan fold current at 821 packets; PR 109 already
open from the 00:53Z attempt and reusable per the runbook. The one precondition
that failed is the one that matters:

  ./build.sh --ci-full  rc=1  31 minutes  351 PASS / 5 FAIL / 98%

So no merge to main, no VERSION bump, no tag, no dispatch. PR 109 updated with
the current list and left marked DO NOT MERGE.

THE POPULATION TURNED OVER ALMOST COMPLETELY IN 8.5 HOURS, and that is the
finding rather than the count. Against the seven failures recorded at 00:53Z:
SIX ARE FIXED (clippy --all-features, the tray/vsock feature test, 4 orphaned
guards, python3 heredocs, and both git-mirror-service litmus specs), ONE
persists (capability-routing-shape), and FOUR are new. The release gate is not
accumulating an unbounded backlog — named defects get repaired fast once a list
exists. What does not change is that ./build.sh --check calls all five GREEN, so
no individual land can see them and only a once-a-day run makes them visible at
all. That is 1087-h2z9, measured twice now with a turnover between.

TWO OF THE FIVE ARE FIXTURES ASSERTING PROPERTIES OF THEIR OWN ENVIRONMENT.
capability-manifest-guard's negative control expects a clean tree to warn about
nothing and gets empty output; committable-branch-guard's scratch checkout has
no repo-local git identity, so the guard CORRECTLY refuses and the fixture calls
that a failure. esmeraldinha found the same shape the same hour in
test-selector-drops-cross-branch-claims.sh, where a low-end host's correct tier
refusal was read as an empty batch. Three in one night; the class looks like a
rung rather than three repairs.

AND ONE IS A READ-ONLY VIOLATION WORTH ITS OWN LOOK: cycle-batch-triage step
23/26 asserts "selection writes nothing to the worktree" and reports
FAIL: selector mutated the worktree. A selector that writes to the tree it
reports on is adjacent to macneo's pre-selector-merge finding and to 1099-t4xr's
staleness framing.

E2E SMOKE: not run and not runnable, unchanged and structural. 1004-vsh2 forbids
the destructive reset on an operator workstation without consent for that run,
and no dedicated smoke host exists. The one validated release is still v56.9.5.1
on pirria, under an authorisation that is discharged. Routed to the operator as
a provisioning ask, where it has been for three cycles.

DISPOSITION BLOCKED, on the gate's verdict rather than my judgement.
