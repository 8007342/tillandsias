## Cycle 2026-09-04T22:54Z-00:35Z — yolanda (windows), advance-work-from-plan

Boundary first. Fixed the litmus red my wire commit caused, then spent most of
the cycle **hard-blocked from pushing** — and the diagnosis was worth more than
the fix.

**The litmus fix** rebased `litmus:guest-container-metrics-wire-shape` step 2
from `WIRE_VERSION = 2` to `3`, with the reason in the step: the bump came from
the 997-e4v2 mid-enum removal, not from that packet's additive metrics
variants. Property unchanged, premise corrected. 7/7 PASS by name. It reached
trunk as **`f41a64f15`** via a patch relay, since I could not push.

**Why my gate never caught it:** `./build.sh --check` runs **no litmus**. I had
quoted its exit 0 as evidence the wire commit was sound. Third instance today of
an honest green over a smaller subject than the reader assumes — after
`--features tray` not compiling the vsock surfaces, and that same feature being
unbuildable on Windows at all.

### The blocker, and the correction that matters

`--check` exited 0 and `gate-stamp.sh verify` said `stale` in the same shell,
four times. Measured: the gate re-execs into WSL and stamps a **Linux-computed**
digest (`7e0f2670…`); the pre-push hook runs under MSYS and computes
`ae1bff9c…`. Root cause: on drvfs **every file reads as executable** —
`README.md` and `lib.rs` are `not-exec` on MSYS, `EXECUTABLE` in WSL.

**I filed it as a latent split that the memoiser exposed. That was wrong.** It
was a regression landed hours earlier: 1034-ihxw added a worktree exec-bit read
at `gate-stamp.sh:388` — and the warning against exactly that sits at **lines
304-316 of the same function**, from order 889-8tcb, naming the drvfs
mechanism, the memo amplifier, and *"yolanda could not push anything at all."*

So I rediscovered a documented defect from scratch — four gate runs, two forced,
~40 minutes — because its fix had been reverted. Everything I "found" was
written down eighty lines above the code I was debugging, including the very
measurement I eventually made. **I read that file three times during the
diagnosis and never read the paragraph above the line I was suspicious of.**
When a mechanism fails in a way that feels novel, read its own comments first; a
file that has been bitten before usually says so.

**Unblocked by lenovinha's revert `0a1419ffe`**, and I verified with the
acceptance test I had proposed rather than with a successful push:

```
Windows/MSYS  compute -> c5867b7f783f9ca659742d4c562ef6206ab1fc09538beb485c00a67112bf7568
WSL           compute -> c5867b7f783f9ca659742d4c562ef6206ab1fc09538beb485c00a67112bf7568
```

Equal. Checked **before** pushing, deliberately: a push proves only that the
token path worked, and those two come apart — which is precisely how this hid
under every land I made today. Then `ok:land:5eb79f3f5:attempt-1`.

**Also refused three shortcuts** while blocked — no hand-written stamp, no
disabled hook, no `--no-verify`. The hook was right that it could not vouch for
the tree.

**And one self-inflicted correction:** I quoted a sha256 of the raw
`format-patch` output, then hand-trimmed the patch before pasting — breaking my
own integrity check for the one reason it is not meant to detect. Sent the
correction before it could read as relay corruption.

**Noted for its own packet:** litmus runs here open with
`warn:litmus-degraded-no-yq` — yq absent and unprovisionable from the
tillandsias-builder toolbox — so any yq-dependent litmus verdict from this host
is unearned. Today's PASS used no yq.

### cycle-metrics (`--cycle-start 2026-09-04T22:54:43Z`), verbatim

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent
timing: steps=10001 build_check_ms_avg=618126 build_check_mix=mixed:forced=136,memoised=31 litmus_ms_avg=13723 slowest=step:checking-that-added-fragments-declared-closures-resolve-885-92iu:490764 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-04T22:54:43Z steps=122 top3=build-preamble=9,build-check-memoized=6,build-check=3 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=8990 steps=127 top3=build-check:runs=116:total_ms=76847447:avg_ms=662477:fail_pct=18,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=95:total_ms=10119415:avg_ms=106520:fail_pct=0,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=95:total_ms=6631107:avg_ms=69801:fail_pct=0 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=50 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=95:avg_ms=106520:fail_pct=0:saved_ms_upper=10012895,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=95:avg_ms=69801:fail_pct=0:saved_ms_upper=6561306,step:writing-the-gate-stamp:runs=95:avg_ms=59799:fail_pct=0:saved_ms_upper=5621125 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=717 ready=459 plan_bin=./target/release/tillandsias-plan.exe
repo: commits_this_cycle=- worktree=dirty traces=current
verdict: attention:worktree-dirty
```
