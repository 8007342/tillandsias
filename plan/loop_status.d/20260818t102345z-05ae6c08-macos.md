## Cycle 2026-08-18T10:23Z — macos (hourly meta-orchestration, capture cycle)

- **Guards**: all clean; mcp `down:project-info` (**twelfth consecutive**);
  stranded `in_progress=0`. Merged `origin/linux-next`. No boot, no code change.

- **This was a CAPTURE cycle, deliberately.** Three things this loop had been
  observing for hours without ever filing — an unfiled finding is a lost finding,
  and two of these had appeared in every handoff for half a day while never
  becoming a packet.

- **827-rjc9 — project-info down 13.4 hours, and forge-plan is fine.** Measured
  from the health log rather than impression: project-info **12 records, 12 down,
  0 up**, `stage=tool-call`, span 2026-08-17T21:00Z → 2026-08-18T10:23Z;
  forge-plan **0 down** across the identical window, same registration mechanism,
  same checkout, same sessions. So it is specific to project-info's serve path on
  macOS, not to MCP or this host generally. The point of filing: 737-zcj5's
  mechanism made the outage VISIBLE and it worked — every cycle emitted
  `mcp_outage:` — but a signal that repeats identically for thirteen hours stops
  reading as a defect and becomes furniture. My own summaries drifted from "down"
  to "no longer a blip" to just quoting a count. Visibility was never meant to be
  where an outage LIVES.

- **827-hakc — a daily gate that can never go green.** `build_cache_hygiene`
  fires on "size >40 GiB OR marker older than 14 days". This host has **no
  marker** and target/ has sat at 23.7–25.4 GiB all day, so the marker arm is
  unconditionally true and the only thing that writes the marker is a completed
  `cargo clean`. The rule therefore reduces to: *perform a destructive clean you
  do not need, in order to stop being told you need one.* Every cycle honestly
  recorded `deferred`/`skipped-under-threshold` — right each time, and the
  condition never clears. That is the same lesson 801-qasc built the marker to
  prevent, reached from the opposite direction: a gate permanently due trains the
  reader to skip it.

- **827-d3dc — literal source pins, third instance this session.** All three had
  the CODE correct and the TEST wrong, and all three repairs were strictly
  stronger than what they replaced. The enforcement gate (634-39ik) fires only on
  NEWLY-ADDED litmus steps and says nothing about pins already in the tree, nor
  about Rust `include_str!`/`source_window` tests — which is where all three
  were. Filed as a **survey, not a sweep**: some source pins are correct and must
  stay (the CA test's whole-file ABSENCE scan cannot be expressed behaviourally),
  so a blind conversion would delete real guards. Proposes and stops, per
  bar_raise_governance.

- **795-hzpg: premise checked BEFORE claiming, and it does not hold.** Its exit
  criterion says to implement `wait_bounded`/`status_bounded` "in terms of
  `timeout(budget, cmd.output())` with `kill_on_drop(true)`", citing backend.rs
  as the correct shape nearby. But that shape is **async** on
  `tokio::process::Command`, while `SyncPodmanCommand` is deliberately **sync**
  — being sync is its whole reason for existing ("a call site that forgets the
  deadline does not compile"). Adopting it needs a runtime the API lacks:
  `block_on` panics if any caller is reached from inside a runtime, and proving
  the call graph safe is itself the task; migrating the call sites to async is
  far larger than this packet's scope. Also worth knowing — the hand-rolled loop
  is **not naive**: its comment records that the reader threads are abandoned
  rather than joined *on purpose*, because killing the child does not close a
  pipe a grandchild holds, and "the first version of this code did exactly that
  and hung its own test". Filed as a note, NOT a refusal: the three hazards it
  names are real; it is the REMEDY that needs restating. The packet wants to be
  the last pass over this surface, so it should start from the right question.

- **Nothing claimed, nothing completed, three packets filed.** Recording that
  plainly rather than dressing it up: shaping findings into well-formed ready
  packets is a valid reduction step, and this loop had accumulated three it kept
  re-observing instead of reducing.
- **NOT done**: the attended **m8 UX smoke** macOS release acceptance owes —
  still unpaid, and no unattended cycle can discharge it. 795-zshi's remainder is
  windows-tray + `wsl_root_sh`, untestable here. 701-g98y, 774-b529, 778-n9z2
  attended-or-restage-only. Ledger compaction is linux's.
