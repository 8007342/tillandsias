## Cycle 2026-09-13T23:35Z — yoga (linux-next)

Investigation cycle on 1132-r4mt under macuahuitl's release freeze (v56.9.13.1 from 0c53aa4ae, gate red at 22:53Z). No crates/ or scripts/ landing; the two commits are plan-only and macuahuitl confirmed nothing was integrating before each push.

- 1132-r4mt claimed at dc1cde5b6, released to ready at 191d606db. Criterion 4 is answered for arm 5's side and refinement 3 — my own — is corrected. Criteria 2 and 3 stay open, which is why the claim went back rather than the row being closed.
- ARM 5 EXHIBITED: killing the archiver mid-run on a clean tree leaves plan_tmp/, five plan_tmp_*.txt files and scripts/archive-plan-packets-check.rb. No gate, no stray, no absent ruby. Arms 4 and 5 are independent subjects.
- FIRST NEGATIVE SAMPLE WITH ps: GATE_RC=0, fixture 5/5, ps taken AT the fixture step — one container-side gate, launcher alive, no stray. Two earlier clean passes on this row have no ps and cannot serve as negatives.
- CORRECTION: a healthy running gate is conmon-parented too (1337185's parent is conmon, never the podman exec that launched it), so conmon parentage discriminates nothing. The discriminator is whether the host-side podman exec still exists. My earlier refinement 3 would have recorded every healthy gate as a stray.
- TRAP RECORDED: plan_tmp's mtime is inherited from the copy source, so it stats hours old the moment it is created — anyone dating an interruption from it will place it early.
- METHOD NOTE: my first two ps captures matched nothing and wrote an empty file, because I matched an awk field index instead of a name. An empty capture looks exactly like "no processes"; the third capture matched by name and found the tree.
- Unexplained and stated as such: eight idle conmon-parented /bin/bash -l in the builder container, cwd crates/tillandsias-headless, 2h02m-4h30m old, no children. A clean with-tillandsias-builder.sh dispatch leaks none (11 before, 11 after, 11 after 3s), so no mechanism is named.
- Sent macuahuitl two dead hypotheses on their litmus:ensure-toolbox-include-shape red (XDG_CACHE_HOME and an inherited TILLANDSIAS_WRAPPER_TOKEN), both killed here before sending, plus the leak surface of the include chain.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
