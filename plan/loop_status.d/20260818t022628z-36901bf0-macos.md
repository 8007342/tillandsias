## Cycle 2026-08-18T02:26Z — macos (hourly meta-orchestration, offline)

- **Guards**: all clean; mcp `down:project-info` (**seventh consecutive cycle**);
  stranded `in_progress=0`. Merged `origin/linux-next`. No boot.

- **Closed 733-mppc — and found WHY it had two sites.** `open_control_wire_stream`
  is **duplicated**: `action_host.rs:353` and `diagnose.rs:173` are two copies of
  the same function, and both bounded only the connect while leaving the Noise
  `client_handshake` that follows it untimed. The same unbounded handshake was
  written twice and fixed neither time — which the filing host could not see from
  Windows. Rather than patch both, the bounded handshake is now ONE `pub(crate)`
  helper both copies call, so the bound and its message cannot drift; the
  duplication was the condition that produced the pair.

- **A THIRD site turned up while implementing.** The push-subscription loop's
  control-wire **Hello** (`client.handshake()`) was also untimed — a second
  handshake on the same seam, after Noise. A guest that finished Noise and never
  answered Hello parked the loop forever with `health` stuck false, so the tray
  showed a stale chip and **never resubscribed**. Bounded at 30s, deliberately
  longer than anything interactive: it is long-lived so failing fast buys
  nothing, and it is bounded at all only because the loop's own recovery —
  backoff and resubscribe — is unreachable while the await never returns.

- **Criterion 2's bound is the caller's, and that is a choice not an
  inheritance.** Callers pass 30s, so the ceiling is tens of seconds rather than
  the "minutes" the criterion warns about. A shorter fixed constant was
  considered and **rejected**: it would make the readiness probe, which
  legitimately retries against a still-booting guest, fail faster than the guest
  can answer.

- **A false doc comment was corrected, not just the code.** `poll_vm_status_once`
  claimed "The 5 s timeout covers connect + handshake + reply". None of it was
  true — it bounded only `vz.open_stream`. The handshake half is genuinely
  covered now; the **reply still is not**, and the comment no longer claims
  otherwise. A doc comment that overstates a bound is worse than none: it is what
  stops the next reader checking.

- **Falsified in BOTH directions, which is what this packet's shape asks for.**
  (a) Remove the bound → the silent-peer test does not go red, it **HANGS**,
  never returning; I killed it after 600s. That is the defect itself reproduced
  under test, and stronger evidence than a red assertion. (b) Over-bound at 1ns →
  the responsive-peer **negative control fails** while the silent-peer test still
  passes, catching the shape that satisfies "is it bounded?" while breaking every
  working connection. Restored: 91 tray tests pass.
- Tests drive REAL sockets: a listener that accepts and writes nothing (the
  packet's "reachable and silent" peer) under a **paused clock**, so the 30s bound
  is proven without waiting 30s; and a real `server_handshake` on the **real**
  clock, because pausing there would let the bound fire before genuine I/O
  completes and turn a passing control into a false failure.

- **NOT done**: 701-kgvk criteria 1+3 (next — the provenance-sidecar refusal and
  the seven dropped `Ok(None)` arms; note the 2026-08-12 tests call `file_sha256`
  directly and would stay green against a hard-wired `Some(true)`, so any new
  test must hit the real staging entry point); 701-iu9b TRAP 1 (ships statically,
  live-verifiable only behind a destructive reprovision); the attended m8 UX
  smoke; ledger compaction. 701-g98y remains attended-only.
