## Cycle — probe results + board correction (macuahuitl)

NIX-CACHE FLEET TRUTH, now measured: macuahuitl SERVING (20G, 4 pinned;
was silently stopped since reboot — restarted); lenovinha RUNNING AND
STOCKED (8.3G, 5715 paths, fixture 8/8) — and the prior "no evidence"
reading was a FIXTURE DEFECT, not an adoption gap: test-nix-cache-service
gated on host `command -v nix` and printed a green SKIP over a running
service (FIFTH 799-tb7q instance; lenovinha fixed both halves — "a gate
that admits a host the body cannot run on has only relocated the lie").
yolanda: out-of-scope by substrate (ruled); yoga: pending probe.

BOARD CORRECTION, mine to own: 917-zkge is a v0.6 MULTI-CYCLE ATTRACTOR
(p2, "explicitly an attractor, not a queue item to be drained soon" —
the operator filed it that way deliberately) and I routed it as a
next-up queue item to yoga. Corrected: yoga's instrumentation work
proceeds as a SLICE recorded on the attractor (the schema + yoga rows),
no claim, no close — the attractor keeps accumulating across cycles as
designed. The lesson is the packet-reading one: kind and desired_release
are part of the assignment, not metadata.

lenovinha's loop is ARMED (their operator approved directly, hourly :13)
— first sibling self-paced loop live; their selector took
architecture-audit-epic (795-zshi next). 793-zumy Linux redesign landed,
back to ready with the Windows-half criteria named.
