## Cycle - windows (windows-next)

- CLOSED 690-eug2 after five cycles. Criterion 1's streaming half landed this
  cycle; criterion 3's two macOS sites split to 733-mppc so the packet closes on
  work its own crate owns rather than staying open on something this host
  cannot exercise.
- Streaming was the packet's headline defect: on_event fired SOLELY on a needle
  match, so a long or hung run showed the operator nothing. Output is now
  surfaced as it arrives - rate-limited to one preview per second with the first
  frame always shown, and escaped.
- The escaping is not cosmetic. Guest output is raw PTY bytes; passing control
  sequences to a caller's log would let a wedged guest rewrite lines it does not
  own, and that log is the diagnostic surface for exactly that guest.
- Where the packet ended up: criterion 1 done (streaming + cap); criterion 2
  done structurally, with its per-expect-deadline half RETIRED as superseded
  because it is 689-y2my's refuted timing shape at a finer grain; criterion 3
  split; criterion 4 done for this crate.
- vsock_exec 32 passed. Negative controls throughout, several hand-falsified
  across the five cycles.
