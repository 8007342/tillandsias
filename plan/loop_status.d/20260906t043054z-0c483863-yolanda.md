## Cycle 2026-09-06T04:30:54Z (yolanda — the checker was wrong, and its report hid it)

**Landed** 13a20ab5c. Routed as "three broken cheatsheet links"; two were mine
and real, and the third finding was that **the checker could not be wired**.

**491 of 577 reported broken here against 3 on Linux**, and every one of the 488
extra targets EXISTED. Cause: the report line splits rg's
`<file>:<line>:<match>` on the FIRST colon, and on Windows `<file>` begins
`C:/`, shifting every field by one — `file` becomes `C`, `lineno` becomes
the path, and `resolve_target` is handed `95:agents/openspec.md`.

**The report then reassembles the mis-split fields** into
`${file}:${lineno}: ${path}` — a plausible path, a plausible line number, a
plausible target, 488 times. Nothing in the output says the parse went wrong.
Not merely a check nobody runs: **one whose output cannot say it is wrong.**
Wiring it would have redded every Windows land with fabricated links each
looking real enough to chase.

**Then I corrected the correction.** A peer swept the shape, found two more
matches, and reported them. I ran both: clean. **The parse shape is necessary
and not sufficient** — it only breaks if the path actually carries a drive
colon, which depends on how the root was derived:

| derivation | form | verdict |
|---|---|---|
| `git rev-parse --show-toplevel` | `C:/Users/…` | **breaks** |
| `cd "$SCRIPT_DIR/.." && pwd` | `/c/Users/…` | fine |

So the rule worth keeping is the **conjunction**, which is greppable and now
returns zero: a `rev-parse` root AND a `${var%%:*}` split of grep output.
That also un-withdrew yoga's `specs=185 traced=184`, which had been marked
Linux-only on my account.

**Two things I nearly filed and did not.** The check began timing out at 120s
where it had run in 15s, and I was composing "this hangs on Windows" — it was
contention from my own backgrounded gate; HEAD and my version both run ~14.8s on
a quiet machine. And I ran the control only *after* deciding what I was looking
at. **The observation was 120s and a matching parse; the attribution was mine
and wrong.**

**Credential channel checked twice over.** `gh api user` rc=0, and the
arm-ordering probe reads `reaches-arm-4` — no store file, no `GH_TOKEN`, no
`GITHUB_TOKEN` — so the verdict I have been quoting came from the arm that
actually verifies, not from a cheaper one returning first.

Agent: windows-yolanda-opus5-20260906t043054z
