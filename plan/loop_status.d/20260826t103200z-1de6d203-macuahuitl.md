## Cycle 2026-08-26T10:32Z — macuahuitl (linux_mutable, linux-next), hourly /loop

SURVEYED WHAT IS BLOCKED ON THE OPERATOR, because the queue has been
accumulating and nobody was counting it. Six packets carry blocked_on: in the
base; three are chained on authenticated-transport work (451, 502, 504) and are
not operator decisions. The operator-gated live ones:

  626-w3fn  p1  ready    UX approval for progress strings (half already landed)
  628-w9sm  p2  pending  rulings on canonical spelling of every divergent string
  613-uy82  p1  obsoleted — not live
plus, from this session's filings:
  907-8ypf  p1  pickup_role: operator — the credential revocation, needs the
                account security log and the OAuth app authorisation list
  902-5bf9      the Rust+Lua direction, with a blocking exit criterion about
                why rust_queries has one adopter in 395 files

DID THE UNBLOCKED HALF OF 628-w9sm rather than waiting: criterion 1 is an
inventory and needs no ruling. Re-ran check-tray-string-corpus-drift.sh ->
en_keys=168 rendered=5 unmatched=163 rendered_pct=2.

AND I ALMOST REPORTED A REGRESSION THAT DOES NOT EXIST. The packet's own
progress event quotes "150 of 168" and "9 rather than 18", which read against a
fresh 5 looks like adoption collapsing. It is not — the 2026-08-17 REPORT FILE
records the identical 163/5, and the 150/18/9 figures predate 556bef2f0, which
corrected the report to stop counting its own test code. The packet's PROSE is
stale against the packet's own DELIVERABLE.

Caught by reading the report file's recorded numbers instead of the event text.
General form for this milestone: A QUOTED MEASUREMENT MUST CARRY THE INSTRUMENT
IT WAS TAKEN WITH, not only its timestamp — a number from a since-corrected
tool reads as a trend when placed beside a current one. That pairs with
pirria's timestamp rule and the cold/warm regime rule; all three are a number
arriving without the context that makes it interpretable.

CONFIRMED THE 628 WEDGE IS STILL LIVE: 628-w9sm is pending and blocked_on an
operator ruling, 628-c7qd is also pending, and w9sm's depends_on names c7qd.
Each waits on the other. yolanda surfaced it 2026-08-23; it has not moved. Only
a ruling, or dropping/inverting the edge, breaks it — both operator acts.

Not claimed: reconciliation changes user-visible strings on whichever platform
loses, and the packet's own notes say even `GitHubLogin` vs `GitHub Login`
needs the ruling. I am not going to be the agent who decides an obvious case is
too obvious to ask about.

ACKed lenovinha's tool-dispatch.sh heads-up (no conflict) and affirmed their
deliberate non-conversion of the two SHIPPED diagnostics — a shipped script
that sources a sibling lib fails exactly when its output is needed.

Guards green, lock clean, boundary preserved, stranded 0, siblings merged.
