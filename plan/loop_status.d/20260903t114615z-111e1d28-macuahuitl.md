## Cycle — macuahuitl 2026-09-03T11:40Z (coordinate-multihost-work, cron 23-odd)

- **THE TRUNK WAS RED FOR LINUX AND I CAUGHT IT MERGING.** macbook's 989-ykks
  landing wired `scripts/test-host-tools.sh` into `build.sh:2740`, and its arm 2
  hid `timeout`/`gtimeout` and grepped for a literal `missing:host-tools:macos:`.
  Both halves are macOS-only — `timeout` is required on macos and NOT on linux
  (the table is git/cargo/rustc for linux, plus timeout/pkg-config for macos), and
  the verdict names the LIVE platform. So on linux the arm read
  `rc=0 / ok:host-tools:linux:3 required present` and failed, taking `--check`
  with it. No linux host had gated against it yet. Fixed at 4c6e3bf0e: the arm
  now picks a tool the current platform actually requires and asserts the current
  platform's verdict. 5/5 on linux.
- **It is that packet's own thesis turned on its own fixture.** "The authoring
  host's build is a smaller universe than the fleet's" is macbook's sentence, and
  the fixture written to enforce it exhibited it. Fifth instance of the shape
  today; first where the instrument built to catch it was the instrument.
  **The probe itself was never wrong** — it reported truthfully throughout.
- **Merges.** `osx-next` 6 ahead, merged (989-ykks probe + fixture, an
  attestation, a claim on order 380). `windows-next` 0 ahead / 9 behind after
  yesterday's fast-forward — inside tolerance, no action.
- **Claims:** in_progress=3, expired=0, held=0, unknown_age=0. 380 (macbook, no
  events yet), 978-juw4 and 980-xcaf (macneo, both awaiting the operator's
  go-ahead for the 2 GiB measurement).
- **Roll call, using the discriminator landed this morning.** yoga 11:27Z and
  macbook 11:16Z active within the half hour. lenovinha, macneo and
  lenovinha-tillandsias-forge quiet ~2.5h; the forge declared itself done and
  macneo is blocked on the operator, so lenovinha is the only genuine unknown and
  I ASKED rather than inferred. pirria finished and resting by instruction;
  macuahuitl-tillandsias-forge holding by instruction; yolanda blocked on two
  operator decisions.
- **989-ykks criterion 5, linux half discharged** on real hardware rather than
  reasoned about. Windows remains: the table carries a windows column for git,
  cargo and rustc that no windows host has verified. It is a probe run, not a
  build — cheap enough for yolanda whenever they are unblocked. Not assigned.
- **Dispatched** lenovinha to 986-ahnc, the choke point they found from the other
  side of 981-n5vx, with 968-uhzg as the fallback. **First dispatch to carry the
  credential preflight** required by 982-sguu, which I landed an hour ago.
- **Litmus:** not run. The merge added a probe, its fixture and ledger fragments;
  `--check` passed on the merged tree after the fix. Recording the omission with
  its reason.
- **Operator queue, four plain asks, unchanged:** renew the esmeraldinha PAT
  (check EXPIRY first); macneo's permission for the forge-build measurement on the
  operator's Mac; yolanda's re-open-a-closed-cycle decision; yolanda's two-hourly
  cron decision.
- **Nothing is blocked.**
