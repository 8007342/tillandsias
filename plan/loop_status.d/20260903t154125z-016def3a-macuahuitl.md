## Cycle — macuahuitl 2026-09-03T15:39Z (coordinate-multihost-work, cron 23-odd)

- **No merges needed.** BOTH siblings are 0 ahead — `windows-next` 0/8,
  `osx-next` 0/12 — so everything either branch had is already on the trunk.
  First cycle today with nothing to integrate. Fast-forwarded myself; left
  `osx-next` alone because its hosts are active and merge `linux-next` as part
  of their own pre-push gate, and left `windows-next` at 8 behind since I
  levelled it 2h ago and 8 is inside tolerance.
- **Credential guard: `refstate-refused` again**, correctly — I was behind
  origin at Start-Of-Cycle, which is 993-yqie's whole point. Ran the remedy it
  named. Third distinct `ok:` arm I have seen since I stopped tailing it.
- **Claims:** in_progress=3, expired=0, held=0, unknown_age=0. 795-5itp
  (macbook), 978-juw4 and 980-xcaf (macneo).
- **THE NEW ROLL-CALL MARKER PAID FOR ITSELF IMMEDIATELY.** `ready` now appends
  `worked:<n>@<host>`, and picking a dispatch took one command instead of a
  per-packet event sweep: 970-7fqk, 979-xyub, 980-krib, 981-a4t4, 980-7rng and
  984-i4k2 carry NO marker and are genuinely untouched, while 981-n5vx
  (`worked:17`), 982-cs43 (`worked:10`) and 980-ja2m (`worked:8`) are visibly
  live. That distinction cost a full sweep this morning and is now free.
- **Dispatched lenovinha to 984-i4k2**, p1 and untouched: order 569's skew check
  compares subcommand CAPABILITY SETS, so it is blind to a subcommand whose
  BEHAVIOUR changed — the forge wrote 38 lines into `plan/index.yaml` while the
  check truthfully reported `skew=none`. Told them to start by establishing
  whether any host is STILL writing to the wrong channel, because that is a
  fleet question rather than a code question and may be the whole packet.
  Fallback 970-7fqk, also untouched.
- **macneo has not answered a status ask from ~2h ago and is quiet ~6.5h while
  holding two claims.** Not stale by TTL (24h), so `expire-claims` correctly
  leaves them. Recording rather than escalating: I asked once, the claims are
  within their lease, and one of the two (980-xcaf) is work they reported
  verified end to end, so the cost is a held claim on finished work rather than
  lost work. If it is still held next cycle I will ask the operator whether that
  host is up rather than guessing from here.
- **yoga active 12 minutes ago**, landing 977-448j — the enforcement rung of the
  obligation lattice, which refuses silence and proves it can. That is rung 5 of
  the split they made this morning.
- **Litmus:** not run. Nothing was merged this cycle; `--check` current.
- **Operator queue, four plain asks, unchanged:** renew the esmeraldinha PAT
  (check EXPIRY first, do NOT rebuild the git image); macneo's permission for the
  forge-build measurement on the operator's Mac; yolanda's re-open decision;
  yolanda's cron decision.
- **Nothing is blocked.**
