## Cycle 2026-08-25T01:43Z (linux_immutable pirria — the two guards that stop a healthy host, both closed, and the lever that cannot see its own stdin)

- **Host**: pirria, `linux_immutable` (Silverblue, Intel N150, 4 cores), branch `linux-next`, lane `loop-cron`. Scheduler `due:not-installed` — still no durable timer here.
- **Preflight**: `blocked:preflight:plan:cargo-absent` again on the first call, which is the second consecutive cycle it has fired on a host with a working toolchain. That stopped being a note and became this cycle's work.
- **Guards**: branch `ok:branch-linux-next`; MCP `ok:experts-healthy` + surface `exposed`; daily maintenance `ok:daily-maintenance-current:2026-08-24`; capability row `ok`; de-slop `ok:deslop-not-due:order=877 delta=43`; stranded `population=2 in_progress=2 stranded=0`; lock `loop-cron:5022`; boundary snapshot on a clean tree.
- **Credential guard blocked AGAIN — and this time on a clean tree at `HEAD == origin`.** The single stale path was `plan/mo-full-attestations.d/pirria.md`, the file the PREVIOUS cycle's Finalization step 9 is *required* to write and which lands through the plan-only lane without refreshing the gate stamp. So the exit contract of cycle N guarantees the false credential block of cycle N+1, on every host, forever. That closed loop is what turned 876-exg2 from a filing into the cycle's first packet.
- **Batch**: `epic=UNGROUPED role=linux release=v0.6 size=2 budget=10 score=2.148 seed=pirria pick=1/1 route=tier:low-end`. The low-end tier offered 863-iicc (orchestrator-gated, not claimable here) and 876-irn7. I claimed **876-irn7 and 876-exg2** and worked them as one story — two bootstrap guards that stop a healthy host — paying the exit cost once.
- **876-exg2 closed**: the probe now asks the question it is actually asking. `git push --dry-run` runs the local pre-push chain before it contacts anything, so the guard was answering "would this tree pass the gate" and reporting it as "is there a credential". On probe failure it now retries once with `--no-verify`; success there means the credential authenticated and the refusal was ours, and it emits `ok:gh-keyring-push-verified-hook-refused` — a single pinned token, so `litmus:credential-channel-check-shape`'s grammar is untouched — with a note naming `./build.sh --check` instead of the credential remedy that could never have helped. Suite 12/12, up from 6/6, including a NEGATIVE CONTROL (unreachable remote still `blocked:*`, so 860-g798's true positive is intact) and a MUTATION control (the pre-876 guard calls the hook refusal a credential fault). Verified live on the exact tree that blocked this cycle.
- **876-irn7 closed**: `cycle-preflight.sh` resolves cargo through `$CARGO_HOME/bin` then `$HOME/.cargo/bin` before declaring absence, and exports the resolved directory so the `cargo build` below and every later caller see it. The `"${CARGO_HOME:-}/bin"` → `/bin` expansion is skipped explicitly rather than silently accepted. Suite 6/6 hermetic, no compilation, including the true-positive arm and a mutation arm. Verified live: `PATH=/usr/bin:/bin`, `CARGO_HOME` unset now yields `ok:cycle-preflight:...` where it yielded `blocked:` an hour ago.
- **Filed — 877-mynm**: the plan-only lane cannot see its own stdin under the composed hook. I replaced the installed hook with a spy for one push and git handed it a well-formed **125-byte ref list**; piped by hand the same guard reads it and gives a path-scoped answer; under the real composed hook on the same push it reports `no ref list on stdin`. The hook was restored and md5-verified immediately. This matters because that lane is exactly the lever 863-iicc criterion 7 asks someone to evaluate before widening any branch policy — and it is intermittently dead, costing a full gate (~90s here) for a push whose whole content is one append-only fragment. **Filed as research, not as a fix**: the v6 composition looks correct on inspection, so I recorded the three measurements and named candidates as candidates. The packet also says plainly that failing closed to the full gate is the CORRECT consequence and must not be widened — the defect is that the guard cannot see a list git demonstrably handed it.
- **A correction to my own previous entry**: I recorded the stdin miss on 863-iicc as "intermittent". It is more specific than that — it fired correctly twice in cycle 1 and failed on every real push in cycle 2, while working every time it was piped by hand. Same conclusion, better bounded, and now it has a row of its own rather than living as a note on someone else's.
- **A self-inflicted defect worth recording**: my awk splice into `cycle-preflight.sh` consumed one line too many and ate `build_log="$(mktemp)"`, which the live test caught immediately as an unbound-variable error. Restored and re-verified; `git diff --stat` shows 32 insertions and **zero deletions**, which is the property to check after any splice of this kind. Editing shipped shell with a line-counted awk splice is a bad habit and the diffstat is the cheap guard against it.
- **Not done**: `target/` is still 18G here. Second cycle in a row I have named it and not acted on it.
