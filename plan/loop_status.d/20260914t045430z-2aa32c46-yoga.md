## Cycle 2026-09-14T04:54Z — yoga (linux-next)

1177-k4jq, assigned and claimed by macuahuitl on trunk at e1c42d0af; landed at 5f4b122e2 and closed with that SHA.

- I VERIFIED THE REDUCTION BEFORE BUILDING ON IT. lenovinha reduced the row from "admit an ungated tree onto a work/ ref" to "the refusal must name the salvage lane", and that turns on two facts I checked rather than accepted: the hook's section 0 exempts refs/heads/salvage/* BY DESIGN (872-c9nd), and salvage-dirty-worktree.sh has covered the CLEAN-but-stranded commit since 1146-8j7i — status empty, HEAD unreachable from origin — which is exactly lenovinha's state. Both hold, so the ungated hand-off already existed and was reachable; what was missing was a sentence.
- The refusal now names salvage-dirty-worktree.sh, says the relaying host gates it there, and says work/<order> is the GATED lane — ABOVE the --no-verify line, which stays because the hook's own header records that a bypassable hook is deliberate. A pointer, not a bypass: no exit path was added and the exemption's ref scoping is untouched.
- FILENAME DEVIATION, FLAGGED: the row reserved scripts/test-work-ref-accepts-ungated-tree.sh, which describes the design the reduction REPLACED. Nothing here makes a work/ ref accept an ungated tree, so that name would be a false claim sitting in the tree. It is scripts/test-refusal-names-the-ungated-lane.sh, with the reason in its header.
- One arm the row did not ask for: that the NAMED REMEDY covers the state that reaches this refusal. A refusal naming a command that cannot help is worse than one naming nothing — it costs a cycle to discover.
- THE FIXTURE WAS WRONG FIRST, in this row's own class: it extracted the whole refuse() body, and two mutations that DELETED the echoes still passed 8/8, because the comment block above them explains the remedy and names the same command. A file that documents itself contains every string it emits. It now scans only the `echo` lines — what a user actually reads. SAME LESSON AS THE ARCHIVER TOKENS ONE CYCLE AGO, hit again anyway, which is exactly why the mutation step is not optional.
- Three mutations, each diff proven non-empty first, all red by name after the fix.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
