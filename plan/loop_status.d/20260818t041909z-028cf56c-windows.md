## Cycle 2026-08-18T04:19Z→05:00Z (windows — YOLANDA, MO FULL: a spec that specified a system nobody built, and a third host-conditional test)

**Result: second consecutive freshness audit, second real defect. The drawn spec required six manifest summarizers; two were NEVER COMMITTED, two ship under different names, and two that do ship are unlisted — while its bound litmus passed all five steps at coverage_ratio 75. Corrected the spec, filed WHY it rotted green (815-yace), and found a third member of 814-75yf's host-conditional family within a day of filing it.**

- **STOP CHECK**: `Mon Aug 17 21:19:09 PDT 2026`, before the 06:00 PDT stop.
- **Host**: YOLANDA, `windows-next` dde428288 → merged origin/linux-next ×2 → d383f41c1, local == remote. All guards green; boundary `/tmp/meta-orchestration-boundary.e7xL3R`. Preflight gained a `+services-no-podman` segment from linux's 798-tk7b work. `verdict: ok:nothing-flagged`; `in_progress=0 stranded=0`.

**THE AUDIT.** `freshness-next` drew `openspec/specs/project-bootstrap-readme/spec.md`. Measured against the tree:

| spec name | actual |
|---|---|
| `summarize-cargo.sh` | ships |
| `summarize-go.sh` | ships as `summarize-go-mod.sh` |
| `summarize-package-json.sh` | ships |
| `summarize-maven.sh` | **NEVER COMMITTED** |
| `summarize-gradle.sh` | **NEVER COMMITTED** |
| `summarize-flutter.sh` | ships as `summarize-pubspec.sh` |
| *(unlisted)* | `summarize-nix.sh`, `summarize-pyproject.sh` ship |

`git log --diff-filter=A` finds no add-commit for maven or gradle and no plan packet proposes them. The delivering commit a7732bbb2 is titled *"implement six language-specific manifest summarizers"* — it shipped a **different six**. The spec was never descriptive of the built system.

**The dispatch shape was wrong too, and that is what misleads rather than merely misnames.** The spec said `regenerate-readme.sh` "invokes language-specific summarizers" and listed them; it actually globs `/opt/summarizers/*.sh` and `scripts/.tillandsias/summarizers/*.sh` (`regenerate-readme.sh:30`) — a plugin directory. A reader implementing to spec would hard-code six calls and miss the extension point entirely. **Disposition `updated`**, stamped in-file; the two-section contract, determinism and validation requirements were accurate and untouched. `litmus:project-bootstrap-readme-shape` passes after the edit.

**815-yace (p2) FILES WHY IT ROTTED GREEN.** That litmus pins the cheatsheet's two-section contract, its provenance, that two dispatcher scripts exist and are executable, and an OpenCode narrative doc — and for summarizers it greps the CHEATSHEET for the single string `summarize-cargo.sh`. Every step passes while four required scripts are absent. **It pins the spec's SHAPE and never its ENUMERATIONS**, at a steady `coverage_ratio: 75`. Proposed closure: a check resolving every `scripts/...` path named in an openspec requirement.

**The sibling spec `project-summarizers` carries the same stale list under `scripts/summarizers/`** — a subdirectory that does not exist. Left for its own audit draw rather than rewritten by a host that was not auditing it. **Not claimed:** that these two are the only drifted specs.

**THIRD INSTANCE OF 814-75yf's CLASS, one day after filing it.** Verifying the spec edit, `litmus:skills-canonical-and-mcp-first-shape` step 1 failed on Windows — "every non-generated skill is a symlink into canonical skills/". Cause: `core.symlinks=false` here, so the mode-120000 entries under `.opencode/skills/` and `.claude/skills/` materialise as regular 35-byte files. It CANNOT pass on Windows and is green in the gate only because `./build.sh --check` re-execs into the WSL2 Linux distro. **The remedy DIFFERS from the metrics case** and that distinction matters: there the test encoded a Linux assumption it did not need (fixed by injection, 73/73); here the litmus is arguably RIGHT to demand symlinks, so it wants a declared host precondition that SKIPS rather than FAILS — the same remedy 806-6y3h proposes. Family now has three known members (806-6y3h, 814-75yf fixed, this): two want skip-with-precondition, one wanted injection. That makes 814-75yf's option (a) — run the suites natively off-Linux each cycle and report the delta — look stronger than option (b), since it would have surfaced all three on day one.

**FOR LINUX:** `plan/index.d/20260818t020740z-801-kqme-linux-mutable.yaml` declares three packet DEFINITIONS under `events:` — `forge-image-has-no-nix-despite-the-spec-requiring-it`, `harmonia-baked-into-an-image-versus-taken-from-the-served-store`, `nix-cache-service-into-the-tray-container-lifecycle`. The fold DROPS all three (their own check reports it on every host). Flagged, not touched: the fragment is theirs and immutable.

**808-43mw / 808-7yrd HELD, seventh consecutive cycle.** Still `ready`, unclaimed, no linux response, no operator steer.

**NOT DONE, flagged:** ledger compaction eligible; bar-raise proposed, not enabled; 806-2r4s NPU half open (809-7e4m).
