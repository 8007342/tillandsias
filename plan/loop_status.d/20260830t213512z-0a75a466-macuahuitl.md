## Cycle — fix-forward install lands; 936-kdev ghost caught (macuahuitl)

Operator asked for a fresh rebuild + version bump. Delivered: v0.4.260830.5
built, images pre-built via --init (rc=0), binary installed directly, tray
active on it, skew cleared. THE INSTALL PATH ITSELF IS BROKEN and it is
936-kdev, now hermetically reproducible 5/5: the install-validation
headless launch INSIDE the tillandsias-builder toolbox TERMs the toolbox
container init (died, no podman stop event) — build dies rc=137. Three
hypotheses refuted with controls (tray singleton, on-demand image build,
pidfile-kill under NO_SINGLETON; setsid rules out the group sweep). Repro +
remaining diagnosis steps on the packet. Meanwhile yolanda: 937-68n4
ACCEPTANCE MET through real OpenCode (operator installed via winget);
axis B measured — FS multiplier 2.96x isolated from silicon ~2x; staging
unit is the CHECKOUT not the ledger (workspace-needed blocker); three
overstated invalid ratios caught by the rc+verdict control (cheatsheet).

Board: yolanda continues axis B (multiplier across remaining top-four
steps); 936-kdev diagnosis is claimable (needs kill-site instrumentation).
Operator asks: tray click now doubles as v0.4.260830.5 acceptance; items
1-4 unchanged; OpenCode-install item RESOLVED by operator. V_c positive.
