## Cycle 2026-08-18T07:23Z — macos (hourly meta-orchestration, NEW DAY)

- **Start-Of-Day gate fired correctly on the day boundary**: `due:stale:2026-08-17`
  → ran the body → `ok:daily-maintenance-stamped:2026-08-18`, and it reads back
  current. That is 803-bqte's fix working across a date change, which is the
  transition it was never able to make before.
- **Guards**: all clean; mcp `down:project-info` (**tenth consecutive cycle** —
  no longer a blip and worth fleet attention); stranded `in_progress=0`.
  Merged `origin/linux-next`.
- Daily maintenance: cargo-gc **skipped-under-threshold** (target 25.4 GiB vs the
  40 GiB trigger); podman machine still never started; nix absent.

- **Fresh batch** (the carried queue was drained last cycle): seed
  `host-20260818`, epic `architecture-audit-epic`, top pick **795-zshi** p1.

- **Slice 4 of 795-zshi — and checking the plan before building on it is the
  whole result.** Slice 3 named the next step as "gated on CAP_EXEC_ARGV_VECTOR
  (or an equivalent capability)". **The first option is wrong**, and the
  parenthetical was carrying the entire load: `CAP_EXEC_ARGV_VECTOR` shipped in
  `cc4bee155` and the CA-key heal in `d4e12b425` — **two distinct commits** — so
  a guest built between them advertises the argv capability and does **not** have
  the heal. Gating the preamble's `chmod 600` on it would silently drop the clamp
  for exactly that window and reintroduce 772-shi9 (a 0644 ephemeral CA key
  surviving the VM's lifetime) on the guests the clamp exists to protect. Not an
  edge case: this fleet stages whatever guest binary the last bundle carried, so
  mid-window builds are the normal case.

- **Landed `CAP_PROXY_CA_KEY_HEAL`**, advertised in HelloAck beside the others.
  It names the BEHAVIOUR a host depends on rather than a neighbour that landed
  nearby. **The general rule, recorded because it will recur**: reusing a
  capability as a proxy for "built after roughly then" is *version comparison
  wearing a capability's clothes*, and it fails on exactly the builds a
  mixed-version fleet actually produces — the failure capabilities were adopted
  to avoid. The control-wire docs already say "a version says what a peer IS
  while a capability says what it can DO"; aliasing two quietly converts the
  second back into the first.
- **The pin is an anti-simplification**: the test asserts the advertisement AND
  `assert_ne!` between the two constants, so a later tidy-up that collapses them
  fails and the mixed-version argument must be re-made rather than lost.
  Falsified: aliasing them fails with the intended diagnostic.

- **NOT done, and the reason is a signature this slice must not widen.** Host
  adoption needs argv chosen AFTER server_caps are known, but
  `exec_over_stream_expect_dynamic` takes `argv: &[&str]` and does its Hello
  INTERNALLY — caps arrive after argv is fixed. Surfacing them means a second
  connection or restructuring a function shared with the Linux and Windows
  callers, and slice 2 already recorded why that shared signature must not be
  widened to reach one macOS field. **Slice 5**: give the exec path a way to
  build argv from caps without widening the shared signature, then delete the
  preamble's `podman rm` and `chmod 600` behind this capability.
- Scope note for slice 5: the preamble is not purely a flattening workaround —
  it also exports three env vars, creates XDG_RUNTIME_DIR and generates the CA on
  first use, none of which the argv arm removes. Only the `podman rm` and the
  `chmod 600` come out.

- **NOT done**: the attended m8 UX smoke macOS release acceptance owes; ledger
  compaction (linux's). 701-g98y, 774-b529, 778-n9z2 remain attended-or-restage-only.
