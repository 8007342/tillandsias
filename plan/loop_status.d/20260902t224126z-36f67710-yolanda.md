## Cycle 2026-09-02T20:29Z-22:35Z — yolanda (windows), meta-orchestration full

Four packets landed, all pushed to windows-next and merged into linux-next by
the coordinator mid-cycle. Two of my own defects were found by a sibling and
fixed in the same cycle, which is the part worth reading.

- **793-zumy REMAINING 2** — the Enumerated/Reachable/Placed rungs were
  modelled, pinned by nine tests, and called by nothing. Wired the producers:
  a bounded read-only shell run INSIDE the inference container for identity and
  `/dev/dri` reachability, and `ollama /api/ps` residency for placement, both IO
  edges injected so the composition is testable without podman, a container, a
  GPU or a network. Emission via `CapabilityDocument.render_nodes` and
  `accel_proof=`, both appended last so yoga's `grep -m1` on `legacy_tier` is
  untouched.
  Then **yoga measured it on the merged head and found two defects of mine**:
  a hardcoded `tillandsias-inference` where dev hosts run
  `tillandsias-dev-inference`, and — worse — `accel_proof=-` conflating "nobody
  to ask" with "asked and found nothing", so a host whose lane was WORKING read
  identically to one with no accelerator. I shipped that conflation hours after
  writing three doc comments in the same file warning about exactly it,
  including one saying under-claiming is the direction that gets missed because
  the wasted work looks like diligence. Fixed: single-source container hook,
  and a `container-lane` enumeration gap rendering `unknown` against `none`.
- **964-9yyp** — implemented, and the deliverable as written was REFUTED.
  Staging plan/ for the general ledger checks loses: a fold is CPU-bound in the
  YAML parse (9p costs 1.36x) and one staging pass costs 5.8s. The 24.6x is
  true about raw file IO (cat 40x, find 23x) and was being read as a claim about
  the checks. The win is entirely in the one step with tree-copy churn:
  archiver check 62.3s → 5.3s on its ledger-bound portion, 78.4s → 43.7s as a
  gate step, via a shared native-scratch probe that returns `$REPO_ROOT` on a
  fast worktree so Linux and macOS cannot regress.
- **803-49re** — Part A was fixed in ONE of two purge paths and read as done.
  `build-and-install-windows-local.ps1 -Purge` had been destroying the guest
  vault and leaving its credentials in Credential Manager for six days, on the
  installer a dev host actually uses. One shared clearer plus a guard keyed on
  the DESTROYING ACT rather than the flag name.
- **965-sxec** (p0, taken mid-cycle) — a gate that could not RUN the archiver
  asserted what it would have FOUND. Routed ruby-absence to exit 3, added a
  narrow forge skip (forge AND one tokenised cause), and pinned the VERDICT
  TEXT rather than the exit code — build.sh failed either way before the fix, so
  a test asserting only failure would have stayed green through the incident.

**The cycle's own theme, unplanned: one copy fixed and one copy missed.** The
fleet counted nine instances today and two are mine. The remedy that keeps
working is not more care on the second copy; it is a guard that makes the second
copy impossible to add silently, or a single source the second copy has to read.

**What I did not do, deliberately.** Did not run the `-Purge` discriminating
test — it destroys the operator's live distro on a machine they are using.
Did not install a Vulkan ICD for 793-zumy REMAINING 1 — provisioning the
operator's machine to make a criterion pass is not evidence. Did not verify
965-sxec's forge skip path; this host has no forge, and that is recorded as
unrun rather than claimed.

**Corrected myself once, on the record.** I reported the pre-push staleness
check as mtime-based and taught that to the coordinator as fact. It is
content-hashed; the mtime list it PRINTS answers a different question. I
designed around the false model for 25 minutes. The coordinator filed the real
bug (970-7fqk) and put the "ledger writes before the gate" rule in the skill.

**Friction:** six full gates this cycle at ~520s each. Three were avoidable —
two from writing ledger events after the gate, one from a `cargo fmt` failure
that printed loudly and still exited 0 without stamping.
