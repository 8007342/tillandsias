## Cycle — coordination tick (macuahuitl, orchestrator)

Integrated: osx-next aa6fb82bb (926-bin4 design slice — CAP_PTY_DATA_SESSION
variant decision with measured postcard skew evidence; packet ready, p1) and
windows-next 81d094972 (760-hzi4 producer half closed on yolanda; consumer
resolves a different root — fourth Windows dual-userland instance, fix shape
already dispatched: repo-relative rung under target/). One plan/index.yaml
conflict, both-appended events, resolved keep-both.

Filed: 932-3ck7 (green --check coexists with red litmus on trunk — decision
packet from lenovinha's 901-jtvi finding; the 718-ja7g regression survived
two packets behind a green gate).

Assignment board:
- macbook: PRIMARY 926-bin4 implementation (their design, their harness);
  warm-up: darwin run of scripts/test-litmus-step-model.sh (901-jtvi exit
  criterion). Fallback: 739-6r6n (macos-only-sources verification report).
- lenovinha: PRIMARY 731-eupn (empty-cloud-reply success discriminator, p1
  stable-milestone). Fallback: 747-knbp (image rebuild orphans launch tag).
- yolanda: continuing 760-hzi4 ladder rung (~1h); NEXT: msys run of
  scripts/test-litmus-step-model.sh (901-jtvi exit criterion).
- yoga: paused pending their operator's word.

Operator queue additions: 722-w7a2 (Trusted Signing identity — credential
provisioning, top-ranked p1, not agent-assignable); 932-3ck7 shape call can
be delegated but option B (trunk-litmus sentinel on the coordination tick)
is the coordinator's recommendation.

Velocity: ~30 closures this session fleet-wide; no deadlock/thrash/drift
alerts (both sibling branches integrated within one cycle of landing).
