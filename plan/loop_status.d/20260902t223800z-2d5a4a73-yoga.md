## Cycle — yoga 2026-09-02T22:38Z (meta-orchestration, full mode; continuation cycle, findings held only in context)
- WHY A SECOND CYCLE RATHER THAN WAITING FOR THE :53 CRON: the evening's cross-host findings (cold-vs-delta, the ms/1k unit defect, the scout-tier filing failure) existed ONLY in this session's context. That is the "an unfiled finding is a lost finding" rule pointed at myself, so the cycle was started to make them durable rather than betting on session survival.
- Start of cycle: preflight ok (tillandsias-proxy still down — operator); guards ok; lock acquired; boundary clean at 93603d4db. Claimed 967-xq5e, claim pushed BEFORE work (2babffffe). Overlap: 3 live claims elsewhere, none on my surface.
- 967-xq5e IMPLEMENTED. spec-index-ensure.sh did no derivation — it INHERITED TILLANDSIAS_EMBED_ENDPOINT and refused when absent, while every derivation lived in the FORGE lane (lib-common.sh twice, lib-dev-env.sh once). A git hook inherits its caller's environment, so on a host the variable arrived only by luck. MEASURED: macuahuitl 32 consecutive skips, ZERO non-skips ever, index 128 commits stale, healthy endpoint on localhost throughout; yoga built fine because ONE line in a git-ignored .claude/settings.local.json exported it. So the host lane had NEVER refreshed an index anywhere and this host carried a private workaround — the inverse of "some hosts go stale", and the framing that points at the fix.
- FIX: images/default/lib-embed-endpoint.sh holds the ONE derivation; lib-common.sh (both blocks, including 919-vvyv's fallback), lib-dev-env.sh and spec-index-ensure.sh all source it. images/default/ is the only directory present both in the image and in a host checkout, and lib-common.sh is sourced before the project clone, so a helper under scripts/ could not serve the image lane. Every documented property preserved (explicit-never-probed, PROBE-do-not-assume per 712-r5x8, /v1 normalisation both directions); the forge/host split moved INTO the helper as the default it picks, so 919-vvyv's fallback is deleted rather than duplicated.
- The refusal now NAMES WHAT IT TRIED. The old verdict named a missing variable and nothing that would supply it, empty stderr — it was not silent, it fired 32 times loudly into a log nobody had reason to open, because it read as configuration rather than breakage. A correct, honest, well-logged refusal can still be unreadable as a problem (macuahuitl's framing, adopted).
- VERIFIED: scripts/test-embed-endpoint-derivation.sh 10/10 hermetic (stubbed curl); live `env -u TILLANDSIAS_EMBED_ENDPOINT bash scripts/spec-index-ensure.sh` — the exact macuahuitl condition — returns ok:spec-index:built:22816; litmus forge-environment-discoverability PASS, inference-container PASS.
- A MISTAKE PINNED RATHER THAN QUIETLY FIXED: first wiring used `v="$(resolve_embed_endpoint)"`, a SUBSHELL, so the export died there and the verdict read `derived` while the caller's variable stayed unset. Caught by the test on its first run. Helper now sets a verdict GLOBAL so no caller needs the substitution; test arm 7 asserts the trap still reproduces so the contract comment keeps earning its place; arm 8 asserts the real caller has not regressed to it; arm 9 fails if any second `export TILLANDSIAS_EMBED_ENDPOINT=` STATEMENT reappears outside the helper (matching statements, not prose — grepping the string made three documentation sites read as violations on the first run).
- FOUND INCIDENTALLY AND PROVED NOT MINE: litmus:long-running-view-agreement STEP 6 (order 943-26rx) FAILS AT HEAD while that row reads `implemented`. Reproduced on a clean clone of origin/linux-next carrying none of my working-tree changes. `./build.sh --check` runs no litmus by design, so a green gate and a red spec coexist indefinitely — the 748-tkjx shape, and my own gate was green through every run tonight. Recorded as a measurement, not a cause; I did not diagnose whether it regressed or was never complete.
- FIVE OTHER EVENTS FILED, all captures on existing rows, no new rows: 963-vmbi (cold-vs-delta — the cost is ENTIRELY in the first build, 41s deltas after; plus pirria's mountinfo finding that the 801-a2by shared volume is mounted READ-ONLY and empty in a forge, so a guest structurally cannot seed itself even if it paid the 2h26m); 862-cq3x criterion 7 (two of three forges measured something real and could not land it, under one prompt in one hour — a property of the scout tier no single host can observe, since a host that cannot file also cannot file THAT it cannot file); 551 (a groundtruth case can go red because the CORPUS GREW — lenovinha's hours-old cheatsheet legitimately displaced an expected code chunk; 680-zphp solved the first instance of that class and named it in passing, so "we already have a guard" is wrong); 824-6qxh criterion 1 (discharged as to INTENT, reasoning attributed to pirria, row left open); 943-26rx above.
- CORRECTED IN PASSING, on esme's challenge: I had folded esme's mirror-403 into the gate class. pirria was refused BY the gate; esme was refused by the mirror relay AFTER the gate passed it. Folding them would have handed criterion 7 a credential defect it cannot fix. Layers kept distinct; only pirria's incident is evidence for that criterion.
- FOUR OVERREACHES BY ME TONIGHT, recorded as a habit rather than four incidents: pirria's 423 ms/chunk into a fleet floor; my own wall-clock into a property of the harness; a class flag raised to the coordinator without checking whether the trunk already owned it; and "my empty volume is the same shape as your stale one", which was false — my hook had built the index all along. Each was caught by the host whose data I was overreaching with. Three exchanges, three reversals, and every one turned on data the other host already had and had not been asked for. What moved them was always "what would I need FROM YOU to falsify this"; the concessions were sincere and did no work.
- UNIT RULE now on 963-vmbi: report ms per 1k CHARS plus the chunk-size distribution, never bare ms/chunk. My corpus is mean 728.6 chars/chunk (median 375, p99 3973, max 3999 against a 4000 cap), so my 185 ms/chunk is 254 ms/1k; pirria's matched probe at my chunk size and batch size gave 684 ms/1k — a real 2.69x, opposite in direction to the withdrawal it had already issued. Two hosts comparing ms/chunk over corpora split by line count are comparing their CHUNKERS.
