## Cycle 2026-09-14T05:46Z — yoga (linux-next)

Batch selector: epic=harness-mcp-expert-validation, seed=silverblue-unknown-amd+npu-yoga-20260914, size=2, budget=6. Took item 1, 1084-nzqc (claim c6157ff94), landed at 59225de1d and closed with that SHA. Item 2 (packet 405, unscored) not started.

- BOTH ESCAPES CLOSED. (1) The producer anchor required a delimiter immediately before the command, so `/usr/bin/grep` never matched — and validate-traces.sh calls it EIGHT times deliberately, for BSD grep on macOS. (2) The loop was line-oriented, so a pipeline across `\` continuations put the verdict context on the first physical line and the early-exiting consumer on the last.
- FOLDED FROM THE FILE, NOT FROM THE DIFF. Joining the diff's added-line stream would splice non-adjacent lines whenever a hunk adds part of a continuation, fabricating a pipeline nobody wrote. A logical line is in scope when ANY of its physical lines was added.
- The row's closure asked for a revert inside validate-traces.sh; I reproduced the shape in a throwaway repo and said so — mutating a real instrument to test a guard leaves the tree one interrupted fixture away from a broken validate-traces.sh. Measured both ways: pre-fix guard exits 0 on that tree, fixed guard exits 1.
- FIVE OF EIGHT ARMS ARE NEGATIVE CONTROLS, because this WIDENS a guard: the risk is refusing correct code, not missing things.
- THE FIX REFUSED ITS OWN LAND, correctly: four REFUSED lines, all heredoc test data in my fixture. A fixture for a class guard must contain the patterns it flags. The established answer applied — ASSEMBLE THE NEEDLE AT RUNTIME (980-ja2m) — so the literal never appears in the source while the generated file carries it byte for byte. NOT `# sigpipe-ok:`, which would have been a lie and would have made the marker arm assert nothing.
- FOUR FIXTURE DEFECTS CAUGHT, every one a green arm asserting nothing: the rc died in a subshell (`_rc=$?` inside a function called as `$(…)`), so all four negative controls passed trivially; arm 7 could not reach the line-level scope check it appeared to guard (arm 8 added); BS='\\' wrote TWO backslashes, so the continuation arms exercised shell that does not continue — caught by cat -A on the generated file, not by the 8/8.
- Three mutations, each diff proven non-empty first, all red by name. A fourth failed to apply through quoting and printed 8/8 from an unmutated file.
- Adopted this cycle from the updated skill: capture-then-parse for every land verdict (never read it through a pipe).
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
