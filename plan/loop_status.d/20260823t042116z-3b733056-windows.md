## Cycle 2026-08-23T04:21Z (windows/esmeraldinha — the floor publishes its rows, and a six-day-stale destroyer was caught before it ran)

- **Host**: esmeraldinha, Intel N100 (4 Alder Lake-N E-cores, no SMT,
  single-channel DDR4-2667), Windows 11 10.0.26200. Branch `windows-next`,
  fast-forwarded 10 commits `6d3648424` -> `fb80c579a` (= `origin/linux-next`,
  verified ancestor before the ff). Operator mandate 2026-08-16 respected:
  perf packets only, **no general drain**.
- **mcp_outage: forge-plan,project-info** — `check-mcp-expert-health.sh`
  answered `down:forge-plan,project-info`. Every read this cycle went to the
  filesystem or to the verified binary directly. Reason: fresh clone, experts
  never registered here. Naming it per `mcp_first_read_path`.
- **The stale instrument was real and was NOT invoked.** The WSL locus binary
  was mtime 2026-08-17 05:20:45, predating `07cfec83e` ("compact must not
  mutate on --help", 2026-08-22 07:52:42) — the same destroyer that deleted 16
  fragments on yolanda. Rebuilt in the gate's own locus; re-verified through
  `. scripts/plan-binary-probe.sh && resolve_plan_binary` at the RESOLVED path
  (mtime 2026-08-22 21:24:31, 41 capabilities). `compact` was never invoked,
  in any form. Evidence added to **851-cduu**, which this host independently
  reproduces.
- **The sharpening 851-cduu did not have**: this checkout was a FRESH CLONE 40
  minutes old and `./target/` did not exist at all, so every freshness signal
  inside the checkout was maximally reassuring and all of them were irrelevant
  — the gate's locus lives inside the WSL distro, outside the checkout, and
  survives re-cloning, `rm -rf target/` and `git clean -xdf` alike.
- **Capability matrix: esmeraldinha is no longer SILENT.** Both loci published
  through the self-serve path — `4 of 7 reporting` -> **`5 of 7`**, silent hosts
  3 -> 2. The in-guest row is the first WSL2 row in the matrix that is actually
  schedulable (`cpu/container/ollama` + `cpu/host-native/ollama`), because it
  was deliberately taken in the RUNTIME `tillandsias` distro rather than
  `tillandsias-build`. That demonstrates yolanda's distro-dependency caveat
  with a concrete divergence.
- **Vendor universality CLOSED, and it AGREES with yolanda** — no correction
  owed. Intel reproduces every structural limit: `/dev/dri` absent,
  `/dev/accel*` absent, `/dev/dxg` present, guest PCI bus carrying only virtio
  plus one Microsoft `0x1414:0x008e` class-`0x030200` function, **no `0x8086`
  function crossing**. Stated loudly and separately: my rows confirm the GPU
  half only. **The N100 has no NPU at all**, so this host cannot witness
  `wsl2-npu-not-exposed` on Intel — its absence is "no such device", not
  "device hidden". The NPU half still follows from the mechanism, which IS
  confirmed here, but the evidence grades differ and the record now says so.
- **858-tnnv filed and implemented** — the Windows low-end floor. Decode
  `qwen2.5:0.5b` **29.64 tok/s**, `phi3.5:3.8b` **6.53** — both at or above
  their bare-metal Windows figures, so **the WSL2 substrate costs nothing on
  the CPU decode lane** (a useful negative result for 718-nkm2). Embedder
  **408.3 ms/embed** at batch-64 = **14.6x** macuahuitl's 28 ms; batching buys
  only 6.6%. Expert-layer floor for 853-6gz3: **~2.3 s per unfold-and-verify
  sub-query and STRICTLY SERIAL** (`OLLAMA_NUM_PARALLEL=1`, 4 cores, one
  memory channel), so the floor is `N x 2.3 s` in the fan-out width — a
  qualitative difference from macuahuitl, not a slower version of it.
- **855-wrr3 NOT claimed** — left with the Silverblue host as instructed; a
  comparison note was added to it instead.
- **iGPU adjudicated honestly**: `present-unusable` /
  `wsl2-no-dri-render-node` is the correct row for the distro's current
  provisioning state (ollama's own discovery finds `library=cpu` and nothing
  else even with `OLLAMA_VULKAN` true). But this host measured on 2026-08-17
  that the same UHD IS reachable in-guest via `/dev/dxg` + Mesa `dzn` at 100%
  offload and 2.16x prefill. The reason string is a **provisioning** statement
  wearing the costume of a **hardware** one. Evidence added to **793-zumy**,
  which already names that defect — no duplicate packet filed.
- **858-ihcb filed** (bug, p2): `bench-inference-floor.sh` measures prefill
  against a warm prompt cache — its warm-up reuses the identical prompt, so
  `prompt_eval_duration` covers a cache hit. It reported 1334.59 tok/s where
  the controlled 793-zumy-method figure for the same model/lane/machine is
  88.3. **Every `prefill_tok_s` this harness has emitted on any host is
  inflated.** Decode and `embed:` are unaffected. Also: the `project:` line
  multiplies two superseded constants and is right only by cancellation.
- **859-b2zc filed** (bug, p2): `check-capability-row.sh` resolves the host via
  `hostname` only, and the `hostname` binary is absent from every Fedora image
  here — both WSL distros and, verified directly under podman,
  `localhost/tillandsias-forge:v0.4.260817.1`. So 850-bif2's self-serve gate
  answers `unavailable:host-unresolvable` inside the forge and has never been
  able to ask it to publish. **That is a mechanical explanation for one of the
  two remaining SILENT hosts.** `agent-identity.sh` already holds the correct
  chain (`tillandsias_node_name()`, order 743-mgf3, whose comment says "some
  environments ship no hostname"); 850-bif2 re-derived it inline and dropped
  the fallbacks — the 704-zcgi shape on a different probe.
- **Hooks were installed nowhere on this fresh clone** — `.git/hooks/` held
  only `*.sample`, `core.hooksPath` unset at every scope, so
  `pre-push-linux-next-merged.sh` was enforcing nothing until
  `scripts/install-hooks.sh` was run. Hook enforcement is a post-first-build
  property, not a post-clone property; recorded on 851-cduu.
- Stranded sweep: `population=2 in_progress=2 stranded=2` (831-ezea, 847-wgy4).
  Neither mine and nothing bulk-closed. 847-wgy4 was claimed by lenovinha in
  the very head this host fast-forwarded onto, so that row is a detector false
  positive by construction — freshly-claimed and abandoned are indistinguishable
  to it.
- Minor, captured rather than filed: `loop-status-append --help` is not
  handled either — the flag falls through to the fragment parser and errors
  with "carries no `## Cycle` section". Non-mutating, so not the 07cfec83e
  hazard, but the same unparsed-argv family.
- Residuals named, not hidden: no alternative low-end runtime evaluated (855
  criterion 5) and no TRUE cold-bootstrap number (855 criterion 6 — the WSL
  cargo cache survived the re-clone, which is the 851-cduu hazard rather than
  a cold start).
