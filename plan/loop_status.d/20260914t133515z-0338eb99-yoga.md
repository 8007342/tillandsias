## Cycle 2026-09-14T13:35Z — yoga (linux-next)

Batch selector: epic=harness-mcp-expert-validation, seed=silverblue-unknown-amd+npu-yoga-20260914. BOTH BATCH ITEMS WERE UNWORKABLE FROM THIS HOST and I recorded why rather than claiming them: 1109-t8kw I released last cycle with the measurement that part 2 needs an AT-RISK low-end host (yoga prints SAFE), and packet 405 is a live codex-vs-opencode comparison needing provider budget authorisation and a 4h-per-host rate limit. Took 865-r6dt from plan_next instead (claim 3642b80e3); closed at ae6bdfa83. One more fix landed at be9f971d0.

- 865-r6dt WAS AN AUDIT, NOT AN IMPLEMENTATION. Both named defects are fixed in this tree, measured rather than inferred from the row's age: the corpus-declaration fixture supplies an index (9/9, sets=4 total=33 pass=33 fail=0 skipped=0, against the row's 31/33), and the 394 references are comments and @trace headers with no live status pin (22/22). Attribution consistent with fcfb909a2, not bisected, and said that way.
- CRITERION 3 ASKED FOR A CI-FULL RUN "to confirm what it unblocks rather than assuming" AND THAT EARNED ITS KEEP: 32 passed, 1 failed, with litmus:expert-groundtruth-harness PASSING (101.8s) and the remaining red being a different test.
- THE RED WAS MY INVOCATION, AND A REAL FIXTURE DEFECT UNDERNEATH IT. litmus:release-gates-run-locally STEP 20/20, not a budget kill (898.5s against 900s, rc=0, the fixture reporting its own arms). Case 11 requires the memo to be TAKEN; case 12 asserts TILLANDSIAS_FORCE_CHECK=1 BYPASSES the memo; case 11 never cleared it. I had launched ci-full with that variable, so case 11 failed by case 12's own contract and reported "memo hit path wrong". Three regimes measured, the middle one the falsification: 14/14 clean, FAIL with the variable, 14/14 after the fix with the variable still set; normal regime re-verified 14/14. Landed at be9f971d0 under 1184-u5mg.
- IT IS 1109-t8kw's CLASS IN A RELEASE-GATE LITMUS, reachable by any host running --ci-full with a forced check, so it is recorded on that row too.
- READING THE SILENCE COST ME TWO WRONG CONCLUSIONS FIRST: that the step's stdin was inherited and blocking (refuted — fd 0 was /dev/null, the shell in do_wait waiting on a child), and that zero CPU meant blocked (refuted — pstree showed a five-deep leaf pipeline of short-lived plan-binary runs that turned over between two samples). A wrapper accumulates no CPU by design; measure the leaf, not the pid you hold. Same error as the stripped-PATH scan last cycle.
- The Monitor's STALL alarms were right to fire and right to be non-terminal: a long step legitimately produces several five-minute silences, and the alarm is a prompt to LOOK, not a verdict.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
