## Cycle 2026-09-05T16:43Z→16:55Z (macos/tlatoanis-macbook-air — coordinator rulings + 317 current-guest slice)

### WORK DRAINED
- **980-ja2m**: TTL SUSPENDED, not expired. Recorded on the packet with the reasoning,
  not just the verdict: the 24h rule (641-e2qa) assumes a claimant who can re-take the
  work, and macneo cannot push until the grant reset (1025-a896). Names both accidental
  releases — tlatoanis-macbook-air 14:49Z, macuahuitl 16:41Z — so the next sweep sees
  the stale claim is deliberate.
- **1067-24q6** -> p1, with macuahuitl's `--list-live` reproduction as the sharper
  instance: a read-shaped FLAG that writes, run every cycle. Added a third defect they
  did not flag — their fragment was written under host bucket `linux`, not `macuahuitl`,
  so the release would have landed UNATTRIBUTABLE. Criteria tightened: the opt-in must
  be the only writing path, and the fixture must assert EVERY documented read/list flag
  leaves the ledger byte-identical (a bare-invocation-only arm passes while --list-live
  writes).
- **1068-cxmf** filed: the four unresolvable declared closures, enumerated and
  grep-confirmed absent. They are FOUR DIFFERENT defects — one rename drift with a
  near-miss (795-jeym), two missing tests (795-tb4a, 795-vq6b), and one on an OBSOLETED
  packet (795-bmbq). That last suggests the real defect is the COUNTER not filtering by
  status: the line may drop 4 -> 2 with no closure work.
- **317** current-guest slice, lease released.

### 317: THE BLOCKER IS NOT PRISTINENESS
The next_action sends criterion 2 to a pristine guest on the reading that installing is
a first-boot event. Containerfile.base:38-41 says harnesses install EVERY_LAUNCH via
ensure_forge_harnesses (lib-common.sh:2635, order 181) in the FORGE CONTAINER. A guest
that has launched a forge before exercises the same path as one that never has.
MEASURED blocker: no forge container and no tillandsias-default image in this guest.
That is an IMAGE, not a destructive re-provision — so the queued operator ask may cover
less than assumed.

I nearly filed "seven harnesses missing" from the first probe. Absence from the guest VM
is CORRECT — they are container-side. Wrong layer, measured confidently; caught by
reading the Containerfile instead of trusting a clean-looking result. Same shape as
1004-x9ua reading the operator's PATH as the host's inventory.

### HOST FACTS WORTH KEEPING
- A bare `target/release/tillandsias-tray` CANNOT start a VM here: no
  `com.apple.security.virtualization` entitlement. Guest work must go through
  /Applications/Tillandsias.app or a freshly built bundle.
- The tray REFUSED to stage its guest binary (701-kgvk): already-staged 03:14:35Z is
  NEWER than the installed bundle's 03:10:27Z. The guard is right, and it bounds what
  any guest measurement on this host can claim about THIS checkout.
- Anomaly, recorded not diagnosed: podman reports the proxy container "Exited (0) 292
  years ago" — 292 years is the i64-nanosecond span, so it reads like a zero/overflowed
  creation timestamp.
