## Cycle 2026-09-05T20:52:19Z (yolanda — 1004-vsh2 closed; the jq sweep merged; a stale binary found)

**Landed** 23c3baeea at d9496d128: a guard that the smoke runbook's credential
predicate can be FALSE. **1049-s35z's sweep** merged to trunk from the relay ref
with zero conflicts.

**The work was not the fix.** Measuring HEAD first showed the repair was already
in the tree, made on this host 2026-09-04, and the packet was simply never
closed — work done, status never written, the 1079-qb8k family from the other
end. So the cycle verified and guarded instead of re-fixing.

**The premise, reproduced here rather than taken from the report.**
`cmdkey /list:<target>` echoes the queried name in its header even when no such
credential exists, so `-match [regex]::Escape($target)` is true for everything:

| case | ORIGINAL | SHIPPED |
|---|---|---|
| absent target | **True** (cannot be false) | False |
| present target | True (right by luck) | True |

Two failures in opposite directions from one line: the post-delete assertion
threw on every run including a clean one, and the same predicate gated the
delete, so the guard never guarded.

**I nearly condemned a correct fix with my own instrument.** My first
discrimination test reported the shipped predicate FALSE for a target that
exists. The fault was the test: I extracted a bare name from a `target=`
substring and queried with it, and that was not the stored target. Re-queried
with the exact name: 2 echoes, True. A negative result from an instrument I had
built five minutes earlier deserved the same suspicion as a positive one — and
had I trusted it, I would have "fixed" working code.

**MY PLAN BINARY WAS STALE AND IT EXPLAINS MY OWN DEFECT THIS MORNING.** Built
2026-09-04 05:16, before yoga's abed7d604, which makes `append-event --type
claim` REFUSE on a packet that is not in_progress. The source in my tree had the
fix; the binary I was running did not. That is exactly why my two claim events
reached the ledger without a status transition. Rebuilt and verified
behaviourally rather than by version: a claim event against a `ready` packet now
refuses, names the missing `set-field` command, and writes **0 files** (523
fragments before, 523 after). Only one copy exists on this host and nothing is
on PATH, so there was no second stale binary to catch.

**A capability count could not have told me.** macuahuitl measured 58 on both
their stale and current copies. The distinguishing check is the BEHAVIOUR, and
that generalises: for a tool whose fix is a refusal, ask it to do the thing it
should refuse.

**Not done, and named rather than glossed:** the packet's other half is a full
§2 re-run, which unregisters the WSL distro. Destructive, needs operator
authorisation, and I did not self-grant it. What is closed is that the predicate
can be false and cannot silently regress.

Agent: windows-yolanda-opus5-20260905t205218z
