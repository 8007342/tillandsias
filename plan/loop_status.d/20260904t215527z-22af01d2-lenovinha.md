## Cycle — lenovinha 2026-09-04T23:20Z — I reintroduced a Windows deadlock; reverted, packet reopened
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T21:55:27Z`:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
      flow: cycles=8 avg_completed_per_cycle=0.13 avg_commits_per_cycle=1.75 overhead_ratio=14 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      timing: steps=29174 build_check_ms_avg=141686 build_check_mix=mixed:forced=383,memoised=108 litmus_ms_avg=12916 slowest=litmus:tool-materialization:304387 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      repeat: window=since=2026-09-04T21:55:27Z steps=412 top3=build-preamble=10,build-check=7,litmus-suite=7 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      recur: window=7d runs=26889 steps=431 top3=build-check:runs=341:total_ms=51027772:avg_ms=149641:fail_pct=31,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=236:total_ms=7863293:avg_ms=33319:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=236:total_ms=5775508:avg_ms=24472:fail_pct=0 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=68 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=236:avg_ms=33319:fail_pct=0:saved_ms_upper=7829974,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=236:avg_ms=24472:fail_pct=0:saved_ms_upper=5751036,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=218:avg_ms=19565:fail_pct=0:saved_ms_upper=4245779 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=- ready=456 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=dirty traces=current
      verdict: attention:worktree-dirty
- I BROKE SOMETHING SERIOUS AND DID NOT CATCH IT. 1034-ihxw's fix (399415ee4) tested `[ -x "$absolute" ]` in gate-stamp.sh. Order 889-8tcb, TWENTY LINES ABOVE the assignment I edited, says verbatim: "The first version tested `[[ -x "$absolute" ]]` and DEADLOCKED EVERY WINDOWS HOST... yolanda could not push anything at all." Reverted in 0a1419ffe; packet reopened with --reopen-evidence.
- MY PREMISE WAS WRONG, NOT JUST MY CODE. The index sourcing is not an oversight: it is the only source both sides of the Windows boundary can agree on, because WSL through /mnt/c (drvfs) reports every file executable while Git Bash sees real bits, and a capability probe answers PER ENVIRONMENT rather than about the tree. On POSIX hosts index and worktree agree (0 disagreements across 4537 files on yoga, 4553 here), so the unstaged chmod I "fixed" is invisible BY DESIGN.
- HOW IT ESCAPED, and this is the reusable part: `./build.sh --check` RUNS NO LITMUS (748-tkjx). litmus:mode-only-regression-gate exists, pins exactly this property, and states the Windows reason in its own arm text. I ran the gate FOUR times on that change and every run was green. macuahuitl's full-output pre-build litmus caught it. A guard can be perfectly written, correctly bound, and still absent from the lane the author is standing in — so "my gate was green" is not evidence about anything the gate does not run.
- WHAT SURVIVES AS A REAL DEFECT, and it is a different one: an unforced --check returned rc=0 on a tree whose exec-bit guard refuses it. The guard reads the worktree and works; the stamp deliberately does not; so the MEMO skipped a guard inspecting state the stamp does not cover. That is memo SCOPE, not the digest. Recorded on the packet with the candidate shapes and an explicit "do not touch the digest".
- SECOND-HOST REVIEW of macbookair's 1033-ev5r, posted as a hand-written fragment (600-c266 — the packet is osx-next-only and append-event cannot see it). VERDICT: the principle is right, the match does not enforce it. `*cargo\ test*` accepts prose that CONTAINS the phrase, including "This is hard to verify; cargo test would not cover it, so judgement is required" — a closure stating the row is unscoreable passes the scorable-obligation gate. Their negative control passes because its prose avoids the literal string, so it exercises a different shape from the one the widening admits.
- AND I CHECKED WHETHER IT WAS NEW: it is not. The pre-existing `*scripts/*.sh*` and `*litmus:*` patterns have the same weakness ("We never wrote scripts/foo.sh for this" is ACCEPTED). So the widening EXTENDS a hole rather than opening one — but materially, since `cargo test` appears in ordinary prose far more often. Recommended anchoring all four rather than narrowing one; did not land it, as it is their packet and a wider blast radius than a review note should take unilaterally.
- LITMUS AFTER THE REVERT: both reds cleared — litmus:mode-only-regression-gate and litmus:gate-stamp-portable-digest have no failing steps. Two OTHER failures in the suite are not mine: litmus:freshness-inventory-shape and litmus:guest-container-metrics-wire-shape (a WIRE_VERSION bump check). Reported rather than touched.
- ONE THING THAT WORKED: the failing litmus line reads `[FAIL] rc=1`, which is the 1018-5f5a change I landed this evening doing its job on my own regression.
