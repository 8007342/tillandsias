## Cycle — macuahuitl 2026-09-05T01:10Z (coordinate-multihost-work, cron :41 lane; FOLDED INTO the 00:09Z meta cycle, which holds the checkout lock with its union land in the gate)

- **Scheduler posture:** session crons armed (coordinate `:41` hourly, meta `9 1-23/4`); this fire did not stack: audit, heartbeat and dispatch ran read-only and by message inside the meta cycle; the record and the next union wait for the land.
- **Heads at 01:10Z:** main 377ba7794 (v56.9.4.1 daily; stable channel still v56.9.2.1), linux-next e6a80609f (yoga landed 972-umik's SERVER default flip, then caught its own regression: all four clients still default to plaintext, so a released binary would open a plaintext connection to a server that refuses it, the control wire broken end to end; yoga is reverting the default to Off inside the shared parser while keeping the reader and the listener conversion, and 972-umik goes back to in_progress with the slice boundary rewritten as the DECISION: the flip lands once, atomically, with the last of the six readers and a cross-crate server-client test; lenovinha claimed 1031-q4pb), windows-next +8 (yolanda: 1032-62rx client-side wire-version refusal tested via WSL, "Windows hid a compile error", records, attest), osx-next +8 (macbookair: 873-vgyg source half 3965419d0, its record, the 1019-ivia claim). The coordinator's local union (windows-next +4, osx-next +3 from 00:41Z, plus the cycle's records) is in the land's gate, 17 ahead. **Merge decision:** windows-next's +8 merges in the next union; osx-next's +8 is HELD until macbookair's 873-vgyg revision lands, because 3965419d0's unconditional --replace violates order 494 (pirria reproduced the collision against a still-running sibling, and --replace would kill it); a daily must not carry it.
- **Claims 4 live** (972-umik yoga, reopened; 980-ja2m macneo; 1025-a896 coordinator; 1031-q4pb lenovinha), stranded 0, expired 0.
- **Heartbeat by asking, every host answered within the hour:** lenovinha on 1031-q4pb (claimed); yoga reverting 972-umik's default (the atomic flip is the packet's shape now), 1028-3eiz completed at 04ade3b56; yolanda landed 1032-62rx's test, next 1042-svey (openssl into the builder distro); macbookair: 873-vgyg back to in_progress for the ownership revision (wait-and-retry on a foreign holder, replace only a corpse), then 1019-ivia C2; macneo holds for slice (b), credential dead; esmeraldinha: v56.9.4.1 floor smoke PASS (report landed under plan/issues), next the parallelism sweep and the clean gate after 1042-svey; pirria: reproduced 873-vgyg (62 ms, podman 125 inside exit 1, orphan self-removed in ~4 min); the sweep is blocked two deep (toolbox absent, then the toolchain), refused TILLANDSIAS_SKIP_TOOLBOX=1 correctly (a host-native build measures a different regime); regime for the sweep when unblocked: enclave UP.
- **Operator directions this hour, recorded:** (1) reduce builder parallelism as the first trial (protocol written before the numbers; baseline at 20 jobs: io-full peak 55.5%, 95 of 150 samples over 20%, with zero rustc during the test phase, so the stalls are not the compiler's alone); (2) the tune-ups belong in the build scripts so every Linux builder picks them up (packet filed this cycle: job cap by host class, nodatacow target trees, builder I/O and CPU limits, dirty-bytes advisory); (3) esmeraldinha and pirria run the floor parallelism sweep (packet filed; protocol sent); (4) a separate build filesystem by repartition is workable but a nodatacow loop image or a planned reinstall beats an in-place shrink, and cleanup is not the gate (build state ~130 GB: images 56.6, volumes 20, target 47, nix 20; disk 952/751/198 GB).
- **Release this cycle (meta duties):** union litmus on the 00:41Z merge __LITMUS__; the daily v56.9.5.1 is cut ONLY after yoga's revert of the 972-umik default is on trunk (no daily carries e6a80609f's one-sided flip), from the landed trunk plus windows-next's +8 if green, carrying the tray honesty fixes, the wire collapse and the tests; the STABLE candidate is the daily after 1031-q4pb and the 873-vgyg revision land, per the operator's answer.
- **Operator queue, plain asks:** (1) security log for 2026-09-04: OAuth token creations with times; actor and reason per destroy row; any revocation email. (2) No `gh auth login` on any host. (3) AMENDED on pirria's measurement: install `toolbox` on pirria (CachyOS: `pacman -S toolbox`), let the tillandsias-builder image build there, and put the toolchain inside it; rustup alone would change nothing, because `./build.sh --check` refuses at the container boundary first ("FATAL: 'toolbox' is not installed on this Linux host … deliberately NO host-native fallback", exit 1, measured 01:1xZ) and no builder image exists on that host; this blocks pirria's plan binary AND the floor parallelism sweep. (4) 978-juw4 permission. (5) 996-bcjx identity call. (6) delete the ten quarantined trees on esmeraldinha. (7) whether unsigned Windows artifacts may be called stable. (8) NEW: `sudo sysctl -w vm.dirty_bytes=1073741824 vm.dirty_background_bytes=268435456` on macuahuitl before the next gate you watch from the desktop, the one root-only arm of the freeze trial.
- **Velocity:** closures this hour 1028-3eiz, 1032-62rx's test half (972-umik landed then reopened), 873-vgyg's source half (revised back), 1039-b64k's regression half; filings 1042-svey, 1043-c73v, 1044-na6u, 1045-8yyw, the capability-row packet, and this cycle's two; no High-Velocity Alignment Event.
