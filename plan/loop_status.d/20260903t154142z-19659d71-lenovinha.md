## Cycle — lenovinha (linux_immutable, linux-next), 984-i4k2

CLOSED 984-i4k2 at c42095568. Started with the fleet question as instructed,
and it was worth doing first.

FOUR HOSTS wrote append-event output into plan/index.yaml after the fragment
fix landed at 823e3ac0d (21:55Z): lenovinha-tillandsias-forge,
pirria-tillandsias-forge, macuahuitl-tillandsias-forge, yolanda. Attributed
by extracting the `host:` line from the event blocks each commit added —
every commit carries the operator's git identity, so the author field cannot
distinguish hosts. Three of four are FORGES, which is the mechanism: a forge's
binary is baked at launch from the seed and never rebuilt in-cycle.

EXPOSURE MEASURED CLOSED, with a positive control rather than an absence of
news: zero event blocks added to the base in the 12h since 03:47Z, against
240 fragment files touched in the same window. The channel is heavily in use
and none of it lands in the base — so the silence is hosts having rebuilt,
not hosts having stopped writing. A bare "no recent occurrences" could not
distinguish those.

THE DETECTOR: build.rs bakes the revision of the sources it compiles
(build-id); source-revision computes what a rebuild from a checkout would
produce. ONE hash, include!'d by build.rs and used by the crate, because two
could drift and a drifted comparison reports skew forever or never — this
order's defect arriving through its fix. Capability comparison untouched; its
verdicts still win, since a missing subcommand is a bigger fact than a stale
one. A binary that cannot report its identity at all reads unknown-currency,
never none. Both new verdicts got advice arms: the existing `*)` fallback says
"No capability skew", so without them a non-green field would sit beside
reassuring prose.

TWO OF MY OWN BUGS, NEITHER FOUND BY READING.
1. source-revision read args.get(2) where `args` is already shifted, so it
   always hashed the default directory and returned a valid, matching,
   plausible answer for every input — including a nonexistent path, rc=0. I
   caught it because pre-fix and HEAD returned the SAME hash, which cannot be
   true of trees differing by four files. I refused a matching value from a
   detector whose whole job is to distinguish.
2. The first version compared revisions whenever the checkout had a
   crates/tillandsias-plan directory — and the skew litmus builds a
   MANIFEST-ONLY fixture checkout, which satisfies that while containing no
   sources. It reported rebuild-required for a synthetic in-sync scenario.
   THE LITMUS CAUGHT THAT, NOT ME — the first time tonight an existing test
   found a false positive in something I had just written, rather than the
   reverse.

PROCESS SLIP, recorded because attribution is standing and the commit is
pushed: c42095568's message omits the Co-Authored-By and Claude-Session
trailers. Written through a heredoc with them left out. Not correctable in
place; present on the rest of the cycle's commits.

NOT MINE: the embed-endpoint-discoverable step of that litmus fails
identically on a stashed clean tree.

metrics:
  gate: TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check green
  fixture: scripts/test-expert-capability-behaviour-skew.sh 5/5
  litmus: expert-capability-skew-honesty step 8 (skew=none baseline) passes
  fleet: 4 hosts affected, 0 base writes in 12h, 240 fragments same window
