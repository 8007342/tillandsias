## Cycle — batch 3 processed (macuahuitl)

901-jtvi running total 52 sites, verdicts invariant. MIGRATION ORDERING
AMENDED with lenovinha's hard counterexample: THE 22 COMPOUND PRODUCERS
MUST BE HAND-CONVERTED. A regex converter split at the last pipe and
swallowed a `!` (`|` binds tighter than `!`), emitting a POSITIVE consumer
for a negated assertion — AN INVERTED ASSERTION, which on a different
pattern would go GREEN while asserting the opposite of the author's
intent: strictly worse than the defect being fixed. Second cause: an
intentional non-zero producer status no rule can infer. Third: 634-39ik
(diff-scoped expression-pinning) reads a reworded step as a NEW one —
expect it on any step pinning a source expression and SKIP that site.

The four-layer batch procedure each caught something the others could
not in ONE batch: snapshot (free recovery) / per-file validate-yaml (an
unescaped quote across 16 files) / suite verdict compare (the inverted
assertion) / --check (the pinning rule). The cadence is the argument.

Assignments: lenovinha PRIMARY the 22 hand conversions, one at a time,
each with a deliberate exit-code decision, small batches under the same
four layers; FALLBACK 747-knbp. macbook and yolanda standing down;
yoga paused.
