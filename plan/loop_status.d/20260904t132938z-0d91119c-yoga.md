## Cycle — yoga 2026-09-04T13:53Z (advance-work; cron :25) — 1003-444f: the gate tests the workspace, and a check I wrote got deleted
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T13:29:38Z`:
      repeat: window=since=2026-09-04T13:29:38Z steps=131 top3=build-preamble=9,build-check=7,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=5
      skippable: candidates=27 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=133:avg_ms=17850:fail_pct=0:saved_ms_upper=2356216,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=130:avg_ms=15860:fail_pct=0:saved_ms_upper=2046031,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=133:avg_ms=14232:fail_pct=0:saved_ms_upper=1878755
      plan: packets=693 ready=451 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=clean traces=current
      verdict: attention:experts-never-called
- READING IT. Third consecutive yoga entry, so `recur:` has a series from this host now. The plan-ledger-tests skippable candidate is stale as printed — its step name still says `-p tillandsias-plan` and this cycle replaced that invocation with `--workspace`, so its 130-run history is a history of a step that no longer exists. Worth knowing on 1008-85ry before anyone memoises against that key. `verdict: attention:experts-never-called` honest and mine: gate mechanics and cargo measurements, no ledger questions.
- 1003-444f COMPLETED (3330dbbdf, merged 1e82211f8). `./build.sh --check` now runs `cargo test --workspace --no-fail-fast -- --test-threads=1` through the existing baseline ratchet: ran=331 -> 2199, tolerated=1, stale=0, gate 103s -> 137s. --workspace rather than a crate list on the tree's own precedent — `--lib` was dropped 2026-08-22 because scoping the ratchet to one target "recreated that blind spot inside the fix", and a hand-maintained roster is that mistake with more entries.
- EVERY CRITERION MEASURED PRE-FIX BEFORE THE DIFF, per the rule adopted last cycle. C1 pre FAILS (21 of 22 crates neither run nor named) post PASSES. C2 pre FAILS (zero occurrences of `cargo metadata`/workspace members in build.sh or check-test-baseline.sh; the selector was the literal `-p tillandsias-plan`) post PASSES with cargo as enumerator. C3 pre FAILS post satisfied structurally. C4 pre FAILS — a deliberately-red test in tillandsias-core left a forced `--check` at **rc=0**, in a log that printed `Checking tillandsias-core`: compiled, never run — post PASSES at rc=1 on the identical probe, reverted both times. C5 moved to the outcome as a constraint, not counted: `--no-fail-fast` was already present and a criterion the tree passes has no teeth.
- A CHECK I WROTE AND THEN DELETED RATHER THAN SHIPPED. scripts/check-workspace-test-coverage.sh was to name crates whose suites produced nothing; it matched the transcript's `deps/<stem>-<hash>` names against workspace members. The stem is a TARGET name — tillandsias-headless declares bin `tillandsias`, macos-tray and windows-tray BOTH declare `tillandsias-tray`, integration targets are named after their file — so it reported headless as silent in a run where headless's suite had just caught a red. A check that misreports is the defect this order is about. Deleted; `--workspace` satisfies the criterion structurally because nothing is excluded, so nothing needs naming.
- WHAT THE FIRST RUN FOUND, on schedule for the packet's own argument: two red targets in tillandsias-headless invisible to every gate. proxy_cache_policy's known-red (845-7a88) became `tolerated=1` rather than never-exercised; and a stale COUNT pin — 5 CLI cleanup dispatches against 6 after `--ensure-enclave` — whose own assertion message says "a new dispatch that wraps itself in the cleanup is compliance, not drift". Same cause as ae85ee471 (1022-y7kc cause 1): one change, two stale pins, this one in a target no gate ran. macuahuitl fixed the same pin independently on the trunk; I took THEIR message in the merge, since it names the flag and cites both orders.
- FILED 1021-hf9e: tillandsias-headless's bin suite gives a different failure set on every parallel run (three runs, three sets; each offender passes in isolation). That is why the gate is serial — a ratchet compares failure SETS and a flaky set makes it a coin flip. Serial also measured FASTER, 51.6s against 61s, so determinism cost nothing. Serial is a mitigation, not a cure; the ca_path HOME race is the precedent for removing shared state rather than serialising around it.
- CORRECTED IN PUBLIC, twice: the packet title said "two crates" and it was ONE (I miscounted build.sh's headless line, which lives in the --test block); and check-test-baseline.sh's header still claimed `--check` wraps `-p tillandsias-plan --lib`, stale for a fortnight, quoted to two hosts as current behaviour and into this packet's own premise before anyone ran the gate. Fixed in the same commit next to its cause.
- ORPHANED GUARDS (1022-y7kc cause 2), activation surface each should have had, as asked — not wired this cycle. `check-ca-path-literals.sh` (mine, 998-3z6g): STATIC, greps source for the literal CA path outside its declaration — belongs in `./build.sh --check` beside the other source-shape guards. `check-ca-bind-migration.sh` (mine, 998-3z6g): RUNTIME, reads a container's recorded bind source via podman — belongs in a post-build/e2e litmus lane or the `--diagnose` surface, never in `--check`, where a dev host with no enclave would go red on a healthy tree. `check-enclave-ca-consistency.sh`: RUNTIME, compares the live proxy's key secret against the bundle — same lane as ca-bind-migration. `check-inference-container-name-agreement.sh`: static if it only greps the name across scripts (then `--check`), runtime if it inspects a live container (then the e2e lane) — I have not read it and am not guessing; that one needs a look before it is placed.
