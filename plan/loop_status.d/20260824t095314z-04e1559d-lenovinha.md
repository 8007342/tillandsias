## Cycle — lenovinha (linux_immutable) 2026-08-24T09:53:14Z — 686-7qcm closed by exercising its criteria, not reading them

Full-mode cycle. All guards green; daily gate current (stamped this morning).

CLOSED 686-7qcm (completed, with evidence). Criterion 1 landed last cycle; this cycle I EXERCISED 2, 3 and 4 rather than inspecting them — the discipline I named after last cycle's premise turned out to be wrong.

CRITERION 2 (invisible-block report): MET, both halves. check emits 8 parked-block: lines naming dependent, blocker, parked status and the outstanding action where one exists; where the blocker is implemented with no next_action the line correctly stops at the status. `status <packet>` emits blocked-on-parked: rows per packet.

CRITERION 3 (evidence gate): MET — but NOT by the mechanism its wording names, and I nearly filed it as a gap. The criterion says "check hard-fails". check does NOT: a synthetic post-2026-08-12 fragment writing status: completed with no evidence event gets `ok: 1 packets` at exit 0. The real enforcement is diff-scoped and author-side, scripts/check-fragment-closure-evidence-added.sh wired at build.sh:1896 and pinned by litmus:fragment-closure-evidence-gate-shape. Exercised on a real staged fragment: violation:closure-without-evidence, exit 1; clean tree, ok:closure-evidence:0 checked, exit 0. THAT DESIGN IS BETTER THAN THE CRITERION AS WRITTEN, for the 699-dycj reason — build.sh runs check on every host, so a fleet-wide hard failure makes one host's hand-authored fragment everyone else's red build. Intent satisfied; mechanism deliberately different. Assuming-absent would have been as wrong here as assuming-present was last cycle.

CRITERION 4 (plan_next verification pickups): DECLINED on the criterion's own terms, with a measurement rather than an opinion. THE JOIN KEY DOES NOT EXIST. 50 packets sit at implemented; 21 carry verification_required; their agents[].id values are antigravity-gemini, claude-opus-highthink, codex-gpt55-highthink, opencode-bigpickle, host-kind-linux-mutable, host-kind-windows, host-windows-esmeraldinha, host-windows-yolanda. That field carries TWO KINDS OF IDENTITY with nothing structural to separate them — four model identities and four host affinities, and the host ones speak two dialects (a KIND vs a specific MACHINE). plan_next takes a pickup_role, a third vocabulary. Matching on it means pattern-matching a `host-` prefix and guessing whether the remainder is a kind or a hostname, and silently missing every model-verified packet. What would make it buildable: an explicit role/kind field distinct from the agent identity, or a published agent->role map. Noted but not promoted: 29 of the 50 implemented packets carry NO verification_required at all — if any population deserves a plan_next surface it is probably that one, and it needs no schema change to find.

FIXED A RED TRUNK LITMUS FOUND EN ROUTE. litmus:enclave-service-health-shape STEP 5 ("one vocabulary, both surfaces") was failing. Cause: it demanded the ASSEMBLED literal `fail:enclave-service-dead:service=` in scripts/check-enclave-service-health.sh, and f4f08709e correctly split that into `_class="fail:enclave-service-dead"` plus a `:service=${name}:...` concatenation. The contiguous string stopped existing; the BEHAVIOUR never changed — cycle-preflight prints the full vocabulary every run. A TEXT PIN BROKEN BY A CORRECT REFACTOR, which is exactly the weakness I flagged in two freshness stamps this week and here is a live instance. The step now pins the vocabulary TOKEN in both surfaces (what "one vocabulary" actually claims) and leaves assembly to the behavioural fixture. runtime-diagnostics spec PASSES.

FILED 869-cvdc, and it is a self-report. scripts/test-enclave-service-health.sh printed "11 passed, 1 failed" once, then 12/0 seconds later and 6/6 after that. I CANNOT SAY WHICH SCENARIO FAILED: the fixture prints one ok: line per PASS and nothing identifying a failure, so only the arithmetic differs — and I re-ran to look closer, which destroyed the evidence. That is precisely the trap the skill warns about under "read check-logs.jsonl FIRST", and I walked into it; there was no check-logs.jsonl either, since that file comes from local-ci runs not direct fixture invocations. So the first fix is OBSERVABILITY, not the race: name the failing scenario and exit non-zero (it currently exits 0 even with a failure). Lead offered but NOT diagnosed: several scenarios shell out to live `podman ps` on a host whose enclave set genuinely changes underfoot.

FRESHNESS: openspec/litmus-tests/litmus-runtime-diagnostics-shape.yaml was the target; running its spec is what surfaced the red litmus above. Stamp deferred to next cycle — the file itself is one step and passes, but auditing it honestly means deciding whether a one-step grep pin still earns its place next to the behavioural fixture, and this cycle already spent its judgement budget on the criteria and the red test.

ADVISORY: compaction not eligible (18 fragments). Deslop not due.
