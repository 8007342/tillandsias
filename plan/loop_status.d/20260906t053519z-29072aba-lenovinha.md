## Cycle 2026-09-06T05:35:19Z (lenovinha linux — advance-work: 1092-xwxv and 1093-hzhi filed, 814-5avq criterion 1 row measured)

MO-FULL: COMPLETE 5fe7e8452e7c7e8089b9756fe3fbcfacd03b9530 linux-next 5fe7e8452e7c7e8089b9756fe3fbcfacd03b9530

Direction: forge local experts remains the theme; reduced against
stable-milestone-v1, which the selector again ranked 2/8 (same seed, seed is
host+date deterministic, so the batch repeated).

THIS CYCLE PRODUCED FINDINGS, NOT FEATURES, and the largest one is that the
instruction I was following was false and I had written it myself.

1092-xwxv FILED. 793-qc6q's next_action — mine, from the previous cycle —
said the wiring was "now unblocked and is the next slice". There is no
inference call path in that crate to wire into. route_phase's only production
caller is routing_summary, the envelope renderer; policy_router::route_request
has ZERO callers (the greps that seem to find one hit metrics_server's
unrelated route_request_line); EngineSlotDescriptor is never constructed; and
nothing in the workspace depends on tillandsias-headless, so all three are
structurally invisible to the inference client, which lives in
tillandsias-plan. The deeper half: the only mechanism reaching a running
engine is engine_slots::pin_backend_environment, which appends
OLLAMA_LLM_LIBRARY to a podman launch keyed on a per-HOST tier — one server,
one library pin — so per-phase routing cannot be expressed through it at all,
and 793-qc6q silently depends on inference-engine-slots, specified and never
built, with no depends_on edge. Same shape as 1009-gccx one level up. I did
NOT add the obvious RoutingLocus::from_lane bridge: more unconsumed surface is
the defect being reported.

1093-hzhi FILED. check-scorable-obligation-added refused the two-channel
fragment shape its own README documents, counting an events entry's packet_id
as an unscored packet row. It matters because the refusal names a real
existing packet, and the obvious response is to add `unscoreable:` to it — an
in-place edit to a landed fragment, the exact operation the 2026-09-05
incident forbids. The guard points its reader at the one remedy the tree has
already learned is destructive. Satisfied it honestly with `unscoreable:`
stating why no litmus can be named yet, rather than naming one that does not
exist.

814-5avq CRITERION 1 ROW for lenovinha, and the packet's blocker does not hold
here. No host nix, no /nix, daemon inactive — and the toolbox rung RESOLVES:
capability ok:nix-capability:toolbox, nix-args returns --store <path> rc=0,
tillandsias-nix carries nix 2.34.8 as nix-core-2.34.8-1.fc44 while its base
image ships none. That is criterion 2's wording already satisfied, via Option
A, implemented as ensure_toolbox_nix. THE BLOCKER RESTED ON THE WRONG PROBE:
the packet reasons from `sudo -n true` failing to `sudo dnf install` being
impossible; here `sudo -n true` is rc=1 and `sudo id -u` is 0 — genuinely
root, unattended, in both toolboxes. Recommended yoga re-measure before
treating the rung as unprovisionable there.

WHAT I ALMOST FILED, and the correction is the point. I first probed
tillandsias-BUILDER, whose /usr/local/bin/nix is a 934-7jd4 host-escape shim
flatpak-spawning a host nix that does not exist, and was one step from
reporting "the rung reports ok while nix cannot run" — a false green of
exactly the class this session kept finding, which is what made it so
plausible. It was wrong because TOOLBOX_NAME is tillandsias-nix, a different
container with a working nix. I caught it by reading the variable rather than
assuming the builder toolbox was the toolbox. Two smaller traps in the same
half hour: NIX_FEATURES is ONE argument containing a space, so retyping it as
two flags builds a command line that fails for an unrelated reason; and I read
a PIPELINE's exit status for `sudo -n true 2>&1 | tail -1` and printed
"sudo -n: WORKS" directly beneath output saying a password was required —
the same pipeline-exit mistake I made with the gate in the previous cycle,
inside the same session, having already written it up as a lesson.

TWO GUARDS REFUSED MY PUSHES AND BOTH WERE RIGHT. The scorable-obligation
guard (above), and check-issue-citation-convention, which rejected
`accel_probe.rs:3986`-style citations: cite code by SYMBOL, because a symbol
survives every edit that does not rename it. Re-cited by symbol; the entry
passes. Worth noting that the packets I filed earlier in the cycle still carry
line numbers — the guard only scans plan/issues — and the principle applies to
them too.

CARRIED OVER AND CLOSED: the 15 full-suite litmus failures from the previous
cycle are attributed and NONE are mine. Zero of my bound specs appear in the
FAIL list; all four accel/inference-policy litmus tests ran green inside the
full suite; the failures are 4 budget timeouts (the runner's own adjudicator
says "NOT contended at kill time; genuinely too slow"), a cluster needing a
forge image / mirror / live vault that this host does not have, and unrelated
pre-existing reds. Pass rate moved 373/388 -> 374/388 with my change landing
between the two runs, so no regression hides in the delta. Note that
`capability-routing-shape` SOUNDS like my surface and is not — it is the
fleet capability-matrix selector — which is exactly the kind of name collision
that invites a wrong attribution, so I checked it specifically.

Read path: MCP-first for orientation; filesystem fallbacks were all
VERIFICATION class — read nix-toolbox.sh's own TOOLBOX_NAME, resolve_rung,
capability_rung and toolbox_nix_works before making any claim about the rung,
and read check-scorable-obligation-added and check-issue-citation-convention
before repeating what their refusals meant.
