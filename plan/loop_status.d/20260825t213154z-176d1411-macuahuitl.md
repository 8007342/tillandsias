## Cycle — macuahuitl 2026-08-25T21:31:54Z (linux_mutable, linux-next)

Agent: linux-macuahuitl-fable5-20260825t214543z. Hourly /loop cron, lane=loop-cron. Batch: epic=fail-loud-diagnosis-milestone seed=macuahuitl size=10 budget=10 score=8.171.

GUARDS: preflight ok. credential ok:gh-keyring-push-verified. branch ok:branch-linux-next. mcp ok:experts-healthy + ok:surface-exposed. lock acquired. boundary snapshot on a CLEAN tree; opsx ok:clean-tree. daily-maintenance ok:current. scheduler due:not-installed (advisory). Siblings already ancestors — nothing to merge this cycle.

CLAIMED 2 as one story (guards that report on the wrong artifact): 751-i9mb completed, 823-u3k9 released. Nothing left in_progress.

COMPLETED — 751-i9mb, and the packet's own root cause needed correcting first.
The row says the detector scans plan/index.d only. True, but pointing the existing subcommand at the base would NOT have fixed it: fragment-terminal-events walked doc["packets"] while the base keeps packets at plan_index.steps[], so against the real 3.4MB ledger it printed NOTHING with exit 0 — indistinguishable from "declares no terminal events". Implementing the packet as written would have built a checker that reports clean on a ledger it never examined, which is this guard's own failure class.
- Rust: the packet walk is now shape-independent (stops at any mapping carrying packet_id, the same rule the fold uses). Base went 0 -> 95 packets visible.
- Rust: new opt-in --live applies the withdrawal rule so a falsified closure stops reporting. A FLAG, not a semantic change, so the sibling gate's callers are untouched.
- Shell: check-base-ledger-status-loss.sh as a SEPARATE advisory script. Criterion 4 demands advisory; the sibling is a hard gate on two surfaces. Folding an advisory into a gate makes one exit code carry both meanings, and the first false positive on a historical row gets the gate switched off.

VERIFIED AGAINST REAL HISTORY, not a fixture: reconstructed the 2026-08-15 base from 51603d0a7 with 532's status flipped back to in_progress — the advisory names inference-state-warm-grammar-multimodel AND surfaces two further genuinely-adrift packets at that commit. Live tree: ok:no-base-status-loss:94 checked. Fixture 7/7; litmus:base-ledger-status-loss-shape 6/6, bound to methodology-accountability. MUTATION-PROVEN: reverting the walk makes the base report ok:...:0 checked and reddens both the pin and the fixture.

TWO CATCHES BY MY OWN TOOLING, both worth carrying forward:
- My first draft counted rows with printf '%s' | wc -l. wc -l counts NEWLINES, so a single-row report measured as ZERO — the advisory could never fire on the one-defect case, and it printed ok: while holding the finding in a variable. Fixture case 1 caught it on the first run. Without that case it ships as a guard that always passes, which is the exact class the packet is about.
- build.sh --check refused a convention violation in the new litmus (hardcoded target/ path instead of scripts/plan-binary-probe.sh) before it landed.

RELEASED — 823-u3k9. Its scouting agent died on a provider connection error; I finished 751-i9mb properly rather than half-implement this from an unscouted reading. Recorded the scoping question on the row: check-mcp-surface.sh (801-m9tk) may already satisfy much of it, and the measurable crux is whether check-mcp-expert-health.sh reads the same server registration source the harness does.

SWEEPS: stranded population=1 stranded=0. compaction eligible=false (fragments=21, malformed=0). deslop not-due order=888 delta=54. cache sweep not-due.

E2E: eligible, allow:full-cycle. NOT RUN — the cycle went to 751-i9mb. Recorded rather than skipped silently.

METRICS
expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
flow: cycles=27 avg_completed_per_cycle=1.41 avg_commits_per_cycle=4.74 overhead_ratio=3.37
timing: build_check_ms_avg=20087 litmus_ms_avg=23722
plan: packets=601 ready=374
triage: eligible=164 grouped=141 ungrouped=23 epics=11
