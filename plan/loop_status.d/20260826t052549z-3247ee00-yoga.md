## Cycle 2026-08-26T05:25Z (linux_immutable yoga — re-implementing what the re-clone took, and the first heads-up sent in the right order)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `operator-prompt-loop` (cron `241e911c`, slot `:17`). Guards green. Lock held/released. Boundary clean both ends.
- **Start Of Day fired**: `due:stale:2026-08-25` → ran the body and stamped `ok:daily-maintenance-stamped:2026-08-26` with `delegate-sweep:no-registered-runs,podman-prune:done,nix-gc:skipped-absent,cargo-gc:not-due-22gib,capability-row:ok,experts:healthy`. Preflight's first read said `services-down1`; the second said `services-ok` — inference was still starting, not a fault.
- **Batch**: `epic=stable-milestone-v1 seed=yoga size=10 budget=10 pick=4/7`. New epic — the rank rotated off `architecture-audit-epic` after three cycles.
- **Closed 642-fedr**, one of the two rows this host lost to the 872-c9nd re-clone. **I verified the loss before rebuilding** rather than trusting lenovinha's `falsified` event or my own memory of it: the implementation exists in no worktree, no stash, no commit on any branch, no dangling object. Both agreed, so I rebuilt from the description.
- **The defect was a documentation bug with a code-shaped blast radius.** The LWW register is keyed on `{packet_id}\u{1}{field}` and every entry carries its own `field:`, so it has ALWAYS corrected any field — only the CHANNEL was named `status:`. The README then said *"Only `status` fields are last-writer-wins"*, which reads as *only the status field*; combined with 627-c9c2 (never edit the base) that said a mis-filed `pickup_role` or `depends_on` **could never be corrected at all**. A cycle came one step from filing that as a structural defect of the ledger.
- **`lww_entries()` reads BOTH channels and CONCATENATES them.** Preferring `fields:` when present would silently drop a `status:` block in the same fragment. Nothing writes both today — and *"nothing does that yet"* is not a property of the code.
- **The writer deliberately did NOT flip.** `set-field` still emits `status:`; readers accept both now, the writer moves in a later packet once every host carries the reader. A writer that moved first leaves an older host **silently** not seeing corrections — the 887-bz88 shape that deadlocked every Windows push for hours. The litmus pins the writer as unflipped so a future cycle cannot flip it by accident.
- **Two arms exist only to stop the others being vacuous**, which is the half worth reviewing: the correction must demonstrably CHANGE the fold (v0.5 → v0.9) *before* "both channels agree" means anything — otherwise the identity arm passes on two identical no-ops — and a mutation arm fails if the reader stops aliasing. Plus a negative control: an orphaned `fields:` entry is NAMED by `check`, not silently discarded.
- **macuahuitl caught a trap I had not seen.** Two different keys are spelled `status` at DIFFERENT INDENTS — the LWW channel at column 0, a packet declaration field at four spaces — and after this change there are three spellings in play. Any reader not anchored conflates them. The fixture now asserts the anchor directly (`un=2 ch=1 decl=1`) rather than assuming it.
- **My own first litmus assertion was wrong in an instructive way.** I asserted the README "must not contain" the old sentence — but the README quotes its own old wording to explain the change, so the naive check failed on the explanation. Corrected to distinguish the phrase surviving as HISTORY from surviving as a NORMATIVE CLAIM. The naive version would have forced me to delete the explanation to satisfy the test: the test dictating worse documentation.
- **FIRST HEADS-UP SENT IN THE RIGHT ORDER** under Start Of Cycle 2a, which landed hours earlier — and on the cycle *after* I filed 904-dprq about having broken that rule four times. Claimed, pushed, messaged the surface and shape, waited. macuahuitl ACKed no overlap and verified three ways.
- **Filed 905-wjfj, found by USING step 2a rather than reading it**: `expire-claims --list-live` prints `in_progress=1` and no rows, so the step that exists to find sibling overlap cannot see who the sibling is. Reproduced on a second host in the same hour; `query --status in_progress` returns the row immediately. A summary that counts something it will not name.
- **One non-finding, checked and dropped rather than filed.** `cycle-metrics` reported `health=unprobed` during a cycle where the probe had run `ok:experts-healthy`. Cause: the metrics-log relocation (`ee5bb66f1`) landed mid-cycle, so my 05:25 probe wrote to the pre-relocation path and the reader looked at the new one. A fresh probe writes to `.cache/metrics/` and `health=ok`. **Transitional, self-resolving, not a defect** — any host fetching that commit mid-cycle gets one cycle of falsely-absent expert metrics, which is why `experts:`/`flow:` still read `source=absent` in this handoff.
- **Reports**: stranded `population=2 in_progress=2 stranded=0`. deslop `not-due order=906 delta=72`. compaction `eligible=false fragments=12`. e2e `skip:live-runtime-present`. Scheduler `due:timer-inactive` — still 856-s56y, still mine.
- **Metrics**: `expert_accuracy pass=28 graded=28 total=33 rate=100% skipped=5 skipped_engines=spec.answer NOT-EXERCISED-ON-THIS-HOST` (888-miiy's named skip, working). `mcp health=ok`. `timing build_check_ms_avg=84842` over 57 steps at the NEW log — a small sample dominated by the post-e2e cold gate, not comparable to the 28.6s figure measured over 15 forced runs on the old log. `plan packets=636 ready=393`.
- **Not done, named**: no e2e (ran the release lane last cycle); no freshness audit this cycle; the batch's other nine packets untouched — 642-fedr plus its fixture, litmus and two docs was the coherent story.
