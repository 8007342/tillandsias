## Cycle 2026-09-05T02:15Z-03:35Z — yolanda (windows), advance-work-from-plan

Two packets landed: **1042-svey** (`2b6302209`) and **972-umik commit A**
(`7623213e2`).

### 1042-svey — the cause was more specific than the packet's title

`with-wsl2-builder.sh` installed **`openssl-devel` and not `openssl`**. On Fedora
those are separate: `-devel` carries headers and pulls `openssl-libs`; the
`/usr/bin/openssl` **command** ships on its own. The Rust build needs the
headers, so `-devel` was deliberate — the CLI arrived only as an incidental
dependency.

**I could not reproduce the failure here, and that is the finding:** yolanda's
builder distro *has* `openssl-3.5.7-2.fc44`; esmeraldinha's has neither. Two
Windows hosts, one provisioning script, different contents. The drift was
invisible because nothing declared openssl required. Criterion 1 stays esme's.

Tagged **`linux`**, load-bearing: on Windows the gate re-execs into the distro
and the step reports `ok:host-tools:linux` — a `windows` tag would never fire.
Measured from the gate log. Now `linux:4`.

Prover is `-` deliberately: the only thing that fails without openssl is a Rust
test, not a check script, so there is no honest prover — inventing one would
repeat the open defect where the macos `timeout` prover measures gh login state.

**Criterion 2 measured with both controls.** My first hide silently failed and
the check passed — I nearly recorded that. The second attempt verified
`command -v openssl` empty **before** reading the verdict, and cargo still
visible so a refusal could not be a false red for another tool.

### 972-umik commit A — ratchet 1 of 3 → 0 of 3, measured both ways

**Three behaviour changes, and only one is a defect** (macbookair's taxonomy):
blank was plaintext by *omission* → now refused (policy); **`ON`/`On` was
PLAINTEXT → now SECURE (live defect** — a Windows operator setting `ON` ran an
unencrypted wire believing it secure, every shipped build through v56.9.5.1,
while macOS accepted the same spelling); invalid values now refused by name.

**Two readers in one file** — the wrapper *and* an `assert_eq!(env::var(..))`
inside the `#[ignore]` e2e. A test that parses the variable is still a second
reader: it would keep accepting a spelling production had stopped accepting,
hiding inside the test meant to prove the path works.

The pairing test drives the **real** Noise handshake with the production psk
expression, a frame each way. Sabotage: one flipped byte → `noise: decrypt
error` / `early eof`, exactly as macbookair predicted.

**The ghost-trace ratchet caught me** annotating `@trace spec:secure-control-wire`
— no such spec exists. I invented a plausible slug; removed rather than
inventing a spec to match. A `@trace` to a nonexistent spec is the documentation
form of the day's theme: reads as a link to authority, points at nothing.

**B is now mine** by the ruling's condition — but I read the number rather than
inferring it. First reading was **2 of 3** (macbookair's conversions were on
osx-next; my merge predated `9a78d982b`). After re-merging: 1 without my change,
0 with. B lands next cycle against the **trunk's** ratchet, not this tree's.

**Credential census** (1025-a896): alive at 03:10:42Z after both operator
refreshes, `gh api user` rc=0 — an API validation, not a push inference. Pair
around the push: 03:15:27Z rc=0 / 03:33:15Z rc=0. My row's interesting property
is that an **old** token survived while two **fresh** ones died, the opposite of
oldest-first eviction.

### cycle-metrics (`--cycle-start 2026-09-05T02:15:06Z`), verbatim

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent
timing: steps=10495 build_check_ms_avg=628722 build_check_mix=mixed:forced=141,memoised=37 litmus_ms_avg=13723 slowest=step:checking-that-added-fragments-declared-closures-resolve-885-92iu:490764 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-05T02:15:06Z steps=121 top3=build-preamble=5,build-check=4,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=3 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=9484 steps=128 top3=build-check:runs=121:total_ms=81432089:avg_ms=672992:fail_pct=18,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=99:total_ms=10405652:avg_ms=105107:fail_pct=0,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=99:total_ms=6895392:avg_ms=69650:fail_pct=0 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=51 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=99:avg_ms=105107:fail_pct=0:saved_ms_upper=10300545,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=99:avg_ms=69650:fail_pct=0:saved_ms_upper=6825742,step:writing-the-gate-stamp:runs=99:avg_ms=59870:fail_pct=0:saved_ms_upper=5867261 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=728 ready=463 plan_bin=./target/release/tillandsias-plan.exe
repo: commits_this_cycle=- worktree=clean traces=current
verdict: attention:experts-never-called
```
