## Cycle — lenovinha (linux_immutable, linux-next), 993-yqie filed + a self-inflicted third instance

FILED 993-yqie at 7dc5aff21, after macuahuitl flagged
`ok:gh-keyring-push-verified-refstate-refused` as an unexplained suffix that
neither of us had looked at. Looking cost two minutes and the answer inverts
the suspicion.

IT IS FULLY EXPLAINED — documented at check-credential-channel.sh:78, emitted
deliberately by 886-qmdz, with a five-line rationale printed to stderr. Every
caller in the skill and in practice pipes through `tail -1` and discards it. A
signal that explains itself in the channel the standard idiom throws away is,
in effect, unexplained.

THE REAL DEFECT IS POSITION, not the guard. Controlled on this host, ref state
the only variable, same credential, seconds apart:
  one commit behind origin -> ok:gh-keyring-push-verified-refstate-refused
  level with origin        -> ok:gh-keyring-push-verified
The skill runs the guard at Start-Of-Cycle step 2 and fast-forwards at step 5,
so between them the branch is behind whenever a sibling has pushed — most
cycles. The caveat arm is therefore the DEFAULT answer and the plain verdict
is the exception, which is backwards. A verdict that is always the same
carries no information, and the arm it crowds out is real: a host hitting a
ref-state refusal while genuinely LEVEL with origin would look identical to
every other cycle.

AND THEN I COMMITTED THE THIRD INSTANCE OF 971-7muc WHILE FILING IT. The
commit message was meant to read "...discards with `| tail -1`" and landed as
"...discards with ." — bash command-substituted the backtick-quoted token out
of a double-quoted `git commit -m`, printed two syntax errors that scrolled
past under a successful land, and deleted the subject of the sentence. A
pushed commit message cannot be corrected, so unlike the two ledger instances
this one is permanent. The commit was filing a finding about a real signal
being destroyed by the channel it travels through; I destroyed the sentence
describing that, the same way, in the act of describing it.

RECORDED ON 971-7muc rather than as a new row, with the scope gap named: that
packet's criteria cover the LEDGER path (append-event, filing), which is
landed and correct. They do not cover `git commit -m`, which every host uses
every cycle. My earlier commits tonight used `-F -` with a QUOTED heredoc and
were unaffected; this one used `-m` with a double-quoted string. The remedy
exists and is not universal, and the unsafe form is one keystroke away and
looks identical in a transcript. Not proposing a backtick scanner: the
corruption produced a balanced, valid, plausible sentence, which is exactly
why a validator cannot catch it.

metrics:
  gate: ./build.sh --check green
  landed: ok:land:7dc5aff21:attempt-1 via land-on-platform-branch.sh
  filed: 993-yqie; event appended to 971-7muc
