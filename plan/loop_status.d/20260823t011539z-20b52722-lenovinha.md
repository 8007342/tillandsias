## Cycle 2026-08-23T01:15:39Z (linux-immutable lenovinha — meta-orchestration full; operator-ordered fleet-enabling story)

### THE STORY: 833-fpe7 → 848-bx2q → 850-bif2, all three COMPLETED in order
Operator directive named the sequence and the rule (finish one before the
next); each was claimed with a pushed claim before work and closed with
commit-anchored evidence.

**833-fpe7 (dirty-start resume path).** `scripts/check-resumable-claim-dirt.sh`
on the order-540 model: exit 0 only when every dirty path is tracked, the
folded ledger names a live claim OWNED BY THIS HOST, and every mtime postdates
the oldest such claim. Ownership reads the claim-convention event, never the
newest event's host — last-toucher attribution would have misassigned a row
another host merely noted on (measured live on 831-ezea). The binary grew
`expire-claims --list-live` (read-only; unit test pins owner-vs-toucher and
the propped-up-stale-claim exclusion). `resumable:` licenses REVIEW, never
auto-commit — SKILL.md step 4b says so and litmus:resumable-claim-dirt-shape
step 5 pins the sentence. Hermetic fixture 6/6; the detector's first live
verdict was about the claim dirt of the cycle building it (refused its own
untracked file, answered resumable:833-fpe7 once staged). At 20163b1f1.

**848-bx2q (plan lane vs a joining host's first attestation).** The new-file
branch of the attestation lane now strips LEADING '# ' header lines (the
exact block mo-full-attest.sh record writes) before the unchanged grammar; a
'# ' after content, a short SHA, or stray prose still refuse.
litmus:plan-only-push-lane-shape grew two BEHAVIORAL steps (real git push
through the installed hook): first-record ACCEPT, malformed-new-ledger
REFUSE — 10/10 green. macOS (next joiner — and it joined mid-cycle, 851-gpb5)
will not pay the full-gate toll lenovinha paid. At 7f3fd70a6.

**850-bif2 (capability rows).** Verified on the fleet's only AMD+NVIDIA
silicon, all four exit criteria:
- Enumeration walks /sys/class/drm by PCI vendor id + uevent DRIVER (the
  identity-probe parse; no substring prose, no hardcoded vendor): lenovinha's
  row lists the RTX 3070 (usable, host-native, cdi-spec-missing) AND the AMD
  Cezanne (present-unusable, rocm-runtime-missing, renderD128) — previously
  the AMD card was structurally invisible beside the NVIDIA one.
- ROOT CAUSE of macuahuitl's "derived_tier gpu-cuda / schedulable none":
  the engine probe was host-PATH-only while the fleet's ollama lives in the
  tillandsias-inference image, so engines:[] emptied every triple.
  EngineRecord grew optional lanes (serde-default = old semantics);
  the probe records a container-lane ollama when the inference image is
  present (bounded podman surface); schedulable_triples honors engine lanes.
- capability-matrix now prints `present-unscheduled:` for usable hardware no
  engine covers — macuahuitl's A5000 is named TODAY instead of hidden, and
  its next re-probe ends schedulable:none (carried in the packet's
  next_action for the fleet).
- Publication is asked for and falsifiable: check-capability-row.sh +
  host-capability-probe.sh --fragment, wired into the daily gate and the
  fleet-join section; lenovinha's row is FILED
  (plan/index.d/20260823t015529z-850-bif2-capability-row-lenovinha.yaml) —
  matrix reads 4 rows, 3 of 7 reporting.
- Negative control runs on every gating host: a cpu-only probe still yields a
  valid schema-2 document with a named host.
At 54ed95240 (rebased over the macOS-onboarding push; additive SKILL.md
conflict resolved keeping both paragraphs).

### GUARDS EATING THEIR OWN COOKING
Two of this cycle's obstacles were the previous cycle's own guards working:
the podman-sync bounds guard (extended last cycle) refused this cycle's first
`Command::new("podman")` draft, and the attestation-lane grammar this cycle
fixed was the one that refused this host's own first attestation last cycle.

### METRICS (verbatim)
- experts: calls=684 answered=675 unsupported=2 degraded=7 errors=0 answer_rate=99% source=/tmp/forge-expert-usage.jsonl
- mcp: servers=3 per_server=cli=664;forge-plan=20;project-info=17 legacy_untagged=0 health=ok source=/tmp/forge-expert-usage.jsonl
- expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
- flow: cycles=1 avg_completed_per_cycle=1 avg_commits_per_cycle=5 overhead_ratio=5 (this cycle's record: claimed=3 completed=3 commits=9 — the story amortization the batch rule predicts)
- timing: steps=752 build_check_ms_avg=43169 litmus_ms_avg=21213 slowest=litmus:freshness-inventory-shape:152167
- verdict: attention:experts-degraded-7-calls-could-not-run — read before alarm: the 7 are this cycle's own CLI usage probes (unknown --help flags, a first append-event without --agent, set-field without --evidence, deslop before the binary existed), not server outages; health=ok, accuracy 22/22.
- daily gate: ok:daily-maintenance-current:2026-08-22 (local day; body skipped by design)
- guards: ok:gh-keyring, ok:branch-linux-next, ok:clean-tree, ok:experts-healthy, ok:surface-exposed; deslop not re-consulted (checked last cycle, delta well under threshold)
- e2e: not run this cycle — operator-ordered targeted story; no release newer than tested evidence (unchanged from last cycle's disposition)
