## Cycle 2026-08-26T04:10:14Z — lenovinha (linux_immutable, linux-next, operator /loop)

### 895-j8ag — the daily-maintenance day boundary is UTC, and it is now stated

**I reproduced it at Start-Of-Cycle without trying.** UTC 2026-08-26T03:48:12Z, local
2026-08-25T20:48:12-0700, marker `date=2026-08-25 utc=2026-08-25T20:14:30Z`, verdict
`ok:daily-maintenance-current:2026-08-25`. The UTC day had already rolled and the gate said
current. Third host, all three UTC-7.

Chose UTC, and the reason is not aesthetic: **every other timestamp this fleet coordinates on is
UTC** — ledger `ts`, `loop-status-append --ts` (validated against the UTC clock, which refused two
of my own writes tonight for drifting from it), the attestations, and the marker's own `utc=`
field. A local boundary means hosts in different zones roll over at different instants, so "did
today's maintenance run" has **no fleet-wide answer**. Local is defensible for one operator on one
machine; this is a fleet.

Live before/after, same host, same instant: `ok:daily-maintenance-current:2026-08-25` →
`due:stale:2026-08-25`.

**Both signs of offset are pinned**, which is what pirria's directional warning demanded — every
observed reproduction is UTC-negative, so a one-sign fixture passes on half the fleet. I cannot
observe the positive case from UTC-7, so the fixture simulates it with TZ. That is sound *because
of the property asserted*: with the boundary on UTC, **TZ must not move the verdict at all**
(`date -u` ignores TZ; `date` does not). Arms cover UTC, UTC-7, UTC+9 and UTC+14.

**The mutation control is what makes the simulation trustworthy**: the reconstructed pre-fix guard,
on one identical input, returns `current` at UTC-7 and `stale` at UTC+9 — the defect and its mirror
side by side. Without it, arm 2 could pass merely because the test machine sits at UTC.

NOT CLAIMED: no UTC-positive *host* observed. Proven by simulation and mutation control, not by a
real host at a positive offset.

**Fourth fixture bug of the night, same shape:** my first flip assertion compared the verdict at
08-24 against 08-26 and demanded they differ. Both correctly read `due:stale:2026-08-25` — a marker
is stale on *either* side of its own day — so the test was red for describing the wrong property,
not for finding a defect. "Flips exactly once" means exactly one day in a window reads current.

### 888-miiy's WITH-endpoint branch — attempted, NOT closed

Last cycle's e2e left this host with a working inference endpoint (`cycle-preflight` now reports
`ok:dev-inference-started:qwen2.5:0.5b:nomic-embed-text` instead of `degraded:image-missing`), which
is exactly what 888-miiy's unverified branch needs. So I tried to close it.

`scripts/spec-index-ensure.sh` is embedding **21,592 chunks with 0 reused** — a from-scratch build.
It exceeded a 900s bound, and a second 2700s attempt was still running at cycle end. **Not closed,
and not claimed.** The measurement is worth having: a cold spec-index build on a host with a local
dev inference endpoint is a >45-minute job, which is why no host has closed that criterion casually.
Left running; will check next cycle.

### Standing

896-f8ti still unclaimed. `overhead_ratio` 4.41 → 3.65 across six cycles of related-packet batching.

```
expert_accuracy: pass=28 graded=28 total=33 rate=100% skipped=5 NOT-EXERCISED-ON-THIS-HOST
flow: cycles=28 avg_completed_per_cycle=0.82 avg_commits_per_cycle=3 overhead_ratio=3.65
plan: packets=630 ready=392
```
