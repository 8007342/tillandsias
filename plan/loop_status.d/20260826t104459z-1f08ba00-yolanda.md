## Cycle 2026-08-26T10:44Z (windows/yolanda — falsified 899-6pwv, filed 911-m7js, caught a fabricated ratio before reporting it)

- **Host**: yolanda, `windows-next`. Guards ok, `ok:checkout-lock:free`, daily maintenance current, compaction not eligible (24 fragments), deslop not due, stranded 0.
- **Batch**: selector offered the same macOS p1 + two stale items as last cycle — poor fit again. Stayed in my own story per the consecutive-packets directive.

### 899-6pwv — my own packet's premise falsified, downgraded p2 → p3

It asked for "an executable check ... plus the runbook/skill capture idioms changed
to redirection". Both halves were assumptions about where the `| tee FILE | head`
idiom lives. Measured across the whole tracked tree: **2 hits, both PROSE describing
the defect**, neither a live instance. The second half has nothing to change.

**And the first half would not have caught me.** The command that truncated my
evidence was typed ad hoc at a terminal, never written to a file, never committed.
A repo-scanning guard cannot see it. That is the author-confound face again — I
proposed a detector that could not observe the defect that motivated it, while the
cheatsheet documenting that exact failure was already on the trunk citing me.

Checked the one historical hit rather than assuming: `land-on-platform-branch.sh:16`
documents `| tee LOG | tail -3` taking its exit status from `tail` — a VERDICT bug,
already covered by 792-ksr8's guard, not artifact truncation.

**Declined to build it anyway.** A guard over committed files would pass forever,
prevent an idiom nobody writes, and read as coverage for a defect it cannot see —
a check whose result is identical whether or not the thing works. Worse than
nothing, because a future reader would take it as the mitigation.

### 911-m7js filed — the archiver check is 30% of every gate

From last cycle's profile: `Checking the plan archiver preserves the ready set` is
**105.1s of a 350s gate**, 2.5x the next largest step, and it runs on every commit
on every host whether or not the change touched `plan/`.

Checked before filing that it is NOT the `cargo run` root cause a neighbouring row
describes — the archiver resolves the prebuilt binary through the shared probe.
Same symptom, different cause; the packet says so, so nobody folds them.

### A fabricated ratio, caught before it left the host

The obvious hypothesis is that the 105s is bridge cost. Testing it, the same script
standalone from Git Bash returned in **12.2s** — which against 105s is an 8.6x
bridge penalty and would have been a compelling headline.

**It bailed at line 33** (`toolbox: command not found`), produced two lines of
output, and did none of the work. 12.2s measured a failed startup. The standalone
WSL run (84.9s) also failed, before its sweep. So I have **no valid cross-userland
comparison**, and the packet says the attribution is UNMEASURED rather than
implying the bridge.

That is the third time in two days a number of mine would have been wrong in my own
favour, and the thing that caught it was checking whether the run did any work
rather than trusting its exit — the same move that caught the truncated e2e log and
the compile-contaminated bridge trial.
