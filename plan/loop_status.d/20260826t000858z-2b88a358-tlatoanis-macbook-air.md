## Cycle 2026-08-26T00:08Z (macos tlatoanis-macbook-air — back to draining; closed 635-bhkb, the macOS half of a two-lane packet)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 18, cadence slot **:47**. Boundary snapshotted FIRST (the 888-3d6x lesson, and the third cycle running where it has held). Daily maintenance `ok:daily-maintenance-current`. All guards green.
- **Deliberately drained instead of coordinating.** The previous five cycles were coordination and one was product code, which is the wrong ratio for a host whose fleet goal is drain > file. I said so to the coordinator and then did it.
- **Batch**: `epic=stable-milestone-v1 seed=tlatoanis-macbook-air size=10 budget=10 pick=3/7`. Claimed **635-bhkb** — chosen because it is macOS-lane AND because I had **live evidence for it from tonight's release prep**: the installed `/Applications/Tillandsias.app` reported `tillandsias-tray 0.1.0 (git 0548ee1f2)` while its Info.plist read 0.4. The defect was still live sixteen days after filing, and I found it while establishing whether this host could validate a release — not by reading the ledger.
- **CLOSED, both lanes now complete.** Every macOS version surface read `CARGO_PKG_VERSION`, which is never bumped per release. Added `WORKSPACE_VERSION` to the crate's build.rs and replaced the five uses: `--version`, `--help`, the diagnose JSON `version` field support tooling reads, its test fixture, and the control-wire skew check.
- **The windows host's 2026-08-13 scope correction is why this was cheap, and it deserves the credit.** They could not compile either tray that night (Smart App Control blocking the toolchain), so instead of attempting a change nothing could verify they made the packet ACCURATE: Windows half already done, macOS half open at five named sites, with a recommended shape — mirror the windows build.rs, do not invent a second mechanism, and name the pin identically. I followed it exactly. **An audit that corrects a packet's scope is worth more than a fix nobody can verify**, and this is the worked example.
- **THE SKEW SITE IS WHY THIS WAS NOT COSMETIC**, which that same note caught and the original filing had missed. `pty_vsock_bridge` compared the guest's reported `build_version` against the literal `"0.1.0"`, so the mismatch warning fired on **every connection to a healthy guest**. A warning that is always true is one an operator learns to scroll past — and build-version skew is exactly the signal 648-772y needed legible when an older tray downgraded a newer guest's wiring and wedged the control wire. The always-on warning was actively destroying the signal it exists to provide.
- **Verified end-to-end on the real binary, not only in tests**: `--version` → `tillandsias-tray 0.4.260817.1 (git 8f7852f1a-dirty, …)` and `--diagnose --json` → `"version": "0.4.260817.1"`. Both said `0.1.0` before.
- **Two pins, hand-falsified.** `version_line_uses_workspace_version_and_commit` carries the same name and assertions as the Windows tray's, so the cross-tray identically-named-pin pattern now extends to versioning; reintroducing `CARGO_PKG_VERSION` turns it red, restoring returns it green. The second, `workspace_version_is_not_the_frozen_crate_version`, asserts the **value** rather than the call site — so a future hardcoded version fails however it is spelled, and it also catches the build.rs fallback firing silently on an unreadable VERSION file, which would otherwise degrade to `0.1.0` with every other test still green.
- **Unblocks downstream**: 624-q4jj step 5 prescribed `--version >= 0.4.260809.2` and could not bind while every build answered 0.1.0. It can bind now, and version proof no longer needs the CFBundleVersion + git SHA workaround.
- **Release still HELD** (not on macOS): `litmus:expert-groundtruth-harness` needs an embedding endpoint it never declares as a precondition, and 887-bz88's gate-stamp change deadlocks the Windows push lane. E2E prep intact — stale `~/Applications` bundle moved aside reversibly, destruction-assert landed. Standing by for a tag.
