## Cycle 2026-09-05T14:45Z→15:05Z (macos/tlatoanis-macbook-air — 1034-whsp second-host slice)

### WORK DRAINED
- **1034-whsp** (`a-claim-is-invisible-...`): criterion 2's pre-fix measurement taken
  from the second side, remedy landed and gated, lease RELEASED back to ready with the
  residual named. Evidence 78df54a34, landed 4002317d8.
- **1067-24q6** filed: `expire-claims` defaults to write mode.

### THE MEASUREMENT, which reframes the packet a second time
yoga reframed it once (retry loop 18-56ms; the pre-push hook is the cost). The push
is not the problem at all:

    claim push, claim-only diff, platform branch ..... 12.0s
    osx-next -> linux-next relay gaps, last 9 ........ 19m to 2h02m
    since the last relay when measured ............... 6h28m
    this host's own claims still unseen on linux-next  1h55m and 2h11m

Folded with `--index`: origin/linux-next says `ready` while this checkout holds
`in_progress`; 8 of this host's fragments were absent there. A claim reaches a trunk
host only when the coordinator RELAYS the platform branch. That kills remedies (a),
(b) and (d) as filed — all three optimise a push that already finishes in seconds.

`git ls-remote origin 'refs/tillandsias/*'` returns ZERO refs, so the out-of-band
store the packet hoped the mirror carried does not exist. Remedy (c) in its smallest
form reads what is already published: scripts/check-claims-across-branches.sh, one
fetch, 1.02s, exits 1 with `claimed-elsewhere:<packet>:<branch>`, exits 2 when it
cannot look. Fixture 4/4, wired into build.sh.

### WHAT IS NOT DONE, and why the lease was released rather than the packet closed
Criterion 2 names the SELECTOR. This refuses at the CLAIM step. The effect is
achieved; the mechanism named is not wired, and select-work-batch.sh is not in
owned_files. Next action recorded on the packet: fold the three siblings ONCE per
selector run, not once per candidate.

### THREE CORRECTIONS AGAINST MYSELF
- "the mandated merge takes the push off the plan-only lane" — WRONG, the lane is
  merge-aware (1056-5344, 1060-7mmm) and accepted it.
- `--root` is not a flag; my first cross-branch reading came from a fallback with
  stderr suppressed and a hidden error I read as a filter. Redone with `--index`.
- my own fixture arm asserted against a condition it never built (`env -i` did not
  remove the binary, because the script cd's to a ROOT that HAS target/release).
  The script was right and the arm was wrong.

### DISCLOSED SIDE EFFECT
`tillandsias-plan expire-claims` defaults to write mode and released macneo's
24h-stale claim on 980-ja2m during a read-intended probe. Fragment deleted unpushed,
980-ja2m untouched. Filed as 1067-24q6: the TTL rule may be right, but the release
must not be a side effect of someone else's read.
