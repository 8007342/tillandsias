## Cycle 2026-08-26T13:53Z (linux_immutable/calmecacpilli — 863-iicc criterion 4 measured: worktrees are free for source, 7.7 GB for build)

- **Host**: calmecacpilli, `linux_immutable` (Silverblue), `linux-next`. Boundary snapshotted first. Guards all green: `ok:gh-keyring-push-verified` (clean this time — no refstate note), `ok:branch-linux-next`, `ok:experts-healthy`, `ok:clean-tree`; preflight `ok:cycle-preflight:rebuilt+expert-absent+services-ok`.
- **Batch**: `epic=UNGROUPED size=1 seed=silverblue-i7-intel-calmecacpilli-20260826`; `triage: eligible=1 ungrouped=1`. Third consecutive cycle on the same single-packet frontier, which is the related-packets directive rather than a stall.
- **Stranded sweep**: `population=1 in_progress=1 stranded=0`. Nothing to act on.
- **CRITERION 4 ANSWERED — the multi-worktree question.** Measured here: `git worktree add` takes **0.34 s** and **52 MB** of tracked source; `.git` stays at 136 MB (object store shared); the new worktree has **no `target/`**, so it needs a cold build; and this host's `target/` is **7.7 GB** (release 920 MB, debug 4.8 GB). So the source cost is a rounding error and the build cost is the entire answer.
- **AND THE OBVIOUS MITIGATION DEFEATS ITS OWN PURPOSE.** Pointing every lane at one `CARGO_TARGET_DIR` is the natural fix, but `target/release/.cargo-lock` exists — cargo takes a per-target-dir lock, so two lanes building against a shared target dir **block on each other**. The worktrees exist to let a long gate in one tree survive a merge into another; sharing the target dir re-serialises exactly that. The choice is 7.7 GB per lane, or concurrency you do not actually get.
- **Why it matters for this packet's own mechanism**: the lane/stamp derivation says the LANE needs a fresh fetch and the STAMP needs an unchanged tree, mutually exclusive. A second worktree is the one structural escape from that exclusivity — gate in tree A while tree B fetches — which is why criterion 4 exists at all. The escape is real and costs ~7.7 GB plus a cold build per lane.
- **Corrected the criterion's own premise**: it asks what this costs "on a 256 GB budget SSD". This host is 111 GB total, 75 GB free — not half that. ~9 lanes of headroom before disk binds, less once the podman store and toolbox lane are counted. Nobody ruling on this should assume 256 GB.
- **Explicitly NOT measured, flagged so nobody infers it**: cold-build wall time for a full workspace in a fresh worktree. The only datapoint is this host building the headless binary alone at ~9 min, which is a SUBSET and therefore a floor, not an estimate. That is the real per-lane setup cost and should be measured on a host that is not the fleet's core-count floor.
- **Worktree created and removed cleanly** — `git worktree list` back to one entry, working tree clean, boundary preserved.
- **Still blocked and unowned**: `due:no-capability-row:calmecacpilli`. Unchanged; not re-tested this cycle (tested last cycle, no fix has landed since).
