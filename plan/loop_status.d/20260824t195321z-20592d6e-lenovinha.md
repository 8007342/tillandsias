## Cycle — lenovinha (linux_immutable) 2026-08-24T19:53:21Z — p0: the refusal path now preserves the work it refuses to touch

Full-mode cycle. All guards green. A p0 appeared at the batch head (872-c9nd, urgent=) and took precedence over everything else.

THE INCIDENT. A host wedged 2026-08-23 with 16 modified paths plus one untracked litmus file belonging to two claimed packets. Three consecutive cycles refused the dirty tree, verified all 17 paths byte-identical to their boundary snapshots, and wrote increasingly detailed prose about the diff. At 2026-08-24T06:09Z the checkout was REPLACED BY A FRESH CLONE. Four hours of finished work are unrecoverable. THE BOUNDARY GUARD DID ITS JOB PERFECTLY AND PROTECTED A DIRECTORY THAT SOMEONE THEN DELETED WHOLESALE — a guard that forbids the AGENT from touching the work does not forbid anything else, and prose describing a diff is not a copy of it.

CRITERIA 1-3 DONE. (1) 642-fedr and 776-cm74 are back to ready with `falsified` events saying the implementation is GONE, not merely unlanded. For 776-cm74 I VERIFIED rather than relayed: both named files exist on trunk and are tracked, which reads like survival — but they predate the packet and the property it delivers (git-normalized hashing) is absent, since the script hashes via find + sha256sum. The deliverable really is missing. Recorded the check as well as the conclusion, because "the file is there" is exactly what would make the next host close the row on a glance.

(2) THE REFUSAL PATH NOW PRESERVES WORK. scripts/salvage-dirty-worktree.sh pushes a COPY of the dirty tree — tracked mods, deletions AND untracked files — to salvage/<host>/<yyyymmdd>-<slug> on origin, required by the skill BEFORE any dirty-start refusal at both refusal sites. The design problem is that it runs on dirt the cycle has just been forbidden to alter, so it uses a temporary GIT_INDEX_FILE and plumbing only (add -A into the temp index, write-tree, commit-tree) — no stash, no add against the real index, no checkout. VERIFIED on this cycle's own dirty tree: ok:salvaged with ref+sha, git status and .git/index byte-identical before and after, and `git show <sha>:<path>` returning the full content of an UNTRACKED file. That last check is the one that matters — an untracked litmus file is precisely what the incident could not recover.

THE MECHANISM ALREADY EXISTED AND NOTHING COULD USE IT, which is the part worth reading twice. The salvage/<host>/<date>-<slug> ref grammar has been accepted by the pre-receive gate — with its YAML check deliberately exempted so a half-edited tree can be pushed — since before the incident. But the LOCAL pre-push hook still demanded a green build stamp, and a salvage ref is by definition a tree no gate has ever seen, so the push could never pass. A capability built for exactly this case sat unusable and unused while four hours of work went into the bin beside it. The hook now exempts a push whose refs are ALL salvage refs (all, so a salvage cannot smuggle a normal branch past the gate).

(3) THE STALE-LICENCE CASE IS IN THE SKILL. An operator licence to land dirt — 833-fpe7's resumable:, 540's opsx merge, or a prompt naming work to review and land — must be checked against the tree first. If git status is empty the answer is "the premise is gone": say what the licence expected to find and stop, rather than reconstructing what you think it meant and landing that. That is how the defect was found — a verbatim unblock prompt replayed hours after the re-clone.

(4) LEFT TO THE OPERATOR, deliberately. Path (b) is a judgement about whether an untracked path referenced by tracked claim dirt may be attributed to the same claim and landed. The losing side of that call is another data-loss event, and it is no longer urgent now that a wedge produces a copy on origin rather than a race against a clone. Packet set to implemented with that in next_action.

ALSO RECORDED, an honest cost of my own last two cycles: the cross-target gate's Windows artifacts are 791 MB in target/ (16G -> 18G). Well under the 40 GiB cargo-clean trigger so nothing is due, but it is a recurring per-host disk cost I had not stated when I argued the change was cheap.

NO FRESHNESS AUDIT this cycle — the p0 took it, and I would rather say so than stamp a file I did not really examine.

ADVISORY: compaction not eligible. Deslop not due.
