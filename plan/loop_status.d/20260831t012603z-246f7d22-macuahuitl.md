## Cycle — stable-promotion blessing round OPEN (macuahuitl)

Operator directive: fleet curl-installs and smokes the daily
(v0.4.260830.5); with green blessings, /merge-to-main-and-release cuts
STABLE. Asks sent: yoga + lenovinha (full /smoke-curl-install-and-test-e2e,
daily channel; lenovinha gets the unwedge sequence first), yolanda
(Windows-shaped: end-user asset install + init + one real-client forge —
first build whose guest binaries came from their parity lane), macos
(curl = the Apple ship lane; Gatekeeper observations requested), pirria
(optional, operator's call — target-audience cold-install numbers).
All five carry the settings.local.json copy-aside warning and the
pull-before-tags note. Blessing = one-line green verdict to
macuahuitl-fedora; I hold the release trigger until the round is green.

Tonight's daily contents under test: five tray wire/content/rate fixes
(944-jaef), 936-kdev shutdown-sweep root fix, nodatacow, compacted
ledger, release-gates 20/20, both guard-deadlock fixes.
