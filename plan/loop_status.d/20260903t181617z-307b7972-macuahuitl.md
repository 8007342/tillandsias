## Cycle — macuahuitl 2026-09-03T18:08Z (meta-orchestration, full mode)

- **Start of cycle:** linux_mutable, clean, level, daily gate current, boundary
  snapshotted and verified. Credential guard gave the PLAIN verdict — level and
  gate stamp current, the rare arm.
- **Drained 980-krib criterion 3 — my own defect, the one I keep re-committing.**
  `scripts/dispatch-brief.sh` emits the local and upstream head with a timestamp,
  warns when they differ, and marks each packet UNTOUCHED (brief from the packet
  as filed) or WORKED (N events, DO NOT brief from the `context:` block) with its
  last three events. The criterion said a brief must carry the head it was
  written against; a discipline the coordinator must remember is one the
  coordinator forgets, so it is a command whose output is pasted into the
  dispatch — the same move as the landing tool and salvage.
- **THE GATE CAUGHT MY FIRST VERSION doing exactly what it forbids:**
  `violation:plan-binary-probe-usage` — I resolved the plan binary from a
  hardcoded `target/` path instead of the shared probe. That is the SECOND
  instance today and the guard found both; lenovinha hit the identical violation
  hours ago in a fixture, for the identical reason. Fixed to `resolve_plan_binary`,
  which also gives it a named remedy on a host with no runnable binary.
- **The staleness warning then fired on me within a minute of being written**,
  on my own unpushed commit. That is the only kind of verification I trust today.
- **990-u3xy: does NOT reproduce on linux, and I declined to fix it blind.**
  `check-cheatsheet-tiers.sh` returns rc=0 here with cargo resolving. macneo
  reports every OTHER cargo phase of the same gate succeeding on macOS, so the
  script runs in a DIFFERENT ENVIRONMENT rather than the tool being absent — and
  that difference IS the packet. Editing a script I cannot make fail would be
  the authoring-host trap this fleet has recorded six times today. Recorded what
  the next taker needs: the PATH this phase sees versus the PATH the succeeding
  phases see.
- **Landed esme's 22-hour forge report for them, since they still cannot push.**
  Two measurements went onto packets as evidence. 981-n5vx: `memory.max` is
  `max`, peak 4.89 GB of 7.9, and every `memory.events` counter at ZERO — not one
  reclaim in 22 hours, let alone a kill. 959-fpc5: pids peak 72 of 4096, zero
  denials. **Neither refutes its packet and I said so on both** — esme's own
  framing is the right one, that unbounded here cost nothing but is unbounded by
  ABSENCE rather than by DECISION. What they do is bound the urgency and add a
  third regime: an agent session rather than a build.
- **esme also corrected two operator assumptions**, which belong in the queue
  reply rather than a packet: local experts are NOT disabled for unreliability
  (l0 and l2 ready, only l1 unset because the embed endpoint is unset and no
  index is built), and the inference constraint is THROUGHPUT rather than model
  size (1331 ms/chunk, single short query embeddings at 16.2s and >60s, on a
  contended shared endpoint). Also: nix is ABSENT from that forge — not broken,
  not provisioned in that lane.
- **Their headline is the fleet's shape tonight:** the substrate was essentially
  flawless — 22h, zero restarts, zero crashes, no ceiling bit, network correct —
  and every real failure was in the tooling layer above it, in the path between
  "I measured something" and "the fleet has it".
- **Litmus:** not run. This cycle added one script and ledger fragments;
  `--check` passed, including the probe-usage guard that rejected my first draft.
- **Operator queue, five plain asks, unchanged:** the esmeraldinha PAT; macneo's
  forge-build permission; whether macneo-macos is still up; yolanda's two.
- **Nothing is blocked.**
