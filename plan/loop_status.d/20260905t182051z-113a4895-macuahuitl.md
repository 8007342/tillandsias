## Cycle 2026-09-05T18:41Z coordinate — macuahuitl (cron lane; ran 18:05Z)

- LastExecutionTime: 2026-09-05T18:20:51Z. Merged windows-next (+4), osx-next (+1) and lenovinha's work/1069-c9w6 (+6); no conflicts this time. Union litmus on the merged union: PASS 292/292 executed.
- THE FLEET'S SPEC-TRACE COVERAGE FIGURE WAS WRONG AND IS NOW VERIFIED HERE, not read from the report: `validate-traces.sh --coverage-threshold 100` emits coverage_percentage 100, 177/177, uncovered 0, where the same tree reported 98% and three uncovered before lenovinha's fix. The three "uncovered" were the three BEST-traced specs (83, 60 and 56 matching files): a wider match keeps the producer traversing past `grep -q`'s exit, it takes SIGPIPE, and pipefail records 141 as uncovered, so the metric penalised what it should have rewarded and its answer depended on disk speed and cache warmth. Blast radius checked: no README release row cites the figure, so no published claim is wrong; consumers are plan.yaml, the enforce-trace-presence spec, the cheatsheet and two archived issues. Found only while adversarially re-checking one of 1063-nraf's twenty-six orphans, and 792-ksr8 could not see it because that guard is diff-scoped to newly added pipelines.
- SILENT 127s reached FIVE across three hosts today and the count is now the argument (recorded on 1004-x9ua). The fifth reframes the others: `--exec-guest` runs with HOME UNSET while USER, whoami and id all say root, so $HOME/.local/bin expands to /.local/bin, a real absolute path that cannot exist, at the head of PATH — nothing errors, nothing warns, and a tool that would resolve under a correct HOME is not found (macbookair, p1 as 1072-qk43). A caller-side `export HOME=/root` does NOT repair it: the login shell computes PATH before the export runs, so there is no workaround and the fix must be in exec-guest. macbookair then measured that no earlier reading changes on that guest, since none of the candidate paths exist and git and jq are genuinely absent.
- 317's BOUNDARY MOVED TWICE AND STOPS AT CREDENTIALS: not a destructive re-provision (it needed a cold cache), not an image (the image builds there in 121 s once HOME is set), but a forge launch with proxy and vault, because a bare `podman run` fails TLS verification before any installer reports anything and therefore measures the absence of the sidecars. macbookair refused to run the login, correctly, since logging in where a token still works evicts a peer's — which is what macneo is blocked by. Cold was MEASURED before anything ran: zero forge containers, zero harness cache dirs, zero npm caches, so nothing had to be cleared and no last-good rollback state was at risk.
- SIZINGS THAT SHRANK THEIR OWN PACKETS, both by measurement: yolanda's jq exposure from "26 scripts, every one exposed" to 63 capture sites in 19 scripts, ten multi-line, three collapsing by construction, SEVEN real and only THREE on this gate's path — because command substitution strips the trailing CR, so ONLY THE FINAL LINE of jq output loses it and exposure is a property of multi-line capture; with the caveat that the safety rests on MSYS bash 5.3 and a different Windows bash re-exposes all 63. And esmeraldinha narrowed my framing of their own evidence: they hit ONE build.sh conflict, not four, resolved by hand in two minutes, and the cost is that resolution is a CODE EDIT forcing a ~25-minute re-gate on a branch outside the plan-only lane, not that resolution is hard.
- LATENT TRAP worth its own line (yolanda): test-hardware-fingerprint.sh:37-38 compares two GPU-name captures and is correct today BECAUSE both fixtures hold exactly one row, not because anyone decided it; a second row breaks it on Windows only and silently. The per-site control for that whole sweep is od -c against a TWO-row fixture, since a one-row fixture cannot tell a fixed site from an unfixed one.
- Claims (read-only, --dry-run): live 1026-ps4n (pirria), 1063-nraf (lenovinha); unclaimed-in-progress 317 and 1025-a896. 317 reads unclaimed while macbookair works it — the fourth instance of 1065-4t7t, now assigned to yoga.
- Assignments this pass: yoga 1065-4t7t (the sweep's phrase needle), esmeraldinha 1072-b7eq (gate-step collisions, claimed), yolanda the seven jq sites with the two-row control, lenovinha 1063-nraf continuing, pirria 1026-ps4n by relay, macbookair 317 blocked on the operator, macneo idle with its packet closed.
- Operator queue: unchanged plus one new item — authorise a forge launch WITH proxy and vault on macbookair for 317's install half, or accept that it waits for the grant reset; that is separate from the reset itself.
- Scheduler posture: coordinate hourly at :41, meta every 4 h at :09 (next 20:09Z); both armed, session-scoped.
