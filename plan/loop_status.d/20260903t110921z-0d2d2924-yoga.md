## Cycle — yoga 2026-09-03T11:09Z (meta-orchestration, full mode; cron lane)
- Start of cycle: guards ok, daily gate current, lock acquired, boundary clean at 41eb3c144. Claim landed with scripts/land-on-platform-branch.sh (attempt 1).
- RUNG 3 (977-j6qu) IMPLEMENTED: there is now ONE ranking function. local-ci.sh calls `tillandsias-plan score-checks`, which runs obligation::centicolon_function over a SpecState; the shell no longer computes a score of its own.
- A CORRECTION TO THE PACKET'S OWN FRAMING, only visible by reading the code rather than the filing: it says local-ci.sh computes "a weighted checklist" against the methodology's "ranking function with a separately reported denominator and residual". But local-ci ALREADY reported earned/total/residual separately — that half it got right and the packet undersold it. What was genuinely missing was the LATTICE: each check was binary, so there was nowhere for an obligation state to live and nowhere for the monotone-regime qualifier to be expressed.
- THE NUMBERS ARE UNCHANGED TODAY, DELIBERATELY, and that is the faithfulness evidence rather than a shortcut: a passing check establishes PositivelyTested, each weight is earned at exactly that bar, so the model reproduces what the loop computed. Verified end to end — `local-ci.sh --phase runtime` wrote expected_total_cc=140 actual_earned_cc=140 residual_cc=0 percent_closed=100 through the model path.
- AND THE HONEST PART: a green check witnesses that a test passed and says NOTHING about RuntimeObserved or EvidenceBundled. Scoring it as though it did would be the model agreeing with whatever the shell already believed. Weights carries an `earned_at` bar per obligation instead of assuming the top of the chain, so a richer evidence source can be added later without silently redefining percent_closed.
- THE REGIME IS REPORTED, NOT IMPLIED. A bare integer cannot say whether it is inside the band where centicolon_function is monotone, so Score carries a Regime; a tombstone removes an obligation from BOTH numerator and denominator, which is a denominator scope change, so it returns Broken with the reason named and local-ci prints note:local-ci:centicolon-regime:… when the score is not comparable with the previous run.
- FAIL LOUD, NEVER FALL BACK — VERIFIED BY MUTATION rather than asserted. Shadowed the binary with a stub whose score-checks exits 2; local-ci printed `blocked:local-ci:scorer-refused (rc=2)` and stopped. A silent fallback to the local arithmetic would have restored two scorers with the second invisible and reachable only on the unhappy path. A malformed input line likewise REFUSES rather than being skipped, because skipping shrinks the denominator and RAISES the percentage — the direction nobody audits.
- FOUR DEFECTS I CAUSED THIS CYCLE, three caught by the gate and one only by running the thing. Gate: the capability manifest must be BYTE-sorted (my insert was locale-sorted — invisible to `sort` under a non-C locale); every declared capability must appear in USAGE; every dispatch arm must too. NOT caught by any gate: I first placed the dispatch arm inside the `if args[0].starts_with("spec-")` block, so score-checks was declared, compiled, documented — and UNREACHABLE. The binary's own error said "unknown subcommand", which was true and pointed nowhere near the cause. Only executing it found that, which is the sibling warning about green builds measuring what was built, arriving in a form I could not have predicted.
- A CARGO FINGERPRINT TRAP worth recording: my edit and a build landed in the same second, so cargo reported "Finished in 0.11s" and did not rebuild; the binary kept answering "unknown subcommand" while the source plainly contained the arm. `touch` on the sources forced it. Same-second mtime is enough to make cargo believe a changed file is fresh.
- SCOPE NOT CLAIMED: obligation states here come from CI check results only. Nothing yet writes RuntimeObserved or EvidenceBundled, so those rungs of the chain are unexercised in production — rung 4/5 territory, not a gap in this wiring.
