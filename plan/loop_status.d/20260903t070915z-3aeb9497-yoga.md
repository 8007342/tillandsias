## Cycle — yoga 2026-09-03T07:09Z (meta-orchestration, full mode; cron lane)
- Start of cycle: guards ok, daily gate current, lock acquired, boundary clean at bc9e8f71c. Two sibling claims live, neither on my surface.
- TOMBSTONE DEFECT IN RUNG 1 FIXED (977-56fd), found by macuahuitl connecting 976-suab to work I had landed an hour earlier. SpecState keys off stable ids and get() returns Absent for a missing key — deliberately, so states over different id sets align through the chain's bottom. Under the operator's tombstone rule that is WRONG: an id whose requirement changed MEANING is tombstoned and replaced, disappears from the map, and reads Absent. The score silently drops, and — worse — rung 2 would have reported a MONOTONICITY VIOLATION against math.fixpoint.convergence-target@v1 that was really my map. A false finding about the methodology's own claim, manufactured by key handling.
- THE DECISION WAS DECIDED BY THE RULING, NOT BY ME. macuahuitl was right that carry-to-successor and refuse are not equivalent and that choosing between them is a decision. methodology/proximity.yaml:47 settles it: a REFINEMENT keeps its identifier (state travels with the key, never at risk); a TOMBSTONE means the obligation is a DIFFERENT obligation, so evidence for the old meaning is not evidence for the new one and there is NO CARRY. I did not have to refuse-and-surface — the sentence picks refuse. Absence is now three cases: never-recorded (Absent, in domain), tombstoned (OUT of domain — ceased to exist is not the same as no evidence), successor (new id, in domain, Absent, no carry).
- THE CLOSURE RATCHET REFUSED MY FIRST RECLAIM and that is worth recording: `implemented` does not return to `in_progress` without --reopen-evidence naming a falsifying observation (650-dq6u). The guard worked on the author of the row it guards. Reopened with the proximity.yaml citation as the falsifier, which is the sanctioned path and left a durable reason in the ledger.
- 14 tests (from 10), gate green. New arms: tombstoned_id_is_not_a_regression (the reopening case — before/after an identity change compares Equal, not Less); successor_starts_absent_with_no_carry (the decided half); refinement_keeps_the_identifier (NEGATIVE CONTROL — a "fix" that tombstoned on every edit breaks it); never_recorded_is_still_bottom_in_domain (the exclusion must not swallow the ordinary bottom case, or differing id sets would stop comparing at all).
- THE SPLIT PARENT NOW REFUSES A CLAIM rather than describing itself as unclaimable. 976-kk6x read `ready` while carrying "do not claim this row" in next_action, and split-parents returned nothing for it — the tooling actively contradicted the prose, and the coordinator re-dispatched the row as unclaimed work on the strength of the status field. Fixed both halves: status -> blocked (the sanctioned resolution for a fully-delegated parent, and the one that actually removes it from selection), and split_into landed as a FIELD naming all five children so the relationship is queryable rather than narrated.
- ENFORCEMENT VERIFIED ON ALL THREE SURFACES rather than asserted, per "if a host can still claim it, the fix is not done": `ready` 0 matches, `select-work-batch.sh linux` 0 matches, `next linux` 0 matches. split-parents now correctly reports nothing for it, for the right reason — that tool names READY parents competing with their children, and this one no longer is.
- Two doc-lint errors from my own splices (an orphaned doc comment, overindented list continuations) caught by the gate rather than by me. Third time this week the toolchain has caught a defect in prose I wrote about clarity.
- Carried into rung 2's brief: it must be written against the THREE-case model — a generator that produces tombstones must not treat them as Absent, or monotonicity reports artifacts of key handling rather than facts about the methodology.
