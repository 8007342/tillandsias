## Cycle 2026-09-14T19:29Z–20:00Z — pirria, curl-install smoke of v56.9.13.1 (daily): PASS with three findings

Host restarted and upgraded this window; reported in to the macuahuitl coordinator and took three assignments.

Measurement (no claim), 1187-iij8: the two named litmus arms at this floor locus. capability-manifest-guard PASS 182.6s
against its 300s budget, rc=0, not killed; build-cache-sweep-trigger PASS 1.7s against 30s, rc=0, not killed. Both spec
suites green — forge-environment-discoverability 24/0/1 (rc=0, 871.4s), meta-orchestration 23/0/1 (rc=0, 148.1s). The
finding is the separation: this host is AT-RISK on the 1109-t8kw git-identity probe and TILLANDSIAS_HOST_TIER is unset,
so it is the floor by the row's own criterion, yet esme's kill at 300.6s and this host's 182.6s are the same tier and the
same arm on opposite sides of the budget. Tier does not predict speed and the key is absent here anyway, which is evidence
against 1187-iij8's tier-scaled-budget arm and for its BUDGET-tally arm; esme's timeout reads as a MINGW-ism, not a
floor-ism. Appended as a note event at 4b5593c4e. Every count on this host carries warn:litmus-degraded-no-yq — yq is
absent and unprovisionable here (toolbox route is Silverblue-only; no sudo password this session) — but neither named arm
shells out to yq, so the two verdicts stand and the other 45 arms are owed a re-run on a yq host.

Smoke, v56.9.13.1 daily channel, operator-authorized per-run under 1004-vsh2 (pirria is a workstation, not a dedicated
smoke host): install_exit=0 and the bounded version assertion passes; reset_exit=0 with store empty; init_exit=0 from a
genuinely cold room; §3b shutdown 0 unclean; §4b proxy alive alongside the lane; opencode_exit=0; health check green
(sealed=false, proxy up), taken last. Timing records emitted for all five smoke steps, exit=0, host=pirria:
curl-install 262.6s, reset 20.8s, init-pristine 439.9s, forge-lane 822.9s, health-check 0.2s. Lane run detached per §4a
on the 15 GiB/4-core host that section names as the measured boundary; the supervisor survived and timing_reap found no
orphaned stamp from the previous run.

900-z3kv is EXERCISED and the result is split: the end state works, the mechanism is incomplete.
clear-vault-host-credentials.sh cleared both fallback files and could NOT remove ~/.cache/tillandsias/vault-data — it is
subuid-owned (100100) and a rootless rm is refused — so it printed warn:clear-vault-credentials:partial and exited 0 with
containers, volumes and images all reading empty beside it. `podman unshare rm -rf` removed it cleanly and flipped the
probe from credential-warm to credential-cold; only then did --init log "Shamir share not found; deriving first-boot dummy
key K" and the fresh-init handover, which is the first time the resync path has genuinely run on this host. Without that
hand fix the room is not cold and every exit code in the run still reads clean. Filed.

Three findings filed, all status ready:
smoke-finding/clear-vault-credentials-cannot-remove-subuid-owned-vault-data (the above; verified remedy included),
smoke-finding/smoke-evidence-dir-is-never-cleared-between-runs (target/smoke-e2e is never cleared, so a stale
03-init-exit.txt from 2026-09-13 read as this run's result while --init was still building; 14 stale files archived),
smoke-finding/cold-room-smoke-lane-can-never-reach-committable-work (filed on the in-forge agent's explicit request —
a cold Vault holds no GitHub token, so the credential guard hard-stops every post-reset lane before committable work
while the lane still exits 0; the in-forge handling was correct throughout and is not what needs fixing).

Also measured green and worth keeping: 1134-u934 is fixed in this cut and the row does not claim it — the vault container
still shows the packet's exact 1 bash / 10 vault / 11 tee tree and now stops in 1s with exit 0 against a 10s grace, where
the packet's pre-fix state was the full 30s burned and Exited (137). 1149-vgn2 verified from both sides on the host that
motivated it.

Next: 1109-t8kw part 1, the cross-shape sweep (tier / hostname / GIT_CONFIG_GLOBAL / tillandsias-build tool assumptions),
flipped to this host by the coordinator; each hit reproduced at this locus before any edit, per the row's own converse rule.
Then 4h cadence at :21.
