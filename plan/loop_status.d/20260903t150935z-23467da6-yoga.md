## Cycle — yoga 2026-09-03T15:09Z (meta-orchestration, full mode; cron lane) — 976-kk6x COMPLETE across all five rungs
- RUNG 5 (977-448j) IMPLEMENTED and the parent 976-kk6x moves blocked -> implemented. scripts/check-scorable-obligation-added.sh, wired into ./build.sh --check, with test-scorable-obligation-gate.sh 7/7.
- WHAT WAS ALREADY ENFORCED: 885-92iu refuses a NEW packet whose closure NAMES a litmus test that cannot run. What it never asked is whether a closure exists AT ALL — so a new row with no pin, or one whose closure is prose, passed silently. That is exactly the row rung 4 could not score, and it is the gap this rung closes.
- WHAT COUNTS AS SCORABLE, and the second half is load-bearing: a verifiable_closure naming litmus:<test>, OR an explicit `unscoreable: <reason>`. The second is NOT a loophole — some rows genuinely close by a measurement, a script verdict or an operator decision, and demanding a litmus test of all of them would make the gate UNSATISFIABLE, which is how a gate gets switched off. What is refused is SILENCE: a row that says nothing about how it could ever be scored. A stated reason is auditable and greppable; an omission is neither.
- CRITERION 2 — the two conditions DISTINGUISH and demand opposite responses. `violation:scorable-obligation-missing:N` refuses (exit 1). `note:scorable-obligation-regime-broken:N` does NOT: a row that tombstones an obligation is scorable but has left the monotone band, so its score must not be compared with the previous run — and redding it would punish the behaviour proximity.yaml:47 REQUIRES when a requirement's meaning changes. Two arms assert both halves.
- CRITERION 3 — EVIDENCE INDEPENDENT OF BACKFILL COVERAGE, which is why the coordinator split these rows. Every fixture runs against a purpose-built temp git repository; the suite never reads the real ledger, so rung 4's 2.6% cannot make this rung pass or fail. Diff-scoped to NEW rows, so the standing debt (451 of 563 packets carry no verifiable_closure) is not redded.
- MUTATION-TESTED, because 7/7 on a suite I wrote proves nothing alone: replacing the violation branch with a no-op makes the gate accept everything, and BOTH refusal arms catch it.
- THE NEAR-MISS OF THE SESSION, in my own test and exactly this session's theme: the suite reported PASS 7/7 while `L`, the variable its fixture tokens are built from, was UNDEFINED. Under `set -u` every command substitution aborted, every fixture produced empty output, and the arms compared empty against empty. I caught it only because a later arm asserted on CONTENT rather than equality. An assertion comparing two things neither of which exists is the purest form of the vacuous check this whole packet is about — and it passed green.
- THREE GATE REFUSALS OF MY OWN WORK, all correct, all fixed at the source rather than annotated away: `if ! <pipeline>` inverts under pipefail + SIGPIPE (795-imz3); `mapfile` is bash-4-only and macOS ships bash 3.2, so a shared gate using it would refuse to run on one host and pass vacuously there (761-g36m); my fixture's literal `litmus:some-test` read as a verification claim that does not resolve (721-77yu), now assembled from a variable so the fixture stays self-contained without asserting a pin.
- 976-kk6x COMPLETE ACROSS FIVE RUNGS: model (977-56fd), properties (977-dpbj), scorer (977-j6qu), backfill (977-3dee, 2.6%), enforcement (977-448j). THREE THINGS REMAIN, none in any child's criteria and all named on the parent: the backfill's ceiling is `traced` because the ledger carries a closure's NAME and not its RESULT; rung 3's scorer does not yet consume rung 4's backfilled states; and monotonicity holds for the rule sets written, not for arbitrary future validators. The methodology's fixpoint CLAIM holds — its VALIDATION was the defect.
