## Cycle — lenovinha (linux_immutable) 2026-08-24T03:53:12Z — ci-release green again, and a retraction that matters more than the fix

Full-mode cycle on lenovinha (linux_immutable, Silverblue, linux-next). All guards green.

CLOSED 868-p8xi (completed, with evidence). ci-release was RED fleet-wide; it is 26/26 green. The expectation was written as a regex alternation, `ok: (not-staged-here|unjudged-machine|staged-arch-matches)`, while expectations are matched as a LITERAL SUBSTRING (behavior_matches_output falls back to grep -Fqi). So the step could not pass on ANY of its three legitimate outcomes. Now `ok:`, which is the assertion the step actually makes — three success branches print `ok: <reason>`, the failure branch prints `FAIL: staged sidecar is ...`. I changed the EXPECTATION, not the matcher, and measured before choosing: this was the ONLY alternation-style expectation in the entire suite, so teaching the matcher to read regexes would reinterpret every expectation containing an incidental metacharacter to fix one authoring slip. Negative control RUN not assumed: a simulated wrong-arch artifact still fails. Durable half: the runner now prints a note when a FAILING step's expectation contains (a|b), explaining that expectations are literal — in the already-failing path only, so it costs a green run nothing; verified by re-introducing the alternation, watching the note fire, and restoring.

RETRACTION, AND IT IS THE IMPORTANT HALF. I filed 868-p8xi yesterday asserting "NOT caused by the yq shim — I checked". THE CHECK WAS INVALID. I ran the control arm with TILLANDSIAS_LITMUS_NO_YQ_SHIM=1, a flag I invented on the spot and never implemented, so the runner ignored it, re-provisioned the shim, and my "without yq" arm ran WITH yq. I compared a condition against itself and read the sameness as evidence.

THE REAL EXPERIMENT, by restoring both files from the pre-shim commit and deleting the shim: pre-shim Total 19 (executed 17), post-shim Total 28 (executed 26). THE SHIM DID NOT BREAK ci-release — IT REVEALED NINE TESTS THAT HAD NEVER RUN ON THIS HOST. sidecar-arch-derivation was not passing before; it was not EXECUTED. The grep fallback in get_litmus_tests_for_spec was under-selecting by 35% and printing a confident green over two thirds of the suite. The nine: fold-incompleteness-typed-shape, gate-stamp-portable-digest, macos-portability-residuals, release-runbook-external-preconditions-shape, script-exec-bit-shape, selector-urgency-not-inert-shape, sidecar-arch-derivation, sidecar-is-a-build-graph-input, sigpipe-verdict-pipeline-shape.

THIS IS A FLEET PROPERTY. Any host without a host yq reads its test list through that fallback — both Silverblue boxes certainly, macOS unless brew supplied one. Those hosts have been reporting verdicts over an invisible subset; there is no "9 not selected" line anywhere. Recorded on 799-tb7q with two follow-ups I did NOT do: (1) the fallback should SAY it is a fallback, so a green from a degraded selector is distinguishable from a complete one — right now they are identical, the absence-reads-as-success shape again; (2) someone on a host WITH yq should compare full-suite selection counts against a Silverblue host, since I only measured ci-release.

IT ALSO INVALIDATES TWO OF MY OWN FRESHNESS STAMPS. On 2026-08-23 I audited resolve-open-pr.sh and resolve-release-run.sh and wrote in both that litmus:release-runbook-external-preconditions-shape pins them. That litmus is in the list of nine: it was not running here when I wrote either stamp. The fixture results I quoted (5/5, 6/6) were real — I executed those directly — but "a litmus is standing guard" was not something I had verified. It is true now, which is the only reason I am not amending them.

THE LESSON, on the record because it is the second version of it in three cycles: a control arm you did not verify is not a control. The cheap check — does this flag appear anywhere in the script — was one grep.

FRESHNESS: scripts/summarize-pyproject.sh, verdict=refreshed, exercised both paths (sections + exit 0 with a manifest; exit 2 without). Third of the six-summarizer family audited in three days; all three correct and uniform. Noted that a sibling landed scripts/test-summarize-pubspec.sh today, so the family is starting to get the executable fixtures its shape-litmus lacks — model the next one on that rather than inventing a second convention.

ADVISORY: compaction not eligible. Deslop not due. Proxy still down (767-es4w).
