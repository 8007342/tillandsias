## Cycle 2026-08-26T07:24Z (linux_immutable/calmecacpilli — 863-iicc criterion 2 answered by measurement; ledger_rule's rationale has expired)

- **Host**: calmecacpilli, `linux_immutable` (Silverblue), `linux-next`. Session restarted after an operator-directed shutdown last night (leaked token rotated, host logged out, loop cancelled); operator re-authenticated and re-armed the loop at 6h.
- **Boundary snapshotted FIRST**, before the guards. Guards: `ok:branch-linux-next`, `ok:experts-healthy`, `ok:clean-tree`, credential `ok:gh-keyring-push-verified-refstate-refused` (lenovinha's 886-qmdz verdict correctly reading a behind-branch as NOT a credential fault — its first use here). Preflight `ok:cycle-preflight:rebuilt+expert-absent+services-ok`.
- **Batch**: `epic=UNGROUPED size=1 seed=silverblue-i7-intel-calmecacpilli-20260826 route=tier:low-end`; `triage: eligible=1 ungrouped=1`. Same single-packet frontier as the previous cycle, so this is the consecutive-related-packet directive applied rather than a new subsystem.
- **Stranded sweep**: `population=1 in_progress=1 stranded=0 threshold_events=0`. Nothing to act on.
- **CRITERION 2 ANSWERED, AND ITS PREMISE HAS EXPIRED.** The criterion asks whether the ORDER-TOKEN COLLISION WINDOW (explicitly not the file-conflict question) makes it unsafe for `plan/index.d/` and `plan/loop_status.d/` fragments to ride a lane. Measured here at 638 packets: three mints at HEAD gave `907-55by / 907-kyvu / 907-858f`; two mints against a deliberately stale base (fragments hidden) gave `903-yyen / 903-s5az`. **The PREFIX tracks ledger freshness — a four-order gap, exactly the feared window. The SUFFIX does not: five mints, five distinct suffixes, including two from identical stale input.** So a stale base yields a shared PREFIX and never a shared token, and a shared prefix is already declared normal and permanent by design.
- **AND THE RULE'S CITED EVIDENCE IS FROM A FORMAT THAT NO LONGER EXISTS.** `ledger_rule` justifies itself with collisions 462/464, 477/479, 478-479/486-487. Measured at HEAD: bare `order: N` tokens run to a max of **578**; suffixed `order: N-xxxx` begin at **583**. All six cited collisions — and the 2026-07-31 pair at 560-562/568-570 — predate the suffix entirely. They were real collisions of a token format that has since been replaced.
- **Proposed a minimal `ledger_rule` amendment and did NOT land it.** This packet requires orchestrator ratification for anything changing fleet practice, and methodology is exactly that. Routed to the coordinator with the measurement attached. The proposal answers ONLY the fragment question and only for the suffixed-token era — it says nothing about whether lanes are sanctioned (criterion 1), the missing reaper (criterion 5), or the green-exit command that has never existed on any ref.
- **STILL BLOCKED, unchanged and re-verified this cycle**: this host's first capability row still cannot be published — `1 fragment entry the fold could not use`, ledger goes `incomplete`, release preflight would refuse every push. Re-tested rather than assumed. So `check-capability-row.sh` still reports `due:no-capability-row:calmecacpilli` and this host remains absent from `capability-matrix`. **That absence caused a real routing error overnight**: the coordinator assigned this host the fleet nix-cache rollout, having themselves exempted Silverblue hosts from nix the night before — they routed by memory because there is no row to route by, and recorded it as their error. That is 889-ewvt's cost, demonstrated within a day of filing.
- **Declined the nix-cache rollout** rather than accepting an assignment this host cannot execute (no `/nix` by construction; and at the time, no credential and no schedule). Also flagged that the operator's "have them all" likely resolves to one or three hosts of six, and that on gates which are ~53% ledger work (yoga's replicated figure) a nix cache should be expected to move the number very little — a prediction now in the ledger BEFORE anyone measures, so it can be falsified.
