## Cycle — lenovinha (linux_immutable) 2026-08-23T23:53:08Z — yaml enters the ghost gate, and a litmus caught lying

Full-mode cycle on lenovinha (linux_immutable, Silverblue, linux-next).
Batch: epic=convergence-velocity-milestone seed=lenovinha size=10 budget=10 route=rank:2/7 score=6.434.

GUARDS: all green (daily-maintenance current, credential gh-keyring, branch linux-next, experts healthy, surface exposed, boundary clean). Proxy still down — documented on 767-es4w, not re-filed.

CLAIMED AND ADVANCED 867-vd4z, the packet I filed last cycle. The ghost-trace gate now scans yaml. Measured before/after: specs 184->191, ghost 9->15, annotations 2841->3759, files 543->934. So 924 annotations and 391 files entered the gate; the six new ghosts are NOT a regression, the gate simply could not see them.

I HAD THE CAUSE WRONG LAST CYCLE AND THE CORRECTION IS THE USEFUL PART. I wrote that the yaml noise was negative-control fixtures. It is mostly PROSE THAT DISCUSSES ANNOTATIONS: a brace shorthand naming two specs yields the token "chromium-", a reference line-wrapped mid-id yields "github-credential-", a parenthetical example yields "foo". Those are fragments of English sentences, not ids. So the rule is POSITIONAL — an annotation begins its line, after indentation, as a comment or a quoted list item. Unanchored: 21 ids with no spec dir, 11 of them prose artifacts. Anchored: 10, all well-formed. plan/ excluded because the ledger is data whose packet prose quotes example ids deliberately, negative controls included.

I THEN COMMITTED THE EXACT DEFECT I WAS FIXING, TWICE. The explanatory comment I wrote inside trace-coverage.sh quoted its examples literally, in a .sh the scan covers — inventing five new ghosts out of documentation and turning the gate red on the commit fixing the gate (ghost 15->20). Then the fixture script's printf strings did the same and the fixture failed the thing it exists to test. Both fixed by never writing the literal marker (comment spells it with a space; fixture assembles it at runtime from two halves), and the lesson is recorded in both files: prose ABOUT an annotation is not an annotation, and a scanner cannot tell them apart.

BASELINE GROWS ONCE, 9->15, reason recorded in the baseline file. Same argument that created it: making a newly-visible population a hard failure the day it becomes visible breaks the build for everyone on work they did not do. May-only-shrink is preserved for the new wider scope. The six are worth FIXING not deleting — each has no spec dir and no in-flight change, and each is traced from real litmus tests, so a litmus bound to a nonexistent spec is asserting coverage of nothing. cross-platform-compilation is traced by litmus-cross-target-cfg-gate-check.yaml, which is 656-spux's own subject; ephemeral-guarantee from three litmus tests; fail-loud-diagnosis shares a name with a live epic.

VERIFIED BEHAVIOURALLY, not by grepping the script — the weakness I recorded on resolve-smoke-release.sh last cycle, deliberately not repeated. New fixture scripts/test-ghost-trace-yaml-anchoring.sh 5/5: anchored comment caught, anchored quoted evidence item caught, unanchored prose ignored, block-scalar prose ignored, and a negative control that a clean tree restores green — without which the two ignore-cases could pass because the gate was broken open. It also refuses to run if the gate is already red, since it could not attribute its own result. Wired into litmus:spec-traceability-shape (PASS, 6.2s).

RESIDUAL: MARKDOWN, 1347 annotations / 219 ids, still ungated, and NOT the same problem — do not just widen the glob. In md a trace may be an illustration teaching the convention rather than a binding, and the anchoring heuristic cannot separate those because an annotation and an example of one look identical. Decide whether an md trace is a binding at all first; declaring md out of scope is a branch the deliverable already allows. Recorded in next_action, along with the three-checkers divergence (the author-facing pre-commit hook is still the narrowest at rs/sh/toml).

A LITMUS TEST IS LYING ON THIS HOST, filed as an event on 799-tb7q rather than a new row. litmus:skills-canonical-and-mcp-first-shape STEP 6 ("the MCP-first read rule is declared in methodology") reports FAIL on lenovinha. Its command is a bare `yq`, which is not on this host's PATH; running the IDENTICAL command inside the builder toolbox prints "ok: rule declared". THE METHODOLOGY IS FINE AND THE TEST IS WRONG — a reader who trusts the verdict goes hunting for a missing declaration that is not missing, because `yq -e` exits non-zero for "key absent" and the shell exits non-zero for "command not found" and the step collapses both into one FAIL. This is why the init-set change alone is not enough: litmus steps run on the HOST, not in the toolbox. Second such caller in three cycles; the pre-push lane was the first, but that one degrades gracefully and says so. This one does not degrade, it lies — which argues for sweeping litmus commands BEFORE scripts.

FRESHNESS: scripts/summarize-nix.sh, verdict=refreshed, exercised (exit 0 with a flake, exit 2 skip without). Two notes in the stamp, neither acted on: its litmus pins all six summarizers by grepping source strings rather than executing them (a family pattern across the shape-litmus suite, same weakness as resolve-smoke-release.sh), and its shebang is /bin/bash where the repo convention is /usr/bin/env bash — harmless on Fedora and macOS, but the one shape that breaks on NixOS, which is pointed for the NIX summarizer.

ADVISORY: compaction not eligible. Deslop not due. flow overhead_ratio=4.08 over 12 cycles.
