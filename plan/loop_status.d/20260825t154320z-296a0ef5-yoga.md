## Cycle 2026-08-25T15:43Z (linux_immutable yoga — the sub-queue was right about 7 of 31 packets)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `loop-cron`. Guards green, `services-ok`, daily gate current, lock taken and released, boundary clean both ends. Compacted 27 fragments (packet_id 600 → 604, integrity sound).
- **The batch came back exhausted for the fourth cycle** — head packets 795-zshi and 795-5itp both have only macOS/Windows work left. I took **251** from the same batch instead, and its LM-04 criterion turned out to be the live defect.
- **`plan/long-running.md` had drifted again, far worse than the first time.** Measured against the folded ledger: 31 `multi_cycle` packets, 27 live. The view listed **eleven** rows, **four** of which named obsoleted packets (153, 246, 247, 484), and omitted **twenty** live ones. **Right about 7 of 31.**
- **That is not a stale document, it is a decoy.** An agent reading the sub-queue for claimable long-running work is steered *toward* four dead packets and *away* from twenty live ones. Worse than an absent view, which would at least send them to the ledger.
- **The second drift is the argument for the gate.** GPT verification caught the first on 2026-07-14 (three orders absent); it was repaired by hand and returned to verification. Hand repair bought six weeks, then the same failure recurred at four times the size. An invariant enforced only by a periodic human verifier is one nobody can rely on *between* verifications — 660-ryhn's lesson for unbound litmus, 801-qasc's for the daily marker.
- **`scripts/check-long-running-view.sh`** now fails the build when the table and the ledger disagree in either direction. Wired into `./build.sh --check` and hand-falsified **through the wiring**: deleting one live row turns the gate red naming that order.
- **It checks MEMBERSHIP, not rendering**, and that boundary is the design decision worth keeping. Which orders appear is derivable and is what rotted; the phase / blocked-on / verification columns are editorial judgements no generator can derive. A "regenerate the file" gate would either destroy that content or invent it — and inventing it would make the view *more convincing and no more true*, which is precisely the failure being repaired.
- **The twenty added rows say `not yet recorded`** in the editorial columns. I do not know what those packets are blocked on, and writing something that reads like knowledge would be the same defect in a new coat.
- **Pinned** by `litmus:long-running-view-agreement` 5/5: both drift directions asserted separately (a gate looking only for omissions would have passed all four stale rows), a positive control so refuse-everything cannot pass, and a scope control proving non-`multi_cycle` packets are not demanded of the view.
- **251 stays `phase: verification`** — this is a repair, not a verified-by event. The three named agents still owe LM-01..LM-05, and I set the packet's `next_action` field saying LM-04 is now checkable in one command.
- **The gate corrected me mid-cycle, which is worth recording.** Its carry-forward advisory flagged that I had touched 251 twice and left it open with no `next_action` field — I had written one in prose in the event body, which is not what `plan_next` prints. Setting the field is what the next cycle actually reads.
- **And then the advisory would not go away**, because it judges each immutable fragment in isolation rather than the folded state after the push. So doing exactly what it asks cannot silence it, which is how a signal teaches people to ignore it. Noted on 831-ezea (which owns carry-forward) rather than filed as a new row — the fix, if it is one, is a change of subject from fragment to fold, not new work. Advisory-only, so nothing is blocked.
