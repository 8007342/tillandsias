## Cycle — macuahuitl hourly coordination (cron lane)

SIBLING HEADS, all converged at zero drift ahead of linux-next:

  main          b528680d2   behind 498    (release line, PR-only)
  linux-next    69f5ac7e3   trunk
  windows-next  4c81bc4ed   behind 8
  osx-next      0ff799bd6   behind 34

Nothing to merge this pass. No branch exceeds D_max=5 ahead, so no
sibling-integration task is forced and no lease is frozen.

STRANDED-CLAIM SWEEP: live=8 expired=0 held=0 unknown_age=0, ttl 24h.
Nothing stranded, nothing to reclaim. Two rows need reading rather than acting:

  - 1025-a896 unclaimed-in-progress, no claimant recoverable (base-index
    compaction dropped host+ts; 1065-4t7t says this row can never become
    attributed). Not routable. Ties to the operator's gh-token queue item.
  - 823-u5zf claim-without-status against yolanda is a FALSE POSITIVE and is
    now a live instance of 1082-k3wp. Yolanda filed a TYPED `release` event at
    23:30:00Z — the machine-readable form they adopted precisely so a reader
    could act on it — and the fold correctly says `ready`. The sweep tests for
    "recent claim event AND status ready" and never looks for the later
    release. The host's discipline was already perfect, so this row cannot be
    cleared by better host behaviour; routed to yoga as evidence, and yolanda
    told they owe no set-field.

VELOCITY: R falling. Closed or landed since the last pass — 1067-24q6
(expire-claims inert by default, both directions pinned), yoga's severity-
before-colour-strip fix at 69f5ac7e3, 1071-adhj (lenovinha, relay held here),
1082-9rub (macbookair), 823-u5zf released with a typed event. V_c >= V_min;
NO High-Velocity Alignment Event. Commit rate is under C_max with V_c > 0, so
no thrashing cooldown.

NEW P1, UNCLAIMED — 1085-g52w (yoga): reopening a terminally-closed packet
reds the gate FLEET-WIDE. `set-field --reopen-evidence` is the sanctioned
correction path, it correctly refuses a walk-back without a falsifying
observation, and its result cannot be landed by anyone on any host, because
check-fragment-status-loss.sh:433 accepts a `completed` event only when the
packet folds terminal. The only workaround is to abandon the reopen, which
silently converts a falsified closure back into a standing one — the
laundering shape. The fix is NOT widening the accepted-status set (that
deletes the guard's real subject, the 11-of-21 unapplied closures of
2026-08-09); it is threading the event ts into the join, which the ledger
already carries and `declared_events` drops. Verified NOT currently poisoning
trunk: 1033-iycs folds `done` and the guard returns
ok:no-fragment-status-loss:121 checked.

ASSIGNMENT BOARD

  yolanda      1004-5f7p windows-ready-not-durable-after-provision-once
               fallback 350 windows-forge-config-trust-live-parity
  macbookair   1004-cp6p macos-tray-build-has-no-declared-prerequisite-set
               fallback 155 macos-tray-stream-refactor
  macneo       1084-x8ya (live, claimed 22:47Z)
  pirria       1074-96z9 (live)
  lenovinha    1063-nraf, 1071-adhj (live)
  yoga         1067-24q6, 1081-gynk, 1082-k3wp (live)
  esmeraldinha 1004-4xie (live) + gate 25 in flight
  macuahuitl   coordination; holding a relay land

Both dispatches carried the 982-sguu credential preflight explicitly.

A THREE-PLATFORM CLUSTER WAS NAMED THIS PASS rather than three packets being
worked independently: 1082-9rub (done, macbookair — `is_provisioned` is a stat
of rootfs.img and twenty sites branch on it as though it meant provisioning
completed), 1084-x8ya (macneo, live) and 1004-5f7p (yolanda, dispatched) are
one conflation on three platforms — an artifact's existence standing in for a
completed transition. Yolanda is to read macbookair's closure first and report
against it rather than file a fourth packet.

CORRECTION LANDED THIS PASS: the item-3 calibration arm's gate-time failure
count on this host is TWO, not four, and the four was minted here and handed
to esmeraldinha, who built a fleet claim on it. Route to four was a double
count of one event class through two instruments — every land refusal IS a
full-fixture run that failed. The conclusion it supported survives on a
different footing: nine corpus nulls taken under one condition with no control
that the condition can produce a positive is a floor at n=0 from here.
esmeraldinha then supplied the in-gate control (the fixture's own arm 1,
manufactured and deterministic, sigpipe-decided:fx:1:3/3 inside ./build.sh
--check on drvfs) which is strictly better than the corpus instance I proposed
carrying in, because a third party can repair a corpus instance out from under
the control and every later null then looks identical.

NEXT LOOP: land the held relay (lenovinha's 1071-adhj) — attempted this pass
and reported below; take up 1085-g52w if no fresh host claims it; chase
calmecacpilli, absent from the peer list, on the operator's website-update
directive.
