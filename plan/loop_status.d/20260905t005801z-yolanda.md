## Cycle 2026-09-05T00:35Z-01:05Z — yolanda (windows), advance-work-from-plan

Boundary first, preflight green, `linux-next` merged. **1032-62rx: the
client-side wire-version refusal is now tested** (`d8889f590`), and I proved the
branch fires before asserting it does — macbookair's "do not inherit my
confidence" caveat was warranted.

The fixture is **parameterised, not copied**: `spawn_hello_responder` delegates
to `spawn_hello_responder_with_version`, because a second responder would be a
second implementation of "what a server says on handshake" — the shape this
fleet paid for repeatedly today.

**Deletion mutation run:** `if wire_version != WIRE_VERSION` → `if false` turns
it RED, green on restore. So it is not another tautology beside the six
assertions this packet catalogues.

### Two things Windows hid, both the shape the packet is about

1. `cargo test -p tillandsias-host-shell handshake_refuses` reported
   **`ok. 0 passed; 83 filtered out`**. The module is `cfg(all(test, unix))` —
   `UnixListener` — so on Windows none of it compiles, the filter matched
   nothing, and it exited 0. **Second time in two cycles I was handed a green
   over an empty set**, and the first time I nearly banked it.
2. Running it in the WSL builder distro surfaced a **real compile error Windows
   could not show**: `expect_err` needs `T: Debug` and `Client` doesn't derive
   it. The test did not build on the only platform it runs on.

**So the honest scope: the host-shell client site is NOT verifiable from
Windows.** I verified it inside the `tillandsias-build` distro against a
separate `CARGO_TARGET_DIR` — now the method for any unix-gated host-shell test
here. Read "yolanda verified it" as "verified via WSL".

**Still owed on the packet:** the server side (guest/Linux; its e2e is
`#[ignore]` and needs `vsock_loopback`), and criterion 3's **all-ten** deletion
arm — I deleted one. The all-ten arm is the one that would catch an eleventh
site nobody enumerated, and it needs a host that can compile every crate;
`--features tray` will not build on Windows at all.

**Handoff note macuahuitl asked for**, exact line: litmus runs on this host open
with `warn:litmus-degraded-no-yq — yq is not on PATH and could not be
provisioned from the tillandsias-builder toolbox. Steps whose commands call yq
will fail or return empty.` Any yq-dependent litmus verdict from yolanda is
unearned until that is fixed.

**Next per macuahuitl:** 1042-svey — no openssl in the builder distro, so the
gate's CA-generating test dies eleven minutes in with `os error 2` while
989-ykks passes seconds earlier.

### cycle-metrics (`--cycle-start 2026-09-05T00:35:44Z`), verbatim

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent
timing: steps=10127 build_check_ms_avg=620332 build_check_mix=mixed:forced=137,memoised=34 litmus_ms_avg=13723 slowest=step:checking-that-added-fragments-declared-closures-resolve-885-92iu:490764 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-05T00:35:44Z steps=121 top3=build-preamble=2,build-check=1,build-check-memoized=1 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=9116 steps=127 top3=build-check:runs=117:total_ms=77767828:avg_ms=664682:fail_pct=18,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=96:total_ms=10207860:avg_ms=106331:fail_pct=0,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=96:total_ms=6698088:avg_ms=69771:fail_pct=0 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=51 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=96:avg_ms=106331:fail_pct=0:saved_ms_upper=10101529,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=96:avg_ms=69771:fail_pct=0:saved_ms_upper=6628317,step:writing-the-gate-stamp:runs=96:avg_ms=59833:fail_pct=0:saved_ms_upper=5684152 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=719 ready=461 plan_bin=./target/release/tillandsias-plan.exe
repo: commits_this_cycle=- worktree=clean traces=current
verdict: attention:experts-never-called
```
