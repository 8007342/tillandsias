## Cycle 2026-09-05T17:01:55Z (yolanda — 891-5shq closed by demonstration, 776-g6r3 closed on ruling, 1066-xxsu filed)

**Pulled from DEEPER in the eligible set rather than reporting the head blocked
again**, on the coordinator's ruling that a queue head waiting on operator
decisions is a fact to state once and route, not to re-derive hourly. The head's
four constrained rows were left where they are; 891-5shq came from the deep
query and was the best available work on this host.

**891-5shq — closed, and criterion 1 DEMONSTRATED rather than argued** (landed
8cf111fce, cycle land 5eae1ca76). Three sequential runs on this host:

| run | invocation | result |
|-----|-----------|--------|
| 1 | `./build.sh --check`, tree changed | re-ran, 1151s, wrote the stamp |
| 2 | `./build.sh --check` again | `ok:gate-fresh … nothing re-run` |
| 3 | `TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check` | **zero** `gate-fresh` lines, re-ran 1000s, new stamp |

**I measured HEAD before changing anything, and it cost me the headline.** The
packet was filed when the WSL dispatch forwarded a single hardcoded flag. The
tree has moved: 889-8tcb already carried the namespace forward across, so
criteria 1-3 were ALREADY MET by another order. Said so rather than claiming
credit for it. Criterion 4 was the open one — "the two dispatches share the
forwarding logic, or a check proves they agree" — and two independent copies of
the same four-line loop existed, differing only in variable names and a stray
space.

**Sharing beats checking, and the criterion offered both.** Both dispatches now
call `scripts/lib-env-forward.sh`, so divergence becomes IMPOSSIBLE rather than
detectable, and a third boundary has something to call instead of a fourth thing
to reimplement. The packet's own warning — "do not fix this by hardcoding a
second flag into `_ENV_PREFIX`, that is what made the current state look
intentional" — was honoured rather than discovered late.

**The sabotage that proved more than expected.** Dropping the `%q` quoting reds
arm 3 in BOTH directions: the value arrives truncated at the first space, AND
the fixture's canary file is **created**. The injection seam is demonstrated,
not asserted — a forwarded flag containing a semicolon is otherwise a command
the far side runs. The seam was already closed; the arm keeps it closed.

**776-g6r3 — closed on the coordinator's ruling that "mapped" means documented**,
with the ambiguity written INTO the closure so a future reader cannot mistake it
for "we have submitted through the API". The exercised reading is unreachable by
any agent: five operator account actions gate the first call, now routed as one
operator ask. Two findings survive the packet: the manual first submission is
MANDATORY rather than advisable (the API cannot create the app), and
**nothing in the release lane may block on `Published`** — certification runs
hours to three business days, and every in-flight status looks like progress.

**A ruling I had to push back on, with measurement.** The coordinator ruled the
stray `plan/index.d/frag.yaml` should be RENAMED to the canonical name, reasoning
that the filename is the concurrency contract and a fixed generic name loses an
event silently. The reasoning is right; the premise was not. `md5` and `cmp`
showed it is a BYTE-IDENTICAL DUPLICATE of
`20260905t133654z-0eeb4214-yolanda.yaml`, so renaming would collide with the file
it duplicates. Deleted instead — the event survives byte for byte under the name
the contract requires. Had it been the only copy, the rename was the right call.
It was my error to begin with: `cp "$TEMP/frag.yaml" plan/index.d/` copies to the
DIRECTORY, taking whatever name the temp copy happened to have.

**1066-xxsu filed** as its own row rather than as a line on esmeraldinha's: the
portable-digest fixture's PATH farm links `shasum` but not `perl`, and MSYS
`shasum` IS a Perl script, so under the farm it emits nothing and the digest
comes back empty. Same class as her preflight finding, different mechanism — hers
was a tool ABSENT from the farm's source dirs, this is a tool PRESENT without its
INTERPRETER. **It is not gate-bound**, which is why it hid: every `--check` here
has been green over it, and it is only reachable through a litmus corpus that,
until 1049-s35z landed, was silently skipping most of itself.

Agent: windows-yolanda-opus5-20260905t170155z
