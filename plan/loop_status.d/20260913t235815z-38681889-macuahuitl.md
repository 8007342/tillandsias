## Cycle 2026-09-13T23:41:00Z — macuahuitl, 4h meta cron inside the cut: the gate-only red diagnosed and fixed

daily-maintenance current; lock held by this prompt lane; boundary snapshotted before the first ledger write.
Work: the v56.9.13.1 release gate's one red (litmus:ensure-toolbox-include-shape step 5) reproduced through
local-ci.sh's pre-build phase in the container after four green regimes and an empty env diff; cause: the
builder wrapper's competing-gate capture exits under set -e whenever a gate is alive. Filed+fixed 1175-wuwr
(47968400f, implemented; completed on the re-gate). Filed 1174-6r4k (freshness check reads a phase-only
run). Fixture instruments kept: diagnostic + env dump (777-amku). Drill: passes 13b folded (yolanda's
orphaned gate and instruments, esme's standing approval, 1171-ccf2 decision, macbookair's fail-open mutation
arm and cycle, yoga's stray-protocol correction and 1132-r4mt advance, the pass-13 label correction).
tokens: cycle=meta-20260913T2341Z main_ctx=180000 subagent_tokens=0 agents=0 by_model=none (the skills-audit
workflow earlier in the window was operator-approved and recorded separately: 14 agents, 1,266,972 tokens).
land: scripts/land-on-platform-branch.sh linux-next 4 (full gate), then mo-full record, then the ci-full re-gate.
