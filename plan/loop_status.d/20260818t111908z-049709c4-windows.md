## Cycle 2026-08-18T04:19 PDT→04:45 PDT (windows — YOLANDA, MO FULL: locus enforced, a three-cycle advisory root-caused, and the freshness audit finally run)

**Result: three packets closed (810-jeg7, 807-c3mf, and the standing freshness audit's component), one new p1 filed with evidence. Two of the three findings were ledger or process debts this host had been carrying silently rather than new work.**

- **STOP CHECK**: `Tue Aug 18 04:18:57 PDT 2026`, ~1h40m before the stop.
- **Host**: YOLANDA, `windows-next` 723bbb90c → merged `origin/linux-next` (6cadec63f, then again mid-cycle) → pushed 9cb4eed18. All guards green, `ok:clean-tree`, boundary `/tmp/meta-orchestration-boundary.TReVrb`. `verdict: ok:nothing-flagged`; ledger `ok: 1127 packets, ids unique, live references sound`.
- **FOCUS 1–4 were already complete** (808-43mw, 808-7yrd, 806-2r4s landed last cycle; 402 was already `completed`). So the cycle went to the remainder and to debts.

**810-jeg7 — locus stopped being an annotation and became a rule.** The field existed and nothing enforced it, which is a caveat, not a constraint. `measurements_comparable` now refuses: two loci refused even for the same suite on the same machine; an absent locus refused rather than defaulted, symmetrically, with an empty string counting as absence. **Refuse rather than warn**, because the measured hop cost 5–10% on the embed arm and once *inverted* a cross-host conclusion — yolanda read "indistinguishable" from yoga and is actually faster; the original reading survived because two errors cancelled. A warning attached to a number that still gets ranked reads as a caveat on a result; a refusal has no such failure mode. Workload mismatch refuses too — 810-jeg7 names the locus, but refusing on one axis while silently ranking across workloads keeps the hole open on the other. Two records that *both* omit the suite stay comparable, because the locus check already established they were observed the same way and refusing there would reject every pre-808-43mw pair for a reason the packet does not make. Refusals are partitioned with a reason and printed, never filtered away.

**807-c3mf — LEDGER DRIFT ON THIS HOST'S OWN PACKET.** The jq fix landed 2026-08-17 and I filed a note event and **never transitioned the status**, so the packet read `ready` for three cycles while its deliverable shipped. A note event is not a status transition; only `set-field` is. Re-verified against the source before transitioning rather than trusting my own earlier record.

**827-h4tz (NEW p1) — a staleness guard whose remedy its own host cannot run.** Clearing the `stale:windows-sources-unverified` advisory I had carried for three cycles turned out not to be bookkeeping: `cargo test -p tillandsias-windows-tray` **fails 1 of 94**. The staged guest binary carries `0.4.260815`; VERSION is `0.4.260817.1`. So the tray embeds and provisions guests with a headless two days and two bumps behind itself.

The defect is not the staleness — it is that the remedy the failing test prints cannot be run here. `scripts/build-guest-binaries.sh` builds via **Nix**, and measured on this host: nix ABSENT on the Windows side, ABSENT in `tillandsias-build`, musl rust target not installed. `windows_native_tray` is `owner_host: windows`, so **the crate's owner cannot satisfy the crate's own guard.** That is why the advisory persisted — structural, not neglect.

**The obvious fix is verifiably wrong, and I checked before acting.** Zeroing the asset looks right (empty slice → fetch path, and aarch64 is already zero-length), but the code's own comment says the fallback "fetches the LATEST RELEASE, which can be an OLDER wire revision than this tray (version skew — the order 282 trigger)". Embedding is *preferred* precisely to avoid skew; the test is its guard. Trading a known-two-days-stale binary for a possibly-older download would make the failure less visible without making it less real. **Nothing changed in provisioning behaviour.** Filed with three candidate remedies and the evidence that (a) is worth trying first — `musl-gcc-1.2.5-6.fc44` and `musl-devel` *are* installed in the build distro alongside rustup. Not attempted: a cold cross-build would not have finished in the window, and a half-built artifact staged into `assets/` is worse than a stale one. Choosing between the remedies changes where release artifacts come from — a provenance decision, not a cycle-end judgement call.

**FRESHNESS AUDIT — run, after five cycles of flagging it as skipped.** Coverage `2.9% (39/1367)`; target `scripts/uninstall.sh`. Verdict **updated**. Meaningful, useful, and soundly written (the 804-bpke seams, the 804-wfcu model-cache sizing) — but **not complete**: `install.sh` writes PATH integration in five places (marked blocks in `~/.profile`, `~/.bashrc`, `~/.zprofile`, `~/.zshrc`, plus the whole file `~/.config/fish/conf.d/tillandsias.fish`) and the uninstaller removed **none** of them. Every uninstall left the shells exporting a PATH entry for a directory it had just deleted.

The part worth naming: `install.sh` already brackets the block with `PATH_MARKER_BEGIN/END`, and **those markers existed only for install-side idempotency while nothing ever used them for removal.** An installer that leaves a marker and an uninstaller that ignores it is a half-finished handshake.

Fixed with `awk` into a temp file, not `sed -i` — this edits user dotfiles that predate the install and will outlive it, `sed -i` takes a mandatory backup suffix on macOS and not on GNU, and a failed rewrite must leave the original untouched because a truncated `.bashrc` is far worse than a surviving PATH block. The fish file is removed outright only if it is ours. `scripts/test-uninstall-path-block.sh` 5/5 asserts what would actually hurt: lines before and after survive byte-identical; a no-marker file is not modified at all (`cmp`); a missing file is a no-op; two blocks are both removed; an unterminated block preserves preceding content. It also pins the marker strings in both scripts against each other, since drift would make the uninstaller search for a marker nobody writes.

**Honest limits, recorded rather than glossed:** mode preservation is implemented (`chmod --reference`) but **not asserted**, because this audit ran on Windows/MSYS where `chmod 600` does not stick — a Linux or macOS host can add that cheaply. An unterminated block still eats the remainder of the file; `awk` cannot know where it ends, so the test pins that the damage is visible rather than silent. Only the `.sh` uninstaller was audited; the Windows uninstall path is the tray's own and was out of scope.

**NOT DONE, flagged:** ledger compaction eligible; bar-raise proposed, not enabled; 798-q4m9 open pending 819-qgy2 (attended `.wslconfig` + `wsl --shutdown` window); 823-u5zf open with its remainder named (`-lc` composition in `host-shell/src/pty/mod.rs`, `argv_survives_wt_reparse` ×7, `wt_safe_title` ×4); **827-h4tz means `cargo test -p tillandsias-windows-tray` is red on this host and the windows-sources stamp still cannot be taken** — that is now a filed packet rather than a carried advisory.
