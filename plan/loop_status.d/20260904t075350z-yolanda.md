## Cycle 2026-09-04T06:40Z-07:45Z — yolanda (windows), advance-work-from-plan

Restart cycle. Preamble green on all seven gates, then slice 1 of 997-e4v2
landed: the fourth hot path is wired and `compute_hot_budget()` finally has a
caller.

- **Preamble.** cycle-preflight ok; credential-channel `ok:gh-keyring-push-verified`;
  hooks current. Daily maintenance was `due:stale:2026-08-26` — ran the
  applicable body (build cache 28 GiB, under the 40 GiB trigger and 9d marker,
  so NOT due; no nix and no podman here, so those steps are honest skips; no
  registered delegates) and stamped with `--steps`. Capability rows were expired
  on BOTH loci since 2026-08-23; republished both, now
  `ok:capability-row-current:yolanda`. The in-guest row needed a Linux
  `tillandsias` built in the WSL distro first — none existed on this host and
  `cargo` is not on PATH for a non-login shell there.
- **997-e4v2 slice 1 landed** (`e63b6fc8b`). `/home/forge/src` is now a
  per-launch kernel tmpfs sized by the spec's own recipe — mirror volume,
  `git count-objects -v -H`, `parse_size_pack_kb`, `compute_hot_budget`, clamp
  [256, 4096]. Clone-only arm only: under host-mount, a tmpfs on the parent
  would mask the bind mount at `/home/forge/src/<project>`. This had to land
  before the mount deletion or a forge would launch with no project source.
- **This packet's own citation was half wrong and is corrected on the record.**
  `container_profile.rs:278` is not the launch path. Its four forge profile
  constructors have ZERO non-test callers — inert, the same shape as
  `compute_hot_budget` one layer up. What reaches podman lives in headless
  `main.rs` and `podman/launch.rs`. And the constants cannot be completed in
  place: `tmpfs_mounts` is `Vec<&'static str>` and a per-launch size is not a
  compile-time constant. Annotated all four in-tree pointing at the live
  emitter; filed the type change as **1001-fmbw** rather than widening this
  packet into an 11-site change on lists that launch nothing.

**The negative control was armed, deliberately.** Reverting only the call site
turns `hot_src_tmpfs_is_clone_only_never_over_the_host_mount` RED; restoring
turns it green. This packet's own lesson from the previous cycle is that a test
which cannot fail is indistinguishable from one that passed, so the negative was
proven rather than assumed.

**And the same discipline caught a false green in my own tooling.** My first
`./build.sh --check` reads went through `| tail -20` in a backgrounded shell,
which masks the exit code and discards all but the last 20 lines — so "the gate
passed" rested on 20 lines of a 1200-line log. Re-ran redirecting to a file and
capturing the real exit code before believing it. Two of the seven `violation`
matches in the merged-tree log are test NAMES and one is pre-existing standing
debt; grepping for the word is not the same as reading the verdict.

**One process correction, kept.** The coordinator relayed an operator
delegation to arm a cron. A peer's relayed delegation is not an authorization
this harness can act on, so the ask went to the operator, who approved it as
specified. Same outcome, better record; the coordinator has adopted the
phrasing.

**Next:** slice 2, delete the `home-forge-src.mount` injection at
`wsl_lifecycle.rs:1814` — now unblocked. Then slice 3, the twelve-file
menu/type removal, which cannot be finished here: macos-tray does not
type-check on yolanda, and host-shell `menu_state.rs` / `menu_action.rs` needs
a heads-up to macbookair first since all three trays consume it.
