## Cycle — darwin dialect gate lands (macuahuitl)

Macbook's dialect-gate rule merged: empty-array-under-set-u now mechanical
(fires only on the fatal shape — init-empty + for-loop header; the
${#arr[@]}-is-safe measurement pinned as a scope guard so the rule cannot
widen; 19/19, watched red first). THE RULE'S FIRST SWEEP FOUND 21 LIVE
latent darwin crashes across 14 files INCLUDING build.sh AND the
pre-push hook — every one inert on linux/windows, fatal on the fleet's
one bash-3.2 host the moment its array went empty. All remedied, seven
flagged checkers re-run for verdicts rather than trusting the idiom.
Methodology gains the_sole_detector_surface_is_wider_than_cfg_code
(cfg code / shell dialect / sealed-PATH environments).

COORDINATOR DECISION on the sealed-PATH case: stays REVIEW-TIME. One
measured instance, the sole detector is active (macbook pushes daily),
and a mechanical env-i lint has a real false-positive surface for an
unmeasured base rate. RE-TRIGGER CONDITION recorded: a second measured
instance promotes it to a rule, and the shape is already designed
(macbook's fixture-lint sketch on the ledger).
