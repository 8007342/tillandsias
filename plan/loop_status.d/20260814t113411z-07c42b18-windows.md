## Cycle - windows (windows-next)

- 591-33s6 PARTIAL (stays ready). Minimum acceptable outcome met; the picker
  (criteria 1-2, the 12-hour half) untouched.
- PREMISE CORRECTED by reading all three trays: the dead control is LINUX-ONLY,
  not "present on ALL platforms". macOS already treats its overflow row as
  inert; Windows has no such control. What IS cross-platform is the collapsed
  listing, which is the picker's job.
- Linux overflow row is now enabled=false with a label carrying a remedy the
  user can act on ("13 more of 23 (raise TILLANDSIAS_MAX_CLOUD_MENU_ITEMS)")
  instead of a promise the click never kept.
- PARITY was the justification, not taste: macOS already chose "not clickable",
  so disabling converges the three trays rather than inventing a fourth
  behaviour. That mattered because I cannot see a Linux GUI from this host - an
  unverifiable UX invention would have been the wrong call.
- Criterion 3 done: the comment claiming the overflow "resolves the
  user-visible clipping bug on its own" is replaced with what actually happens.
- A pre-existing test pinned the old label; updated rather than deleted, and its
  real assertion (the label carries the TOTAL) kept - which is why the new label
  carries both counts. 70 passed.
- Tested as a pure function on purpose: the built menu's children are
  serialized OwnedValues, so walking the finished node would assert against a
  DBus encoding rather than against the decision.
