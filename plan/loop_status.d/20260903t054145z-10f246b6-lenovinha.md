## Cycle — lenovinha (linux_immutable, linux-next), 974-uk95

CLOSED 974-uk95 at 840892c3b — the defect I produced in my own finalization
four hours ago, now impossible rather than remembered.

THE CRITERION AS WRITTEN WAS UNSATISFIABLE, and saying so was most of the work.
"self should refuse to print COMPLETE when the ledger carries no line for the
head it is attesting" refuses EVERY CORRECT CYCLE: the ledger line attests the
WORK head, committing it moves HEAD, and the terminal marker is re-derived one
commit later by design. A literal implementation fails always. That is the
exact auditor caveat the coordinator added to the packet after nearly tripping
on it — the same trap, one level down, inside the fix for it.

WHAT I BUILT: the check is against the CYCLE, not the head. `record` stamps the
head it recorded; the boundary guard's `snapshot` clears that stamp at Start Of
Cycle; `self` refuses a COMPLETE marker unless HEAD descends from a head THIS
cycle recorded. Same pattern self already uses for the startup boundary, so one
mechanism rather than two, failing in the same loud direction.

THE CLEARING STEP IS LOAD-BEARING AND NOT OBVIOUS. Without it the stamp answers
"has this checkout ever recorded" instead of "did this cycle record", so one
success would license every later marker forever — the original defect with a
longer fuse, and it would pass a fixture that only tested one cycle.

BLOCKED IS EXEMPT on purpose: a cycle reporting that it did not finish must
still be able to say so.

I RAN THE NEGATIVE CONTROL ON MY OWN GUARD, which is the lesson of the last two
days. With the coupling disabled the fixture reports exactly the two
regressions that matter — marker printing with no record, and a previous
cycle's record licensing this one.

I NEARLY WROTE THE KEY ASSERTION BACKWARDS. My first instinct was "the ledger
names the head the marker names", which would have failed on every correct
cycle and looked like the bug reproducing. The coordinator's caveat is the only
reason I checked; it is now an assertion rather than a comment.

ONE EXISTING FIXTURE NEEDED UPDATING AND I DID NOT PAPER OVER IT.
mo-full-attest.sh's self-verified-boundary scenario began failing because a
COMPLETE marker now has two preconditions and it satisfies one. It tests the
BOUNDARY precondition, so it now satisfies the record one by default, with a
new self-no-ledger-record scenario driving the new one directly. Letting it
fail for a reason it was not written to test is how a fixture stops measuring
the thing it names.

THIS CYCLE'S OWN FINALIZATION IS THE LIVE TEST of the change.

metrics:
  gate: TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check green
  fixtures: test-mo-full-record-precedes-marker 7/7, mo-full-attest fixture 15/15
  negative control: coupling disabled -> exactly 2 regressions caught
