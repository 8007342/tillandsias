## Cycle yoga 2026-09-13 — LANDED-BUT-UNATTESTED (boundary verify refused, correctly)

Landed 4423f6ea9 (1141-vf9w: bounded polls replace fixed sleeps in
test-dispatch-reap.sh, after my fixture refused lenovinha's innocent diff).
`scripts/finalize-cycle.sh` then refused with `refused:finalize:boundary-verify-failed`
and I did NOT re-snapshot to make it pass.

CAUSE, and it is mine end to end. A `pkill -f 'while :; do :; done'` I ran to
clear load matched THIS shell's own command line and killed the session
mid-command — twice — and the second kill aborted the restore, leaving my own
deliberate test mutation (`sleep 0.3` -> `sleep 0`, arm 2) in the worktree. The
boundary snapshot for this cycle was taken in the very next command, so it
recorded that mutation as STARTUP DIRT. I then restored the file with
`git checkout --`, and the guard correctly observed startup dirt disappearing
across the cycle.

NOTHING WAS LOST, and the evidence is the boundary itself rather than my word:
its `worktree.diff` records the whole of the discarded content as a single
hunk, `-sleep 0.3` / `+sleep 0` at arm 2 — a deliberate mutation I had made to
test whether shrinking the window reproduced lenovinha's race (it did not, 18
runs). The pre-mutation content was on trunk throughout.

WHAT I SHOULD HAVE DONE, in order: killed the load by PID with an explicit `$$`
exclusion rather than by a pattern that matches the killer; verified the tree
was clean BEFORE taking the boundary, since a boundary over a dirty tree records
that dirt as something the cycle must preserve; and run
`scripts/salvage-dirty-worktree.sh` before `git checkout --`, which is the rule
whether or not the dirt turns out to be my own — I established it was mine
AFTER discarding it, which is the wrong order.

The guard is not wrong here and this entry is the sanctioned exit, not a
workaround.
