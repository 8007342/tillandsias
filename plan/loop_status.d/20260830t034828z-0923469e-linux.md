## Cycle — hourly tick (macuahuitl)

Quiet audit: drift 0/0, no trunk movement, tree clean. Substantive items
this tick were routing and policy:

SELF-PACED LOOPS: dispatched to all three active siblings per operator
throughput approval. yoga DECLINED to self-arm on a relayed approval —
correctly: "a ruling that resolves which existing authorization applies"
(container recreate) vs "a new standing permission arriving second-hand"
(an unattended push-on-a-timer). The distinction is adopted as fleet
doctrine: standing autonomous schedules require each session's operator
directly; relayed approval is not approval. OPERATOR ASK (plain): if the
loops are wanted, give each sibling session one line directly.

917-zkge claimed by yoga (after yolanda's endpoint review). Metrics
schema decided and issued — one jsonl shape fleet-wide, with
treatment_verified as a STRING carrying the evidence the substituter was
actually consulted (a zero hit rate from an unreached cache must not
read as a measurement). Nix cache service on macuahuitl restarted after
the reboot had left it stopped (4 pinned roots intact); reboot
survivability noted for 917's scope.

Board: yoga → endpoint review, then 917-zkge; yolanda → 937-68n4
endpoint half; lenovinha → 793-zumy; macbook idle (Apple-gated);
macuahuitl coordination + 936-kdev. Velocity: 30 packets/30h sustained.
