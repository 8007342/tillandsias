## Cycle — lenovinha 2026-09-04T19:47Z (advance-work; cron :05, selector's pick) — 1004-inkc closed: podman rm no longer satisfies the health check
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T19:35:13Z`:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
      flow: cycles=8 avg_completed_per_cycle=0.13 avg_commits_per_cycle=1.75 overhead_ratio=14 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      timing: steps=27404 build_check_ms_avg=139416 build_check_mix=mixed:forced=365,memoised=96 litmus_ms_avg=13419 slowest=litmus:tool-materialization:304387 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      repeat: window=since=2026-09-04T19:35:13Z steps=127 top3=build-preamble=2,build-check=1,build-check-memoized=1 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      recur: window=7d runs=25266 steps=429 top3=build-check:runs=325:total_ms=47989513:avg_ms=147660:fail_pct=30,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=226:total_ms=7562235:avg_ms=33461:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=226:total_ms=5639009:avg_ms=24951:fail_pct=0 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=59 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=226:avg_ms=33461:fail_pct=0:saved_ms_upper=7528774,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=226:avg_ms=24951:fail_pct=0:saved_ms_upper=5614058,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=220:avg_ms=19512:fail_pct=0:saved_ms_upper=4273312 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=705 ready=457 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=dirty traces=current
      verdict: attention:worktree-dirty
- SELECTOR'S PICK RETURNED MY OWN MORNING FINDING. 1004-inkc was filed from the two readings I reported at 07:08Z, and the selector routed it back here with the measurement already in hand. Landed d08566639 (verified on trunk before citing).
- QUESTION (1) SETTLED FIRST, as the packet demands because it changes the fix: it was the BARE SCRIPT, not cycle-preflight. Reproduced LIVE before any edit — the proxy is still absent on this host from this morning's teardown, and the bare check printed `ok:...services=5:absent=0` while enclave-services.txt declares the proxy required. So this is 994-8r3w's residual, not a failure of its wiring.
- QUESTION (2), the vault anchor, moved into the check WITH the default. Moving only the default would have re-created the same caller-dependent split one layer down: a never-provisioned laptop has no vault, is missing nothing, and must still read ok — a check that fires on correctly-configured hosts gets switched off, which is how the original gap survived. Arm (c) pins that and is as load-bearing as the defect arm.
- FIXTURE FIRST, RED BEFORE GREEN. Pre-fix arm (b) failed verbatim: "REMOVED READ AS HEALTHY — 'podman rm' satisfied the check: ok:...services=1:up=1:down=0:dead=0:absent=0". Post-fix 23/23.
- WHY THE NEW ARM PASSES NO --expect: scenario 6 already proved the absent loop works when a declaration is PASSED IN, and scenario 6 passed throughout the entire period this defect was live. That is exactly why it did not catch it. An arm exercising only the supplied-declaration path cannot see a defect in the DEFAULT path — the same shape as a control that never goes red.
- `services=` NOW COUNTS EXPECTED PLUS EXTRAS so a deletion cannot shrink the total. That shrink was half of why the two readings looked like an improvement: 6 down to 5 reads as "one fewer thing to worry about", not "one thing vanished". Real host after: services 5 -> 6, absent 0 -> 1.
- NEW EXPLICIT OPT-OUT `--expect none`, because once empty means "use the default" there is no way left to ask for pure enumeration — a caller wanting the old semantics would have had to pass a FAKE SERVICE NAME. Three legacy fixture arms went red on my first post-fix run for precisely this: they model a vault without a proxy while testing exit codes and origin labels, and had begun reporting an absence they were never written to model. Coupling every unrelated arm to the fleet's declared set would have been worse than the defect.
- THE FAKE PODMAN'S `container exists` IS MODELLED AGAINST PS_OUT, not stubbed: answering yes unconditionally makes arm (c) pass for the wrong reason; answering no unconditionally silently disables the default set in every scenario and the fixture goes green while testing nothing.
- CYCLE-PREFLIGHT NOW CONSUMES THE VERDICT: its expected-set derivation and anchor block are deleted, and it passes nothing deliberately — an explicit --expect there would restore the caller-supplied behaviour this order removes, one layer up. `ok:cycle-preflight:...services-down0-absent1`.
- UNEXPLAINED, RECORDED NOT DROPPED: my first fixture run after adding the arm reported "21 passed, 2 failed" with only ONE FAIL line printed. Four consecutive re-runs before touching the check gave 22/1 with only my arm failing; I could not reproduce a second failure and the extra one is unidentified. It is NOT the three legacy arms — those failed only after the check change, for the expected-set reason above. Flagged in case another host sees an intermittent here; one observation is not a flake claim.
- EVIDENCE: fixture 23/23, litmus:enclave-service-health-shape 4/4 (pre-build instant), gate green 269s, real-host before/after captured.
