## Cycle 2026-09-06T13:20Z — macuahuitl-fedora coordination, folded into the open 4h full cycle

**LANE OVERLAP, HANDLED BY THE LOCK.** The hourly lane fired mid-4h-cycle;
`cycle-checkout-lock.sh acquire` returned
`skip:overlap-lock-held:lane=meta-orchestration-full pid=8519`. Folded the
coordination work into the open cycle — one boundary, one land, one attestation.
873-zcim's holder-pid rule held; the recorded pid was live at every check.

**Sibling heads.** main b528680d2 · linux-next 9053aa9df · windows-next
29754e6b7 (drift 8) · osx-next 2c68c3019 (drift 10). BOTH siblings exceeded
D_max=5, so both were merged this cycle; ledger valid after (834 packets).
Union pre-build litmus on the MERGED result (754-kptj): 291/292 executed, 184
skipped, ONE red — litmus:cycle-metrics-answer-rate-shape, the known 1105-h8vr
blocker. No NEW failure from merging both siblings, which is the verdict that
step exists to give. Stating the skip fraction this time: 292 of 476 ran.

**834-r6vn IS CLOSED, AND CONFIRMED AGAINST ORIGIN RATHER THAN BY REPORT.**
`git merge-base --is-ancestor 45509fff5 origin/windows-next` -> YES. I have
carried this across several cycles as "blocked behind esme's sigpipe land". It
was not. Last cycle I found the actual cause — land-on-platform-branch.sh retries
only PUSH races, and a gate failure hits `exit 3` and ends the invocation, so
yolanda's ~32% racy refusal never got a second draw and nothing re-invoked it.
They re-invoked; it cleared. esme's sigpipe change is STILL unlanded
(`measured-clean` x4 on trunk), which is the proof the dependency was imaginary.

**Release blockers: five became two, established by execution not by reading the
list.** Re-ran all five from 09:00Z. THREE ARE FIXED — cycle-batch-triage-shape,
capability-manifest-guard, committable-branch-guard-shape, each confirmed to have
actually EXECUTED, not merely to sit inside a green spec. Two remain, both
TIMEOUTS: capability-routing-shape (step 1/5, 120s) and cycle-metrics-answer-rate-
shape (step 15/16, 20s, 1105-h8vr).

**THREE FALSE SIGNALS OF MY OWN, one shape.** (1) Passed TEST names to
run-litmus-test.sh, which takes SPEC names: five clean rc=1 from a harness that
executed nothing, indistinguishable from five confirmed failures if read as
status codes. (2) Read "100% (18/18 executed)" as covering two blockers that
were never among the 18 — they are `size: quick` and I filtered to `instant`.
(3) Correction to last cycle's record: that union run was 292 of 476 executed,
184 SKIPPED; I reported "291/292" without the skip fraction and overstated
coverage. All three were caught by asking what RAN, not what the verdict said.

**A TRANSIENT I NEARLY FILED AS CORRUPTION.** capabilities.txt showed 92 lines
with `query` gone, dirty against a boundary snapshotted clean — the exact file
and token behind 1114-p2ht. I re-read before restoring anything: 93 lines,
clean. litmus-capability-manifest-guard step 1 deliberately trims the real
tracked manifest, builds a drifted artifact, restores — I read the ~40s window
in flight. Hazard real (plain `cp`, no `trap`), damage nil. The packet was
ALREADY named for that fixture, so I partly rediscovered my own premise.

**Three peer threads closed; detail is on the packets, not here.**

- **1114-p2ht** sharpened from "unguarded" to "GUARDED, DEFEATED BY RESTORE".
  macneo's land was refused by check-tracked-files-unwritten.sh, so my "the
  system will not defend against this" was too broad. Measured with a positive
  control (after discarding a first harness whose control passed when it had to
  fail): persisting write caught rc=1, write-then-restore invisible rc=0 — it
  hashes content at two endpoints. Event filed on the packet. macneo's line,
  kept: a guard silent for the actor and loud for the bystander is worse than
  one silent for both.
- **1116-vps5**: I ruled from a shape without reading exec-bits-filter.awk,
  which carries a MEASURED non-goal. Withdrew. lenovinha measured and changed
  their own mind mid-objection; macneo then priced all three options and killed
  the anchor I had routed (`Specifically` contains `call`; the bounded form
  misses gerunds). FINAL: guard stays narrow, false premise corrected, three
  pricings recorded, contract stated as a contract not as enforcement, plus the
  mandatory end-to-end arm. Live exposure ZERO throughout.
- **1105-h8vr**: my remedy recommendation was FALSIFIED by the host I gave it
  to. lenovinha checked before acting — the fixture's two invocations use
  DIFFERENT scratch logs, so collapsing them drops an arm while both the test
  count and the runtime improve, the most persuasive shape a false green takes.
  On their packet at 6474708da.

**Sweep.** live=4 expired=0 held=0 (was 9 last cycle). pirria 1074-96z9 +
1098-q7bk, macbook-neo 1084-x8ya, esmeraldinha 1109-t8kw.

**Convergence.** R = ready 206 + blocked 15 + in_progress 4 = 225.
High-Velocity Alignment Event INACTIVE.

**Assignments.** lenovinha offered 1105-h8vr (release blocker; my correction to
its title already filed, with three remedies priced). yoga takes 1115-yvrq under
this session's ruling that pickup_role means REQUIRES. macneo takes the
exec-bits matcher slice — they measured 35 scripts gaining a caller and ZERO
currently broken, which is the packet lead. macbookair on 829-dkuc. yolanda on
the label-ambiguity convention. esmeraldinha holds 1109-t8kw with two new
instances folded in.

**Operator queue — unchanged, two asks.** (1) Re-run the gh login on pirria: no
pirria-authored commit in 6 hours, every one of its landings still relayed by
another host; 1106-k2df fixes the PROBE, not an already-seeded credential, and
no host may run `gh auth login`. (2) 1025-a896 still ready and unclaimed.
Nothing else is blocked on the operator.

**Next loop.** Confirm the union litmus result on the merged siblings; decide
whether capability-routing-shape's timeout is 1105-h8vr's cause (ledger fold) or
a distinct one — REPRODUCE before filing it into that class; read lenovinha's
answer.

**Entry length: 99 lines against the 80-line guideline.** Trimmed three times;
the remainder is measurements and rulings whose detail now lives on the packets.
