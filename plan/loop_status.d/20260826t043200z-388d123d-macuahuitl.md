## Cycle 2026-08-26T04:32Z — macuahuitl (linux_mutable, linux-next), hourly /loop

TWO FIXES FROM LAST CYCLE VERIFIED IN PRODUCTION. 899-q9di: the lock acquired
cleanly where the previous cycle was refused by its own stranded lock.
895-j8ag (lenovinha): the daily gate read `due:stale:2026-08-25` at 04:32Z
where an hour earlier it read `current` — UTC had rolled, local had not. Ran
the maintenance body and stamped it; the marker now records date=2026-08-26
utc=2026-08-26, agreeing for the first time.

DAILY MAINTENANCE (first genuinely-due run under the UTC boundary). nix-gc
ok:under-ceiling 8G/20G; nix-toolbox guard 9/9; nix-deps-stability pass;
nix-cache skipped-no-enclave; cargo-gc not-due (24GiB/40GiB); delegate-sweep
no-registered-runs; podman-prune NOTHING-TO-PRUNE — the version tags are
ALIASES (tillandsias-git:latest/:v0.4.260817.1/:<digest> are all 0d02d572d90f),
so a zero-byte prune is the correct result, not a skipped step.

897-5m9y THIRD INSTANCE, on the coordinator. Installed binary is 0.4.260818.1;
`gh release view v0.4.260818.1` -> "release not found". Same phantom shape as
yoga's 0.4.260823.1. Forge images sit at v0.4.260817.1 against it, so a forge
launched here would be version-skew DOA. Deferred, stamped into the marker as
forge-images:deferred-version-skew rather than silently skipped.

COORDINATOR: merged windows-next (3) and osx-next (2). The osx merge brought
COMPILED SOURCE, so I ran --ci-full over the union instead of waiting for the
release path — 754-kptj's discipline — and it caught a real skew.

CROSS-BRANCH SKEW, found and fixed. macbook's 731-eupn added `outcome` to
CloudRefreshReply and updated vsock_server.rs:1365; tray/mod.rs:1383 still
treated fetch_cloud_projects as returning a bare Vec. Green on both branches,
E0063+E0308 in the union. Fixed by PROPAGATING the outcome — substituting
Unknown would have compiled, passed, and restored four-outcomes-one-
representation on Linux while macOS reported it correctly. Also relocated
build_menu's docs back to build_menu (they had drifted ~200 lines above it, so
clippy's empty_line_after_doc_comments was pointing at real misattribution) and
added a reasoned identity_op allow where `base + 0` is a slot-offset table.
Union --ci-full then GREEN, 2017 checks.

DRAINED 896-f8ti (macbook's, claimed and completed). Root cause is sharper than
the packet: append-event did NOT invent a packet. resolve() ends with
resolve_archived() (lib.rs:763) — correct for reads, wrong for a write — so
624-q4jj resolved to the REAL, ARCHIVED, COMPLETED
macos-unstable-channel-installer-validation, and the 699-usxc arm filed a
fragment no live reader folds. resolve()'s own comment names the fix it did not
apply: "callers that must distinguish ask is_archived; status, the answer rows
and the citation path all do." Three read paths asked; the one write path did
not. macbook has since retracted their packet's central claim themselves after
verifying — they had searched index.yaml and index.d but not plan/archive.

FOUR SELF-INFLICTED DEFECTS WHILE BUILDING THAT FIXTURE, all recorded in it:
(1) a hardcoded --ts made every arm pass on the 900s clock guard instead of on
resolution — an arm green because the thing it tests never ran; (2) an order
regex scraped `64-harness` out of the packet_id `forge-harness-icap-proxy`;
(3) arm 4 targeted a BASE-hosted packet, so the fixture wrote 30 lines into
plan/index.yaml on every run and would have done so on every host's every
--check — caught by `git status` after wiring, not by any arm; (4) a hardcoded
target/ path, caught by the gate itself with violation:plan-binary-probe-usage
and the remedy printed.

FILED. 901-jtvi (step composition, not shell pipes — operator diagnosis; 71% of
2679 command fields carry `;`/`&&`, only 2% duplicate, so no DAG engine).
902-5bf9 (Rust+Lua litmus step form) — the investigation found
tillandsias-litmus-rust ALREADY EXISTS with a typed non-shell `rust_queries:`
key and a dispatch seam at run-litmus-test.sh:896, and ONE adopter in 395 files
after three months. yoga and macbook independently raised the same objection
within the hour: the constraint was never availability, so a second
implementation needs to say what it does differently. Adopted as a blocking
exit criterion. 903-8wsa (mechanise the sibling-overlap check).

METHODOLOGY: sibling_heads_up_protocol added at operator direction, plus Start
Of Cycle 2a. Its first worked example was FALSE and macbook refuted it: I wrote
that they "could not see" the Linux call site; `cargo check --features tray`
runs in 6.8s on their Mac and the site was line 10 of their own `head -10`.
Corrected to the structural argument, which needs no exculpation — the union is
a state no single branch's gate observes. yoga then self-audited and found FOUR
shared-surface changes shipped with zero heads-ups, including wiring a new gate
into build.sh:2016; their framing is now in the section: a heads-up is a
falsifiable act and care is not.

STRANDED 0. Boundary intact. Gate green.
