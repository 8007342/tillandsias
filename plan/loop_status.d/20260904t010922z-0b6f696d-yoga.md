## Cycle — yoga 2026-09-04T01:09Z (meta-orchestration, full mode; cron lane)
- New UTC day: daily maintenance body run and stamped. target/ is 43G, over the 40 GiB threshold, so the build-cache sweep is due at finalization.
- 998-qrwu IMPLEMENTED: the CA path is single-sourced, 38 literal occurrences down to 1, and the ratchet tightened 38 -> 1 so it cannot grow back. 998-3z6g can now move the directory with ONE edit.
- THE DECLARATION is images/default/ca-path.txt: Rust reads it with include_str! through tillandsias-core::ca_path (the crate BOTH headless and macos-tray already depend on, which is what lets one declaration serve both), shell through scripts/lib-ca-path.sh. Same pattern as capabilities.txt and enclave-services.txt — the value travels inside the artifact, so a binary cannot disagree with the file it compiled in.
- THE HARD PART WAS NOT THE RUST: 8 of the 38 sites were inside an EMBEDDED SHELL PREAMBLE in crates/tillandsias-macos-tray/src/diagnose.rs, where the path is text in a format! string sent into a guest VM rather than a path value. That is why it escaped single-sourcing before, and why 975-rsgm could not move the directory safely — a sed over Rust paths would never have seen them.
- TWO HIDDEN SITES THAT WOULD HAVE SURVIVED THE MIGRATION SILENTLY, both in tests, both now interpolated. (1) `let key = concat!("/tmp/tillandsias-ca/inter", "mediate.key")` — the literal SPLIT ACROSS TWO STRINGS, invisible to any grep by construction, which is how it survived earlier audits and why my ratchet counted 38 rather than 39; after the move it would have kept matching the OLD path, so the test would have gone quietly vacuous rather than failing. (2) A preamble assertion pinning chmod 600 against a literal — whose own comment records that this exact literal-pin failure had ALREADY happened twice (701-iu9b in vz.rs, then again in this file); my change would have made it a third.
- VERIFIED: macos-tray 44/44, core ca_path 2/2, both crates compile, gate green; the two converted shell checks still answer correctly on this host; ratchet mutation-verified at the new baseline (adding one literal gives violation:ca-path-literals-grew:2 of 1).
- SCOPE NOT CLAIMED, and it is the important caveat: I cannot RUN the macOS lane. macos-tray compiles and tests here only because it is cfg-gated to a stub off Darwin, so 44/44 says the preamble BUILDS with the interpolated path, not that a Mac guest accepts it. A macOS host should confirm before 998-3z6g lands, because that is when the value actually changes. Named in next_action.
- The remaining 1 literal is a prose mention in a cheatsheet — documentation that will read stale after the move rather than break. Named so the next host updates it deliberately instead of discovering it.
