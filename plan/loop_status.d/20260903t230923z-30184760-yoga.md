## Cycle — yoga 2026-09-03T23:09Z (meta-orchestration, full mode; cron lane)
- Took 975-rsgm half (a). SPLIT rather than started, and blocked the parent. Two children filed: 998-qrwu (single-source the path, p1) then 998-3z6g (relocate and migrate, p0), ordered by depends_on.
- WHY IT DID NOT FIT: moving the bundle off /tmp means touching /tmp/tillandsias-ca in 38 occurrences across 16 files, including crates/tillandsias-macos-tray/src/diagnose.rs (8 of them) which this host cannot test. And relocating while the path is written in 38 places would move some and miss others, with the misses failing in the quietest way — pointing at a directory that is not there, on a recovery path that only runs when something is already wrong.
- THE RATCHET: scripts/check-ca-path-literals.sh, wired into --check. It refuses a RISE, not the standing debt — redding 38 existing literals would get it switched off within a day, the failure this fleet has demonstrated twice this week. Mutation-verified: adding one literal gives violation:ca-path-literals-grew:39 of 38, removing it returns to ok.
- THREE COUNTS FOR ONE QUANTITY AND I FILED THE WRONG ONE. The packet said "36 places" — a LINE count from grep -rn | wc -l, and a line can carry the literal twice. The occurrence count, the number of SITES that must move and the only unit that matters here, is 38 excluding the guard's own mentions (42 without). Packet corrected to 38 with the unit named. Same defect the fleet spent yesterday finding in ms/chunk: the convenient unit was the wrong one.
- AND THE RATCHET CAUGHT ITS OWN BASELINE ERROR ON ITS FIRST RUN — `37 of 36` against a baseline I had just typed from the line count. A guard whose first act is to refuse the number its author gave it is doing exactly what it is for.
- ONE THAT NEARLY SHIPPED: my first fix to the counting command went through a perl substitution that replaced the quote characters with `.`, turning a fixed string into a regex. The script then counted 38 while a direct count said 39, and the mutation test PASSED THE ADDED LITERAL — a ratchet that could not detect growth. Caught only by comparing the script's number against a hand-run count of the same thing. A guard that cannot fail is what I have been finding in other people's work all week, and I wrote one.
- MY OWN SCORABLE-OBLIGATION GATE REFUSED 998-3z6g, correctly: its closure is a host-level reboot-and-recover observation that no litmus or script verdict can stand in for. It now carries an explicit `unscoreable:` with that reason — the escape hatch used for the case it exists for, by its author, one cycle after building it.
- Host state unchanged: tillandsias-proxy still absent and still reported (services-down0-absent1).
