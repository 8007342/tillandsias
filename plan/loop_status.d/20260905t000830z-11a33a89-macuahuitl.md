## Cycle — macuahuitl 2026-09-04T23:47Z (coordinate-multihost-work, cron :41 lane; lock session-cron-coordinate)

- **Scheduler posture:** session crons armed (coordinate `:41` hourly, meta `9 1-23/4`); this fire's merge work ran under its own lock once the osx-next hold was lifted; the lock was released at the end of the cycle.
- **Heads at 23:47Z:** main 377ba7794 (v56.9.4.1), linux-next d5a5c4f21 (moved through the hour by lenovinha's revert of its two litmus reds, 1034-ihxw obsoleted and refiled as 1036-e5w9, and the coordinator's plan-lane records), windows-next +0, osx-next +6 (macbookair: the rest of 980-ja2m slice (a), 5fbb750dc, six bullets verified separately against origin by the lander, by macneo and by the coordinator; 1036-jamx anchoring all four scorable-obligation patterns after lenovinha's 1033-ev5r review; the 1035-qf8t closure made an honest unscoreable). **Merged this cycle:** osx-next → c0d4b547d; union litmus (full output, 23:54Z-00:05Z) 290/291 executed, 99%, ONE red: litmus:guest-container-metrics-wire-shape step 2 (expected "ok: WIRE_VERSION still 2 (additive-only)", output empty), yolanda's re-pin, now the only owned red on trunk and dispatched ahead of everything on its queue; lenovinha's two reds gone by revert; 1038-d7vw's fix landed (d731382be) and the freshness-inventory step passes; landed 964a6806b with the tool's gate on the first attempt. The hold on osx-next was lifted only after the tray and scripts/tray-diagnose.sh agreed on origin, so linux-next never carried the disagreement.
- **Claims live:** 980-ja2m (macneo, stays in progress for slice (b)), 1025-a896 (coordinator), 1028-3eiz (yoga), 1038-d7vw (lenovinha, the two-line fix). Stranded 0.
- **Heartbeat by asking:** every host answered within the hour. lenovinha: reverted its two ci-release litmus reds (the `[ -x ]` probe 889-8tcb records as having deadlocked every Windows host), obsoleted 1034-ihxw, filed 1036-e5w9, reviewed 1033-ev5r (NO on the matching, principle kept), profiled the freshness-inventory walk to two quadratic membership tests and now lands the fix; yoga on 1028-3eiz; yolanda: 1032-utne landed, the WIRE_VERSION re-pin still owed (the one remaining owned litmus red); macbookair: 980-ja2m slice (a) complete, 1036-jamx, next 1019-ivia (macOS-only C2); macneo verifies the six bullets and resets its held commit; esmeraldinha timed the walk (25.0 s idle) and runs the 23:55Z clean gate; pirria timed the walk (8.1 s contended) and inverted the tier premise.
- **Packets filed this hour:** 1037-x5mk (set-field --reopen-evidence offers a move the status-loss guard refuses; RULING: completed never returns to ready, obsolete or file anew citing the old), 1038-d7vw (freshness-inventory litmus step 2 over budget; four hosts measured, cause found at two lines, budget stays as a tree-size tripwire, fix to lenovinha, esme re-times after).
- **1038-d7vw LANDED by lenovinha at d731382be, 18.39 s → 0.68 s, budget untouched; 1033-ev5r SETTLED** (both of lenovinha's accepted-prose sentences now refused by the anchored gate, with controls: a litmus pin and an explicit unscoreable still accepted, so the refusal is specific; run hermetically against the gate at 1ab269f08 before the merge, and said so). THE PART TO PROPAGATE: lenovinha's first patch used `declare -A` (0.83 s, output identical) and the gate refused it, blocked:bash4-unguarded (761-g36m): macOS ships bash 3.2 and check-bash-dialect.sh's own header records that the string sets were introduced AS the 3.2-clean replacement for two `declare -A` maps, so the quadratic was the cost of a correct portability fix and the obvious repair would have reopened the bash-4 defect on macbookair and macneo to make one Linux host faster. What landed is one awk pass into a positional 0/1 marker array (awk supplies the hash table 3.2 lacks; indexed arrays are 3.2-clean), 0.68 s, faster than the refused version; both sites refuse on a length mismatch and each was mutated to prove the refusal fires. Two of lenovinha's controls were invalid before they were right, both by the control-fails-open mechanism it wrote the rule about this morning: a baseline copy left in scripts/ shifted the exclusion count, and a mutant run from a temp dir resolved REPO_ROOT to that dir and inventoried an EMPTY tree so the desync guard "passed" by never executing. Pirria closed the cross-host ratio at the construct level (1.33 s on bash 5.3.15 vs 3.23 s on 5.3.9, 2.43x against 2.26x whole-script) and corrected its own ps-%CPU reading to loadavg. STILL OPEN on the packet: esmeraldinha's re-timing under Git Bash, where the awk-plus-process-substitution shape is exactly what tends to differ; a desync refusal there would be a real finding. Not closed until that number lands.
- **FILED AT CLOSE, p1, 1039-b64k: on Windows the gate stamp's writer (the WSL2 builder, Linux semantics) and verifier (MSYS git) compute different digests and cannot agree, ever; the one-shot pass token hid it on every Windows landing today, and a MEMOISED gate mints no token and deadlocks the push** (yolanda, reproduced four times on 3dd0e195f, the WIRE_VERSION re-pin, which is therefore stuck and routed by patch relay to the coordinator). Ruling: the memoiser mints the token whenever it declares the tree fresh; the digest becomes index-sourced (blob hashes and modes, 889-8tcb); lenovinha implements, yolanda verifies. Macbookair's refused-then-passed land was this mechanism.
- **Trunk litmus state:** one owned red remains (guest-container-metrics-wire-shape WIRE_VERSION pin, yolanda) plus the 1038-d7vw budget red on slower hosts until the fix lands; this cycle's union re-measures below.
- **Lessons recorded this hour, in the finders' words:** `./build.sh --check` runs no litmus, so a four-times-green gate landed a change whose guard lives in a litmus (worker skill line landed); a slice is reported bullet by bullet against origin with unlanded bullets named in the same sentence; "pushed, verified on origin at <sha>" or "in flight, will confirm", never the participle alone; a check that could not run must not print the same 0 as one that ran (zsh `:s` history modifier); a control must not be derived from its treatment; a check assembled only from things someone forgot to update inherits the omission.
- **Operator queue, plain asks (unchanged):** (1) security log for 2026-09-04: OAuth token creations with times; actor and reason per destroy row; any revocation email. (2) No `gh auth login` on any host until then. (3) pirria's rustup reinstall. (4) 978-juw4 permission. (5) 996-bcjx identity call. (6) delete the ten quarantined trees on esmeraldinha. Nothing is blocked that a host can unblock; macneo's push was routed around by reproduction.
- **Velocity:** closures this hour: 1022-px54, 1009-gccx, 1032-utne, 1029-5wvd, 980-ja2m slice (a); filings: 1036-e5w9, 1036-jamx, 1037-x5mk, 1038-d7vw; no High-Velocity Alignment Event.
