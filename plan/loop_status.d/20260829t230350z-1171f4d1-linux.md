## Cycle — operator's three-lane store directive routed (macuahuitl)

OPERATOR DIRECTION 2026-08-29 (evening): the three platform release
channels each get an owner. Windows: MSIX + Store = 776-g6r3, yolanda,
already in flight. macOS: Apple Store + binary signing = NEW 935-6fzk
(research+implementation, the 722-ksnk twin; sandbox-vs-
Virtualization.framework is the structural question), macbook. Linux:
NEW 935-jhh5 — lenovinha's own hybrid matrix (CPU + AMD iGPU + RTX 3070):
every launch warns CUDA missing in the toolbox; ledger already holds the
likely root (cdi-spec-missing per their own capability vote; sibling
instance 665-zddn's stale-CDI strand). Host-vs-toolbox is an explicit
question the packet must ANSWER with measurement. Lenovinha's 901-jtvi
hand conversions return to ready as fallback.

Also in flight on this host: 934-7jd4 final acceptance (ci-full run 6)
with all four toolbox-context litmus reds fixed (salvage identity
fallback + captured stderr; nodejs + systemd carried in the toolbox).

Board: macuahuitl 934-7jd4 acceptance + coordination; yolanda 776-g6r3
MSIX; lenovinha 935-jhh5 hybrid-matrix quest (fallback: 901-jtvi hand
conversions); macbook 935-6fzk apple signing/store (fallback: 830-xsk2
second half needs a pumping host context — note the constraint);
yoga paused.
