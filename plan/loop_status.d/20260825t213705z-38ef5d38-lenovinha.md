## Cycle 2026-08-25T21:37:05Z — lenovinha (linux_immutable, linux-next, operator /loop 2h)

Full mode. Checkout lock held (lane operator-loop). Daily maintenance already current.
Scheduler `due:not-installed` (advisory). MCP experts healthy, surface attested `exposed`.

### The cycle opened on my own regression, and a sibling had already caught it

`check-credential-channel.sh` was `Permission denied` at Start-Of-Cycle: LAST cycle's
886-qmdz fix rewrote it through a temp file, and `mv` drops the exec bit. macuahuitl found
it minutes after it landed, repaired the trunk (887-bz88) and filed the mechanism question
separately — correctly refusing to let the repair stand in for the finding. The
fast-forward brought the repair in before I could re-break anything.

### 887-bz88 claimed and closed — the lane is NEITHER candidate

The packet named two candidate holes and said not to guess. As the host that CAUSED it, I
have the sequence rather than a reconstruction, and it is a THIRD mechanism: two blind
spots composed, neither sufficient alone.

1. `mv` dropped the exec bit in the WORKING TREE; the index still said 100755, nothing
   staged.
2. `./build.sh --check` ran. `check-script-exec-bits.sh` reads the INDEX (`git ls-files
   -s`), saw 100755, PASSED. The guard that exists for exactly this defect was looking at
   the wrong view. The gate then stamped the tree.
3. `git add` wrote 100644 — no BYTES changed, so the content-only stamp stayed fresh.
4. The pre-push hook verified a fresh stamp and accepted.

The ordering is structural, not accidental: the skill runs the gate at Finalization step 4
and stages at step 5. Any mode change from an edit is worktree-only when the gate looks and
invisible to the stamp once staged. Every host runs that order every cycle.

Candidate (a), the plan-only fast lane, is RULED OUT — the push carried `scripts/`.
Candidate (b) was half right: the stamp genuinely never hashed `st_mode`, but a stale stamp
alone would not have mattered, because the chmod happened BEFORE the gate ran, not after.

Both closed, because closing either alone leaves the hole reachable by the other route.
`gate-stamp.sh` frames the exec bit (existing stamps go stale once, self-healing; Windows
`core.filemode=false` unaffected). `check-script-exec-bits.sh` condemns on EITHER view, so
the failure lands before staging, and its remedy line now branches — printing
`git update-index --chmod=+x` for a worktree-only case would have told the reader to STAGE
the broken mode.

Also corrected `test-script-exec-bits.sh`: its `executable-bare-ok` scenario set the index
mode with `update-index --chmod` while leaving the file 644 on disk — a split real git never
produces, since checking out a 100755 entry materializes an executable file. It was
asserting on an impossible tree, which is why my change appeared to break it. 14/14 green.

### Verification

E2E on a scratch clone with a local bare remote and the real pre-push hook installed:
PRE-FIX the incident shape was ACCEPTED (push-rc=0 — the hole reproduced e2e, not argued
for); POST-FIX the identical sequence REFUSED. Controls: a content change with a refreshed
stamp still pushes; an ordinary plan-only fragment push still takes the fast lane without a
build stamp, so exit criterion 3 holds.

Pinned by `litmus:mode-only-regression-gate` over a 9-arm fixture including a mutation
control that reconstructs the index-only guard and asserts it PASSES the incident tree.
The unbound-litmus ratchet caught the new litmus file before it could become an assertion
nothing runs — bound in `openspec/litmus-bindings.yaml`. ci-release pre-build litmus 28/28.

### E2E deferred again

`e2e-preflight.sh eligibility` -> `skip:live-runtime-present`. The curl-install gate this
immutable host is routed to is destructive and did not fit alongside the above. Latest
release still v0.4.260817.1 (8 days). Second consecutive deferral — recording it so the
gap is visible rather than implied.

### Metrics

```
experts: calls=13172 answered=13163 unsupported=2 degraded=7 errors=0 answer_rate=99%
mcp: servers=3 per_server=cli=13120;forge-plan=52;project-info=43 health=ok
expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
flow: cycles=24 avg_completed_per_cycle=0.75 avg_commits_per_cycle=3.17 overhead_ratio=4.22
timing: steps=4362 build_check_ms_avg=45916 litmus_ms_avg=20914 slowest=litmus:freshness-inventory-shape:152167
plan: packets=601 ready=372
```
