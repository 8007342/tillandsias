## Cycle 2026-09-14T11:35Z — yoga (linux-next)

Batch selector: epic=harness-mcp-expert-validation, seed=silverblue-unknown-amd+npu-yoga-20260914, size=2. Took 1109-t8kw (claim c5329320d); PART 1 landed at d1a9516cc; claim RELEASED because part 2 needs a host yoga is not.

- FOUR PREDICATES, COUNTED BEFORE READING, REPRODUCED BEFORE EDITING: 708 files scanned, 95 candidates, 3 real, 92 false positives, 3 fixed. (a) tier: 13 candidates, 10 unpinned, 2 real — under TILLANDSIAS_HOST_TIER=low-end the selector correctly refuses (847-wgy4) and two litmus specs read that refusal as a grammar failure and as "urgency inert". Pinned on the 25 EXECUTING command lines only, because ~4 of the 26 mentions are prose. (b) 1 candidate, 0 real. (c) 70 candidates, 10 unset, 0 real — confirms 51c0ad583's sweep closed rather than assuming it. (d) 14 candidates, 1 real.
- THE ONE (d) HIT WAS MINE, FROM THIS MORNING: test-claims-across-branches arm 2b, added at 73e6b2902, used `hostname -s`, which is absent in tillandsias-build — so it would red in the builder while the check under test is correct. This row's exact shape, committed by me on the day I read the row. Now falls back through TILLANDSIAS_WORKSTATION / TILLANDSIAS_HOST_KIND / the platform constant and SKIPS BY NAME if none resolves.
- TWO OF MY OWN INSTRUMENTS NEEDED CORRECTING BEFORE THEY SAID ANYTHING. The (d) scan first reported 12 of 14 because my stripped PATH removed jq, cargo, podman and more — two fixtures returned 127, the harness breaking rather than the property. Shadowing ONLY hostname gives 1. And the (a) triage regex matched mentions and executions identically: a pattern returning EVERYTHING discriminates no better than one returning nothing, so classification is by measured rc under a low-end tier.
- A THIRD INSTRUMENT CAUGHT BY ITS CONTROL: yq is absent here, so my first YAML validation printed INVALID for both edited files. The before-control showed it printed INVALID for the untouched originals too — instrument, not files. tillandsias-plan validate-yaml says ok.
- PART 2 LEFT, WITH THE REQUIREMENT MEASURED: the closure artifact needs an AT-RISK, low-end host. On yoga the probe prints SAFE and the tier is unset — neither half. Producing it here would mean constructing a world I do not live in and calling it a host report, which is the failure this row is about.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
