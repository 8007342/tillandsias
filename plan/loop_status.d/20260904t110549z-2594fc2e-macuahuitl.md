## Cycle — macuahuitl 2026-09-04T11:05Z (coordinate-multihost-work, cron :41)

- **Merged:** osx-next +1 (macbookair's claim of 1004-8vkv, the loop-status-append stdin defect) into linux-next; windows-next level (its +4 tray slice was merged at 10:55Z). One live claim after the merge: 1004-8vkv on tlatoanis-macbook-air.
- **Stranded 0. Heartbeat 6/0/0/3/1:** macneo NEVER ATTESTED (two independent push faults; revised ask on the board; now behind 79 on osx-next and holding, not merging locally, by decision); esme and pirria SILENT by author matching and accounted for; every other host attested within two hours.
- **Recurrence audit, third pass, amended loop:** platform-label stems are reported as unattributed buckets (linux 56, windows 36, macos 15) and never as hosts (1012-hu7d). Named hosts pasting the block in their newest entry: lenovinha (as of the 10:5x fire), yolanda; macuahuitl's newest is this coordination entry, which carries no metrics by design (only meta cycles run the reporter). Six hosts agree on the three skippable steps (1008-85ry).
- **Filed this hour, all from host measurements:** 1013-xm63 (plan-only lane refuses append-only ledger edits; pirria), 1013-qv7c (the smoke emits timing records so the toolchain-less floor feeds the recurrence rung; pirria), 1014-xf2u (diff-scoped gate checks accuse the change when the base is stale; yolanda), 1015-mq6d (the plan-only lane refuses a legitimate attestation append, red on its ACCEPT arm since b300d6b92; macbookair; unowned, the coordinator takes it in its next meta cycle if still ready). Root cause of the windows-tray red bin test read from the sources: a stale source pin after yolanda's own b22d09fd8 moved the credential names into a helper; test-side fix on 803-49re, yolanda's lane; the union pass runs `cargo test -p tillandsias-windows-tray --bins` each hour until green.
- **Coordinated window green-lit:** yolanda's Windows tray half landed (7bf738dbe; the tray no longer creates %USERPROFILE%\src on start); the wire types are consumed by macos-tray and host-shell and die LAST; sequencing is on 997-e4v2; macbookair takes the macOS half at its 12:15 fire after yolanda's heads-up; the union pass runs that hour.
- **Idle hosts: none.** yoga: 1011-d578 (its own routing regression); lenovinha: 1011-zp59 claimed at :05; macbookair: 1004-8vkv then the window; yolanda: the 803-49re pin fix, then the window; esme: 1004-pg9p unleased at :55; pirria: read-only until the toolchain ask; macneo: holding.
- **Release:** not this hour (1006-jfwv instant-tier red on the macOS lane; 1011-d578 quick-tier red, yoga's; the windows-tray bin red, yolanda's). No PR in flight; latest v56.9.2.1. The 05:09 local meta cycle records the decision.
- **Operator queue, plain asks, unchanged:** ESMERALDINHA `gh auth login`; macneo `gh auth refresh -h github.com` AND `gh auth setup-git` (or an SSH remote); pirria rustup; 978-juw4 permission; 996-bcjx identity call; the ten quarantined trees. Nothing is blocked that a host can unblock.
- **Scheduler:** `due:not-installed`; session crons armed (coordinate :41 hourly, meta 9 1-23/4).
