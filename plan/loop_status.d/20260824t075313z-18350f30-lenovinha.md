## Cycle — lenovinha (linux_immutable) 2026-08-24T07:53:13Z — a closure rung could be dropped, permanently, by one fragment

Full-mode cycle. DAILY GATE WAS DUE (first roll-over this session, local date 2026-08-24) and I ran the body: post-restart skipped (up 1d9h, not freshly booted); cargo-gc DEFERRED (target 16G, under the 40 GiB trigger, and build_cache_hygiene puts the sweep at END of cycle anyway); nix steps skipped-absent (no nix on this host); podman prune noop; delegate sweep no-registered-runs; expert containers builder+vault+inference+dev-inference up and healthy (proxy still down, 767-es4w); MCP source_commit matches HEAD exactly; capability row reported. Stamped with all nine steps named.

CLAIMED 686-7qcm and met exit criterion 1 — and the packet's own premise was wrong in a way worth recording. Its next_action said "route compact_text through the same rank-aware rule as the live fold", presuming the fold was correct. IT WAS NOT. I found that only because I wrote the packet's regression BEFORE touching code, and watched 2 of 3 fail.

THE DEFECT, in both paths: status_entry_wins was consulted only when a previous FRAGMENT entry existed. The first fragment to claim a field hit `None => true` and won unconditionally. But None does not mean there is no incumbent — the BASE carries a value, and for status it may be a closure rung. So a lone fragment writing `implemented` over a base `completed`, with no falsified event, won on both paths. No race needed: one host, one fragment.

COMPACTION MADE IT PERMANENT. Reproduced on a synthetic ledger: base completed, one downgrade fragment, `compact` wrote `status: implemented` into the base AND deleted the fragment. Nothing survives to re-derive the rung. compact_text also did not consult the rank rule at all, while its comment claimed the wins were "recomputed exactly as fold resolves them" — true when written, false since 650-dq6u made the fold rank-aware, and invisible because the two paths agree on every case EXCEPT a rung-lowering write.

FIX: both paths now compare an incoming status entry against the BASE value through status_entry_wins. Incumbent (ts, host) passed empty on purpose — the base has no clock, and the ladder does not need one: higher rung wins outright, lower needs a falsified event regardless of timestamp, equal rungs fall through to LWW where a real timestamp beats the empty incumbent, which is what keeps same-rung and working-state writes behaving as before.

VERIFIED across four transition classes on the real binary and live ledger: ready->in_progress still applies (this cycle's own claim folds correctly and is hidden from `ready`); ready->completed still applies (868-p8xi folds completed); rung->lower without falsified now REFUSED on both paths; rung->lower WITH a same-fragment falsified event still permitted — the positive control, without which a fix that froze all status writes would pass the other two. Tests 3/3 (written first, observed failing 2/3), suite 220+25, live ledger unchanged at ok: 562 packets.

The losing fragment is still consumed and deleted, and that is correct: fragment_coverage_gaps documents a status write that loses LWW as designed semantics, not data loss. Only WHICH write wins changed.

DELIBERATELY NOT ASSESSED: criteria 2-4. check does emit parked-block: lines and closure-evidence-check exists in the pre-push lane, so parts LOOK present — but "looks present" is exactly how this packet came to assert the fold was already correct, and I am not repeating that on three criteria I have not exercised.

FRESHNESS: openspec/litmus-tests/litmus-runtime-diagnostics-typed-events-shape.yaml, verdict=refreshed, EXECUTED (5/5, spec runtime-diagnostics-stream). Meaningful for the reason its header gives — the four typed formatters it pins are staged behind #[allow(dead_code)] awaiting phase-2 wiring, so nothing else exercises them. Note in the stamp: its value has an EXPIRY. When the live wiring lands it becomes the weaker of two guards; re-audit then rather than re-stamping forward on a green, and apply the discard-over-repair bias.

ADVISORY: compaction not eligible (12 fragments). Deslop not due (order=869 delta=35).
