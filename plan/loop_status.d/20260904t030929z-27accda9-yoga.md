## Cycle — yoga 2026-09-04T03:09Z (meta-orchestration, full mode; cron lane)
- Took 998-3z6g (p0, urgent, batch head). RELEASED to ready: the migration machinery landed, the VALUE did not change, and the remaining work turned out to be a decision rather than an edit.
- MIGRATION DETECTOR LANDED: scripts/check-ca-bind-migration.sh. Podman records a bind SOURCE in the stored definition at CREATION, so changing ca-path.txt makes new containers correct and leaves existing ones binding a path that will not be there — 975-rsgm's own failure reintroduced by its fix. It checks EVERY container, not just the proxy (remote_projects.rs mounts the same bundle into git-login containers, and a migration fixing only the proxy would leave those failing on a path nobody looks at). It REPORTS and never acts: recreating the enclave's only egress path is a mutation with a blast radius.
- VERIFIED AGAINST A REAL STALE BIND rather than a clean host. This host has no CA-binding container at all (proxy absent), so `ok:0` proved nothing — I created a throwaway container binding a stale path, got migrate:ca-bind-stale:1 naming it, removed it, and confirmed the host returned to its prior state. A detector that has only seen the healthy case is the shape I have spent the week catching.
- THE DECISION, NOW EVIDENCED FROM BOTH SIDES AND RULING OUT A STATIC LITERAL. Guest (macbook-macos, real Mac VM): ca_dir()=/tmp/tillandsias-ca -> key present, perms 600; ca_dir()=/var/lib/tillandsias-ca -> key present, perms 600, owner root, survives_reboot_dir=yes. So the interpolation is robust to the MOVE, not merely correct today. Host (yoga, measured): uid 1000 rootless, `mkdir -p /var/lib/tillandsias-ca` -> Permission non accordée; writable and durable here are $HOME/.local/state and $HOME/.local/share. /var/lib works in the guest BECAUSE it runs as root and fails on a rootless host for the same reason.
- RECOMMENDATION offered with the evidence rather than as a decision I am entitled to make alone: a HOME-relative path is the only candidate measured to satisfy both, and it requires ca_path::ca_dir() to become a DERIVATION rather than the static string the manifest holds — a deliberate change to the contract 998-qrwu established one cycle ago.
- ALSO CONFIRMED BY macbook: ca_dir() correctly skips the manifest's 22 comment lines. Had it naively included the file, the preamble would still have COMPILED and produced garbage shell — the failure shape I was right to be suspicious of. And no CA path literals remain in their crate; the surviving concat! uses are the legitimate include_str! source-window idiom, 115 crate tests pass, ratchet reads 1 of 1.
- macbook DECLINED to own the Darwin half separately and I agree: eight interpolations in one function, verified against two values, and splitting would put a lane boundary through a change whose whole point is that the path is declared once.
- TWO CAVEATS PRESERVED so nobody over-reads the evidence: they did NOT run the whole preamble (it ends with `exec tillandsias-headless`, which would have taken over the session doing the measuring) — the CA block only. And their first harness attempt flattened the block with `tr '\n' ' '`, eating the newlines that terminate `fi`, which the guest correctly refused; that was their harness, not this change, and a bare "syntax error from the guest" in a transcript would read as a finding about the preamble.
- Build-cache sweep ran last cycle after the lock released: cargo clean reclaimed 48.5 GiB (43G -> 0), stamped.
