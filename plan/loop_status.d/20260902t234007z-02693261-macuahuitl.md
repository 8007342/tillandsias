## Cycle — macuahuitl 2026-09-02T23:40Z (coordinate-multihost-work, cron 23-even; run under an OPERATOR DISK PAUSE)
- Direction: forge-local experts (391) primary. Lock coordinate-cron. HOST CONTEXT: the operator reports desktop freezes; measured as btrfs 927/951 GiB ALLOCATED, ~129 GiB stranded in allocated chunks, 22,858 extents queued for async discard, device 96% busy at 8.1 MB/s during stalls with NO process writing >20 KB/s. Fleet builds paused on THIS machine only. Disk was 0.00% at start; this pass's three gates took it to 1.18% peak and back to 0.02%. Every other host is on its own hardware and was dispatched normally — the pause is host-scoped, not fleet-scoped, and conflating the two would have idled six working hosts for one machine's disk.
- MERGED: windows-next (9 ahead — 965-sxec, 793-zumy producer fix, 803-49re both purge paths, 964-9yyp criterion 2 closed WITH its regime) and osx-next (5 ahead — 702-6jza D1 verified visually and D2 landed). Drift now 0/0.
- THE MERGE FOUND A THIRD DUPLICATE-IMPLEMENTATION COLLISION, and this one was subtle. 965-sxec was implemented INDEPENDENTLY by yoga (gate inside _ruby via _require_ruby, exits 3, dispatches on a recorded lane so probe and call site cannot disagree) and by yolanda (a probe plus a TOKENISED early exit, so build.sh can tell this exit 3 from a stale plan binary or an unreadable fragment). Both correct. COMPOSED THEY FAILED: yolanda's probe worked by calling _ruby, and yoga's _ruby now exits the script from inside it — so the probe could never return false and the token was unreachable. Observable: exit 3 with no token, which means the forge skip never keys. Each branch was honestly green (yoga 5/5, yolanda 4/4); only the union fails, and only the gate saw it. FIXED with one probe (_ruby_runnable delegates to _ruby_usable, the lane-recording one _ruby itself dispatches on) and ONE copy of the refusal text (token on stdout for the machine, canonical refusal on stderr for the reader — the two tests wanted different streams, which is why the merged tree briefly carried two refusals naming the instrument differently). Both suites pass.
- CLAIMING DID NOT PREVENT IT: 965-sxec carries two claim commits, eb6a9b5c0 and 3c4c88a86, pushed to DIFFERENT branches, so neither host could see the other's. That is the third collision today (944-vim8 READ_KEYS, the core.hooksPath fixture, this) and all three were invisible to their authors. NOTED, NOT PROMOTED — a cross-branch claim visibility gap is a pattern promotion and those are Tlatoāni-gated.
- ALSO FIXED THIS CYCLE, mine: I broke litmus:long-running-view-agreement step 6 in 2d3405f99 and shipped it. `query --json` returns the live fold only; `status` also resolves ARCHIVED rows, so my map-only join kept archived 831-ezea and made the gate demand a closed packet stay in the active view — the exact 943-26rx defect the overlay exists to fix. My gate stayed green through four runs because --check runs no litmus (748-tkjx) AND zero of the 28 current view orders are archived, so the live tree structurally could not expose it. Fixed at 13fb887bc, both fixture controls pass, live answer unchanged at 28 packets and 251ms from 5415ms.
- THE LESSON, yoga's ranking adopted over my own: the copied reader is a tenth instance of a known class; the NEW failure mode is that my verification was vacuous and I did not notice. I compared batched against fallback on the live tree, found byte-identical output, and called it semantics-preserving. The comparison was TRUE and proved nothing, and its truth is what hid it. Family, three members tonight: yoga's ms/chunk carrying chunk size, my capability row carrying lane-exists in place of engine-places, my diff carrying no-archived-rows-here. Detector: before trusting a control, ask which case would make it FAIL and whether that case is present.
- DISPATCHED (all off this machine): pirria-tillandsias-forge — 965-sxec criterion 2 (it was the host the ruby knot stranded, so it is the observation yoga could only stub) plus the reflink question 964-js34 left open for the N150 regime. lenovinha — 964-r98h (its own filing; two of three GPU cases in one machine) or 968-uhzg, its choice, no double-assign. yoga and yolanda hold their own crons and closed cycles this hour. macbook active on 702-6jza. HELD: macuahuitl-tillandsias-forge, paused for the disk, with three items queued in order (compaction-linearity experiment, the second cp -a plan/, then 964-tzmp's architecture).
- 964-tzmp REDIRECTED by the forge's decomposition: of the ~200ms load constant, ~2ms is startup, ~47ms is the base parse and ~150ms is FOLDING FRAGMENTS. My framing aimed at the smaller quarter. Favours a pre-folded snapshot over a resident server, and the real objection is that the gate WRITES to the ledger during itself (3 append-event, 1 set-field, 1 compact in one run), so a stale snapshot makes a BROKEN tree PASS — criterion 3's mutation control now must run stale as well as fresh.
- Operator items unchanged: esmeraldinha's mirror 403 needs a credential refresh; yoga's enclave proxy failed to restart; two 100-day openspec changes await promote-or-archive; and the host's btrfs balance is operator-gated.
