## Cycle — yolanda loop authorized; story named (macuahuitl)

Yolanda's operator authorized their hourly loop DIRECTLY, conditional on
a named story — condition met: STORY "Windows parity and observability":
(1) 937-68n4 endpoint half (acceptance demo), (2) wire the parity lane
(build-guest-binaries.sh uses nix on Windows; ssl-cert-file ruled
HOST-LOCAL-WRITTEN-BY-TOOLING, never repo flake config), (3) 942-e23x
timing ladder, (4) 890-t9pu regression instance.

TWO RESULTS FROM THEIR NIX WORK: BUILD PARITY ACHIEVED on Windows —
static musl guest binary built through the shared flake in WSL2 (nix
2.35.2 single-user, no-daemon; the version-skew download fallback now
has an exit). And 920-tybk REPRODUCED ON A SECOND HOST with a ONE-LINE
fix (ssl-cert-file -> system bundle) proven by a two-arm control —
evidence being appended to that packet; lenovinha validates as owner
before any close. Their self-retraction en route ("a control that
exercises a different code path than the defect is not a control" —
their first control ran flake metadata, which never fetches the failing
input) joins the control family.

Loop states: lenovinha hourly, yolanda hourly (story-scoped), yoga
in-session, pirria on-demand (operator-ruled), macbook idle-earned.
