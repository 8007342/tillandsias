## Cycle — macneo advance-work-from-plan (cron 5a838dc0, :40 lane)

Ledger-only, relayed to trunk (e2afbd580, 2 fragments; then the starvation note).
Branch NOT advanced — see the arithmetic below. Content is durable regardless.

Freeze checked first WITH a control: 0 freeze refs against 552 total, so the
absence is real rather than an unqueryable namespace.

TWO ROWS WORKED, NEITHER CLAIMED, both needing a podman-present host:

800-ivkg — the fixture runs `podman run --rm`; this host has none, so the 30s
budget is not the binding constraint and no timing from here speaks to it.
Measured anyway: 20ms, rc=1, `podman: command not found`. It FAILS FAST rather
than skipping by name, so on any podman-less host it reds for ABSENCE in the
same colour as the load-sensitive timeout the row is about — and the two want
opposite responses. 1187-iij8's shape in a third file.

798-rvqb — four live sites confirmed (build-forge.sh 1, run-forge-standalone.sh
1, scripts/run-safe-browser.sh 2). Two counting corrections for the next reader:
build.sh still MATCHES a bare grep because its occurrence is a COMMENT
describing the removed behaviour, so 797-r6tc is complete and grep -c gives a
false positive on the file that proves the fix; and the two root launchers are
at the repo root, not scripts/ — my error, the title is right. Both mistakes
were mine and both were caught by controls, not by care. Undocumented extra:
run-safe-browser.sh HARDCODES uid 1000 rather than deriving from
XDG_RUNTIME_DIR, so its guard silently never fires for another uid and the path
does not exist on macOS at all.

THE LAND REFUSED AND I DID NOT RE-SPEND THE GATE. pre-push refused with
"osx-next does not contain origin/linux-next" — trunk moved during the gate.
The script's own text says a retry is worth it ONLY if the gate is shorter than
trunk's inter-commit interval, and tells the caller to measure. Measured:
origin/linux-next ran 19 commits in the last hour, ~3 min mean, against a ~17
min macOS gate. Trunk moves five or six times per gate; the race is unwinnable
by arithmetic rather than by luck. Recorded on 1142-85zx.

A CORRECTION TO THAT INSTRUCTION: it says to measure origin/osx-next, but the
ref that refused me is origin/LINUX-NEXT. osx-next was quiet this hour — 0
commits — so following it literally would have said "spend the gate" while the
ref that actually moved ran at 3 minutes. Measure the ref named in the refusal.

macneo can SPEAK but cannot LAND: the plan-only lane still relays. Not a wedge.

Keychain: no wedge. Probe rc 0, no orphans, no SecurityAgent.
Sub-agents: 0 of 4 budgeted.
tokens: ~95k this cycle, single agent, no fan-out, no Workflow.
