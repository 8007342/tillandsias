## Cycle 2026-09-12T01:33Z addendum 3 — the two trunk fixes the platform lanes were blocked on are landed; fleet lands in assigned order

The guard-auditor fix (7b9b58e55: search the canonical `skills/` tree before the
symlink farms, which BSD grep and drvfs do not descend) and the ripgrep path
operand (d15aaf3d4: `scripts/check-cheatsheet-refs.sh` read stdin with no path
and hung the gate forever under an inherited pipe; FIFO control pre-fix rc 124,
post-fix returns) are on origin/linux-next at d15aaf3d4. The land had been
refused three times as attempts-exhausted (origin moved every ~2.5 min under an
~8 min gate); under a ten-minute fleet-wide push quiet it passed on attempt 1.
All seven hosts acknowledged the quiet with verified state before it lifted.
Landing order after the all-clear, one host per platform branch at a time,
each messaging its SHA to the next: linux-next lenovinha (1124-7f3u) → pirria
(1096-p3tn) → yoga (1026-ps4n retitle, plan-only); osx-next macbookair (report,
find -L auditor, 1087 evidence) → macneo (1123-99jc correction, 1127-xm3m fix);
windows-next yolanda (1129-3yv7 and queue) → esme (one land as the positive
control for the cheatsheet step, GATE_PROFILE=1, no stdin redirection). Then
macbookair writes the keying fix (1084-x8ya) on its landed tree and yolanda the
three-site hvsocket.rs conversion; the next daily cut waits on both, and the
stable promotion waits on that cut provisioning to Ready on macOS and Windows.
Stable remains v56.9.2.1. Hazards recorded in
`plan/issues/fleet-restart-2026-09-12.md`: the stdin-shape regime gap and the
blast-radius rule, the push quiet as an instrument, and the harness reaping its
own background waiters during a gate.
