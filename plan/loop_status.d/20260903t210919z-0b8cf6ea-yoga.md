## Cycle — yoga 2026-09-03T21:09Z (meta-orchestration, full mode; cron lane)
- Batch epic=stable-milestone-v1, zero live claims fleet-wide. Took 975-rsgm (p0, batch head) for half (b). RELEASED to ready, not closed — half (a) is untouched.
- HALF (b) FIXED: the self-heal was the SECOND FAILURE'S AUTHOR. check-enclave-service-health.sh:319 used a bare `podman start`, which bypasses ensure_proxy_ca_key_secret — the refresh that runs on the LAUNCH path only. So after any CA regeneration the heal reported `fix:enclave-service-restarted:...:action=started` and squid was dead seconds later with X509_check_private_key(). The heal was reporting SUCCESS for an action that could not work. It now refreshes the secret from the current bundle first (proxy only, and only when the pair actually disagrees), and if the rotation cannot happen it emits `fail:enclave-ca-desync-unrepaired:...:action=operator` and does NOT start. A start that reports `started` and dies is worse than a refusal that names the reason.
- THE PRINTED REMEDY WAS ALSO WRONG AND IS REWRITTEN. It offered the harmful action FIRST — "then 'podman start <service>' or re-run the enclave orchestration". It now names the orchestration and says why a bare start is not equivalent.
- PINNED AND MUTATION-VERIFIED: scenario 20, 20/20. Removing the `continue` so the refusal falls through makes it FAIL with "a desynced proxy was reported as restarted anyway".
- FOUR PRECONDITIONS THE SCENARIO HAD TO SATISFY, each of which silently made the assertion vacuous until met: dead-of-signal (a clean rc=0 stop is a `note` the ladder deliberately never fights), aged past ACT_GRACE_S=1800, at least one OTHER service UP (whole-stack-down refuses to act), and a fresh state dir (a previous scenario's operator hold marker persists). My first four attempts each passed the wrong thing for a different one of those reasons — a test can be wrong in as many ways as the system has guards.
- AND MY FIRST MUTATION LANDED ON THE WRONG LINE — it replaced the string terminator rather than the `continue`, the suite stayed green, and I nearly recorded "mutation-tested" on the strength of it. A mutation that does not change behaviour proves exactly as much as no mutation. Caught by printing the line before and after the edit.
- A TESTABILITY SEAM ADDED DELIBERATELY: TILLANDSIAS_CA_CONSISTENCY_CHECK overrides the checker path. Without it the guard resolves the real checker beside itself, which on a healthy host answers `ok`, so the desync branch was unreachable from a test and the scenario passed while exercising nothing.
- STILL OPEN: half (a), the bundle in /tmp. Criterion 2 (a rebooted host reaches a running proxy without hand-repair) remains UNMET — this cycle removed the amplifier, not the cause. next_action carries the migration hazard: the bind SOURCE is recorded per container at creation, so relocating the directory means existing proxy containers must be RECREATED, not restarted.
- Host state unchanged: tillandsias-proxy still absent, still reported (services-down0-absent1) rather than silent. Restoring it needs the installed product or a vault+tray build.
