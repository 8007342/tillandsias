## Cycle 2026-08-20T04:31:32Z (macos — meta-orchestration full)

### WORK: 795-zshi SPLIT, NOT IMPLEMENTED
Claimed the batch head (`architecture-audit-epic`, p1) at 04:33Z, shaped it,
released it back to `ready` the same cycle. Exit criterion 2 requires all five
argv-flattening workarounds deleted in ONE change, and three live in
`windows-tray` and `wsl.rs`, which do not compile here. A macOS cycle could
delete only the macOS one — producing exactly the two-code-paths state the
parent's own `notes:` forbids. 828-r2ek dispositioned `hvsocket.rs` the same
way on the same grounds.

**The parent's `context:` is now stale**, which is the finding worth carrying.
It states "There is no arm that accepts a plain argv vector for the bare-VM
case." Slice 1 landed: `exec_allowlist.rs` has the verbatim arm with 7 tests,
declared unconditionally so the decision is testable on every host. The guest
half of criterion 1 is DONE; the remaining work is entirely host-side, because
nobody emits the vector yet.

Two design unknowns resolved so the implementer doesn't pay for them again:
`/usr/local/bin/tillandsias-headless` is **already admissible** under the
verbatim arm (no allowlist change needed), and the login shell is **not**
load-bearing for PATH (`pty_handler` seeds `DEFAULT_CHILD_PATH`; an absolute
argv[0] needs no lookup). Only the non-PATH login env remains guest-verifiable.

One exit criterion is probably **wrong** and was filed as its own decision
packet rather than silently ignored: criterion 2 demands
`build_exec_guest_shell_cmd` be deleted, but `--exec-guest 'a | b'` is a shell
PIPELINE and a verbatim vector cannot express one — that is what makes the
vector form safe. It differs in kind from the other four.

Slices: **837-m47d** (`launch_spec` emits the vector — shared by both trays,
p1), **837-svzd** (windows deletions, blocked on it), **837-et6t** (the named
litmus that has never existed, carrying a 634-39ik caution on its source-scan
leg), **838-48ca** (the `--exec-guest` disposition).

### I CORRECTED MY OWN PACKET FROM LAST CYCLE
**839-veey** corrects **835-kjuv**, filed by this host 7 hours earlier. That
packet claimed 1050s of a 1072s gate was a cold debug build via `cargo run`.
Measured this cycle, same tree, uncontended:

| step | contended | alone |
|---|---:|---:|
| `check-plan-binary-probe-usage.sh` | 918.0s | **2.963s** |
| `pre-commit-openspec.sh` | 920.2s (15m20s) | **4.178s** |

The second was 15m20s wall for **3.06s of CPU — 0% utilization**: waiting, not
computing. Decisively, the gate's slowest phase **moved** — this cycle it was a
bash script that invokes no cargo and cannot have paid for a cold build. The
1050s was never established as a cold build; it was inferred from a gate I had
**backgrounded while running greps, ledger queries and test suites alongside
it**. I manufactured the number I then filed a packet about.

What survives 835-kjuv: the gate does invoke `cargo run` where a probe-resolved
prebuilt binary would do, inconsistent with 721-nyev's rule that this same gate
enforces three steps later. What does not: the 98%-of-the-gate framing.

Third instance of one shape (783-xyk5, 785-ibu9, now this): 785-ibu9 added
span-vs-work attribution to stop exactly this, but **both** are wall clock, so
neither distinguishes work from waiting-for-someone-else. The unattributed-gap
suffix correctly stayed silent — there was no gap, the step really was blocked.
The instrument was honest and still produced a false conclusion, twice.

**Operational note worth more than the tooling fix:** the gate is not a
background task on a 2-core laptop. Either wait for it, or treat every timing
it prints as unusable.

### VERIFICATION
- `./build.sh --check` exit 0 (gate stamp recorded).
- `tillandsias-plan check` ok. 795-zshi confirmed back at `ready`.
- Packet count 1143 → 599 across the merge: verified as `178d1a068`, a
  Tlatoāni-authorised archival sweep on linux-next, **not** loss.

### COORDINATION
Merged `origin/linux-next` (a014e4e06) before pushing. Conflict-free. The merge
brought non-plan files, so the plan-only push lane did not apply and the claim
could not be pushed ahead of the work without paying a second ~16-minute gate;
claim and split were pushed together at 842e3bbf7. Noted as a real cost of the
gate/claim interaction, not hidden.

### MCP
`project-info` still down (`health=down:project-info`). Every read went through
`./target/release/tillandsias-plan`; surface attested `unexposed`.
