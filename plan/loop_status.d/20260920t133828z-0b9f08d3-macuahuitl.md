## Cycle 2026-09-20T11:30Z→2026-09-20T13:30Z (linux — macuahuitl, coordinator: every host routed; the installer reset settled as one contract with a per-platform body; five rows filed from the fleet's own instrument findings; three relay gates refused at their last lines by one-second checks, and the lane changed for it)

Direction ("We're giving forge agents local EXPERTS"): served by keeping trunk integrating through five lands and eight levels, and by five rows that make instruments say what they measured and where (1302-7j8p, 1303-2d5g, 1304-wbb2, 1305-udgs, 1306-ifhv).

DONE, verified on origin: 1268-m2ir, 1282-rkkm, 1300-q7eq closed (previous cycle's tail). LAND 24 b843b238f (pirria's 1190-swen cold-criterion fix + the README v56.9.19.2 row with the three stable smokes and both known reds resolved). LAND 26c 5928dbad2 (esme's 1258-8wfb: install-windows.ps1 regains its post-install --diagnose health check of the install bits; exit 1 fails, 0/2/3 proceed with the verdict printed; the litmus on assert_exit: 0 with every literal load-bearing, negative control run at the step level). yoga: 1273-4mak b740fb1f2, the 416 digest fix 54613b0f2 (all four sha256sum sites; verified by macbookair on both Mac paths incl. the never-exercised shasum candidate), the 416 resolved-tool line 960b3c4d0, the 411-416 command list on 1302-7j8p. macneo's 1286-4437 macOS survey relayed (61af551fa). Levels: every pass, all three branches equal at the end of each.

RULINGS (coordinator), each with its reason on the row: 1286-4437 is ONE CONTRACT WITH A PER-PLATFORM BODY, not one code path — the name --reset-state (--reset-guest kept as an alias where it exists), local state destroyed and the installation identity preserved (Windows: tillandsias-vm-uuid, which anchors the in-VM Vault's master key; "reset-install" would name the forbidden thing, yolanda), the announcement naming PRESERVED beside DESTROYED before anything is destroyed, the TILLANDSIAS_DESTRUCTIVE_RESET_OK guard as the ONLY skip (pirria withdrew the new variable the three arms and I had agreed; it forked an existing opt-out), exit 1 on any failure; the guard function and the skipped-case line live in tillandsias-core as one function and one constant that all three crates already depend on and import (pirria; corrects my "one host's text wins" ruling of the same quarter hour). Bodies: Linux new in headless (pirria), Windows a rename + guard + announcement + cache purge on the tray whose --reset-guest already had the semantics (yolanda; BUILT and its exit criterion RUN on their host under the operator's standing consent, 6768f9fa6: a broken distro, --reset-state exit 0 in 68 s cold, diagnose Ready after, both guest credentials absent and tillandsias-vm-uuid present, the builder distro untouched; the run forced one fix, the installer probes the tray's --help for the flag by CONTENT and skips loudly on a pinned older tag; the download step itself is not claimed; a staged-build finding for its own row: scripts/build-guest-binaries.sh cannot run on a Windows host, Nix and strings absent, aarch64 lacks its musl gcc), macOS a delta over reset_guest_main where the promised credential clearing has NO production caller (macneo: the tray removes six file paths and no keychain item while announcing it discards cached credentials). No litmus may assert "--reset-state does strictly more than --reset-guest": true on Linux, false on Windows. Installer order on Windows: --version, esme's health check of the bits (aborts before any destruction), --reset-state, the arm's own final diagnose. 1288-5qpn: the authenticated ssh lane (749-54pv) on lenovinha only; the loopback publish of the anonymous mirror daemon is dead by lenovinha's own retraction (a confused deputy with the Vault credential; Vault's publish authenticates, the mirror's does not). 1132-r4mt released by macneo (fifth regime green, no refusing log anywhere). 830-xsk2: the shell script that ran python3 in the guest is retired with its Rust port in headless (the guest has no perl or jq).

FILED: 1301-ie39 (macbookair: the no-python policy matches spellings, not the runtime). 1302-7j8p (two gate steps wired from one regime reddened both Macs in one day: 414 demanded a line a toolbox-less host cannot print; 416's stub could not reach its digest tool under a pinned PATH and its substitution arm passed vacuously; rule: a recorded second-regime run naming WHICH TOOL ANSWERED before a step enters --check; necessary and not sufficient — a stub-only fix would have gone green on macbookair's /sbin sha256sum and still reddened a stock Mac). 1303-2d5g and 1304-wbb2 (esme, from yolanda's findings, reproduced on a throwaway corpus: the runner's --parse-only never loads the document and says ok for a file that is not YAML; nothing compares a litmus file's spec field with the block it is bound under — esme's audit of the real corpus under the CORRECTED rule (membership, at least once; my "equals, exactly once" would have reddened 14 multi-spec files and 42 multi-bound tests): 69 misbound across 42 files incl. a platform-swap pair on the tray specs, 16 unbound files never run by an unfiltered gate incl. ca-ephemeral and both tray-menu-renders, one misbound entry mine from land 21). 1305-udgs, unclaimed (three sub-second deciders — no-Python, the if-not pipeline guard, enclave membership — run after the compile phase instead of in the fast-refusals tier that exists and asks for them; today's three relay refusals at lines 5813, 5345 and 6929 of ~6900-line logs). 1306-ifhv, unclaimed (yoga: test-salvage-audit.sh reds intermittently on its disclaimer arm while the same capture's last line parses; my budget-kill hypothesis REFUTED by yoga structurally and by timing and kept on the row as the eliminated candidate; next lead: blob bytes in the capture vs the grep's classification). Plus 1299-s2sv from the previous cycle (yoga now).

CORRECTIONS OF MINE THIS CYCLE, each recorded where it happened: the 1258-8wfb closure clause "a non-ready diagnose fails the install" would have failed every fresh Windows install (esme read the exit contract: 2 = distro not registered, every first install; only 1 ever threw); the 1304-wbb2 closure rule (above); "nproc absent on stock macOS" (present via Homebrew on both Macs); "yolanda's string wins" (import the constant); the salvage-audit budget-kill hypothesis; two relay-script guards of my own that refused CLEAN refs (an ancestry test on salvage refs, which are snapshots; a grep reading a comment mentioning python3 as a command); two claims relayed to esme after their authors had retracted them. THE RELAY LANE CHANGED: before any relay land this host now runs the gate's own one-second deciders on the merged tree (no-Python, if-not pipeline, sigpipe pipelines, litmus YAML parse, added fragments, enclave membership, cargo fmt --check) and refuses to spend the gate on a red; a refused ref is dropped and the rest relaunched at once (three lands lost ~60 min of gate to it before the rule).

FLEET STATE: macuahuitl: LAND 27 (yolanda's 1276-2hc6 litmus v3 in awk with the case idiom; macbookair's Rust seccomp port v3 with the enclave-membership bullet; yolanda's Windows reset arm v2) launched 13:18:58Z ahead of pirria's core half after a nine-check preflight, ok:land:319c1e7e0:attempt-1 (all three verified by content on origin); then the plan-only fold of yolanda's rows ref (5c8fe05b1) and one plan push (08b0cf379). ALSO FILED after the draft: 1307-kic6 (yolanda: a push to linux-next from their host never returns AFTER the pre-push hook has printed its verdict, on both the accept and refuse sides; the transport exonerated by a two-arm probe, --no-verify 2 s vs the hook path killed at 150 s; two events they reported as unlanded were on trunk all along and only the lane's byte-for-byte drop kept the retries from double-committing; closure: the push returns; operating rule on that host: the remote is the verdict surface), 1308-9ej7 (yolanda: scripts/build-guest-binaries.sh cannot run on a Windows host, Nix and strings absent, aarch64 lacks its musl gcc; a staged binary lacking listen-vsock must be refused at staging time, not 524 s into a provision), 1307-ermc (the 795-imz3 guard catches `if ! pipeline` and not `pipeline && ok || bad`, the spelling the salvage-audit fixture uses throughout; from 1306-ifhv's CONFIRMED mechanism: `printf "$out" | grep -q` under pipefail, SIGPIPE 141 with the match present, reachable only when the pipe is smaller than the 22 KB capture, which pipe-user-pages-soft exhaustion under gate load causes; yoga proved it in isolation with a size threshold, 23 KB clean / 66 KB SIGPIPE; five instruments corrected on that row incl. my own discriminator, PIPESTATUS must be captured before any assignment). Unclaimed: 1305-udgs, 1306-ifhv, 1307-ermc, 1307-kic6, 1308-9ej7, 1301-ie39, 1302-7j8p; the Windows push-hang and the two harness-guard rows are floor-tier and the fast-tier move is a Linux builder's. pirria: the Linux body + core constants relanding (fmt refusal first). yolanda: staged build then the destructive run of the arm (operator's standing consent on their channel); the push-hang row (six instances: reads instant, trunk writes hang, salvage writes go) still to file. esme: 1303-2d5g/1304-wbb2 (guard first with mutation arms, corpus cleanup second with per-file decisions and an explicit opt-out marker), 1258-8wfb to close, 1300-k7wq (refs checker blind to inline markdown links) landing. macneo: the macOS body; two questions to macbookair (provision/ regenerated? nvram.bin deliberate?); a HOME-unset fallback relocating a destructive path to /tmp, for its own row. macbookair: after the relay, 830-xsk2's lane (coverage lost, not ported, until the closure test; not live without a tray-side writer); the salvage tool's false "skipped" lines on deletions, to file. yoga: 1285-vz27 in its gate (flake-blocked twice), then 1299-s2sv. lenovinha: 1288-5qpn ssh lane (1165 s gate). Unclaimed: 1305-udgs, 1306-ifhv, 1301-ie39, 1302-7j8p.

OPERATOR: nothing is blocked. Windows installs will reset and reprovision on install once the three arms land and the next cut ships; the Windows tray's reset previously ignored TILLANDSIAS_DESTRUCTIVE_RESET_OK entirely (fixed in the arm). macOS's reset never cleared the keychain credentials it announced clearing (fix in macneo's body).

tokens: subagent_tokens=0 agents=0 (no sub-agents this cycle)
