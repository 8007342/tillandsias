## Cycle 2026-09-04T08:45:00Z (macos — advance-work-from-plan)

Selector seed `tlatoanis-macbook-air`; Direction *forge agents local EXPERTS*
(plan/loop_status.md "## Direction", commit 724691251). Packet taken on
coordinator assignment rather than from the printed batch — see the selector
note below.

### WORK: 1001-i5ux (e9ce61688, left `ready` not `completed`)
Measuring the mirror before porting anything changed which gap this closes.
The FORCE shape the packet was filed for is already refused at the receiving
end — `REJECT: non-fast-forward branch update is disabled` under `--no-verify`,
from the order-579 containment pass. The real gap is the FAST-FORWARD one: a
merge commit whose resolution drops the trunk's files deletes them relative to
the remote tip while staying a clean fast-forward, so no force is involved and
no existing rule looks at it. Measured on the unpatched hook: accepted,
relayed, two files silently reverted.

STANDING CAUTION FOR EVERY NON-TRUNK HOST: that is the ROUTINE operation, not
an exceptional one. Every platform host merges the trunk before every push by
standing policy, and `git log --name-only` shows no merge commit's own changes,
which is exactly why those deletions never enter the touched set.

Fixture written first and RED first — 2/6 before the hook change, 6/6 after;
that sequence is the mutation control. Left `ready` because this changes the
hook SOURCE: running mirrors keep their installed `hooks/pre-receive` until the
image is rebuilt and the container recreated, and that propagation cannot be
verified from macOS. macuahuitl owns the deployed proof.

### PROCESS
- Boundary snapshot taken at cycle start (the prior cycle took it mid-cycle and
  said so).
- Self-inflicted and caught: awk-based edits dropped the executable bit on
  `build.sh` and the hook (100755 -> 100644). Surfaced as `./build.sh:
  permission denied` and by `git diff --summary`; restored before commit.
- Ledger gap this entry closes: my last loop-status entry before this one was
  2026-08-20. Today's earlier cycle (06:34Z, 830-xsk2 + 1002-9xmb) produced
  MO-FULL attestations and packet events but NO loop-status entry, so the
  cross-host recurrence audit could not read either cycle from the ledger.

### CYCLE METRICS (`scripts/cycle-metrics.sh --cycle-start 2026-09-04T08:45:00Z`, verbatim)

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=12 avg_completed_per_cycle=0.33 avg_commits_per_cycle=2.08 overhead_ratio=6.25 source=.cache/metrics/tillandsias-cycle-flow.jsonl
timing: steps=10356 build_check_ms_avg=141177 build_check_mix=mixed:forced=158,memoised=27 litmus_ms_avg=69500 slowest=litmus:codex-e2e-launch-parity:61000 source=.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-04T08:45:00Z steps=118 top3=build-preamble=5,build-check=2,build-check-memoized=2 source=.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=9763 steps=166 top3=build-check:runs=148:total_ms=21409000:avg_ms=144655:fail_pct=30,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=103:total_ms=3079000:avg_ms=29893:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=103:total_ms=1982000:avg_ms=19242:fail_pct=0 source=.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=23 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=103:avg_ms=29893:fail_pct=0:saved_ms_upper=3049107,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=103:avg_ms=19242:fail_pct=0:saved_ms_upper=1962758,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=103:avg_ms=14126:fail_pct=0:saved_ms_upper=1440874 source=.cache/metrics/tillandsias-timing.jsonl
plan: packets=673 ready=444 plan_bin=./target/release/tillandsias-plan
repo: commits_this_cycle=- worktree=clean traces=unknown
verdict: attention:experts-never-called
```

READINGS. `build-check` at `fail_pct=30` over 148 runs is the one to look at
first: nearly a third of gate runs fail, and this cycle's three failures were
all cheap shapes (a clippy nested-if, a scorable-obligation grammar miss, a
dropped mode bit) each costing a full ~145s re-run. The top three `skippable:`
candidates all show `fail_pct=0` over 103 runs — checks that have never once
caught anything.

`verdict: attention:experts-never-called` is accurate and not dressed up: one
`plan_answer` call for the Direction, no `expert-serve` at all. The work was
measurement and shell.

`repo: commits_this_cycle=-` is UNPOPULATED even with `--cycle-start` supplied,
on a cycle that made three commits. Recorded as an observation, not a claim
about the cause.

### SELECTOR
`select-work-batch.sh macos --seed tlatoanis-macbook-air` returned
`epic=stable-milestone-v1 size=4` (1000-rqmx, 793-qc6q, 967-6ax6, 380) with
`frontier socket-audit-master 16.531` above the picked `8.204`. Coordinator
attributes 1000-rqmx's rank to its open author call and the rest to the
per-epic weighted roll; recorded here so the seed and batch can be replayed.
