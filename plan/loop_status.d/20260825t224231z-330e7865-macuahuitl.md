## Cycle — macuahuitl 2026-08-25T22:42:31Z (linux_mutable, linux-next)

Agent: linux-macuahuitl-fable5-20260825t224835z. Hourly /loop cron. RELEASE CYCLE CONTEXT: an operator-directed daily cut is HELD pending an operator ruling (see below). This cycle deliberately did NOT touch the packet blocking it.

GUARDS: preflight ok (inference degraded:image-missing — the very defect this cycle worked on). credential ok. branch ok. mcp ok:experts-healthy + ok:surface-exposed. lock acquired. boundary clean; opsx ok:clean-tree. daily-maintenance current. stranded=0.

THE RELEASE IS HELD, AND WHY
./build.sh --ci-full failed with ONE red out of 2007 passing checks: litmus:expert-groundtruth-harness scenario 6. Root cause environmental — no embedding endpoint on this host, so no spec index, so the grader emits HARNESS ERROR and the fixture counts it as a red case. yoga reproduced identically. NO HOST KNOWN TO US CAN CURRENTLY PRODUCE A GREEN --ci-full. Runbook is unambiguous (gate red -> do not merge, do not tag) and I declined to edit the gate blocking my own release. Taken to the operator with a recommendation.

FILED THIS EVENING (all from the release attempt):
- 888-miiy p1 — the groundtruth harness reds the gate for an UNDECLARED environment dependency, and reports a HARNESS ERROR as a red case, while scenario 4 of the SAME fixture asserts the opposite rule. Handed to lenovinha with a hard constraint: the skip must be LOUD and RECORDED, a release must be able to tell the tier was not exercised, and a genuinely wrong graded answer must still turn it RED. A silent green here would be strictly worse than today's wrong red.
- 889-ewvt p1 — check-capability-row.sh verifies a row EXISTS, not that it is TRUE. yoga's committed row advertised gpu/container/ollama; the live probe reports engines: []. That false row misrouted the authoritative release gate to a host with no ollama. yoga has since published a corrected row (schedulable: none).
- 890-27mv p1 — the daily loop gates on --check, the release on --ci-full, and the quick litmus tier between them is NEVER exercised. yoga measured that the failing check had never RUN on its host, not that it failed quietly. Release-blocking failures can therefore only surface at release time.
- 888-vgs8 p2 — the mandated cache sweep destroys the mandated failure log. CORRECTED in the same session: I claimed tonight proved a loss; it did not and I cannot show it did. Mechanism real, worked example withdrawn.

COMPLETED THIS CYCLE — 811-28eh diagnosis slice (3807164f4), deliberately chosen because it is upstream of the blocker WITHOUT being the blocker.
The embed-endpoint handler emitted `refused` for every failure mode including "nothing is listening". Measured live tonight: this host has no ollama and an empty podman store, and the verdict said refused with curl exit 7 underneath it. There was nothing to refuse, and the opening minutes of a blocked release went to hunting a misconfiguration instead of an absence. Classification is now DERIVED from curl's exit code and HTTP status: absent / timeout / died-mid-request / unreachable / refused. The died-mid-request arm is the packet's headline symptom (serves 200s, then exit 2) and needs a completed-batch counter to be distinguishable from never-reachable. Fixture 4/4 hermetic, mutation-proven.

A FIXTURE BUG WORTH CARRYING: my first attempt served a canned 400 with a one-shot `printf | nc -l`, and my own readiness probe consumed the single connection — the script then met a closed port, correctly said absent, and my fixture reported a false FAIL against a correct implementation. It was measuring its own listener's death.

THE PATTERN OF THE NIGHT, named by yoga and worth keeping: FIVE instances of an ARTIFACT read as evidence of the CHECK that would have produced it — a Windows PASS counted as Linux coverage; an unrecorded local build counted as recency; a capability ROW counted as the capability; my own unproven cache-sweep claim; and my fixture's listener. macbook's rule is the defence: before trusting a probe, ask what result would have falsified it — if nothing would have, you measured your own input. macbook reached it independently while retracting their own 889-bx99, which was false and is now obsoleted.

COORDINATION: issued the 863-iicc lane-branch ruling (recorded myself; pirria is credential-blocked and correctly refused an unpushable commit). Ruled on lenovinha's promote-gate disclaimer question (matcher counts PASS as PASS, warn loudly). Ruled 4.6% freshness coverage is not a finding today. Approved yoga's not-obsoleted judgement on the cheatsheet validator.

OPERATOR ACTIONS OUTSTANDING: pirria needs an interactive `gh auth refresh`; tillandsias-inference and tillandsias-proxy are down across hosts (action=operator) and are upstream of tonight's gate failure.

E2E: eligible; NOT run — the fleet was primed for release validation that never came.

METRICS
flow: cycles=28 avg_completed_per_cycle=1.39 avg_commits_per_cycle=4.68 overhead_ratio=3.36
timing: build_check_ms_avg=20163 litmus_ms_avg=23483
plan: packets=610 ready=380
triage: eligible=168 grouped=143 ungrouped=25 epics=11
