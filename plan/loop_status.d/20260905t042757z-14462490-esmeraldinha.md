## Cycle 2026-09-05T04:27Z — esmeraldinha (windows floor)

**THE FLOOR'S FIRST CLEAN FULL-GATE NUMBER: 25m17s, forced, idle host, correct
configuration, no builder re-init and no plan-binary rebuild, including an
18-crate post-merge compile.** Gate 10, `03:59:19Z -> 04:24:36Z`, `exit=0`,
`ok:test-baseline:ran=2211 new-red=0 tolerated=0 stale=0`, gate stamp recorded.
The instrument's own row reads `build-check duration_ms=1507081 exit=0` after a
`build-preamble` of 8,245 ms, and the gate's internal accounting (1514 s) agrees
with the wall clock (1517 s) to three seconds — the first time those two have
been cross-checkable on this host. Against a four-hour floor cadence that is
about a tenth of the cycle.

It took five attempts and every failure was real. Gates 4 and 6 were red because
I launched build.sh from INSIDE the distro, which skips the re-exec that sets
CARGO_TARGET_DIR, so the probe resolved a stale Windows .exe (1030-i2p8, and
1021-a944 for the call site that reported could-not-run as a violation). Gate 7
died on 1022-px54. Gate 8 died on openssl missing from the builder (1042-svey).
Gate 9 died on a leak guard comparing sub-millisecond cycles by ratio with no
absolute floor (1051-f7e2). The earlier 47m27s and 35m55s "passing" runs were
contended AND, per 1003-444f, ran a narrower gate; this is the only number
measuring the current gate.

Also this cycle: the v56.9.5.1 floor smoke PASSED (cold provision 129 s,
`wire_version=3`, `guest_version` populated, `diagnose_exit=0`); **803-49re
walked at last and does NOT reproduce** — with host vault credentials cleared
the GitHub login completed, so Part A holds on the floor while the reconcile
half (890-y72v) stays untouched; 1004-vsh2's credential predicate took its third
and decisive confirmation, on a store that genuinely held both credentials
beforehand; and the builder-parallelism trial found CARGO_BUILD_JOBS=2 cuts the
io_full peak 65% for 2.9% wall, with jobs=1 worse on both axes.

Method notes worth carrying: a harness `killed` notification describes the
handle, not the process — a driver I declared dead ran on for twenty minutes and
contaminated two arms; confirm with `Get-CimInstance Win32_Process` on
CommandLine, never a `ps` grep that cannot match. Every measurement needs a
second observable beside the headline number: line counts beside timings, a
compile-work guard beside a parallelism arm, a sample-window check beside a
pressure trace. Four times tonight that second observable was the only thing
standing between a plausible number and a wrong conclusion.
