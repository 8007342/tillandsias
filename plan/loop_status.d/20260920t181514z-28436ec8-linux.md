## Cycle 2026-09-20T16:15Z→2026-09-20T17:55Z (linux — macuahuitl, coordinator: the plan-only lane's freshness check was fail-open; the gate digest tracks the index mode, not the file; the relay preflight grows to fourteen deciders and refuses on the late ones too)

Direction ("We're giving forge agents local EXPERTS"): served by keeping trunk integrating through three lands and eight folds, by the 1287-h6qn ruling that makes the plan binary prove its own currency from a content hash instead of an mtime ladder, and by three relay-side deciders that say in a minute what the gate said at minute twenty.

DONE, verified on origin by content: LAND 35c 9b023d006 (pirria's 1287-h6qn v3: the plan binary embeds a content hash of its validator surface; the lane and the checker accept only the literal ok line; the 851-cduu fail-open closed with three arms; fixture resolves the binary through the shared probe); LAND 32b c73d796f2 (yolanda's core import of RESET_SKIPPED_LINE + fmt fixup) and the fold cb351ba46 that completed 1276-2hc6; LAND 33b 07f397fd5 (yolanda's status-loss batching, three calls per fragment folded into one; pirria's Linux body v3 of --reset-state with the installer calling it and its litmus bound and PASS 7/7 on trunk; relay-side clippy fixup 71ec243a7, behaviour-identical); LAND 34 a43c175a6 (lenovinha's 1309-qc95: the mirror ssh lane fails closed when enabled and unwired). Folds: 1e370f944 (yolanda 1308 note), 16b1fd1d4 (1287-h6qn claimed for pirria), ce0ad8f61 (macbookair's 1309-rb3p: the tray pushes the forwarder target after Ready and the PSK binds the version, so no mixed-version session exists), 2027bb4ee (yolanda, two fragments). Peers' own trunk pushes: yolanda 634f02e92 (1307-kic6 closed; the first completed trunk push from Windows this day), pirria d0fb98cee + 09911b325 (1310-7apk raised to p1 with both regimes in-row; 1286-4437 Linux-arm evidence), esme (five 1304-wbb2 events; 1258-8wfb closed), yoga 0aa2187c6 (1285-vz27 closed). Closed in this window: 1276-2hc6, 1307-kic6, 1309-qc95, 1285-vz27, 1258-8wfb. Filed: 1310-7apk (p1), 1310-5e6g (wire row), 1309-rb3p, 1311-ighu.

REFUSED LANDS, each handed back with the gate's own line: 33 (clippy 1.98 needless_borrow ×3, fixed relay-side); 35 (the 851-cduu freshness fixture — both my readings were wrong; pirria found a FAIL-OPEN in the lane and the checker: exit 0 from a binary that cannot answer was read as fresh, and the fixture's stub answers exit 0 to everything); 35b (721-nyev: the new fixture runs tillandsias-plan from a hardcoded target/ path without the shared probe — the refusal yoga took on 1285-vz27 this morning; pirria v3 asked); 36 (macneo's macOS --reset-state body: main.rs:30 `mod reset_state;` carries no cfg(target_os = "macos") while its siblings do, so the Linux workspace compile cannot find diagnose; one-line fix asked, follow-up commit on osx-next).

RULINGS: no litmus budget is raised (macneo's 120 s refused; case 1 of the bindings-reconciliation fixture asserts the rule on a throwaway corpus, 0.162 s on every tier — esme's floor numbers, 83-95 s skipped and 693 s swept against a 30 s budget, decided it). 1287-h6qn: the plan binary embeds a content hash of its validator surface via build.rs; the lane and check-plan-binary-current.sh accept only the literal ok:validator-surface:<hash> line; the stamp ladder stays beneath; next-order refuses on a stale binary. 1310-5e6g link 3: a per-mirror AppRole bound to exactly the per-mirror host-signer policy — never attach the policy to the global git-mirror-agent role (cross-project signing authority); wire the ssh flags at the launch site in main.rs, container_profile.rs is inert. 1286-4437 stays one contract with three bodies: Linux proven on trunk (pirria, 7/7), Windows landed, macOS after the cfg fix; pirria closes it citing all three.

FINDINGS TO CARRY: (1) the plan-only lane's freshness check was fail-open (pirria; fixed in v2, three arms); (2) the gate-stamp digest takes each path's MODE FROM THE INDEX, so a new 755 file staged AFTER ./build.sh --check moves the digest with zero content change and every instrument correctly reports nothing moved (macneo, 1276-mugq, reproduced both directions with the tree restored) — stage before the gate; the gate-add-push order the skills teach pays a second full gate on every slice that adds a guard script; (3) run-litmus-test.sh --parse-only answered ok: for a file validate-yaml refuses because its extraction is line-based (esme, 1303-2d5g; fixed, fixture 3/3 on Windows and on Fedora, verdict passes the loader's message through verbatim; the fixture is invoked by nothing yet, binding asked); (4) esme's correction on 1304-wbb2: the sixteen quiet litmus are exempt by design (10 grandfathered + 6 retired, 415+6+10=431 at rc=0); the red reconciliation guard explains pirria's stray only.

CORRECTIONS OF MINE, recorded where they happened: both land-35 readings (an mtime-pinned fixture; a file-set surface) — the cause was the fail-open; the chmod read of macneo's stamp refusal and its find -newerct discriminator, one layer off (the staged mode record, not the worktree mode); "wire the ssh flag through container_profile.rs" (inert module); I repeated pirria's checker-caused-the-win claim in a dispatch before they retracted it; the relay preflight caught land 36's cross-target red and launched anyway because its refusal test ran before the late deciders — both relay scripts now refuse after them.

THE RELAY LANE: fourteen deciders before a gate is spent (added today: plan-binary-probe-usage, the late refusal, cross-target compile, plan-orders + plan check on refs that add fragments); a decider refusal costs one minute, a compile refusal eight, a fixture refusal twenty-plus.

FLEET STATE: macuahuitl bundles land 36b from macneo's cfg fix, esme's 1303 v2 and pirria's 1287 v3 as they arrive, then levels and hands each closure back by order. pirria: v3 (source the probe), self-demonstration, close 1287-h6qn; close 1286-4437 once macOS lands. macneo: the one-line cfg on osx-next; then the mode-record diagnostic row (970-7fqk territory). esme: bind the 1303 fixture (litmus yaml + bindings line) as v2; 1304's 2a cross-check after macneo lands. lenovinha: 1310-5e6g link 3 (the mirror authenticates with the per-mirror signer role; sshd then starts) — the mirror-to-GitHub half is PROVEN by outcome (operator-seeded token, 150 → 155 refs relayed, zero stranded, keyring PID unchanged all day); 1310-rec6 filed (59 "remote end hung up" lines on a fully successful sweep, same shape as a real auth failure; noise-versus-failure first, then refuse-to-advertise-when-broken). yoga: the 1299-s2sv emitter (root/root_unusable/unknown), unreported for about ninety minutes. yolanda: 1308-9ej7 (claimed on trunk at 2027bb4ee). macbookair: tray + guest rebuild (the VM guest predates the forwarder lane), then the closure test. Branches: trunk 2027bb4ee; windows-next levelled to it; osx-next carries macneo's slice (3 ahead, refused, awaiting the fix).

OPERATOR: nothing is blocked. Your join-the-fleet direction is filed as 1311-ajpm (p1, claimed here): one idempotent skill, two regimes, six steps, lenovinha's initialize-bare-metal-host folded in as its substrate step; the coordinator drafts it, lands through the full gate. Your question through lenovinha, decided here: no fleet-wide credential-free-push instruction until a host push has travelled host → mirror → GitHub on lenovinha (link 3 is unbuilt; the per-host token copy is your per-host call with the fleet shape you named); the idempotent ./skills/initialize-bare-metal-host is approved scoped as "bring the stack up" (ensure-enclave + tillandsias --bash <project>, prints --github-status, never seeds or prompts), filed and built by lenovinha after link 3. Your token seed on lenovinha is what proved the mirror-to-GitHub half; nothing further is needed from you.

tokens: subagent_tokens=0 agents=0 (no sub-agents this cycle)
