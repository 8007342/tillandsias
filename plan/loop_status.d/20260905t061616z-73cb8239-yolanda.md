## Cycle 2026-09-05T06:16:15Z (yolanda — 972-umik commit B, 1049-s35z, 1056-w2yy, 1055-6yp8)

**Landed on origin/windows-next at 22a21d661** (push line
`8e839826f..22a21d661`, preceded by `ok:linux-next-merged:1`). Verified
against the remote rather than the worktree: 79e3ca876 and df5708607 are both
ancestors of origin/windows-next; `git show origin/windows-next:` on the
reader gives `Err(std::env::VarError::NotPresent) => Ok(SecureWireMode::On)`
and on the ratchet gives `BASELINE=...:-0}`.

**972-umik commit B — the control wire is encrypted by default.** The condition
was the trunk's ratchet, checked with my own merge-base and not taken from a
message: 7623213e2 an ancestor of origin/linux-next, ratchet 0. Three arms, each
verified to discriminate rather than banked on its green:

1. The sabotage arm (`a_plaintext_peer_is_refused_by_the_now_secure_default`)
   was negative-controlled by giving the plaintext peer a real handshake and
   watching it go red at hvsocket.rs:529.
2. **The ratchet was inert and the control is what found it.** With the flip in
   and the hvsocket converter reverted by hand, the count went 0 -> 1 and the
   script STILL EXITED 0, because BASELINE was 3. The exact regression the order
   exists to prevent passed the gate silently. Baseline 3 -> 0 in the same
   commit; the identical sabotage now exits 1. Rule taken from it: a ratchet's
   baseline drops with its count, or the guard is decorative.
3. The litmus meant to pin the default was **green over an absence** — it
   grepped for a deleted test's name and matched the doc comment explaining the
   deletion. Re-anchored, with a control that mutates a copy back to Off.

The fixtures MOVED rather than opting out: three host-shell tests went red on
the flip and now speak Noise, because an explicit `off` would have kept the
suite green while testing a path production no longer takes.

**1049-s35z (p1) — the litmus runner reported PASS while skipping its corpus.**
jq.exe writes CRLF, so every value through the runner's yaml tier carried a
trailing CR. Before: two SKIPs "Test file not found" for files that exist, then
`Total: 3 (executed: 1, skipped: 2)` and `Status: [PASS]`. After: executed 3,
skipped 0. Skips are excluded from coverage, which is the mechanism that turned
a broken lookup into a pass. Fixed ONE site with the before/after measured; the
other 25 jq call sites are in the packet, deliberately unswept.

**The instrument lied about the instrument.** My first blast-radius measurement
said "0 of 476 names affected" — MSYS grep treats CRLF as the line terminator,
so the CR is invisible to it. `od -c` shows it. Any audit of this class that
uses grep will report all-clear.

**Also filed:** 1056-w2yy (the yq banner is out of band — only 2 of 413 litmus
files invoke yq in a step, so a run-wide warning that names no test invalidates
everything or nothing) and 1055-6yp8 (check-skill-canonicalization.sh reads the
worktree, so a `core.symlinks=false` checkout reports all 75 skill entries as
violations, calls a 24-byte regular file a "real directory", and prints a REMEDY
that would move 75 correct entries if followed here).

**Mistakes worth the record.** (a) Python text-mode writes turned two LF shell
scripts into CRLF and broke `bash -n`; heredoc-escaped backslashes then injected
real CR and newline bytes into a patch. Both from editing shell scripts through
a text-mode writer on Windows — patches now go in as literal files, byte-safe.
(b) I read `cargo test` output on Windows as evidence about a module that is
`cfg(all(test, unix))`: it reported 7 passed while three tests in that same
module were broken by my change. On Windows, name the runner beside a test
count.

**Push race.** Two pushes refused by `blocked:linux-next-not-merged` because a
~23-minute gate loses to a faster trunk. Not worked around: no `--no-verify`,
and the merges were substantive (vz.rs, inside this flip's blast radius, derives
the guest's systemd value from the reader I changed). Resolved by macuahuitl
opening a freeze window on the three Linux pushers; landed inside it.

Agent: windows-yolanda-opus5-20260905t061616z
