## Cycle — lenovinha (linux_immutable) 2026-08-23T17:35:12Z — toolbox init set + two ledger-observability findings

Full-mode cycle on lenovinha (linux_immutable, Silverblue, linux-next).
Batch: epic=convergence-velocity-milestone seed=lenovinha size=10 budget=10 route=rank:2/7 score=6.407.

GUARDS: daily-maintenance ok:current:2026-08-23; cycle-preflight ok (rebuilt+expert-absent+services-down2, dev-inference ready); credential ok:gh-keyring; branch ok:branch-linux-next; mcp ok:experts-healthy + ok:surface-exposed; boundary clean.

WORK: claimed and advanced 799-tb7q (builder toolbox init set). Grew the tillandsias-builder dnf init set with jq/yq/ripgrep/openssl and extended _toolbox_initialized to require all four, so PRE-EXISTING toolboxes fleet-wide re-run init rather than staying silently short. Measured, as the deliverable demanded: 6.3 MiB, +8s first-run init cost (~6% on top of this host's ~132s first forced --check). Verified yq is mikefarah v4, not the kislyuk python wrapper that shares the binary name and takes different syntax. Released the claim back to ready with the sweep residual in next_action — the ~50 bare-jq/rg/openssl callers are now POSSIBLE for the first time, but two of them (tray-diagnose.sh, diagnose-macos-provision.sh) ship to end-user machines that have no builder toolbox, so the dispatch pattern degrades there rather than improving. That split wants deciding before the sweep, not during it.

COMPACTION: eligible=true, folded 29 fragments (+888/-17). The 17 REMOVED lines cost me a stop-and-investigate before resolving to nothing wrong — they are last-write-wins field supersessions (5x status, one multi-line next_action on 849-tz8g), which is a correct fold. Filed 865-ng6r: the skill states compaction produces "zero removed lines" and calls that "the property to protect", which was true on 2026-08-03 when the fold only appended and became false the moment set-field fragments existed. Every host that compacts will pay the same investigation until the prose is corrected.

FILED 866-pvsx, by making the mistake: I hand-wrote a `set_field:` key into a fragment to release the claim. The fold does not recognize it and SILENTLY DROPPED IT — yq parsed, closure-evidence-check said ok, fragment-misplaced-definitions said nothing, plan check said `ok: 550 packets`, and the packet count even rose because the packets: half of the same fragment folded fine. The claim was still open. Fragments are immutable and five hosts write them concurrently, so a dropped directive is permanent; the failure mode is a host that believes it released a claim and did not, which is 641-e2qa reached by an honest mistake.

EVENT ON 767-es4w (no new row): that packet is marked `implemented` and its behaviour is absent here. tillandsias-proxy sat Exited 2d22h and tillandsias-inference Exited rc=137 for the same 2d22h, both with RestartPolicy `no` and restarts=0, and nothing alarmed for three days — cycle-preflight found them, which is not the mechanism the packet asked for. The crash is also no longer the 263-SIGSEGV shape: squid 6.12 now dies deterministically with X509_check_private_key() failed / FATAL: No valid signing certificate. That is a provisioning mismatch, plausibly the 755-qcxh bind-mount-to-podman-secret migration leaving a stale key against a regenerated cert (flagged, not confirmed — the remedy touches the running enclave stack). Restarted inference (clean, killed from outside); left the proxy stopped since restarting a deterministic failure only adds noise. Second independent "enclave service dies unnoticed for days" sighting, two months apart — noted as overflow, not promoted.

FRESHNESS: scripts/resolve-open-pr.sh, verdict=refreshed, no change needed. Fixture 5/5 green, litmus pins both the executable bit and the blocked:open-pr:gh-failed: grammar, and its three branches (null / empty / gh-failed) are named separately rather than collapsed.

ADVISORY: stranded-in-progress summary population=3 in_progress=3 stranded=2 threshold_events=0 — both "stranded" rows are yoga claims from 16:20Z today, live not abandoned. Deslop not due (order=865 last=834 delta=31 hours=88). triage ungrouped=24 of 166 eligible.
