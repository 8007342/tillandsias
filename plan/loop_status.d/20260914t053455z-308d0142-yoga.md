## Cycle 2026-09-14T05:34Z — yoga (linux-next)

1174-6r4k, assigned and claimed by macuahuitl; landed at bff49e06c and closed with that SHA.

- FULL TIER IS DEFINED FROM THE WRITER'S VOCABULARY, not invented: local-ci.sh sets CI_PHASE="all" for a whole run and the phase name for --phase <p>. A run counts if its records say `all` OR if between them they cover pre-build AND post-build AND runtime — three deliberate phase runs under one id HAVE exercised the tier, and keying on the literal word would report a covered run as no answer at all.
- THE dispatch=ci-full MARKER THE ROW NAMES DOES NOT EXIST IN THIS INDEX. _stamp_dispatch writes `dispatch` into the GATE STAMP; a check-log record carries ci_run_id, ci_phase, check_id, status, source_log, archived_log, sha256, duration_ms. Recorded rather than assumed, and adding the marker to the writer is a separate change I did not smuggle in.
- "No full run anywhere" is never, NOT fresh — the same fact the no-index branch reports, reached another way. Ignored runs are NAMED with their coverage; silence would look identical to "there was nothing newer".
- THE FIXTURE'S OWN HELPER WAS PART OF THE SUBJECT: rec() hardcoded ci_phase:"pre-build" while every arm meant "a run", so once the guard could tell them apart every pre-existing arm was writing a partial run and the guard correctly refused to call any of them a tier answer. 5 of 14 arms rewritten for a reason only visible after the discriminator existed.
- ONE ARM I WROTE WRONG, caught by the fixture: I expected `stale:` for the covered-separately case from a date-stamped run id — the wrong verdict (0d old is fresh) AND an absolute date that would rot into staleness on its own. It uses the harness's now-derived RUN_ID; the ordered arms above are ordered relative to each other and their verdicts do not depend on age.
- Three mutations, each diff proven non-empty first, all red by name — M1 reproduces the defect exactly: a green phase-only run reads fresh, rc 0.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
