## Cycle — macuahuitl 2026-09-02T21:41Z (coordinate-multihost-work, cron 23-even; first coordination pass after the fleet restart)
- LastExecutionTime: 2026-09-02T21:41Z → 22:00Z. Direction cited: forge-local experts (391) primary; web-share (373) secondary. Lock: coordinate-cron.
- Heads at start: main d6d3e3ed9, linux-next 93714734d, windows-next 925f7557a (ahead 2), osx-next 33e04ca58 (ahead 12 — OVER Dmax=5, drift alert). Both merged this pass; drift now 0/0.
- MERGED: osx-next (macbook's five macOS-dialect fixes from the union red — ungated enumerate_npus failing -D dead-code on Windows too, a BRE `\+` that made a guard green and inert on every Mac, a bash-3.2 `.` abort, two fixture gaps; plus the 944-vim8 READ_KEYS repair and their re-land of lenovinha's dropped measurement, a no-op after the upstream repair). windows-next (yolanda's native-scratch-dir.sh: the archiver check's tree churn moves off 9p, 62.3s → 5.3s).
- UNION LITMUS (skill step 3, run on the MERGED tree, which is the point): PASS 284 / FAIL 5 / executed 289, coverage 100% [100/100 specs], pass rate 98%. FOUR of the five are 3-10s TIMEOUTs taken while this host's own forced gate ran concurrently — contention artifacts, not reported as regressions (956-llei's censoring rule). The FIFTH IS REAL AND IS WHY THIS STEP EXISTS: litmus:forge-experts-ephemerality-shape red on `$state_dir/model-warm` from 965-hz3f, which landed an hour earlier. The write IS ephemeral; what was missing is the proof every new indirection owes (621-2re2). Fixed here in the shape that file prescribes, re-verified 16/16 PASS. NEITHER branch gate could have seen it: ./build.sh --check runs no litmus (748-tkjx).
- MEDIATION 1, thrashing/duplicate-fix: macbook and lenovinha both fixed the 944-vim8 capability-row deadlock, independently, within the hour, both defensibly, and BOTH LANDED. The trunk then carried `capabilities` in READ_KEYS and in NON_FOLD_CHANNELS — two lists asserting opposite things — with READ_KEYS matched first, so the second was also dead code. Resolved to READ_KEYS by the rule lenovinha's own comment prescribed (846-idhn is completed+archived; compaction serialises the channel into the base and the fold reads it via fold_capabilities). Allowlist kept, emptied. Neither host could see it; each gate passed on its own branch.
- MEDIATION 2, my own error: I relayed yoga's "the fleet has no host that can produce `placed`" as a fleet fact. lenovinha falsified it within the hour on their CUDA lane (accel_proof=placed). Retracted on 793-zumy with the scope that was actually measured, and corrected to yoga directly. An n=1 universal negative is the shape that hardens into a premise nobody re-tests — 599-3b9h is the fleet's prior instance.
- FILED THIS PASS: 967-xq5e (p1) the spec index never refreshes on a bare-metal host — expert_accuracy fell 33/33 → 31/33 with two citations the harness classes FABRICATED; cause VERIFIED, not inferred: the index carries .commit b300d6b92, HEAD was 79 commits later, main.rs +201 lines over that span, a consistent ~55-line offset, and the post-commit hook refreshed nothing on any of those 79 commits because spec-index-ensure.sh exits 0 on skip:no-embed-endpoint while dev-inference serves qwen2.5:14b on localhost. 968-uhzg (p2) sweep validators pinned to `== <version>`, from lenovinha's self-report that their schema bump was refused by the validator the bump was for.
- THE PATTERN OF THE DAY, named by yolanda and now at five instances: ONE COPY FIXED, ONE COPY MISSED (plan-binary probe, hardware fingerprint, the accel probe's two transports, the Windows purge credential clear, and the embed endpoint — 967-xq5e is lenovinha's forge fix landed in the forge image's copy of the lib with no host-lane equivalent). Their remedy is the one the fleet keeps arriving at: a guard that makes the second copy impossible to add silently.
- ASSIGNMENT BOARD (primary + fallback, no idle hosts): yoga 821-73es→824-6qxh, fallback 966-7umc on request (claimed 20:29Z, working). lenovinha 793-qr4t CLOSED + 793-qc6q implemented-left-ready on purpose (order 480 shape), resting by choice, next packet to be handed not picked. yolanda 793-zumy REMAINING 2 done + 964-9yyp done + 803-49re Part A reclassified p1 and fixed, fallback pending. macbook osx merge done + 702-6jza claimed. macuahuitl-forge 920-pxg6 + 965-rb3v + 965-hz3f + 448 all landed, now on 966-7umc. lenovinha-forge 919-vvyv landed. pirria 959/956/863 + 966-rq7f landed via two salvage branches. esme 959/956 readings landed by relay.
- VELOCITY: 76 commits and ~15 terminal transitions in the 80 minutes since the restart, against 14 packets filed — arrival still above service, so the acceptance boundary matters more than throughput (b>1 regime, distributed-work.yaml). No High-Velocity Alignment Event: R is falling and no host is blocked.
- BLOCKED/OPERATOR: esmeraldinha's own git mirror 403s on GitHub, so its forge cannot push and its findings came over the message channel — needs a GitHub re-login or Vault token repair on that host. Two 100-day openspec changes still need promote-or-archive. Neither blocks any host's current work.
