## Cycle 2026-09-05T12:35Z→13:15Z (macos/tlatoanis-macbook-air — 1024-c3h3 reopened+closed, 1004-x9ua closed)

### WORK DRAINED

Direction: forge agents local EXPERTS. Selector seed
macos-m5-apple-tlatoanis-macbook-air-20260905, epic harness-mcp-expert-validation,
batch = 1004-x9ua + 317. Landed 6d4e98d8c and 6e1940cff.

- **1024-c3h3 REOPENED from done and re-closed.** Its fixture measured the
  checker's SKIP branch: a cwd-relative validator probe run from a scratch repo
  with no target/. Invisible on Linux because the fall-through finds a
  tillandsias-plan on PATH, so the arms passed by measuring ambient tooling.
  Third instance after 1058-fenk and 1060-wxdh. THE FALSIFICATION: arm 5 is a
  NEGATIVE control, and a skipped gate flags nothing, so it passed VACUOUSLY
  while three siblings went red — "2 passed" was itself a false reading. The
  no-validator path is now a named skip reporting 0/0.

- **1004-x9ua closed, all four criteria.** check-host-tools.sh read the
  operator's PATH and reported it as the host's inventory:
  missing:host-tools:macos:timeout,cargo,rustc,pkg-config with all four
  installed. WIDER THAN FILED — two prefix families (~/.cargo/bin AND
  /opt/homebrew/bin), so the filed fix would have left two tools still lying.
  Registered as the fifth site in scripts/lib-cargo-sites.sh per that file's own
  instruction. gtimeout now satisfies timeout, read off the consumer
  (check-credential-channel.sh:17-20) rather than asserted here.

**The cycle's lesson, and it is about my own work.** My first fix for
1004-x9ua's criterion 1 was wrong and NO RUN ON THIS HOST COULD HAVE SHOWN IT.
I skipped when the control already reported $expect; the real case never
produces $expect at all — on macneo's Mac the prover said
missing:no-credential-channel in BOTH runs. The discriminator is "removing the
tool changed nothing", not "control equals expect". With a live gh token neither
form fires, so the fixture read 17/17 for the correct condition AND the
incorrect one. Found by reasoning about another host's transcript, not by
trusting a green. A guard verified only where it cannot fail is verified
nowhere — 1066-dkkb, which I filed this morning, pointed at me.

Also recorded: I misread my own instrumentation twice, reporting GATE_EXIT and
LAND_EXIT from `$?` after a pipe, which read tail's status rather than the
script's. One of those labelled a REFUSED land as exit 0. Redirect to a file and
read $? directly.

Still open, neither mine: 317 criterion 2 (install half needs the operator's
authorisation for the destructive e2e; the current-guest half is runnable and
reaches only "no silent 127s" and channel identity), and the standing
violation:declared-closure-unresolvable:4 that prints on every gate as "not a
gate" and appears unowned.
