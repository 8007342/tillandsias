## Cycle — lenovinha (linux_immutable) 2026-08-24T05:53:16Z — degraded runs now say so, and I retract yesterday's measurement

Full-mode cycle on lenovinha (linux_immutable, Silverblue, linux-next). All guards green.

CLAIMED 799-tb7q to build the follow-up I named last cycle: make a degraded litmus run announce itself. It landed — the runner prints `warn:litmus-degraded-no-yq` once when yq is neither on PATH nor provisionable, saying that yq-calling steps will fail or return empty and metadata falls back to grep. A warning, never a refusal; a host without yq must still be able to run its suite, the point is only that a reader can tell which kind of green they are holding. Verified BOTH directions — present when yq is unavailable, absent when yq is there, which is the control that stops it becoming noise nobody reads.

RETRACTION — the second in two cycles, same root cause. Yesterday I reported "without yq Total 19 (executed 17), with yq Total 28 (executed 26) — NINE TESTS WERE NEVER SELECTED on this host, the grep fallback under-selects by 35%", named the nine, and called it a fleet property. THAT DOES NOT REPRODUCE AND I WITHDRAW IT. The 19-arm restored the runner from a pre-shim commit AND deleted the shim, so it varied the runner version and yq availability together, and I attributed the whole delta to yq.

CONTROLLED RUNS TODAY, one variable at a time: current runner with yq genuinely absent -> 28/26. Pre-shim runner with yq present -> 28/26. Current runner with yq present -> 28/26. Selection is IDENTICAL in every arm. I also compared the parsers head to head: yq and the awk fallback return the same 28 test names for the ci-release binding, and for one of the supposedly-missing tests both agree phase=pre-build size=instant. The mechanism I described cannot happen.

MAKING THE CONTROL ARM HONEST TOOK THREE ATTEMPTS and that is the reusable part: the shim RE-PROVISIONS, so deleting it does nothing; a non-executable `toolbox` shadow does not work either because command -v skips it and finds /usr/bin/toolbox; what works is an EXECUTABLE stub named toolbox that exits 1, early on PATH. Recorded in next_action so the next person does not rediscover it.

WHAT SURVIVES, and it is enough to justify the shim: with yq genuinely absent, real steps give WRONG ANSWERS. Two confirmed — skills-canonical-and-mcp-first STEP 6 reports a present-and-correct methodology rule as missing, and added-fragment-parse-gate STEP 8 (a POSITIVE CONTROL, "the real ledger loads and its vocabularies agree") returns EMPTY and fails. Wrong answers, not skips, with nothing saying the toolchain was short a parser. ci-release is 26/26 green with yq present.

ALSO WITHDRAWN: the claim in yesterday's entry that two of my freshness stamps were invalidated because litmus:release-runbook-external-preconditions-shape was not running here. That rested on the same bad measurement. It does run here, and did. The stamps were never edited, so nothing needs undoing.

THE HABIT I AM TRYING TO FIX, stated plainly because writing it twice has not been enough: check that the control arm is ACTUALLY IN THE CONTROL STATE by observing it directly, before reading anything off the comparison. Both failures were free to catch — one grep for an env var that appears nowhere in the script; one look at a diff that changed two things. I now print the observed state of each arm rather than assuming the flag or the file edit achieved it.

FRESHNESS: openspec/litmus-tests/litmus-cheatsheets-license-allowlist-shape.yaml, verdict=refreshed, EXECUTED (spec cheatsheets-license-tiered, 2/2 100%). Correctly scoped: deliberately grep-only, pinning the FILE STRUCTURE of cheatsheets/license-allowlist.toml while its sibling runs the validator, so a refactor that moves the file fires pre-build. Note in the stamp: it is selected by SPEC id, not by its own name — filtering on the file name returns "no litmus tests matched", which reads as absence rather than a wrong filter, and the freshness report prints the FILE path.

ADVISORY: compaction not eligible. Deslop not due. Proxy still down (767-es4w).
