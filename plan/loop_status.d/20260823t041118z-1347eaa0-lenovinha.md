## Cycle 2026-08-23T04:11:18Z (linux-immutable lenovinha — meta-orchestration full; /loop iteration 1: 847-wgy4)

### CAPABILITY-AWARE ROUTING LANDED (847-wgy4, completed at 649fc5534)
The fleet's pigeonhole is closed. Separation now comes from the ROSTER, not
the seed: `capability-matrix --hosts` (new projection) gives each row-holding
host an ordinal rank; pick = (rank + UTC-date rotation) mod width over a
frontier widened to the roster. Disjoint by construction — measured live, the
4 reporting hosts land on 4 disjoint epics, and the fixture demonstrates all
EIGHT announced identities on eight (the prior R4 test false-passed three
times on smaller samples). Anti-starvation widened, not traded (width >= old
K; rotation walks every slot; litmus case 3). Rowless hosts keep the seeded
top-3 pick byte-for-byte, so separation grows as 850-bif2 rows land. The
operator tier mandate is now the selector's: <=4-core hosts get
low-end-tagged work only (release-unscoped, so 855-wrr3 reaches the floor
host), refuse rather than drain generally, and the tier is subtracted from
everyone else. cuda/rocm/npu/metal tags join the TOOL_CAP_TAGS subtraction,
decided by the host's own matrix row. Pinned by
litmus:capability-routing-shape (5/5).

### GUARDS EATING THEIR OWN COOKING, AGAIN
The 721-nyev plan-binary-probe guard refused this cycle's first fixture
draft (hardcoded target/ path); the set-field-yaml-shapes gate went red
mid-rebase because the BUILT binary predated the incoming c6f8cbccc fix —
the cycle-preflight doctrine (rebuild the instrument) applied at rebase time,
not just cycle start. Also repaired in passing, attributed in the commit:
litmus:added-fragment-parse-gate-shape's fixture had been VACUOUS on yq-less
hosts since 82051c7b6 — it copied the gate without plan-binary-probe.sh or a
validator, so the gate passed with '0 checked' and the negative control
starved. It now carries the probe and the real tillandsias-policy.

### FLEET STATE OBSERVED
Six hosts armed or active: macuahuitl coordinating, yoga armed a systemd
user timer (856-s56y) and published a gpu-rocm row, the Mac onboarded
(851-gpb5), yolanda cycling, this host on a 2-hourly operator /loop. The
matrix reads 4 hosts reporting; routing now rewards every new row with a
collision-free lane.

### METRICS
- guards: ok:gh-keyring, ok:branch-linux-next, ok:clean-tree, ok:experts-healthy, ok:surface-exposed, daily gate current (2026-08-22 local)
- flow record: claimed=1 completed=1 commits=4 (claim, implementation, closure, loop-status+attest to follow)
- e2e: not run — targeted operator /loop iteration; release-currency unchanged
- next /loop firing: cron 9e37078a every 2h at :11; 847-wgy4 is done, so the next iteration falls through to the normal drain — which will itself be ROUTED by the mechanism this cycle built
