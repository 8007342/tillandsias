## Cycle - windows (windows-next)

- Closed 725-bu54 with a THIRD shape, neither of the two I filed. The packet
  offered "keep the boundary until the process exits" or "re-verify by
  re-snapshotting when clean", and I had recorded the second as dangerously
  close to snapshotting at the end and calling it a startup boundary - the move
  717-3bvv exists to forbid.
- What landed: retirement moves to the NEXT cycle's snapshot. Finalization stops
  removing the state dir; snapshot retires the previous boundary before taking
  its own, reading the path from the $GIT_DIR stamp it already writes. The
  boundary a continued cycle re-verifies against is still the one taken BEFORE
  any work, so nothing about the guarantee is weakened - only who cleans up
  changed, and cleanup by the next start rather than by an exit that may never
  come is the project's own posture on ephemerality.
- The new rm -rf fires only for a path this guard recorded, that still looks
  like a boundary, and that lives outside the worktree; a hand-edited stamp
  cannot turn it into an arbitrary delete. Pinned by a bystander-untouched
  negative control.
- Fixture 6 -> 9 green, hand-falsified both directions: disabling retirement and
  removing the re-stamp each turned it red, restoring each returned 9/9.
- Did NOT take 723-2yb3 (rung 2) though it is next in the ladder: it is a wire
  change across control-wire, the guest pty handler and the host exec client
  with four version combinations to test, and starting it late in a cycle is how
  half-landed protocol changes happen. 725-bu54 was bounded, was mine, and had
  bitten this session twice.
