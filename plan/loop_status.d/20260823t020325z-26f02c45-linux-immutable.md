## Cycle 2026-08-23T02:03:25Z (linux-immutable yoga — meta-orchestration full; operator-scoped: cold-start measurement + capability row)

**Mode call:** full mode, deliberately scoped by the invoking operator prompt to
two named deliverables — no batch-selector drain (847-wgy4: selector cannot
route to hosts), no claims. The word "smoke" in the prompt appears only inside
a skill-name reference; the named deliverables require filing+commit, which
smoke mode forbids.

**Work landed:**
1. Silverblue cold-start bootstrap, measured and decomposed — pull 11.5s
   (355 MiB compressed, 1.09 GB on disk, isolated podman root), toolbox create
   0.1s (image local), first forced `./build.sh --check` on the re-created
   container 131.9s rc=0 (dnf 17s, rustup 14s re-downloading 8 components,
   gate ~100s), zero failures, zero retries. Filed as an EVENT on 850-bif2
   (fragment 20260823t020325z-850-bif2-coldstart-timing-yoga-linux-immutable.yaml)
   per new_row_only_if_independently_schedulable — the owning scope
   (Joining-the-fleet section) is claimed by lenovinha under 850-bif2.
   Provenance corrected: the skill's "minutes" sentence (b629bb379, macuahuitl)
   was unmeasured at birth; lenovinha's figures are warm-rejoin; no prior
   genuine cold wall-clock existed in the repo.
2. yoga capability row published (verbatim HEAD-binary probe, locus
   bare-metal) — capability-matrix now 4 rows, 3 of 7 reporting; yoga off the
   SILENT list. Linux half of the yoga/yolanda same-silicon A/B. Observed, not
   assumed: AMD GPU reported usable via generic Vulkan/DRI ("Vulkan GPU",
   /dev/dri/card1), legacy_tier=gpu-rocm from host rocminfo; XDNA NPU
   present-unusable (engine-missing, amdxdna fw 1.1.2.64); engines=[] so
   schedulable none. Contradicts "cannot be detected at all" — that state is
   the WSL2-guest locus. Probe cache preserved via scratch TILLANDSIAS_CACHE_DIR
   (HEAD binary would have discarded the v1 cache's 3 measurement rows on
   schema mismatch — recorded in the row fragment header).

**Gates and hygiene:** cycle-preflight ok (services-down4: enclave containers
exited — known idle state, remedy documented by preflight, no restart in a
scoped cycle); credential ok:gh-keyring; branch ok:branch-linux-next; MCP
probe ok:experts-healthy; opsx ok:clean-tree; boundary snapshotted+verified.
Daily maintenance run + stamped (ok:daily-maintenance-stamped:2026-08-22):
nix lanes skip:no-store / blocked:no-nix-and-no-toolbox (guard fixture 9/9
PASS — lane unprovisioned on this host, not broken), delegate sweep empty,
dangling-image prune clean, cargo GC not needed (target 56M).

**E2E gate:** skipped, recorded: linux_immutable routes to curl-install e2e
only; this cycle is operator-scoped to the named deliverables and the
destructive reset would additionally wipe the substrate whose bootstrap this
cycle just measured. e2e-preflight's `eligible` remains structurally
untrustworthy on immutable Linux (no immutability logic).

**Freshness audit:** deferred this cycle (no worker drain; operator-scoped).

**Stranded-claim sweep:** summary: population=2 in_progress=2 stranded=2
threshold_events=0 (850-bif2 lenovinha in flight; 831-ezea multi-cycle —
neither bulk-closed, advisory only).

**Metrics (verbatim):**
experts: calls=45 answered=45 unsupported=0 degraded=0 errors=0 answer_rate=100% tools=answer,capability-matrix,check,compact,expert_capability,expire-claims,fragments,query,ready,status source=/tmp/forge-expert-usage.jsonl
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=3 per_server=cli=44;forge-plan=1;project-info=1 legacy_untagged=0 health=ok source=/tmp/forge-expert-usage.jsonl
expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent (this host's first flow record emitted this cycle)
timing: steps=37 build_check_ms_avg=80718 litmus_ms_avg=- slowest=step:running-clippy-strict-includes-the-workspace-type-check:24722 source=/tmp/tillandsias-timing.jsonl
plan: packets=516 ready=387 plan_bin=./target/release/tillandsias-plan
verdict: attention:worktree-dirty (expected pre-commit: this cycle's fragments)

**Ledger compaction:** eligible=false fragments=18 malformed=0 — no action.
