## Cycle — yoga 2026-09-03T03:09Z (meta-orchestration, full mode; cron lane) — DIAGNOSTIC CYCLE, one p0 filed
- Start of cycle: guards ok, daily gate current, lock acquired, boundary clean at f6d6b0fbf. No packet claimed: the cycle went to a thing I had been REPORTING for five cycles without diagnosing.
- 975-rsgm FILED, then RAISED TO p0 BY MY OWN FOLLOW-UP. Preflight has said `fail:enclave-service-start-failed:service=tillandsias-proxy:action=operator` in every cycle since 20:28Z yesterday. I passed it to the operator four times. It costs ONE `podman start` to explain, and nobody ran it — because the verdict already told a complete-sounding story: a named service, a named owner, and a clean `Exiting normally` in the logs. A refusal that reads as fully-formed is one nobody investigates.
- THE CHAIN, measured end to end. (1) /tmp is cleared, so /tmp/tillandsias-ca vanishes. (2) `podman start` fails with `crun: cannot stat .../intermediate.crt` — podman recorded the bind SOURCE at creation, so it can never succeed again on its own. (3) The documented remedy, `tillandsias --init`, DOES re-materialize the bundle. (4) But the proxy mounts only the CERT from that bind; its KEY arrives as the podman secret `tillandsias-ca-key`, and the image's /etc/squid/certs is empty. (5) --init rotated `tillandsias-vault-tls-{ca,cert,key}` — all three "About a minute ago" — and did NOT rotate `tillandsias-ca-key`, still "3 days ago". (6) The proxy now starts and dies differently: `X509_check_private_key() failed`, `FATAL: No valid signing certificate configured`.
- SO THE REMEDY CHANGED THE FAILURE'S SIGNATURE WITHOUT FIXING IT, and that is the expensive property. Before: a stat error naming a path, obviously environmental. After: a TLS keypair mismatch inside squid, which reads like a certificate bug in the proxy and points investigation at the wrong subsystem. An operator who ran the documented remedy and then read the logs would be FURTHER from the answer than before starting.
- THE ACTUAL DEFECT: CA regeneration is not atomic across its consumers. The bundle lives in one place (/tmp, volatile), the private key in another (a podman secret, durable), and the code that regenerates the first does not rotate the second. /tmp guarantees the destroying event will happen; the desynchronization is what makes it unrecoverable.
- I CORRECTED MY OWN FILING two hours after making it. As filed it said "the launch path materializes the bundle; the RESTART path does not" — wrong. Service::Proxy depends on Service::CaBundle (container_deps.rs:83) and satisfy_ca_bundle re-materializes, so the ORCHESTRATED path is fine; what bypasses it is the self-heal at check-enclave-service-health.sh:319 calling `podman start` directly. And the packet as filed would have sent someone to move the CA out of /tmp, declare victory, and leave the rotation bug untouched — relocating the directory alone leaves a host that still breaks the first time anything regenerates the CA.
- DELIBERATELY NOT REPAIRING THIS HOST. Rotating the secret and recreating the container would fix yoga and destroy the fleet's only reproduction of a state that took five cycles to notice. Same rule I gave macuahuitl earlier today when it offered to rebuild its stale index: the first host to repair a broken instrument destroys the evidence. The proxy sits at Exited(1) with the mismatched pair in place, available for any experiment.
- ONE HOST MUTATION MADE AND WORTH NAMING: I ran `tillandsias --init` as the decisive experiment, which pulled images and re-materialized the CA. That is what produced step 5's rotation evidence, and it is also what moved this host from failure-shape-2 to failure-shape-6 — I cannot un-run it, and a future reader comparing yoga to an untouched host should know the /tmp directory here is post-remedy, not pristine.
- Sibling: 964-zedm, closed hours earlier, was the spec-index builder staging into /tmp and dying on a 256 MB tmpfs with 1.2 TB free. Two independent "/tmp used for something whose lifetime must exceed /tmp's" in one day suggests the ambient temp directory is being reached for as a default rather than chosen, and that a third instance is likely elsewhere in the launch graph.
