## Cycle 2026-08-23T11:37Z (windows/esmeraldinha — the alternative-runtime question answered, and the answer was not about runtimes)

- **Host**: esmeraldinha, Intel N100. `windows-next` fast-forwarded 19 commits
  `844e0e98b` -> `ab6abb327`. This cycle `origin/windows-next` WAS a verified
  ancestor of `origin/linux-next` (10 behind), so the prompt's fast-forward was
  the real move — unlike last cycle, when windows-next already contained
  linux-next and it was a no-op. Checked rather than assumed either way.
- **mcp_outage: forge-plan,project-info** — still down, third cycle running.
  All reads via the filesystem and the verified binary.
- **Instrument**: `ensure_fresh_plan_binary` rc=0 with an UNCHANGED, six-hour-old
  mtime across 19 commits — the same suspicious shape as last cycle, so verified
  the same way: `git diff 844e0e98b..ab6abb327 -- crates/` is EMPTY (the whole
  range is plan/, scripts/, openspec/). True pass. `compact` not invoked.
- **855-wrr3 criterion 5 CLOSED for the Windows side (858-tnnv residual 1).**
  An alternative IS available without layering — `llama-cpp b6153-3.fc44` is in
  Fedora. Measured against ollama on the identical GGUF blob, same 4 threads,
  CPU-only, idle box:
    ollama HTTP API            prefill ~125 tok/s  decode 28.99
    ollama llama-server bare   prefill  59-91      decode 29.52
    Fedora stock llama-cpp     prefill   9.77      decode  7.22
- **The finding is that "which runtime" was the wrong question.** Both are
  llama.cpp. Fedora's build reports `CPU : LLAMAFILE = 1 | REPACK = 1` and NO
  AVX/AVX2/FMA/VNNI on a host whose capability row lists all four; ollama ships
  17 microarchitecture ggml objects and loads `libggml-cpu-alderlake.so` — this
  host's exact microarch, read out of the live runner's /proc maps rather than
  inferred. **12.8x prefill, 4.0x decode, entirely from build packaging.**
  The tempting simplification ("ollama is a wrapper, the bare engine is leaner")
  would have been a 12.8x regression that read as "the floor host is slow".
  Filed **861-n7f5** so the trap gets a check instead of a note.
- Second trap in the same package: it is a ROCm build, defaults to `ngl 99`, and
  prints `failed to initialize ROCm` before falling back — inviting a GPU
  diagnosis for a CPU-path defect. `-ngl 0` moves it 9.71 -> 9.77 t/s, which is
  the proof offload was never involved.
- **On the ollama layer itself**: decode at parity (1.8%), wall-clock TTFT ~25%
  better bare (844 vs 1131 ms) — and that is token count, not speed: ollama's
  chat template turns a 51-token prompt into 80. Worth ~0.3 s of the measured
  2.3 s sub-query for template-free workloads like 853-6gz3's.
- **Not claimed**: ollama's per-token prefill reads 2x the bare server's on the
  same ggml and I could not attribute it — bare varied ±35% across runs where
  ollama varied ±0.1%, prompt lengths differed, and my one probe changed two
  variables at once. The recommendation rests on wall-clock TTFT, which none of
  that touches.
- **858-tnnv residual 2 still open**: no true cold-bootstrap number. It needs an
  isolated CARGO_HOME + CARGO_TARGET_DIR (so the working cache survives) and an
  hour-plus of uncontended wall clock. Not started.
- 858-ihcb's last criterion also still open: marking superseded `prefill_tok_s`
  citations where the ledger uses them as engine-lane evidence.
- Exit hygiene: both test `llama-server` instances (ports 11435/11436) confirmed
  terminated; the sanctioned dev endpoint on 11434 left up. `llama-cpp` was
  installed into the RUNTIME distro, which destructive smoke e2e unregisters —
  it will not survive one, and the finding argues against adopting it anyway.
- Compaction eligible again (fragment-count). Still not run: ledger GC is not a
  perf packet and concurrent hosts are writing.
