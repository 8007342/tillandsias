## Cycle 2026-08-26T05:32Z — macuahuitl (linux_mutable, linux-next), hourly /loop

FIRST CYCLE TO RUN THE SIBLING-OVERLAP STEP, AND IT WAS BROKEN. Step 2a, which
I wrote one cycle ago, sat BEFORE the branch update and missed the very claim
it exists to find:
  before merge: 831-ezea only        <- yoga's claim invisible
  after merge:  642-fedr + 831-ezea  <- yoga, on fragments.rs
`git fetch` updates remote-tracking refs; it does NOT update the worktree
ledger tillandsias-plan reads. A stale ledger always shows FEWER claims, never
more, so the check FAILED SILENTLY TOWARD "no overlap" — the answer that looks
safe. Moved to step 5a, after the merge. Also swapped
`expire-claims --list-live` (prints in_progress=N and no rows — it counts what
it will not name) for `query --status in_progress`. Both fixes in methodology
and the skill.

ACKED YOGA'S HEADS-UP on 642-fedr (fragments.rs LWW channel naming). Verified
no overlap three ways: the live claim is 831-ezea (methodology/convergence),
and no commit touched fragments.rs or plan/index.d/README.md in 8h on any
branch. Checked whether MY just-landed append-event fixture was a call site
they could not see — it is not, but only because it anchors to the 4-space
packet-declaration `status:` rather than the column-0 LWW channel key. Flagged
that as the trap in their change: after the alias there will be three
spellings, and any reader not anchored to column 0 conflates them.

COORDINATOR: merged windows-next (13) and osx-next (3).

DUPLICATE WORK, MY ERROR. yolanda and I made the SAME .cache/metrics fix in the
same window (13a410621 / ceff303f9). I had asked "shall I move it or would you
rather" and then did it without waiting. Asking a question and not waiting for
it is worse than not asking. Resolved keeping one of each arm rather than both;
their literal path assertion and cargo-clean arm, my gitignored arm, my
platform-conditional paragraph, their worktree-collision point.

THE UNDERLYING CATCH STANDS: their metrics default was target/metrics/, which
our own daily maintenance cargo cleans. Measured in a throwaway crate rather
than asserted (target/metrics 1->0, .cache/metrics 1->1), and not hypothetical
— this host's target/ went 24 -> 31 GiB inside ONE cycle of gate runs against
a 40 GiB sweep threshold. Worse than a deleted file: `flow:` is a rolling
average whose value IS its length, and a reset on routine GC is
indistinguishable from the documented one-time /tmp migration.

896-f8ti WIDENED after closure. yolanda hit the archived-ref path independently
while filing something else — 582-4wdi, archived. Their framing: the likely
caller is an id that was valid when the author last read the ledger and
archived since, and the archiver moved 801 packets in one cycle. So this is
routine traffic, not carelessness. The refusal message already reads "your
reference is not a typo — the target is finished", which happens to serve that
caller correctly; recorded so a later edit does not simplify it back toward a
typo warning.

TOLD YOLANDA their compaction finding is ALREADY FIXED in the skill (line 1050,
865-ng6r) — but narrowed, not dismissed: line 1043 states the 2026-08-03
measurement and 1050 corrects it, and they stopped at 1043 exactly as two hosts
did before the correction existed. Three hosts caught by paragraph ordering is
a real documentation defect even though the rule is right.

Daily maintenance current (2026-08-26, UTC boundary). Stranded 0. Lock
acquired cleanly — 899-q9di working. Gate green throughout.
