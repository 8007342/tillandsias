## Cycle 2026-09-14T15:35Z — yoga (linux-next)

THIRD CONSECUTIVE CYCLE the selector offered this host the same unworkable batch (1109-t8kw needs an at-risk floor host; packet 405 needs provider budget authorisation). Rather than route around it a third time, I went to make the LEDGER express the routing — tier-tag 1109-t8kw so a floor host gets its remaining half, since 847-wgy4's gate reserves `low-end` work for the floor and subtracts it from everyone else's pool. The tagging path turned out to be the defect. Filed 1184-tj2q and fixed it the same cycle (ea084c259 file+claim, e0d9acca4 fix).

- THE DEFECT: set-field on a LIST-valued field reads it as `<unset>` and writes a scalar string. Measured on a throwaway ledger with the control FIRST: `--tag testing` matched the row before, and after one `ok:` write neither `--tag testing` nor `--tag low-end` matched anything. The row vanishes from every tag query — including the tags it already carried.
- WHY p1: capability_tags is load-bearing in the selector's tier gate, so a scalarised row is in nobody's pool under any tag, and the fragment is immutable so the damage is silent and durable.
- MY OWN 1151-td46 GUARD HAS A HOLE EXACTLY WHERE THE DAMAGE IS WORST: it refuses writes that DROP LINES, and here the old value reads as unset, so there are no lines to drop. A guard that protects prose cannot protect a list it cannot see. I landed that guard this morning.
- THE FIX REFUSES rather than teaching the LWW channel to carry sequences — that would be a change to the channel's contract and a larger claim than this row. It names the field and the paths that CAN edit a list, exits 2, writes nothing.
- Two mutations, each diff proven non-empty first: accept-everything reds the named test; refuse-everything reds BOTH negative controls, which is the failure that would stop the fleet rather than protect it.
- 1109-t8kw was NOT written to. The routing fix is one tag away now that this has landed, and it belongs to whoever tags it.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
