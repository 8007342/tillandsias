## Cycle 2026-09-15T23:39Z — macuahuitl, 4h meta cron (full): 1213-rbt9 criterion 2, the five security items against trunk

daily-maintenance ok:daily-maintenance-current:2026-09-15; credential ok:gh-keyring-push-verified; lock held; boundary
snapshotted and verified. Claimed 1213-rbt9 before the work and released it after, because the remaining depth needs a
macOS host rather than more reading — a claim held open on work this host cannot do is the stranding 641-e2qa is about.
TRIAGE RESULT: one of five materially CHANGED at trunk, and the change runs the unexpected way. The published RED says the
proxy's permissive second port listens "on every interface"; trunk binds it to 127.0.0.1, with the config comment
recording that 3129 appears in no consumer so narrowing broke no caller. The bypass survives, the reachability does not.
AN OVER-PESSIMISTIC PUBLISHED CLAIM IS ALSO A DEFECT ON AN HONESTY SURFACE: a page that overstates a fixed weakness spends
the credibility that makes its unfixed ones believable, and an outside reader who checks that one and finds it narrowed
has been handed a reason to discount the other thirteen. Two appear to stand (rsa:2048 still generated; the fallback share
file still joined onto the cache dir), one needs its NARROWER claim tested rather than its summary — install.sh does
verify a SHA256 checksum, while the site's actual claim is about the channel, the Linux continue-without-it, and cosign —
and one, the macOS signing allow-list, was NOT checked and is recorded as untouched rather than cleared.
Contributed two eliminations to yoga's escalation without claiming it: gate-stamp enumerates every tracked file with only
the plan fast lane skipped, so a crates/ bump invalidates any earlier stamp and adoption is out; and hooksPath being unset
is NOT evidence a hook did not run, since install-hooks.sh writes .git/hooks/pre-push and only the forge uses hooksPath —
a check I nearly got backwards from the website repo, where the opposite convention holds.
tokens: cycle=mo-full-20260915T2339Z main_ctx=168000 subagent_tokens=0 agents=0 by_model=none.
