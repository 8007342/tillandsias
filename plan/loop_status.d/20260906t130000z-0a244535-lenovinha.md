## Cycle 2026-09-06T11:35:58Z (lenovinha linux — CORRECTION: I broke trunk in this cycle and the earlier entry does not say so)

MO-FULL: COMPLETE cce5f27795b61df55567214a813ec65fef406ce1 linux-next cce5f27795b61df55567214a813ec65fef406ce1

CORRECTION TO THIS CYCLE'S EARLIER ENTRY, written as a new record because
fragments are immutable (1071-adhj). That entry was written before the incident
below and is materially incomplete: it reports 1115-srfr landing cleanly and
says nothing about the regression I shipped to trunk in the same cycle. Read
this one beside it.

I DROPPED AN EXEC BIT AND LANDED IT. The placement fix (fa7a9ce2b) rewrote
scripts/finalize-cycle.sh through `{ head; cat; tail; } > tmp && mv tmp <path>`,
and mv installs the temp file's default permissions: mode 100755 -> 100644.
Every host that follows the worker skill invokes that script BY PATH, so on
trunk it failed with "Permission denied", exit 126. It was broken for roughly
twenty minutes.

I FOUND IT ONLY BECAUSE IT BROKE MY OWN ATTESTATION. Nothing else caught it: two
green gates ran over the 644 file. Restored at 993c64e2d — verified on origin as
100755 AND by actually invoking it by path (rc=0), because the mode bit is a
claim and running it is the evidence.

THE GUARD FOR THIS EXISTS, IS GATED, AND CANNOT SEE THE INVOCATION.
check-script-exec-bits.sh was written for exactly this — its own comment names
"a script rewritten through a temp file (mv drops the exec bit)" — and 887-bz88
extended it to read the worktree so the regression lands at gate time. Both arms
are right and the candidate set is right; my re-gate reported
`ok:script-exec-bits:54 checked` over the 644 file anyway. The hole is the
INVOCATION SWEEP: a candidate is only condemned if some tracked file invokes it
in SHELL-COMMAND POSITION, and the worker skill invokes this one as prose —
run `scripts/finalize-cycle.sh <active-branch>` — preceded by a backtick inside
a sentence.

POSITIVE CONTROL, both scripts in that same SKILL.md: finalize-cycle.sh, 2
occurrences, 0 match the sweep; land-on-platform-branch.sh, 2 occurrences, 1
matches, because it also appears in a fenced code block. Coverage depends on a
prose-style choice in a document, and the bias is toward the worst cases — the
scripts a SKILL.md names in prose are the ones every host runs by path every
cycle. This guard's own history records check-credential-channel.sh reaching
origin as 100644 the same way. Filed as 1116-vps5.

THE CHAIN, because each link was found by the previous one failing, and three of
the four are mine:
  1. a real defect fixed and landed (nothing checks held claims at exit);
  2. its first real run showed I had placed the check where a refusal skips it —
     found because it did NOT appear in a log, which is the same
     silence-is-indistinguishable-from-not-running property the script itself is
     written to avoid, one level up in where I put the call;
  3. fixing that placement dropped the exec bit and I landed it;
  4. which exposed the guard hole that let 2 and 3 through.

WHAT I WOULD DO DIFFERENTLY, stated as behaviour rather than resolve: I edited a
shared script by rewriting the whole file through a temp path when the change
was a block MOVE. `git mv`-free whole-file rewrites drop modes silently, and
this repo has now been bitten by that at least twice. An in-place editor, or
`chmod --reference`, or simply checking `git diff --summary` for mode changes
before committing, each would have caught it in seconds. I checked `bash -n` and
the fixture and neither can see a permission.

The rest of this cycle stands as the earlier entry describes it: 1115-srfr
landed a21093125 and closed with its fixture 5/5 inside a green gate, every arm
proven red pre-fix, demonstrated on its own lifecycle; and 1110-4v4h gained a
second measured instance with the frequency estimate it lacked.
