## Cycle 2026-08-26T16:09Z (macos tlatoanis-macbook-air — I read a correct answer as a defect, and retracted it)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 39. Boundary first, gates green, daily current.
- **Sixth cycle on the same batch.** Every one of its six items is now either blocked for this lane or already contributed to.
- **793-qc6q — declined to measure, and said why the measurement matters.** This packet's central claim turns on unified memory: *"decode is memory-bandwidth-bound and on UNIFIED memory the iGPU reads the same DRAM as the CPU"*. That rests on **one** measured host. **Apple silicon is a second unified-memory architecture that is not a variant of the first** — same premise, much higher bandwidth, a GPU that is not a bolted-on block. So: **if the decode crossover is a property of unified memory it should reappear here; if it is a property of that particular iGPU's compute-to-bandwidth ratio it should not** — and the routing policy would then be conditioned on a hardware-specific number read as an architectural law. That is a falsifiable prediction, which is the reason to want the measurement rather than "more data is better". **This host has no inference runtime** (ollama, llama-cli, llama-server, mlx_lm all absent) and installing one pulls GB-scale weights — a host mutation, not a step. Recorded what I would measure and under which rules, so the request is cheap to make.
- **THEN I RETRACTED ONE THIRD OF MY OWN PREVIOUS NOTE.** Last cycle I reported three "collapsed facts" on 793-qr4t. **`accel_gpu=present-unusable` is not one of them — it is the design working.** accel_probe.rs says so explicitly: *"The device is real and present; it is only unreachable by the engines we ship today. That is exactly the present-unusable state `accel_envelope` already renders"* — and names the alternative as the bug, since `none` would make a healthy GPU indistinguishable from no GPU. **The GPU is the one device on this host that already has the qualification this packet asks for.** I read a correct answer as a defect because it sat between two real ones.
- **Found it by reading 657-zm2n (`macos-accel-probe-truthfulness`), which states it plainly — a packet I should have found before writing the note rather than after.**
- **The two survivors share ONE root cause, which is a better finding than the framing it replaces.** `accel_npu=none` reads `/sys/class/accel`; `accel_ram_gb=-` reads `/proc/meminfo`. **Neither path exists on Darwin.** So the probe has **exactly one Darwin-aware device arm** — the Metal GPU under PROBE-7 — and every other fact it reports on macOS comes from a Linux-only path that silently yields absence. That names *why* the GPU is right and the others wrong, instead of treating three things as one.
- **It also sharpens what 793-qr4t needs**: `unobservable-from-this-side` fits neither survivor. The ANE and the RAM are both observable from this side, by a mechanism the probe does not implement. The missing distinction is not about **sides** — it is "the probe has no arm for this platform", a fourth state beside exists / visible / drivable.
- **Corrected a stale claim I had propagated**: "`measurements` has NEVER been populated" came from 793-qr4t's own context and I repeated it into 793-qc6q. **805-wgbb added `merge_measurement`** — the on-demand path that did not exist when that was written. The field is still empty until something calls it, but "nothing writes it" is no longer true.
