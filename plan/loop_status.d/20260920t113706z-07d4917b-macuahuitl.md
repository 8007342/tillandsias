## Cycle 2026-09-20T09:20Z→2026-09-20T11:30Z (linux — macuahuitl, coordinator: the v56.9.19.2 promotion fully evidenced by three post-promotion STABLE-channel smokes, all PASS on the unpinned default path; one wrong diagnosis of mine corrected by the fleet in four slices; three rows filed, two closed)

Direction ("We're giving forge agents local EXPERTS"): served by keeping trunk integrating through 24 lands and levels, by routing every macOS gate red to the host that could measure it, and by three rows that make the instruments say where they ran and what they could not measure (1299-s2sv, 1300-q7eq, 1301-ie39). The first Linux forge lane to run to completion on either v56.9.19 tag happened this cycle (pirria, stable channel).

DONE, each verified on origin: POST-PROMOTION STABLE SMOKES of v56.9.19.2, all three PASS on the unpinned /releases/latest path — Windows yolanda 491dce2b2 (relayed d8b1a7680), macOS macneo 54fc85cdb (relayed 7e33445f9; the first macOS run of the default operator path, 1280-58kq's criterion's first live test), Linux pirria 993d8a4e5 (§2 cleared the subuid-owned vault-data unattended, 1284-jf86 holds; §4 forge lane COMPLETED, 8m36s, opencode_exit=0, zero rate-limit lines, ending in the correct cold-host stop blocked:upstream-no-credential); routine smoke back on the daily channel. README v56.9.19.2 row edited (PROMOTE-EXISTING): the three passes recorded and the cut's two known reds marked RESOLVED — the Console rate limit lifted between 2026-09-19T22:05Z and 2026-09-20T09:30Z; the keyring crash is upstream (gnome-keyring #195) with both Locked probes removed (lenovinha). 1268-m2ir CLOSED by yoga (ceb013146) after this host answered the one-command check from its own transcript: gate #1's stray timing records came from a detached temporary worktree (mktemp -d + git worktree add --detach + litmus at 2026-09-18T21:16:53Z); the row's title ("cwd is the checkout") was my wrong premise, corrected in yoga's closing event. 1273-4mak landed (yoga, b740fb1f2): the release acceptance smoke verifies a signature and could-not-run is never a pass. 1282-rkkm CLOSED by macneo (959181906): nine green observations incl. the racing arm 0 red of 6. Relays/levels: 61cc60910→2c6434a26 level (stamp minted), 959181906 (ff), 3a8869c3a, 6b68420ed (LAND 23: macneo's 1300-q7eq v2), da8bf5c8a (osx-next plan-only: 1301-ie39 + a macneo event), 993d8a4e5, 66e2aa860 (yoga's land of v4), LAND 24 ok:land:b843b238f:attempt-1 (pirria's 1190-swen criterion fix + the README row), then macneo's close of 1300-q7eq relayed from osx-next.

FILED: 1299-s2sv (p2, ready, unclaimed) — a timing record carries no root field, so a fallback record's origin was answerable only from one host's transcript; own row per yoga's reasons (schema change with many readers, ~124k records without it, the fallback record is the half that matters); four-arm pin, pre-fix jq -r .root prints null on the preserved stray records. 1300-q7eq (p1, macneo, closing) — both macOS gates red 4-of-5 on 1269-gfdi's gate step 414. 1301-ie39 (macbookair) — the no-python policy checker matches spellings, not the runtime: scripts/derive-vsock-seccomp.sh (shipped in v56.9.19.1) runs python3 in the guest past it; the guest has no perl or jq, so the derivation moves into headless in Rust and the script + fixture are retired in the same commit (decided; macbookair implementing under 830-xsk2).

CORRECTION OF MY OWN DIAGNOSIS (1300-q7eq): I filed the cause as the regime probe reading /proc/loadavg and nproc from the passing host's SOURCE. macneo refuted it from the failing host's ARTIFACT: the subject skips arms 4-7 on any host without a toolbox, the margin arm is arm 7, so the regime line ARM 0 demanded can never print on a Mac whatever the probe reads; ARM 0 asserted a precondition it never checked (yoga's fixture, wired that morning, green on the Linux regime only). My 5/5-on-a-Mac criterion was unachievable honestly and macneo refused to loosen arms; restated as 3/3 with 3 skipped by name on a toolbox-less host. nproc is present on BOTH Macs via Homebrew (6 and 10), so my "absent on stock macOS" premise was wrong twice. The row's title and first two events carry the wrong cause; the correction and relay events, and macneo's closing event, carry the pointer. Four slices: v2 (ARM 0 accepts the named toolbox skip; LC_ALL=C on the sysctl load read — macbookair measured `{ 1,58 1,69 1,73 }` under fr_CH and the case guard rejects a comma value, so the pin is load-bearing; a toolbox-independent ARM 4 inducing the comma locale; a pipefail `locale -a | grep -q` false skip fixed, 1076-kft9 class), v3 (skip reasons accumulated; bare-metal Linux REFUSES both subject skip strings — the discriminator is container-ness via /run/.containerenv, /.dockerenv and TILLANDSIAS_HOST_KIND, NOT uname, because forge lanes are Linux without a toolbox), v4 (the aggregate tag DERIVED from which skip fired; separator). yoga verified the two paths a Mac cannot reach (refusal on both anomalies; named skip in the builder, induced because a nested toolbox run succeeds there); macbookair confirmed ARM 4 on the native fr_CH locale (3/3, 3 skipped); the two Linux gate lines are on the row (land 23's 5/5 with the v2 hardcoded "no toolbox" sentence as the pre-fix control; yoga's v4 5/5 with "(1 skipped: no comma-decimal locale)"). yoga's 414 advisory slice was never pushed and was dropped; macneo's fix was the unblock.

pirria's self-found defect (1190-swen): their §4a-cold criterion grepped ^MO-FULL: generically and flagged a correct BLOCKED attestation as a finding — a proxy (a marker exists) for the property (a cycle claims to have finished), validated only against runs where the agent died before attesting; fixed (COMPLETE and BLOCKED read apart, a BLOCKED marker is not a finding), recorded as an event, relayed via salvage ref in land 24 rather than a 45-minute floor-host gate.

LANDING-WINDOW LESSON (mine): yoga's 65-line v4 burned two full gates to three integrate races; two of the three trunk moves were my own non-urgent pushes made after I gave them the go. A go is a landing window: hold every own trunk push until the peer's SHA lands.

FLEET STATE: macuahuitl land 24 gating (pirria's fix + README), then level. yoga idle at 66e2aa860. macneo closing 1300-q7eq; then 1132-r4mt (C3 parked). macbookair 830-xsk2 (Rust port of the seccomp derivation with 7 tests, retiring the script; waits on the osx-next level to land). pirria 1286-4437 Linux arm (claimed, starting). lenovinha 1288-5qpn step 2 (no report this cycle). yolanda, esme idle (Windows). No host reported blocked.

OPERATOR: nothing is blocked. Notable for you: the Console rate limit that killed every Linux forge lane on 09-19 has lifted; the first complete Linux forge lane on the v56.9.19 series ran on pirria's stable smoke. v56.9.19.2 STABLE now carries three passing stable-channel smokes in its README row.

tokens: subagent_tokens=0 agents=0 (no sub-agents this cycle; all reads through the MCP experts and the object store)
