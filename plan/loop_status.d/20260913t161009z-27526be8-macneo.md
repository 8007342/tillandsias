## Cycle — macneo advance-work-from-plan (cron de51113a, :40 lane)

Landed fb811c291, attempt 1, verified as an ancestor of a freshly-fetched
origin/osx-next with a negative control the test can actually refuse (the
deleted salvage tip, not contained).

Direction: stable-milestone-v1. Seed
macos-unknown-apple-tlatoanis-macbook-neo-20260913, size 6 budget 6.

CLOSED 1127-xm3m, a second ready-but-landed row, verified from an EXISTING green
gate log with no rebuild — so the closure stayed inside the cargo-check-only
rule. check-stale-ready-rows.sh had flagged it as
stale-candidate:1127-xm3m:1:04cb49cd1 on its cites-the-order pass and the
citation was real, but citing is not verifying.

THE CONTROL TOOK THREE TRIES and each failure looked like an answer. The live
crashloop.state still carries this packet's damage, frozen 46 seconds before the
fix commit, and no gate since has touched it — worthless alone, because a test
that never runs writes nothing either. Attempt 1 read a land-gate log that had
refused early on the citation guard and never reached the test phase. Attempt 2
concluded the gate never runs macos-tray tests, killed by noticing every crate
name scored zero in that same log including crates certainly built — measuring
the log's verbosity, not what ran. Attempt 3, the completed green gate, carries
crate names, test-result lines, and all four guarded tests ran and passed.

Corrected mid-verification: stat's %Sm prints LOCAL time and a trailing Z in the
format string is literal text, so the mtime read eight hours off until it was
decoded from the record's own written_at epoch field.

FAST-FORWARD HAZARD (1152-y3bv) handled as instructed: merged trunk with --no-ff,
and the predictor immediately before the push listed only my own two commits.
The branch was in the triggering state at cycle start (zero commits trunk lacks),
so a plain merge would have fast-forwarded.

Keychain: no wedge. Probe rc 0, no orphaned decrypts, no SecurityAgent.

Sub-agents: 0 of 4 budgeted.

tokens: ~75k this cycle, single agent, no fan-out, no Workflow.

verdict: attention:experts-never-called — accurate, plan experts report
source=absent on this host.
