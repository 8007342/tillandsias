## Cycle — macuahuitl, fleet binary survey for 2026-09-06T00:16Z

FIVE OF EIGHT HOSTS WERE RUNNING A PLAN BINARY THAT WRITES. Measured by
each host running scripts/test-expire-claims-write-is-opt-in.sh, not inferred:

  macuahuitl    16/16   (only after reinstalling; would have been 5/16)
  yoga          16/16
  macbookair    16/16   no PATH copy exists — structural, not virtuous
  lenovinha      5/16 -> 16/16 after scripts/cycle-preflight.sh rebuild
  macneo         5/16
  pirria         5/16   and cannot fetch: missing:no-credential-channel
  esmeraldinha   5/16   AFTER reporting itself clear on my source check
  yolanda        5/16

No host had invoked a bare `expire-claims`; every one verified that rather
than asserting it. macbookair's 14:49Z release of macneo's 980-ja2m lease is
the only known instance and is what 1067-24q6 was filed on.

THE COORDINATOR SHIPPED THE INSTRUMENT DEFECT. I broadcast
`git merge-base --is-ancestor b98f2f9a7 HEAD` — a SOURCE check for a hazard in
the BINARY. lenovinha showed that pulling flips it green with the installed
binary unchanged, so the host that takes the warning seriously and acts on it
is the host told it is safe. esmeraldinha then reported clear on it and was
5/16. Corrected to all six recipients. Recorded on 1079-qb8k.

FOUR FURTHER TRAPS, each found by the host it bit:

  1. Fetching the fixture to a scratch path empties its $ROOT, the plan-binary
     probe fails to source, and it prints SKIP and EXITS 0 — on a fully
     exposed host (macneo). Run it from the checkout root; treat SKIP as a
     failed check, never as an absent hazard.
  2. A host that merged trunk RECENTLY is more likely to report a false
     all-clear, because the source flips green immediately and the artefact
     lags until something rebuilds it (esmeraldinha).
  2b. And the same trap aimed at the REMEDY, not the diagnosis: a trunk merge
     closes the source exposure and not the binary one, and NO ORDINARY CYCLE
     REBUILDS THE PLAN BINARY. I told yolanda to merge and do nothing; that
     advice was false (yolanda).
  3. 5/16 is not partial safety. The fixture's own header records that arms 7
     and 8 pass pre-fix only because `--write` is an UNKNOWN FLAG there. Two
     readers say they would otherwise have counted 5 as protection.
  4. `TILLANDSIAS_PLAN_BINARY="$(command -v tillandsias-plan)"` expands to
     EMPTY on a host with no PATH entry; the fixture falls back to its default
     and prints a TRUE 16/16 for a question nobody asked (macbookair). My
     command could not distinguish "I tested what you named" from "what you
     named does not exist".

WHY THE FIXTURE IS THE RIGHT FLEET PROBE (yoga): a stale checkout does not
HAVE it, so a behind host gets a loud "no such file" rather than a green run of
an old fixture testing the old contract — unlike `--dry-run`, which exists in
both versions and means different things in each. It is right on both
diagonals.

MACOS-ONLY GATE RED, FIXED AND LANDED at 756945a66. macbookair's
./build.sh --check was 11/1 at test-trace-coverage.sh while Fedora was 12/12 on
the same commits. BSD `wc -l` right-aligns to width 8 on stdin, command
substitution keeps leading spaces, so TOTAL_ACTIVE_SPECS held "       2" and
the JSON rendered `"total_active_specs":        2,`. COVERAGE_PCT comes from
$(( )) and was clean, which is the asymmetry macbookair could observe and not
explain. Stripped at the source rather than loosening the arm: the alignment is
an accident of the platform, a tolerant pattern would have left the field
rendering differently on two platforms for every consumer, and the strict arm
is the only reason anyone learned it was platform-dependent. Verified by
macbookair on macOS, 11/1 -> 12/0, and the mechanism measured directly there.

LANDS THIS PASS, all attempt 1: cf9f35ea7 (lenovinha's 1071-adhj relay),
a13009510 (esmeraldinha's gated arm-2 fix + the artefact-scan methodology
rule), 756945a66 (the macOS fix). The arm-2 calibration tally is now 2 failures
in 7 gate runs — the green run with the pre-fix arm in place belongs in the
denominator and is the one most likely to be dropped.

OUTSTANDING: macneo's 1074-96z9-adjacent patch relay (both parts and all
hashes in hand, assembly not started); 1085-g52w still an unclaimed p1.
