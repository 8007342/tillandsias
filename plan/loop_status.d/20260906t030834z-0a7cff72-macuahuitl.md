## Cycle — macuahuitl hourly coordination (cron lane)

SIBLINGS: windows-next +3, osx-next +2, both merge-tree CLEAN, both merged.
Carried yolanda's 964-9yyp close and macbookair's 829-dkuc claim. All four
branches level at exit. main behind 561.

SWEEP: live=8 unclaimed=1 expired=0 held=0. Guard activation orphan=0.
Binary currency: build-id == source-revision.

ONE LAND THIS PASS, carrying three held records plus both sibling merges —
the land_batching rule landed by this same land. Relays went in immediately
because hosts were waiting on them; my own records batched.

THE FOLD CAUGHT UP WITH YOLANDA MID-CYCLE: 964-9yyp read `ready` here until
their close merged, then `completed`. That lag is exactly what their packet
is about, observed on the coordinator's own instrument during the pass that
merged the fix.

TRIAGE OF THE 22 DONE — 19 wire / 2 fix-first / 1 false entry. Measured, not
read: every entry executed with a timeout, duration and exit code recorded.
TOTAL RUNTIME FOR ALL 22 IS 5.4 SECONDS, which retires the cost argument for
excluding them; 748-tkjx's reasoning about slow gates is right and does not
apply to sub-second greps. Full disposition on 1087-h2z9.

THE YIELD IS THE TWO REDS, invisible to every landing host today:
  check-cheatsheet-refs.sh    3 broken refs of 577 — two in the Windows
                              cheatsheet, one pointing at a docs/ path
  check-dead-env-branches.sh  dead=29 unassigned=190 of 491 — its own slice
And one FALSE declaration: check-version-monotonicity.sh does not exist and
is named only inside local-ci's missing-guard ERROR MESSAGE, so the harvester
matched text that describes rather than text that acts. Baseline is 21, not
22, and the ratchet does not catch a declaration that never named a real file.

OPERATOR QUEUE, unchanged and all three still open:
  - restore pirria's PUSH credential (throughput; fetch works, five packets
    relayed off that host)
  - authorise the floor-tier destructive smoke — closes 1004-pg9p AND
    discharges the observation 1026-ps4n is owed, on the host both were
    measured on
  - provision a dedicated smoke host; no release can be e2e validated without
    one, and the only Silverblue hosts are the operator's own machines

ASSIGNMENT BOARD: yoga has the pty_handler release red. lenovinha holds
1087-h2z9 criterion 4. esmeraldinha has 1030-i2p8 landed and 1085-rn8u
unclaimed. macbookair claimed 829-dkuc. yolanda idle after closing 964-9yyp
— routing the cheatsheet-refs red to them, two of its three broken links are
in the Windows cheatsheet they own. pirria idle BY CONSTRUCTION pending the
operator's authorisation. macneo relaying via macbookair.
