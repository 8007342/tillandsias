## Cycle 2026-08-18T23:23 PDT→00:00 PDT (windows — YOLANDA, MO FULL: overnight loop cycle 6, 827-h4tz CLOSED — premise falsified)

**Result: the tray test suite is fully green for the first time this session (94/0), and the `stale:windows-sources-unverified` advisory that appeared on every gate run tonight is cleared.**

- **Host**: YOLANDA, `windows-next` 1fcbf4e50 → 957262646 (claim) → this cycle. Preflight `ok`, guards credential `ok:gh-keyring` / branch `ok:branch-windows-next` / expert-base `ok:expert-base-ready`, opsx `ok:clean-tree`. Boundary `/tmp/meta-orchestration-boundary.N9d4tz`. Sibling heads: main=0bba6525f, linux-next=6de002620 (merged), osx-next=841ad4b43.

**827-h4tz's CENTRAL PREMISE DOES NOT HOLD.** The packet says the crate's owning host "cannot satisfy the crate's own staleness guard" and calls it structural, carried three cycles on that basis. `scripts/build-guest-binaries.sh` already has a wired `build_with_cargo` fallback; both musl targets are installed in `tillandsias-build`; and the aarch64 arm is covered by the script's own `elif command -v clang` branch, with clang present. Two of the packet's three measurements are false.

**The likely reason the original reading differed is worth keeping:** `command -v rustup` and `command -v cargo` answer ABSENT under a bare `bash -lc` in that distro (a PATH-init quirk) while rustup itself works fine — the same *infer-capability-from-presence* trap `build-guest-binaries.sh`'s own header warns about for nix. The staleness was an unexecuted step, not an impossibility.

**Ran end to end:** guest binaries built and verified against VERSION 0.4.260817.1 (loudly "DEGRADED to the local Cargo fallback", as designed); tray staged the host-arch binary (14,239,064 bytes, was 13,567,216 from Aug 15) and built; `cargo test -p tillandsias-windows-tray` went 93/1 → **94 passed, 0 failed**; stamp taken.

**A GUARD CAUGHT ME TAKING THE SHORT PATH.** `check-windows-only-sources-verified.sh stamp` refused with `refused:stamp-needs-evidence: … a stamp with no transcript is an assertion, not a verification`. Re-run against the captured cargo output → `ok:windows-sources-verified:4`. Exactly the behaviour that guard exists for, recorded because it worked.

**The packet's open question is answered:** "must the staged artifact match the release bit-for-bit, or merely the wire revision?" — merely the version. The guard asserts only that the embedded bytes CONTAIN the workspace VERSION string, so byte-identity with the Nix release was never the contract and remedy (a) carried none of the risk the packet feared.

**NOT DONE, carried:**
- **The residual is real and filed as a note**: nothing runs the refresh automatically, and both the asset (gitignored, `.gitignore:98`) and the stamp (outside the repo) are host-local. So every host, after every VERSION bump, re-enters this state until someone runs the two scripts by hand. The *loud* half already exists — `build-windows-tray.ps1` refuses a staged binary older than the checkout (order 689-gipe) — so nothing ships silently. The cheap fix is for that script to RUN the refresh rather than only refuse, but that edits the release lane's build script and whether CI may fall through to the cargo path is a release-integrity question, not a Windows one.
- Queue `829-p7wq`: entries 1-5 are now drained. Remaining: entry 6 (ledger compaction — wants a full window and siblings quiet) and entry 7 overflow (643-64bx, 628-c7qd, 628-r2vk). Also open by operator decision: whether to delete `argv_survives_wt_reparse`/`wt_safe_title` or keep them and make a violation loud (823-u5zf's remaining closure), which needs an attended lane observation either way.
