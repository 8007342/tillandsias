## Cycle — lenovinha 2026-09-04T15:56Z (advance-work; cron :05) — 1018-5f5a closed: every [FAIL] now carries rc
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T15:35:14Z`:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
      flow: cycles=8 avg_completed_per_cycle=0.13 avg_commits_per_cycle=1.75 overhead_ratio=14 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      timing: steps=27022 build_check_ms_avg=138336 build_check_mix=mixed:forced=362,memoised=89 litmus_ms_avg=13453 slowest=litmus:tool-materialization:304387 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      repeat: window=since=2026-09-04T15:35:14Z steps=123 top3=litmus-suite=4,build-check=2,build-preamble=2 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      recur: window=7d runs=24884 steps=428 top3=build-check:runs=322:total_ms=47180157:avg_ms=146522:fail_pct=31,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=223:total_ms=7469805:avg_ms=33496:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=223:total_ms=5564741:avg_ms=24953:fail_pct=0 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=58 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=223:avg_ms=33496:fail_pct=0:saved_ms_upper=7436309,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=223:avg_ms=24953:fail_pct=0:saved_ms_upper=5539788,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=220:avg_ms=19512:fail_pct=0:saved_ms_upper=4273312 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=697 ready=452 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=dirty traces=current
      verdict: attention:worktree-dirty
- THE FILED SCOPE WAS WRONG IN BOTH DIRECTIONS, and measuring it first is what made the fix right. It named lines 441/1034/1421 as printing neither rc nor stderr: 1421 ALREADY named exit_code, and FIVE sites were unnamed. The real step-exit arms are the litmus-rust check, the success_pattern miss, the expected_behavior miss and strict-exit — all four now carry `rc=<n>` ON THE [FAIL] LINE, including the one that already had it in its body, because a reader or a tool scanning for a verdict reads that line. Uniform grammar is what lets "no rc" mean "the runner did not know".
- THE STDERR HALF WAS ALREADY SATISFIED, not skipped for cost. Steps are captured `>"$step_capture" 2>&1`, so stderr is MERGED and already printed in full. There is no separate stream to tail without splitting the capture, which would reorder interleaved output — a real cost for content already reaching the reader. rc alone was the missing half, exactly as macuahuitl predicted.
- AND THAT EXPLAINS TODAY'S TWO HIDDEN CAUSES PRECISELY. Cause 5's step printed one line because THE STEP emitted one line; the runner printed what it got. Cause 8 lost its case names to the step's own `| tail -1`, discarded BEFORE the runner saw them. Neither is fixed by printing more of what the runner has; both are fixed by the number the runner already knew and did not say.
- THREE SITES ARE rc-EXEMPT WITH INLINE REASONS: the spec/test-level reporter (caller passes a message, no exit in scope) and the two gating_points arms (regex over COMBINED output, not a step exit). The marker is the deliberate part — a new [FAIL] site must be CLASSIFIED rather than default to silence — and the fixture keys on the marker, not on line numbers, which drift.
- FIXTURE 5/5: arm A behavioural (the three shapes really produce 1/141/124 through the runner's own execution line), arm B the static invariant, arm C the CONTROL — the rule run against the pre-fix runner FAILS, so it has teeth.
- MY OWN MISTAKE IN THE FIXTURE, same class as the packet: arm A's first draft used a SINGLE-LINE 4 MB producer into `grep -q`, measured rc=0, and reported "141 untested on this host". Wrong, and I had measured why four hours earlier — GNU grep matches line-at-a-time and DRAINS a single long line, so only a multi-line stream reproduces 141. I nearly shipped a fixture declaring a property untestable on the host where I had pinned it 20/20 the same day.
- LIMIT STATED: arm B is STATIC, not end-to-end. Driving the real runner needs a spec registered in litmus-bindings.yaml, and a fixture that edits a shared registry (or rebuilds a temp PROJECT_ROOT with every sourced dependency) costs more than it proves. The seam is named in the fixture header; true end-to-end coverage wants a permanently-bound always-failing spec, which is a different packet.
- FIRST CLOSURE UNDER 1024-c3h3'S NEW RULE: landed FIRST (97fac228b), verified `git merge-base --is-ancestor` against origin BEFORE writing the evidence event, then cited. This morning I would have cited the pre-rebase local — four of four such refs were unresolvable on trunk.
- PERL WARNING, THIRD TIME TODAY AND MINE: multi-line `perl -0pi -e 's|...|...|'` corrupted a file's header again, this time the new fixture's shebang block. I filed this warning for the fleet at 11:49Z and then hit it twice more myself. Use the editor's exact-match replace on this repo; I am the evidence.
- VERIFIED: runner healthy end-to-end (accel-capability-probe 2/2, Status [PASS]); gate green 250s.
