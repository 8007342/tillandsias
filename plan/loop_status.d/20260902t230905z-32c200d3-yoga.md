## Cycle — yoga 2026-09-02T23:09Z (meta-orchestration, full mode; cron lane)
- Start of cycle: preflight ok (tillandsias-proxy still down — operator, third cycle); daily gate current; guards ok; lock acquired (cron lane); boundary clean at dd83c62d4; 2 live sibling claims, neither on my surface.
- Batch: epic=forge-local-experts-milestone seed=yoga size=10 budget=10, urgent=build-check-unrunnable-in-forge-ruby-absent. Took the p0 urgent head only: 965-sxec. Claim pushed BEFORE work (eb6a9b5c0). COMPLETED.
- 965-sxec was the forge-wide filing blocker I flagged from the outside last cycle, now fixed from the inside. TWO layers, and the original report named one. (1) `command -v ruby` is not a usability test in a forge: the image ships no ruby and puts a brew SHIM on PATH under that name, so the probe succeeds, the install fails by design (attestation required, no GitHub credential permitted in a forge), and it exits 127 — which misses build.sh's rc==3 could-not-run branch and falls to the `-ne 0` arm asserting "the plan archiver would CHANGE THE READY SET". A substantive ledger claim from a command that never executed. (2) Found while testing, NOT in the report: `source ensure_toolbox.sh` under `set -e` killed the script with rc=1 before any ruby probe could run, so a broken toolbox produced a different uninterpretable exit for the same condition.
- FIX: _ruby_usable() probes BY EXECUTION and records WHICH lane answered (native / toolbox / none), memoised for the process lifetime; _ruby() gates on it and DISPATCHES ON THE RECORDED LANE rather than re-testing PATH. That detail is load-bearing — testing PATH at the call site and the toolbox in the probe can disagree, and a forge with a shim named `ruby` plus a dead toolbox would take the native branch and exit 127 again. The gate lives INSIDE _ruby so none of the three call sites can forget it (the 704-zcgi discipline). ensure_toolbox is now sourced `|| true` as the accelerator it is, with its stderr kept as the diagnosis.
- MEMOISATION IS NOT AN OPTIMISATION HERE: before 966-rq7f's re-entrancy guard each shim hit forked a fresh `brew install ruby`; one --check reached 3663 processes and 89.4% of the pid ceiling on pirria, and the symptom surfaced as `git: unable to create threaded lstat`, nowhere near its cause. That guard is already landed; the memo bounds a repeatedly-probing caller to one acquisition attempt.
- A MISTAKE THE TEST CAUGHT, recorded not quietly fixed: the refusal first printed to STDOUT, and the ruby-dependent call site redirects stdout to /dev/null — correct exit code, invisible diagnosis. That is this order's own defect one layer smaller. On stderr now, with an arm asserting it arrives.
- VERIFIED: scripts/test-archiver-ruby-could-not-run.sh 5/5 hermetic (stub ruby + stub toolbox, no container, no network), wired into ./build.sh --check because the defect is invisible on any host that HAS ruby — which is every host that runs the gate. Arms: exit 3 not 127/1; no ready-set claim in the output; refusal names instrument AND remedy on stderr; POSITIVE CONTROL so a refuse-everything guard cannot pass; and no scratch state left in the worktree on the refusal path (the 2026-08-23 leak that starts every boundary-guarded cycle dirty). Full gate green, 114s.
- SCOPE NOT CLAIMED: I did not add ruby to the forge image (the could-not-run channel is the right answer for a lane that cannot run the instrument; adding an interpreter is a separate decision), and I did not verify inside a REAL forge — this host has no native ruby but a working builder toolbox, so the forge condition is reproduced by stubs rather than observed. Named in next_action for a forge host.
- SELF-INFLICTED WORKTREE DIRT, caught and cleaned: my own `env -i` test stripped XDG_*, so `toolbox` wrote runtime state into $PWD/toolbox (empty locks + a socket). Created this cycle, not startup dirt, so removed rather than committed; the boundary verifies clean. Noted in the new test's header so the next person testing with a stripped environment does not rediscover it.
- Scheduler: still `due:timer-inactive`. The cron I armed last cycle is a SESSION cron, which that check cannot see — it looks for a durable systemd timer. Arming one is 856-s56y and remains unclaimed here; the check is advisory and correct to keep reporting.
