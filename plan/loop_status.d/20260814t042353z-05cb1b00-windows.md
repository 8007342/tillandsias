## Cycle - windows (windows-next)

- Closed 723-g4bk (rung 3) AND its parent 689-y2my. The whole ladder landed in
  three consecutive cycles tonight: probe, wire, action.
- Rung 3: a v2 heartbeat reporting BlockedOnInput while an expect is pending
  fails the exec with a typed DEADLOCK error. ONE heartbeat is enough, and the
  argument is ordering rather than confidence: frames are ordered on the stream,
  so if the guest had printed the prompt before blocking, that PtyData arrived
  BEFORE this heartbeat and the match arm already fired - a pending expect
  co-occurring with a blocked guest means the needle genuinely has not been
  printed.
- NOT the refuted progress deadline. Nothing here measures elapsed time. A slow
  guest loading a 580MB image reports NotBlocked and is untouched; both
  order-332 pins are green and untouched through all three rungs.
- The three failures are now distinguishable, which was the point: idle timeout
  = dead wire; wall-clock ceiling = a bound, not a diagnosis; DEADLOCK = the
  guest told us. Previously one undifferentiated hang.
- vsock_exec 22 passed. The regression test feeds ONE HUNDRED blocked heartbeats
  and asserts failure on the first - if the exec were still waiting for a bound
  it would consume all of them and hang. Negative controls over 50 heartbeats
  each of NotBlocked, Unknown, and v1.
- Honest consequence recorded: rung 2's version matrix, written an hour earlier,
  asserted a v2 BlockedOnInput heartbeat COMPLETES the exchange. It no longer
  does, by design. Updated that test rather than routing around it.
- The 70-minute wedge that filed 689-y2my would now fail within one 30s
  heartbeat, naming what it was waiting for and what the guest was doing.
