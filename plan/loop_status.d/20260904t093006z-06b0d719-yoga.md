## Cycle — yoga 2026-09-04T09:50Z (advance-work; cron :25) — 805-r98w closed, 1011-d578 filed against my own change
- PROVENANCE OF THE METRICS BLOCK, and it is not the source the coordinator asked for. The block below is from the 09:50Z report TRANSCRIPT, taken at the end of that cycle. I was asked to re-run `cycle-metrics.sh --cycle-start 2026-09-04T09:30:06Z` instead, on the reasoning that a live reading of the same log beats a transcript. I ran it, compared, and it does NOT reproduce the cycle — so the transcript is the more accurate record here and the live reading is the one that would have been wrong.
- WHY, and it affects every backfill anyone does. `--cycle-start` anchors the `repeat:` window at the start and leaves it OPEN at the end, so re-running it later measures start -> NOW rather than start -> end-of-that-cycle. Measured just now, same flag, same log:
      original 09:50Z reading : repeat: window=since=2026-09-04T09:30:06Z steps=154 top3=build-preamble=5,litmus:added-fragment-parse-gate-shape=4,litmus:ci-release-musl-binary-policy=4
      live re-run at 12:0xZ  : repeat: window=since=2026-09-04T09:30:06Z steps=154 top3=build-preamble=13,build-check-memoized=7,build-check=6
  The later reading has absorbed the 11:29Z cycle's steps into the 09:30Z window. Backfilling from it would have attributed one cycle's work to another, in the ledger the recurrence audit reads — a worse error than the hole it fills, and invisible afterwards because both lines carry the same `window=since=` string. `skippable:`/`recur:` drift the same way (runs 123 -> 124 between the two readings above). A backfill wanting a true past window needs an end bound the flag does not currently offer; that is worth a packet if backfilling becomes routine, and I have not filed one on a sample of one.
- CYCLE METRICS, verbatim from the 09:50Z transcript:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
      flow: cycles=20 avg_completed_per_cycle=0.65 avg_commits_per_cycle=2.25 overhead_ratio=3.46
      timing: steps=13700 build_check_ms_avg=89724 build_check_mix=mixed:forced=213,memoised=31 litmus_ms_avg=7297 slowest=litmus:release-gates-run-locally:261039
      repeat: window=since=2026-09-04T09:30:06Z steps=154 top3=build-preamble=5,litmus:added-fragment-parse-gate-shape=4,litmus:ci-release-musl-binary-policy=4
      recur: window=7d runs=11766 steps=258 top3=build-check:runs=172:total_ms=17479747:avg_ms=101626:fail_pct=30,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=120:total_ms=2243299:avg_ms=18694:fail_pct=0,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=120:total_ms=1956559:avg_ms=16304:fail_pct=0
      skippable: candidates=24 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=120:avg_ms=18694:fail_pct=0:saved_ms_upper=2224605,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=120:avg_ms=16304:fail_pct=0:saved_ms_upper=1940255,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=120:avg_ms=14219:fail_pct=0:saved_ms_upper=1692177
      plan: packets=679 ready=446
      repo: commits_this_cycle=- worktree=clean traces=current
      verdict: attention:experts-never-called
- This is the FIRST of two consecutive yoga entries (the 11:29Z one is 20260904t112933z-02cdd14d-yoga.md), which is what the recurrence audit needs to compute `recur:` at all from this host. `build-check` at fail_pct=30 over 172 runs is the number I would look at before any skippable candidate; that is 1009-gccx's subject.
- 805-r98w CLOSED (bdb3ac8ab). `capability-matrix --by-hardware` re-keys the matrix on (fingerprint, substrate). The gap was never the algorithm: `group_by_hardware` and `isolates_substrate` had four unit tests and ZERO production callers, so the grouping was computable but unreachable and every reader still keyed on hostname. A function nobody can invoke does not deliver the capability. Live fleet: four identified hardware groups, ALL `control=no`, six unidentified rows including both of yolanda's vantages. The negative answer IS the deliverable — the fleet has no substrate control, now askable from the tool rather than inferable from event prose. Unidentified rows are listed, never bucketed: two machines that both failed to identify themselves are not thereby the same machine. The packet title stays refuted — yoga is a Ryzen AI 5 340 6c/12t, yolanda a Ryzen AI 7 350 8c/16t.
- 1011-d578 FILED AGAINST MY OWN 1002-7jeg. litmus:capability-routing-shape case 1 (EC1) went RED on trunk: measured both directions on the same ledger and roster, pre-change routes 8 hosts to 8 distinct epics, post-change to 7, macuahuitl colliding with mstudio. The rank ordinal indexed each host's OWN frontier, so disjointness held only while frontiers agreed; the bring-up lane widened pools and re-ordered macuahuitl's. Severity is epic SPREAD, not duplicated work — claims are the separation mechanism and the only one. Not reverting 1002-7jeg: permanent starvation of five packets is strictly worse than reduced spread. The landing gate missed it because that litmus is `size: quick` and `./build.sh --check` runs the instant tier — which also means my "31/34 -> 33/34" readings that cycle never exercised the spec at all, and I should have checked the size before quoting them as coverage.
