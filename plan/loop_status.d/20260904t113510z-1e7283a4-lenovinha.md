## Cycle — lenovinha 2026-09-04T11:49Z (advance-work; cron :05) — 1011-zp59 closed: the Vega is not broken, it is deliberately unscheduled
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T11:35:10Z`:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
      flow: cycles=8 avg_completed_per_cycle=0.13 avg_commits_per_cycle=1.75 overhead_ratio=14 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      timing: steps=26181 build_check_ms_avg=136843 build_check_mix=mixed:forced=354,memoised=85 litmus_ms_avg=11414 slowest=litmus:tool-materialization:304387 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      repeat: window=since=2026-09-04T11:35:10Z steps=117 top3=build-check=1,build-preamble=1,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=1 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      recur: window=7d runs=24043 steps=424 top3=build-check:runs=314:total_ms=45545011:avg_ms=145047:fail_pct=31,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=216:total_ms=7237500:avg_ms=33506:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=216:total_ms=5427268:avg_ms=25126:fail_pct=0 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=57 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=216:avg_ms=33506:fail_pct=0:saved_ms_upper=7203994,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=216:avg_ms=25126:fail_pct=0:saved_ms_upper=5402142,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=216:avg_ms=19634:fail_pct=0:saved_ms_upper=4221347 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=685 ready=449 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=dirty traces=current
      verdict: attention:worktree-dirty
- READING IT: `skippable:` names the same three steps as last cycle — terminology-dictionary, plan-archiver-ready-set, plan-ledger-tests — all fail_pct=0. Those saved_ms_upper figures are UPPER BOUNDS over a 7d window, each assuming the step is skipped every time and never catches anything; fail_pct=0 is simultaneously the argument for memoising them and the reason nobody has. Now the fifth host on 1008-85ry.
- 1011-zp59 CLOSED. The row said `present-unusable: gpu/Cezanne (rocm-runtime-missing)` — a TRUE reason carrying a FALSE implication. Now: `present-unscheduled: gpu/Cezanne ... (policy:discrete-gpu-preferred; measured lenovinha 2026-09-04, both models fully resident size_vram==size: decode 4.3-4.9x, embed 3.1x in favour of the discrete card)`, zero present-unusable lines for it, yoga's Krackan byte-identical. Verified by republishing the row from the rebuilt probe and rendering the matrix, not argued.
- THREE ENCODINGS, TWO OF THEM LIES, which was the whole question. `present-unusable` reads as "does not work". `usable` is worse — a scheduler that believed it would place work there and lose 3-4x. So this is a THIRD fact: `policy_unscheduled` travels beside the capability reading, and usable/rocm-runtime-missing are preserved verbatim because that is what was actually probed. Deliberately NOT done by flipping `usable`: the probe reads sysfs and a ROCm runtime and cannot verify a Vulkan lane, so deriving usability from a policy check would assert on EVERY host a capability measured on ONE.
- I DID NOT IMPLEMENT THE PACKET'S LITERAL DISCRIMINATOR, and this is the substantive divergence. It says "an iGPU on a host whose tier is gpu-cuda"; I keyed on "a discrete GPU is present AND SCHEDULABLE". effective_inference_tier() downgrades gpu-cuda to cpu when no CDI spec exists, and on that host the iGPU is the ONLY accelerator — the best lane, not a rejected one. The tier-keyed version would stamp "deliberately deprioritised" on the best device the host has, failing toward a worse falsehood than the one being fixed. My refinement was accepted verbally but was NOT in the fragment I read, so I restated it in full on the evidence event rather than assuming it was recorded.
- MY OWN TEST WAS WRONG FIRST, same shape as the proxy assertion I caught in 1003-v3dc this morning. I put all four arms against `igpu_policy_unscheduled_reason`, which takes a BOOLEAN — so "no discrete card" and "a discrete card that cannot be scheduled" are the SAME INPUT, and my CDI-absent arm was a byte-for-byte duplicate of the yoga arm that read as though it proved the thing I had specifically asked for. Split it out against the call-site predicate `discrete_gpu_is_schedulable` over DeviceRecords, with a positive control so the negatives cannot pass vacuously.
- MUTATION CONTROL RUN, because an arm that has never gone red is a claim and not a test: dropping the `usable` check from the predicate — exactly the tier-keyed shape — makes the CDI-absent arm FAIL; restoring it passes.
- TEST HOME DEVIATES from the packet's named scripts/test-capability-row-check.sh, recorded not glossed: that fixture tests row PRESENCE and FRESHNESS, a different property from CLASSIFICATION, and cannot reach this decision without fabricating a sysfs tree for the Rust probe. The discriminator is a pure function and is pinned where it lives. A shell-level arm, if wanted, is a follow-up.
- TOOLING NOTE FOR ANYONE EDITING accel_probe.rs: multi-line `perl -0pi -e 's|...|...|'` misfired on this file TWICE, both times inserting the replacement into the module header comment on line 2-3 instead of at the intended call site, corrupting the doc block while the line count still looked plausible. Caught both times by the compiler and by diffing deletions, and repaired without reverting good work. Use the editor's exact-match replace on this file, not multi-line perl.
- NOT DONE: nothing was measured about INTEL iGPUs beside discrete cards. The predicate is vendor-neutral by construction so the rule will fire for them, but the reason string cites an AMD measurement on one host — if such a host appears, that string is citing evidence from different silicon and should be re-derived rather than inherited.
- `--host lenovinha` passed on this append, per the recurrence audit; previous entries defaulted to the shared `linux` stem (1012-hu7d carries the fleet half).
