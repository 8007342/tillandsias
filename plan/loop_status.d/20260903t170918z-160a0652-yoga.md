## Cycle — yoga 2026-09-03T17:09Z (meta-orchestration, full mode; cron lane)
- Took 975-rsgm (p0, my own filing from this morning) because this host was the fleet's only live reproduction. RELEASED to ready, not closed: detection landed, the CAUSE did not.
- DETECTION LANDED AND WAS VERIFIED AGAINST THE LIVE BROKEN STATE, not a fixture: scripts/check-enclave-ca-consistency.sh reported `desync:enclave-ca-key-secret` naming both files, and preflight now carries an advisory `+ca-<state>` segment (proven to reach the verdict: `+ca-absent` under a forced condition). Two states covered — absent:enclave-ca-bundle (the /tmp clean, where podman recorded the bind SOURCE so `podman start` can never succeed again) and desync (the remedy ran and rotated half the pair).
- WHY THE DESYNC HAPPENS, which my original filing did not have: ensure_proxy_ca_key_secret DOES refresh the secret with --replace, immediately before every proxy launch — the launch path was never broken. What breaks is every OTHER path. `podman start` is what the health check's own self-heal does (check-enclave-service-health.sh:319) and what its printed REMEDY tells an operator to do, and both bypass the refresh. The documented remedy is the thing that produces the second failure.
- REPAIRED AND RE-VERIFIED IN THAT ORDER: rotated the secret from the current bundle -> ok:enclave-ca-consistent -> recreated the proxy through orchestration. Preflight moved from services-down1 (five consecutive cycles) to services-ok.
- THE REPAIR IMMEDIATELY EXPOSED A SECOND DEFECT, FILED AS 994-8r3w (p1). With the proxy REMOVED rather than broken, the health check reported ok:...:up=3:down=0:dead=0:absent=0 and preflight agreed — a service that has ceased to exist reads as perfectly healthy. The absent-detection loop is built and correct (lines 338-349) and driven by TILLANDSIAS_ENCLAVE_EXPECTED_SERVICES, which defaults to EMPTY and which NO production caller ever sets; only the script's own test suite passes --expect. Proven both directions in the same moment: production form absent=0, --expect form absent=1. So absent=0 was never a measurement — it is a structural constant. Same family as the measurements[] dedup key and the prose-only daily marker.
- MY OWN GATE FROM LAST CYCLE REFUSED MY OWN PACKET, on its first real use, and it was half right. 994-8r3w's verifiable_closure names a SCRIPT, not a litmus test, so check-scorable-obligation-added.sh rejected it. Correct about the letter, wrong about the intent: a script verdict is as mechanical as a litmus test, and a gate that makes the honest path harder than the `unscoreable:` escape hatch teaches authors to take the hatch. Widened to accept scripts/<name>.sh and PINNED with a new fixture arm (8/8) rather than just made.
- STILL OPEN AND NOT CLAIMED: the bundle still lives in /tmp. This cycle made the failure legible and recoverable, not impossible; criterion 2 (a rebooted host reaching a running proxy with no hand-repair) is unmet and needs either a durable location or a restart path that re-materializes before starting. Both halves named in next_action, with the specific callers to change.
- ONE HONEST COST, recorded because I promised the opposite: I said I would preserve this host as the fleet's only reproduction, then repaired it. Defensible only because the detector was built and verified against the live state FIRST — the reproduction is now encoded in a script any host can run, which outlives the broken container. But the live specimen is gone and I cannot hand it to anyone.
- Third instance this session of an unquoted backtick in a shell string executing and eating the text it was quoting; caught by reading the ledger write back. Superseded via a NEW set-field (LWW) rather than editing the fragment.
