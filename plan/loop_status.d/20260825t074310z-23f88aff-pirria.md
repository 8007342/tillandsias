## Cycle 2026-08-25T07:43Z (linux_immutable pirria — I was wrong about 877-mynm, and the correction is the finding)

- **Host**: pirria, `linux_immutable` (Silverblue, Intel N150, 4 cores), branch `linux-next`, lane `loop-cron`, genuine 6h cron fire. Fast-forwarded 26 commits. Scheduler still `due:not-installed`.
- **THE 876-exg2 FIX WORKED IN THE WILD.** The credential guard ran immediately after that fast-forward — the exact sequence that hard-stopped the two previous cycles — and returned `ok:gh-keyring-push-verified-hook-refused`, rc=0, with the note naming `./build.sh --check`. First live confirmation, on the first real occurrence after it shipped.
- **Guards**: branch `ok`; MCP `ok:experts-healthy` + surface `exposed`; capability row `ok`; de-slop `not-due order=879 delta=45`; lock `loop-cron:5022`; boundary on a clean tree. Daily maintenance was `due:stale:2026-08-24` (new day) and is stamped.
- **A correction I owe my own last two entries**: I flagged `target/` at 18G as something I was ducking. I finally measured the disk — 475G, 15% used, **406G free**. `cargo-gc:skipped-under-threshold` was the correct call both times; the worry was unfounded and I have stopped repeating it. The stamp now records the measurement rather than the label.
- **Batch**: `epic=UNGROUPED size=2 budget=10 score=2.160 seed=pirria route=tier:low-end`, offering 863-iicc (orchestrator-gated) and **877-mynm**, which I filed yesterday. Claimed and closed it.
- **I FILED 877-mynm ON A WRONG PREMISE.** I wrote that git hands the composed hook a 125-byte ref list while `pre-push-local-gate.sh` reports "no ref list on stdin", and called the v6 composition intermittently broken. That was an over-generalisation from one spy reading: the 125 bytes came from a push that SUCCEEDED, and I carried the number across to the pushes that failed without re-measuring them. A scratch repo reproducing the v6 shape — capture stdin once, replay to each guard, first guard draining its own pipe in a `while read` loop — passes all 125 bytes through to the last guard. The composition is fine.
- **What is actually true**, measured across five cases against a local bare remote: fast-forward 125 bytes; `--dry-run origin HEAD` (the credential probe form) 108; already up to date **0**; non-fast-forward with a **current** remote-tracking ref **0**; non-fast-forward with a **stale** remote-tracking ref 125, with the rejection then arriving from the remote. **The list is empty exactly when git has already decided to send nothing.** The guard's message was accurate the whole time; what was missing was anyone decoding it.
- **The fifth case is why the rule is not "non-fast-forward"**, and without it the fourth would have licensed a wrong rule. Emptiness means *git already knows*.
- **The cost removed**: on a lost claim race — the ordinary outcome of claim-before-work on a fleet of six, and exactly the state that leaves the remote-tracking ref current-and-ahead — the guard paid a full gate (~90s here) for a push that could not happen, then told the reader to run `./build.sh --check` when the remedy was `git pull --rebase`. Both halves recurred by construction. An empty ref list now exits 0 with the true remedy named. Fixture 10/10, with a NEGATIVE CONTROL (a real outgoing ref is still gated — this must not become a licence to accept an unscoped push, which is criterion 3, which I wrote precisely to fence this fix) and a MUTATION control.
- **Criterion 4 was restated, not met as written.** I had asked for a fixture asserting the plan-only lane fires on a fragment-only push, "which today would fail on this host". It would not have failed — the lane fired on this cycle's own claim push and twice in the first cycle. The fixture worth writing pins the characterisation instead, because that is the part nobody knew.
- **Bearing on 863-iicc criterion 7**: the shorter-gate lever was never broken. It was being bypassed on precisely the pushes that were going nowhere. That removes one of the reasons a slow host was thought to need a private lane, and it should be re-measured before the lane question is answered.
- **Stranded, reported not touched** (advisory, and closing a packet needs its exit criteria checked against the tree): `population=2 in_progress=2 stranded=2` — 831-ezea and 865-n8vq, both `any` role, both up from 0 last cycle. 865-n8vq is titled "the trunk has been unreleasable for a week behind a gate no push runs", which is not a thing to leave sitting in a state where `ready` queries skip it and burndown does not count it.
- **Flow**: `overhead_ratio` fell 3 → **1.67** across two cycles as the batch grew from 1 to 2 packets. That is the direction 682-yiz7 predicts for greedier batching, on a sample of two — worth watching, not yet worth concluding from.
