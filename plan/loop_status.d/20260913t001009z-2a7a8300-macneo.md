## Cycle — macneo advance-work-from-plan (cron :40 lane)

Landed 7fe19c312, verified as an ancestor of a freshly-fetched origin/osx-next,
with a negative control (origin/windows-next head not contained) so the check
can return false.

Direction: stable-milestone-v1. Batch selector seed
macos-unknown-apple-tlatoanis-macbook-neo-20260913, epic=stable-milestone-v1,
size=5 budget=6, route=rank:6/8. Claimed 1080-4deb, released it back to ready.

Outcome (a), partial slice. The packet's next_action named a ref deletion as
"the smallest next action" on the grounds that
origin/salvage/unknown/20260910-1080-4deb-arm1 was integrated and deletable.
It is not: the tip is an ancestor of none of the four published branches, with
a positive control confirming the negatives are load-bearing, and it carries
three plan/index.d fragments that exist nowhere else. The wrong verdict was
reachable because ARM 1's code landed while ARM 1's provenance did not — the
gate step is byte-identical on trunk, so the natural check answers yes and the
missing record never surfaces. That is this packet's own subject one level up.

Restored the three fragments unmodified; additive and CRDT-safe, with the
packet's status fold-verified undisturbed across the restore in both
directions. Corrected next_action so the next host that can push does not
re-run the deletion. Item 2 (the 46-hit live-lane sweep rollup) untouched and
claimable.

Gates: check-scorable-obligation-added, check-long-running-view,
check-fragment-status-loss, and check --strict-fragments all green, run twice —
before the commit and again on the final tree.

Sub-agents: 0 of 4 budgeted. The cycle was ancestor checks and a file compare;
delegating would have cost more than it saved.

tokens: ~95k this cycle, single agent, no fan-out.

verdict: attention:experts-never-called — accurate, the plan experts are absent
on this host (source=absent) and every read went through the plan binary and
git directly.
