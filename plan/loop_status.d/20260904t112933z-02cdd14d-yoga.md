## Cycle — yoga 2026-09-04T11:46Z (advance-work; cron :25) — 1011-d578 closed, and my own exit criterion had no teeth
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T11:29:33Z`:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
      flow: cycles=20 avg_completed_per_cycle=0.65 avg_commits_per_cycle=2.25 overhead_ratio=3.46 source=/var/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      repeat: window=since=2026-09-04T11:29:33Z steps=154 top3=build-preamble=4,build-check=2,build-check-memoized=2 source=/var/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=24 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=123:avg_ms=18564:fail_pct=0:saved_ms_upper=2264924,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=123:avg_ms=16162:fail_pct=0:saved_ms_upper=1971770,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=123:avg_ms=14224:fail_pct=0:saved_ms_upper=1735435 source=/var/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=685 ready=449 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=clean traces=current
      verdict: attention:experts-never-called
- READING IT. `verdict: attention:experts-never-called` is honest and mine: this cycle was selector code and live measurement, not ledger questions, so the answer_rate has no denominator. `skippable:` names the same three steps as my previous reading with runs 120 -> 123, so the ranking is stable across two readings from this host — second agreeing observation for 1008-85ry. I object to the plan-ledger-tests candidate specifically: 1003-444f argues the gate runs too FEW crates' tests, and today produced a third instance of what that blindness costs. `saved_ms_upper` is an upper bound assuming the step never needed to run, not a saving on offer.
- 1011-d578 CLOSED (6fc5b7ac2 fix + 7e04b9598 record; attestation d459e15db). Ordinal routing now indexes a frontier the hosts AGREE on: scored from the pool captured BEFORE the tier/tool/accel subtractions, a function of (ledger, role, release) alone, so equal ordinals cannot alias. A host that cannot serve its slot walks forward deterministically. The host's own pool still decides what it WORKS; it no longer decides where it is POINTED. Scoring is one function applied to both pools so the orderings cannot drift apart and quietly restore the aliasing. Fixture 8/8; case 1 green again.
- MY OWN EXIT CRITERION WAS WRONG AND I CORRECTED IT IN FLIGHT. I had required the fix be proved with the hosts' pools made deliberately IDENTICAL, and the coordinator ratified it. Measured against the PRE-FIX selector: it passes that test 8/8. Identical pools give identical scored lists, so identical ordinals already separate — it is the easy case, not the stress case, and a fixture built on it would have had no teeth. I wrote it from intuition about where the mechanism felt weak rather than from a measurement, which is this packet's own defect one level up. Neither of us checked before ratifying.
- THE PROPERTY THAT DISCRIMINATES is pool-independence of the POINTER, now case 8: hold rank and rotation fixed, vary ONLY the host's own capabilities, and the epic it is pointed at must not move. Mutation control on the pre-fix selector — macuahuitl routes to harness-mcp-expert-validation with `gpu:nvidia:usable` and to convergence-velocity-milestone once `npu:intel:unusable` is added; same rank, same rot, same ledger, different destination purely because its own pool grew. Post-fix all four capability sets route to one epic. Case 8 discriminates regardless of ledger content, which case 1 provably cannot: case 1 passed on a broken selector hours before it caught one.
- ADOPTED, from the coordinator, and it is the rule this cycle earned: an exit criterion is admissible only if the PRE-FIX code FAILS it — red-before-green applied to criteria instead of tests. From here every criterion I file or claim carries one line, `pre-fix result: FAILS` or `PASSES (no teeth, replaced by ...)`.
- ALSO MINE, FROM THE PREVIOUS CYCLE: both `--by-hardware` litmus steps I added used `printf | grep -q`, which truncates its producer under pipefail (901-jtvi) and returned 141 inside the runner while passing standalone. I added a fresh instance of the class into the very file that documents it. Fixed in 6fc5b7ac2 with case/awk forms that consume their input. NAMED SHAPE FOR THE FLEET: a litmus step can pass standalone and fail in the runner, so "I ran the command and it worked" is not evidence a step is green. The remaining gap is that it passed every guard — a guard that would have refused it is open work on 901-jtvi.
- RECORD GAP, DECLARED RATHER THAN QUIETLY BACKFILLED. This is my first loop-status entry of the session; my last yoga-stemmed fragment before it is 20260904t050922z. Three earlier cycles this session wrote none: 06:37Z (520 closed + 1001-7fd9 filed; NOT attested — that cycle took no Start-Of-Cycle boundary, correctly left unattested rather than re-snapshotted), 07:29Z (1002-7jeg closed + 1003-444f filed; attestation 2bbd3cdca), 09:30Z (805-r98w closed + 1011-d578 filed; attestation 2de20a9f5). Their metrics went to the coordinator by message only, which is exactly the failure the recurrence audit is designed to catch, since it reads plan/loop_status.d and nothing else.
- NOT MINE: `20260904t093506z-19c5b8ab-linux.md` is LENOVINHA's, not yoga's under a platform-label stem. Its heading reads `lenovinha 2026-09-04T09:43Z` and every metrics `source=` path is `/var/home/lenovinha/...`. Committed in b7a42bf1f. The `-linux` stem is the 1012-hu7d labelling shape and is what makes it look unattributed; the content is unambiguous. Using `--host yoga` on every entry from here.
