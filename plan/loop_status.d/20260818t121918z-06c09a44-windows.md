## Cycle 2026-08-18T05:19 PDT→05:30 PDT (windows — YOLANDA, MO FULL: short deliberate cycle — two carried gaps closed, compaction declined with a reason)

**Result: a 40-minute window scoped to one completable item rather than started on something that would not finish. The two facts the Windows capability row had been carrying invisibly are now displayed.**

- **STOP CHECK**: `Tue Aug 18 05:19:03 PDT 2026`, ~40m before the stop. The next cron fire (06:13) is PAST the stop, so this is the last cycle of the window.
- **Host**: YOLANDA, `windows-next` af736c0fb → merged `origin/linux-next` 56c2b779f → pushed 3a074a8fa. All guards green, `ok:clean-tree`, boundary `/tmp/meta-orchestration-boundary.w6yNVf`. `verdict: ok:nothing-flagged`; 1128 packets.

**SCOPE WAS THE DECISION THIS CYCLE, and it is worth recording as such.** With ~15 minutes of real work time after guards and before finalization, the choice was between starting something substantial and finishing something small. Starting 827-h4tz's non-Nix cross-build, or the 823-u5zf tray remainder, would have produced a half-landed change at a hard stop. Neither was started.

**LEDGER COMPACTION DECLINED, deliberately, with the reason recorded.** `compaction: eligible=true fragments=143 bytes=868422 malformed=0 reason=fragment-count` — genuinely eligible and flagged as pending for several cycles. Not done, and not from neglect:
- it is pure GC. The skill's own words: "an uncompacted ledger is slower, never wrong". There is no correctness debt accruing.
- it rewrites a 31k-line base and deletes 143 fragments by name. That is the largest-blast-radius operation available here.
- **linux was mid-cycle**, having pushed 56c2b779f minutes earlier and landed a fragment at 11:55Z. The tool deletes only what it folded, by name, so the GC-versus-writer race is handled by design — but the minutes before a hard stop are the worst time to find out otherwise.
A large ledger rewrite wants room to go wrong in. It stays eligible and costs nothing to defer.

**THE TWO CARRIED GAPS, closed.** Both were flagged in last cycle's entry as known rather than left unnoticed, which is the only reason they were cheap to find again.

`os_status` is now printed BESIDE the unusable reason, never folded into it. 806-2r4s is precisely about not collapsing "present" with "usable", and the same care applies one level down: Windows calls this NPU **healthy** AND our lanes cannot reach it. A reader seeing only `wsl2-npu-not-exposed` cannot distinguish "the OS says it is broken" from "the OS says it is fine and we have no lane to it" — different engineering problems, and telling them apart is the whole reason the record is emitted. Omitted when the contributing context reported none, rather than guessed.

`machine_ram_gb` is now printed because it is deliberately NOT a Rust `HostInfo` field — adding one would give a second home to a value `DeviceRecord.system_ram_gb` already owns, the drift class 807-c3mf removed rather than documented. Until now the number was carried in the ledger and visible to nobody: **the write-only field this milestone keeps collecting**, reintroduced by my own last cycle. Printing it is the cheap half; typing it waits for a consumer that needs machine-capacity RAM as distinct from context-available RAM, which is a real design question and should not be answered by accident. Printed PER ROW, not per host, because two loci legitimately disagree — the guest's 7.3 GB VM slice and Windows' 15.2 GB installed are both correct answers to different questions.

```
host:yolanda locus:windows-host
  machine_ram_gb: 15.16
  present-unusable: npu/NPU Compute Accelerator Device (wsl2-npu-not-exposed) os_status:OK
  present-unusable: gpu/AMD Radeon(TM) 860M Graphics (wsl2-no-dri-render-node) os_status:OK
```

**NOT DONE, carried to whoever picks up next:**
- **827-h4tz (p1, filed last cycle)** — `cargo test -p tillandsias-windows-tray` is RED on this host (staged guest binary `0.4.260815` against VERSION `0.4.260817.1`) and the windows-sources stamp cannot be taken until it is resolved. Remedy (a) — a non-Nix musl build in `tillandsias-build`, where musl-gcc and musl-devel are already installed — is the one worth trying first.
- **Ledger compaction** — eligible, 143 fragments, deferred with the reasoning above.
- **823-u5zf** — open, remainder named: the `-lc` composition in `host-shell/src/pty/mod.rs`, `argv_survives_wt_reparse` ×7, `wt_safe_title` ×4. The blocking design question was answered two cycles ago; what remains is mechanical translation.
- **798-q4m9** — open pending 819-qgy2, which needs an attended `.wslconfig` + `wsl --shutdown` window.
- **Bar-raise** proposed, not enabled.
- The uninstaller's **mode-preservation assertion** is implemented but untestable on Windows/MSYS; a Linux or macOS host can add it cheaply.
