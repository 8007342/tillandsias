## Cycle macuahuitl-20260911t015500z — 2026-09-11T01:55:00Z (antigravity, linux-next)

- CONTEXT:
  - Fatal forge launch error: fatal: could not create work tree dir '/home/forge/src/tillandsias': Permission denied
  - Host checkout elimination: Remove leftover host checkout in <host_home>/src/<project> from both tray UX and CLI in favor of pure CLOUD > PROJECT > AGENT -> GIT_MIRROR > FORGE. The host filesystem under HOST/src/<anything> must no longer be touched (infra is fully ephemeral; uncommitted in-forge work is lost on stop).
  - Agent and harness affordances: clearly specify ephemeral architecture, and add bootstrap for Codex and Antigravity into forge-awareness.
  - Reinstall fresh binary with monotonic build version increment as per specs.
- IMPLEMENTATION:
  - crates/tillandsias-headless/src/main.rs:
    - Updated forge_hot_src_tmpfs to configure --tmpfs=/home/forge/src:size=...m,mode=0777 so user forge (uid 1000) has write access in rootless podman container without permission errors.
    - Updated unit test forge_src_is_a_hot_tmpfs_sized_by_compute_hot_budget asserting mode=0777.
    - Rewrote resolve_cloud_project_checkout to parse nwo, set TILLANDSIAS_PROJECT_REMOTE_URL in env, and return short name without touching host projects_root() or host disk.
    - Updated ensure_enclave_for_project to fall back to TILLANDSIAS_PROJECT_REMOTE_URL when project_path is None or lacks git remote.
    - Updated run_forge_agent_cli_mode, run_opencode_mode, run_opencode_web_mode, and run_observatorium_mode to recognize is_cloud (TILLANDSIAS_PROJECT_REMOTE_URL), bypass host project.exists() checks, and avoid host mount.
  - crates/tillandsias-headless/src/tray/mod.rs:
    - Rewrote handle_launch_cloud_project to eliminate all host filesystem touches. Now builds argv [current_exe, "--cloud", repo_id, mode_flag] and spawns terminal directly via launch_in_terminal.
  - openspec/specs/forge-hot-cold-split/spec.md & docs/strategy/forge-hot-cold-split-plan.md:
    - Updated /home/forge/src tmpfs specification from mode=0755 to mode=0777.
  - Harness Affordances & Discovery:
    - Added Forge Awareness & Ephemeral Architecture section to AGENTS.md specifying /forge-quick-intro fast bootstrap, /home/forge/src/<project> RAM tmpfs (0777), zero host disk touches, and mandatory commit+push before exit.
    - Created root symlink CODEX.md -> AGENTS.md (matching GEMINI.md -> AGENTS.md) so Codex discovers forge awareness immediately.
    - Updated skills/forge-quick-intro/SKILL.md to document RAM tmpfs ephemeral workspace and mandatory push requirement.
    - Updated images/default/startup-context-addendum.md and images/default/lib-common.sh with quick-intro and ephemeral architecture instructions across all harnesses (.claude, .codex, .gemini, .opencode).
- VERIFICATION & BUILD:
  - Removed old tillandsias binaries from ~/.local/bin/.
  - ./build.sh --install compiled and installed musl-static launcher to ~/.local/bin/tillandsias (16M) with build version incremented to 56.9.11.3.
  - bash scripts/check-skills-single-source.sh passed (5:15).
  - Cargo tests on tillandsias-headless passed (525/525).
  - ./build.sh --check passed cleanly across all phases; gate stamp recorded.
