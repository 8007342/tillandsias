## Cycle 2026-09-04T20:15Z-20:55Z — yolanda (windows), advance-work-from-plan

Boundary first; `linux-next` merged before the selector. Step 3 still blocked
(macbookair's consumers not landed — `tray/mod.rs` went 14 → 15). Took
**823-u5zf** and made its unobservable closure criterion observable.

**The finding is what I could NOT measure.** The packet's closure asks for a
lane "OBSERVED opening in Windows Terminal rather than conhost". I tried three
observables and **all three were incapable of answering it**:

1. **Counting `WindowsTerminal` processes** — it reuses ONE process, so a new
   window is a tab. before=1, after=1, a number **compatible with both
   outcomes**.
2. **`wsl.exe` parentage** — the two plausible candidates traced back through
   `cmd.exe` to `claude.exe`: they were **my own tool calls**. The tray's lane
   left no process at all, because a lane into a missing project exits at once.
3. **`tray.log`** — the headless `--forge` path never writes it. No record of
   the spawn decision existed to read.

**I almost closed the packet on (1).** A count that doesn't move reads as
"nothing happened", and "no new conhost either" would have felt like
corroboration. Both readings are worthless: the instrument cannot distinguish
the outcomes. Same proxy-read-as-the-thing shape as the rest of today, this time
in my own verification rather than in a test.

So rather than leave advice, I made it checkable: `terminal=` is logged from the
**same predicate** that selects the spawn, immediately before it, and a test
pins that agreement — a label drifted from its branch would be worse than no log,
because it would be believed. Control armed: keying the label on a different
intent turns it red, green on restore. **Packet left open**; criterion 1 is met
(`c3f6591a1`), criterion 2 now has an instrument but still wants one real lane.

**Incidental, deliberately not claimed as evidence:** the scratch project I made
to force a persistent lane got **quarantined** — renamed `.invalid-<epoch>` in
the host's `~/src`. That is 997-e4v2's path firing on the shipped release, and
it says nothing about the classifier fix: v56.9.2.1 predates `e8a995a41`, *and*
an empty dir is truthfully not a checkout, so the fixed code would quarantine it
too. The observation cannot discriminate. Residue removed; `~/src` is empty.

**Also this cycle:** answered the credential diagnostic — yolanda runs
**gh 2.100.0 with a live `gho_` token** and the same scopes as pirria's revoked
one, which refutes "every survivor runs a gh older than 2.100.0" and kills scope
set and credential path as discriminators in one data point. I reported the
token **type** rather than the requested 8 characters: the type is the whole
signal, the rest is secret with no diagnostic value.

**And a schedule correction that was overdue:** my armed cron
(`45 */2` local = odd UTC hours at :45) has **never fired today**; every fire has
been at even UTC hours at :15, operator-driven. Every "next fire at HH:45Z" I
gave the fleet was wrong. The fleet now wakes me by message instead. I had
fixed the timezone arithmetic twice without ever checking the premise against my
own ledger, which had the answer in it the whole time.

### cycle-metrics (`--cycle-start 2026-09-04T20:15:05Z`), verbatim

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent
timing: steps=9374 build_check_ms_avg=601523 build_check_mix=mixed:forced=131,memoised=18 litmus_ms_avg=3478 slowest=step:checking-that-added-fragments-declared-closures-resolve-885-92iu:490764 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-04T20:15:05Z steps=121 top3=build-check=1,build-preamble=1,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=1 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=8615 steps=125 top3=build-check:runs=115:total_ms=73581446:avg_ms=639838:fail_pct=18,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=94:total_ms=10372075:avg_ms=110341:fail_pct=0,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=94:total_ms=6421694:avg_ms=68315:fail_pct=0 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=50 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=94:avg_ms=110341:fail_pct=0:saved_ms_upper=10261734,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=94:avg_ms=68315:fail_pct=0:saved_ms_upper=6353379,step:writing-the-gate-stamp:runs=94:avg_ms=58055:fail_pct=0:saved_ms_upper=5399181 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=705 ready=457 plan_bin=./target/release/tillandsias-plan.exe
repo: commits_this_cycle=- worktree=clean traces=current
verdict: attention:experts-never-called
```
