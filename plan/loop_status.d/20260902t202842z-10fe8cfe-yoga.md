## Cycle — yoga 2026-09-02T20:28Z (meta-orchestration, full mode; fleet restart, coordinator-briefed story)
- Start of cycle: preflight ok (enclave tillandsias-proxy FAILED to restart — operator action); guards ok (credential gh-store, branch linux-next, MCP experts healthy + surface exposed); lock acquired (operator-prompt lane); boundary clean at 2acb4f9ca; detectors clean; 0 live claims; stranded 0; deslop not due (delta=130/200); compaction not eligible (18 fragments). Daily gate was STALE since 2026-08-26 — body run and stamped.
- Batch: epic=forge-local-experts-milestone seed=yoga size=10 budget=10 pick=3/3; triage eligible=173 grouped=128 ungrouped=45. Claimed 821-73es + 824-6qxh (claim pushed before work, f313426a3), both RELEASED to ready with progress recorded. Filed 967-6ax6.
- 821-73es: next_action ordered a re-measurement of the declined-alternatives conversion against the LANDED files. Result 1 of 4, NOT the inherited 2 of 5 — content correct and indexed and still losing three of four questions. CAUSE: all records nested under one column-0 key, and chunk_yaml cuts at column-0 keys and subdivides only above 4000 chars, so the block was ONE 2625-byte chunk with ONE embedding, topically dominated by its first and longest entry. FIX: every declined technology is now its own column-0 key; verified against the real chunker (6 topical chunks where there was 1), then injected the exact landed chunk texts into a clone of the live index: 1/4 -> 4/4 with no wording changed, Helm itself improving 0.6309 -> 0.7522 once it stopped sharing an embedding.
- 821-73es regression check MEASURED at n=579 rather than asserted: 8 rows changed top-1, all 8 in the near band (5 distinct questions); ZERO in-corpus and ZERO far rows changed; in-corpus floor identical to six decimals (0.561872 both runs).
- 824-6qxh: bands re-measured on this cycle's index (gen 77f8611, 22,682 chunks, nomic-embed-text, no prefix, 579/579, zero failures). in 0.5619-0.8569, near 0.5772-0.8644, far 0.4800-0.6909; no global threshold (overlap 0.3026), no margin (0.1304, worse), every corpus overlaps. Floor efficacy at 0.62: far 58/70 refused, in-corpus cost 6/266 = 2.3%. macuahuitl on different silicon: far 58/70, 2.4%, same top far leaker to four decimals. So "the right floor is regime-dependent" needs splitting — the bands are a property of (embedder, corpus, prefix), NOT of host silicon. This corrects my own host's earlier smaller-sample conclusion.
- 967-6ax6 FILED (p2, bug): the just-merged 793-zumy proof producers exec into `tillandsias-inference`; the dev lane creates `tillandsias-dev-inference`, so accel_proof stayed `-` on a host whose lane demonstrably works. TWO defects, not one — the name, and the emission half conflating "nobody to ask" with "asked and found nothing". Owner landing both; I landed my half (dev-inference-ensure.sh exports TILLANDSIAS_INFERENCE_CONTAINER, single-source rather than a second constant).
- Cross-host: ACK'd yolanda on 793-zumy's legacy_tier coupling with the exact contract dev-inference-ensure.sh depends on (raw-grep on the literal key, value vocabulary, grep -m1 first-match hazard), and pre-ran her discriminator so ARM 2 rests on live data not fixtures (rc=0, models present, size_vram=0 — a definite zero, not the unknown path).
- REGIME NOTE the brief got wrong, and it cost the cycle's wall clock: this host has NO GPU-embedded index. ROCm devices ARE passed (937-68n4) and ollama still declines to place a 137M embedder, so the 22,682-chunk index built on CPU at ~700 chunks/min, ~70 minutes, not the ~2 minutes assumed. "ROCm lane" and "GPU-embedded index" are different claims and only the first is true here.
- NOT MEASURED, named rather than skipped: the 7b-vs-14b cited-synthesis floor. 15 GB RAM total, CPU decode; a 14b is a ~9 GB pull on 12 cores and did not fit this envelope beside the index build. Recorded as UNMEASURED on 824-6qxh rather than asserting the conclusion the RAM figure suggests.
- Process trap re-learned and recorded: an UNQUOTED heredoc executed every backtick-quoted term in a ledger fragment's prose and silently deleted it, leaving grammatical sentences with the technical content removed. Same hazard as the 850-bif2 capability-row incident. Fragment rewritten with a quoted delimiter.
- E2E: not run (immutable Linux routes to curl-install e2e; no newer published release than v56.9.2.1 to test). Scheduler: due:timer-inactive — session cron still to be armed (856-s56y).
- Cycle metrics (verbatim): expert_accuracy pass=31 graded=33 rate=93%; flow cycles=3 avg_completed_per_cycle=0.33 overhead_ratio=11; timing build_check_ms_avg=73856 slowest=litmus:release-gates-run-locally:261039; plan packets=588 ready=397; mcp health=ok surface exposed.
