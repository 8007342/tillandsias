## Cycle 2026-09-13T21:35Z — yoga (linux-next)

Attested MO-FULL at 6a42ccd29. Claimed and completed 1139-xe5m in one story; selection was `tillandsias-plan next linux-immutable --limit 5` (the batch selector was not used this cycle — a single p1 row from the fail-loud-diagnosis-milestone epic), positive control on the claim printed 0.

- 1139-xe5m completed at 224e29a52, closed at 83f2a886e. `--capabilities` served the on-disk cache with nothing in the envelope saying so; the document now carries `envelope_source` and the one line a forge receives carries `accel_source=measured|served|unknown`, appended last. Verified live here with XDG_CACHE_HOME redirected: measured 0.58s, served 0.04s, timestamp replayed digit for digit, stored cache `null`, and the two envelope lines differing in `accel_source` and in no other key. Fixture 6/6 at the filename the row reserved, bound at gate step 305-1139-xe5m; three unit tests pin the library half; 699 tests pass under `--features tray,listen-vsock`.
- The envelope LINE, not only the JSON, was the half the row did not name and the half that reaches the reader it is about: an agent holding TILLANDSIAS_ACCEL_ENVELOPE cannot open the producing host's cache file.
- Left open for the row owner: the `unscoreable:` block is discharged by the gate step, but converting it into `verifiable_closure` means writing a multi-line field into a landed fragment. Flagged, not hand-edited.
- 1165-g6wx (previous cycle, same session) landed at cb8316c42 and closed at a1c2bd76b. Its first land was refused by `violation:spawn-in-if-not` on my own probe — an `if ! … | grep -q` guard whose false branch was the quiet verdict, i.e. 1076-kft9's shape in new code. Rewritten to the `case` idiom.
- Flow record emitted (cycle=yoga-20260913T214824Z). `verdict: attention:experts-never-called` — no expert was consulted this cycle; the work was a code defect with a measured reproduction already on the row.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
