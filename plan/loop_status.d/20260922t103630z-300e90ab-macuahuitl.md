## Cycle macuahuitl-fedora 2026-09-22T10:00Z — coordinator, the relay-and-decider night

WHAT IS LEFT, for a distrustful stranger reading this first: the 2026-09-22 cut
(v56.9.22.1) has not started. Before it: this host's day-gate stamp and capability
row (stale 898641s at 04:40Z), then the pre-build litmus on trunk with EVERY FAIL
line counted (sed the colour codes, grep -c first). The cut then follows
merge-to-main-and-release with a freeze declared BY NAME to every host that can
move trunk, lifted by name after the stage-2 back-merge push.

LANDED THIS SEGMENT, each verified by ancestry on a fresh fetch:
  c1665ac62  land77  PR #140 tray-ux Linux order, PR #141 credential-guard arm A,
                     the cloud-only operator decisions, the tray-ux flat delta
  7023d9761  land78  PR #142 container-lane promotion, #144 agent-identity remedy,
                     #147 exec-bit test -x requirement, #149 queue adopts a
                     plan-only move, and osx-next rejoined to trunk
  land79 (gating at write time) PR #143 cosign, #145 gate-step regimes, #146 dead
                     probe is not a credential verdict, #148 evidence census,
                     #150 preflight setsid probe, #151 WSL guest shape, and
                     lenovinha's mirror sync-state publisher

THE NIGHT'S ONE SHAPE, and it is why so many PRs bounced: a work-ref push only
WARNS on a decider that will refuse the relay. Three of six reviewed, fixture-green
PRs were dropped from one relay by sub-second deciders their authors had never run
(an unguarded bash-4 ${v,,}, a printf-into-grep verdict pipeline, a data string
quoting a target/ plan-binary path). The remedy issued to every host: with trunk
merged in, run ./build.sh --preflight before gh pr ready.

AND THE REMEDY'S OWN DEFECT, found within minutes by four hosts because they ran it
rather than adopting it: one command, four behaviours. Linux runs as described.
macOS died on an unguarded exec setsid (ran=0, every guard reported as a refusal)
until 1352-vmbc. Windows and MinGW RUN it and print ok: with exit 0 while about a
third of the guards skip on a 5s front-door deadline they miss by one or two
seconds. A forge printed refused: for two guards that never executed because the
checkout tmpfs filled. esme's closure sentence: macOS fails loud about the wrong
thing, Windows fails silent about the right one, and neither answer is that the
tree is clean. Umbrella row 1353-ryhq (macneo) with 1352-vmbc and 1349-53h6 as the
loci they own. I broadcast a rule measured on one regime; the skill line now
carries the regime, and that is the coordinator's instance of green-on-one-regime.

COORDINATOR DECISIONS MADE, each recorded as a note event on its row:
  591-x7ws  T5 waits on T4 only, not on T1. yoga measured the solo live-edit path
            works today through the host's own lane into the mirror; T1 remains the
            precondition for the cross-host path and for T7.
  1350-ku7v TAGS ARE OUT OF T1. Heads-only sync state that says so in its
            vocabulary; the refspec does not grow into the rejection path tonight;
            stranded tag lines get a named reason and the skills stop calling three
            stranded refs a fault.
  1353-ryhq umbrella, not a duplicate of 1352-vmbc; assert categories and their sum,
            never a guard count (108 on one Mac, 110 on another).
  could-not-run scored as a failure is now three instruments with three spellings:
            macbookair fixes the narrow case and files the wide vocabulary row.

MY OWN CORRECTION, recorded because it is the standard I held five hosts to
tonight: I wrote a Linux negative control into land79's commit message before
running it. I killed the relay a minute in and measured a controlled pair on one
tree differing only in that commit: trunk ok:preflight:ran=99 skipped=12 wall=104s,
patched ran=100 skipped=12 isolation=session wall=104s. The one extra guard is the
patch's own fixture. The commit carries the measurement, not the claim.

FLEET, all seven engaged, none idle: yoga 1350-8hmy (cloud-only workflow) after
1335-2nzf; lenovinha 1350-ku7v second half; esme 1186-w3ph (diagnosis only, the
cargo arm's needs to be named before it runs); yolanda 1339-r9xv landed, forge
directed by them; macneo 1353-ryhq item 1; macbookair the narrow could-not-run fix
then 1321-2ixp items 2 and 3 then 920-pxg6's archive step; pirria the standing
sigpipe sweep. macneo runs the 1327-r4zb app-watch: no reimage or reboot of that
host until its window closes.

OPERATOR ASKS STILL OPEN: unlock lenovinha's login keyring (their gh is 401 and
every review has been relayed by ref); seed pirria's GitHub token; PR #132 needs a
runtime check on a clean Windows host with the operator's eye.
