## Cycle — lenovinha (linux_immutable, linux-next), 969-nhh7

CLOSED 969-nhh7. The implementation had ALREADY LANDED at 8b08a0305 by
macuahuitl-tillandsias-forge; what remained was exit criterion 4. I did that
plus an independent verification of criteria 1-3, and did NOT rebuild a landed
fix.

READING THE EVENTS BEFORE THE CONTEXT IS WHAT SAVED THE CYCLE. The dispatch
described this row as the highest-value open security item, still ready and
unclaimed — all true — and the context block reads as though nothing exists,
because it is a filing-time snapshot. The events carried a passed live negative
control and a released claim. A cycle that trusted the context would have
re-implemented a shipped fix.

VERIFIED INDEPENDENTLY rather than on another host's self-report, in the exact
condition that caused the defect. A repo under a GLOBAL core.hooksPath pointing
at a directory holding only post-commit — the measured forge shape, where the
redirect also steers every repo away from .git/hooks.
  BEFORE: git resolved hooks to the global dir; post-commit alone; no pre-push.
  AFTER:  repo-local .git/hooks with pre-commit + pre-push + the post-commit
          trailer; the GLOBAL dir unchanged, still exactly one hook.
  PUSH:   refused by the pre-push hook, and ls-remote showed the ref was never
          created.

THE HONEST LIMIT OF MY OWN CONTROL, because "green is not proof" cuts both
ways: my synthetic repo refused for a VERSION parse error, not a stale gate
stamp. So I proved the hook FIRES and the push DOES NOT LAND under the redirect
— the property that matters — not that I reproduced the stale-stamp refusal.
That one is the forge's evidence, on a different host. Two hosts, two
conditions, same conclusion, and neither of us verified only our own change.

THE CALLER, since a gate nothing runs is not a gate: lib-common.sh:1440, in the
lifecycle, SYNCHRONOUS before the background lane. I checked the call site
rather than the definition — a grep proves the function exists, which is not
the question.

CRITERION 4: order 448 re-qualified, NOT closed. Its guard is a pre-commit
hook, and forges were the host class behind all three measured cheatsheet-drift
incidents while carrying no pre-commit at all. That class is now covered; two
gaps survive and neither is a forge problem (a non-forge host that never ran
install-hooks.sh, and --check still running no pre-commit). The operator
decision 448 names is unchanged with a smaller blast radius. Re-qualifying is
not closing and the criterion asked for the former.

OVERLAP CHECKED as asked: 965-rb3v is TILLANDSIAS_FORGE_SEED_BRANCH resolving
to main in the launcher; this is hook installation in the lifecycle. Same
symptom class, different code, different fixes. Taking both would have produced
the two half-fixes the dispatch warned about.

FLAGGED, NOT FILED: the forge's own closing note records that now the gate
actually enforces in forges, the push loop is fetch -> rebase -> gate -> push,
every rebase stales the stamp so the gate must re-run, and exposure is gate
duration times fleet push rate (90s here, 276s on the slowest host). I hit it
tonight — a push loop reported PUSHED while still two commits ahead, and it
took three rebase-gate-push rounds to land. They judged it needs a decision
rather than an implementation; after tonight I think it has earned a row, but
that is a queue-discipline judgement and belongs to the coordinator.

metrics:
  gate: ./build.sh --check green
  fixture: scripts/test-forge-project-guard-hooks.sh PASS
  independent control: hook fires under core.hooksPath redirect, ref never created
  real environment: global core.hooksPath still unset, worktree clean
