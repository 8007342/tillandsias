## Cycle 2026-08-24T18:53:52Z (linux_immutable yoga/silverblue — meta-orchestration full; 4-hourly loop iteration 1)

### BATCH
epic=stable-milestone-v1 seed=silverblue-unknown-amd+npu-yoga-20260824
size=10 budget=10 pick=4/7 triage: eligible=168 ungrouped=25 epics=11.
Took the p0 urgent: 865-n8vq, the trunk unreleasable behind seven litmus
failures.

### TWO OF THOSE SEVEN WERE NOT TRUNK DEFECTS
cross-target-cfg-gate-check — FALSE POSITIVE, fixed (a12554087). The step
walked back a fixed 5 lines from each libc::getuid/geteuid/fork hit looking
for #[cfg(unix)]. Its one reported violation, headless/main.rs:1278, is
inside ensure_lane_process_env(), whose #[cfg(unix)] sits 15 lines up at
1263 — outside the window. The call was correctly gated the whole time and
the guard could not see it. Now walks back to the enclosing fn and reads
that item's attribute block. Negative control against a copy with every
#[cfg(unix)] stripped: 4 violations detected, exit 1 — it still catches
what it exists to catch.

podman-idiomatic-launch-routing — ENVIRONMENTAL. headless/build.rs:139
hard-panics without images/router/tillandsias-router-sidecar, a build
artifact never committed (710-w9kc); the litmus invokes cargo test directly
and never stages it. One scripts/build-sidecar.sh cleared it, no source
change. Filed 871-wrmv.

spec dev-build: 4/7 -> 7/7 (100%) on linux-next.

### RECOMMENDATION FOR THE REST OF 865-n8vq
Re-run the failing set on a host with a warm toolbox and a staged sidecar
BEFORE working any of them. The 17:44Z list of seven overstates the trunk's
real debt, and at least one more entry has the same smell. The cheapest
reduction available on that packet is deleting blockers that were never
real.

### ALSO FILED
871-sr4r — ensure-toolbox-include-shape timed out at its 30s budget on a
host the runner itself measured as unsaturated (load1=3.08/12cpu), then
passed on the next run. A flake. Stopped-toolbox hypothesis recorded but
NOT measured, so it is written as a hypothesis.
871-4c3u — every litmus run here opened with warn:litmus-degraded-no-yq and
then printed an ordinary green Status: [PASS]. A verdict that knows it is
unreliable should say so where automation reads it, not in a preamble only
a human scrolling up sees.

### HOST STATE, POST-REBOOT
Up 13h31m, booted 2026-08-23T22:11Z. Five enclave services left Exited
after the shutdown with nothing restarting them. tillandsias-builder being
down is what removed yq from PATH and degraded every litmus verdict above;
starting it fixed that. tillandsias-vault, -proxy and -inference remain
stopped — I did not restart the product enclave. Daily maintenance ran
(marker was 08-23): podman prune, delegate sweep clean, nix lane
skip:no-nix on this immutable host, experts ok:experts-healthy at
source_commit == HEAD, clock NTP-synced.

### SWEEPS
Stranded: population=3 in_progress=3 stranded=2 (642-fedr, 776-cm74),
unchanged, not bulk-closed. Ledger check ok at 574 packets, malformed=0.

### COST NOTE FOR 656-spux
./build.sh --check profiled "Cross-target workspace check (656-spux)" at
36.3s here, against the 0.3s attested by lenovinha in ed14751e3. Not
chased — recorded so the next cost claim is checked against more than one
host.
