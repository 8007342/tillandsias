## Cycle 2026-08-19T04:23 PDT→05:00 PDT (windows — YOLANDA, MO FULL: overnight loop cycle 11, 833-w8rc's design question answered)

**Result: the packet's stated blocker — "a running container has no channel to receive new credential material" — is wrong. The channel exists, is already load-bearing, and one ~5-minute attended experiment converts this from a design question into an implementation task.**

- **Host**: YOLANDA, `windows-next` e34bb1a24 → 7baea48a8 (claim) → this cycle. Preflight `ok`, guards credential `ok:gh-keyring` / branch `ok:branch-windows-next` / expert-base `ok:expert-base-ready`, opsx `ok:clean-tree`. Boundary `/tmp/meta-orchestration-boundary.U2ycJC`. Sibling heads: main=0bba6525f, linux-next=6de002620, osx-next=9a47f1ddd.

**THE CHANNEL.** The Podman secret is read exactly ONCE, at container start: `vault-agent-bootstrap.sh` copies `role_id`/`secret_id` into two tmpfs files at 0400 and `exec`s Vault Agent. After that the Podman secret is irrelevant to the running container — which is precisely why the host destroying it never disturbed a live mirror, and why 828-k3mq's symptom only surfaced at max_ttl. What Agent consumes thereafter is the FILE: `secret_id_file_path = "/tmp/tillandsias-vault-secret-id"` with `remove_secret_id_file_after_reading = false`.

**The option is the evidence.** Agent's approle auto-auth deletes that file after reading by default; the only reason to set it false is that the file must survive to be read AGAIN at the next authentication. This repo's own comment states the rationale outright. So rotation is: write fresh material into that tmpfs path inside the running container, on **stdin** per order 424 (`podman exec -i … 'cat > …'`), and the next auth attempt picks it up — no restart, no new Podman secret, no posture change. `/tmp` is a writable tmpfs and Agent runs as uid 1000.

**NOT MEASURED, and I am not pretending otherwise.** That Agent re-reads per authentication is inferred from the option's documented semantics plus the repo's stated rationale. Strong, but inference. The settling experiment is written into the packet as four steps and needs a live enclave. Deliberately not run unattended at 04:30 — and landing a credential *rotation* mechanism whose central premise is unverified would be exactly the pattern this milestone keeps filing packets about.

**A WRITE-ONLY SIGNAL, found in passing.** `policy_role_name` says containers wire `VAULT_ROLE` "so vault-cli knows which login to perform when the secret-id is rotated". `VAULT_ROLE=git-mirror-agent` is indeed set on the mirror — but vault-cli has no approle login path at all; grepping finds only `renew-self`. The env var exists for a rotation that was anticipated and never built: the same write-only-signal shape 806-2r4s and 824-vr2d record elsewhere. Noted rather than deleted, since it becomes load-bearing if this rotation lands.

**NOT DONE, carried (all named on the packet):**
- The ~5-minute attended experiment.
- The host-side trigger question: periodic SecretID-age check, versus reacting to the mirror's own `agent-unauthenticated` verdict. The latter is attractive because the signal already exists and is published where the host can read it — but it is reactive, so the mirror is briefly write-blocked before rotation fires.
- Whether rotation should also rewrite ROLE_ID (it does not change, so probably not).
- Released to `ready` rather than held: the next step needs a human at the keyboard, so holding the claim would only hide it from the selector.
