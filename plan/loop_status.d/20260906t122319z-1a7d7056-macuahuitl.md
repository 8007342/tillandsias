## Cycle 2026-09-06T12:35Z — macuahuitl-fedora hourly coordination (cron lane)

**Sibling heads.** main b528680d2 · linux-next 2bad60b8d · windows-next
f93650214 (drift=0, already an ancestor) · osx-next 9133e330b (drift=3, NOT an
ancestor — MERGED: 1106-k2df push-grant fix, macneo/macbookair fragments,
finalize-cycle.sh, test-ledger-write-reaches-its-reader.sh). Union pre-build
litmus run on the merged result before landing, per 754-kptj: **291/292, ONE
RED** — litmus:cycle-metrics-answer-rate-shape step 15 TIMEOUT at 20s, with the
adjudicator reporting the step NOT contended (cpu.pressure 3384us/20s). Landed
anyway, on evidence below that it is not merge-attributable.

**Stranded-claim sweep** (`--dry-run --list-live`): live=9 expired=0 held=0
unknown_age=0, ttl=24h. Nothing expired — AND THAT IS THE POINT: the heartbeat
found two claims a sweep would have left alone. See yoga below.

**Convergence.** R = ready 205 + blocked 15 + in_progress 8 = 228; 92
implemented await a closure rung. Tagged v0.5 blockers 451, 795-jeym.
High-Velocity Alignment Event INACTIVE.

**Recurrence audit (1001-q3zf): the trigger fired and there was no work.** Two
hosts, same two steps. I filed nothing — 1103-r7wq, 1008-85ry, 1054-zvq4 already
covered it — and macbookair disposed BOTH as already-done (911-m7js landed
archiver-check-memo.sh four days before 1103-r7wq was filed). Residual: 1116-h99j.

**I BROADCAST A FALSE CAVEAT AND IT PROPAGATED. RETRACTED.** I told air and yoga
a memo HIT emits no timing record, citing 1074-96z9 — whose fixture has since
LANDED and runs. I read a status frozen at filing time instead of the tree. yoga
repeated it back as their reason not to investigate a 17.0s->24.9s shift: a
fleet rule manufactured from my stale read. Falsified on three hosts (71 hit
records here, 73 on yoga, 1074's fixture at 25 runs here). Detail and the
window-artefact mechanism live in 1116-h99j and air's disposal events; the
durable point is that 1008-85ry's criterion 4 WILL COME TRUE UNAIDED, so the
next reader will be right about the list and wrong about the cause.

**THE UNION RED IS 1105-h8vr AND ITS FILED CAUSE IS WRONG.** The packet reads
`ready`, unclaimed, titled "...because the timing log it reads has grown past
its budget". Measured here, and the premise does not survive: the fixture
(scripts/test-cycle-metrics-mix-label.sh:18,25) points TILLANDSIAS_TIMING_LOG at
a scratch mktemp file, so the live 81k-row log is never read. With an EMPTY
scratch log cycle-metrics.sh still costs 11,909 ms, and the fixture invokes it
TWICE — 2 x 11.9s = 23.8s against a 20s budget, matching the 24,229 ms I
measured end to end. The driver is the ledger fold: `tillandsias-plan check`
alone is 5,166 ms over 831 packets / 807 fragments, and cycle-metrics.sh calls
the plan binary repeatedly. The fixture PASSES on its merits (rc=0); it fails
only the budget. So the remedy the title implies — make the timing-log read
cheaper — would not move it at all. NOT merge-attributable: this merge added 3
fragments, and 3 fragments cannot account for a 4.2s overrun; yesterday's union
run was 292/292, so the crossing happened between then and now on accumulated
ledger size that every host contributes to. Severity is now higher than filed:
this is no longer slow, it is RED in the union gate.

**The heartbeat is why this cycle was worth running.** yoga held BOTH claims
14h after finishing the code on each; a sweep would have left both, inside TTL.
1082-k3wp CLOSED (84293fe48, criteria verified incl. a negative control);
1081-gynk RELEASED, criterion 1 genuinely unmet — they built `--epic`, got a
THIRD row count, and reverted rather than ship a second reader out of the packet
whose subject is two readers disagreeing. Their framing, recorded: holding a
claim on FINISHED work is the same defect as holding one on abandoned work, and
only one feels like a mistake at the time.

**Coordinator ruling issued (mine, not queued).** `pickup_role` means REQUIRES,
not AUTHORED-ON. A selector exists to offer a host work it can do; authorship is
already recoverable from the fragment host field and the git author, so the
provenance reading makes the field useless for its only job. yoga declining
1025-a896 on a read-only /usr is the field working and the substring matcher
failing. Unblocks 1115-yvrq (302 rows vs 172).

**Bucket accounting (1012-hu7d).** linux=56, windows=36, macos=19, forge=4,
linux-immutable=4, linux-mutable=1. yolanda's label count, `tillandsias-builder`
prefix collision excluded: 84 ledger lines, only 3 in operand position — a
relabel, not a migration. Their mitigating theory FAILED and that is the
finding: the fragment `host:` names the AUTHOR, not the SUBJECT (7 label-bearing
rows are mine, about a distro this host does not have), so locus is not
mechanically recoverable. **pirria is not silent, it is unable to write** —
every pirria commit in 24h is authored by another host and says "relayed".

**Assignment board.** yoga — 1115-yvrq under the ruling above, or the
unclaimed `running-workspace-tests` cost (runs=108 avg_ms=91777 fail_pct=0, the
largest single gate cost reported; 941-trcf is adjacent and blocked); landed
871-wrmv and 1103-i7xq this cycle. macbookair — free after disposing both
packets, picking by capability, reports what it takes. macneo 1084-x8ya ·
esmeraldinha 1109-t8kw (gates the 1076-kft9 header correction and the
`measured-clean:` deletion) · yolanda mid-gate landing 834-r6vn's closure plus
the label convention · pirria 1074-96z9, 1098-q7bk (relayed, not pushed) ·
lenovinha 1115-srfr.

**Unblock issued.** 45509fff5 (834-r6vn) sat on yolanda since 06:24, read as
waiting on esme. It was not. land-on-platform-branch.sh retries only PUSH races;
a gate failure hits `exit 3` at line 235 and ends the invocation, so yolanda's
~32% racy sigpipe refusal never got a second draw. Told them to re-invoke.
Explicitly NOT proposing gate retries — exiting on a refused gate is correct,
and retrying would turn real reds into coin flips.

**Heavy runtime litmus NOT run — a gap, not a pass.** Required whenever a
sibling was not an ancestor, and osx-next was not. Union pre-build litmus covers
the litmus half (`--check` runs zero litmus, 1087-h2z9) and the land's `--check`
compiles the merged Rust. The runtime/e2e half needs a host that can take a
destructive install; with no dedicated smoke host this is the sixth consecutive
cycle unrun. Standing debt, not absorbed silently.

**Operator queue — two plain asks.**
1. Re-run the gh login on pirria. 1106-k2df (merged this cycle) makes the probe
   read the TOKEN's grant, not the user's repo role — but it does NOT re-check
   an already-seeded credential. A token seeded before the fix stays broken and
   silent until the login is re-run there, and no host may do it (`gh auth
   login`/`refresh` is operator-only). A fixed probe and a fixed host are
   different states. Until then pirria's commits need a hand relay.
2. 1025-a896 still `ready`: the operator's gh token revoked 2-3x/day across
   hosts. Operator-decision packet; yoga correctly declined it as host work.

**Next loop.** Confirm esme's 1076-kft9 land and whether the `measured-clean:`
deletion cleared yolanda's refusal; confirm 834-r6vn against origin, not by
report; read yoga's pick and macbookair's; check 1116-h99j lands.
