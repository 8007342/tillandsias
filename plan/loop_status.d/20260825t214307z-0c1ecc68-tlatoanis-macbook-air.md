## Cycle 2026-08-25T21:43Z (macos tlatoanis-macbook-air — the daily-maintenance body found a dead gate, so the body WAS the work)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, full mode, iteration 9. Guards green: `ok:gh-keyring-push-verified`, `ok:branch-osx-next`, `ok:experts-healthy` + surface attested exposed, `ok:capability-row-reported`, deslop not due (order=886 last=834 delta=52, under threshold).
- **Ran the daily-maintenance body FIRST**, as the previous cycle's handoff said this host should. It had been `due:stale:2026-08-20`. It is now stamped, with each step named honestly: delegate-sweep no-registered-runs, podman-prune skipped (no machine running on this host), nix-gc skipped-absent, cargo-gc deferred to cycle end per `build_cache_hygiene`.
- **The body itself surfaced a dead mechanism, and that became the cycle's work.** `check-build-cache-sweep.sh` reported `bytes=0:gib=0` for a `target/` that `du -sh` puts at 15G. **`du -b` is GNU-only and BSD du REFUSES it** rather than ignoring it, so the command substitution was empty and the `|| bytes=0` fallback beside it silently became the answer. The SIZE half of a two-trigger gate could never fire on macOS; only marker-age could. Filed and closed as **886-yizb** with evidence.
- **THE PART WORTH READING IS WHY THE LITMUS WAS GREEN THROUGH ALL OF IT.** `litmus:build-cache-sweep-trigger`'s DUE-by-SIZE case sets `TILLANDSIAS_BUILD_CACHE_MAX_GIB=0`, so `gib >= 0` holds whatever `du` returned. **It pinned the COMPARISON and never the MEASUREMENT** — and a test shaped that way passes identically on a working host and a broken one. 709-in2f closed it 8/8 from a Linux host in complete good faith.
- **Generalisable, and the reason it was filed rather than quietly fixed**: a threshold test that sets the threshold to zero is testing the comparison operator. Any gate that MEASURES and then compares needs one leg where the measurement itself is the assertion, or the measuring code is untested by construction and the failure is invisible on whichever platform the author was not using.
- **A SECOND GNU-ism in the same suite, found only by running it here**: the DUE-by-AGE step built its old marker with `date -d '30 days ago'`, so that step had been **RED on every macOS run since it landed** and nobody was looking. A suite that can only pass on one platform is not a fleet gate. Now BSD-arm-first; the suite is **9/9 on macOS**, where it was 8 steps with one permanently red.
- **`check-bash-dialect.sh` (761-g36m) exists to stop exactly this family** and had rules for bash-4 constructs and `date(1)` and none for `du(1)`. Added one, with the same following-window and line-exemption convention, so a portable `|| du -sk` arm nearby still passes.
- **Hand-falsified in both directions**, which is the actual deliverable rather than the one-character fix: reintroducing `du -sb` makes the dialect check emit `blocked:bash4-unguarded:1` naming the exact file and line, AND turns the new litmus leg red (`output=` against its expected string) while steps 1-3 stay green — demonstrating directly that the old suite could not see this. Restoring `du -sk` returns `ok:bash-dialect-clean` and 9/9.
- **The gate caught my own shortcut and was right to.** The first version of the fragment set `status: done` with no evidence-bearing event; `./build.sh --check` refused with `violation:closure-without-evidence` (686-7qcm). Added a `completed` event with `evidence_refs` rather than working around it.
- **Stranded claims improved sharply**: `population=4 in_progress=4 stranded=3`, down from 11/11/7 two hours ago — the 24h reaper and sibling hosts releasing their claims. Worth watching rather than acting on; the ratio is still high against a small population.
- **No packet claimed from the batch this cycle.** The maintenance body produced a closed p2 defect in fleet tooling instead, which is a valid reduction step and is where the envelope went. E2E not run.
