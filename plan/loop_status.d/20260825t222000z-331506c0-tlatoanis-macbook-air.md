## Cycle 2026-08-25T22:20Z (macos tlatoanis-macbook-air — release-readiness prep, and I falsified my own finding)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 11. Boundary snapshotted FIRST this time (the 888-3d6x lesson from iteration 9), clean tree both ends.
- **Context**: coordinator (macuahuitl) is cutting an operator-directed daily, then per-platform curl-install e2e, then a stable promotion if all three pass. macOS had ZERO validation of v0.4.260817.1 — in fact this host has never had that release installed at all; `/Applications/Tillandsias.app` is a LOCAL build (git 0548ee1f2, Aug 15). macOS is the long pole.
- **Reported readiness GREEN** with the state they needed: 12 GiB VM substrate would be destroyed, 808 GiB free, gh auth live, `TILLANDSIAS_DESTRUCTIVE_RESET_OK` unset everywhere, release ships the arm64 macOS lane, and the published `install-macos.sh` handles quarantine correctly (probes the xattr, warns only when present).
- **Neutralised a real trap without deleting anything**: a stale `~/Applications/Tillandsias.app` from Aug 14 (git e71bc6f7) — the "live mixup 2026-07-16" shape the runbook warns about. Inert tonight because `/Applications` IS writable so the installer and the runbook agree on the path, but the installer's `if ! [[ -w /Applications ]]` fallback would split them. Coordinator ruled move-not-delete; moved to `Tillandsias.app.pre-e2e-20260825`, verified reversible and still runnable.

- **I FILED 889-bx99 AND THEN FALSIFIED IT MYSELF, ~40 minutes later, on this same host.** It is now `obsoleted` with a `falsified` event. I claimed the app writes capital-`T` `Tillandsias` while the smoke runbook removed lowercase `tillandsias`, making the destruction a silent no-op on a case-sensitive volume — a false PASS on the precondition that gates stable promotion. **The stored name is lowercase**, and it matches every source of truth (status_item.rs:367, diagnose.rs:71, e2e-preflight.sh:112, uninstall.sh:19). The runbook was right on every path.
- **THE METHOD ERROR IS THE PART WORTH KEEPING**: I tested a case-sensitivity hypothesis by TYPING a capital-`T` path and watching it resolve. On a case-insensitive volume every spelling resolves, so that observation carried **exactly zero bits** about the stored name — it re-confirmed the volume's case-folding, which I had already established separately. Then I reported the case I had typed as the case on disk. **A case hypothesis can only be tested by reading the name the filesystem STORES — list the parent — never by probing a name you supplied.** More generally: an observation identical under both hypotheses is not evidence.
- **The cost was real, which is why this is recorded at length**: the coordinator upgraded it from a risk note to a release-gating prep task, held a promotion-validation run for it, and had relayed it to lenovinha as grounds to weaken their macOS positive control and to the operator as a defect in the validation path. All three corrections went out, sourced to me. I told them the moment I found out — mid-fix, before the commit landed — rather than after.
- **What actually found it**: being pushed to FIX it rather than note it, which sent me to the code for the authoritative path. A finding I was allowed to leave as prose would have survived.

- **WHAT SURVIVED THE RETRACTION, kept deliberately separate from it**: the macOS destruction step really was one prose sentence while the Linux branch beside it captures `PIPESTATUS` and then proves the store is empty. That is the **727-kmks asymmetry one layer down**, on the path that tests PUBLISHED releases, and its justification does not touch letter case at all — a removal that silently matched nothing FOR ANY REASON would let the smoke report clean-room against pre-existing state. The step now removes and then ASSERTS absence (`02-macos-residue.txt`, fails on residue), and carries a note recording the retraction so the case question is not re-opened.
- **Told the coordinator their decision was moot as stated** rather than accepting credit for solving the problem they thought they had: their stated worry (an accidentally-correct step) never existed, AND the surviving change is a genuine improvement. Both halves, said plainly.
- **Started flagging which of my claims are MEASURED and which are INFERRED**, unprompted, after this. The `/Applications`-writable claim stands because `[ -w ]` plus `ls -ld` tests the actual thing; the case claim did not because it tested a string I supplied.
