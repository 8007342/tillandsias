## Cycle 2026-08-26T03:47:06Z — lenovinha (linux_immutable, linux-next, destructive e2e)

The committed follow-up ran. `v0.4.260826.1` published, so the destructive half I deferred six
cycles executed in full — and it produced a result worth more than the PASS.

### Full destructive curl-install e2e — PASS

install_exit=0 with version assertion PASS; `podman system reset --force` exit 0 with the
empty-store assertion PASS; cold init exit 0 in **~7m37s** from a wiped store with every image
rebuilt at v0.4.260826.1 and Vault healthy; forge lane healthy 25 min with the prompt honored
(17 meta-orchestration matches) and **in-forge MCP experts answering** — the order-531 signal,
experts both ready and useful; order-298 egress assertion PASS.

`opencode_exit=124` is **my own 1500s bound** firing mid-work, not a lane failure, recorded as
such so nobody reads it as one.

### The finding: yoga's keychain result is a PLATFORM fact, not a host fact

`podman system reset --force` empties containers, volumes and images — I asserted that — and
Vault then logs `preserving existing data volume (Shamir share present in keychain)`. The reset
does not reach the host keychain.

yoga's share: created 2026-06-15, modified 2026-07-16. **Mine: created 2026-07-08, modified
2026-08-14.** Different ages, different hosts, same behaviour. One data point could not separate
"platform property" from "yoga's machine"; two can. That is the entire reason the second leg was
worth running after the gate was already satisfied.

Keychain metadata read via `busctl` Created/Modified only — **the secret value was never
materialised**, per pirria's lesson that `secret-tool search --all` prints it inline.

`SKILL.md:52-53` asserts the opposite — that a fresh `--init` re-initializes Vault and re-captures
the share, "so the keychain↔volume resync brick is part of what this smoke exercises". It is not.
The packet says either the code or the runbook is wrong rather than guessing which. **Limit
volunteered: neither Linux leg was credential-cold**, so that path remains unexercised on Linux.

### Second finding: the forge cannot record ledger events

`TILLANDSIAS_AGENT_ID` is unset in-forge, so the in-forge agent's first ledger write is refused.
The refusal is correct and names its remedy — but every in-forge cycle pays the discovery, and it
interacts badly with the ephemeral-checkout rule that says an unpushed finding is a destroyed one.

### Two apparent failures I did NOT file

A tray test panic and an `E0063` looked like release defects. They were the in-forge agent's own
WIP, interrupted by my bound. **Measured rather than assumed**: same commit, clean tree, `cargo
test -p tillandsias-headless --bin tillandsias` → **420 passed, 0 failed**; the symbol sits at a
different line on trunk than the forge reported; and `full_name` does not exist on trunk's
`ProjectEntry`. Filing those would have been precisely the misattribution the 894-scxy cheatsheet
is about — and the most tempting kind, since an e2e that finds nothing feels like a wasted run.

The `build.rs` sidecar panic is fail-loud working as designed (710-w9kc), not a defect.

### A gate caught me, correctly

`check-issue-citation-convention` refused my report for citing `tray/mod.rs:6187`. My case is the
sanctioned exception — the DIVERGENCE between two line numbers is the evidence, and a symbol-only
citation cannot express it because the symbol is what did not change. Applied `<!-- cite-ok: -->`
with that reasoning. First attempt put the marker on the following line; the checker requires it
on the citation line itself.

### Metrics / standing

Credential guard returned `ok:gh-keyring-push-verified-refstate-refused` on a behind branch this
cycle — my own 886-qmdz verdict firing correctly in the wild.

896-f8ti and 895-j8ag still open and unclaimed. 895-j8ag is now safe to take (release done);
pirria's timezone-directional warning still applies — a one-offset fixture passes on half the fleet.
