## Cycle — macuahuitl 2026-09-03T23:40Z (coordinate-multihost-work, cron 23-odd)

- **Merges.** `osx-next` 1 ahead, merged — macbook's claim on 795-bmbq
  (interactive TUI amputated to work around argv-join). `windows-next` 0 ahead /
  8 behind, levelled again with ancestry re-verified at push time; fifth time
  today. It keeps drifting because yolanda pushes in bursts rather than because
  anything is wrong.
- **Claims healthy: live=1, unclaimed=0, expired=0, held=0.** One live claim,
  taken 24 minutes ago and being worked. That is the board in the state it is
  supposed to be in, and it is the second cycle running.
- **Roll call.** macbook 23:16Z and yoga 23:19Z active within the half hour.
  lenovinha 3.6h and yolanda 3.4h — both finished their last packets rather than
  stalling, so I ASKED both and dispatched rather than inferring.
  macneo silent 14.5h; already an operator-queue question about the host, not
  another ping. pirria, esme and both forges finished or blocked by their own
  statement.
- **Dispatched from the marker rather than by guessing.** `ready | grep -v
  worked:` gave ten genuinely untouched rows in one command; a fortnight ago that
  was a per-packet event sweep. lenovinha → **995-srbf** (the login poll gates on
  presence and never on validity — the defect behind the operator's own 22-hour
  outage), fallbacks 970-7fqk and 991-85bh. yolanda → **979-xyub** (a
  curl-installed macOS host cannot publish a capability row), chosen because its
  last exit criterion asks whether Windows has the same gap and they are the only
  host that can answer it; fallbacks 968-uhzg and 986-andw.
- **yolanda's 997-e4v2 quarantine fix is merged, gated and level on both
  siblings.** Recorded last cycle in full; the short version is that they
  improved on the prescription esme-windows and I had written, caught a
  capability loss my version would have shipped, split structurally rather than
  by prose, mutation-verified in both directions, and applied this morning's
  cfg-gating lesson to their own new code unprompted.
- **`scripts/finalize-cycle.sh` is in use and was exercised in both failure
  modes before being trusted** — refused on an uncommitted tree, and refused
  again when I had composed its internal order backwards. Neither produced a
  marker. It came from lenovinha's measurement and it is the first remedy the
  token-consciousness directive produced.
- **Litmus:** not run. The merge carried one claim fragment; `--check` passed on
  the merged tree, including yoga's new `check-ca-path-literals.sh`.
- **Operator queue, five plain asks, unchanged:** the esmeraldinha PAT (confirm
  Contents:write, re-run from a checkout with an origin); macneo's forge-build
  permission; is macneo-macos still up; the root-vs-forge privilege call, now
  with yolanda's finding that the migration dissolves the class rather than
  patching it; the ten quarantined trees.
- **Nothing is blocked.**
