## Cycle 2026-09-04T08:15Z-08:55Z — yolanda (windows), advance-work-from-plan

Second cycle of the restart, fired by the 2h cron. Boundary taken FIRST this
time, on a clean tree, before any ledger write. 997-e4v2 slice 2 landed.

- **The host `~/src` drvfs mount is gone** (`e061b9d5f`). `home-forge-src.mount`
  mounted `%USERPROFILE%\src` at `/home/forge/src`; drvfs reported every file as
  uid 1000 while the headless evaluated as root, `git rev-parse` exited 128 on
  dubious ownership, and healthy checkouts were renamed aside. Removed the unit
  injection and the `systemctl enable --now` clause. Safe only because slice 1's
  per-launch tmpfs already exists — tmpfs first, mount second.
- **Forward-only was honoured as an ABSENCE, and the absence is pinned.** The
  tempting kindness here is a `systemctl disable` so hosts that already enabled
  the unit stop mounting. That is exactly the compatibility code the operator
  ruling forbids. A delete-only diff cannot show a decision not to add
  something, so the test asserts `systemctl disable` does not appear.

**The near-miss worth recording is the anchor, not the deletion.** The comment
I deleted, `// 5. home-forge-src.mount`, was the START ANCHOR of the 798-emje
provisioning window — used by both the in-file Rust test and the vsock litmus
YAML to prove the module loads before the units start. Deleting it would not
have failed loudly: the awk window goes empty and the litmus reports FAIL with
blank line numbers, or `.split().nth(1)` returns None and the test panics about
something unrelated. Re-anchored both on `// 5. HOST SRC MOUNT REMOVED`.

**And the first replacement marker was itself a silent trap.** I initially used
`THE HOST ~/src MOUNT IS GONE` — the `/` inside terminates the awk regex in the
litmus command, so the pin would still have run and still have printed `ok:`
while measuring a different region. Caught by running the litmus step by hand
against the edited source rather than trusting that a green pin means the pin
still means what it did: `ok: module-load-precedes-unit-start (67<98)`, both
inner markers confirmed still inside the window.

**Negative controls, again, both directions.** Re-introducing the unit path as
production code turns the new test red (23 passed, 1 failed) and green on
restore, injected line confirmed absent afterwards. The first version of that
test also had to be fixed for the opposite defect: scoped to the whole file it
matched its own assertion literal, making it a check that could only fail — the
mirror image of one that can only pass. It now strips comments and scopes to
the production half, which also protects the explanatory record from being
deleted by someone making a test go green.

**Out of scope, named:** `vm-layer/src/vz.rs` is the virtiofs/macOS lane, not the
Windows tray. Its `After=home-forge-src.mount` at :791 comes from an fstab line
at :692 and is pinned at :3113; removing that fstab entry without the pin is a
silent ordering regression (701-iu9b). It belongs to the macOS half of slice 3.

**Next:** slice 3, the twelve-file menu/type removal. Cannot be finished here —
macos-tray does not type-check on yolanda — and host-shell `menu_state.rs` /
`menu_action.rs` needs a heads-up to macbookair first, since all three trays
consume it.
