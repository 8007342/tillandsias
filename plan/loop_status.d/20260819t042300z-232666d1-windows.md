## Cycle 2026-08-18T21:23 PDT→22:00 PDT (windows — YOLANDA, MO FULL: overnight loop cycle 4, 823-u5zf mechanism correction)

**Result: 823-u5zf's deliverable names a mechanism the Windows lane does not use. Corrected with measurement, released, and made cheaper than it was written.**

- **Host**: YOLANDA, `windows-next` 07a7e9bd3 → 99d801d85 (claim) → this cycle. Preflight `ok`, guards credential `ok:gh-keyring` / branch `ok:branch-windows-next` / expert-base `ok:expert-base-ready`, opsx `ok:clean-tree`. Boundary `/tmp/meta-orchestration-boundary.A7KT8K`. Sibling heads: main=0bba6525f, linux-next=6de002620 (merged), osx-next=40004a25a→58278aec2 (macOS active).
- Claimed and pushed before work; released to `ready` at exit.

**THE CORRECTION.** The packet says the tray should "feature-detect CAP_EXEC_ARGV_VECTOR and send PtyOpen{argv, env} verbatim". It doesn't send PtyOpen at all: `notify_icon.rs:3765` spawns `wsl.exe` in a native terminal (conhost or wt.exe) through CreateProcess. Grepping the whole windows-tray crate for PtyOpen / server_caps / CAP_EXEC_ARGV_VECTOR finds only hvsocket test round-trips and a doc comment — no production caller. That capability governs the control-wire exec path, a different transport. Implementing the packet as written would rewrite lane launch onto the wire and discard the terminal window the operator types into — far from the "mechanical, not a design problem" the packet claims.

**THE GOAL IS STILL REACHABLE, AND CHEAPER.** `argv_survives_wt_reparse` admits `[A-Za-z0-9 / \ . - _ : =]`. Measured against the real argv: today's flattened form fails on exactly ONE token (the whole `export … && exec …` script), which is the entire reason every project lane falls to conhost and cannot paste. Every token of the verbatim form — `/usr/local/bin/tillandsias-headless`, `--cloud`, `8007342/tillandsias`, `--claude` — passes. So emitting an unflattened argv reaches `spawn_wsl_terminal` with **no wire change, no capability negotiation, no PtyOpen**.

**WHY THAT IS POSSIBLE NOW:** the preamble's only irreducible side effect was `install -d -m 0700 "$XDG_RUNTIME_DIR"`, and `ensure_xdg_runtime_dir()` already moved it into the binary under this same order. What remains is pure env defaulting, which belongs in the binary for the reason the packet already accepted.

**A QUEUE ASSUMPTION CORRECTED.** `829-p7wq` ordered 827-h4tz before this on the belief that tray verification was blocked. It is not: `cargo test -p tillandsias-windows-tray` builds and runs here — **93 passed, 1 failed, 7 ignored** — and the single failure is `embedded_guest_headless_matches_workspace_version`, the staged-guest version skew, unrelated to argv or wt logic. The 7 `argv_survives_wt_reparse` and 4 `wt_safe_title` assertions are among the passing 93. Slices (a)-(d) are verifiable without clearing 827-h4tz first.

**NOT STARTED, deliberately.** ~25 minutes remained after the measurement. Slice (b) edits a file shared with the macOS lane and needs four test rewrites; half-landing that on the lane the operator needs for device-code logins is precisely the failure mode this packet warns about, and the cycle that filed it declined on the same grounds. The measurement is this cycle's deliverable; the implementation wants a full window and should be the next cycle's single item.
