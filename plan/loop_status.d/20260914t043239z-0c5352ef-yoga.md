## Cycle 2026-09-14T04:32Z — yoga (linux-next)

1176-fn2p, assigned and claimed by macuahuitl on trunk at 45cc65a0a; landed at be9f54100 and closed with that SHA. lenovinha's OOM row, worked here because yoga is under the same 16 GB cliff and both halves are measurable on it.

- TWO HALVES, AND THE DIVISION IS THE DESIGN: the floor catches the START state, the post-mortem catches the RUN state. lenovinha's two kills were the second kind — enough memory to begin, none left inside clippy — so the floor alone would NOT have caught them. Said plainly rather than implied.
- THE FLOOR IS A LEGIBILITY DEVICE, NOT A CAPACITY PREDICTOR. Default 1 GiB MemAvailable against yoga's measured 11-12 GiB at rest on a host that completes this gate routinely. An arm asserts the shipped default stays at or under a quarter of a completing host's headroom — a property, not a literal, so a later tightening reds it. That is the packet's first negative control.
- MemAvailable, not MemTotal: the row's own measurement shows the total was constant across five successful lands and two kills on the same day. What moved was what was free.
- THE POSITIVE CONTROL ON THE OOM PROBE IS LOAD-BEARING: `journalctl -k -g <pattern>` prints "No entries" and exits 0 both when the window is quiet AND when the user cannot read the kernel journal — identical output for opposite facts. The probe asks whether ANY kernel line is readable first. Measured unprivileged on yoga: journalctl -k returns lines, dmesg answers "Operation not permitted", which is why the journal is the source.
- Wired in two places for two deaths: _run when a child exits >= 128, and the land driver when the gate produced NO failing line — the case where its own message already said "may have died rather than refused".
- NC2 enforced twice, by construction and by position: clippy exits 101 and never reaches _run's branch; the driver consults only on the no-match path; and a --victim narrows the kernel record. An arm asserts the consult's position by LINE ORDER.
- Fixture 12/12; four mutations, each diff proven non-empty first, all red by name.
- A FIXTURE BUG CAUGHT BY ITS OWN MUTATION: arm 9 first ranged from the sentence "no violation/refusal line matched" to the next `fi`, and my change MOVED that sentence below the consult — the range began after what it was looking for and reported the wiring missing. A matcher anchored on text the change itself moves is measuring its own edit.
- The land took three attempts: two lost the push race while origin moved, the third landed. The floor ran live in every gate — ok:gate-memory:11654MB available, floor 1024MB.
- Not done, deliberately: no rung below one job. 1047-h88p untouched; the row rules it out of scope.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
