## Cycle 2026-08-23T07:37Z (windows/esmeraldinha — two packets implemented, and the first fix for one of them was wrong in a way a unit test would have passed)

- **Host**: esmeraldinha, Intel N100. Branch `windows-next`, fast-forwarded 18
  commits `8f44aa575` -> `5c3c6ab2c`. **No linux-next merge was needed**: unlike
  last cycle, `origin/windows-next` already CONTAINED `origin/linux-next`
  (0 behind, 2 ahead), so the prompt's fast-forward step was a no-op and the
  real action was catching the local branch up. Verified rather than assumed.
- **mcp_outage: forge-plan,project-info** — still down; all reads via the
  filesystem and the verified binary.
- **Instrument verified, and the `rc=0` was interrogated rather than trusted.**
  `ensure_fresh_plan_binary` returned 0 with the binary's mtime UNCHANGED and
  two hours old across 18 commits. That looks exactly like a false pass, so I
  checked: `git diff 8f44aa575..5c3c6ab2c -- crates/tillandsias-plan/ Cargo.lock
  Cargo.toml` is EMPTY. The binary is genuinely current and rc=0 is true.
  `compact` not invoked.
- **851-cduu is now `completed`** on three-host evidence, including this host's
  verification last cycle.
- **859-b2zc IMPLEMENTED — and the blast radius was four scripts wider than I
  filed it.** `hostname` is absent from every Fedora image here, and FIVE
  scripts re-derived the chain inline instead of sourcing
  `agent-identity.sh`. Two matter more than the one in the packet title:
  `check-resumable-claim-dirt.sh` (the 833-fpe7 deadlock detector — it answered
  `unattributable:host-unresolvable`, which falls through to the dirty-start
  refusal, so the detector written to discharge that deadlock was inert inside
  a forge) and `reclaim-stranded-claims.sh` (wrote `host: unknown` durably into
  append-only ledger fragments). Also `select-work-batch.sh` and
  `windows-host-capability-probe.sh`. Verified live: the WSL gate locus went
  from `unavailable:host-unresolvable` to `ok:capability-row-reported:esmeraldinha`.
- **The half that is not a wider chain**: repairing the fallback ALONE would
  have turned the forge from silent into noisy — no `--hostname` at launch and
  no exported workstation, so it would publish a matrix row per container
  (measured id `d872da8c03df`). It now returns
  `unavailable:forge-identity-ephemeral` and names the remedy.
- **858-ihcb IMPLEMENTED, and my FIRST fix was wrong — this is the useful part.**
  `unique_prompt` incremented a global nonce, but every call site invokes it
  inside `$( )`, a SUBSHELL, so the counter never advanced and every "unique"
  prompt was byte identical. The harness reported `prefill_tok_s=1528` — it
  reproduced the exact defect it was written to remove, while looking fixed.
  A unit test calling the helper directly in one shell would have seen three
  different strings and PASSED. Caught only by disbelieving a suspicious number
  and running a three-arm control on the live endpoint: identical 1338-1448
  tok/s, nonce-prefixed 93-148, fully distinct 125-140.
- **Corrected numbers**: T0 prefill 1334.59 -> **138.68** (9.6x), T2 prefill
  480.93 -> **17.67** (27x); decode unchanged (28.99 / 6.38) exactly as
  predicted. **The validation that matters**: this host's own 2026-08-16
  BARE-METAL figures for the same models were 108.9 and 19.0, taken with a
  different harness on a different substrate. The corrected numbers land within
  ~10% of those; the cached ones were 12x and 25x away. An independent
  measurement the new code knew nothing about now agrees with it.
- Also: `project:` no longer multiplies two superseded constants — it declines
  (`full_rebuild=skipped:no-measured-corpus-chunk-count`) unless given a
  measured count, and the embed chunk size is REPORTED (`chars=`) and defaults
  to the corpus p50 rather than an undocumented 2000.
- **Both fixtures wired into `./build.sh --check`** and both proven to catch
  their defect: `test-capability-row-check.sh` 9/9 (fails against the old
  chain), `test-bench-prompt-uniqueness.sh` 6/6 (fails 2/6 against the pre-fix
  form). The bench fixture intercepts `curl` and inspects what the REAL call
  sites put on the wire, because that is the only level at which the subshell
  defect is visible.
- **The mandate is now machine-enforced**: `select-work-batch.sh windows`
  answers `refused:no-tier-work: this is a low-end host (tier gate, 847-wgy4)
  ... the mandate forbids the general queue`. No general drain taken; the
  operator's 2026-08-16 prose constraint is now a gate.
- Compaction became eligible mid-cycle (`fragments=31 reason=fragment-count`,
  `malformed=0`). NOT run: it is ledger GC rather than a perf packet, other
  hosts write concurrently, and this host was twice warned off that command.
  Reported for whoever owns ledger hygiene.
- Advisory still open: `stale:windows-sources-never-verified:4`. This IS the
  Windows host that could clear it (`cargo test -p tillandsias-windows-tray`
  natively), but there is no native cargo lane here — `./target/` does not
  exist by design, since `with-wsl2-builder.sh` and this host's 2026-08-16
  record both forbid a cargo target tree on the NTFS volume. Establishing one
  is a real decision, not a chore; flagging rather than doing it.
- Left alone deliberately: `derive-host-identity.sh` has a fifth copy of the
  chain but a COMPLETE one, for a different purpose (compound roster slug).
  Not broken; consolidating it would churn host seeds for no correctness gain.
