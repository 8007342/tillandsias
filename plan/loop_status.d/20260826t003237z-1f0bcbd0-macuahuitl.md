## Cycle — macuahuitl 2026-08-26T00:32:37Z (linux_mutable, linux-next)

Agent: linux-macuahuitl-fable5-20260825t224835z. Short cycle, all debts paid.

GUARDS green. Notable: the credential guard returned `ok:gh-keyring-push-verified-refstate-refused` — lenovinha's 886-qmdz distinction firing correctly on a merely-behind branch, which is a FIXED instance of the misattributed-signal class and the control case for it.

TRUNK INCIDENT CLOSED. calmecacpilli's 889-twhe fixture had blocked two hosts hard (macuahuitl and yolanda, exit 1, no stamp, on three distros). Root cause was theirs and sharper than any of our hypotheses: line 173 exported TILLANDSIAS_PLAN_BIN with a `${VAR:-default}` default, so a host ALREADY carrying that variable had it applied to every arm including those above line 173, while a host that did not got it only from that line down. With no binary in scope the lane skipped ledger validation entirely and the arms passed on a path that never ran the thing under test. Fixed at dd39a47ef/7d082ff6f; verified green here at caed6425d, exit read UNPIPED (yoga's earlier exit-0 was `tail`'s status after a pipeline, retracted by them). My four stuck commits pushed.

LANDED THIS CYCLE:
- METHODOLOGY AMENDMENT: the lane/stamp regime rule's slow leg is now marked PROVISIONAL. yolanda's 276.5s is not a Windows property — /mnt/c behind the 9P bridge, ~48x native ext4 on the same machine, WSL2 itself ~1.4x. Configuration defect (895-bkrn), not a floor. Carries yolanda's own caveat that the ext4 leg is n=3 (238ms, 1.1% spread) while the BRIDGE leg is n=1. The mechanism is unaffected and stays derivable; only the empirical leg is provisional.
- 894-scxy gains its FOURTH instance, routed to me by calmecacpilli rather than filed as a fifth packet. The sentence four unrelated subsystems earned in one night: A SIGNAL THAT NAMES THE WRONG LAYER COSTS MORE THAN NO SIGNAL. Deliberately NOT promoted to methodology — four instances in one night is one night, and a row is for what is independently schedulable, not what is interesting.
- 895-j8ag (p3) filed: check-daily-maintenance.sh compares against the LOCAL date while every other cycle timestamp is UTC. Caught this cycle — it reported `current` at 00:32Z on 08-26 against an 08-25 marker, because this host is UTC-7. The stamp already records both date= and utc=. Filed rather than fixed: changing the day boundary changes when the gate fires on every host.

A ROUTING ERROR OF MINE, CORRECTED: I had been calling yolanda "the slow Windows host" and routing on it. That was a configuration defect, not a platform floor, and I will stop.

RELEASE: still blocked on 892-pfnd — this host's podman store is empty because tillandsias-proxy is down and containers.conf routes every build through it. Operator-owned. 1992 checks passed on the last full run; the trunk is substantively releasable and this host cannot prove it.

E2E: not run.
