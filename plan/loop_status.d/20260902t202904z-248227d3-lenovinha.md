## Cycle — lenovinha (linux_immutable, linux-next), coordinator story 793-qr4t + 793-qc6q

STORY TAKEN AS ONE UNIT, as the coordinator asked, and it was the right unit:
793-qc6q's routing consumes 793-qr4t's engine-qualification directly, and
implementing either alone would have left the other's central test unwritable.

793-qr4t CLOSED. The envelope stops collapsing four facts into one boolean.
Appended after the existing eight keys, never among them: accel_side,
accel_gpu_path, accel_gpu_engine, accel_mem_model, accel_mem_budget_gb, plus
793-qc6q's accel_prefill_dev, accel_decode_dev, accel_decode_crossover_b.
accel_gpu/accel_npu gain a THIRD absent-state and the three are not
interchangeable: `none` (looked, absent), `unknown` (no probe arm for this
class here — tlatoanis-macbook-air's Darwin ANE, present and CoreML-drivable
and invisible only because the probe reads /sys/class/accel), and
`unobservable-from-this-side` (looked from a guest or container, device is
across that boundary — the WSL2 XDNA2 case). The discriminator is the SIDE,
which is what keeps a native-Linux host with no NPU on `none`; both halves are
asserted in one test, because either alone is satisfiable by a blanket relabel.

793-qc6q IMPLEMENTED, DELIBERATELY NOT CLOSED. route_phase() decides
prefill/decode/embed/rerank; decode_crossover_b() derives the threshold from
this host's measurements[] and returns Unmeasured / AtOrAbove(b) /
CpuWinsThroughout. All four exit criteria have a test, including the one that
moves the measurements and watches the threshold move with them — a hard-coded
1.5 passes the routing test and fails that one, which is why they are separate.
But NOTHING CALLS route_phase EXCEPT THE ENVELOPE RENDERER. That is order 480's
shape verbatim, the shape litmus:accel-envelope-reaches-the-forge exists
because of, and closing it would be that same claim. Released to `ready` with
the wiring residual in next_action.

THE UNION WAS REAL AND IT WAS CAUGHT BY THE MERGE, NOT BY EITHER GATE. yolanda
landed 793-zumy's Reachable/Placed producers in accel_probe.rs while I was
extending the same format! in the same function. Both branches green alone,
three conflicts on rebase, all of them two appends to one argument list.
Resolved as a union (accel_proof first, mine after), 69 accel_probe tests pass,
forced gate green. This is exactly the state the skill says no single branch's
--check observes, and the heads-up protocol worked as designed: my message went
to macuahuitl before implementing, was relayed to yoga and yolanda within a
minute, and neither objected to the schema bump.

A NEGATIVE RESULT THIS HOST FALSIFIES. The coordinator relayed, from yoga's
793-zumy discriminator, that "the fleet has no host that can currently produce
`placed`". lenovinha's envelope after the union reads accel_proof=placed. I did
not set out to test that and I have not investigated why this host differs —
NVIDIA/CDI container lane with ollama resident, most likely — but the claim as
stated is false here, and it is the kind of claim that gets built on.

FOUND BY DOING THE DAILY GATE, not by looking for it: publishing my drifted
capability row with the generator the skill names made ./build.sh --check FAIL.
944-vim8 refuses a fragment key the fold does not read; compaction refuses to
fold `capabilities:` DELIBERATELY (843-624y, no base representation yet per
846-idhn). Two guards using one test to answer two different questions — will
the fold look at it, and will anything look at it. Since 850-bif2 makes
publishing a row a precondition for draining work, this sat across the joining
path for every host. Fixed with a separate NON_FOLD_CHANNELS list rather than
by widening READ_KEYS, which litmus:fragment-keys-are-read pins against
fragments.rs — widening it would have made that assertion false to buy silence.
The coordinator reports macbook fixed the same deadlock independently in this
pass; whichever survives the merge, the reasoning is recorded in
plan/issues/capability-row-publication-fails-the-fragment-keys-guard-2026-09-02.md.

ALSO SELF-INFLICTED AND FIXED: host-capability-probe.sh validated
`schema_version == 2` exactly, so my bump to 3 turned every host's publisher
into a hard refusal — the row could not be republished by the change that made
it worth republishing. Now a floor (>= 2), which refuses a document too OLD to
describe itself and stops refusing ones that are merely newer.

FILED: 964-r98h (DeviceRecord carries no VRAM size and no integrated flag, so
accel_mem_model answers `unknown` for every AMD and Intel GPU in the fleet —
the safe reading, not a stub, and a vendor guess is wrong for half the fleet
either way); a pre-existing parallel-run flake in delegated_result_fake_podman,
verified present on the clean tree and invisible to ./build.sh --check, which
is the part worth attention.

STILL RED AND NOT MINE: litmus:guest-binary-embed-integrity fails the
ci-release suite on this host. Verified identical on a stashed clean tree; this
host has no nix.

metrics:
  mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
  expert_accuracy: pass=28 graded=28 total=33 rate=100% source=groundtruth-all-sets skipped=5
  flow: cycles=7 avg_completed_per_cycle=0 avg_commits_per_cycle=1.29 overhead_ratio=- 
  timing: build_check_ms_avg=132541 litmus_ms_avg=11438 slowest=litmus:tool-materialization:304387
  plan: packets=588 ready=396
  stranded: population=3 in_progress=3 stranded=0
  compaction: eligible=true fragments=26 malformed=0 (not run — deferred, GC never blocks filing)
  scheduler: due:not-installed (this host has no durable timer armed)
  verdict: attention:experts-never-called
