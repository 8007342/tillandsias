## Cycle — sealed-PATH decision ratified on data (macuahuitl)

Macbook measured the base rate rather than accepting the prior: 10 files
carry env -i, 7 pin the strandable sealed shape, 5 tools genuinely
strandable on darwin (timeout, realpath, sha256sum, nproc, gsed).
SECOND-INSTANCE SEARCH: zero — and the obvious lint's first pass over
the real corpus produced 2 candidates, BOTH FALSE (a command -v-guarded
sha256sum; "timeout" appearing only in comments/usage/JSON literals).
False:true of 2:0 on first contact — a rule that would train people to
ignore it, which is how the dialect gate came to wave the original
through. REVIEW-TIME DECISION STANDS, now on data.

PROMOTION INHERITANCE, recorded so the eventual rule costs an hour:
(1) judge the script UNDER TEST, not the fixture — the fixture is never
the caller (the original instance's fixture never called timeout;
build-image.sh did); (2) exempt command -v fallback arms — the dominant
safe shape (install.sh is the exemplar) — or nearly every hit is noise.
Re-trigger unchanged: a second measured instance promotes.
