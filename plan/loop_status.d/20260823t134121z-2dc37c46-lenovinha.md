## Cycle 2026-08-23T13:41:21Z (linux-immutable lenovinha — meta-orchestration full; /loop iteration 6: 815-gdjk + the 805-3egx vote)

### 815-gdjk COMPLETED: ONE CACHE ROOT, MEASURED BEFORE AND AFTER
The packet demanded the ten-minute experiment before any code, and it earned
its keep: the split is REAL (XDG set -> the accel probe wrote to HOME/.cache
while every shell site resolved under XDG). tillandsias_core::cache_root is
the one resolution; six deviating sites routed; the model cache ADOPTS a
pre-XDG download (rename; keep-legacy-loudly on failure; never deletes)
instead of orphaning 1.5GB+. Verified both directions post-fix; unset-XDG
hosts byte-identical. At aa5892c86.

### 805-3egx: LENOVINHA'S FLEET VOTE RECORDED
Approve mesa vulkan userspace in the inference image (inert without
passthrough; unblocks yoga's measurement); conditional-approve /dev/dri
strictly tier-gated on gpu-rocm — never cpu/gpu-cuda (this host's Cezanne is
a 2GiB-carve-out Vega beside a strong Zen3; the windows crossover does not
transfer); OLLAMA_VULKAN placement must be pinned until the workload-shape
router exists. The decision packet stays open for the remaining host kinds.

### 829-dkuc: DELIBERATELY NOT RE-CLAIMED
Its remaining half (the sweep protocol) wants operator pairing or sweep
budget per its own next_action; a 2h loop iteration is neither. Left for the
right session rather than nibbled.

### METRICS
- routed batch: epic=convergence-velocity pick=7/7 route=rank:2/7:width=7
- flow: claimed=1 completed=1 commits=5; guards all ok; grade 22/22
- e2e: not run — loop iteration; release-currency unchanged
