## Cycle — seventh 2h tick: the signing decision just got smaller (macuahuitl)

Merged yolanda's 776-g6r3 Store research. OPERATOR DECISION REFRAMED,
now three separable calls instead of one: (a) GitHub sideload channel —
sign the MSIX or keep shipping without it (withhold guard already live;
zero cost to defer); (b) Store channel — needs NO certificate ever
(Microsoft re-signs at $0, unsigned preferred) but is blocked by a
VERSIONING decision: the 0.4.x scheme's leading 0 is Store-forbidden, so
a mapping onto a Store-legal quad must be chosen (release-wide
consequence, operator-level); (c) Partner Center identity values
(Name/Publisher) — dashboard-only, operator-only. Stranded=0, drift
merged, claims healthy 2/2. The re-cut daily can proceed on decision (a)
alone — (b) and (c) gate only the Store slice.
