## Cycle — lenovinha (linux_immutable) 2026-08-24T15:53:25Z — the cross-target gate, and the live break it found in its first minute

Full-mode cycle. All guards green; daily gate current. Deliberately took PRODUCT work this cycle after many cycles in litmus/ledger tooling.

CLOSED 656-spux (completed, with evidence). Its premise is that every host compiles for ITSELF, so cfg-gated code is verified by exactly the platform that cannot exercise the other arms. THE GATE'S FIRST RUN PROVED THE PREMISE ON TRUNK: `cargo check -p tillandsias-headless --target x86_64-pc-windows-gnu` failed with error[E0425] cannot find function `enforce_ca_key_mode` — a #[cfg(unix)] definition with THREE unguarded callers and no fallback arm, sitting on linux-next, invisible to every host's gate. 653-7rag's exact shape, found in the first minute of looking.

FIXED with a #[cfg(not(unix))] no-op counterpart rather than cfg guards at the three call sites: one place instead of three, since three guards are three chances to forget the fourth and the callers genuinely do not care about the platform. Not a Windows security regression — the key is not protected by mode bits there and that path receives it as a podman secret.

ANSWERED THE SCOPE QUESTION THE PACKET LEFT OPEN. Its notes ask whether Windows must build tillandsias-headless at all (it runs in the WSL guest, injected prebuilt) and say either answer closes the packet provided the boundary stops being undocumented. I chose MAKE IT BUILD EVERYWHERE: `cargo build --workspace` is the most ordinary command in a Rust repo and a contributor should not hit a wall in a crate they never touched. The fix was 4 lines and the workspace now cross-checks clean, so excluding the crate would have traded a real check for a documented exception.

EXIT CRITERION 2 TURNED OUT UNNECESSARY. It offered a cfg-exhaustiveness heuristic as fallback on the premise that `ring` blocks a full cross-check. The premise is true and the blocker is removable — mingw64-gcc is in Fedora, and with it the FULL workspace cross-checks clean in 37s warm. The real check beats the proxy, and the proxy would not even have caught this defect's shape: an unguarded CALL, not a missing arm at the definition.

LANDED scripts/check-cross-target-build.sh, wired into ./build.sh --check. Grammar: ok:cross-target:<target> / fail:cross-target:<target> (exit 1, error lines only — a full cargo log buries the three that matter) / skip:cross-target:<reason> (exit 0).

SKIPPING IS NOT FAILING, and this is the decision to challenge me on. I did NOT add mingw64-gcc to the builder toolbox init set: 33 MiB down / 115 MiB installed is an order of magnitude beyond the jq/yq/rg/openssl addition (6.3 MiB, +8s) that 799-tb7q justified, and forcing it fleet-wide as a side effect of a lint is not mine to decide. A host without it prints skip:cross-target:no-c-toolchain:x86_64-w64-mingw32-gcc — naming the exact binary, since the fix is one dnf install and nobody should have to reproduce the ring failure to learn that. THE HONEST COST: hosts that do not opt in get no cross-target coverage, so this protects the fleet only as far as the fleet opts in. Making it universal is a one-line init-set change and needs no edit to the script.

VERIFIED all four ways, which is the packet's own verifiable_closure — reintroducing 653-7rag's defect fails a gate on a Linux host with no Windows machine in the loop: fallback arm deleted -> fail: + E0425 "configured out", exit 1; restored -> ok:, exit 0; bare host with no toolchain -> skip:, exit 0; full ./build.sh --check green with the step wired in. Measured: rustup target add 2.8s, mingw64-gcc 33 MiB, warm cross-check 37s.

RESIDUAL, stated rather than smuggled into the closure: ONE target, one direction. macOS is not covered — no free cross-toolchain — so a cfg(target_os="macos") arm with no fallback is still invisible from here. Windows is the target this packet's originating defect actually reached and the one that is cheap; macOS deserves its own answer.

FRESHNESS: openspec/litmus-tests/litmus-cheatsheet-tooling-structure.yaml, verdict=refreshed, EXECUTED (PASS) and clean under the false-pass detector I committed last cycle. Noted in the stamp which of its four steps carries the real weight (the index-is-generated one), so a future trim keeps the right one.

ADVISORY: compaction not eligible. Deslop not due.
