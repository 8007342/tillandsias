## Cycle — macuahuitl meta-orchestration FULL (linux_mutable coordinator)

DAILY GATE: due:stale:2026-09-05 -> ran the body -> ok:daily-maintenance-current:2026-09-06.
Stamped with named steps. Skipped with reasons recorded: cargo clean (281G free,
target 35G, no pressure) and post-restart verification (containers up 21-36h).
Boundary snapshotted and verified; checkout lock held.

RELEASE DUTY: ATTEMPTED, BLOCKED. All four preconditions held — trunk gate-fresh
at eec480b5c, siblings converged at 0 after merging osx-next, plan evidence ok
(790 packets), no release in flight, last cut 2026-09-05. Computed v56.9.6.1,
zero tray-parity gaps, opened PR #109.

  ./build.sh --ci-full   cifull_rc=1     NINE conditions red
  release-preflight      preflight_rc=0  ok:release-preflight

No merge, no tag, no VERSION bump. main untouched at b528680d2. PR #109 retitled
DO NOT MERGE with the failure table and LEFT OPEN, because the runbook reuses an
existing linux-next->main PR rather than opening a second.

THE DAILY RELEASE GATE AND THE PER-LAND GATE DISAGREE ON SEVEN
CONDITIONS, AND THE FLEET LANDS THROUGH THE WEAKER ONE. Measured on
macuahuitl 2026-09-06T00:53Z against origin/linux-next at eec480b5c:
`./build.sh --ci-full` RED, `./build.sh --check` GREEN on all seven.

  1  clippy --all-features  empty line after doc comment,
                            tray/mod.rs:3359
  2  tray/vsock feature tests
                            pty_handler::tests::child_env_empty_input_
                            gets_path_and_proxy_exemption FAILED
                            (672 passed, 1 failed)
  3  guard activation       4 orphans; population=121 active=117
  4  no-python              python3 in 3 tracked scripts
  5  litmus                 ci-release / capability-routing-shape
  6  litmus                 git-mirror-service / forge-gitconfig-
                            bidirectional-quarantine
  7  litmus                 git-mirror-service / forge-standard-
                            gitconfig-path

Three of the seven arrived within eleven hours. The release gate runs
ONCE A DAY, so a full day of landings accumulates defects that no
individual land can detect. Every host tonight, including the
coordinator, landed green through a gate blind to all seven.

FAILURE 1 IS NOT COSMETIC. a00a0f76b deleted
`build_local_projects_submenu` and left its doc comment behind. That
orphaned block shifted the CLOUD submenu's entire doc — including
`@trace spec:tray-ux, spec:remote-projects` — onto `cloud_overflow_row`.
The real `build_cloud_projects_submenu` at line 3405 lost its
annotation, and `validate-traces.sh` still reports 100% coverage
because the marker exists in the file. A trace satisfied by a function
that does not implement it is a false green in the trace ledger.

RELEASE v56.9.6.1 NOT CUT. PR #109 retitled DO NOT MERGE, left open
because the runbook reuses an existing linux-next->main PR. No tag, no
dispatch, no VERSION bump; main is untouched at b528680d2.

THE E2E VALIDATION HALF WOULD HAVE BEEN IMPOSSIBLE EVEN ON A GREEN CUT,
and this is a standing structural fact rather than tonight's
scheduling. Order 1004-vsh2 forbids the destructive `podman system
reset --force` on an operator workstation without consent for THAT run,
and states that an orchestrator's instruction to run the procedure is
not that consent. NO DEDICATED SMOKE HOST EXISTS IN THIS FLEET:
yoga and lenovinha both inventoried live Vault stores, project mirrors
(tillandsias-mirror-tillandsias), spec-index volumes and build
toolboxes, and both are Silverblue — the platform the daily release
exists for. Neither could consent, and neither could I consent on their
behalf. Routed to the operator as a provisioning ask.

FILED 1086-kx8i p1, assigned lenovinha: the divergence itself, not the nine.

TWO CLAIMS OF MINE WITHDRAWN THIS CYCLE, both broadcast before being checked:
  - 'had check-plan-binary-current.sh been wired, the stale-binary incident would
    not have happened.' FALSE. macneo's host passes it at 5/16; it probes only the
    1079-qb8k claim refusal and never mentions expire-claims, under a verdict named
    for the general property. A wired guard would have green-lit the binary that
    produced the false accusation. Withdrawn to four hosts.
  - 'five of eight hosts are running a writing binary.' NOT DEFENSIBLE AS A COUNT.
    esmeraldinha established the fixture false-fails on Windows: --check re-execs
    into WSL, which cannot execute a .exe, so eleven arms fail with Exec format
    error and score 5/16 for a healthy binary. Their reading was not a measurement.
    The claim survives per host on other evidence and will be restated with its
    basis attached rather than as a number.

That is three false causal claims in one night, each a CONSEQUENCE attached to a
true observation, each with a ready mechanism. yolanda's rule from the receiving
end: state the observation and the inference as separate claims, so a reader knows
which half they are being asked to trust. macneo's diagnosis: a mechanism that
EXPLAINS an observation feels like evidence for it, most strongly when it is one
you already believe.

REMEDIATION CONFIRMED BY ARM COUNT, not by the word fixed: macneo 5/16 -> 16/16,
yolanda 5/16 -> 16/16 (rebuild alone; no PATH copy to install to), lenovinha
5/16 -> 16/16 via cycle-preflight, esmeraldinha 16/16 both artefacts. macbookair
and yoga were already 16/16. CAVEAT from esmeraldinha: cycle-preflight backgrounded
through a harness produced zero bytes, exit 0, and DID NOT REBUILD — run it in the
foreground, or a host stays stale while believing it complied.

OPERATOR QUEUE:
  - Grant revoke + login, macneo then pirria. pirria can no longer PROVE its host
    safe, not merely cannot push: binary 5/16, build-id disagrees with
    source-revision, base predates the fix, and no fetch or merge is possible.
  - Decide the python-in-scripts question: 7 invocation sites, 3 files, blocking
    the daily release. Rewrite in Rust or record an approval.
  - Provision a dedicated smoke host. No release can be e2e validated without one.
    yoga and lenovinha both inventoried live Vault stores, project mirrors and
    build toolboxes; the only Silverblue hosts, the platform the daily release
    exists for, are the operator's own machines. Standing, not incidental.
  - A full-reset authorization was granted to lenovinha with the destruction
    itemised, and correctly HELD unspent: it was granted to validate v56.9.6.1,
    which is dead at the gate. Recorded as standing, naming what it covers.
