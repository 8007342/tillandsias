## Cycle — lenovinha 2026-09-04T20:00Z (follow-up on 1004-inkc, same session) — the opt-out is now enforced fixture-only
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T19:49:43Z`:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
      flow: cycles=8 avg_completed_per_cycle=0.13 avg_commits_per_cycle=1.75 overhead_ratio=14 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      timing: steps=27650 build_check_ms_avg=140044 build_check_mix=mixed:forced=367,memoised=98 litmus_ms_avg=13419 slowest=litmus:tool-materialization:304387 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      repeat: window=since=2026-09-04T19:49:43Z steps=121 top3=build-check=2,build-preamble=2,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=2 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      recur: window=7d runs=25512 steps=429 top3=build-check:runs=327:total_ms=48498535:avg_ms=148313:fail_pct=30,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=228:total_ms=7623679:avg_ms=33437:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=228:total_ms=5665202:avg_ms=24847:fail_pct=0 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=59 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=228:avg_ms=33437:fail_pct=0:saved_ms_upper=7590242,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=228:avg_ms=24847:fail_pct=0:saved_ms_upper=5640355,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=220:avg_ms=19512:fail_pct=0:saved_ms_upper=4273312 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=705 ready=457 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=clean traces=current
      verdict: attention:experts-never-called
- LANDED 95026ae46 (verified on trunk before citing). macuahuitl accepted `--expect none` on one condition — that it be fixture-only and SAID SO where it is parsed, because an opt-out reachable from production turns the check back into one that cannot fail. That condition was right and I implemented it as an enforced rule rather than a comment.
- A LINT, NOT A RUNTIME GUARD, and the reasoning is the point. The obvious alternative is to honour `none` only when some TILLANDSIAS_*_TEST variable is set — but that variable is settable from production too, so it MOVES the hole rather than closing it, and adds a second thing that must hold at runtime. A lint over the tree needs nothing to be true at runtime.
- CONTROL RUN: a temporary script passing `--expect none` is REFUSED (violation:expect-none-fixture-only:1, rc=1); removing it returns ok. Prose mentions are excluded, so the rule keys on CALLERS rather than on the string appearing — otherwise the check's own explanatory header would have failed it.
- SECOND REMEDY TODAY THAT DOES NOT EXIST ON THIS HOST'S BINARY. macuahuitl advised `tillandsias --ensure-enclave` to recreate my proxy. Installed v56.9.2.1 answers "Unsupported option: --ensure-enclave", and it is absent from --help. This is the same shape as this morning's cycle-preflight REMEDY naming `tillandsias --init` as "re-run the enclave orchestration" when --init's own help calls it "Pre-build container images". Reported rather than substituted: there is no ensure/up script in scripts/, and orchestrate-enclave.sh always ends in `podman rm -f` of proxy/git/inference, so it cannot leave one running.
- THE PROXY STAYS ABSENT, AND THAT IS NO LONGER A MYSTERY — which is the fix working. preflight now prints `services-down0-absent1` and names tillandsias-proxy explicitly. The concern that "the next thing needing egress reads as a mystery" was true of the code I replaced this evening; it is the specific thing 1004-inkc removed. The forge launch path recreates the proxy on demand by design, as established this morning.
- Gate green 270s; enclave-service-health fixture still 23/23.
