## Cycle 2026-08-26T06:32Z — macuahuitl (linux_mutable, linux-next), hourly /loop

DRAINED THE p1 THAT HAD BEEN BLOCKED THREE TIMES: 606-um5s, the nixpkgs/crane
migration. Inputs landed at 9b0c27c1c; released back to ready with a precise
residual rather than left in_progress.

nixpkgs nixos-24.11 (EOL) -> nixos-26.05, crane v0.20.3 -> v0.24.0. A COUPLED
pair by construction (crane v0.21+ needs >= nixpkgs-25.11), which is why this
sat for months as "bump two inputs". Target chosen by ENUMERATING branches
(git ls-remote -> 25.05, 25.11, 26.05) rather than assuming a scheme.

TWO REAL API BREAKS, both found by evaluating:
  1. buildLayeredImage rejects `copyToRoot` — two different builders, two
     different names, and 24.11 accepted the wrong one. buildImage takes
     copyToRoot; buildLayeredImage takes contents. Confirmed by reading
     streamLayeredImage's formals in the locked nixpkgs, not by guessing.
  2. `nodePackages` REMOVED, and there is no pkgs.npm to move to (enumerated:
     npm ABSENT, nodejs/_22/_24 present). Dropped the line — npm comes from
     nodejs itself, VERIFIED because criterion 3 forbids losing a tool:
     nodejs-22.23.2/bin = corepack, node, npm, npx.

THE MIGRATION REPAIRED A PRE-EXISTING TRUNK BREAK. Measured both ways: the OLD
flake+lock cannot evaluate .#forge-image (same copyToRoot error, against the
OLD locked nixpkgs); the new one can. That output has been unevaluatable on
the trunk and nothing noticed, because `--check` does not evaluate the flake
and `nix flake show` does not force a derivation.

`nix flake show` SUCCEEDED with the nodePackages break still present — listing
a devShell does not force its inputs. A flake that shows clean is not a flake
that evaluates.

VERIFIED: flake check rc=0; aarch64-linux evaluates; realize proven
(router-sidecar built, REALIZE_RC=0, closure 1.52 MiB, ZERO runtime refs —
correct for static musl); check-nix-deps-stability ok (765-8hc3's invariant
survives the crane bump); devShell 64 -> 63 tools, name-diffed against the OLD
lock, NOTHING disappeared (bash -> bash-interactive is a nixpkgs rename, pname
confirms; python3.12-pip -> 3.13; npm consolidated into nodejs_22).

RESIDUAL, stated: five realizes remain (~1,000-crate cold compile each,
790-mbk9's unknown) plus their closure diffs. Note the forge-image old-vs-new
closure comparison CANNOT be produced — the old flake cannot evaluate that
output at all. `flake check --all-systems` fails on x86_64-darwin (glibc for an
FHS symlink); verified PRE-EXISTING, old lock fails it too; worth its own
packet since eachDefaultSystem generates darwin outputs nothing has ever built.

Coordinator: merged windows-next (4) and osx-next (11), no compiled source.
5a overlap check clean. Stranded 0. Gate green throughout.
