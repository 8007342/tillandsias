## Cycle — macuahuitl 2026-09-03T13:43Z (coordinate-multihost-work, cron 23-odd)

- **Merges.** `osx-next` 6 ahead, merged (macbook's all-platforms host-tools
  fixture, two attestations, two claims). `windows-next` 0 ahead / 38 behind,
  **fast-forwarded to level** — ancestry re-verified at push time as CLAUDE.md
  requires, `a932ac6e3..996192ae5`, through the hook with no override. Second
  time today; yolanda is still blocked on two operator decisions so it keeps
  falling behind, and a 0-ahead branch costs nothing to level.
- **THE CREDENTIAL GUARD PRODUCED TWO DIFFERENT SUFFIXES IN THIS ONE PASS AND
  BOTH WERE FULLY EXPLAINED.** This is the first cycle since I stopped piping it
  through `tail -1`, and it paid off immediately. Behind origin:
  `refstate-refused`, with five stderr lines naming the mechanism and the remedy
  `git merge --ff-only` — which I ran, and it was correct. After merging
  `osx-next`: `hook-refused`, a DIFFERENT arm distinguishing this checkout's OWN
  pre-push hook from the remote, remedy `./build.sh --check`. A day ago I would
  have reported both as unknown; yesterday I reported one of them to the operator
  as exactly that.
- **That second arm is 982-cs43 seen from the credential guard**, and it makes
  the distinction macbook was making by eye and getting wrong in their
  hand-rolled push loop — own hook versus remote. So the guard has at least three
  `ok:` arms and the PLAIN verdict is the rarest in normal fleet operation: an
  ordinary cycle passes through two non-plain arms in sequence. That strengthens
  993-yqie's position argument rather than merely illustrating it.
- **989-ykks: macbook's all-platforms fixture verified 11/11 on linux.** It
  partitions all three declared platforms from any one host, which is the form I
  commissioned — and they added three arms I did NOT ask for: `--platform os`,
  `mac` and `inux` must each select nothing, pinning exact rather than substring
  matching. That is the arm that would catch the next person's convenience regex,
  and it closes the loop from their earlier fixture hardcoding `macos` and going
  red on linux.
- **Claims:** in_progress=3, expired=0, held=0, unknown_age=0. 795-5itp (macbook,
  claimed 26 min ago), 978-juw4 and 980-xcaf (macneo).
- **Roll call.** yoga 13:17Z and macbook 13:17Z active within the half hour;
  lenovinha 12:22Z. macneo quiet ~4.5h holding two claims, so that is the one
  genuine unknown and I ASKED — including whether 980-xcaf is closeable, since
  they reported it verified end to end and a held claim on finished work is
  invisible to routing. pirria and lenovinha-tillandsias-forge both declared
  themselves done; macuahuitl-tillandsias-forge holding by instruction; yolanda
  blocked on the operator; esme still cannot push.
- **Litmus:** not run. The merge brought a fixture, ledger fragments and
  attestations; `--check` passed on the merged tree and the new fixture passes
  11/11. Recording the omission with its reason.
- **Operator queue, four plain asks, unchanged:** renew the esmeraldinha PAT
  (check EXPIRY first, and do NOT rebuild the git image for it); macneo's
  permission for the forge-build measurement on the operator's Mac; yolanda's
  re-open-a-closed-cycle decision; yolanda's two-hourly cron decision.
- **Nothing is blocked.**
