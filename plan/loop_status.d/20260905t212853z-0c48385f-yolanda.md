## Cycle 2026-09-05T21:28:53Z (yolanda — 1075-yuxt closed; the answer was the opposite of the one expected)

**Landed** 4b3080590: the tool-dispatch guard captures into a variable and tests
emptiness, so there is no early-exiting consumer and no SIGPIPE.

**I was asked to measure before fixing, and the measurement contradicted what I
expected to find.** The coordinator's brief said the packet might be
understating itself — that its "the producer emits one line today so the
pipeline is honest" claim could already be false on a drvfs checkout. It is not.
The real tree yields ONE match, this file's own exclusion removes it, so
`grep -q` never matches, never exits early, and there is no SIGPIPE:
**[0 1 1 1], 10/10, correct verdict.** The site is honest today here and its
priority does not move.

**I refined the mechanism rather than inheriting it.** esme established producer
I/O LATENCY as the cause in the sibling case — drvfs 10/10 against ext4 0/10 on
a byte-identical file. That predicts failure from a slow scan, so I tested
exactly that: the real 547-entry tree on drvfs, with an early-sorting offender
planted so the consumer exits while the scan continues. **Clean 10/10.** A
producer that has finished WRITING never receives SIGPIPE — a slow scan does not
matter once the output is done. For this site the trigger is the SIZE OF THE
MATCH SET.

| matches | producer output | PIPESTATUS | verdict |
|---|---|---|---|
| 1 (real tree) | — | `[0 1 1 1]` | correct: no caller |
| 40 | ~4 KB | `[0 0 0 0]` | correct: caller found |
| 1000 | 112,896 B | `[0 0 0 0]` | correct: caller found |
| 2000 | 226,896 B | `[141 141 141 0]` | **SILENT: all-clear** |
| 20000 | 568,896 B | `[141 141 141 0]` | **SILENT**, 10/10 |

The last PIPESTATUS entry is **0** in the failing rows — the consumer MATCHED.
The verdict was inverted by the producers' corpses, not by the test.

**That makes the packet's timing argument sharper than it claims.** The match set
is empty in a healthy tree and grows only when the defect is reintroduced across
callers. A SMALL reintroduction still fires; a WIDESPREAD one goes silent. The
check is loudest when nothing is wrong and mute when everything is.

**The threshold is evidence that it flips, not a safety margin.** A sibling
byte-margin model was falsified the same evening, so the table is labelled in
the comment as one host's measurement and the fix does not depend on it.

**The closure's negative control, honoured and measured** — 40 and 1000 matches
fire IDENTICALLY before and after; only 2000 and 20000 change, from silent to
firing. A bug fix, not a rewrite that changes answers the site already got right.

**PIPESTATUS rather than rc throughout**, because rc alone cannot separate "no
match" from "the producer died" — esme's first count conflated them and had to
be redone.

Agent: windows-yolanda-opus5-20260905t212853z
