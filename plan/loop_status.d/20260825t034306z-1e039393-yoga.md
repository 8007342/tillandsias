## Cycle 2026-08-25T03:43Z (linux_immutable yoga — contract repairs before a hazardous migration, and a salvage ref that turned out to be fine)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `loop-cron`. Guards all green; lock acquired and released; boundary clean at both ends. `tillandsias-proxy` still stopped (~7h now, `health=stale-healthy`) — third cycle unrestarted, and it is time someone decided.
- **A deleted salvage ref, chased and cleared.** The fetch pruned `origin/salvage/macuahuitl/20260824-dirty-start` — the copy of the 872-c9nd dirty tree, and by definition the only copy of work that existed nowhere else. Local ref gone, no reflog, no packed-ref. Recovered the commit from unreachable objects (`git fsck --unreachable` → `1589a268`) and anchored it under `refs/salvage-rescue/` before doing anything else, because a `gc` would have ended it.
- **Verdict: the deletion was correct and macuahuitl's claim was true.** Two of the three salvaged paths are byte-identical to `linux-next`; the third, `scripts/hooks/pre-push-local-gate.sh`, differs only in that linux-next has the deletion-protection block the salvage predates — i.e. the salvage is a strict subset. Their 874-w2gc closure event says the ref was verified absorbed, then the new guard REFUSED the deletion in production before it was consciously overridden. That is the guard working, on its first real use, on the ref it was written for. Rescue anchor dropped.
- **Worth the operator's attention**: 874-w2gc recommends a server-side GitHub ruleset protecting `refs/heads/salvage/*`. The local pre-push guard covers every fleet push path, but nothing local can stop a branch deleted through the GitHub UI or API — and that is precisely the shape that would take the last copy of a rescue with no hook in the loop. It needs repo-admin action.
- **795-5itp: the two contract repairs its `next_action` ordered first, landed.** (1) `openspec/specs/tray-host-control-socket` clause 3 said "No length limit enforced at protocol level". False since the code existed — `read_control_envelope` has always refused a prefix over `MAX_MESSAGE_BYTES`, and 828-r2ek added the outbound half. Now states the ceiling, names the constant, and says what each direction does, including that NO reply is sent. (2) The closure contradiction — the named scan demands one raw length decode while slice 1 deliberately KEEPS hand-rolled fake peers as interop evidence — resolved as a definition: production sites only, plus an explicit exception list where each entry carries its reason, the list being itself the assertion. A blanket `#[cfg(test)]` exemption would be wrong, because `tests/vsock_listener_e2e.rs` frames identically and should migrate.
- **Why the contradiction mattered**: the spec is what a reader consults before touching the framing, and it denied a bound they would find in the code. A migration trusting it builds `LengthDelimitedCodec` with no `max_frame_length` — 8 MiB, 128x looser than every reader here — and ships green, because until slice 1 that bound had no test anywhere.
- **The vsock_server.rs slice: designed, deliberately NOT started.** The hazard is worse than slice 1's warning. `serve_ready_stream` reads the Hello by hand, writes HelloAck, then `tokio::io::split(stream)` hands halves to a spawned reader and to 17 `write_envelope_with_shutdown` call sites. The shape that works is not `Framed::into_parts` but `Framed::new(...)` then splitting the FRAMED (sink/stream), never the raw stream — one owner of the read buffer across the handshake-to-loop boundary. Consequence: `write_half` stops being an `AsyncWrite` and becomes a `Sink<Bytes>`, so all 17 sites plus the `select!` shutdown arm move together. Atomic by construction, and a full cycle's work rather than a tail-of-cycle edit. Measured at HEAD for the next claimant: 18 raw decodes across 10 files. First test should be a Hello-plus-Subscribe pipelining regression written to FAIL against the naive split.
- **Claim released to `ready`**, packet not closed — one of ten sites is migrated and saying otherwise would be false.
- **Stranded sweep**: 831-ezea and 865-n8vq now read stranded (was 0 last cycle). Both are macuahuitl's active work this hour, not abandonment — reported, not touched.
