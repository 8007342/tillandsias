## Cycle — macneo advance-work-from-plan (cron de51113a, :40 lane)

Ledger-only, landed 41fc29d29 through the plan-only lane, verified ancestor with
a control the test can refuse.

Skipped 1084-x8ya deliberately despite it being the selector's urgent pick: I
assessed it last cycle and its remaining work is Rust wiring or a live VM,
neither in a cargo-check-only lane. Re-claiming to add nothing would be churn.

718-jqt5: claimed, measured the two criteria this host can settle without
compiling, released back to ready.

CRITERIA 1 and 4 MET, with a caveat worth restating. The projection is
deterministic — two runs at a fixed --now-epoch hash identically, and a
different limit hashes differently, which is the positive control proving the
comparison can detect change. But there is no --seed; the reproducibility
parameter is the EPOCH, and the command does not print it the way
select-work-batch prints its seed. Ten days on the clock yields a different set,
correctly, because the projection is age-ranked. A replay without the epoch
reads as nondeterminism rather than as a missing parameter.

CRITERION 2 UNMET IN BOTH HALVES, and the second half failed where the condition
is LIVE rather than simulated. Nothing routes story-shaped questions through the
projection. And the requirement that an absent inference endpoint degrade to the
deterministic list rather than refuse currently fails on this host: l0 ready, l1
unset, l2 unreachable, and a story-shaped question returns a typed `unsupported`
refusal with empty citations. Honest, but not what the criterion asks for, and
the list is one subcommand away.

A CORRECTION TO MY OWN PRIOR ENTRIES: I have written "experts report
source=absent on this host" several times, taking it from cycle-metrics. The
probe disagrees — l0 is ready and answered the query. `source=absent` refers to
that metric's own artefact, not to the expert layer. Repeating a summary line as
a measurement is the same shape as every other finding this week.

Keychain: no wedge. Probe rc 0, no orphans, no SecurityAgent.

Sub-agents: 0 of 4 budgeted.

tokens: ~70k this cycle, single agent, no fan-out, no Workflow.
