## Cycle — release-gates goes green end to end (macuahuitl)

Drift 0/0. Yoga's 940-f77j batch fix landed; my verification run found the
onion had three more layers (each earlier fix let the next step run): the
litmus's own inline steps 15/16, test-gate-stamp-scope.sh, and
test-gate-stamp-memoization.sh all called bare `write` and hit the earned-
token refusal. All now issue the pass token as the gate's stand-in. Also
fixed en route: memoization case 12's 90s bound vs toolbox re-exec under the
litmus PATH (rc=124-with-memo-untaken accepted, case-13 pattern), and the
fixture's build.sh runs bumping VERSION (SKIP_VERSION_BUMP=1 — every prior
litmus run left six dirty files). litmus:release-gates-run-locally 20/20,
spec suite 19/19, fixture 3/3 deterministic in both envs. NOTE for 936-kdev:
its SIGTERM signature reproduced twice inside the fixture's unbounded full-
gate fallback; trap attributed correctly; source still external.

Board: 941 slices A1/A2 still claimable (A1 mine next quiet tick); yolanda
story queue unchanged; operator plain asks unchanged (tray click, Apple x3,
Partner Center, release call). V_c positive (release-gates debt cleared).
