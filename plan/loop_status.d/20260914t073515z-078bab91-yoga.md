## Cycle 2026-09-14T07:35Z — yoga (linux-next)

1174-u5wp taken as promised at its release last cycle — a row that changes the tool every host lands with, given a fresh cycle with its own boundary. Claimed at e65d6b7dd, landed at f80ea839a.

- TWO DEPARTURES FROM THE ROW AS FILED, both deliberate and both recorded. "On entry" is the wrong place: the fetch-and-merge at the top of each attempt CHANGES THE TREE, so a stamp consulted before it describes a tree the tool is about to replace. The check sits AFTER the integrate, where a no-op integrate — yolanda's case exactly — leaves the stamp valid.
- THE FILE PREDICTED THE SECOND ONE. land-on-platform-branch.sh already carried 1056-5344's note that the gate is mandatory whenever the union-debt marker exists, "because the whole point of writing the debt down is that a future 'skip the gate when nothing changed' shortcut must not silently inherit it". This IS that shortcut. The debt now vetoes adoption and an arm fails by that name. A comment written for a change that did not exist yet, and right.
- Scope must be `full` too: the hook enforces verify and scope SEPARATELY, so a narrower stamp can satisfy the hook for a narrow push while saying nothing about the gate this tool owes.
- NOT A PUSH-SAFETY HOLE, and the argument is mechanical rather than asserted: the stamp is already what the pre-push hook trusts and binds to a TREE DIGEST, so adoption grants precisely the authority the hook grants seconds later. A stamp older than the tree fails verify with stale:tree-changed-since-gate — the same verdict the hook acts on.
- VERIFIED LIVE TWICE. With my edits in the tree the stamp read stale, adoption did NOT fire and the gate ran. Then the land itself printed exactly one ok:land-adopts-valid-stamp line: a later attempt adopted the stamp an earlier attempt had earned, which is the saving the row is about, on its own landing.
- Fixture 7/7, four of seven arms negative controls, driving the decision block extracted from the shipped tool under the tool's OWN `set -uo pipefail` — 1175-wuwr was that exact mistake one file over. The extraction fails by name when the markers move.
- Four mutations, each diff proven non-empty first, all red by name.
- Left as the row left it: whether the same orphaning happens on osx or on a Linux host with no WSL boundary. The mechanism is not Windows-specific; the memory-hold half plainly is.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
