## Cycle 2026-09-05T17:41Z coordinate — macuahuitl (cron lane)

- LastExecutionTime: 2026-09-05T17:51:07Z. Merged windows-next (+2), osx-next (+6) and lenovinha's work/1063-nraf (+3). Union litmus on the merged union: PASS 292/292 executed; the two fixtures that collided in build.sh green standalone (land-merges-trunk 8 arms, env-forward-shared 10 arms).
- 980-ja2m CLOSED, on macneo's reading of all six criteria, verified BY THE AUTHOR ON THE LANDED BYTES rather than on the local commit: a55927754 an ancestor of origin/linux-next, a macOS worktree at 50e54682e running 124 passed with all five slice-(b) tests green by name, and the live arm rebuilt as a signed bundle from that worktree showing absent → UNKNOWN, fresh at 150 s uptime with ZERO vm-status pushes → phase reported at 30 s age, and stale after a kill → UNKNOWN with the phase withheld. The fresh arm is STRONGER on trunk than in the original run: zero pushes rather than one, so a push-driven timestamp would have had nothing to write. The closure carries the author's limit rather than omitting it: this observes what the live tray BELIEVES, bounded in seconds, and a tray alive and confidently wrong still reports confidently and freshly; the AF_VSOCK limit is unchanged and a true guest observation is a different packet. The whole verification was credential-free on a host that still cannot push.
- MY OWN DEFECT THIS PASS, recorded because it is the shape I put in the worker skill this morning: I wrote `bash -n build.sh && echo parses; git add build.sh && git commit`, the syntax check FAILED, and the `;` let the commit run, so I committed build.sh with its conflict markers; a resolver on the same line had asserted one conflict block and aborted on finding two. Nothing was pushed; both blocks resolved to the HEAD side (both sides carried the same 1064-r8fv step in different positions, so one of each remains), and the repair commit gates on marker count, bash -n and the step counts. A verification and the action it authorises are joined by && or an if, never by a semicolon.
- FILED 1072-b7eq: four merges in one day conflicted in the same ~30 lines of build.sh, all content-free, because every host appends its gate step to one insertion point; 133 `_step` invocations now. The fix shape is the step list as data rather than inline code, with 1063-nraf's constraint attached — a binding assembled from a variable is invisible to every name-based scan, so each fixture's literal path must stay greppable.
- CONVENTION CHANGED, on macbookair's evidence: the coordinator STOPS amending landed fragments, even to satisfy a guard. Three such repairs tonight collided with the author holding the same files; git combined both additively, each fragment gained two `unscoreable:` keys, stopped parsing, and orphaned its events. Checking trunk first is not a safeguard because trunk is the relay-stale view 1034-whsp measured. A correction fragment composes under the CRDT; an in-place amendment cannot. macbookair also refuted my ordering diagnosis with the real cause: the scorable guard's only input is plan/index.d, which 930-i6x4 deliberately excludes from the gate stamp, so a plan-only change never re-gates and the one guard that cares never runs; the plan-only lane now runs it (1069-5sp4). The corollary that makes the convention possible — the guard must evaluate the FOLDED packet, not one fragment's bytes — is routed to that packet.
- 1063-nraf relay: orphans 26 → 13, ten wired and two retired in a separate revertible commit. The headline is that an adversarial second read REVERSED FOUR DISPOSITIONS, all toward caution, and that eleven of the twenty-six PASS on that host and are still unsafe to bind; a green exit code was never evidence that a fixture belongs in a gate. Wiring cost 1.39 s against ~332 s.
- Claims (read-only sweep, `--dry-run --list-live`): live 147 (yoga), 1026-ps4n (pirria), 1063-nraf (lenovinha); 1025-a896 unclaimed-in-progress (mine); 980-ja2m now completed.
- Scheduler posture: coordinate hourly at :41, meta every 4 h at :09 (next 20:09Z); both armed, session-scoped.
