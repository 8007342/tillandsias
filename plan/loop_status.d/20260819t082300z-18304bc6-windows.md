## Cycle 2026-08-19T01:23 PDT→02:00 PDT (windows — YOLANDA, MO FULL: overnight loop cycle 8, 832-q4mn filed with measured evidence; 643-64bx triaged)

**Result: last cycle's unverified hypothesis is now a demonstrated defect — the order-uniqueness gate cannot see the fragments where every new packet actually lands.**

- **Host**: YOLANDA, `windows-next` 7b2d308c5 → this cycle. Preflight `ok`, guards credential `ok:gh-keyring` / branch `ok:branch-windows-next` / expert-base `ok:expert-base-ready`, opsx `ok:clean-tree`. Boundary `/tmp/meta-orchestration-boundary.tyWcLD`. Sibling heads: main=0bba6525f, linux-next=6de002620, osx-next=10f285143.

**RESOLVED MY OWN LOOSE END.** Cycle 7 recorded, explicitly as unverified, that `plan-orders` might read the base only. Read the source: `crates/tillandsias-policy/src/main.rs:4946` hardcodes `plan/index.yaml`, and nothing in the crate references `plan/index.d/`. Then **demonstrated** it rather than resting on the read: a throwaway tree with the real base plus a fragment declaring `order: 828-k3mq` — a token the base already carries — still answers `ok: plan orders unique`. Filed as **832-q4mn**.

**WHY IT MATTERS ON THIS FLEET.** Every host files new packets as fragments, by design (582-nqw5). So new orders — exactly where collisions happen — are precisely what the gate cannot see. It runs on every push and passes; the collision waits for a compaction that may be days later and on a different host. Near miss the same night: windows filed 828-k3mq/829-p7wq while linux independently filed 828-h7kw/829-dkuc within the hour, both gates green throughout. Distinct suffixes, so nothing actually collided — but each host picked its prefix against a ledger the other's fragments had not reached.

**643-64bx TRIAGED, NOT TAKEN.** Criterion 2 was already done in August. What remains is criterion 1 — "decide whether the local build counter should touch TRACKED files at all" — which contradicts `methodology/versioning.yaml`'s declared "Automatic on every local build", making it a methodology amendment before it is a code change. CLAUDE.md makes methodology the source of truth; a worker cycle cannot take that decision. Its own notes also fence the shortcut ("Do NOT fix this by relaxing the VERSION guard. The guard is load-bearing").

**AND ONE THING I DECLINED TO MEASURE.** Criterion 3's closure wants `./build.sh --install && git status --porcelain` measured on a clean checkout. `--install` replaces the operator's installed tray. Running it unattended overnight would mutate the machine the operator uses, to gather evidence for a decision this cycle cannot take anyway. Left for an attended session, and recorded as a note on the packet rather than silently skipped.

**NOT DONE, carried:**
- 643-64bx should probably become `pickup_role: operator` or gain an explicit methodology dependency so the selector stops offering it as drain work. Not changed here — re-scoping another host's packet on inference is the same overreach in a different direction.
- Queue `829-p7wq` entries 1-6 drained; entry 7 overflow remaining: 628-c7qd, 628-r2vk (tray i18n).
- Operator-gated, unchanged: the attended lane click (823-u5zf / 805-ek9e closure) and the `argv_survives_wt_reparse` delete-versus-make-loud call.
