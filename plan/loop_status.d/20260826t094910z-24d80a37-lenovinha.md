## Cycle 2026-08-26T09:49:10Z — lenovinha (linux_immutable, linux-next, operator /loop)

### 799-tb7q second slice — one dispatch definition, three hottest callers

Measured before building: **61 scripts call jq, 15 already dispatch, 46 remain**, and the toolbox
genuinely carries jq now (jq-1.8.1), confirming this packet's earlier slice landed.

**Why a lib rather than 46 more inline copies:** the only two dispatchers that existed before
today duplicate the six-line block verbatim — and I wrote both, in one cycle. That is exactly the
drift hazard I flagged in my own fixture note the same day. 48 copies would be worse.

`scripts/lib/tool-dispatch.sh` exposes `resolve_tool` / `require_tool`, echoing a command **prefix**
rather than running the tool, because callers pipe, redirect and pass heredocs. Converted
`cycle-metrics.sh`, `select-work-batch.sh`, `local-ci.sh` — each keeping a literal-jq fallback so a
lost lib degrades to the previous behaviour rather than a crash. Output verified unchanged.

**Heads-up sent before landing**, per the new `sibling_heads_up_protocol`. ACKed, no conflict.

### The deliberate non-conversion, and why it now lives in the files

`tray-diagnose.sh` and `diagnose-macos-provision.sh` SHIP and keep inline copies. A shipped script
that sources a sibling lib acquires a dependency that may be absent exactly where it runs, and **a
diagnostic that fails to source its helper fails at the moment its output is needed, with a failure
that looks like the thing it was sent to investigate.**

macuahuitl's addition, which I took: put the reason **in the files**, not only the packet — a future
sweep sees 46 converted and 2 inline and reads oversight. A rule living only in a packet is a rule
nobody reads at the call site.

### Seventh instance of one class, and it was mine again

**Adding that explanatory comment immediately broke my own guard.** The arm asserting "shipped
diagnostics do not source the lib" grepped for the *string* `lib/tool-dispatch.sh` anywhere in the
file — so the sentence explaining why the file deliberately does not source it tripped the guard
about that decision.

Identical to the arm-14 bug tonight (a word-absence test firing on *"Do NOT go looking at
secret-service"*) and to `check-bash-dialect` refusing a regex that *searches for* bash-4
constructs. **The rule, three times paid for: assert the BEHAVIOUR, not the string.** The arm now
matches an actual source directive with comments excluded, and I verified it still has teeth by
inserting a real directive, watching it fail, and reverting.

The shape has a signature worth naming: it appears wherever a guard's subject and its own
explanatory prose share vocabulary — which is precisely where careful authors add comments.

### A cost the next converter needs

Measured: a single `toolbox run … jq` is **~264 ms** against ~1 ms for host jq. Fine for 7–9 sites
(~2s on a jq-less host). **Not fine for a caller invoking jq inside a LOOP** — one call per ledger
row turns milliseconds into minutes. Check for loop-invocation before converting.

The fallback arm cost me two wrong answers first, both harness faults that looked like code faults:
`env -i` strips what podman needs (I nearly recorded "the fallback does not engage"), and dropping
`/usr/bin` removes every other utility. The fixture now **asserts** the jq mask before relying on it.

### Not taken, with reasons

**829-dkuc** outranks at p1 but its own `next_action` says the remaining half "wants either
sweep-budget headroom or an operator-paired session" — not unattended work. **Priority is not
permission.** Packet left `ready`, not half-claimed.

43 jq callers, the `rg` callers and the openssl callers remain on 799-tb7q; packet back to `ready`.
