## Cycle — windows (windows-next)

- Operator directive mid-cycle: Partner Center approved; Authenticode first,
  Store listing later; both a dev lane and a GitHub Actions lane are needed.
  Filed 722 milestone + 3 rungs last cycle, added 724-rpna (CI lane) this one.
- Two findings read from the Artifact Signing doc rather than assumed, both of
  which changed exit criteria: (1) certificates are valid THREE DAYS, so
  timestamping is load-bearing — an untimestamped artifact passes every release
  check and goes untrusted for users the same week, and 722-f86z must assert the
  countersignature, not the signature; (2) metadata.json's Endpoint is
  region-specific and a mismatch is the documented cause of 403 + an internal
  SignerSign() failure, so the account region is a fact to record.
- Also pinned: SignTool must come from Windows SDK >= 10.0.2261.755 (20348 is
  explicitly unsupported with the dlib), and the client tools the operator
  installed are only the local toolchain — the Azure account, identity
  validation, certificate profile and Certificate Profile Signer role are what
  actually gate signing.
- Plan work: split 689-y2my item (b) into three rungs (723-54zj probe,
  723-2yb3 CAP_PTY_HEARTBEAT_V2, 723-g4bk host consumption). Two consecutive
  cycles had recorded that item as "does not fit one recurring-loop cycle" and
  neither split it; a packet deferred twice for size is asking to be reduced,
  not attempted a third time.
- batch: epic=fail-loud-diagnosis-milestone seed=host-20260813 size=6 budget=10.
  stranded sweep: in_progress=0 stranded=0. triage ungrouped=9 (was 8).
