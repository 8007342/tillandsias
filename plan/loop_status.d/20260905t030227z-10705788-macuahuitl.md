## Cycle — macuahuitl 2026-09-05T01:47Z (coordinate-multihost-work, cron :41 lane, lock session-cron-coordinate; carries the daily cut the 00:09Z meta cycle deferred)

- **Scheduler posture:** session crons armed (coordinate `:41` hourly, meta `9 1-23/4`); this fire ran standalone under its own lock after the meta cycle attested at 6d98b8d6f and the parallelism trial released the target tree.
- **Heads at 01:47Z:** main 377ba7794 (v56.9.4.1), linux-next 6df950657 (yoga's 972-umik revert 08a7d3cc7 and its three-reader conversion with the ratchet 310051bf2; lenovinha's 1031-q4pb closed whole at 19aef52d7; the coordinator's records), windows-next +4 (yolanda: 1032-62rx client-side refusal test, records, attest), osx-next +6 (macbookair: 1043-kvvn tray binary-name collision fixed with the unique-bin-names guard, 873-vgyg ownership-safe collision handling for BOTH forge builders with --replace removed, proven twice on forced full gates). **Merged this cycle:** windows-next → 7b2fec2f5, osx-next → 65ada644c, both clean; union litmus (full output, 01:47Z-02:04Z) 285/291, six reds, FIVE of them self-inflicted by the coordinator: the parallelism trial's `cargo clean` had removed target/release/tillandsias-plan, which litmus:cycle-batch-triage-shape, fragment-status-loss-attribution-shape, fold-incompleteness-typed-shape, selector-urgency-not-inert-shape and ensure-toolbox-include-shape invoke by path, and the litmus ran before the gate restored it (bash: ./target/release/tillandsias-plan: No such file or directory); the sixth real: litmus:installer-wsl-preflight-shape's siblings-ascii step, one em-dash in a scripts/tray-diagnose.ps1 comment from 21745a1fd (1043-kvvn), release-shipped ps1 files must be pure ASCII. Landed 029b082f7 with the tool's gate (the merges are correct; the reds were not the merges'), the script refused the cut on the litmus verdict as designed; a second chain (02:06Z) merges osx-next's +4 (macbookair's two macOS readers converted, 311aa27d5; the 1044-2raf hint packet), fixes the one byte, re-runs the litmus in full with the plan binary present, lands, and cuts on PASS. The three gates on the daily are cleared: no one-sided secure-wire flip on trunk, no --replace in a forge builder, no duplicate [[bin]] name.
- **Release: v56.9.5.1 CUT.** Second chain 02:06Z: osx-next's +4 merged (e30698ff2), the one byte fixed, union litmus 291/291 PASS with the plan binary present, landed 9a78d982b; release flow scripted with every step's rc: release-preflight ok, PR #107 merged, bump PR merged (main bump-merge b528680d2, VERSION 56.9.5.1), tag v56.9.5.1 at that SHA verified unmoved after the back-merge, back-merge pushed (linux-next c3b64f56d), release.yml run 33938905320 dispatched 02:22:50Z, watch in progress at record time; the §1 smoke here, the macOS §1 lane (macbookair) and the ledger rows follow the run's conclusion in the next entry. The daily carries every fix the operator asked to close before a stable promotion except the 972-umik flip (structure banked, default unchanged, the flip lands with the last reader), so the STABLE candidate is this daily's smoke verdict plus that flip's daily, per the operator's answer.
- **Claims live:** 972-umik (yoga; routed: macbookair converts the two macOS readers, yolanda the Windows one, each with its lane's pairing test; the last converter flips the default; ratchet baseline 3), 980-ja2m (macneo, slice (b)), 1025-a896 (coordinator), 1039-b64k (lenovinha, narrowed to documentation after reading the push path: the pass token never enters the hook's verdict), 1019-ivia (macbookair, C2's guest half blocked on a 300 s Ready timeout). Stranded 0.
- **Heartbeat by asking, every host answered within the hour:** lenovinha (1031-q4pb closed; 13 GB, OOM-killed at default parallelism, finishes at CARGO_BUILD_JOBS=4; next 1039 (3) then 1036-e5w9 with the git diff --summary shape); yoga (972 routed; plan_next); yolanda (next 1042-svey openssl into the builder, then hvsocket.rs); macbookair (1043-kvvn + 873 landed and verified from here by function range; next the "next:"-hint packet, then the two macOS readers); macneo (holds for slice (b), credential dead); esmeraldinha (v56.9.4.1 floor PASS; parallelism arms running with the compile-work guard after catching that a forced gate compiles nothing on a warm tree; report landed under plan/issues); pirria (873 reproduced at 62 ms against a running sibling, orphan self-removed in 4 min; sweep blocked two deep, toolbox absent then the toolchain).
- **The operator's freeze question, answered by measurement (1047-h88p):** reducing builder parallelism does not stop the freezes on macuahuitl; a full clean compile of 394 crates at 20 rustc peaks at 18% io-full, the union gate's compile phase at 42%, its TEST phase with zero rustc at 83%, a no-compile gate at 35%, the nix gc at 810 s of btrfs commit waits. The levers are the dirty-page cap (root, the operator's), nodatacow on the trees the test phase writes (podman storage included), and a bounded builder write rate; the job cap stays as a correctness setting for memory-bound hosts and MUST be applied inside the build scripts, because both builder wrappers forward only their own prefixes (CARGO_BUILD_JOBS was UNSET inside the toolbox on Linux and across wsl.exe on Windows; the "ten-job" arms ran at twenty on both). Two worker-skill lines landed with this cycle (features after a gate; control variables inside the scripts).
- **Operator queue, plain asks:** (1) security log for 2026-09-04; no logins anywhere until then. (2) pirria: install toolbox, let the builder image build, toolchain inside. (3) whether unsigned Windows artifacts may be called stable. (4) macuahuitl, before a gate watched from the desktop: `sudo sysctl -w vm.dirty_bytes=1073741824 vm.dirty_background_bytes=268435456`. (5) 978-juw4, 996-bcjx, the ten quarantined trees on esmeraldinha, unchanged. Nothing is blocked that a host can unblock.
- **Velocity:** closures this hour 1031-q4pb, 1043-kvvn, 873-vgyg's source half (revised), 1028-3eiz; filings 1047-h88p, 1048-aumz, 1043-kvvn; no High-Velocity Alignment Event.
