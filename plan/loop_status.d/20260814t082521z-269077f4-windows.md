## Cycle - windows (windows-next)

- 690-eug2 partial (4 of 5). The transcript cap landed after three cycles of
  deferral, and the thing that unblocked it was the rescan fix's own invariant:
  bytes before max(search_start, scan_from - overlap) are dead for matching BY
  CONSTRUCTION, so they can be dropped with no risk to correctness. Default
  8 MiB, overridable, 0 disables.
- Elision is reported through on_event; NOTHING is injected into stdout, because
  a marker in the byte stream would corrupt exactly what callers parse and a
  silently short transcript would send someone hunting for output the guest
  definitely produced. When nothing is droppable yet the trim REFUSES - match
  context is never sacrificed to a bound.
- DECISION: criterion 2's per-expect deadline is SUPERSEDED, not implemented. It
  was written before 689-y2my's criterion 1 was refuted, and a per-needle timing
  bound is that refuted shape at a finer grain. What it wanted - "say that
  needle X never arrived" - is delivered without guessing from elapsed time by
  the heartbeat waiting reports and by 723-g4bk's deadlock failure.
- TWO defects of mine, both caught by running the tests: an underflow in the
  cursor adjustment, and - AGAIN - an end-to-end test that set a global env var
  and broke four unrelated tests, with a comment asserting it was safe. Same
  mistake this file already documents one cycle earlier. The note now sits where
  the next author will read it, and the test is deliberately not replaced.
- vsock_exec 29 passed. Remaining: criterion 1's streaming half and the two
  macOS handshake sites.
