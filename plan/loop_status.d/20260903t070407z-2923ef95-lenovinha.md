## Cycle — lenovinha (linux_immutable, linux-next), 981-n5vx

NOT IMPLEMENTED, DELIBERATELY. The spec MUST this packet asks me to implement
would, implemented as written, OOM-kill every forge in the fleet. Released to
`ready` with the decision named rather than shipping a compliant change that
breaks the product.

DEFECT CONFIRMED, n=2 on different hardware. lenovinha's live forge:
memory.max = max, memory.high = max, podman Memory=0 MemorySwap=0, pids.max
4096. pirria's finding reproduces exactly. compute_memory_ceiling_mb verified
to have zero production callers — its own doc says "the caller is responsible
for emitting both args", and there is no caller.

THE MEASUREMENT THAT STOPPED ME. Spec ceiling = sum(tmpfs) + 256 = 586 MiB for
the current profile. The forge running on this host right now:
  memory.current = 1809 MiB   3.1x the spec ceiling
  memory.peak    = 4801 MiB   8.2x the spec ceiling
Implementing the MUST would have put both flags in the argv, equal, from the
helper — satisfying criteria 1 and 2, turning every gate green — and killed
the container. A compliance change that breaks the product, which is this
packet's own defect shape one level up: an artifact that satisfies its check
and does not do its job.

THE FORMULA IS STALE, NOT WRONG. Its scenario assumes a `budget` tmpfs sized
from a memory budget; no such mount exists, and the workspace is now a bind
mount rather than tmpfs, so sum(tmpfs) no longer tracks the working set. The
256 MiB baseline was never sized for a container running cargo with
pids-limit 4096.

WHY I DID NOT PICK A REPLACEMENT. It is fleet policy with fleet blast radius —
too low OOMs real work, too high preserves the swap-thrash the packet is about
while looking done — and I have two data points against a fleet spanning an
8 GiB macOS host upward. 978-juw4 already owns a host-memory-reserve policy;
inventing a second here is the dual-source-of-truth shape main.rs already cites
959-fpc5 for.

A TRAP RECORDED FOR THE IMPLEMENTER, which cost most of the investigation.
THREE forge-argv builders exist and only one is live. launch.rs:26 says in its
own comment that it has no production callers; tray/mod.rs:2217 is legacy. The
live path is build_stack_common_args (main.rs:3246) + build_opencode_forge_args
(main.rs:6618) via build_forge_common_args (main.rs:6031). I found it by asking
which site emits the --pids-limit that is observably 4096 in the live cgroup,
not by reading names. Fixing either dead builder yields a green gate, a passing
argv assertion, and an unbounded forge.

EXIT CRITERION 5 DELIVERED, since it is pure investigation: macOS and Windows
have the SAME gap. No cfg gating on any of the three builders, and neither tray
crate builds forge argv of its own. One fix covers all three lanes.

metrics:
  claim: taken, then released to ready with next_action — not left in_progress
  evidence: n=2 hosts unbounded; live peak 4801 MiB vs 586 MiB spec ceiling
  delivered: criterion 5, the live-path identification, the zero-caller trap
  blocked on: the ceiling policy, needing an operator ruling or a fleet pass
