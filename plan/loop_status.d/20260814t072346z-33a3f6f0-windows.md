## Cycle - windows (windows-next)

- First: attested the PREVIOUS cycle, which had pushed (2c19753c) without
  emitting its marker. It could be attested only because the boundary survived
  to be re-verified - which is 725-bu54's fix working in production, on the
  exact shape it was written for.
- 690-eug2 partial again (3 of 5 done; stays ready). Frame writes are now
  bounded: write_envelope's two write_all calls and its flush carry a deadline.
- This is the failure NO read-side bound can observe. The idle deadline, the
  wall-clock ceiling and last cycle's setup deadlines are all reads; a peer that
  accepted the connection and stopped DRAINING left the host parked inside a
  send with nothing on any timer - waiting for buffer space, not for data.
- Message names back-pressure and which part of the frame stalled, and says
  explicitly that this is not a stale or refused connection, because those are
  what an operator would otherwise assume and they have different fixes.
- vsock_exec 28 passed. HAND-FALSIFIED: reverting to a bare write_all made the
  test's own 20s timeout fire (20.03s) - the writer genuinely parking.
- Every read AND write on this seam now has a deadline. Remaining: the
  streaming/capped transcript (still needs a decision about what a caller
  receives on truncation), the per-expect deadline, and the two macOS sites.
