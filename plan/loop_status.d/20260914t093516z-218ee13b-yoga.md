## Cycle 2026-09-14T09:35Z — yoga (linux-next)

Batch selector: epic=harness-mcp-expert-validation, seed=silverblue-unknown-amd+npu-yoga-20260914, size=2, budget=6. Took item 1, 1104-w9np (claim beeef3d38), landed at 73e6b2902. Item 2 (packet 405, unscored) not started.

- THE DEFECT TOLD A HOST TO KEEP ITS HANDS OFF ITS OWN FINISHED WORK. lenovinha's own in_progress claim, merged into osx-next and windows-next by routine integration, came back as "it is NOT yours to implement"; 1071-adhj sat in_progress for a day with every criterion met. The tool is authoritative on that question, so the verdict was repeated to the coordinator as fact — correct behaviour toward a tool that is confidently wrong.
- THE NEW VERDICT IS AN OK, not a refusal with nicer words: own-claim-reflected:<packet>:<branch>, exit 0, and it says the work is the reader's to resume or release.
- AMBIGUITY FAILS TOWARD THE OLD VERDICT: an empty or unrecognised host is NOT "mine" and still refuses, because treating a sibling's claim as your own is 814-iyu7. An arm pins it, and that arm is the load-bearing one.
- BOTH HOST VOCABULARIES COUNT AS ME (1012-hu7d is stacked on this row): --host yoga and the TILLANDSIAS_HOST_KIND fallback `linux` are one host wearing two labels, and no query keyed on one returns the other.
- STATUS THROUGH THE FOLD, ATTRIBUTION SEPARATELY: the file already says why status must not be a fragment grep (635-i6vm); attribution takes the status entry with the greatest ts, and where the two could disagree the comparison falls to the old verdict.
- THE SEAM IS NOT THE ONLY COVERAGE: the arms force attribution because a fixture cannot plant a fragment on origin/osx-next, so a separate arm runs the parser itself against a planted fragment pair.
- FALSIFIED AS THE ROW ASKS: with the pre-fix checker in place the new arm reds by name, and the parser arm reports it ASSERTED NOTHING rather than passing. Both negative controls pass on pre-fix code too — they are controls for the fix, not the defect.
- A FIXTURE DEFECT CAUGHT: the parser arm first sourced the check, which runs it with no arguments and exits at its usage path before defining anything — it reported a parser failure about a function that never existed. Extracts by markers now, and fails by name when they move.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
