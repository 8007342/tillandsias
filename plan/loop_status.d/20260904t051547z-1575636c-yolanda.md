## Cycle 2026-09-03T19:00Z-2026-09-04T05:15Z — yolanda (windows), meta-orchestration full

Took ownership of what looked like esmeraldinha's host bug and found an
interrupted architectural migration underneath it. Landed the destructive half,
scoped the rest, and got the operator's ruling before touching it.

- **997-e4v2 filed and half-landed** (`e8a995a41`, merged `25754b682`).
  esme-windows' daily `.invalid-<epoch>` quarantines were not a host quirk: the
  Windows tray injects a persistent drvfs mount of `%USERPROFILE%\src` at
  `/home/forge/src`, `projects_root()` prefers it, the containerized `gh` clone
  writes it as uid 1000 while the headless evaluates as root, and git's exit 128
  was read as "invalid checkout" — so ten healthy trees were renamed aside,
  `fsck` clean on every one, each rename also destroying that checkout's
  repo-local `.gh-credentials`, which nothing reseeds. `classify_git_checkout`
  is now three-state; only a truthful Invalid may quarantine.
- **The migration is the real finding.** `forge-hot-cold-split` mandates
  `/home/forge/src` be a per-launch tmpfs; three of its four hot paths ship,
  that one does not, and `compute_hot_budget()` has ZERO callers — in a change
  ARCHIVED AS COMPLETE on 2026-04-27. Cloud and Local are one host directory
  behind two menus, which the mount unit's own comment states outright.
- **A live hole in yesterday's 965-sxec fix**, found by following esme's
  correction: `_archiver_cleanup` could rewrite `exit 3` into `exit 1` under
  `set -e`, converting could-not-run back into "the archiver would CHANGE THE
  READY SET" — the exact verdict that fix removed, reintroduced hours later
  through the tidying. Proven with a genuine EACCES, fixed, fix proven.

**FOUR FALSE RESULTS IN ONE EVENING BETWEEN TWO HOSTS, and the pattern is one
thing:** an injected failure that silently did not fail. esme's `chmod` was a
no-op on NTFS; my trap was never armed because the exit path preceded `trap`;
my first hardened arm used `sed 's|…|\|\||'` so both arms ran the same script.
The rule that came out of it is narrower and more useful than "measure it":
**prove the failure you injected actually occurred before believing a
negative.** A test that cannot fail is indistinguishable from one that passed.

**I was wrong twice and both are on the packet.** My first classifier asserted
in its own doc comment that a 128 is "essentially never" real damage — false,
esme measured it. And I reported "the exit-3 contract survives" off the unarmed
test. Every strong result today has the shape of someone checking someone else.

**OPERATOR RULING, and it deletes scope:** an existing `~/src` on any disk is
ignored and left to rot — forward-only, no migration, no detection, no warning,
because there are no users in the wild. And the local `~/src` path is removed
ENTIRELY. The hardest open question on the packet was answered by removing it.

**Next cycle starts here:** wire the tmpfs FIRST, delete the mount SECOND —
removing `home-forge-src.mount` before `compute_hot_budget()` is wired leaves a
forge with no project source at all. Then the twelve-file menu/type removal.
Cannot be finished on yolanda: the macOS tray does not type-check here.
