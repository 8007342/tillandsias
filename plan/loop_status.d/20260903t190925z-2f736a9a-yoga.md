## Cycle — yoga 2026-09-03T19:09Z (meta-orchestration, full mode; cron lane)
- Took 994-8r3w (p1, my own filing from last cycle) DELIBERATELY OUTSIDE the selector's batch head: it is ungrouped, so epic ranking never surfaces it, and it is a one-hour close of a fail-loud gap. Deviation recorded in the claim reason rather than left implicit.
- 994-8r3w IMPLEMENTED. images/default/enclave-services.txt declares the enclave services a provisioned host should be RUNNING; cycle-preflight reads it into TILLANDSIAS_ENCLAVE_EXPECTED_SERVICES, which the absent-detection loop has always consumed and was never given. A health check cannot derive what SHOULD exist from what DOES — that is circular — so the expectation had to be declared.
- THE LIST IS A JUDGEMENT, NOT AN INVENTORY, and the file says so: only persistently-running services (vault, proxy). GitLogin is transient, NixCache dev-tier and optional by design, builder is a toolbox, dev-inference is the dev lane. Listing any of them manufactures false absents, and a check that cries wolf gets switched off — which is how the original gap survived. A Rust test in container_deps.rs asserts the file matches the persistent set in BOTH directions, so neither an omission nor a well-meaning addition can drift. Guarded for unprovisioned hosts: the expectation is exported only when tillandsias-vault exists, vault being the anchor every other persistent service depends on.
- IT CAUGHT A REAL ABSENT SERVICE ON ITS FIRST RUN, and that is my correction to make. Preflight went ok -> `services-down0-absent1`, naming tillandsias-proxy. LAST CYCLE I REPORTED THE PROXY FIXED AND IT WAS NOT: it had been removed and never recreated, and `services-ok` was the blind spot — the very one I had filed as 994-8r3w minutes earlier. I read my own defect's output as evidence of health, in the same cycle I described the defect. The check now says absent1, which is the true state and has been for two cycles.
- I DID NOT FAKE A REPAIR. Both orchestration flows that bring the proxy up refuse on this build: --github-login needs a terminal, --list-cloud-projects reports "this build lacks remote-projects support (requires the vault and tray features)". Hand-crafting a podman run would bypass ensure_proxy_ca_key_secret and recreate exactly the CA desync 975-rsgm is about, so I did not. Restoring it is an operator/host action and is now VISIBLE rather than silent, which is what this packet set out to achieve.
- A THIRD INSTANCE OF THE BUILD-VS-EXISTS LESSON, relayed from macbook-macos two cycles ago: the flows that would recover the proxy are feature-gated out of the binary I have, so "the code exists" and "this artifact can run it" are different claims. It arrived as advice about signature changes and turned up as an inability to repair my own host.
- SCOPE NOT CLAIMED: criterion 3 (a test pins the PRODUCTION path, not just --expect) is only partly met. The drift test pins the manifest against the enum and preflight is verified by observation, but nothing yet fails if a future edit stops preflight exporting the variable — named in next_action.
