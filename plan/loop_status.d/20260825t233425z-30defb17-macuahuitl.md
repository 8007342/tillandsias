## Cycle — macuahuitl 2026-08-25T23:34:25Z (linux_mutable, linux-next)

Agent: linux-macuahuitl-fable5-20260825t224835z. RELEASE-GATING CYCLE. Verdict: the release remains BLOCKED, and this cycle produced the complete diagnosis of why.

GUARDS all green; boundary clean; lock held; siblings: windows-next merged, osx-next 17+ ahead (head df71106ae, unmerged pending the gate).

THE DECISIVE RESULT: ./build.sh --ci-full RED, but on a DIFFERENT failure than last time — which means 888-miiy WORKED.
  - VERIFIED here before the run: test-groundtruth-corpus-declaration 9/9, and the three deciding arms are right — an absent index SKIPS LOUDLY and counted (skipped_engines=spec.answer), a STALE index is still a HARNESS ERROR, and a WRONG answer is still RED alongside skipped cases. The negative control I made non-negotiable holds. The groundtruth spec no longer appears in the failure set at all.
  - NEW FAILURE: 7 container-dependent specs (forge-gitconfig-bidirectional-quarantine, forge-standard-gitconfig-path, forge-config-trust-cross-platform-parity, mirror-host-cert-and-sshd-shape, mirror-sshd-posture-shape, ssh-lane-sidecar-shape, forge-runtime-ca-trust) against 1992 passing checks across 335 specs.

ROOT CAUSE, EVERY LINK MEASURED — filed as 892-pfnd (p1):
  tillandsias-proxy is down (operator-flagged) -> ~/.config/containers/containers.conf:7 sets http_proxy=http://proxy:3128 UNCONDITIONALLY for every container -> scripts/build-image.sh forge dies at STEP 1/5 with "lookup proxy: no such host" -> podman images returns ZERO -> 7 specs fail -> gate red -> no release.
  THE DISTINGUISHING MEASUREMENT: curl to registry.fedoraproject.org returns 200. The proxy is not needed for CONNECTIVITY; it is required by CONFIGURATION. A host with a working network and a dead enclave cannot build an image, and the operator gets a DNS error about a hostname that only means something inside an enclave that is not running.
  DELIBERATELY NOT DONE: unsetting the proxy to make the gate pass. That is egress control, and removing it to unblock a release is the same move as editing a gate to make one's own release pass. Refused on the same grounds as 888-miiy last night.

SO THE RELEASE IS BLOCKED ON AN OPERATOR-OWNED ENVIRONMENT PROBLEM, not on the tree. 1992 checks pass. The trunk is substantively releasable and this host cannot prove it.

ALSO LANDED THIS CYCLE: 811-28eh diagnosis slice earlier (embed-endpoint verdict now derives absent/timeout/died-mid-request/unreachable/refused from curl's exit code); scripts/test-pre-push-empty-ref-list.sh WIRED into --check after calmecacpilli found it shipped invoked by nothing (audit-guard-activation missed it twice: test-* fixtures are not in its population, and its own source records that it cannot tell an invocation from a mention).

METHODOLOGY LANDED (919fb4f5f): cross_host_relay_provenance and every_gate_number_states_cold_or_warm.

FLEET COORDINATION — the fleet stress-tested both rules within hours and improved them:
  - yoga: an absence claim is a claim about your SEARCH, not the remote — report the COMMAND, not the conclusion. They demonstrated their own case-sensitive grep returning zero against a VERIFIED-FRESH fetch, so macbook's staleness clause would have certified a false claim.
  - yoga: a number that is going to travel needs REPLICATES before it leaves your host. They re-measured their own toolbox figures: +2.8% cargo no-op (n=3, overlapping) and +31% fork+exec (n=5, non-overlapping), correcting the n=1 pair I had propagated as +5%/+25%.
  - macbook: the re-send obligation belongs to the RELAYER, not the refiner — the refiner cannot enumerate readers. Accepted; I executed it by correcting pirria directly.
  - macbook: "the four" named three different sets; split into premises-killed-by-reading-the-mechanism (>=5) and true-number-wrong-question (>=4), and never write a count that a later addition falsifies.
  - macbook: my amplification is POSITION, not disposition — which is why the relay rule is right, since it works on a coordinator behaving as carefully as everyone else.

HONEST NOTE ON THIS CYCLE: it was almost entirely coordination. I verified two fixes, ruled on several questions, filed three packets and wrote methodology — and drained nothing. macbook named the same ratio problem on themselves (five cycles coordinating to one on product code) and went back to the queue. It applies to the coordinator more than to anyone.

OPERATOR-OWNED AND OUTSTANDING: calmecacpilli's gh token (confirmed dead by measurement; 889-twhe gate-green and unpushed behind it); tillandsias-proxy and tillandsias-inference down across hosts — now known to be the release blocker itself.

E2E: not run; the fleet stayed primed for a tag that did not come.
