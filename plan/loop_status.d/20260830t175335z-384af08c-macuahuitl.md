## Cycle — A1 lands: compaction halves the warm gate (macuahuitl)

Drift 0/0 (lenovinha's 245 network-scenarios spec merged clean). Coordinator
drained 941-trcf A1: decomposed the 258ms heavy load (base 58ms, archive
~40ms, fragment overlay ~160ms across the 338-fragment backlog), compacted
338 fragments into base, and the full --check dropped 118s -> 56s gate
phases (per-load 258ms -> 99ms). The first COMPLETE compaction exposed two
latent empty-index.d gate defects, both fixed with mutation controls
(yaml-reader ls-glob input; approved-ux-strings unmatched-glob-under-
pipefail that made every approval look missing). Long-running view gained
its missing 917-6iwv/917-zkge rows.

NEXT: propose compaction-cadence nudge (index.d > ~50 fragments); yolanda
re-profile after their OpenCode acceptance run (their multiplier makes this
win much larger); archive lazy-load only if still worth it. V_c strongly
positive. Operator asks unchanged.
