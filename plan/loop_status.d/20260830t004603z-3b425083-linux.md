## Cycle — first tick post-crash (macuahuitl, fresh session)

RECOVERY COMPLETE: coordinator host crashed + rebooted (desktop freeze,
root cause OURS — tray dbusmenu Event replied "(ib)" vs spec "()" and
wedged gnome-shell's grab; 938-9yh4 filed+fixed, both handler signatures
spec-exact, pid-matched wire verification + live-fire Event call clean;
EventGroup input a(isvu) also fixed). Fixed binary installed and running
as user unit; app-vs-images skew reconciled via --init --force-downgrade;
hourly cron re-armed (session-local, 7-day expiry). NEW 936-kdev
datapoint: --install SIGKILLED (137) at the same headless-smoke boundary
post-reboot; same steps manually complete fine.

OPERATOR RULING recorded on 937-68n4: no twin framing — each host tunes
7B for its own silicon, own regime. Fingerprint refusing the twin claim
was the system working (805-r98w earned its keep pre-first-token).

THE CLASS FINDING OF THE MORNING (four instances, two days): "a label
that SUBSTITUTES for the wiring it names" — lenovinha's circular cdi_ok,
yolanda's engine-claiming entrypoint, yoga's gpu-rocm tier with empty
.HostConfig.Devices (dev-inference-ensure.sh wires --device only for
gpu-cuda; the comment above documents the identical failure fixed for
CUDA and never extended), yolanda's file-existence wsl2 detection.
Class fix = 793-zumy redesign (lanes proven by placement, never label),
now LANDING: lenovinha pushed real DRM-render-node enumeration this hour.

Board: lenovinha 793-zumy (first slice on trunk); yoga CPU budget ladder
→ ROCm wiring fix in dev-inference-ensure.sh (instance repair, endorsed);
yolanda endpoint half + regime-(a) baseline landed (merged this tick);
macbook idle (Apple-credential-gated); macuahuitl coordination + 936-kdev
open. Twin-tier numbers so far: 7B decode 12.2/9.1 tok/s; 60s budget ≈
one 300-token answer at this tier (shippability datapoint). Cheatsheet
gained gate-under-load + confident-nothing family.
