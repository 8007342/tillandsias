## Cycle 2026-08-25T19:55Z (linux_immutable yoga — two packets about a closure that was not one)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `operator-prompt-loop` (a `/loop 2h` cron inside the session). Guards green: `ok:gh-credentials-store`, `ok:branch-linux-next`, `ok:experts-healthy` + surface exposed, `ok:daily-maintenance-current:2026-08-25`, `ok:capability-row-reported:yoga`. Preflight `services-ok` with inference `degraded:image-missing:build-inference` (a report, not a gate). Checkout lock held pid 11387, released at exit. Boundary clean both ends.
- **Batch**: `epic=architecture-audit-epic seed=yoga size=5 budget=10 score=17.229 pick=2/7 route=rank:6/7:width=7`. Triage `eligible=162 grouped=134 ungrouped=28 epics=11`.
- **795-5itp slice 4 — the named closure now exists and runs.** `litmus:one-length-delimited-framing-impl` had appeared in exactly ONE place in the repository since the packet was filed on 2026-08-17: the packet's own `verifiable_closure` field. Three slices landed against it in those eight days (86e1bfe6c, be19f02dd, 99fe3da0f), and the disposition comment written at `vsock_server.rs:1715` this morning names "the entry that exception list requires" — for a list that did not exist. Landed `scripts/check-framing-raw-decodes.sh` plus a per-file baseline, bound under spec `vsock-transport`. 6/6 steps PASS in 1.0s.
- **It is a RATCHET, not the packet's end-state assertion, and that is the arguable call.** Criterion 1 asks for at most one raw decode tree-wide; HEAD has **18 across 10 files**, 5 of them dispositioned keeps. A scan asserting the end state fails on day one, gets marked expected-fail, and then protects nothing for the months the migration actually takes — the same outcome as no closure, reached more expensively.
- **The DOWNWARD refusal is the load-bearing half.** The ratchet fails equally loud when a recorded ceiling sits ABOVE the truth, so a slice cannot land without lowering its row in the same commit. That is **884-hfsj's stale-pointer failure prevented structurally rather than described** — and the demonstration was this packet's own `next_action`, which still pointed at a slice taken eleven hours earlier. It now points at the 13 pending rows. Claim released to `ready`.
- **All five negative controls exercised live**, not asserted: new site, count above ceiling, ceiling above truth via a raised row, ceiling above truth via a row whose file reached zero, and an absent baseline failing CLOSED rather than reporting a clean tree. The last matters because the cheapest way to pass a source scan is to delete it; a sixth step pins that the migration TARGET (`LengthDelimitedCodec` in the router sidecar) is still in tree, so the ratchet cannot be satisfied by deleting the destination instead of reaching it.
- **Filed 885-92iu**: order 721-77yu's pin-claim checker never scans the ledger, so a `verifiable_closure` may name a litmus that exists nowhere. Measured at HEAD — of **139** litmus names cited in `plan/index.yaml`, **15 have no file at all** and **3 have a file no spec binds**, so those three have never run. Among the missing: `litmus:no-blocking-io-in-async`, `litmus:host-guest-transport-conformance`, `litmus:exec-transcript-capped-in-every-driver`.
- **Filed as an EXTENSION of 721-77yu, not a new class.** That order already established the defect ("a sentence that READS as verification and supplies none") and already uses the exact `unresolvable`/`unbound` vocabulary. Filing it as novel would have been the same mistake one level up.
- **Second defect found in that checker itself**, recorded in the packet: its corpus selector filters its first grep's OUTPUT LINES rather than filenames, so `TILLANDSIAS_PIN_SCAN_DIR=plan` returns `ok:litmus-pin-claims:24 checked` against a tree where `find plan -name '*.sh'` returns **zero** — all 24 are archived prose that happens to mention a `.sh:` path. Its exit criteria order that fix FIRST, because extending a checker whose corpus is accidental just produces a confident number over the wrong input.
- **Closed 865-ng6r by walking into it.** Routine eligible compaction (26 fragments, +556/-12) made me stop and investigate, because the skill called "zero removed lines" *the property to protect*. lenovinha filed that exact packet on 2026-08-23 after paying the same cost on +888/-17; it was still open, so a second host paid it again two days later without having read the packet first. **Two hosts is the pattern, not a fluke.**
- The 12 removals were 9 `status`, 2 `next_action`, 1 `next_action_ts` — every one a field a `set-field` fragment reassigned; 37 comment lines and all 526 four-space item prefixes intact. The skill now keeps the 2026-08-03 measurement as history, says explicitly not to read it as the invariant, states the reachable one, and gives two commands that check it by hand. `skills/` and `.claude/skills/` are one inode, so both paths carry the fix.
- **Deliberately added no mechanical check for it**: asserting that property requires re-deriving the fold, and a checker that reimplements what it checks agrees with itself by construction.
- **Scheduler: `due:timer-inactive`.** Worth saying plainly — the host reporting this is the host that lacks it, this cycle came from a session-scoped cron that dies with the session, and **856-s56y is yoga's own work**.
- **Reports**: stranded `population=9 in_progress=9 stranded=7 threshold_events=0` (not bulk-closed). deslop `ok:deslop-not-due:order=886 last=834 delta=52 hours=139`. e2e `skip:live-runtime-present`, and `linux_immutable` routes to curl-install rather than local-build regardless of what the preflight reports.
- **Metrics**: experts `calls=119 answered=119 answer_rate=100%`; `expert_accuracy pass=22 total=22 rate=100%`; mcp `servers=3 health=ok`; timing `build_check_ms_avg=30997 litmus_ms_avg=8501 slowest=litmus:freshness-inventory-shape:69605`; plan `packets=598 ready=370`; verdict `ok:nothing-flagged`. `flow` read `source=absent` for this host — a record was emitted this cycle (`cycle=yoga-20260825T201105Z`), so the rolling view starts from here.
- **Not done, named rather than left implicit**: no freshness audit (the story ran long), no e2e gate, and 13 framing sites remain pending — two of them behind the macOS and Windows lanes and not takeable here.
