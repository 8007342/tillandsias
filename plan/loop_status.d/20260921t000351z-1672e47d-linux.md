## Cycle 2026-09-20T19:10Z→2026-09-20T22:00Z (linux — macuahuitl, coordinator: the branch-discipline direction becomes three rows and a migration; the front door lands with a measured budget; the day's daily cut v56.9.20.1 runs on this host under a declared freeze)

Direction ("We're giving forge agents local EXPERTS"): served by landing the two operator-directed skills that bootstrap agents (join-the-fleet, used live by lenovinha after a reboot; initialize-bare-metal-host), by the front door that lets a host run every push-refusing guard in one command before it opens a PR, and by the 920-pxg6 ruling that leaves the Lua expert layer one OpenSpec sync from done.

DONE, verified on origin by content: LAND 38c 49897e337 (join-the-fleet 1311-ajpm + initialize-bare-metal-host v1 1312-i6da, linked from four runtimes, bound); LAND 39 4897877b7 (macneo's macOS --reset-state arm with the ungated-module guard; macbookair's 830-xsk2 closure event; pirria's 1313-w78k fragment-ts-skew lane check, one relay-side BSD-date fixup); LAND 40 (esme's 1303-2d5g v4: --parse-only loads the document, both fixtures mutate one named anchor line; trunk 266f692fc); LAND 41 (pirria's ts-skew helpers validate what date printed; d0d111736); LAND 42b 6e677d42f (pirria's ./build.sh --preflight front door, litmus:preflight-front-door 12/12, the traps page cheatsheets/tooling/exit-status-is-not-an-answer.md with its derived copy staged relay-side after the hook's guard 1b refused the push); LAND 43 28ce80c75 (macbookair's re-runnable macOS vsock inference closure fixture bound post-build; yoga's 1299-s2sv timing-record root on both emit sites, step 418; lenovinha's 1313-prin link 3 — the mirror's second Vault identity, signer, sshd; pirria's fix for MY ci-release arm that counted a fixture's mention as a second opt-out). Plan pushes of mine: 73375ce28 (920-pxg6 task 4.5 accepted linux+darwin+windows-MSVC with macbookair's narrowing, pickup_role any; 1315-4a7j, 1316-bnzt, 1317-9ugn filed; the 17:55Z→19:10Z handoff). Closed by peers: 1287-h6qn, 1313-w78k, 1286-4437 (all three arms), 1293-wka4, 1305-udgs, 830-xsk2, 1303-2d5g (in flight), 1309-qc95 (fold). Filed by peers: 1313-prin, 1313-w78k, 1314-w9fx, 1314-2mdv, 1315-d4qd, 1318-3dgq, 1319-vd5h. Claimed on trunk: 1315-4a7j (yoga), 1309-fhxb (pirria), 1309-rb3p (macbookair), 1317-9ugn (here).

OPERATOR DIRECTIONS, verbatim where it matters: the push-race churn ("every commit I see has a few retries with 'remote moved'… we're still not leveraging branching power the way we should") → measured first (13 of ~46 lands retried, one thrice; the hook exempts salvage/* only while methodology excludes work/ from the gate and the regex admits no work/; 92 salvage vs 34 work refs on origin) and researched (bors/CQ/merge-train shape; GitHub's merge queue needs a reporting check and there is no server CI by ruling) → three rows: 1315-4a7j the ungated work/<order> lane with AFFORDANCES in every refusal ("prefer work branches… See ./skills/join-the-fleet §3"), 1316-bnzt a serialized landing queue from PRs with LIGHT/SCOPED/FULL tiers proportional to the blast radius ("Plan is cheap because it doesn't run full heavy gates… integration cost proportional to the blast radius… All pull requests to main do full integration"), 1317-9ugn the skill text; MIGRATION STYLE per the operator: phase 1 adds methodology, skills and affordances and refuses nothing new, phase 2 flips to hard enforcement on a measured landing-provenance number ("adding new hard rules would make a bunch of agents crash… then we'll make it a blocking hard enforcement to enforce the stragglers to comply"). The operator's acceptance criterion for the mirror experiment is on 1313-prin verbatim.

RULINGS: 1305-udgs — (b) the door enumerates the three rosters that can refuse a push (fast tier, gate-steps.d, the hooks) by scanning their literal bindings, never the scripts drawer (138 scripts, a dozen refuse a clean tree); the TIME BUDGET is the contract: per-guard deadline 5 s (the knee: 94 of 105 under it), whole-run budget 150 s from the FIRST enforced measurement (140 s on the floor host; the earlier 120 s was arithmetic over a deadline that bounded nothing and was never a budget), the no-raise rule from then; guards run in their own process group with output to a file; concurrency is 1314-2mdv. 1309-fhxb — a shared runner grammar: a terminal skip:/advisory: line is SKIPPED/ADVISORY never PASS or FAIL, any failure verdict still fails, fabricated-skip arms ship with it, TEST-level verdict derived from steps (an all-skip test is SKIPPED and out of the rate: 1273-4mak one level up), lines added never reshaped, a fleet announcement before it lands. 1313-w78k — a future ts beyond 900 s is refused at the lane, a past ts is noted, the six standing fragments get one note. macneo's image_root — a destructive path has no silent default: Result on the destructive variant only, --diagnose labels its fallback, the live tray's twin removed, no live fire on macneo. lenovinha's client half — a per-host AppRole and per-host client-signer role (exact allowed_users, no extensions, force-command, 30m), on-demand mint, client-side validation, DISTINCT principal til:host-push:<host> beside the forge's, source-address the enclave subnet (a 127.0.0.1/32 proposal was retracted within the hour: the lines cited were the git daemon's healthcheck, not sshd's). Smoke consent: esmeraldinha is the operator's workstation and takes no smoke without per-run consent; yolanda holds the standing consent; I sent esme "as usual" once and retracted it.

FINDINGS TO CARRY: (1) exit status is not an answer — four zero-exit-as-answer defects in one day (the mute plan binary vouched for by the lane and the checker; a `date -d` fallback that can never fire; `date +%s%N` on BSD), now a traps page and a fleet rule: check what the command produced. (2) A structure-aware writer and a text-aware reader agree on nothing but the parse: a YAML round-trip dropped eight comments; a library dump's indentless sequences hid `unscoreable:` from the scorable checker on two landed rows (restated by set-field; checker row 1319-vd5h); the derived cheatsheet tree needed a command to stay true. (3) The runner's `--list` lists 0 of 440 suites on every host (looks for litmus:<name>.yaml; 1318-3dgq). (4) 144 litmus steps invoke skip-capable scripts without admitting skip (pirria's census; the reason for 1309-fhxb's grammar). (5) The WSL balloon shape reaped yolanda's gate a fourth time (Windows 18% free, guest healthy); the slice goes by salvage and the relay gates it. (6) The vsock vocabulary links into both guest builds; only the feature-less binary's own refusal string discriminates (yolanda's disproof of my presence probe). (7) The skills single-source guard reads the git index; an unstaged new skill is green locally and refused at the gate (1314-w9fx). (8) A merge commit diffed against its first parent lists trunk's own landings as the peer's additions — I told macneo their code had reached trunk ungated when their push was three plan files; check ancestry against the previous tip.

CORRECTIONS OF MINE: the macneo merge-diff read above; "as usual" smoke on esmeraldinha; a "launched" written before the launch ran (land 39 had refused twice); accepting lenovinha's loopback source-address on evidence from the wrong log without asking which process wrote it; an event dispatched to an archived row (660-ryhn; the tool refused it and pirria placed it on 1077-vzwq); my ci-release arm counting fixtures; my working-log timestamps composed from memory twice (an hour, then twenty minutes ahead); a bindings copy that overwrote a merged file; a `--list` probe that dropped stderr and used head.

THE RELAY LANE: nineteen deciders plus the derived-cheatsheet sync run before a gate is spent; five refusals caught pre-gate today, three more caught only by the gate and added since; the front door on trunk makes the same set one command on every host.

FLEET STATE at the cut: the cut freeze on linux-next from 21:13Z until the back-merge; plan lane, salvage/ and work/ refs exempt. Post-lift relay queue in order: lenovinha 4239446e7 (link 3's mount-target fix; then the client half and the three pushes), yolanda's 1308-9ej7 salvage (gate reaped on their host), pirria's 1309-fhxb grammar after the fleet announcement plus the `date -v` sign fix, macneo's 1315-d4qd on osx-next when gated, macbookair's plan-only osx-next fold (830-xsk2 closed, 1309-rb3p claimed), esme's 1304 2a. yoga owns 1315-4a7j with today's ten cycles (five pure races) as its pre-fix number. Smokes of v56.9.20.1 after the README row lands: pirria (Linux), macneo (macOS), yolanda (Windows, standing consent re-read at the moment of use).

OPERATOR: nothing is blocked. Your three directions today are rows, and the branch migration's first phase refuses nothing; the mirror experiment is marked successful only by your criterion on 1313-prin, and the server half is up. One decision still yours from earlier: the Pi 5 fleet-server topology (1290-5833).

tokens: subagent_tokens=453807 agents=6 by_model=sonnet:6 (two read-only Workflow sweeps: the ledger by theme, 236,304; the branch-discipline investigation, 217,503; no other sub-agents this cycle)

OUTCOME (appended at push time): the cut. The release gate refused TWICE on pre-build litmus reds (seven in all, all present on run one; two fixed batches: exec bits, the claims scaffold, the SKIP_GRADE env for a 20 s step, the manifest arm, a locus-gated skip admitted, the front door skipping the live-ledger fixture it was killing mid-arm) and passed on run three; PR #121 merged to main; VERSION bump PR #122; tag v56.9.20.1 at cc9efe7ad (main's bump-merge, verified unmoved); back-merge fb404e363 on linux-next (memo-green, gate-fresh-except-plan); freeze lifted at the back-merge push; release run 35546386490 dispatched and watched. The operator approved the scoped integration tiers ("I approve them"; both LIGHT and SCOPED per yoga's follow-up) — recorded on 765-xpct by yoga (302118c56) and by the coordinator (this push); the operator handed the worker cadence-command decision to the coordinator: workers run check-fleet-membership.sh then advance-work-from-plan; meta-orchestration full is the coordinator's loop (join-the-fleet §3 corrected in 1317-9ugn). Rows filed in this push: 1320-44rs (a fixture plants probes in the live ledger dir), 1321-2ixp (a Windows salvage snapshot loses exec bits), 1322-qfxs (stale usage strings, measured on the installed binary by yoga). Post-lift relay bundle next: lenovinha 4239446e7, yoga 48a768610 + 80bf3acc1 + 69d957699, yolanda 8f46e1add, esme 4e0d00535; then pirria's 1309-fhxb grammar on its own gate after the fleet broadcast; smokes of v56.9.20.1 to pirria, macneo and yolanda after the README row lands.
