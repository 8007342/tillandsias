## Cycle 2026-08-25T13:43Z (linux_immutable pirria — the guard against invisible work was itself made blind by garbage collection, and it had already fooled me)

- **Host**: pirria, `linux_immutable`, branch `linux-next`, lane `loop-cron`, 6h cron fire. Fast-forwarded 28 commits. Scheduler `due:not-installed`.
- **Guards all green**, including `ok:gh-keyring-push-verified` — the plain verified form, not even the hook-refused path, because the fast-forward brought another host's gate stamp for the same tree. Daily maintenance `current:2026-08-25`; de-slop `not-due order=882 delta=48`.
- **The tier is nearly drained.** `select-work-batch linux --seed pirria` returned **size=1**: only 863-iicc, which is orchestrator-gated and whose ruling I cannot produce. Down from 2 last cycle. I did not fall back to the general queue — the selector refuses that by design (847-wgy4) and a 4-core host running a fast host's work measures the wrong machine.
- **I OWED A CORRECTION AND IT TURNED INTO THE CYCLE'S WORK.** Last cycle I escalated 865-n8vq — "the trunk has been unreleasable for a week" — as stranded work nobody was tending. It is a p0 the **coordinator had pushed progress into four times that morning**, most recently 106 minutes before the detector called it stranded. My alarm was wrong, and the detector was my only evidence.
- **The detector was reading half the ledger.** `check-stranded-in-progress.sh` counted activity with `grep -rh "packet_id: <pid>" plan/index.d/*.yaml`. Events live in fragments only until compaction folds them into `plan/index.yaml` — and compaction is routine garbage collection the cycle is told to run. So its window was "since the last fold", and the longer a packet is worked the more likely its evidence has been folded and the more confidently it was reported abandoned. Measured: **865-n8vq — detector saw 0, ledger held 35. 831-ezea — saw 0, held 1.**
- **Why that is worse than a noisy guard**: 641-e2qa built this check because an `in_progress` packet is invisible in both directions. Pointing it at the best-tended packet in the ledger inverts the signal and dilutes the real strandings in the same list. The skill's "watch whether the count is rising cycle over cycle" was unusable while the count tracked compaction age rather than neglect.
- **Filed and closed 882-vqe4 in the same cycle.** Fixed by asking the thing that already folds correctly, not by adding a second grep with a wider window — that would be the same defect one storage location later (704-zcgi: the copy has to go, not just the instance). New `tillandsias-plan plan-events <packet_id|order>` prints a packet's folded events, base plus overlay; the detector classifies from it, and **declines to classify at all** (`summary: unavailable:no-runnable-plan-binary`, exit 2) when no folder is available rather than guessing from half the ledger. That the subcommand did not already exist is itself the finding the skill's "if the loop needs it repeatedly, that is a missing tool" predicts.
- **The fixture pins the true positive, not just the false one.** 6/6 hermetic on a scratch ledger: base-evented, frag-evented, **a claim with no events anywhere must STILL report stranded** (without that arm this fix could have "succeeded" by deleting the feature), the summary shape, **compaction invariance** — run, compact, run again, verdicts identical, which is the property that was actually violated — and a mutation control reproducing the pre-fix blindness.
- **Live after the fix**: 865-n8vq and 831-ezea no longer report stranded. The list now names 881-29me and my own 882-vqe4 — both claimed with no progress event yet, which is the correct true-positive reading.
- **A win for the gate, recorded as such.** Adding a dispatch arm without a usage entry failed `every_dispatch_arm_is_documented_in_usage` — "a subcommand nobody can discover might as well not exist". That is the orphaned-capability shape 865-n8vq's own guard-activation failure is about, enforced one level down, and it caught me mid-fix.
- **Not touched, deliberately**: 865-n8vq itself. It is live coordinator work with events from two hours before this cycle started; the skill says do not bulk-close what the sweep reports, and the right move was to fix the instrument that mislabelled it.
- **Flow**: `overhead_ratio` 1.67 → 1.75 on a 1-packet batch (was 2 packets last cycle). Consistent with the batching story — smaller batch, slightly worse amortisation — on a sample of three.
