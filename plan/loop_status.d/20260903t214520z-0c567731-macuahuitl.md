## Cycle — macuahuitl 2026-09-03T21:40Z (coordinate-multihost-work, cron 23-odd)

- **THE CLAIM BOARD IS COMPLETELY CLEAR: in_progress=0, unclaimed=0, expired=0,
  held=0.** First time in this session. 980-xcaf was released to `ready` last
  cycle, 978-juw4 is `blocked` on an operator decision, and everything else
  closed out.
- **`windows-next` was 1 AHEAD — first Windows activity in ~20 hours.** yolanda
  is back and unblocked. Merged both siblings: `windows-next` carried the
  997-e4v2 quarantine fix, `osx-next` carried two ready-but-done closures and an
  attestation.
- **THE 996-bcjx DESTRUCTIVE HALF IS FIXED, and yolanda improved on the
  prescription esme-windows and I recorded.** My ledger said "treat a 128
  refusal as undecidable". **That is too coarse.** esme measured ten damage
  modes against git 2.55.0 and yolanda reproduced them before acting: 128 also
  covers an EMPTY `.git`, a DELETED HEAD, and HEAD as garbage text — three
  genuinely-broken states printing the same message the refusal does. Treating
  every 128 as indeterminate never destroys AND retires the feature's whole
  purpose, since the quarantine exists for exactly the deleted-checkout case an
  empty `.git` produces. **A capability loss shipped as a side effect of an
  error-handling fix** — which is the shape this packet is about.
- **Their split is STRUCTURAL rather than by prose or by exit code alone:** does
  the tree still have a HEAD file, determined in our code, immune to git's
  localised and rewordable stderr, handling both the `.git` directory and the
  gitfile layouts. **Conservative by construction in the right direction** — in
  doubt it answers present, because a wrong TRUE costs a refusal the operator
  resolves and a wrong FALSE costs a renamed directory. Mutation-verified in
  BOTH directions across six arms.
- **A blind spot they recorded that predates the change and survives it:** a
  corrupted commit OBJECT returns 0, because `rev-parse --verify` resolves the
  ref and never reads the object. This predicate detects a damaged REF, not a
  damaged OBJECT — true before and after.
- **They did NOT ship `safe.directory`, and their reason beats mine.** I ruled it
  should wait on the privilege question so we would not land an exception the
  real fix deletes; they observed that as a FLEET remedy it **entrenches the
  mount the migration exists to delete**, and that the tmpfs-per-launch design
  has a SINGLE writer and a SINGLE reader — so the identity disagreement cannot
  arise at all. Every current remedy is a mitigation for an arrangement being
  removed.
- **A third host applied this morning's cfg-gating lesson to this afternoon's
  code, unprompted.** The classifier is deliberately NOT feature-gated though
  both callers are, because gated it would be invisible to the default test build
  — a data-destroying path tested on no host that builds neither tray nor
  listen-vsock. That is lenovinha's 972-6vaj finding travelling on its own.
- **Roll call: macbook 21:38Z, yoga 21:19Z, yolanda 20:17Z, lenovinha 20:01Z** —
  four hosts active within the last ninety minutes and a clear claim board, which
  is the healthiest the fleet has looked today. macneo silent 12.5h and already
  in the operator queue as a question about the host. pirria, esme and both
  forges finished or blocked by their own statement.
- **No dispatch.** Four hosts active and self-selecting, claim board empty, queue
  deep. Nothing idle that is not already accounted for.
- **Litmus:** not run. `--check` passed on the merged union carrying yolanda's
  new classifier and its six-arm test.
- **Operator queue, five plain asks, unchanged in substance:** the esmeraldinha
  PAT (confirm Contents:write, re-run from a checkout with an origin); macneo's
  forge-build permission; is macneo-macos still up; the root-vs-forge privilege
  call — now with yolanda's finding that the migration dissolves the class;
  the ten quarantined trees. yolanda's own two decisions appear ANSWERED.
- **Nothing is blocked.**
