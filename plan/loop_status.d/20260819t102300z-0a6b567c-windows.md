## Cycle 2026-08-19T03:23 PDT→04:15 PDT (windows — YOLANDA, MO FULL: overnight loop cycle 10, 832-q4mn CLOSED)

**Result: the order-uniqueness gate now reads the ledger the fleet actually writes to. A collision filed as a fragment is caught on the push that introduces it instead of waiting for whoever next compacts.**

- **Host**: YOLANDA, `windows-next` 4fd7cd99b → 69cd1cd82 (claim) → this cycle. Preflight `ok`, guards credential `ok:gh-keyring` / branch `ok:branch-windows-next` / expert-base `ok:expert-base-ready`, opsx `ok:clean-tree`. Boundary `/tmp/meta-orchestration-boundary.LyVwZH`. Sibling heads: main=0bba6525f, linux-next=6de002620, osx-next=10f285143→9a47f1ddd.

**THE FIX.** `plan_orders_fold_fragments` overlays `plan/index.d/*.yaml` onto `plan_index.steps` before the uniqueness pass, using the same base ⊕ fragments shape `tillandsias-plan check` already uses so the two cannot disagree about what the ledger is. Fragments resolve relative to the index under test, so `--index` on a fixture tree reads that tree's fragments. An unparseable fragment is a hard error — skipping it would restore the exact blindness this removes.

**THE NEAR MISS, recorded because it is the lesson.** Folding only `packets:` would have read every fragment-completed packet as still OPEN — and "is any member open" is precisely what the duplicate rule turns on — so a done-only group whose closure lives in a fragment would have started reporting a FALSE violation. `set-field` records terminal status in a `status:` LWW channel and deliberately does NOT re-declare under `packets:`, because that is a G-Set no-op (order 636-9m79/642-fedr, stated in the fragment header itself). The fold applies that channel, last-write-wins by ts, before the open-member test.

**Five hermetic cases**, wired as step 8/8 of `litmus:plan-index-order-uniqueness` (whole methodology-accountability suite 16/16, 100%): base collision caught; fresh-order control passes AND is counted; two fragments colliding with each other caught — the real multi-host shape; the status channel applied before the open test; and a negative control proving that case passes because the channel was read, not because the rule went lax.

**Case 4 failed on first run and was right to.** My fixture had one member open and one done and expected grandfathering — but grandfathering requires EVERY member terminal, so that group is a genuine violation. The fixture was wrong, not the code. Corrected so the base member is already terminal and the fragment closes the other, which makes the channel the thing that flips the verdict.

**The verdict line now reports the fragment contribution** (`1129 packets, 3 from fragments, 0 grandfathered`) so a reader can see the fold happened rather than trust that it did.

**NOT DONE, carried:**
- **One second implementation left disagreeing, and named rather than left to be found**: `litmus:plan-index-order-uniqueness` step 2 falls back to an inline ruby reimplementation when cargo is absent, and that ruby still reads the base only. Not fixed — rewriting a multi-line ruby one-liner embedded in a YAML scalar at the end of a cycle is the kind of edit that looks free and is not, and the fallback is dead on any host that builds this repo (cargo is a hard `./build.sh` requirement). Probably the right answer is to delete the fallback rather than teach it to fold, which is a decision, not a mechanical change.
- **The ghost-trace habit caught me twice now**: this fixture shipped `@trace spec:plan-ledger`, a spec that does not exist, exactly as the 805-yzhw fixture did. Fixed in a follow-up commit both times. Worth a personal rule: check `ls openspec/specs/` before writing a trace line.
- Remaining claimable: 833-w8rc (the 48h credential arithmetic), 628-c7qd / 628-r2vk (tray i18n).
- Operator-gated, unchanged: the attended lane click, the `argv_survives_wt_reparse` call, and whether 643-64bx becomes operator-owned.
