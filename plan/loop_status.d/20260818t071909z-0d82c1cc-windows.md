## Cycle 2026-08-18T00:19 PDT→00:55 PDT (windows — YOLANDA, MO FULL: 795-zshi is HALF DONE, and its Windows half is the fix for last night's paste failure)

**Result: no code landed, deliberately. The cycle's yield is a correction to a packet's premise plus a linkage nobody had drawn — 795-zshi reads as an unstarted protocol change and is in fact half complete, and its remaining Windows half closes the operator-facing defect filed as 805-ek9e. Filed as 823-u5zf rather than started, because 30 minutes is not enough for a change whose own note forbids leaving two code paths.**

- **STOP CHECK**: `Tue Aug 18 00:19:09 PDT 2026`. The PDT date now MATCHES the deadline's date, which is the mirror of last cycle's trap — the time is 00:19, ~5h40m short of 06:00 PDT. Proceeded.
- **Host**: YOLANDA, `windows-next` 45d87ae59 → merged origin/linux-next ×2 → 74946cc57, local == remote, worktree clean. All guards green. `experts: 25/25 answered`, `expert_accuracy: 22/22`, `mcp: health=ok`, no `mcp_outage:`. `verdict: ok:nothing-flagged`; `in_progress=0 stranded=0`.

**795-zshi's TRUE STATE, measured — the packet text does not say any of this:**

| layer | state |
|---|---|
| guest allowlist | **DONE** — `exec_allowlist.rs` has the verbatim arm (`other if other.starts_with('/')`), gated on VERBATIM_ARGV_PREFIXES + VERBATIM_ARGV_FORBIDDEN |
| control wire | **DONE** — `CAP_EXEC_ARGV_VECTOR = "ExecArgvVector"`, with the skew rule stated: feature-detect on `HelloAck.server_caps`, never compare wire versions |
| hosts | **UNTOUCHED** — `argv_survives_wt_reparse` ×7, `wt_safe_title` ×4, `build_exec_guest_shell_cmd` ×9 (macOS's, separate rung) |

The guest arm's own note argues correctly that it is not a widening of authority: the `/bin/bash -l|-lc <string>` arm never inspects the script, so everything reachable through the new arm was already reachable through the old one. What changes is only HOW the request is expressed.

**THE LINKAGE NOBODY HAD DRAWN, and why 823-u5zf is p1.** 805-ek9e records that every project lane opens in legacy conhost with no clickable links and no working paste — which made the operator's Claude and Antigravity device-code logins impossible to complete on 2026-08-17. Its proposed remedy was an injected wrapper script, which treats `argv_survives_wt_reparse` as a fixed constraint. **It is not one.** That predicate exists ONLY to decide whether a flattened `bash -lc "<script>"` survives wt.exe's re-parse, and the flattening exists only because the guest allowlist used to admit nothing else. Send a verbatim argv → no string to re-parse → no predicate → no conhost fallback → paste and links return, with no wrapper to maintain. **One change closes an architecture packet and a field-reported UX failure.** Recorded as a note on 805-ek9e: keep it open as the UX statement, prefer 823-u5zf's fix over the wrapper.

**MECHANICAL, NOT A DESIGN PROBLEM — checked rather than assumed.** The obvious objection is that the flattened script also sets up environment. `ControlMessage::PtyOpen` already carries `env: Vec<(String,String)>` and `cwd`, documented as "env REPLACES the in-VM process environment (no host-env inheritance)", so the exports move into the message.

**THE ONE WRINKLE, named so it is not found late:** the script also runs `install -d -m 0700 "$XDG_RUNTIME_DIR"`, a side effect the verbatim arm cannot express because it forbids a shell. Either the guest creates it when honouring `env`, or the lane binary does it on startup. Decide BEFORE deleting the flattening path.

**FILED, NOT STARTED — and that is the judgement, not a shortfall.** 795-zshi's own note: *"Widening without deleting the five workarounds leaves two code paths for the same thing, which is how this subsystem got here."* With ~30 minutes to the next fire, a rushed half-migration is precisely the failure mode the packet warns about. The rung carries the mechanism, the sequencing rule (guest advertises before any host sends; keep the flattened path for a guest lacking the capability, since a staged guest binary can be older than the tray) and the wrinkle, so whoever takes it starts with the analysis done.

**808-43mw / 808-7yrd HELD, tenth consecutive cycle.** Still `ready`, unclaimed, no linux response, no operator steer.

**NOT DONE, flagged:** freshness audit skipped in favour of attempting a drain; ledger compaction eligible; bar-raise proposed, not enabled; 798-q4m9 open pending 819-qgy2; 806-2r4s NPU half open (809-7e4m).
