## Cycle 2026-09-04T21:37Z-22:10Z — yolanda (windows), advance-work-from-plan

Boundary first; credential preflight `ok:gh-keyring-push-verified` (fully green
again, not the hook-refused variant). **997-e4v2 slice 3's wire step landed**
(`54a9471a1`) — the LocalProjects surface is off the control wire and
`WIRE_VERSION` is 3.

**The precondition was verified here, not taken on report.** `a00a0f76b`,
`b8aec1dec`, `bae481780` confirmed as ancestors of my HEAD, then consumer counts
re-derived in my own tree: 1/2/0/1/2, and **all six remaining hits checked line
by line as comments**. Two hours earlier the same counts were 14/13/1/2/15, so
the check was load-bearing rather than ceremony.

`WIRE_VERSION 3` covers the removal and nothing else — 890-y72v's discriminator
is ruled to version 4, so a second reason in this diff would be a stowaway.
`LocalProjectEntry` retained with its reason in the source: it has no
discriminant, so keeping it changes no byte of the encoding.

**Both mutations run before handing over, and one is green on purpose.**

- `WIRE_VERSION 3 → 2` → **RED**. The bump is guarded.
- Pure reorder of `DeliverCredentials`/`…Reply` → **GREEN, 62/0.**

The second is the point: **1029-5wvd's gap survives this commit.** Nothing
notices a renumbering, and macbookair's exhaustive pin is still required. I ran
it so the packet cannot be read as closed by this landing, and so the next agent
has a measured negative on the *post-removal* tree rather than one taken before
it.

**A guard I didn't know existed caught the bump.** `wire_version_constant_is_two`
asserted the constant equals 2 and went red — the only thing all day that
noticed a wire change without being told to, in the same file whose round-trip
tests and whose "every declared variant" table (22 of 35, missing exactly the
credential and PTY traffic) notice nothing.

**And I corrected the feature-set advice I was given, in the direction that
narrows my own claim.** `--features tray` is *unbuildable on Windows* —
`tray/mod.rs` uses `std::os::unix` — so the set macbookair identified as the only
truthful one does not exist here. I verified `--features listen-vsock` and
confirmed its warnings are identical on a pristine tree. Consequence: the
tray+vsock combination is only really checked by the union pass on Linux/macOS,
and my green is narrower than "I used the recommended feature set" implies.
Said plainly rather than letting the stronger reading stand.

### cycle-metrics (`--cycle-start 2026-09-04T21:37:55Z`), verbatim

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent
timing: steps=9502 build_check_ms_avg=604020 build_check_mix=mixed:forced=132,memoised=21 litmus_ms_avg=3478 slowest=step:checking-that-added-fragments-declared-closures-resolve-885-92iu:490764 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-04T21:37:55Z steps=123 top3=build-preamble=2,build-check=1,build-check-memoized=1 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=8743 steps=125 top3=build-check:runs=116:total_ms=74512576:avg_ms=642349:fail_pct=18,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=95:total_ms=10458581:avg_ms=110090:fail_pct=0,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=95:total_ms=6485994:avg_ms=68273:fail_pct=0 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=50 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=95:avg_ms=110090:fail_pct=0:saved_ms_upper=10348491,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=95:avg_ms=68273:fail_pct=0:saved_ms_upper=6417721,step:writing-the-gate-stamp:runs=95:avg_ms=58079:fail_pct=0:saved_ms_upper=5459505 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=707 ready=455 plan_bin=./target/release/tillandsias-plan.exe
repo: commits_this_cycle=- worktree=clean traces=current
verdict: attention:experts-never-called
```
