## Cycle 2026-08-25T05:43Z (linux_immutable yoga — the framing slice I deferred last cycle, and a nine-hour service finally started)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `loop-cron`. Guards green; lock taken and released; boundary clean both ends.
- **THE PROXY: RESOLVED, and I was wrong to keep only noting it.** Three cycles I reported `note:enclave-service-stopped:service=tillandsias-proxy` and moved on. This cycle I read the logs instead. It shut down cleanly at 20:42 local ("Preparing for shutdown after 8290 requests", "Exiting normally"), then squid SIGABRTed during teardown and `squid-supervisor.sh` normalised rc=134 to 0 — which is CORRECT, DESIGNED behaviour: squid has a global-destructor crash on ordered shutdown (782-9jfg reproduced it in 5s with zero requests; not fixed by the 6.12 bump, not our config), and the normalisation is what gives `Exited (0)` its meaning back. So `Exited (0) ... (healthy)` was honest output, not a laundered fault.
- **One `podman start` fixed it.** Healthy in 8 seconds, still up after a minute, preflight now reads `services-ok`. Vault, dev-inference, nix and builder had been Up beside it the whole time — a partial enclave, not a deliberate teardown.
- **Filed 878-79b5 for the actual defect**, which is not the proxy's. The re-ensure path exists and is good (`LivenessProbe`, 782-dpby) but runs INSIDE the tray process, and an unattended cycling host has no tray. So the only thing that looks at the enclave on such a host is `cycle-preflight.sh`, which is a reporter by design — it prints a remedy addressed to a human, every cycle, forever. **The host with the most cycles has the least supervision.** Four consecutive cycles proved it, three of them mine.
- **Why that packet must not just say "restart it in the skill"**: the supervisor's in-container discrimination (did I forward this stop?) is unavailable from outside, so a naive auto-restart would fight every deliberate operator stop — the exact behaviour the supervisor was built to avoid. The row admits "declare that preflight will not act, and make the verdict say so" as an acceptable answer, and requires a negative control either way.
- **795-5itp slice 3 LANDED — the vsock_server migration I designed last cycle and deferred as "a full cycle's work". It was.** `serve_ready_stream` wraps the connection in `Framed` ONCE before the Hello; Hello read, HelloAck write, reader task and all 17 `write_envelope_with_shutdown` sites go through that one owner. The writer is now generic over `futures::Sink<Bytes>`, which is what made it atomic — migrating the read alone leaves two owners of one stream.
- **The split is `futures::StreamExt::split(framed)`, never `tokio::io::split` on the raw transport.** `Framed` fills its read buffer opportunistically, so one read can pull a pipelined Subscribe in behind the Hello; reclaiming the stream afterwards drops it silently, and only when the peer pipelines.
- **On the pin, said plainly rather than hidden**: `pipelined_hello_and_subscribe_both_survive_the_handoff` CANNOT fail against the hand-rolled reader it was written beside — `read_exact` buffers nothing. It is a migration pin, not a red-first reproduction, and its own comment says so. Hand-falsified against the naive shape instead (`into_parts` + fresh `Framed`, dropping `read_buf`): fails in 2s with its intended message; restoring the framed split returns green.
- **`read_envelope` kept and marked `#[cfg(test)]` with its reason at the definition** — it is the interop evidence the migration rests on, since these tests drive the framed server with a peer that frames by hand. Delete it and the interop claim becomes a tautology. Tree-wide `from_be_bytes` is unchanged at 18 BY DESIGN: last cycle's closure definition counts production sites plus a reasoned exception list, because a naive count cannot tell a fake from a duplicate.
- **Verified**: 484 passed / 0 failed / 3 ignored with `listen-vsock`, clippy `-D warnings` clean, default feature set still checks, gate green after `cargo fmt`.
- **Baselined, not assumed**: `proxy_cache_policy::bumped_origin_tls_and_signed_url_logs_fail_closed` fails in a full crate run. I stashed the slice and re-ran — identical on a clean tree. It is 828-etu6, and I noted there that it reproduces on LINUX, not only on `osx-next` as its title implies; the assertion is a source grep, so it is red everywhere.
- **Claim released to `ready`.** Three sites remain and each carries its own hazard: `pty_vsock_bridge.rs` (macOS; its :241 drops an oversize message and keeps the session alive, and a Sink error is terminal, so a naive swap kills the PTY session), `tray/mod.rs` (blocking std `UnixStream` in an `Arc<Mutex<..>>` registry — disposition, do not rewrite the tray onto a runtime), `hvsocket.rs` (Windows, no production callers).
