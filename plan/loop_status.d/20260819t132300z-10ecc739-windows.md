## Cycle 2026-08-19T06:23 PDT→07:00 PDT (windows — YOLANDA, MO FULL: overnight loop cycle 13, 830-t4qp cause (a) closed)

**Result: the git-mirror-service suite goes 70% → 85% on this host, and my own filed diagnosis of why turned out to be imprecise — which only measuring found.**

- **Host**: YOLANDA, `windows-next` 945769e43 → c62463821 (claim) → this cycle. Preflight `ok`, guards credential `ok:gh-keyring` / branch `ok:branch-windows-next` / expert-base `ok:expert-base-ready`, opsx `ok:clean-tree`. Boundary `/tmp/meta-orchestration-boundary.CAlwnj`. Sibling heads: main=0bba6525f, linux-next=6de002620, osx-next=9a47f1ddd.

**I FILED THIS PACKET AND GOT ITS DETAIL WRONG.** 830-t4qp said three fixtures "commit without setting user.name/user.email" and prescribed exporting `GIT_AUTHOR_*`/`GIT_COMMITTER_*`. All three already exported `GIT_AUTHOR_NAME` and `GIT_COMMITTER_NAME` — it was the **EMAIL halves** that were missing. With only names set, git auto-detects an address from `user@host`, which works on a Linux host and fails on a Windows/MSYS one with no domain: `fatal: unable to auto-detect email address (got 'bullo@Yolanda.(none)')`. Every commit then fails and the cascade surfaces as `error: src refspec HEAD does not match any` — which is why it reads as a refspec problem, and why the error text alone led me to the wrong statement.

The remedy *pointer* was right — `test-git-mirror-relay-verified-ack.sh` exports both halves, which is exactly why it passes here. The statement of what was missing was inferred from the error rather than read from the source. Recorded plainly, because "I filed it, therefore it is right" is precisely the failure mode this milestone keeps collecting.

**Fixed:** all three now export both EMAIL variables, with a comment naming why they are load-bearing and why the symptom appears as a refspec error. Verified individually (3/3 PASS) and in the suite.

**THE REMAINING FOUR are unchanged and were already attributed to different causes:** `forge-packed-refs-shared` (CRLF, case 2), `forge-gitdir-staging-chown` (blocked by the native-Windows `E0425`), `forge-upstream-auth-gate` step 6 (guard fixture under a restricted PATH), and `harness-crash-supervision-shape` (MSYS has no `pgrep`).

**NOT DONE, carried:**
- **830-t4qp released to `ready`** — four of seven still red, and the deliverable asks for each to be fixed or explicitly marked not-executable-on-windows.
- **Two of those four are blocked on decisions, not typing**: whether `tillandsias-headless` should compile natively on Windows at all (it only ever runs in the guest), and whether a `pgrep`-dependent scenario should be made portable or declared not-executable here. Same shape as 643-64bx — cheap once someone decides the direction.
- Everything from the closed queue that needs the operator is unchanged: the attended lane click, the 833-w8rc rotation experiment, the `argv_survives_wt_reparse` call, 643-64bx's ownership.
