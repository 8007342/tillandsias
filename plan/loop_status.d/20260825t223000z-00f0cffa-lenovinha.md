## Cycle 2026-08-25T22:30:00Z — lenovinha (linux_immutable, linux-next, retraction follow-up)

Short follow-up cycle. Coordinator relayed that 889-bx99 — the macOS case-sensitivity defect I
had cited in the promotion gate's accept-path caveat — was FALSIFIED and withdrawn by its own
author. The "verification" was typing a capital path on a case-INSENSITIVE volume and watching
it resolve, which cannot fail and therefore measured nothing.

**Re-checked independently before editing**, rather than acting on a relayed retraction the same
way I had acted on the relayed claim — which is how the bad citation got in. `grep -rn
"Application Support/Tillandsias"` over the whole tree returns NOTHING; every site is lowercase
(status_item.rs:367/:383, diagnose.rs:71/:2328/:2515, vz.rs:48/:767/:2369, vz-spike.rs:220,
diagnose-macos-enclave.sh:43). There is no capital-T form to mismatch against. Retraction correct.

Removed the citation from both the comment and the emitted line. The general caveat STAYS — "a
PASS proves the run completed, not that its preconditions held" is true on its own merits, and a
smoke that silently skips its destruction step still reports a clean room. What a release gate
must not do is cite a retracted finding as though it were evidence; that is the same category
error as the junk-matching this gate was filed to fix.

Deliberately did NOT re-point the citation at a substitute defect. I have no verified instance in
hand, and inventing one to keep the sentence looking authoritative is the failure mode itself.

New negative control pins that the gate's OUTPUT never cites 889-bx99 — scoped to the output
rather than the source, because the comments legitimately explain WHY it is not cited and that
explanation is what stops someone re-adding it. A control forbidding the token outright would
delete its own reason for existing. Suite 25/25, ./build.sh --check green.

Coordinator also approved the case-5 narrowing I had escalated (removing the legacy pass that
counted a "promotion verdict UNCHANGED (morning FAIL)" line as promotion evidence), and dropped
the defect-window-registry suggestion on my reasoning. Both recorded on 888-m75r.

### Push contention worth noting

Six push attempts, five lost to races — the coordinator and siblings are pushing continuously
during the release window, and a fetch+rebase+gate cycle is ~40s, which is longer than the gap
between sibling pushes. The gate passed every time; only the ref race failed. Not filing a
packet: this is a transient property of release night, not a standing defect. Recording it
because if it becomes normal it IS one, and a stale-stamp refusal after each rebase is the
correct behaviour that makes the loop slow (the stamp doing its job, post-887-bz88).

### Release status (not mine)

Blocked on 888-miiy, not on my gate: macuahuitl's --ci-full went red on
litmus:expert-groundtruth-harness scenario 6, environmental — no embedding endpoint on that host,
so no spec index, so the grader emits a HARNESS ERROR the fixture counts as a red case. Scenario
4 of the same fixture correctly treats a missing corpus as a harness error rather than a red.
They declined to edit the gate to make their own release pass; authoritative gate may move to
yoga.

### E2E: fourth consecutive deferral

Backup validator by assignment. Flagging that the count keeps rising.
