## Cycle 2026-08-25T23:05Z (macos tlatoanis-macbook-air — I own the fleet measurement protocol; filed it before the results arrive)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 12. Boundary snapshotted first, clean both ends. Cadence re-slotted to **:47** per the fleet phasing scheme (lenovinha lost 5 of 6 pushes to ref races tonight; the trunk is effectively single-writer at ~40s granularity). My `:13` was my own arbitrary pick, not the operator's, so re-slotting cost nothing anyone specified.
- **Release HELD** — not on macOS. Two independent stoppers found before the tag: `litmus:expert-groundtruth-harness` needs an embedding endpoint it never declares as a precondition (no host can currently produce a green gate), and 887-bz88's gate-stamp change deadlocks the Windows push lane. Stood down on e2e; prep holds (stale bundle neutralised, destruction-assert in).
- **NIX CACHE: DOES NOT APPLY HERE, answered by running it rather than reasoning.** `scripts/nix-cache-service.sh ensure` -> `skip:nix-cache:no-nix`. The better reason is the second one: the CONSUMER does not exist in this host's model. That script serves a persistent host store to a disposable forge CONTAINER that is enclave-only by spec; macOS has no podman enclave (the substrate is a Virtualization.framework VM, and podman here has no running machine), and the build path is NATIVE — Xcode CLT, rustup, never a nix toolbox. Installing nix would add a toolchain this host does not use to serve a container it does not run. Coordinator is carrying "three lanes, three different answers" to the operator so "every host runs a nix cache" does not become a standing expectation most of the fleet structurally cannot meet.

- **I NOW OWN THE CROSS-HOST MEASUREMENT PROTOCOL**, after pushing back on the original framing. The ask was "give us a third wall-clock number from Darwin". Three wall-clock numbers on a mixed workload differ for unattributable reasons and settle nothing.
- **The design change that matters: within-host deltas, not across-host comparisons.** yoga-vs-yolanda was paired as "matched hardware" but carries a CPU generation difference AND two distros, so any gap it shows cannot be attributed. Measuring a host **against itself** across the single variable of interest holds everything else constant BY CONSTRUCTION: yolanda native-ext4 vs `/mnt/c` (that delta IS the 9P/virtiofs cost), yoga toolbox vs host-native. Both ratified. The second also settles a pending question elsewhere — calmecacpilli measures `--check` at 107-122s dominated by LEDGER work, not compilation, so if the container boundary costs nothing on the stat pass the immutable-host story is ledger-bound.
- **Probe v1**: warm twice, then `/usr/bin/time -p` around 20 zero-change `cargo check --workspace --all-targets`. Near-pure filesystem stat traffic — which is where WSL2's penalty lives and where APFS differs again. **Darwin leg: 161 ms per no-op** (real 3.22 / user 2.05 / sys 1.09 over 20), 4617 tracked files, 23 crates, M5 10-core, 16 GiB, at 5c21d37fb. The user/sys split is what confirms it is stat-bound rather than compute-bound, i.e. that it can discriminate at all.
- **Pre-committed the falsifier in writing to both hosts, before any numbers exist**: if WSL2's FS layer dominates, yolanda's `/mnt/c` is multiples of everyone and her native number lands near yoga; if instead yoga and yolanda are close and **I** am the outlier, the story is Apple silicon and the pairing does not show what it was designed to show; if all three are within noise, the probe is not discriminating and I will say so rather than dress it up. Now a fleet rule: no cross-host performance claim without a stated falsifier.
- **Withdrew my own baseline entirely.** I had offered `build_check_ms_avg=39171` with caveats. It is a MIX of memoised and forced runs and is not a measurement of anything — since 765-tkq2 an unchanged tree returns `ok:gate-fresh` in ~0.4s, so the mean encodes how many no-op runs a host happened to do. Flagging a caveat while still leading with the bad number puts the burden on the reader to discount it, and readers do not. Forced-only is now fleet practice; my forced figure is ~48-50s.
- **THIRD GNU-ISM OF THE NIGHT, caught before shipping this time.** I nearly used `date +%s%N` as the protocol's timer. `%N` is GNU-only by reputation; my `/bin/date` IS BSD (it rejects `--version`) and expanded `%N` correctly anyway on Darwin 25. So an idiom verified on my own host would have produced PLAUSIBLE GARBAGE on the very lane the exercise exists to characterise — worse than a hard failure. `/usr/bin/time -p` is POSIX. "Do not substitute your own timer" is now a fleet rule. With `du -sb` (886-yizb) and the case retraction (889-bx99), that is three instances in one evening of the same family: **verified on the host where it happens to work.**

- **FILED 890-nkdz: all of tonight's ratified practice exists ONLY in session transcripts.** Four rules plus the threshold review rule, ratified by the coordinator, living in cross-session messages and nowhere else. That is 801-qasc (a marker that lived as prose) / 660-ryhn (an unbound litmus) / 709-in2f (methodology claiming an implementation that did not exist) **one level further out** — ratified-in-a-transcript reads as settled and is not written down. Filed DELIBERATELY BEFORE the other two legs report: if it waits for the conclusion it becomes part of a results write-up, and results write-ups are precisely what nobody consults when they next need to measure something.
- The packet demands the two MECHANICAL rules be enforced rather than restated (a mixed `build_check_ms_avg` should be LABELLED at emission; the timer rule is checkable by check-bash-dialect.sh), and that the two JUDGMENT rules be recorded as judgment rather than dressed as mechanism — a rule that cannot be enforced should say so, or it becomes the next 709-in2f.
- **Verified the closure names a real bound litmus BEFORE committing** this time (`litmus:cycle-metrics-answer-rate-shape`), having been caught by the 885-92iu gate on 888-3d6x two cycles ago.

- **Adopted fleet-wide from 886-yizb**: *a threshold test that sets the threshold to zero is testing `>=`; any gate that measures and then compares needs one leg where the MEASUREMENT ITSELF is the assertion.* The coordinator places it in tonight's larger pattern — five instances of "an artifact read as evidence of the check that would have produced it", and this is the same error one level deeper: a TEST read as evidence of a MEASUREMENT it never exercised. One question defeats both: **what would have falsified this?**
