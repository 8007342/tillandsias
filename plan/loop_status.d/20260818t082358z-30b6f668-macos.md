## Cycle 2026-08-18T08:23Z — macos (hourly meta-orchestration)

- **Guards**: all clean; mcp `down:project-info` (**eleventh consecutive cycle**);
  stranded `in_progress=0`. Merged `origin/linux-next`. No boot.

- **Slice 5 of 795-zshi — the briefed work was measured and DECLINED.** Slice 4
  left a capability gate for deleting the preamble's `podman rm` and
  `chmod 600`. Three facts, checked rather than assumed: both lines are already
  `|| true` **no-ops**; **neither is one of the packet's five named
  workarounds** (grep returns zero in that file); and removing them leaves three
  `export`s, an `install -d` and the openssl block — so **the preamble stays a
  shell script and not one flattening workaround becomes deletable**. The
  briefed slice would have bought a second connection and handshake, on a path
  with wedge history, to delete two no-ops and move the packet's goal nowhere.
  Declined, capability left unspent for the slice that can use it.

- **Did the reduction that IS real instead**: the preamble was duplicated
  **verbatim** at both one-shot call sites, differing by exactly one token — a
  `diff` reported a single changed line. Now one builder. That matters beyond
  tidiness: two copies of a guest-bound shell string is precisely how 733-mppc
  got two `open_control_wire_stream`s with the same unbounded handshake written
  twice and fixed neither time — same session, same crate.
- **Proven byte-identical, not assumed**: the built string was captured from the
  compiler and diffed against the pre-refactor literal reconstructed from
  `git show HEAD:` under Rust's `\`-continuation rule — 664 bytes each, `cmp`
  clean. A shell string sent to a guest is exactly where a "harmless" whitespace
  drift is not harmless.

- **Two more literal source pins broke, and both were repaired UPWARD.**
  `github_login_preamble_pins_the_shared_lock_namespace` and
  `guest_ca_preflight_never_widens_the_private_key` grepped the *source window*
  of the `*_main` functions for literals that had just moved into the builder.
  Both now assert the **built value**, which is strictly stronger — a
  source-window grep passes on a literal that is present but never reaches the
  guest. **This is the second cycle running** that a literal pin turned a correct
  change red (701-iu9b hit it in vz.rs). Worth naming as a pattern: the
  expression-pinning gate discourages *adding* new ones but says nothing about
  those already in the tree.
- The CA test's whole-file NEGATIVE scan (no path may chmod the key to 644/755/…)
  was deliberately left a source scan — it asserts absence across the file, which
  does not depend on where the literal lives.
- **Falsified**: dropping `install -d` fails the parts test; moving the CA heal
  back inside the `test -s` guard — the 772-shi9 shape — fails the repaired CA
  test with the intended message. 98 tray tests pass.

- **NOT done, out of reach from here**: 795-zshi also needs
  `argv_survives_wt_reparse`, `wt_safe_title` (windows-tray) and the
  `wsl_root_sh` hazard deleted — none testable from macOS. Also outstanding: the
  attended **m8 UX smoke** macOS release acceptance owes; ledger compaction
  (linux's). 701-g98y, 774-b529, 778-n9z2 remain attended-or-restage-only.
