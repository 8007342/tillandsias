## Cycle 2026-08-26T04:45Z (windows/yolanda — 890-t9pu and 902-j49y closed; ledger compacted)

- **Host**: yolanda, `windows-next`. Preflight ok; guards `ok:gh-keyring-push-verified`, `ok:branch-windows-next`, `ok:experts-healthy`, `ok:surface-exposed`; `ok:checkout-lock:free`.
- **Batch**: the selector offered `harness-mcp-expert-validation` — a macOS p1 and two stale unscored items, a poor fit. Took the **windows-boundary story** instead, per the coordinator's directive to work related packets on consecutive cycles rather than follow the selector across epics.

### 890-t9pu — closed, both halves proven across the real boundary

`/tmp` was the default in **four** places across **three** scripts, and `/tmp` is
not one place: the gate re-execs into WSL2 and writes there while
`cycle-metrics.sh` reads Git Bash's filesystem. Fixing one script would have
re-created the defect between the health PROBE and the health REPORT — the same
bug one subsystem over — so the rule now lives in one file
(`scripts/metrics-log-path.sh`) and every participant asks it.

Host attribution: 2977 records read `host="unknown"` because the WSL2 build
distro is a Fedora **container image** with no `hostname` binary at all.
`/etc/hostname` reads the right name inside that same distro — the information
was there behind a probe that could not reach it.

**Proof, not inspection:** a record emitted from inside WSL2 was read on the Git
Bash side carrying `host="Yolanda"`. `build_check_ms_avg` populated for the
first time on this host (366673 ms, was `-`).

**First actionable result the fixed metric produced**, offered to whoever owns
gate velocity: this host's gate is not uniformly slow. `checking the plan
archiver preserves the ready set (831-ezea)` is 108.6 s of it — 30 % — then the
router sidecar staging at 52.1 s and the ghost-trace ratchet at 41.2 s. Those
three are 55 % of the gate. NOT diagnosed and NOT filed: one host, one run,
newly-trustworthy numbers rather than well-understood ones, and this host is also
the 38.6x-bridge host, so the **ratios** are the portable part.

### 902-j49y — found because 890-t9pu rotated the log, fixed same cycle

The order-531 verdict guarded a claim about the USAGE LOG with a number from the
GROUNDTRUTH HARNESS. With an empty log it fired, so one run printed
`expert_accuracy rate=100%` and
`verdict: attention:expert-answered-nothing-check-base-branch` together. A 100 %
score and "answered nothing" cannot both be true, and order-531 is not mild — it
means the ARTIFACT is wrong, check the base branch. The correct verdict existed
one arm below and was unreachable whenever the harness had graded anything.
**Any fresh host hits this on its first cycle.**

**Limitation stated rather than glossed:** I could not cleanly hand-falsify it.
The `worktree-dirty` arm fires before the expert arms, and any broken copy placed
inside the repo dirties the tree and masks the arm under test. Before/after was
observed live, not driven by a fixture. A deterministic fixture needs the verdict
logic extracted from the script's global state.

### Ledger compaction — and a correction to its stated invariant

79 fragments folded. `git diff --numstat` gave added=2851 **removed=2**, against
a skill that names "zero removed lines" as *the property to protect*. Checked
rather than assumed: the two lines were `status: ready` on 635-bhkb and 804-ckst,
each replaced by `status: done` — a set-field transition being materialized,
which folding a field update must do. The 2026-08-03 run folded append-only
events, so its zero was circumstance, not construction.

Real invariants verified instead: comments 38→40 (none lost), packets 542→561
(none lost), four-space item prefix intact on all 561, zero non-status removals.
Recorded on 582-4wdi, because an agent applying the proxy literally would refuse
a correct compaction and the skill's instruction on seeing it violated is to
restore a refusal that blocked compaction for months.

### Heads-up sent, LATE

The `sibling_heads_up_protocol` (Start Of Cycle 2a) landed at `fd18881ed` while
this work was in flight. I changed four shared scripts including one every host
runs, and sent nothing until after it landed. **Every host's metrics default path
moved**, so `experts:`/`flow:`/`timing:` will read `source=absent` on their next
cycle — a real cost imposed on four lanes to fix one, and reversible per-host via
the existing env overrides. That message is evidence FOR 903-8wsa's caveat, not
against it: a protocol that depends on noticing the protocol now exists has the
failure mode of the warning it replaces.

### Advisory

- stranded=2 (`831-ezea`, `896-f8ti`) — neither mine, both `any`-role; not closed.
- `ok:deslop-not-due:order=903 last=834 delta=69`.
- Gate went red once this cycle, correctly: my 902-j49y fragment set `done`
  without an evidence-bearing event (686-7qcm). Re-filed as `ready` and closed
  through `set-field --evidence`, the path the error message names.
