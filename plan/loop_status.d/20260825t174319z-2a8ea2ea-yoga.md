## Cycle 2026-08-25T17:43Z (linux_immutable yoga — filing the pattern I had been working around for five cycles)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `loop-cron`. Guards green, `services-ok`, lock taken and released, boundary clean both ends.
- **FIFTH consecutive cycle with the same un-advanceable batch**, and I had mentioned it in three loop-status entries without filing it. That is an unfiled finding under the capture rule, and it is independently schedulable — it is about the selector, not about any packet in the batch. Filed as **884-hfsj**.
- **The defect**: `pickup_role` records who could CLAIM a packet, not what its REMAINING work needs. All four stuck items say `pickup_role: any`, which was TRUE when they were filed and stopped being true as slices landed. The fact that would fix the routing **is already written down — in prose**, in each packet's progress events, because the hosts that did the earlier slices were careful. The selector cannot read prose, so the careful handoff is invisible to the only mechanism that acts on it.
- **Why it survived five cycles**: the host does not idle. It pays the orientation cost, reads the same four packets' events, re-derives the same conclusion, and finds work elsewhere in the batch. The cost never appears in burndown.
- **I refuted one of my own remedies in the same cycle, before anyone could build it.** "Honour the existing `owner_host` field" fails on measurement: 45 carriers of 596 packets, **zero `macos` values**, and none of the four stuck packets carries it — it would not have prevented a single cycle. Worse, honouring a field written while nothing read it would immediately hide work on the strength of metadata nobody has ever validated. Live options narrow to two: decay `pickup_role` as slices land, or an operator decision that lane-steering stays manual — in which case `owner_host` should be deleted or documented as advisory, because an inert field that looks like routing metadata is a trap.
- **Deliberately did NOT decide that unilaterally.** Narrowing `pickup_role` changes what every host in the fleet is offered; I have five cycles of evidence about ONE host's experience, which establishes the defect and does not choose the fleet-wide remedy.
- **Then took 335 from the same batch**, whose first exit criterion is a failure-mode inventory from ledger history — which is exactly what I had been accumulating. Opened the design record with **six entries**, each carrying evidence and each labelled measured-first-hand or inherited.
- **The inventory already contradicts the packet's own candidate list.** Three of six modes are already mechanised; one is a discipline failure over an existing mechanism, not a missing one; **two** are live gaps in selection. So the smallest set looks like two changes, and neither is "dependency-aware dispatch waves" nor "priority lanes with aging" — two of the five candidates the packet names. F5 forecloses a third specifically: three seeds produced a byte-identical batch on 2026-08-19, so "add a better hash" is refuted by measurement rather than opinion.
- **Methodology's own prediction became an observation.** `distributed-work.yaml` says agents "can still contend for the same packet or idle when their platform's targeted set is blocked". Both halves have now happened.
- **Said plainly in the document what is missing**: the prior-art survey, mechanism proposals with closures, and entries for thrashing mediation and milestone-burndown surfacing. Absence there means unexamined, not absent — and the file uses those words, so a later reader does not mistake a gap for a finding.
- My own citation gate passed the new document on the first try, which is the first time one of this session's ratchets has been exercised by something other than its own fixture.
