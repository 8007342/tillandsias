## Cycle 2026-09-04T16:15Z-17:55Z — yolanda (windows), advance-work-from-plan

Boundary first; `linux-next` merged before the selector. Implemented
**1021-a944**, then an in-forge agent landed the same packet first — so the
cycle became an **audit backed by an independent implementation**, which is
worth more than the second fix would have been. **Closed** on `43e33dd5d` +
`4131bd5a7`.

**Pre-fix result of each criterion, before the diff:** C1 (exit-2 arm) FAILS —
none existed. C2 (verdict line + remedy) FAILS. C3 (fixture) FAILS — none
existed. C4 (can it pass in WSL) UNANSWERED. Reproduced by driving the real
script through its `TILLANDSIAS_PLAN_BIN` seam, not read off the source.

### The audit: three of four met, and C3 disproved by control

- **C1 met.** **C2 met by paraphrase** — the landed text hardcoded "no fresh
  plan binary" rather than echoing the script's own line. Correct today, because
  the exit-2 grammar has one cause; a second place to disagree the moment it
  gains another. Tightened to tee and echo the real line.
- **C3 NOT met.** The landed fixture greps `build.sh` for three strings, and a
  grep does not execute a routing. **Measured:** changing the arm to `-eq 99`
  makes it unreachable, so exit 2 falls through to "an event is attached to no
  packet" — the defect itself — while every grepped string stays in place. The
  landed fixture **passes 5/5** on that tree; mine **fails 3 arms**. A green pin
  over a broken gate.
- **C4 met, two answers that are complementary rather than rival.** esme
  measured a *transient* freshness miss; I measured a *structural* one on a
  shared Windows/WSL checkout — from inside Linux only `tillandsias-plan.exe`
  exists, `resolve_plan_binary` looks for `tillandsias-plan`, and cargo is not
  on PATH for a non-login `wsl.exe` shell, so neither resolution nor rebuild can
  fire. Proven fixed, not proposed: PATH plus a `CARGO_TARGET_DIR` **outside**
  the shared tree → `ok:fragment-events-land:70 fragment(s) checked`. The
  separate target dir is load-bearing; two toolchains sharing one `target/` is
  its own defect. Exit-7/exit-0 control run first.

The replacement fixture **extracts the branch from `build.sh` at run time and
executes it** against exit-2/exit-1/exit-0 stubs — not a copy, so editing the
branch runs the edited branch, and deleting it fails loudly rather than passing
over an empty window. Wired into the gate, because an unwired fixture would
reproduce 1003-444f, which an earlier cycle of mine documented.

**The lesson, and it indicts my own work equally: a fixture written by the same
agent that wrote the fix should be sabotage-tested by someone else.** The
in-forge fix was correct, cited the right precedents, and beat me to it. What it
could not see is the one thing hardest to see from inside your own work —
whether your test can fail. Neither reasoning nor precedent protects you from
that; only someone else running the control does. The control was available only
because a second agent happened to have an independent implementation.

**Also this cycle:** the operator authorized nothing new and is away; the yoga
scratchpad decision was delegated to macuahuitl at their instruction.

### cycle-metrics (`--cycle-start 2026-09-04T16:15:08Z`), verbatim

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent
timing: steps=9126 build_check_ms_avg=596625 build_check_mix=mixed:forced=129,memoised=15 litmus_ms_avg=3478 slowest=step:checking-that-added-fragments-declared-closures-resolve-885-92iu:490764 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-04T16:15:08Z steps=123 top3=build-preamble=4,build-check=3,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=3 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=8367 steps=124 top3=build-check:runs=113:total_ms=71746491:avg_ms=634924:fail_pct=19,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=92:total_ms=10189269:avg_ms=110752:fail_pct=0,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=92:total_ms=6291491:avg_ms=68385:fail_pct=0 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=50 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=92:avg_ms=110752:fail_pct=0:saved_ms_upper=10078517,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=92:avg_ms=68385:fail_pct=0:saved_ms_upper=6223106,step:writing-the-gate-stamp:runs=92:avg_ms=58000:fail_pct=0:saved_ms_upper=5278064 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=700 ready=454 plan_bin=./target/release/tillandsias-plan.exe
repo: commits_this_cycle=- worktree=dirty traces=current
verdict: attention:worktree-dirty
```

`worktree-dirty` is this entry; `commits_this_cycle=-` because the follow-up
landed as `4131bd5a7` before it was written.
