## Cycle 2026-08-18T11:24Z — macos (hourly meta-orchestration)

- **Guards**: all clean; mcp `down:project-info` (**thirteenth consecutive**, now
  filed as 827-rjc9); stranded `in_progress=0`. Merged `origin/linux-next`.

- **798-q4m9 criteria 1, 2 and 4 landed.** The vsock listener was spawned LAST,
  behind five background spawns. The bind was already first in *program order*
  inside its own task — which is what refuted 795-jeym — but **spawn order is
  what decides on a 1-vCPU guest**, where the single worker drains the injection
  queue in order. Moved it (and order-620-duta's preflight, which exists to print
  before the bind) ahead of the background work.
- Both blocking offenders now use `spawn_blocking`, named individually as
  criterion 2 requires. `run_disk_usage_check` was `tokio::spawn(async move
  { f() })` — **wrapping a sync fn in an async block does not make it async**, it
  parks a blocking `Command::output()` (plus first-boot asset materialization) on
  a worker. `run_trace_budget_enforcement` was an `async fn` whose only `.await`
  was the log read, with a no-yield serde_json parse loop after it; converted to
  sync and spawn_blocking'd. Deliberately **not** fixed with `yield_now()` — that
  leaves the same CPU-bound work on a worker, merely interruptible.
- **Criterion 4 stated**: this ordering affects bind latency from **daemon
  start**. It does not touch the unit's `Requires=`/`After=` fetch gate with
  `TimeoutStartSec=300s`, which on the download variant can add ~135s of retry
  plus transfer *before the binding process is launched*. A clock started at
  `systemctl enable --now` measures fetch + start + bind and must not be reported
  as bind latency.

- **Criterion 3 is NOT done, and nothing here substitutes for it.** It demands a
  measured before/after bind latency on a guest constrained to **one vCPU**, cold
  boot, and explicitly rejects a multi-vCPU pass — "the exact 'verified where it
  was written' shape 757-4hdt filed against itself". This guest is capped at
  4 vCPU (`vz.rs .min(4)`), so the measurement needs a boot-spec edit, two cold
  boots (one against the pre-change binary) and a guest-binary swap. Not
  attempted in the ~1.5h before the loop's stop rather than half-measured — the
  packet's framing applies to its closure as much as to its diagnosis.

- **Falsified both ways**: restoring the async-block wrapper fails both tests;
  moving the vsock spawn back to last fails the ordering test with the intended
  message. 442 headless tests pass.
- Per the packet's own note, the five background tasks were **not** deleted —
  they are wanted, merely mis-scheduled.
