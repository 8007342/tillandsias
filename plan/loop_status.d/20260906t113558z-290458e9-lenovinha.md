## Cycle 2026-09-06T11:35:58Z (lenovinha linux — 1115-srfr: a cycle's exit now asks what this host is still holding)

Direction: the selector returned the same four packets a FIFTH time, all
dispositioned by this host earlier today, so this cycle took the item the
previous cycle had named as its own next step. Deviation stated, not silent.

1115-srfr FILED, IMPLEMENTED AND CLOSED — landed a21093125, placement fix
fa7a9ce2b, both verified ancestors of origin/linux-next before being cited.

WHY IT EXISTED: this host stranded TWO claims in one day. 1071-adhj finished
and unreleased ~24h, found by a PEER after I had told them the packet was not
mine; 1063-nraf ~20h, found by running `expire-claims --list-live` by hand at
the end of an unrelated cycle. Both cycles did the work and skipped the release
step, and neither noticed, because nothing in a cycle's exit path asked.

WHY NOT THE EXISTING SWEEP, measured before writing anything:
check-stranded-in-progress.sh defaults to STRANDED_HOURS=8 and carries NO host
filter (zero occurrences of `hostname`). It is a FLEET sweep for a COORDINATOR
and its advisory-never-a-gate discipline is right for that question, because a
packet legitimately in flight is indistinguishable from one abandoned an hour
ago. The exit-time question needs no judgement — claims are cycle-scoped, so
one held by THIS host at ITS OWN finalization is unreleased by definition — and
the aging is what makes the sweep unable to serve as the fix: both instances
would have surfaced eight hours later on someone else's screen. The guard is
also never invoked by a cycle: build.sh gates the FIXTURE while the guard's
only caller is reclaim-stranded-claims.sh, which the worker skill records as
having no caller outside its own source. 1009-gccx one level up, and why this
landed as a call site and not only a script.

DEMONSTRATED ON ITS OWN LIFECYCLE, which is the evidence I would want as a
reader: run while 1115-srfr was still claimed it printed
`warn:cycle-claims-held:1 by lenovinha` naming the packet and its release
command; run after closing it, `ok:cycle-claims-released:0`. Zero prints an
AFFIRMATIVE line rather than silence, because silence is indistinguishable from
the check not running.

THE CHECK FOUND ITS OWN DEFECT ON ITS FIRST REAL RUN, BY NOT APPEARING. I had
placed it just above "derive the terminal marker", at the end of the sequence.
finalize then refused earlier at `record` — a 1110-4v4h false negative — and
the report never executed. I noticed only because I grepped the finalize log
for it and got nothing, which is the same "silence is indistinguishable from
not running" property the script is written to avoid, showing up one level up
in where I had put the call. A cycle that refuses is still a cycle that is
ENDING, and it is the one most likely to strand a claim, because its author is
now debugging a refusal instead of finishing an exit. Moved above every
refusable step, with that reasoning in the code.

1110-4v4h FIRED A SECOND TIME, one cycle after I filed it: `MO-FULL: FAIL local
HEAD 1632d57cf is not durably on origin/linux-next (observed 37fe21840)`, while
1632d57cf verifies as an ancestor of origin and 37fe21840 is a later commit
from another host descending from it. Two instances in two consecutive cycles
is the frequency estimate that packet lacked — it is not a rare race, it fires
whenever any host pushes inside the 120s window. Recorded there, with the
second-order cost I had not anticipated when filing it: a spurious refusal
early in a sequence silently removes everything after it, and the things after
it are the exit contract.

ADVISORY BY RECORDED DECISION, not by omission. The report runs on every host
and turning it into a refusal is a fleet call, so criterion 3 states the
escalation condition instead: if a host strands a claim WITH the report in
place, that is the evidence, and the refusal should name the release command,
since releasing at that point is always correct and costs seconds.

Fixture 5/5 running INSIDE a green gate, wired with a LITERAL path so the
bound-or-retired scan can find it (1063-nraf's own lesson). Every arm observed
RED against the failure it names before being kept: silencing the affirmative
line reds arm 1, dropping the host filter reds arm 1, silencing the no-binary
skip reds arm 3.

Read path: MCP-first for orientation; every filesystem read was VERIFICATION
class — check-stranded-in-progress.sh's aging and host-blindness, the shared
probe's override contract, and build.sh's fixture-wiring pattern were each read
before any claim was made about them.
