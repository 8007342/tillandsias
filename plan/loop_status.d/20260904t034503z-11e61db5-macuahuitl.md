## Cycle — macuahuitl 2026-09-04T03:40Z (coordinate-multihost-work, cron 23-odd)

- **Merges.** `osx-next` 4 ahead, merged. `windows-next` 0 ahead / 7 behind,
  levelled. Both siblings now level.
- **Claim board completely clear again: 0/0/0/0.** macbook released 795-bmbq on
  cycle exit rather than holding it.
- **AN AUTHOR DECISION MADE AS COORDINATOR, and I took the disposition from the
  methodology rather than inventing one.** macbook falsified 795-bmbq's premise
  and asked for an author call; the filing host (windows, from the 2026-08-17
  architecture audit) is idle awaiting its own operator, and this is a
  disposition rather than new scope.
- **The premise was FALSE WHEN THE PACKET WAS FILED.**
  `build_opencode_forge_args` pushes `--interactive --tty` under
  `if !diagnostics && prompt.is_none()`, so the flags are present for a genuinely
  interactive launch — and macbook **extracted main.rs as of the audit date** and
  found the conditional there verbatim. Nothing was ever amputated.
- **Closed `obsoleted`, NOT `done`, and that distinction is the ruling.** `done`
  would assert a defect was found and fixed. None existed, so it would put a
  fictional fix in the record and make the 2026-08-17 audit look correct.
  Disposition per `philosophy.obsolete_mechanism_live_intent.dispositions`:
  **mechanism live, intent dead → retire deliberately, check dependents first.**
  Checked — nothing depends on it, and its own dependency reads completed, which
  is why the measurement was legitimate rather than a measurement of the old
  system. macbook checked that first too.
- **What survives is the real yield.** Criterion 3 had genuine content: the
  source comment asserted an UNMEASURED causal claim — that forcing
  `--interactive --tty` under a non-TTY shell makes podman REFUSE. Measured:
  `podman run --rm -it alpine true < /dev/null` **warns and proceeds**, container
  ran, exit 0. Both comments now carry the measurement with its bound. A packet
  that finds no defect and converts an unmeasured causal assertion into a
  measured one has earned its cycle.
- **Their stated limit is recorded rather than glossed:** they could not drive an
  interactive TUI end to end from a non-interactive session, which criterion 2's
  first branch asked for. Moot — there was nothing to restore — but recorded.
- **Roll call.** macbook 03:35Z and yoga 03:22Z active within the half hour.
  lenovinha 3.5h after landing 995-srbf. yolanda 7.4h, **idle by choice and by
  their own statement**, awaiting their own operator on new scope. macneo 18.5h;
  an operator-queue question about the host, not another ping. pirria, esme and
  both forges finished or blocked by their own statement.
- **No dispatch.** Two hosts active, one recently landed, one waiting on its own
  operator, one unreachable, three finished. Nothing idle that is unaccounted
  for.
- **Litmus:** not run. `--check` green on the merged union.
- **Operator queue, five plain asks, unchanged.**
- **Nothing is blocked.**
