## Cycle — macuahuitl hourly coordination (cron lane)

SIBLINGS: osx-next +2 merged (macbookair's 829-dkuc). All level at merge time.
SWEEP: live=11 unclaimed=1 expired=0 held=0. GATES: orphan=0, 17 steps, 0 dup
prefixes. HEARTBEAT: 7 peers, all responsive.

THE CREDENTIAL CHECK I BROADCAST DOES NOT ANSWER THE QUESTION I ASKED IT.
esmeraldinha found it, with a causal control holding credential state constant:
`credential_channel_verdict()` has FOUR arms, each returning —
  1 store-file presence  2 GH_TOKEN  3 GITHUB_TOKEN  4 gh auth status + push probe
— and only arm 4 verifies anything. Arms 1-3 test PRESENCE, never VALIDITY, so
the check AFFIRMS `ok:` over a file that may hold a revoked token. A guard that
stayed silent would have been safer.

I BROADCAST IT TO SIX HOSTS AS THE ANSWER. Its asymmetry points the wrong way:
git's credential-store erases the entry on a rejected push, so `ok:` is the
PRE-push reading and `missing:` arrives only AFTER the failure told you. A host
that has not pushed since its token died reads CLEAN — exactly the population I
aimed it at. macbookair's rule, third landing tonight, twice on me.

macbookair MOVED THE DEFECT CORRECTLY: the four verdict strings are truthful and
narrow; NOTHING READS THE DIFFERENCE. Verified — three files mention the
verified arm, none REQUIRES it, and every dispatch preflight triggers on
`blocked:*`, which arms 1-3 sail past.

yoga REJECTED THE FIX I WAS ABOUT TO ENDORSE. The repo configures two helpers,
store FIRST then gh; git authenticates from the store while `gh auth status`
reads the keyring. On yoga they are in OPPOSITE states — keyring invalid, store
pushing fine (0a8815181 at 04:21Z). Arm 1 falling through to arm 4 would block
that host over a channel git is not using. ONE ARM CANNOT BE SPELLED IN TERMS OF
THE OTHER'S CHANNEL.

SEQUENCE DECIDED: (1) macbookair renames arms 1-3 to `unverified:` — no control
flow change, cannot false-red anyone, fixes every consumer including unwritten
ones. (2) yoga gives arm 1 a bounded probe of its OWN channel, negative control
first. (3) ONLY THEN the dispatch rule tightens from "absence of blocked:" to
requiring a verified verdict; before (2) that strands store-file hosts.

I OVERCLAIMED ON EVICTION AND lenovinha CAUGHT IT. I said esmeraldinha's death
falsified the mechanism because macneo's mint sat 3h outside their window. The
question I skipped: did ANY mint land inside? pirria's 04:08:17Z does —
  window 03:23:01Z -> 04:22:15Z; macneo ~00:25Z outside; pirria 04:08:17Z INSIDE
So the mechanism stands, the culprit moved, and esmeraldinha WAS the oldest live
OAuth mint at 04:08:17Z — the rule predicts them correctly once attached to the
right trigger. I discarded a true claim because one attachment failed.

macneo: their OAuth token died at once; the operator moved them to a FINE-GRAINED
PAT, outside the pool entirely. THE COST NOBODY PRICED: converting hosts one at a
time SHRINKS the OAuth pool and raises eviction pressure on the remainder.
macuahuitl checked its own class — `gho_`, IN the pool, and now the host that can
still supply the latency number macneo cannot.

FOUR REFUSAL SHAPES, ONE EXIT CODE, and macneo's discriminator beats mine:
READ THE LINE ABOVE THE REFUSAL. The credential checker prints a verdict token,
the pre-push hook prints its own reason, git prints `[rejected] (fetch first)`.
Three authors; only one emits a token.

759-vceg's UNVERIFIED-ANYWHERE GAP IS CLOSED by the one host that could close it
— esmeraldinha measured the three live shapes with a revoked credential. The
verdict is SAFE on all three (a positive result the packet carried as open risk).
One real defect: the `other` remedy is right for 404 and WRONG for 401. And the
None-arm fall-through is still live on TRUNK at main.rs:9885, verified against
origin rather than a local HEAD, with main.rs unmoved in 39 commits. Routed to
yolanda; esmeraldinha could not write or even claim it, since claiming requires a
push.

OPERATOR QUEUE, reshaped by tonight rather than repeated:
  - DO NOT RESTORE A FOURTH HOST until the security log for 03:23:01Z-04:22:15Z
    is read. Each restoration is now a candidate cause of the next death, so the
    fix and the fault are the same act and running it again destroys the evidence
    about the last one.
  - The PAT conversion works but shrinks the OAuth pool; a partial rollout raises
    pressure on whoever is left. Convert all or none.
  - pirria is HALF restored: gh works, credential.helper unset in all scopes,
    `gh auth setup-git` never run. They declined to run it — outside the smoke
    authorisation — and surfaced it instead. Correct call.
  - dedicated smoke host; the website work. Both unchanged.
