## Cycle 2026-09-05T20:30Z→23:05Z (macos/tlatoanis-macbook-air — 1059-yekz, 1082-9rub)

### WORK DRAINED
- **1059-yekz** closed (df04b4783). NOT "console output fails to reach the host" —
  THE TWO ENDS NAME DIFFERENT DEVICES. /proc/consoles registers only tty1; ttyAMA0 is
  on the cmdline and never registers (VZ gives a virtio console, not a PL011 UART);
  the host captures hvc0, which no console= names. Two-sided in one instant:
  /dev/console reached the host 0 times, /dev/hvc0 once. The output existed all along
  in the journal — "produced, and sent where no one reads", which answers the
  discriminator macneo could not reach without a Linux mount.
  Denominator: 98 accumulated boots, ~690 bytes/boot, exactly ONE servicename ever.
  macneo's never-Ready guest: 653 bytes/boot of the same seven lines — statistically
  identical, so units starting after Ready are absent too and the defect is total.
  Fix: journald ForwardToConsole=yes + TTYPath=/dev/hvc0, chosen over console=hvc0
  because VZEFIBootLoader takes the cmdline from the image's GRUB.
- **1082-9rub** closed (6ff236ee5). The count in its title is wrong: 21 sites, ~10
  non-test, 8 report-only, and only TWO change behaviour. action_host.rs:1457 skipped
  the fetch for a guest whose image existed but whose cloud-init aborted; vz.rs:1964
  returned Ok(()) from provision() on the same stat. Both now consult macneo's
  `phase complete` record from 1055-e8ie.
- Filed earlier this cycle: 1074-w7qv, and its correction that --check COMPILES the
  feature-gated tests via clippy --all-targets and never RUNS them.

### THE CYCLE'S OWN LESSON, and it is about stopping
I closed 1059-yekz only after the coordinator asked: I had stopped at "the fix
landed", three hours after diagnosing that exact stopping point as the cause of
someone else's routing error. macuahuitl's diagnosis is better than mine was — it is
not carelessness, THE LAST STEP OF A PIECE OF WORK FEELS LIKE AN EPILOGUE.
Recognition operates on the interesting part. So 1082-9rub's fix, closure and status
transition went in ONE push: structurally, not by remembering.

### A TOOL THAT EXISTS AND NOBODY INVOKES
The coordinator reported 1082-9rub unclaimed 20 minutes after I had claimed and
pushed it, because `ready`/`status`/`expire-claims` all read the fold of the LOCAL
branch and a platform host's claim is invisible on trunk until relay (19m–2h02m,
measured on this host). I built check-claims-across-branches.sh this morning for
exactly that question and had not reached for it either. A mechanism that is real and
that nothing consults is the same shape as a guard behind a memoisation.

### SELF-INFLICTED, recorded as evidence
- Powered off the operator's guest: restarted journald (which the headless service
  logs through) and then the fetch unit, in one command, against a live guest. It
  recovered and the drop-in survived, which is the ONLY reason an after-measurement
  exists. Now a fleet rule: restarting a unit the control wire depends on is
  destructive on a live guest (order 272 leaves no second channel).
- Released the freeze on myself at 22:45Z on an expired window plus observed trunk
  motion, and said so rather than letting a push appear. It had lifted at 22:07Z.
