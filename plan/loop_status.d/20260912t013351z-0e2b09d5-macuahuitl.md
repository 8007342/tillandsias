## Cycle 2026-09-12T01:33Z addendum 2 — the promotion is HELD: the candidate's control wire does not work on Windows or macOS

v56.9.12.1's run 34668648875 concluded success on all three jobs (the Windows tray
shipped; 1122-xi2f complete on yolanda's evidence). Smoke: macuahuitl Linux
PASS §1-§4c in 13 min including a completed forge lane; macbookair macOS PASS on
the install/provision axis with 1084-x8ya reproduced (guest ready at 36 s, host
never observes it); esme Windows FAIL at cold provision — "secure handshake
failed: noise: input error", the same wire, deterministic; lenovinha Linux
§1-§3 PASS, §4 detached. So the published artefacts install and re-provision on
three platforms, and the host-to-guest control wire is broken on the two VM
platforms. That is not a stable. Decision: HOLD; current stable stays
v56.9.2.1. Hypothesis and its falsification are on 1084-x8ya (972-umik's
encrypted-by-default wire, e6a80609f, in v56.9.5.1 and not v56.9.4.1; knob
TILLANDSIAS_SECURE_CONTROL_WIRE=off); esme, yolanda and macneo run it.

**Wiring-time regressions from lenovinha's 1087-h2z9 land (16 steps into
--check), found by three regimes within the hour:** a chmod-000 arm that root
cannot fail (Windows gate runs as root; yolanda's euid-0 skip), a false
content-failure verdict when rg is absent (esme), and BSD grep -R not
descending symlinked skill dirs so five wired guards read as orphans on macOS
(macbookair's find -L fix). "Measured green" was a property of one regime.

**tokens (addendum 2):** subagent_tokens=0 agents=0; the whole coordinator
cycle ran without delegation.
