## Cycle — yoga 2026-09-03T13:09Z (meta-orchestration, full mode; cron lane)
- Start of cycle: guards ok, daily gate current, lock acquired, boundary clean at 0fee4a82d. Claim landed via land-on-platform-branch.sh (attempt 1).
- RUNG 4 (977-3dee) IMPLEMENTED and THE COVERAGE IS 2.6% — scored=16 unscored=584 total=600 ceiling=traced unresolvable-closures=4. The coordinator's ruling predicted sparse and accepted it in advance; this is how sparse.
- WHAT CAN HONESTLY BE PINNED IS LESS THAN THE PACKET ASSUMED. 125 rows carry evidence_refs, but those are FREE TEXT and reading a state out of prose is exactly the inference the ruling forbids — only ~10 carry a machine-shaped token at all. The one structured source is verifiable_closure: 112 rows have one, and 25 name a litmus:<test> over 22 distinct tests.
- AND NAMING A TEST IS NOT PASSING IT — the distinction that decided the rung, one layer below the error the ruling already rejected. A closure naming a RESOLVABLE litmus test establishes that a verification path exists and resolves: that is `traced`. It does NOT establish positively_tested, because the ledger carries the closure's NAME and not the test's RESULT. Crediting a declared closure as a passing one would score the DECLARATION while presenting it as evidence. The ceiling is therefore stated PER ROW, so a later source that can genuinely pin positively_tested does not silently inherit this one's meaning.
- THE 4 UNRESOLVABLE CLOSURES ARE NOT SCORED: rows naming a litmus test that no test declares. Crediting them would credit a verification path that cannot run. Reported separately in the verdict so they read as a defect rather than as absence.
- UNSCORED IS ABSENCE, NOT ZERO. The emitted fragment contains only the 16 scored rows; the other 584 are deliberately absent rather than present with a zero, because a defaulted zero is indistinguishable from a measured nothing — the objection this rung answers. Each scored row carries derivation= and ceiling= IN THE DATA, so a consumer reading only the data can tell a backfilled value from a measured one without reading the packet, which is why the name-the-field-approximate option was rejected.
- CRDT-STYLE as the operator required: one NEW fragment keyed by packet_id, never a read-modify-write. Two concurrent backfills write two fragments and the fold takes both; no lock is needed for correctness. VERIFIED: 16 events emitted, fragment-event-packets resolves all 16 with none dropped, check ok at 635 packets, gate green.
- THE COVERAGE FIGURE IS THE RESULT, NOT A SHORTFALL. 2.6% says the ledger can currently evidence only the tracing rung retroactively, for one row in forty. A number that starts there and rises is a measurement; a dense inferred one would have started at something nobody could audit and never improved, because there would be nothing to improve toward.
- SCOPE NOT CLAIMED, both named in next_action as DIFFERENT derivations that must be tagged as such: running the 22 named litmus tests would raise the ceiling to positively_tested for those that pass, and rung 3's scorer does not yet consume these backfilled states.
