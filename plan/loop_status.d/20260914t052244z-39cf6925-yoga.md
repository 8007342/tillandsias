## Cycle 2026-09-14T05:22Z — yoga (linux-next)

1173-a5ng, assigned and claimed by macuahuitl; landed at be5a3275e and closed with that SHA. Implemented as filed — the checker consults trunk before calling a deleted ref an outstanding rescue, and the sweep states the deletion rule.

- THE MARKER, NOT THE LINE. The fallback requires a trunk line naming the ref AND ending in ' deleted'. Matching the ref alone would forgive every outstanding rescue silently; an arm reds on exactly that mutation. That is the row's negative control, made falsifiable.
- THREE STATES FROM `git show`, and the third bites: on a missing ref it exits non-zero and writes ZERO BYTES, indistinguishable from "read it, no marker" if the rc is discarded. I have made that exact mistake on this repo before and reported a fabricated hazard from it. The helper returns a distinct code and the caller REFUSES, saying the behind-tree case could not be RULED OUT.
- The deletion rule is stated where the delete happens AND stated as what it is: a REDUCTION, not a guarantee. A floor host's gate runs an hour, so no waiting period closes the race; the rule shrinks the window the fallback covers.
- TWO FIXTURE BUGS CAUGHT BY THE FIXTURE, either of which would have made every arm meaningless: (1) the ledger line had the wrong FIELD ORDER, written from memory instead of from the checker's header — it said "malformed sha" about a field holding a ref; (2) the first version created NO remote and assumed that made the ref absent, but it made ls-remote FAIL, so the checker took its origin-unreachable SKIP and the reachability half — the entire subject — never ran. An unreachable remote and an empty one are different facts: this packet's own distinction, one layer down.
- Three mutations, each diff proven non-empty first, all red by name. A fourth failed to apply through a quoting error and printed 7/7 from an UNMUTATED file — recorded because it is the false result this discipline exists to catch, appearing in the same cycle I wrote the rule down.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
