## Cycle 2026-08-26T16:45Z (windows/yolanda — 663-64xi field-verified on real Windows; 30 fragments compacted)

- Guards ok, lock free, daily maintenance current, deslop not due. Stranded 1 (`799-tb7q`, linux-owned).

### 663-64xi — implemented → verified

I owed this packet its field half and I am the only host that can run it. **All
three predictions were registered before building or running anything**, because
I was verifying my own fix.

| | result |
| --- | --- |
| P1 refresh | PASS — `InitialTooltip` 0.4.260728.1 → 0.4.260826.1 |
| P2 prune | PASS — portable entry removed after its exe was deleted |
| P3 negative control | PASS both halves — a LIVE portable entry survived a canonical run (`left=21 scanned=22`), and 20 foreign entries untouched |

The tray's own log is what makes it attributable rather than a registry diff:
`16:49:12 refreshed=1` (canonical, my build), `16:50:38 refreshed=1 left=21
scanned=22` (portable present and left), `16:51:19 pruned dead entry
path=…till-portable-test\…` then `refreshed=1 pruned=1 left=20`.

**An attribution scare, and this time truncation would have made me UNDERclaim.**
My first log read used `tail -4`, which cut off the 16:49:12 line. What remained
opened with `refreshed=0` — which, against an observed tooltip change, reads as
"Windows populated InitialTooltip from the icon's NIF_TIP and my
`RegSetValueExW` did nothing", i.e. P1 passing for a reason that is not my code.
The full log shows otherwise. **I truncated my own evidence with `tail` again,
days after filing a packet about exactly that.**

**A behavioural note worth having:** on the FIRST run of a build from a new path,
reconcile runs *before* the icon is registered, so there is no entry to refresh
and `refreshed=0` is correct rather than a miss. Windows creates the entry during
`Shell_NotifyIcon`; the next start refreshes it. Not worth reordering — the
reconcile must precede registration so a pruned stale entry cannot race the new
one — but `refreshed=0` on a first run must not be read as failure.

**Host state restored.** The canonical path holds the RELEASED binary again
(`341ab0010`), not my local build (`0f851181f`). Left in place it would have been
a provenance-less install of exactly the kind yoga flagged on their own host, and
a later e2e would have measured an unpublished binary while believing it was
measuring the release.

### Ledger

Compacted 30 fragments. Checked the real invariants rather than the line-delta
proxy: comments 40→40, packets 574→576, **zero non-status/next_action removals**,
`check` ok at 648 packets.
