## Cycle 2026-09-05T18:45Z — macuahuitl-fedora coordination (hourly :41 lane)

LastExecutionTime: 2026-09-05T18:45Z UTC. Scheduler posture: hourly coordinate
:41 and 4h meta :09 armed on this host; no cron changes made, none requested.

LANDED THIS PASS. pirria's 1026-ps4n relay (a079083ba, applied under pirria's
authorship after a two-part hash-verified paste; whole-file sha256 3cee8a20…
matched on the first assembly, 22010 bytes, 410 lines) plus its ledger record,
landed as ba8ba36ec on attempt 3 of the landing tool — origin moved under
attempts 1 and 2, which is the tool behaving correctly. Status set to
`implemented`, NOT completed: criterion 3's end-to-end observation on a floor
host's next real smoke is owed and needs the operator authorisation queued under
1004-pg9p. The owed observation is recorded as an event so the next floor smoke
knows to look for it, which is what pirria asked for.

SIBLING MERGES. origin/osx-next (3 ahead) and origin/windows-next (4 ahead) both
merged into linux-next with zero conflicts. osx-next carried macbookair's
1069-5sp4 second half — the scorable-obligation guard now runs on the MAIN push
path, not only in the plan-only lane. That is the corollary I routed to them and
they measured the cause correctly where my first diagnosis was wrong: a valid
gate stamp cannot vouch for plan fragments because gate-stamp.sh:343 excludes
plan/index.d/*.yaml by design (930-i6x4), so a plan-only change never re-gates
and the lane is never consulted. Their own next plan-only push bypassed their
first fix and they caught it.

CROSS-HOST RECURRENCE AUDIT (order 1001-q3zf) — 19 distinct stems, 19 rows, no
host dropped. Four hosts agreed on the same top-2 skippable steps, the strongest
signal that rule has produced:

  checking-the-plan-archiver-preserves-the-ready-set-831-ezea
    macuahuitl  runs=347 avg_ms=15342 fail_pct=0 saved_ms_upper=5308353
    lenovinha   runs=230 avg_ms=24841 fail_pct=0 saved_ms_upper=5688602
    yoga        runs=150 avg_ms=17048 fail_pct=0 saved_ms_upper=2540281
    macbookair  runs=122 avg_ms=18204 fail_pct=0 saved_ms_upper=2202796

  checking-user-visible-terminology-against-the-dictionary-629-t6b
    macuahuitl  runs=347 avg_ms=13240 fail_pct=0 saved_ms_upper=4581129
    lenovinha   runs=230 avg_ms=33414 fail_pct=0 saved_ms_upper=7651905
    yoga        runs=150 avg_ms=14275 fail_pct=0 saved_ms_upper=2127016
    macbookair  runs=122 avg_ms=29803 fail_pct=0 saved_ms_upper=3606197

Every saved_ms_upper above is an UPPER BOUND — total_ms minus one run, the most
a perfect skip could have saved — because the timing log carries no input
identity and cannot claim a run would have hit a cache.

AND THE PACKET THE RULE TOLD ME TO FILE WAS ALREADY DONE. I read build.sh before
filing and both steps are already memoised: the archiver behind
scripts/archiver-check-memo.sh (build.sh:2637, landed 09-04) and the terminology
check behind scripts/terminology-check-memo.sh (build.sh:3021), whose own comment
already cites THIS audit and carries four hosts' numbers including pirria's
25.4s. Filed 1074-96z9 about the instrument instead: a memo HIT emits no timing
record for the archiver, so its hit rate is not computable and "did the memo
help" has no answer in the log — which is exactly the bound the terminology memo
eliminated for itself nine hours later. Two further defects in the same line:
`skippable:` is windowed to 7 days, so both fixed steps will be re-nominated by
the next six audits as their pre-memo runs age out, and the line does not print
its window token while `recur:` does, so a reader comparing runs= across hosts
cannot tell 7-day counts from all-time counts. I could not; I read the source.

UNATTRIBUTED BUCKETS (order 1012-hu7d), unchanged and still growing: linux=56,
windows=36, macos=18, linux-immutable=4, forge=2, linux-mutable=1. 117 entries
that name a platform rather than a host. Report by count, never read one as a
host, and never read a host as silent because its entries sit in a bucket.

NOT PASTING cycle-metrics in their newest entry: macuahuitl (me — corrected
below), pirria, yolanda, calmecacpilli, and both forge stems. esmeraldinha's
entry has the numbers embedded in prose rather than pasted verbatim, so the
token-anchored grep reads a sentence fragment instead of a step name.

STRANDED-CLAIM SWEEP (--dry-run --list-live, read-only): live=2 unclaimed=1
expired=0 held=0 unknown_age=1. Live: 317 (macbookair), 1063-nraf (lenovinha).
Unclaimed-in-progress: 1025-a896, operator-blocked, its unattributable row now
explained inline by yoga's 1065-4t7t fix. Unknown-age: 1064-r8fv, whose artifact
IS on trunk and gate-bound at build.sh:3301 — routed to yolanda for the terminal
event rather than closed from here, because a fixture passing on my host says
nothing about the two sabotage arms she wrote.

ASSIGNMENT BOARD.
  yoga (silverblue)      -> 1070-a4gc, the SIGPIPE-verdict guard's two blind
                            spots. Idle before this, reassigned this pass.
  pirria (cachyos)       -> 1074-96z9, memo-hit observability. Idle after the
                            1026-ps4n relay, reassigned this pass.
  yolanda (windows)      -> 1064-r8fv terminal event; seven jq sites continuing.
  esme (windows)         -> 1072-b7eq, claimed.
  lenovinha (silverblue) -> 1063-nraf continuing, 13 orphans still to dispose of
                            before the guard may land.
  macbookair (macos)     -> 317's install half, BLOCKED on a forge launch with
                            proxy and vault, which needs credentials they
                            correctly refused to mint. Operator ask is queued.
  macneo (macos)         -> 980-ja2m landed by relay; next assignment owed.
Fallbacks: every assigned host has its own packet plus the ready set; no host is
waiting on another host's artifact this pass.

CONVERGENCE. Vc positive this pass: two packets moved off ready (1070-a4gc,
1074-96z9 assigned), one to implemented (1026-ps4n), sibling drift returned to
zero on both platform branches. High-Velocity Alignment Event: INACTIVE.

EXPECTED NEXT LOOP. yolanda's terminal event on 1064-r8fv; yoga's corpus count
for 1070-a4gc, which decides whether the diff-scope blind spot is worth widening;
pirria's pre-fix reds on 1074-96z9; macbookair still blocked until the operator
authorises the forge launch.

CYCLE METRICS, pasted verbatim (I was one of the hosts not pasting):

```
experts: calls=8 answered=8 unsupported=0 degraded=0 errors=0 answer_rate=100% tools=check,ready source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=1 per_server=cli=8 legacy_untagged=0 health=ok source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
flow: cycles=17 avg_completed_per_cycle=0.76 avg_commits_per_cycle=5.47 overhead_ratio=7.15 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
timing: steps=68702 build_check_ms_avg=88009 build_check_mix=mixed:forced=576,memoised=191 litmus_ms_avg=40622 slowest=check:litmus-pre-build:1967661 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=3h steps=446 top3=litmus-suite=32,build-preamble=11,build-check=9 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=60613 steps=591 top3=build-check:runs=482:total_ms=45574766:avg_ms=94553:fail_pct=28,litmus-suite:runs=662:total_ms=30445944:avg_ms=45990:fail_pct=60,check:litmus-pre-build:runs=29:total_ms=23778529:avg_ms=819949:fail_pct=45 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=79 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=347:avg_ms=15342:fail_pct=0:saved_ms_upper=5308353,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=347:avg_ms=13240:fail_pct=0:saved_ms_upper=4581129,step:running-workspace-tests-cargo-test-workspace-all-targets:runs=54:avg_ms=77677:fail_pct=0:saved_ms_upper=4116934 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=772 ready=470 plan_bin=./target/release/tillandsias-plan
repo: commits_this_cycle=- worktree=dirty traces=current
verdict: attention:worktree-dirty
```
