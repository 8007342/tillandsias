## Cycle 2026-08-26T14:09Z (macos tlatoanis-macbook-air — the smoke skill validated a release without ever reading what the release claimed)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 38. Boundary first, gates green.
- **905-97xh and 906-qi89 were both fixed upstream while I was away** — `check-build-cache-sweep.sh` now stamps UTC (6 sites) and initialises on `no-marker` under threshold, with a negative control pinning that "initialise" did not become "always initialise". My pre-fix local-time marker is still readable, which was that packet's second exit criterion.
- **Took 380 and delivered criterion 2**, the half this host was best placed to write: I ran the smoke-curl skill twice on 2026-08-26 and **never once consulted the release ledger**.
- **§0 gains step 2b**: read the README row for `$SMOKE_TAG` before testing. Verified both directions — a real tag returns its row, a nonexistent tag returns `NO LEDGER ROW for <tag>`. **A missing row is a FINDING, not a skip**: it means either the release skill's append never ran, or the smoke is testing an artifact nobody described. Both are invisible if the step proceeds quietly.
- **§5 gains a required `## Ledger claims` section** sorting each claim into **EXERCISED / NOT APPLICABLE / NOT CHECKED**. The third heading is the one that earns the section: a report with no NOT-CHECKED list reads as though the run covered everything the release claimed, and a reader cannot tell that from a run that never looked.
- **THE MOTIVATING OBSERVATION IS MY OWN RUN.** The v0.4.260826.1 row claims 635-bhkb — "the macOS tray finally reports the workspace VERSION instead of `0.1.0`". My macOS leg **did** verify exactly that — **by coincidence.** I had changed the runbook hours earlier to assert the exact tag, for unrelated reasons. Nothing directed the run at the release's own claim, and without that accident the same PASS would have been reported having checked none of it.
- **Assessed the other two criteria against the tree instead of assuming.** Criterion 1: the append hook exists and has run once (the v0.4.260826.1 row is the most thorough in the table) — one release of the two it asks for, so it stays open on evidence, not on missing code.
- **Criterion 3 is documented and NOT APPLIED, so I filed it rather than counting it — 914-nkc4.** The distillation policy is written in two places with its destination line already at README.md:111, and **the table has 19 rows against a "~10 row" trigger.** Nothing runs it; it is a sentence someone is expected to notice while doing something else, at nearly double its own threshold. That is the 801-qasc / 709-in2f / 660-ryhn shape in a mild form — a rule that reads as active because it is written down. **The rows are unusually good** (the v0.4.260826.1 row states what the release does NOT claim, per platform), and that is part of why nobody wants to distil them; that tension is the real decision the new packet asks someone to make.
- **380 released back to `ready`** on criteria 1 and 3 — one release of two, one clause of two. Not closed on the half that was done.
