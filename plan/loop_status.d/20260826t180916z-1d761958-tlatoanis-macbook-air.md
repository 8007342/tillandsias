## Cycle 2026-08-26T18:09Z (macos tlatoanis-macbook-air — stopped taking the selector's word for it)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 40. Boundary first, gates green.
- **SEVENTH cycle on the same six-item batch**, of which three are packets whose macOS half I completed and released myself. Queried `pickup_role: macos` directly instead and found **two p1 packets the selector was not offering** — 635-kagg and 690-pz68. A `pickup_role: macos` packet is mine by definition and cannot collide with another host, so taking one is not stepping on the separation the selector exists to provide.
- **Took 635-kagg (p1)** and attacked the half that is tractable in a cycle. Its primary finding is a `--bash` forge lane that wedges between vault readiness and podman-run, with observed wedges of 45m–3h; reproducing that is a deliberate long experiment, not something to start inside a routine cycle. Its **secondary** finding is exec-wire stdin semantics — the path I changed in 795-zshi.
- **Converted that secondary from a symptom into a mechanism, read out of the tree.** The packet observed *"a piped `cat > file` blocks forever; byte-exact `head -c N` works"*. **The wire has no way to say stdin is finished**: host→guest stdin is `PtyData{ToGuest}` and nothing else — no `PtyEof`, no `CloseStdin` variant exists — and `exec_over_stream_with_input*` writes its chunks then goes straight to draining, sending no terminator and closing nothing. The child sits on a PTY whose master never closes. **That predicts both halves exactly**: `head -c N` stops after N bytes and never needs EOF; `cat` waits for one that cannot arrive.
- **Recorded two candidate fixes with the tradeoff each carries**, because they are not equivalent: in-band VEOF (no wire change, works today — but only in canonical mode, and corrupts binary stdin containing 0x04, so it must be opt-in and callers who forget keep the bug); or an explicit wire message (correct in both modes — but a wire change needing the same feature-detect-on-HelloAck handling as CAP_EXEC_ARGV_VECTOR and CloudRefreshOutcome). That is a design decision, not a patch, so I did not pick one.
- **AND FLAGGED THAT THE TWO FINDINGS ARE PROBABLY INDEPENDENT** — the thing most likely to mislead the next reader. The packet records that a setsid-detached variant with **`stdin </dev/null`** produced zero output and no container. Stdin was already closed host-side there, so the missing-EOF mechanism cannot explain that reproduction. **If the wedge were an stdin-EOF artifact, `</dev/null` should have been the case that worked; it is the case that produced nothing at all.** Treating the secondary as an explanation for the primary would send someone down the wrong path.
- **Released 635-kagg back to `ready`** — the primary is untouched and is what the p1 is for.
- **Gave 884-hfsj a second host's evidence.** yoga had five cycles on one host; this host has had seven, and the reason is visible in the packets' own text rather than inferred: *"needs Windows-lane evidence"*, *"macOS: **PROVEN**"*, *"needs a forge host"*. **The routing information already exists — written by the hosts that did the first half, in prose the selector cannot read.** Including prose I wrote myself and still could not route with.
- **The concrete cost, which is the answer to "the host does not idle":** every cycle's real work came from **outside** the batch — 629-t6bx and 380 by reading the epic directly, 635-kagg and 793-qr4t by querying the macOS role. The selector's output was not merely unhelpful, it was consistently a detour, and the routing that worked was ignoring it.
- **Did not propose the remedy** — yoga already refuted `owner_host` on measurement and declined to decide fleet-wide routing from one host's experience; a second host's experience does not make it mine either. Offered one candidate to narrow it: in all three cases the remaining lane was already stated in a **closing event written at release time**, which is exactly when a host knows what is left and who needs it. A structured field there costs the releasing host nothing it is not already typing.
