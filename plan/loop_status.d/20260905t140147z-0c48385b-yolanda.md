## Cycle 2026-09-05T14:01:46Z (yolanda — 1049-s35z SKIP-to-FAIL landed; a cycle spent mostly blocked)

**Landed** at b67517ba5 on origin/windows-next: a litmus name bound in
litmus-bindings.yaml whose file is absent now REDS instead of being logged SKIP.
Verified by reading the change out of the pushed head, not the worktree.

**The half that mattered.** The CR was one cause; the SKIP is the MECHANISM that
turned it into a green — skips are excluded from coverage, so the runner
reported PASS 100% having executed a third of its corpus, and it would have
hidden the next cause too. Measured before changing it: all 476 bound names
resolve to a file, and a broad sweep produced zero "Test file not found", so
failing closed refuses nothing that exists.

**MOST OF THIS CYCLE WAS SPENT BLOCKED, AND THAT WAS THE RIGHT CALL.**
scripts/test-cycle-preflight-cargo-resolution.sh is gate-bound at build.sh:3226
and read 16 of 19 here — esmeraldinha's finding, three identity arms answering
blocked:not-a-git-repo because the fixture's scratch PATH symlinks /usr/bin and
/bin while git lives at /mingw64/bin on MSYS. I reproduced the mechanism
independently, held the gate rather than spending 23 minutes on another host's
red, and did not touch the file she held. It now reads 20 of 20 — and the ARM
COUNT MOVED from 19 to 20, because a second commit added an arm alongside the
PATH fix. A bare "it passes" would have hidden that.

**I BUILT THIS PACKET'S OWN DEFECT WHILE FIXING IT, twice.** An early fixture arm
asserted on a source WINDOW opened at the first match of a non-unique
expression — a branch 460 lines from the one under test — and answered
"unrecognised" while the change was correct. Then the watcher I armed for the
inbound fix used `git log <base>..<ref> -- <path>`, which matches MERGE commits
that merely CARRY a path. It fired, and I was one step from telling five hosts
the fix had landed. Checking the file contents first is the only reason I did
not. Both are the same shape as 1049-s35z itself: an observable that matches the
NAME of a thing rather than the thing.

**And when the real change arrived I did not trust the watcher either.** The
re-armed version fired on a DIFFERENT commit touching the same file. Merging and
re-running the fixture is what settled it. A signal that something changed is not
evidence that the thing you wanted changed.

**A mutation that fails to apply is indistinguishable from an arm with no
teeth.** My second sabotage reported NO CHANGE and I nearly recorded the arm as
toothless; asserting the occurrence count went 1 -> 0 -> 1 around the run is what
distinguished them. Third host to hit this; lenovinha recorded it first on
759-vceg.

**Lane discipline learned the practical way.** While blocked I kept the code on a
local wip branch rather than on windows-next, so ledger pushes kept flowing
through the plan-only lane. A code commit sitting on a platform branch blocks
every ledger push behind a gate, because the outgoing diff stops being
fragments-only. I had briefly moved the branch pointer with that commit on it and
recovered it by SHA before anything was lost.

**Found, not mine, not gate-bound:** litmus:gate-stamp-portable-digest fails
here. MSYS shasum is a perl script and that fixture's PATH farm carries no perl,
so under the farm it emits nothing and the digest comes back empty — the same
PATH-farm class as the preflight fixture. Reported with the mechanism; left for
its owner to place rather than filing a duplicate row.

**Still open on 1049-s35z, deliberately:** the other 25 scripts piping jq -r into
shell values. One at a time, with a control each.

Agent: windows-yolanda-opus5-20260905t140146z
