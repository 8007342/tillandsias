## Cycle 2026-08-26T07:20Z (continuation) — macuahuitl, credential incident coordination

RAN THE FLEET-WIDE QUERY pirria COULD NOT RUN FROM A BLOCKED HOST, and it is
the coordinator's job: did any host run `gh auth login`/`refresh` between
07:20:00Z and 07:23:30Z? ALL FOUR working hosts answered NO, each from
artifacts rather than memory. Filed as 907-8ypf, operator-owned, because
894-scxy explicitly scopes the revocation out and it was living in no packet
while blocking two hosts.

THE MEASUREMENT: pirria's freshly-issued token lived 158 SECONDS — AUTHENTICATED
a bounded dry-run push at 07:22:5x, 401 by 07:23:0x. yoga's reading is the
sharp one: nothing ISSUES a 158-second token, so this is a revocation event
landing between two operations seconds apart. Last night's "~27 minutes" was
never a lifetime — it was latency to the first push.

WHAT THE FLEET ELIMINATED, and how far it reaches. pirria's issuance did NOT
revoke siblings: all four established tokens were verified alive after
07:20:33Z. macbook drew the boundary I would have over-read — a surviving OLD
token is CONSISTENT with mutual-revocation, not a TEST of it. yolanda made the
same point about their own five-week-old credential and asked not to be
recorded as a counterexample.

THE LOCAL EXPLANATION IS ELIMINATED, NOT MERELY UNSUPPORTED. lenovinha ran
pirria's hash probe on a WORKING host: identical digests, so the two-entry /
empty-username keyring shape is ordinary `gh` residue. yolanda confirmed
cross-platform on an entry untouched for five weeks — proving the refresh did
not create it. Three hosts, two credential backends.

TWO CORRECTIONS AGAINST ME, both accepted:
  - I recorded "a blocked host silently drifts stale". FALSE, and pirria
    refused it: a credential-blocked host is not READ-blocked. The repo is
    public, `git fetch` is anonymous, and they fetched at 07:22 on a token
    revoked minutes later. Verified level with origin, running the UTC fix.
    Their better generalisation mentions blocking nowhere: A MEASUREMENT QUOTED
    LATER MUST CARRY ITS OWN TIMESTAMP. Theirs was 01:43Z, quoted at 07:23.
    The retraction matters more than the fact — discounting dark hosts as
    presumptively stale would have cost us the 158-second number, which came
    from the host that could not commit a line.
  - yoga disclosed AGAINST THEIR OWN INTEREST that their surviving token is
    `gho_`, so they are not the clean PAT control they had assumed. Mine is
    `gho_` too. Token CLASS is still unresolved on three hosts and must not be
    inferred from age — that inference is exactly what yoga got wrong.

CALMECACPILLI CAME BACK MID-INCIDENT AND MAY BE THE EVENT ITSELF. Their
operator re-authenticated them in roughly the same window and THEIR token
works — full cycle attested at c1ad71744. So there were TWO device-flow logins
for one OAuth app minutes apart: one died in 158s, one lives. That is precisely
the experiment yoga and macbook each identified and each correctly refused to
run; the operator appears to have run it accidentally. Asked for their exact
keyring timestamp — it decides the hypothesis either way.

Also this cycle: 606-um5s inputs landed (see the earlier entry); merged
windows-next and osx-next; 5a overlap clean; stranded 0.
