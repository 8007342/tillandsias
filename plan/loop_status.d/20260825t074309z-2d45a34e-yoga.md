## Cycle 2026-08-25T07:43Z (linux_immutable yoga — Start Of Day, and the sweep methodology already claimed to have)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `loop-cron`. Guards green; lock taken and released; boundary clean both ends. Preflight now reads `services-ok` — last cycle's proxy restart held.
- **START OF DAY GATE RAN** (local date rolled to 2026-08-25, marker read `due:stale:2026-08-24`). Delegate sweep: no registered runs. Podman prune: done. Capability row: current. Nix: absent on this host, so the nix lane and cache service are honestly `skipped-absent` rather than silently omitted. Experts healthy. Stamped naming every step.
- **The daily body is what surfaced this cycle's packet.** Working the cargo-GC step, I went to read the threshold and found `methodology.yaml`'s `build_cache_hygiene` rule — an operator directive of 2026-08-12 — ending with the sentence *"Implemented + litmus-pinned by the ledger packet build-cache-sweep-on-durable-hosts"*. That packet is **709-in2f**, and it was `ready`. No script. No marker. No litmus. Nothing in the tree read or wrote `.last-build-cache-sweep`. **The canonical policy document asserted its own implementation into existence.**
- **That is plausibly WHY nobody wrote one in thirteen days.** A rule naming its implementation reads as settled; anyone checking whether the sweep existed would have read that sentence and stopped. Same family as a litmus bound to no spec (660-ryhn) and the daily-maintenance marker that lived only as prose (801-qasc) — except here the prose actively argued the mechanism was present, which is worse than silence.
- **709-in2f CLOSED.** `scripts/check-build-cache-sweep.sh` with a one-line verdict grammar; due at >=40 GiB, >14d, or an absent/unreadable marker; exit 0 when NOT due (the sibling convention, stated in the header because it has been got backwards by a copy-paste before). `stamp` refuses without `--action`, for the same reason the daily gate requires `--steps`. Pinned by `litmus:build-cache-sweep-trigger` (8/8), bound; meta-orchestration instant 20/20. Wired as Finalization **step 9c, after the lock release** — methodology is specific that the reclaim happens at cycle END so the fuller next build is paid in the idle gap.
- **A sibling gate stopped me shipping a cross-platform outage.** My first draft aged the marker with `date -d`. `check-bash-dialect.sh` (761-g36m) refused the build: BSD `date` SUCCEEDS on `-d` with garbage output, so an exit-code guard cannot catch it and the failure would have been silent **on macOS only** — precisely the 803-bqte shape, where a GNU `sed \b` made three markers write-only on Macs while every self-test passed on Linux. Replaced with the days-from-civil awk converter from `check-deslop-due.sh`. A guard I did not know existed caught me in the one file whose whole purpose is being trustworthy about time.
- **One correction I made to my own work**: the first version reported a CORRUPT marker as `reason=no-marker`. Both are due, but that verdict sends the reader hunting for a file sitting right there. Now `unreadable-marker`, with its own litmus step.
- **Swept honestly, which here means NOT sweeping**: `target/` is 15 GiB against a 40 GiB threshold, so the only reason the trigger fired was that no marker had ever existed. Running `cargo clean` to satisfy a bookkeeping absence would discard a healthy cache and force a full rebuild — the opposite of a rule whose whole point is rarity. Stamped `cargo-clean:skipped-under-threshold-15gib`.
- **Note for the next editor of this skill**: `.claude/skills/` is a symlink; `git add` refuses paths through it ("au-delà d'un lien symbolique"). The real path is `skills/meta-orchestration/SKILL.md`. The edit lands correctly either way — only the staging path differs.
