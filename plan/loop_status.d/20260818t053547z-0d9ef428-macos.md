## Cycle 2026-08-18T05:35Z — macos (hourly meta-orchestration, offline)

- **Guards**: all clean; mcp `down:project-info` (**ninth consecutive cycle**);
  stranded `in_progress=0`. Merged `origin/linux-next`. No boot.

- **Closed 701-iu9b — the last offline packet in the macOS queue.** TRAP 1 in
  both halves, and the second half matters more: ordering cannot help a boot
  where the share is genuinely absent.
- **Ordering**: the fetch unit gains `After=home-forge-src.mount`. Using last
  cycle's measurement rather than re-deriving it — `RequiresMountsFor=` was
  empty and `After=` omitted the mount, and the race did *not* fire (mount
  3.005s, fetch 4.461s, **margin 1.456s**), small enough that a slower virtiofs
  mount closes it.
- **Plain `After=`, and the test asserts the ABSENCE of `RequiresMountsFor=`.**
  That implies `Requires=`, the fstab is deliberately `nofail` because a VZ
  config may legitimately omit the share, and `tillandsias-headless.service`
  requires the fetch unit — so the "stronger is better" version would turn a
  benign no-share boot into a **dead guest**, and only a boot would reveal it.
- **Visibility**: the silent fall-through now names *why* it skipped the staged
  binary and distinguishes the two causes, which need opposite responses and
  were previously indistinguishable because the path printed nothing:
  `unreachable reason=share-not-mounted` (file exists, invisible) vs `absent`
  (nothing ever staged). Probed with `findmnt`, not `mountpoint` — under
  `set -euo pipefail` a *missing* `mountpoint` binary would make
  `! mountpoint -q` succeed and report a healthy share as unmounted.

- **NOT live-verified, and that is structural.** cloud-init runs first-boot-only,
  so the edited unit reaches only a freshly provisioned guest; the only way to
  make it take effect is `--reset-guest`, which wipes the 11.83 GiB VM dir and
  the in-VM Vault. Shipped shape-tested and recorded as such — the packet's own
  notes warned that "let me verify my TRAP 1 fix live" leads straight to the
  destructive verb.

- **An existing test broke, correctly, and I widened it rather than patched it.**
  `vz_cloud_init_headless_fetch_unit_is_idempotent` pinned the *literal*
  `if [[ -x "$DEST" ]]; then exit 0; fi`. Growing that line into a diagnostic made
  a correct change look like a regression — the failure mode
  `litmus:litmus-expression-pinning-enforcement-shape` exists to discourage.
  Re-expressed as the property the literal stood in for.

- **Criterion 3 amended, not quietly dropped.** Its test half is **unachievable
  by construction**: `mod vault_bootstrap;` is itself `cfg(feature = "vault")`, so
  with vault off the module and every `cfg(not(vault))` item in it never compile
  — no test can reach an item that does not exist. The *documentation* half was
  genuinely undone and is now done: the stub carried **no comment at all**, so a
  reader arriving at the item that *looks* dangerous found nothing; the rationale
  lived only in main.rs and the ledger. Not deleted — ten siblings are dead for
  the same reason, and removing eleven items to tidy one is a bigger regression
  surface than the tidiness is worth while `compile_error!` makes the class
  unreachable.
- **Falsified both ways**: ordering removed → fails; `RequiresMountsFor=`
  substituted → also fails. 79 vm-layer tests pass.

- **QUEUE STATE**: with this closed, the macOS offline queue from the operator's
  original brief is drained. What remains is attended-or-destructive by nature:
  701-g98y (live CLI-login-then-tray-launch on a diverged host), 774-b529
  (a human clicking a submenu leaf the AX script cannot descend), 778-n9z2
  (needs a guest restage that invalidates the sha baseline), the attended **m8 UX
  smoke** macOS release acceptance still owes, and ledger compaction (linux's,
  by canonical-home convention). Next cycle should take a fresh
  `select-work-batch macos` rather than the carried list.
