## Cycle — lenovinha 2026-09-04T07:17Z (advance-work; cron lane armed 5 */2) — 917-6iwv slice landed, 998-3z6g closure DOES NOT hold here
- LANDED 9ca6bdc85, gate green attempt-1. FINALIZATION REFUSED and no mo-full marker was emitted: the boundary was snapshotted AFTER the issue file and the recorder were already written, so `finalize-cycle.sh` correctly reported `refused:finalize:boundary-verify-failed`. The delta is exactly the files I then committed (two untracked hashes gone, worktree otherwise clean). I did NOT re-snapshot to make it pass — the guard names that move and refuses it. So this cycle attests the narrower claim: nothing was left uncommitted FROM THE SNAPSHOT POINT, not from cycle start.
- 917-6iwv, FIRST GRADED RAG NUMBER ON THIS HOST. before: total=33 pass=28 skipped=5 skipped_engines=spec.answer. after building the index: total=33 pass=31 fail=2 skipped=0, validated 93% over graded=33. spec.answer itself is 3/5, not 5/5. Index cost 23,154 chunks cold — 2.3x the 9,910 the 801-a2by header measured on macuahuitl, same class as the 21,592 that blew yoga's bounds in 888-miiy.
- THE FAILURE MODE IS ONE SHAPE, TWICE: the retriever cites a CHEATSHEET SUMMARY where the normative text was required. forge-offline/spec.md lost to cheatsheets/runtime/networking.md; `fn effective_inference_tier` in tillandsias-headless lost to cheatsheets/architecture/cpu-only-model-tier-ladder.md. Both retrieved chunks are correct restatements of the answer; both cases demanded the source they restate. Corpus weighting, not a broken index. Recorded as a baseline and deliberately NOT tuned, so the number stays attributable to this model and generation.
- CRITERION 3 PROVEN FIRING, NOT ASSERTED. post-commit-expert-refresh.sh already forked spec-index-ensure.sh but was DEAD here: gated on TILLANDSIAS_HOST_EXPERTS, resolved from an untracked marker this host had never created (`ok:dev-host-experts:absent`) — 685-yidq's shape again, a refresh nobody had seen fire. Declared the host, then read the hook's own verdict on a real commit: `ok:spec-index:built:23164`, `delta reused=23154 embed=10 of 23164`. Ten chunks for a three-file commit.
- CRITERION 5 SHIPPED: scripts/record-expert-accuracy.sh appends JSONL with model id, quantisation, engine, lane, host, index generation and corpus commit beside every rate. estimated_rate_pct is null because nothing here produces an estimate — inventing one collapses the two columns the packet keeps apart. Host from derive-host-identity.sh; noting forge-expert-health.jsonl records "host":"unknown" while that helper sits in the same directory, which makes THAT series unharvestable across hosts.
- THE CONTROL IS PINNED, 6/6, and the subtle half matters more than the obvious one: never-called renders rate=null (not 0, not 100), AND a partial run whose graded cases all pass still reads 100%, so status/denominator/skipped_engines must travel with it. The historical `rate=100%` beside `expert-answered-nothing` was never a wrong number — it was a number whose scope was undisclosed.
- METHOD NOTE, costs an hour if rediscovered: the skip CANNOT be provoked from the environment. A stale FORGE_SPEC_INDEX_DIR or TILLANDSIAS_SPEC_INDEX_DIR does not pin the grader to a dead rung, it falls through to a working one — measured, an empty override still graded 33/33 skipped=0. A fixture that forced the skip that way would silently test nothing.
- 998-3z6g SECOND REGIME, CLOSURE DOES NOT HOLD. `tillandsias --init` exits 0 without creating or starting the proxy — twice, including from a fully absent container. Its own --help calls it "Pre-build container images", so cycle-preflight's REMEDY ("re-run the enclave orchestration (`tillandsias --init`)") prescribes a command that does not orchestrate the enclave.
- AND THE CA WAS NEVER THE BLOCKER HERE. The live proxy binds /tmp/tillandsias-ca/intermediate.crt — which exists and --init refreshed — while the migrated checker reports the post-fix ~/.local/state path missing. Installed v56.9.2.1 predates 44a33f328: the CHECK moved and the RUNTIME did not, so 975-rsgm's wrong-subsystem failure is reproduced by the fix's own migration on any host whose app predates it. The proxy DOES come up unaided once something actually launches it (health on :3128, attempt 2).
- CAUTION: scripts/orchestrate-enclave.sh ends with `podman rm -f` of proxy, git and inference. It is a demo/e2e path, not a restore path. I ran it and it left the proxy ABSENT; recorded rather than tidied away.
- A GREEN THAT SHOULD NOT BE GREEN, found as a consequence: proxy DOWN reads `degraded:services=6:up=5:down=1`; proxy REMOVED reads `ok:services=5:up=5:down=0:absent=0`. A health gate a `podman rm` can satisfy, printing absent=0 while a service is absent, because it enumerates containers that exist rather than an expected set.
- NEW DEFECT FILED (plan/issues/spec-index-durable-tier-unreachable-rootless-podman-2026-09-04.md): under rootless podman the 801-a2by durable tier is unreachable from the host-side producer. podman reports a mountpoint that is EACCES outside the user namespace; the rung-1 `[ ! -d ]` guard reads permission-denied as absence; the index lands in target/, which cargo clean removes wholesale — the exact hazard metrics-log-path.sh already refused target/ for. 23,154 embeddings in a GC target, silently, with `--where` reporting serving-exists=yes while volume= and root= disagree. NOT established, and stated as unmeasured: whether the forge resolves rung 1 correctly INSIDE the namespace, which would make producer and consumer disagree — 890-t9pu in a third subsystem.
- NOT DONE, stated rather than implied: 917-6iwv criterion 2 (expert inside a toolbox, host without one failing loudly) untouched — this slice indexed and measured on the host. Criterion 6 needs a second host's record; only lenovinha has written one. 805-3egx (Radeon Vulkan) not started, stays fallback; per macuahuitl it is likely discharged by 32dc4278c and wants verification, not reimplementation.
- HOST LEFT: builder, nix, nix-cache, dev-inference, vault up; proxy/git/inference absent, recreated on next forge launch. Daily maintenance stamped (nix lane blocked:nix-unusable — no host-side nix). Hooks v7, matching the tree; v8 is on osx-next and not yet merged.
