## Cycle 2026-09-14T18:52Z (lenovinha linux-immutable — five rows landed, one credential outage, one self-inflicted regression)

LANDED, all verified on origin by tree or ancestry rather than by a push's exit code:
- 1185-9qx6 -> 1e3e1e01e. ci-full now records the phases it runs OUTSIDE local-ci (post-build, runtime) under an exported TILLANDSIAS_CI_RUN_ID, so check-release-tier-freshness.sh can read a green release tier. Scope correction for the 890-27mv daily exercise: bare `--ci-full` exits after the pre-build gate and still reads never, correctly — the tier is only exercised by `--ci-full --install`.
- 1189-2ra5 -> b3a93780b. blocked:gh-keyring-locked, so a LOCKED-but-present keyring stops reading as missing:no-credential-channel, whose obvious remedy is the gh re-auth 1025-a896 forbids.
- 1125-92xa -> df65f1377. The Rust CLI writes its usage log where the shell reader reads; 1223 historical /tmp records merged here, independently reproducing pirria's 901.
- 1193-yw6u -> 660e092c8. Fixing MY OWN regression from b3a93780b (below).
- 1196-5hva -> 59b13d649. The heartbeat reads a blocker from the ledger and asks before calling a committing host wedged.

FILED: 1188-mm9y (installed Linux launcher carries no build provenance), 1194-davi (platform-scoped gate arms land unseen; macbookair obsoleted their duplicate 1194-smtb in favour of it and owns the work), 1196-5hva, 1189-2ra5.

THE OUTAGE: the login keyring on this host locked between 18:52Z and 19:37Z, mid-cycle. check-credential-channel.sh read ok: at the start and missing:no-credential-channel after, a gated and integrated land was refused at the push, and four commits sat local-only until the operator unlocked it. No gh auth login or refresh was run here (1025-a896). Preserved at refs/heads/salvage/lenovinha/20260914-1185-9qx6 and pushed once the channel returned. That outage is what 1189-2ra5 came from.

THE REGRESSION, mine, and the most useful thing in the cycle: b3a93780b bounded `gh auth status` in _ccc_timeout, which on a host with NO timeout(1)/gtimeout(1) returns 127 WITHOUT RUNNING THE COMMAND (order 988, correctly). So a healthy gh read as missing:no-credential-channel and BOTH macOS hosts lost the ability to land for a day. No Linux or Windows gate could see it: check-host-tools.sh's prover row for `timeout` is scoped platform: macos. macbookair bisected it in pristine worktrees, macneo verified the fix on the red cell AND its control, and yoga routed it because macuahuitl's session had ended. 1194-davi is the class and is NOT mine.

WHAT I GOT WRONG BESIDES THAT, recorded because the pattern is one thing three times: every instrument failure this cycle was in the thing WATCHING the work. A `pkill -f 'build.sh --check'` matched its own watcher shell and killed the monitor plus the relaunch. The harness reaped its background waiters under memory pressure twice; the land survived only under setsid. And a STALL I reported was my monitor hardcoding .git/tillandsias-land-gate-attempt-1.log while the land had moved to attempt 2 after losing a push race — watching "both logs" is not enough when the path carries an attempt number.

AND ONE I CAUGHT BEFORE LANDING: 1196-5hva's own exit criteria, which I wrote, named signals that did not exist fleet-wide (zero type:blocked events, zero type:failed, macbookair's row filed status ready). A detector built to them would have passed its own fixture and caught neither macOS host. I did not edit the criteria into something I could pass.

SELECTOR: seed=silverblue-ryzen7-amd+nvidia-lenovinha-20260915, epic=socket-audit-master for the 1125-92xa cycle. Direction: fail-loud-diagnosis-milestone dominates this host's night — four of five rows are its children.

CADENCE: 2h at :51 per macuahuitl. The cron prompt was itself defective — it hardcoded a packet and kept re-asserting it hours after closure — and has been replaced with one that reads the ledger for what this host holds instead of trusting its own text.
