## Cycle macuahuitl-20260910t053800z — 2026-09-10T05:59:00Z (gemini, linux-next)

- SELECTION: `scripts/select-work-batch.sh linux` returned batch `harness-mcp-expert-validation` (seed `linux-core-ultra-npu+nvidia-macuahuitl-20260910`). Selected and claimed packet `1025-a896` (`the-operators-gh-token-is-revoked-two-or-three-times-a-day-on-several-hosts-while-the-coordinator-never-re-logs-in`, priority p1).
- CLAIM: pushed claim fragment `plan/index.d/20260910t053810z-213265ff-macuahuitl.yaml` in commit `fa36833c0`.
- DELIVERABLE & EVIDENCE:
  - Researched and confirmed root cause: GitHub's OAuth app token policy limits 10 tokens per (user, application, scope combination). In a multi-host fleet, repeated `gh auth login` calls churn and evict the oldest token in the pool, causing rolling 401 Bad Credentials errors.
  - Formulated architectural fixes and recommendations in `plan/issues/gh-auth-token-invalidation-investigation-2026-09-04.md` (recommending fine-grained PATs per host scoped to repo read/write, analyzing GitHub App trade-offs, and noting downstream Vault-fanned enclave token impact).
  - Clarified the benign nature of the trailing notification "! You were already logged in to this account".
  - Updated `scripts/check-credential-channel.sh`'s `rejected` arm to attribute 401 failures to token eviction/revocation under order 1025-a896 and suggest the direct `gh auth login` remedy.
  - Verified with `scripts/test-credential-remedy-runnable.sh` (7 passed, 0 failed) and full `./build.sh --check` (575s, all phases green).
  - Code and report committed and landed on `origin/linux-next` as commit `5ec619991`.
- CLOSURE: closed packet `1025-a896` via `tillandsias-plan set-field 1025-a896 status completed --evidence 5ec619991`, pushed fragment `plan/index.d/20260910t055826z-02278133-linux.yaml` in commit `85e67c300`.
- METRICS: `scripts/cycle-metrics.sh --emit-flow` emitted for cycle `macuahuitl-20260910T055858Z`.
