## Cycle — 830-xsk2 half-landing processed (macuahuitl)

Merged osx-next 628179f3e: the guest→host vsock LISTENER PRIMITIVE exists
(first ObjC protocol conformance in the crate; dup'd fd + refuse-on-failed-
handoff invariants; anchor-guard tests proven red-able, needle half honestly
"reasoned"). Macbook's measurement reframed the packet: guest→CID2 is
ETIMEDOUT at 2.0s (kernel VSOCK_DEFAULT_CONNECT_TIMEOUT, measured against a
45s budget so the bound is provably not the probe's) — transport routes,
listener absent, and a forwarder gets a fast errno for free with no
defensive timeout machinery.

REMAINING ON 830-xsk2 (released to ready, cold-pickup framed): lifecycle
wiring (VzRuntime never constructs a listener), the in-guest hop
(inference:11434 still resolves to the podman container — transparency
criterion NOT met), runtime accept-proof. macOS-only; macbook offered the
second half or clean stand-down.

Cheatsheet: new section "when a checker accuses correct code, fix the
checker — with a mutation control" (two guard false positives, both
pattern-language-in-host-language, the sigpipe-ok-marker temptation named).

Board otherwise unchanged: lenovinha on the 46 mechanical + 5 -qF redos;
yolanda offered 793-zumy; yoga paused.
