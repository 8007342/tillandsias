## Cycle 2026-08-26T11:45:21Z — lenovinha (linux_immutable, linux-next, operator /loop)

### 799-tb7q third slice — five more callers, and three classes that must NOT be converted

**Classified before converting**, using the ~264 ms-per-toolbox-invocation cost measured last
slice. Flat callers convert safely; the seven **LOOP** callers (`bench-inference-floor.sh` 9 sites,
`measure-bands.sh` 9, `claude-mcp.sh` 8, and four others) are deferred — one jq per iteration turns
milliseconds into minutes, and they want restructuring to a single pass rather than a mechanical
conversion.

Converted: `check-stranded-in-progress.sh`, `check-mcp-expert-health.sh` (16 sites),
`generate-dashboard.sh`, `manage-cache.sh`, `host-capability-probe.sh`. Behaviour verified
unchanged on all five.

**The valuable output is the exclusion set, not the conversions.** Three classes that a naive
sweep would have broken, each looking like ordinary work:

1. **Curl-piped installers.** `install-macos.sh` is fetched from a release URL and piped straight
   into bash — `release.yml:408` and the smoke runbook's macOS row both do exactly that. **There is
   no checkout and no sibling file to source.** Converting it breaks the macOS installer for every
   user. This is the shipped-diagnostic rule one step more severe: those at least run from a
   directory; this one runs from a pipe.
2. **Bootstrap wrappers.** `with-tillandsias-builder.sh` *creates* the toolbox — it cannot use the
   toolbox to decide how to make the toolbox. Same for `with-wsl2-builder.sh`.
3. **Shipped diagnostics**, already guarded from slice one.

All three are now pinned by fixture arms, since each reads as an oversight to whoever finishes.

### Two measurement errors of my own, both caught before acting

- **My classifier regex matched a package name.** It reported the two builder wrappers as having
  bare-jq call sites. The lines are `jq yq ripgrep openssl \` — the dnf **install list** for the
  toolbox init set. `^\s*jq ` matched a package name in a list of packages. Eighth instance this
  session of a pattern matching text that merely *looks* like its subject, and the reason the
  exclusion arm names those files explicitly rather than trusting a grep.
- **`generate-dashboard.sh` returned rc=1 after my change**, which looks exactly like I broke it.
  Stashed, ran, unstashed, ran: rc=1 both times, output differing only in a line number my inserted
  header moved. Pre-existing — the script requires two arguments. Exercised the real path with a
  synthetic metrics file: rc=0 and a dashboard produced.

### Standing

Remaining on 799-tb7q: ~35 flat callers (mostly `test-*`), the 7 loop callers needing
restructuring, the `rg` callers, and the openssl callers. Packet back to `ready`.

**829-dkuc** still declined at p1 — `next_action` says it wants operator pairing or sweep-budget
headroom. Unchanged from last cycle; not unattended work.
