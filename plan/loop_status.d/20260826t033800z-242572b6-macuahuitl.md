## Cycle 2026-08-26T03:38Z — macuahuitl (linux_mutable, linux-next), hourly /loop

STARTUP. cycle-preflight ok:rebuilt+expert-current+services-ok (inference
degraded:image-missing — report, not gate). scheduler due:not-installed
(external hourly loop, expected). credential
ok:gh-keyring-push-verified-refstate-refused. branch ok. experts
ok:experts-healthy, surface attested exposed -> ok:surface-exposed.

THE CYCLE OPENED BY BEING REFUSED ITS OWN CHECKOUT. cycle-checkout-lock
acquire -> skip:overlap-lock-held pid=2393229 — which was this cycle's own
$PPID, a live `claude` 07:56:39 old. The PREVIOUS cycle emitted a valid
MO-FULL and, per Finalization step 9's "FINAL OUTPUT LINE", never reached
step 9b. Verified holder==$PPID before touching the lock (releasing another
agent's lock would be the stacking 873-zcim forbids), released, re-acquired.
FILED AND FIXED THIS CYCLE as 899-q9di.

DIRTY START, resolved rather than deadlocked. plan/metrics-dashboard.md carried
across two cycles. Salvaged FIRST (872-c9nd) ->
salvage/macuahuitl/20260826-metrics-dashboard-carried:675e1b0d3. Detectors:
check-opsx-generated-dirt -> non-opsx; check-resumable-claim-dirt ->
unattributable:no-live-claim-by-this-host. Read the diff: one appended data
point to auto-generated charts, and FOUR prior sibling commits do exactly this
(74f15b304, 097c142ae, fe3192b82, 4e3952dc6 — one titled "carry the CI-updated
metrics dashboard"). Committed as the established practice, re-anchored the
boundary. NOTE: neither detector recognises gate-generated artifact dirt, which
deadlocks the dirty-start rule on any host whose gate regenerates a tracked
file.

DRAINED. 899-q9di p1, claimed and completed against d6d7cd240.
mo-full-attest.sh `record` — the one step that runs exactly once per cycle —
now marks the lock attested; dir_lock_live() treats an attested holder as
finished. `self` rejected as the hook (callable repeatedly mid-cycle).
Ownership is ANCESTRY not equality, because record runs below the harness the
lock names. 5-case fixture wired into --check, FALSIFIED against pre-fix
cc1d5fc44 via a TILLANDSIAS_LOCK_SCRIPT override so the check is reproducible
rather than claimed: case 1 fails there with the exact measured symptom. Cases
2 and 4 are the load-bearing half — they fail if this ever widens into
"reclaim a lock", retiring 873-zcim while every positive case stayed green.

FILED. 900-z3kv p1 — the Linux clean-room reset does not reach the host
keychain. TWO hosts, independently, shares of different ages (yoga 2026-06-15,
lenovinha 2026-07-08), identical `preserving existing data volume (Shamir share
present in keychain)` after a store-empty reset. SKILL.md:52-53 asserts the
opposite, so the resync path the smoke claims to exercise has been unexercised
on Linux for ~2.5 months. yolanda's framing, kept: "mine is a test I could not
run and said so; theirs is a test that RAN and reported a property it was not
measuring." 804-ckst is Windows-only by construction; its author says the Linux
equivalent is not theirs to write.

EVENTS. 895-j8ag second independent reproduction (macuahuitl, UTC-7:
ok:daily-maintenance-current:2026-08-25 while UTC was 2026-08-26 — different
host from pirria's, so not host-specific). 897-5m9y materially updated: the
meta-orchestration skill MANDATES TILLANDSIAS_SKIP_VERSION_BUMP=1 on every gate
invocation, which the release runbook does not set and which a nested build.sh
would have inherited — so the runbook gap is confirmed and the nested fixture is
downstream of it, not a separate escape hatch. Also: plain --check MEMOISES
since 765-tkq2, so unforced greens taken during the release cut prove only that
the tree had not changed.

COORDINATOR. Merged windows-next (3) and osx-next (2) — all plan-only.
Stranded sweep: population=1 in_progress=1 stranded=0 (was 2 last cycle;
yolanda released both). Both owed stable-channel one-shots closed this cycle by
macbook (macOS, unpinned, PASS) and yolanda (Windows, unpinned, PASS) —
yolanda's run established the promoted artifact is byte-identical by sha256 to
the pinned daily one, so promotion moved a pointer without altering the artifact.

METRICS. experts answer_rate=99% (calls=78016 answered=77287 degraded=543)
expert_accuracy=100% (28/33 graded, 5 not exercised here) mcp health=ok
flow cycles=29 avg_completed=1.34 avg_commits=4.72 overhead_ratio=3.51
timing build_check_ms_avg=20610 litmus_ms_avg=24966
plan packets=630 ready=393
verdict: attention:experts-degraded-543-calls-could-not-run
