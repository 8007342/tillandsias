## Cycle 2026-09-04T10:15Z-10:50Z — yolanda (windows), advance-work-from-plan

Boundary first on a clean tree. 997-e4v2's Windows tray half landed: the tray no
longer touches the host's `~/src` by any path.

- **Both feeds of the Local menu removed.** The host-side scanner
  (`create_dir_all(%USERPROFILE%\src)` + watch on every tray start) and the
  VM-side wire path (subscription, 10-tick `EnumerateLocalProjects` poll,
  `LocalProjectsPush` handler, menu mapping).
- **The scanner is the one that mattered.** It did not merely READ a host
  directory, it CREATED one — a machine that never had a checkout root got one
  from the tray alone. Under forward-only that is the single behaviour that
  cannot survive, and a delete-only diff cannot show a deliberate absence, so
  it is pinned.

**I corrected the assignment before acting on it, not after.** The direction was
to take the tray half *and* the control-wire types as "both your surfaces".
Measured by consumer, all five wire symbols are used by
`macos-tray/action_host.rs`, and `EnumerateLocalProjects` also by
`host-shell/vsock_client.rs` — the surface explicitly held for the coordinated
pass. Deleting them here would have broken two crates this host cannot
type-check: the exact union red the heads-up protocol exists to prevent.
Coordinator agreed and withdrew that half. **Wire types die last**, same
tmpfs-first/mount-second discipline as slices 1 and 2.

**A vacuous pin kept on purpose.** The two topic lists are now identical and the
version-skew fallback cannot engage from here. They are not collapsed: order
260's stale-guest concern goes moot only when the topic leaves the wire, so that
pin should die in the *same commit* as the variant. The test now states the
distinction is vacuous and why.

**Two process failures of mine, both caught pre-landing.** I twice cut source by
find-marker rather than verified line ranges on a 5,700-line file where doc
phrases recur — one cut removed 1,741 lines including the whole tick loop,
another orphaned a doc comment and a brace. Both caught by compiling, reverted,
redone by explicit line range with asserted first/last lines. Nothing corrupt
reached a commit. Marker-cutting is not a safe edit primitive at this size and I
stopped using it. I also ran a fragment I had myself labelled a placeholder —
that is how the orphan happened.

**A pre-existing trunk red, reported not absorbed:**
`every_guest_wipe_path_clears_the_host_side_vault_credentials` fails at
`windows-tray/src/main.rs:521`. Verified pre-existing by stashing my change and
re-running against the pristine file — identical failure. It arrived via a
linux-next merge, and `./build.sh --check` does not run it, so the branch gates
green while a bin test is red.

**And a stale-base false positive.** The 634-39ik expression-pinning gate
refused my change naming a litmus file I never touched: it is diff-scoped
against `origin/linux-next`, my branch carried an older copy, so the diff read
as *removing* a negative control. Merging `origin/linux-next` made it vanish —
exactly what the new `pre-push-no-stale-base-revert` hook is for. A stale base
is indistinguishable from a deliberate revert if you only read the diff.

**Next:** the coordinated window — macos-tray and host-shell drop their
consumers, then the wire variants and the headless side come out.
