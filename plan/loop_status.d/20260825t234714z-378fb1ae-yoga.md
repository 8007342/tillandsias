## Cycle 2026-08-25T23:47Z (linux_immutable yoga — reading the sites instead of migrating them)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `operator-prompt-loop` (cron f26cbe2f, slot **:17** — the fleet skew macuahuitl assigned; my previous :13 was arbitrary). Guards green, preflight `services-ok`, inference `degraded:image-missing:build-inference`. Lock held/released. Boundary clean both ends.
- **Batch**: `epic=architecture-audit-epic seed=yoga size=5 budget=10 pick=2/7`. Third consecutive cycle on this epic — deliberate, per the coordinator's request to work related packets on consecutive cycles rather than let the selector scatter.
- **795-5itp slice 5 turned out to be a REDUCTION, not a migration, and the packet's headline is overstated.** I claimed it to migrate the Linux-testable sites. I read them first. **Five of the six cannot migrate for reasons this packet has already accepted for other sites.**
- Four are TEST PEERS inside `#[cfg(test)]` (control-wire/transport.rs, host-shell/provisioning.rs, host-shell/vsock_client.rs x2) — independent hand-rolled decoders whose value IS being independent. Migrating them onto the codec under test makes the interop claim a tautology; that is the vsock_server.rs reasoning, reached again from four more sites. The fifth (headless/main.rs) is PRODUCTION but **BLOCKING** — `read_exact` with no `.await`, the std UnixStream *client* side of the tray control socket, i.e. the tray/mod.rs keep seen from the other end of the same socket.
- **So "nine hand-rolled copies" counts things that are not the same kind of thing.** After reading all 18 decode sites in the tree, **the Linux production migration surface is ONE function.** Not nine, not thirteen. Baseline: 13 pending → 8, of which 7 are behind the macOS and Windows lanes. Criterion 1's "at most one raw decode" reads very differently now — on the Linux lane it is nearly satisfied already, and it was never satisfiable as literally written.
- **I did not start the one real target, and named the hazard instead of producing a diff.** `vm-layer/vsock_exec.rs` `read_envelope` has **41 call sites sharing one stream**, so a per-call `Framed` would over-read and silently drop pipelined bytes on the exec control path — a data-loss bug, not a compile error — and `tokio-util` is not yet a dependency of that crate. Deferring with the hazard written down beat starting a correctness-sensitive refactor late in a cycle.
- **The ratchet paid for itself.** Slice 4 built the baseline; this is the first slice to USE it. It turned "which sites remain" from a re-derivation into a lookup, and the per-site classification now lives beside each row so the next host does not repeat the reading.
- **Captured this host's cross-host probe measurements on trunk** (`plan/issues/research-cross-host-probe-measurements-yoga-2026-08-25.md`). They existed only in cross-session messages and in macbook's fragment on `osx-next`, which is **unmerged** — so a linux-next reader found the conclusions cited and the numbers absent. Includes gate n=15 mean 28.6s range 13.4–40.4s, the 53%-ledger-work breakdown, the cargo/toolbox/spawn numbers with replicate counts, and **the corrections to my own reporting**.
- **Both my toolbox figures were published at n=1 and both were wrong** — +5%/+25% became **+2.8%/+31%** on replication, and the n=1 pair travelled to three hosts via the coordinator and became "the container boundary costs almost nothing" before being caught. My single native cargo trial was the fastest of three, my single native spawn trial the slowest of five; both draws made the two regimes look more alike than they are. Then I replicated the totals and left the **components** at n=1 — on the trial whose total I had just discredited. macbook caught that. The components were a fair draw anyway, which is the non-obvious part: **replicating a total tells you nothing about whether its components were a fair draw.**
- **Corrected the compaction recipe I landed two cycles ago, having broken it myself.** 865-ng6r's fix told the reader to check that removed lines are "ONLY field keys". This cycle's fold removed `- container`, `- cpu`, `- gpu`, `engines`, `lanes` — because my stale capability row's `engines: [{name: ollama…}]` was superseded by `engines: []`. The fold was correct and my own histogram made it look like structure loss. The recipe now says READ the removed lines; the question is not "are these field keys" but "does each belong to a field some fragment reassigned". Scalar-only reasoning in text about a ledger that stores structures.
- **888-miiy is visibly working**: `expert_accuracy` now reads `pass=28 graded=28 total=33 rate=100% skipped=5 skipped_engines=spec.answer NOT-EXERCISED-ON-THIS-HOST` — a loud named skip where a silent green used to be. Relevant to the held release; reported to the coordinator.
- **Reports**: stranded `population=1 in_progress=1 stranded=0` (down from 9 two cycles ago). deslop `not-due order=894 delta=60`. Compaction 27 fragments, +2039/-14, 38 comments and 542 prefixes intact. Scheduler still `due:timer-inactive` — 856-s56y, still mine.
- **Metrics**: experts `calls=701 answered=701 rate=100%`; `expert_accuracy` as above; mcp `health=ok`; flow `cycles=2 overhead_ratio=4.5`; timing `build_check_ms_avg=32020 slowest=litmus:freshness-inventory-shape:70073` (the 12x-repetition finding, 888-92k6); plan `packets=614 ready=383`; verdict `ok:nothing-flagged`.
- **Not done, named**: no e2e (release held, and this host cannot green `--ci-full` — every spec-index rung is empty and there is no ollama); no freshness audit this cycle (discharged last cycle); the vsock_exec refactor deferred with its hazard recorded.
