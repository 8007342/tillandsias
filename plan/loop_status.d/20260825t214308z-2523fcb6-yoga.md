## Cycle 2026-08-25T21:43Z (linux_immutable yoga — the ledger's closures now have to resolve)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `operator-prompt-loop`. Guards green (`ok:gh-credentials-store`, `ok:branch-linux-next`, `ok:experts-healthy` + surface exposed, `ok:daily-maintenance-current`). Preflight `services-ok`, inference `degraded:image-missing:build-inference` (report). Lock held pid 11387, released at exit. Boundary clean both ends.
- **Batch**: `epic=architecture-audit-epic seed=yoga size=6 budget=10 score=17.232 pick=2/7`. Triage `eligible=165 grouped=137 ungrouped=28 epics=11`. Same epic as the previous cycle, and **885-92iu — filed by this host two hours earlier — was now selectable**, which is the batch mechanism doing exactly what it should.
- **Closed 885-92iu.** Order 721-77yu had already caught this defect shape in shell scripts ("Pinned by litmus:<x>" headers that read as verification and supply none) with the two verdicts `unresolvable` and `unbound`. It never looked at the LEDGER, where the claim carries more weight — `methodology/convergence.yaml` makes `verifiable_closure` the thing distinguishing a reduction from a restatement.
- **Criterion 1 first, and it mattered.** `check-litmus-pin-claims.sh` selected its corpus by filtering its first grep's OUTPUT LINES rather than filenames, so any line whose CONTENT mentioned a `.sh:` path was scanned as shell source. Now `find -name`. Measured in both directions: `SCAN_DIR=plan` went **24 → 0** (against a tree containing zero shell scripts — all 24 were archived prose quoting a script path) while the real `scripts/` corpus stayed at **exactly 71**. Both halves pinned, because a selector fix that quietly empties the corpus it was written for is the same defect with the sign flipped.
- **`declared-closures` (REPORT, exit 0) and `declared-closures-check <fragment>` (GATE, exit 1)**, the gate wired through a diff-scoped wrapper mirroring `check-fragment-closure-evidence-added.sh`. The split is the design decision: there is standing debt, and **a gate that reds the trunk on day one gets switched off** — the same argument that made 795-5itp's closure a ratchet last cycle. New debt is refused at the door, fragment-scoped so a closure is validated when it is FILED.
- **The report is now REPORTED, not merely reportable.** `./build.sh --check` prints `standing declared-closure debt: violation:declared-closure-unresolvable:5` on every run. A number available on demand is a number nobody looks at, which is precisely how 795-5itp's closure stayed empty for eight days.
- **MY OWN FILING WAS WRONG BY 9, and the correction is the point.** Filing this packet I reported 15 file-missing names, from `grep -o "litmus:[a-z0-9-]*" plan/index.yaml`. Field-scoped extraction over `verifiable_closure` ONLY gives **6**. The other nine were spec ladder shorthands and prose mentions — a **60% false-positive rate** on a check whose entire job is policing honesty about verification. The packet's own context had warned that a grep "produces a number nobody trusts and gets switched off"; writing the check is what proved it.
- **Criterion 3, all six dispositioned by examination.** 885-92iu's own `litmus:declared-closure-resolves` was BUILT this cycle (9/9 PASS, 1.4s, bound under spec-traceability) — the packet declared a closure that did not exist, so the report named the packet fixing the defect as an instance of it until the test landed. That was deliberate. The other five (795-jeym, 795-tb4a, 795-vq6b, 795-imz3, 795-bmbq) are each a REAL intended boundary matching their own packet's subject, none an extractor false positive. Building them here would be doing five packets' work under a sixth packet's name; what changed is that none of them can close silently now.
- **Three project guards caught me mid-change, all correctly** — worth recording because the change was itself a guard: `583-dv9n` (dispatch arm missing from `capabilities.txt`, so the MCP wrapper could not have routed to it), the capabilities sort-order test, and `check-plan-binary-probe-usage` (my litmus ran `tillandsias-plan` from a hardcoded `target/` path, which breaks on a Windows checkout where the runnable artifact is `.exe`). The guard net works on the hand that feeds it.
- **Last cycle's 865-ng6r fix paid for itself on first use.** Compaction (28 fragments, +466/-12) — I ran the verification recipe the corrected skill text now carries, got `11 status, 1 pickup_role`, and moved on in one command. The previous two cycles on two hosts each stopped and investigated the same shape.
- **Scheduler: `due:timer-inactive`** — unchanged, still 856-s56y and still yoga's own work; this cycle came from a session-scoped cron.
- **Reports**: stranded `population=2 in_progress=2 stranded=1` (down from 9/9/7 last cycle — macuahuitl's batch closed out). deslop `ok:deslop-not-due:order=888 last=834 delta=54 hours=141`. e2e `skip:live-runtime-present`.
- **Metrics**: experts `calls=363 answered=363 answer_rate=100%`; `expert_accuracy pass=22 total=22 rate=100%`; mcp `servers=3 health=ok`; **flow `cycles=1 avg_completed_per_cycle=1 avg_commits_per_cycle=4 overhead_ratio=4`** — the rolling view is live now and that ratio is the number 682-yiz7 wants driven DOWN by larger batches; one cycle is not a trend. timing `build_check_ms_avg=26466 slowest=litmus:freshness-inventory-shape:70073`. plan `packets=601 ready=372`. verdict `ok:nothing-flagged`.
- **Not done, named**: no freshness audit again (two cycles running — `litmus:freshness-inventory-shape` is also the slowest step at 70s, and that pairing is worth someone's attention); no e2e gate; 795-5itp's 13 pending framing sites untouched this cycle.
