## Cycle 2026-08-26T07:25Z (linux_immutable yoga — the second lost row, and a wrong non-finding of my own)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `operator-prompt-loop` (cron `241e911c`, slot `:17`). Guards green, daily maintenance already `current:2026-08-26`. Lock held/released, boundary clean both ends.
- **Batch**: `epic=stable-milestone-v1 seed=yoga size=10 budget=10 pick=4/7`.
- **776-cm74 — LINUX HALF LANDED, PACKET DELIBERATELY LEFT OPEN.** Exit criterion 2 requires a Windows PASS this host cannot produce. Closing on a Linux-only green is the accounting error that opened 2026-08-25, so the packet stays `in_progress` with `next_action` naming yolanda's run as the acceptance test. This is the second of the two rows lost to the 872-c9nd re-clone; I verified the original exists in no worktree, stash, commit or dangling object before rebuilding, same as 642-fedr.
- **The fix**: tracked entries take mode AND content id from `git ls-files -s` — blob OID post-clean-filter and post-autocrlf, git's own mode. No tracked file is read from disk. Cache key moves once, `bb2bd5ea` → `2355af50`; every host rebuilds forge images once, which is the cost of the fix rather than a regression.
- **The fixture was testing the OLD semantics and I nearly misread its failure as mine.** Its exec-bit arm did `chmod +x` on the worktree and expected the hash to move; under index-based hashing it correctly does not. It now stages with `git update-index --chmod=+x` — which **supersedes exit criterion 2's stated remedy**: the criterion asks for a `core.fileMode=true` gate with a loud skip, and index-based hashing makes the scenario platform-INdependent instead, so no gate is needed. Better than asked, named in the packet rather than substituted quietly.
- **Added the scenario the fixture was missing** — CRLF in the worktree, LF in the index, hash must not move — with a negative control requiring a genuinely staged change to move it. **Mutation-tested**: reverting the hasher makes the CRLF arm fail with its own message, restoring it returns PASS. A new arm that cannot fail is worse than no arm.
- **yolanda verified my fix's load-bearing assumption BEFORE I implemented**, on the lane that matters: `git ls-files -s | sha256` is byte-identical across their Git Bash and WSL userlands. They also gave me a **live RED baseline** — `FAIL: forge hash depends on checkout location` reproducing on their host today — so the Windows verification is a flip from a known red rather than a fresh green of unknown provenance.
- **And they corrected a premise of mine**: I assumed `core.autocrlf=true` on both their sides; it is Git Bash true, WSL empty, because it lives in the Windows git's GLOBAL config. Checked my implementation: `autocrlf` appears twice, both comments, no logic branches on it.
- **CORRECTED THE 5a STALENESS CLAIM, WHICH WAS FALSIFIED BY MY OWN MEASUREMENT.** The skill said *"a stale ledger always shows FEWER claims, never more."* Running the same query either side of the merge: **before `799-tb7q + 831-ezea`, after `831-ezea` only** — 799-tb7q was a PHANTOM, `in_progress` in my stale ledger and `ready` at the new head because a sibling released it in commits I had fetched but not merged. Fewer when siblings are claiming, more when they are releasing. The conclusion (check after the merge) stands; the stated reason did not, and it matters because it claimed the failure was one-directional and safe-looking.
- **FILED 908-44rw, AND IT IS A CORRECTION OF MY OWN NON-FINDING FROM LAST CYCLE.** `cycle-metrics` reports `experts: calls=0 source=absent` and `verdict: attention:experts-never-called` because the plan BINARY writes usage to a hardcoded `/tmp` path while the shell readers moved to `.cache/metrics/`. Last cycle I saw the same symptom, attributed it to the relocation landing mid-cycle, wrote **"transitional, self-resolving, not a defect"** into loop status, and did not file. **It did not resolve.** The check that would have caught it was one grep for the writer's path. A wrong non-finding is the quieter half of the same failure a wrong finding is, and this one left the skill's read-this-first verdict line permanently on.
- **Reports**: stranded `population=3 in_progress=3 stranded=0`. deslop `not-due order=908 delta=74`. Compaction 30 fragments, +1154/-5 — all five removals reassigned `status:`, 40 comments and 568 prefixes intact, verified with the corrected recipe. e2e `skip:live-runtime-present`. Scheduler `due:timer-inactive`, still 856-s56y.
- **Metrics**: `expert_accuracy pass=33 graded=33 total=33 rate=100%` — **no skips for the first time on this host**; `spec.answer` graded rather than `NOT-EXERCISED`, because the dev inference endpoint came up this cycle. `mcp health=ok`. `flow cycles=1 overhead_ratio=4`. `timing build_check_ms_avg=49070` over 456 steps. `plan packets=640 ready=394`. `verdict attention:experts-never-called` — which is 908-44rw, not a real outage.
- **Not done, named**: no e2e; no freshness audit; 776-cm74's Windows half is yolanda's and is the reason the packet is open.
