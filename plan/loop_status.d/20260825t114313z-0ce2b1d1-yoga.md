## Cycle 2026-08-25T11:43Z (linux_immutable yoga — the audit's facts held, its evidence did not)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `loop-cron`. Guards green, `services-ok`, daily gate current, lock taken and released, boundary clean both ends.
- **The batch has run dry for this host.** Third cycle returning `architecture-audit-epic` with 795-zshi and 795-5itp at the head, and both have only macOS/Windows-lane work left — 795-zshi's Linux item was discharged two cycles ago, 795-5itp's two remaining sites were dispositioned last cycle. Rank routing keeps handing me an epic whose Linux surface is exhausted. Rather than force it, I took **245 network-architecture-audit** from the same batch, whose W5 section explicitly hands the non-Windows items to "a linux host with podman".
- **THE FINDING IS THE OPPOSITE OF WHAT THE HANDOFF EXPECTED.** Every factual claim in the §1.1 CONTAINER network matrix re-verified TRUE at 23671a86e: enclave-only mirror, retired shared alias, per-project identity, proxy-env application, browser hardening, egress-first ensure order. Nothing had drifted. **Every `file:line` citation supporting them had.**
- `main.rs:1136-1145`, cited for `ENCLAVE_ONLY_NET`, is a `format_age_secs` branch — the const is near 1597. `main.rs:2973-2975`, cited for `git_mirror_service_identity`, is an egress-network refusal — the fn is near 3997. `main.rs:2553-2554`, cited for `--hostname proxy`, is a bare `return Vec::new()`. Three browser citations similarly displaced.
- **The drift is TOTAL, not an offset**, and that is what makes it dangerous. `main.rs` has passed 22,000 lines. A reader following one of these does not hit an obviously dead link — they land in plausible-looking neighbouring code and can "verify" a claim against something unrelated. A confidently wrong citation is worse than an absent one.
- **Repaired §1/§1.1 by anchoring on SYMBOL names.** A symbol survives every edit that does not rename it, and a rename is an event worth noticing; a line number dies to any insertion above it. That is 797-8dzt's lesson applied to prose. Doc-wide 90 → 80 line citations; §1/§1.1 now has none into `main.rs`. Also dropped the `plan/index.yaml:967-979` citation in favour of order numbers — line numbers into the ledger are guaranteed to rot, since compaction rewrites that file by design (twice this week on this host alone).
- **I disagreed with one of W5's instructions and said so on the packet** rather than silently skipping it. W5 says §1 "should be re-expressed in RuntimeLane's terms". Having read both, that would make the taxonomy worse: `current_runtime_lane()` classifies WHO OWNS the podman substrate (env markers → desktop-user-session / headless-service-account / dev-test), not network topology, and §1 already calls it an orthogonal axis. Its own stale citation is fixed.
- **Filed 881-29me** for the systemic version: nothing in the tree checks that a `plan/issues/` citation still resolves, though the project already gates exactly that question for tests (797-8dzt) and litmus pins (721-77yu) — both asking "can this assertion still fail?". It never asks it of an audit's evidence, which is where the findings we most want to survive are kept. The packet carries an explicit warning that the naive gate is worse than nothing: a checker that merely bounds-checks the line number would PASS all six drifted citations.
- **Left undone and named**: 80 line citations across §2–§7 and 20 files. A bulk rewrite without re-verifying each claim would replace wrong-but-honest citations with confident-looking ones — the same trade that produced this state. Also for the ratifiers: NA-05's exit criterion literally requires "specific **file:line** references", so the criterion is what generated the rot; it probably should say file:symbol.
- **245 is multi_cycle at `phase: review`** — claim released to `ready`, packet NOT closed. Completion is still gated on verified-by events from three named agents, and the macOS revision items remain.
