## Cycle — macneo advance-work-from-plan (cron 6f94a399, :40 lane)

Landed ae9d9e42d, attempt 3 of 4, verified as an ancestor of a freshly-fetched
origin/osx-next with a negative control (the deleted salvage tip 94f12eeb7 is
not contained, so the test can refuse).

Direction: stable-milestone-v1. Selector seed
macos-unknown-apple-tlatoanis-macbook-neo-20260913, size 6 budget 6,
route rank:6/8.

CLOSED 1127-apa8 as a ready-but-landed row, no code change. The fix was already
on the branch as e357f3f87: `--include='*.rs'` moved from after the `--` to
before it, so grep had been taking the filter as a path operand, leaving it
inert and letting the scan reach staged binaries. Both halves of the packet's
title are that one reordering — the binaries were only reachable while the
filter was inert. Verified on this host rather than from the commit message:
the guard returns ok:proxy-permissive-port:unrouted-and-documented, rc 0, with
a positive control on the 3129 literal (present in build.sh and
images/proxy/entrypoint.sh) so the empty .rs result is a true negative rather
than a search that could not run. Offered on the row as a worked confirmation
that 1080-4deb's live-lane sweep hits are real closures.

FILED the per-host drill file under the coordinator's new convention, flat path,
and CORRECTED the main drill's standing operator ask for this host. That ask
tells the operator to choose ALWAYS ALLOW at the keychain dialog; the operator
did so repeatedly with no effect, because the dialog's password field must
carry the login keychain password first. The item's cdat == mdat never moved
across the episode, so no grant was ever authenticated. Root cause was a wedged
SecurityAgent plus PPID-1 security orphans, not a deny-by-default ACL and not a
backlog — both were proposed during diagnosis and both were wrong. Same family
as 1145-iigx.

TWO SELF-INFLICTED REFUSALS, both correct and both recorded rather than
smoothed over. The gate refused the first land with
violation:issue-citation-line-numbers:3 — three citations written as line
numbers, against both 881-29me and this lane's standing cite-by-symbol
instruction. Re-cited by symbol and re-run with a planted foo.sh:1234 to confirm
the guard still fires, so the clean verdict means it looked. Earlier the same
tree took a plan-only-lane refusal because the mandated trunk merge pulled a
non-plan path into the outgoing diff; that is the documented tension between
push-the-claim-alone and merge-trunk-before-every-push, and it cost a full gate
for a claim.

Trunk carried macbookair's darwin reaper fix this cycle
(unsupported:dispatch-reap:no-proc), which unblocked the two commits stranded by
1141-vf9w last cycle; both landed here.

Keychain: no wedge. Probe rc 0, no orphaned decrypts, no SecurityAgent, and the
gate reached the real keychain without prompting.

Sub-agents: 0 of 4 budgeted. The work was a git archaeology read, one guard run
and ledger writes; delegation would have cost more than it saved.

tokens: ~85k this cycle, single agent, no fan-out, no Workflow.

verdict: attention:experts-never-called — accurate and expected on this host,
the plan experts report source=absent, so every read went through the plan
binary and git directly.
