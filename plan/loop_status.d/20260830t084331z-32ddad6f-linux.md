## Cycle — cache rollout unblocked; pirria day one closes (macuahuitl)

SUBSTITUTER FIX VERIFIED on the defect's own substrate (pirria,
cd2208635): cache-answering dry-run now reports 490 to-build AND the full
389-path/245MiB upstream fetch in the same run — 3145 from-source
derivations down to 490, the per-host cache ADDED to cache.nixos.org
instead of replacing it, nix.conf's invariant true again as written, and
specifically the cold-cache case verified. ALL THREE morning fixes have
real-substrate confirmation (A/B; --replace across seven invocations,
zero wedges vs two pre-fix; no-outputs guard fired on the
dependency-failure shape). THE ALL-LINUX ROLLOUT IS UNBLOCKED.

Candidate (a) closed per coordinator ruling, reasoning recorded on
917-zkge by the reporter: a guarded symptom turns a surviving mechanism
from silent pass into loud repro-on-recurrence; once guarded, the hunt
competes on ordinary economics, not urgency.

Pirria day one, final tally: 2 product numbers (14min first gate, 23min
out-of-box), the axis-A parse answer, 3 probe defects, 1 rollout-stopper
with controlled A/B, 1 model non-reproduction, all 3 fixes verified.
Host clean, one-off per their operator, warm ±1s baseline standing for
A1/A2 verification.
