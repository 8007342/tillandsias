## Cycle — macuahuitl hourly coordination (cron lane)

SIBLING HEADS: all four converged at ahead=0, nothing to merge.
  main b528680d2 (behind 552) · linux-next f3a6fefb0 · windows-next 7854185db
  · osx-next 0f45223ff

SWEEP: live=8 unclaimed=1 expired=0 held=0. Nothing stranded. The one
unclaimed row is 1025-a896, unattributable by construction (1065-4t7t).
HEARTBEAT: 7 peers — 5 idle, yolanda running, pirria requires_action.

THE LAST ORPHAN WAS MINE AND IT IS CLOSED. The guard-activation count went 4
-> 1 -> 0 this hour, and only one of the original four was ever real. See the
correction fragment: two were never orphans (invoked one hop past the
auditor's surface list, yoga at dcf772b2e), one was a library esmeraldinha
renamed with a refused:not-a-check exit, and check-plan-binary-current.sh was
mine.

REWRITING IT WAS THE BLOCKER, NOT SCHEDULING. The first version probed ONE
commit and printed a verdict naming the general property, so wiring it would
have been WORSE than leaving it orphaned — it would have green-lit the exact
binaries that were stale in the way that mattered. It now delegates to the
two-sided fixture and reports an arm count, probes executability FIRST and
says `unmeasured:` rather than `stale:` when the locus cannot run the artefact
(esmeraldinha's WSL case), and reports whether a second copy exists and
whether a bare name resolves (macneo's precondition). Three controls fired
before wiring: PE binary under Linux -> unmeasured rc 0; stub failing the arms
-> stale rc 1; ledger byte-identical throughout.

AND WIRING IT EXPOSED AN AUDITOR BLIND SPOT OF THE SAME SHAPE. The .step
binding did NOT clear the orphan: `surfaces` in audit-guard-activation.sh has
no gate-steps.d entry. Eleven bindings moved there in the 1072-b7eq migration
and the surface list never followed — and the migration comment TWENTY LINES
BELOW the array predicts it exactly, "one indirection further from every
name-based scan". Nothing redded before because those eleven are all test-*.sh
while the auditor's population is check-*.sh; mine was the first check- bound
that way. Surface added, orphan 1 -> 0, with a control confirming a genuinely
unbound guard is still reported. yoga's framing: a blind spot that only opens
when a new KIND of thing passes through it is invisible to every previous
instance, so "it has always been green" is not evidence.

Verified by execution rather than existence, in the land's own gate log:
  [build] Checking this host's plan binary refuses to write when asked to read
  ok:plan-binary-write-is-opt-in:... arms=16/0 copies=1

ROUTING: 1030-i2p8 to esmeraldinha. pirria DECLINED it on regime grounds and
was right — the defect lives on a shared Windows/WSL checkout where the probe
resolves the .exe and refreshes the ELF, which a Linux floor host cannot
reproduce, cannot verify, and could only write a Linux-passing fixture for.
Two trunk reds tonight came from exactly that mistake in the other direction.

1004-pg9p IS PIRRIA'S THE MOMENT THE OPERATOR AUTHORISES, and the ask is now
worth more than one packet: an authorised destructive smoke also discharges
the observation 1026-ps4n is still owed (`implemented`, not completed —
criterion 3 needs a REAL forge lane to produce either a smoke-forge-lane
record or a named-cause gap, and the stamp/reap machinery has never run
against one). It would also be the first floor timing series with the
supervisor-loss path armed, on the host whose wrapper the harness has killed
twice. pirria is otherwise idle BY CONSTRUCTION this cycle: one packet they
cannot authorise, one they cannot see. They reported that rather than
manufacturing work.

OPERATOR QUEUE: restore pirria's PUSH credential (throughput — five packets
relayed off that host; fetch works, which corrects an earlier overstatement of
mine); authorise the floor-tier destructive smoke (closes two packets);
provision a dedicated smoke host.
