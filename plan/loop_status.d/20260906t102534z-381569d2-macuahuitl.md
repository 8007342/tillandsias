## Cycle — macuahuitl hourly coordination (cron lane)

SIBLINGS: osx-next fully merged. windows-next +4 merged clean — six of
esmeraldinha's fragments, all plan-only, no code. Compaction green.

I DISCHARGED WHAT I OWED esmeraldinha AND IT TURNED OUT TO BE A REAL DEFECT.
I promised to run the release gate's capability-manifest failure and name the
failing link rather than leave them guessing. It reproduced here, and the step
is a four-link && chain whose second link is the one that fails:

  warning: dispatch arm 'forgotten' is not listed in capabilities.txt —
  the MCP wrapper cannot route to it (order 583-dv9n)

DISPATCH_ARMS 59, capabilities.txt 58, difference exactly one. `forgotten` is a
real subcommand — handler at main.rs:5448, usage at :318 — added six hours ago
by 0a8815181 for 718-jqt5 with no manifest entry. So a shipped subcommand was
UNREACHABLE through the expert interface, because lib-expert-capability.sh
reports blocked_capabilities from that manifest. Fixed by adding the entry; the
negative control now passes at 59 against 59, stderr empty. Filed 1113-j9wv.
One of the five release blockers is gone.

THE GUARD WAS RIGHT AND NOTHING READ IT. It printed the defect, the remedy and
the order number, on stderr, six hours ago. ./build.sh --check does not run this
litmus (1087-h2z9), so no land could see it; the release gate runs once a day and
reported a failing test NAME. It took a manual four-link decomposition to learn
that stderr held 180 bytes. esmeraldinha's reportability fix — naming each link
on failure — would have made this a one-minute fix six hours ago.

AND I MISFILED IT, WHICH IS THE MORE USEFUL FINDING. I put it in 1109-t8kw
(fixtures asserting properties of their ENVIRONMENT) on a symptom match. It
asserts a property of the TREE and was correct to. Struck, and esmeraldinha
named the hazard my own packet creates: the rule "never score a correct refusal
as a failure" has a converse — ONCE A FIXTURE CLASS IS NAMED, A NEW RED MATCHING
ITS SHAPE MUST STILL BE REPRODUCED BEFORE IT IS FILED INTO THAT CLASS. Symptom
match is not membership. 1109's own closure already required reproducing each
red before fixing; I applied it to the two that needed it and skipped it for the
one that looked obvious. The discipline was written in that row and not followed
in that row.

esmeraldinha COULD NOT REPRODUCE IT AT ALL and reported that honestly — their
checkout predates 0a8815181, so their arms and manifest agreed at 58. Two hosts,
opposite results, six hours of trunk between them, and "reportability fixed,
cause not reproduced, not claiming more" was right for a reason neither of us
had: there was no defect on their tree.

1109-t8kw WAS BEING WORKED WITHOUT A CLAIM. It read `ready` in the fold while
esmeraldinha had fixes gating on it — 1079-qb8k arriving on the host that filed
about it three times last cycle. They claimed it at f93650214 within minutes, and
narrowed their own 1111-m6qd wording in the process: claim-before-EVENT IS
enforced (the tool refused their event and named the hazard), and the real gap is
claim-before-WORK, which cannot be enforced because nothing in the ledger can
observe a host implementing a packet it never claimed.

SWEEP: live=9 unclaimed=0 expired=0 held=0. Nothing stranded. esmeraldinha's two
releases from last cycle fold correctly. 834-r6vn is still under the `windows`
BUCKET rather than yolanda (1012-hu7d).

HEARTBEAT: five idle, two running. All idle hosts hold claims — between tool
calls, not starved. No reassignment owed; the cycle's value was tracing a
release blocker to a one-line fix.

RELAY REFS: work/1069-c9w6 and work/1087-h2z9 still report UNMERGED by ancestry
and are both content-landed, verified two cycles ago. They will keep costing
every coordinator a check until someone deletes them.

OPERATOR QUEUE, unchanged: pirria cannot push; the OAuth all-or-none PAT
decision; the 03:23:01Z-04:22:15Z security log; and no dedicated smoke host, so
post-release e2e has been unrunnable for four cycles.
