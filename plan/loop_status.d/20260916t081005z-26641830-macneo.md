## Cycle — macneo advance-work-from-plan (cron 5a838dc0, :40 lane)

Ledger-only. Relayed to trunk (52a66a734). Branch not advanced; content durable.

Freeze checked with a control: 0 freeze refs against 555 total.

793-rb9u — produced the macos HOST-KIND REPORT this row's exit criterion asks
each host kind to emit, as a verified-by event. Not claimed: the row is a fleet
decision and each host reports its own compatibility rather than voting.

  bash resolves to /bin/bash 3.2.57 — Apple's, and the ONLY bash here,
  identical under the agent PATH and an augmented one, so unlike esmeraldinha
  there is no PATH-order ambiguity to resolve.
  The experts ANSWER: both stdio servers rc=0 under 3.2 with stdin closed and a
  bound; no bash-4-only idiom in the launcher.
  A compatible but a no-op here. B compatible and beneficial for a reason the
  option does not state — pinning /bin/bash makes the 3.2 DIALECT declared
  rather than emergent, which 761-g36m's guard assumes but cannot enforce at the
  launcher. C and D NOT APPLICABLE: both exist to cross a runtime boundary
  macOS does not have.

THE macOS ANALOGUE, which is the part a next host would rediscover: the hazard
here is a DIALECT split that does not exist yet. One `brew install bash` makes
`bash` PATH-order-dependent between 3.2 and 5, silently changing which dialect
the MCP servers run in. B is the only option of the four that prevents it.

METHOD NOTE: my first control on the single-bash claim was WEAK — it found one
grep, which proves nothing about whether the loop could find several. The
stronger form confirmed the search space was real (/bin 37 executables,
/usr/bin 884, /opt/homebrew/bin 1) with bash in exactly one. Three independent
confirmations in total, because a single-source claim is what this row exists to
distrust.

Stamp went stale from the mandated trunk merge, so the relay refused until a
gate refreshed it; ran the gate alone rather than the land, since the land's
push is what failed last cycle against a 3-minute trunk.

Keychain: no wedge. Probe rc 0, no orphans, no SecurityAgent.
Sub-agents: 0 of 4 budgeted.
tokens: ~80k this cycle (ESTIMATE — cycle-metrics reports token telemetry
source=absent on this host), single agent, no fan-out, no Workflow.
