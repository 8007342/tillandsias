## Cycle 2026-08-18T12:24Z — macos (CLOSE-OUT, loop ends at 06:00 PDT)

- Verification-only. No new work started: 37 minutes remained at stop check and
  the next firing would have landed past the deadline. Guards clean, tree clean,
  0/0 with remote, `in_progress=0`, ledger `ok: 1125 packets`.

- **THE ONE UNMET OBLIGATION, stated first because it is the most important
  thing this loop did NOT do**: the **attended m8 UX smoke** that macOS release
  acceptance owes was **never paid**. 735-6iki is gate evidence, NOT acceptance.
  No unattended cycle could discharge it and none did. Any claim that macOS is
  release-accepted is unsupported by this loop.

- **Also untouched, by nature**: 701-g98y (p0, needs a live CLI-login-then-tray
  launch on a diverged host), 774-b529 (a human clicking a submenu leaf the AX
  script cannot descend), 778-n9z2 (needs a guest restage that invalidates the
  sha baseline other packets depend on). The local-build **e2e was never run** —
  it destroys the 11.83 GiB $VM_DIR this loop measured and filed two packets
  about. **Ledger compaction never ran** (linux's by canonical-home convention);
  fragments went from 43 to **132** overnight — worth a coordinator pass.
  **798-q4m9 criterion 3** (1-vCPU cold-boot bind measurement) is OPEN and was
  deliberately not faked with a multi-vCPU pass. **795-zshi is NOT closed** —
  remainder is windows-tray + wsl_root_sh.
  **project-info MCP was down for the entire second half** (13 probes, 0 up,
  while forge-plan had 0 down) — filed as 827-rjc9, not fixed.

- **CLOSED WITH EVIDENCE, each falsified** (verified against the ledger, not
  memory): 803-bqte, 805-pr3p, 812-64j7 (the bash-3.2/BSD trio), 663-69kp,
  690-cb62, 701-se6x, 701-kgvk, 701-iu9b, 723-fndi, 723-whrx, 733-mppc,
  681-er9z, and **804-bpke (p0)** — a shipped release artifact that deleted the
  11.83 GiB VM directory outside its own `--wipe` guard while printing "Cache
  preserved".

- **FILED AND OPEN** (9): 803-rbqf/r8u4/825k (accel probe), 804-deux (model cache
  durability), 807-bjjv (the fleet matrix cannot include macOS), 811-j9fc
  (unsigned tray cannot boot a VM), 827-rjc9/d3dc/hakc.

- **DECLINED WITH MEASUREMENT — a first-class outcome, not a gap.** Four briefed
  steps this session were wrong or not worth doing, and only measuring revealed
  it: 795-zshi slice 4's briefed gate (CAP_EXEC_ARGV_VECTOR shipped in a
  different commit than the CA heal, so gating on it would have reintroduced
  772-shi9); slice 5's briefed deletion (two `|| true` no-ops, neither among the
  five named workarounds, preamble stays shell regardless); 795-hzpg's exit
  criterion (an async shape cannot serve a deliberately-sync API); and the
  boot-harvest plan two cycles earlier (the packet's own events had already
  retired the hypothesis it was built on).

- **THE PATTERN WORTH CARRYING FORWARD**: three bash-3.2/BSD defects (GNU `\b`,
  `"${arr[@]}"` under `set -u`, BSD `wc` padding) that `bash-dialect-clean`
  caught **none** of, and three literal source pins that turned a CORRECT
  refactor red. Both classes are filed (827-d3dc covers the pins); the dialect
  rule is proposed, not built, because a new failing gate is the operator's call.
