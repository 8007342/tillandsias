## Cycle 2026-08-25T23:12Z (windows/yolanda — BLOCKED on push; 889-8tcb deadlock, review + two findings filed)

- **Host**: yolanda, `windows`, branch `windows-next`. Daily maintenance `ok:daily-maintenance-current`. Preflight `ok:cycle-preflight:rebuilt+expert-absent+wsl-ok+services-no-podman`.
- **DISPOSITION: BLOCKED.** No `MO-FULL:` marker, deliberately — `mo-full-attest` requires local HEAD to be the converged remote head, and this host cannot push. A marker over unpushed state is the fabrication 651-2x5s exists to prevent, so the missing marker is the signal working, not a skipped cycle.
- `check-forge-findings-persisted.sh` reads `unpersisted:unpushed` and is CORRECT to. Five commits sit local; none are lost, and all push together when the blocker clears.

### The blocker (filed as 889-8tcb, p1)

887-bz88 added the exec bit to the gate-stamp frame as a WORKTREE test. Windows
drvfs reports every file executable, and on this host the gate runs inside WSL
while the pre-push hook runs in Git Bash — so the two sides cannot compute the
same digest. The hook refuses the push as stale while the gate refuses to re-run
because its own memo says fresh: a deadlock, not a slow path. `TILLANDSIAS_FORCE_CHECK=1`
does not cross the WSL boundary (`with-wsl2-builder.sh:231` forwards only
`TILLANDSIAS_SKIP_WSL2`), so the documented escape hatch is inert.

Second measurement, which is what settled the design: **887-bz88's own fixture is
red on this host at clean HEAD**, because `chmod -x` is a no-op on drvfs, so it
fails its own setup and then reports the hole it failed to open. That makes case
2's assertion unrunnable on a `core.fileMode=false` worktree rather than merely
weaker — and `litmus:mode-only-regression-gate` binds it.

Resolved **index-always** by lenovinha (its author). I had patched it locally
with a `core.fileMode` conditional; their fixture went red against my patch, so I
reverted it rather than land over another host's pinned assertion. Their reason
for declining even the conditional is the better rule and worth keeping: *"when I
cannot test a platform, no branch beats correct branch."* I own the third exit
criterion — confirming the fixture becomes RUNNABLE here, not merely quiet.

yoga (matched silicon, bare-metal Linux) supplied the control group: fixture fully
green there, and index-vs-worktree mode agreed on all 4537 tracked files, making
index-always a provable no-op where bits are real.

### Filed this cycle

- **889-8tcb** (p1) — the deadlock above, with the divergence table, the
  fixture-red-at-HEAD result, the WSLENV gap, and a rejected design recorded so
  it is not retried (a capability probe answers per-ENVIRONMENT, which is the
  wrong axis; both sides answer honestly and still diverge — measured).
- **890-y72v** (p1, `pickup_role: linux`) — the 803-49re Part B wire review the
  coordinator requested. Key finding: the precedence rule Part B needs ALREADY
  EXISTS at `vault_bootstrap.rs:80-85` and is merely too narrow, so implementing
  it widens an accepted rule rather than arguing a new policy. Also: validate
  before persist, the podman secret the self-heal never consults (which lets the
  validation half land without the wire change), the misleading error text, the
  30s spin, and the wire-compat asymmetry.
- **890-t9pu** (p2) — on Windows the gate writes timing records into WSL's
  `/tmp` while `cycle-metrics.sh` reads Git Bash's `/tmp`. 322 build-check
  records on one side, 0 on the other, so every timing metric this host has
  published was stale or absent. The number that was hiding: **`./build.sh
  --check` 276.5s here against yoga's matched-hardware 26.5s — ~10.4x for the
  WSL2 lane**, the first hard number for the operator's "negligible overhead"
  goal. 2977 records also carry `host="unknown"`.

Three defects, one cause (the Windows process/filesystem boundary). Noted in
890-t9pu that it may deserve one explicit contract rather than a fourth point
fix; not proposed, as that is above a drain host's call.

### Honest ratio

This cycle FILED four and DRAINED two. That is the wrong direction against the
coordinator's drain>file directive. Both undrained items are blocked on the push,
not on a choice to file instead — but the ratio is the ratio.

### Coordination

- Cadence slot :37 taken, keeping the operator-set 3h interval and shifting only
  the phase (`37 */3 * * *`). The slot plan assumes 2h; my :37 is occupied every
  third hour, so re-slot around me rather than assume coverage.
- `nix-cache-service.sh` (801-kqme): **does not apply here.** No nix and no
  podman on the Windows host; the forge lane lives inside WSL2, so if this is
  ever meaningful it is a guest-internal service between a store and an enclave
  both inside the guest. The host is not a party to either end.
- Paired directly with yoga per operator directive. Next joint experiment is
  process-spawn cost, the first suspect for where the 10.4x lives.
