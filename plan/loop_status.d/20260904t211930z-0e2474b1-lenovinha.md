## Cycle — lenovinha 2026-09-04T21:34Z (advance-work) — 1022-px54 closed: the trunk-red that blocked --check for every host
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T21:19:30Z`:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
      flow: cycles=8 avg_completed_per_cycle=0.13 avg_commits_per_cycle=1.75 overhead_ratio=14 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      timing: steps=27900 build_check_ms_avg=140691 build_check_mix=mixed:forced=370,memoised=100 litmus_ms_avg=13419 slowest=litmus:tool-materialization:304387 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      repeat: window=since=2026-09-04T21:19:30Z steps=122 top3=build-check=3,build-preamble=3,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=2 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      recur: window=7d runs=25762 steps=430 top3=build-check:runs=330:total_ms=49158115:avg_ms=148963:fail_pct=30,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=230:total_ms=7685319:avg_ms=33414:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=230:total_ms=5713443:avg_ms=24841:fail_pct=0 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=59 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=230:avg_ms=33414:fail_pct=0:saved_ms_upper=7651905,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=230:avg_ms=24841:fail_pct=0:saved_ms_upper=5688602,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=220:avg_ms=19512:fail_pct=0:saved_ms_upper=4273312 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=706 ready=456 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=dirty traces=current
      verdict: attention:worktree-dirty
- LANDED 9cac76676 (verified on trunk). PRE-FIX quoted red first as the criterion required: panicked at proxy_cache_policy.rs:112:5, "Squid launch", 0 passed / 1 failed. POST-FIX 3 passed / 0 failed, gate green 226s.
- A CORRECTION TO THE ASSIGNMENT: I was told scripts/test-squid-supervisor.sh was the test. It is not — it passes 8/8 and always did. The failing test is the Rust one the packet's own owned_files names. I ran the shell suite first on the instruction, got a clean 8/8, and went to the packet rather than concluding there was nothing to fix. Third time today a handed-over detail did not survive checking; each check cost seconds.
- THE PACKET'S OPEN QUESTION IS SETTLED, YES, and the reasoning matters more than the verdict. The supervisor relaunches on crash with `"$@" &` and does NOT re-run `squid -k parse`. That LOOKS like a gap and is not one: parse runs at entrypoint:84, BEFORE the supervisor is exec'd at :97, so it precedes every Squid launch in the container's life, restarts included, and the config is immutable for that lifetime — a restart has nothing new to validate. It would only break if the config could change at runtime, and that condition is now written AT the assertion so the next person catches it instead of re-deriving it.
- I PINNED THE SUPERVISOR, NOT THE SUBSTRING. `squid -N` still appears in the supervised line, so the one-word fix passes — and would ALSO pass for a future bare `exec squid -N` that dropped the supervisor entirely, silently undoing 767-es4w and restoring the uncounted-crash state it was written to end. The OLD assertion had exactly that weakness. Two mutation controls fire with distinct messages.
- THE GATE MADE ME FINISH THE JOB, correctly: after the fix, --check FAILED with `stale=1`. A known-red that has started passing makes the NEXT break of that same test invisible, so removing the test-known-red.txt entry is half the fix rather than bookkeeping.
- AND THAT ENTRY'S STATED CAUSE WAS WRONG, which is likely why it sat two weeks: "fails on any host without a working Squid launch in the test environment" invites every reader to conclude it is environmental and not theirs. The test never launched Squid at all — it reads entrypoint.sh as TEXT. A known-red whose description misattributes the cause is worse than an unexplained one, because it actively redirects the person who could have fixed it.
- LAND ORDERING HELD: merged origin/linux-next immediately before pushing as instructed, and inherited the release in flight — tag v56.9.4.1 and the VERSION bump arrived in the same merge.
