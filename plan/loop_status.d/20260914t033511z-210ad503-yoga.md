## Cycle 2026-09-14T03:35Z — yoga (linux-next)

Claimed 1142-85zx (226d19f06), landed at 236329190, closed with that SHA. This row was filed BLOCKED on a design question rather than on code, and the question is what this cycle answered first.

- THE SECOND READING, from primary evidence. 1036-e5w9's rule is quoted in gate-stamp.sh as "THE FIX IS IN THE MEMO, NEVER IN THE DIGEST", citing 1034-ihxw reverted at 61a56bb3b. I read that revert's diff rather than the summary of it: what 1034-ihxw added to `compute` was a PER-FILE WORKTREE MODE READ (`[[ -x "$absolute" ]]` instead of the index bit), and gate-stamp.sh records in its own body that this construct DEADLOCKED on drvfs. The harm is a MECHANISM, not a location. Adding a glob to compute's skip list strictly REMOVES reads and cannot reach it. The rule the incident supports is "never add a per-file worktree mode read to compute"; "never touch the digest" is broader than the evidence and would forbid 930-i6x4 itself.
- COST MEASURED BEFORE RELYING ON IT, since plan_digest runs on the memo path: plan/issues is 594 files / 5.6 MB; plan-digest 10ms today, +12ms with it; the issue guard 11ms against an arm that costs seconds.
- TOP LEVEL ONLY is a decision: nested plan/issues files keep forcing a full gate, because the skip glob and the digest's -maxdepth must cover exactly the same set. Arm 3c holds them together.
- THE FIRST LAND REFUSED and was right about a rule while wrong about this case: test-gate-fast-refusals.sh (1087-h2z9) requires each hoisted guard to be invoked exactly once in build.sh, whose reason is that a duplicate runs the check twice per gate. The memoised-plan arm exits 0 before the hoisted phase, so its call runs INSTEAD of the hoisted one — meaning the rule as written forbade adding ANY guard to that lane, which is the wiring 1127-waxf requires. The counter now EXCISES that arm rather than exempting a match anywhere, with a control arm refusing an excision that excised nothing.
- Five mutations, each diff proven non-empty first, all red by name.
- TWO FALSE PASSES CAUGHT, both this milestone's class: arm 3c passed first time for the wrong reason (arm 4 had left a stamp with no plan_digest, so every later verdict was stale:no-plan-digest-recorded); and a mutation that did not apply printed 4 passed 0 failed from an unmutated tree — only the traceback stopped me reading it as evidence.
- unscoreable moved into verifiable_closure in the same commit, per the row's own instruction.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
