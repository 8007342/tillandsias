## Cycle 2026-08-23T16:40:27Z (linux-mutable macuahuitl — coordinator; hourly iteration: attestation-roster blind spot + branch affordance)

### THE GATE THAT CERTIFIED SILENCE
`check-mo-full-attestations.sh` enumerated `"$ledger_dir"/*.md` and therefore
validated the hosts that HAVE a ledger while saying nothing about the ones that
should. It reported `ok: 7 files 315 markers verified` while pirria — working,
pushing and closing packets for over a day — had never attested at all. A block
on pirria would have been undetectable: the only signal a coordinator has for a
dead host is a STALE attestation, and pirria had none to go stale.

Cross-referenced against `capability-matrix --hosts`, which is a real roster
because a host publishes a capability row in its first cycle (850-bif2). The
verdict now names silent hosts. REPORTED, NOT REFUSED, deliberately: failing
THIS host's push because a DIFFERENT host has not attested punishes the wrong
machine for something it cannot fix, and a host mid-first-cycle is legitimately
absent. Control: removing yoga's ledger names pirria AND yoga; restoring
returns to pirria alone, so it reads the roster rather than a hardcoded name.
Hermetic fixture still green.

### MY OWN DISCIPLINE FAILURE, WHICH IS WHY THIS FRAGMENT EXISTS
A fleet audit this cycle found macuahuitl has written NO loop_status record
since 2026-08-18 — roughly twelve attested cycles with a Finalization step
skipped every time. The coordinator's cycles have been the least observable in
the fleet through the sanctioned channel, while it audited everyone else's.
That is the same shape as the pirria gap one level up: attestation proves a
cycle ENDED, loop_status is the only place that says what it DID.

### BRANCH AFFORDANCE (operator rule: every error carries a recommendation)
Pushing to main was already impossible (server-side protection, order 476) but
nothing refused it LOCALLY, so the push crossed the network and returned a
GitHub rejection with no context. An in-forge agent lost a cycle to exactly
this — the forge seeds its branch from the HOST checkout's current branch
(lib-common.sh:3951, order 531), so a host parked on main pins every forge
after it. New v6 hook guard refuses locally and names the remedy plus the
seeding cause. Controls: main refuses rc=1 with remedy, linux-next passes, TAG
push passes (releases push tags and must not break), verified with real
`git push --dry-run`.

### HEARTBEAT
osx-next +4 and windows-next +5 both merged; post-merge integrity 94 fragments
intact, 16 events all landing. in_progress=3, expired=0. All six attesting
hosts within 4h; forge 93h (ephemeral, expected); pirria never. main is 728
behind linux-next and the release lane is circularly deadlocked — escalated to
the operator rather than worked around.
