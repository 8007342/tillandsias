## Cycle — lenovinha (linux_immutable, linux-next), 986-ahnc

CLOSED 986-ahnc at 386bc9ea6 — the packet I filed while closing 972-6vaj.
Validation now runs at run_agent_container_attached, the choke point every
attached agent launch passes through and the one the forge argv actually
takes.

THE DECISION, made explicitly because criterion 2 required it: REFUSE, not
log. Two production call sites, both forge launches already carrying the
envelope; the apparent third is inside `mod tests`, which I checked rather
than counted. The function already returns Result and both callers handle it.
And a test pins that the REAL builders produce a validating argv, so a false
positive fails the gate rather than someone's launch. The only way to reach
the refusal is to remove a hardening flag — the regression it exists to stop.

A DETAIL THAT WOULD HAVE REFUSED EVERY LAUNCH: "run" is prepended downstream
by run_container_attached_observed, so the `args` this function receives is
not what podman gets. Validating `args` directly returns MissingRunSubcommand
for every correct launch — a check that refuses everything looks exactly like
a check that works, until you run it.

CRITERION 3 EARNED ITS KEEP BY FAILING INFORMATIVELY FIRST. I removed
--cap-drop=ALL from build_stack_common_args, rebuilt, launched for real — and
it LAUNCHED. For a minute that read as my guard being dead. It was the guard
being RIGHT: the flag appears TWICE in the forge argv and the occurrence in
build_opencode_forge_args still carried it, so the argv was compliant and
there was nothing to refuse. Confirmed on the container that started: podman
reported the full expanded CapDrop set. Stopping at "removed a flag, launched
anyway, guard is broken" would have filed a false defect against my own
working code; stopping at the unit test would never have revealed the flag is
duplicated.

WITH BOTH REMOVED, a real `tillandsias . --opencode` refused before podman was
spawned, rc=1, no container created, missing flag named. Criterion satisfied
on the real path rather than reasoned about.

RESTORATION VERIFIED, not assumed, since I had deliberately damaged a security
path: diff against HEAD is 101 additions and ZERO removals, and a grep for
removed lines matching any of the four flags is empty.

Used scripts/land-on-platform-branch.sh for both landings — ok:land:60d19d7df
and ok:land:386bc9ea6, first attempt each, sha proven against the remote.

metrics:
  gate: TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check green
  live control: refusal observed on a real launch, both directions
  restoration: 101 additions, 0 removals, no flag lost
  not touched: 981-n5vx (same choke point, blocked on a fleet policy decision)
