## Cycle — lenovinha (linux_immutable) 2026-08-23T21:53:05Z — 658-kcd5 premise corrected, and a gate blind to half its own inputs

Full-mode cycle on lenovinha (linux_immutable, Silverblue, linux-next).
Batch: epic=convergence-velocity-milestone seed=lenovinha size=10 budget=10 route=rank:2/7 score=6.416.

GUARDS: daily-maintenance ok:current; cycle-preflight ok (services-down1 — the proxy, documented on 767-es4w two cycles ago, still down, not re-filed); credential ok:gh-keyring; branch ok:branch-linux-next; mcp ok:experts-healthy + ok:surface-exposed; capability row ok; boundary clean.

CLAIMED 658-kcd5 (ghost traces), released back to ready with a corrected premise rather than closed. TWO OF ITS FOUR EXIT CRITERIA WERE ALREADY MET, which the packet is worded as if they were not. Criterion 1 (repo-wide ghost check): met — scripts/trace-coverage.sh --gate is a shrink-only ratchet against openspec/ghost-trace-baseline.txt, wired into build.sh --check under 584-2qq2, failing in BOTH directions (new ghost, or a baseline entry that is no longer a ghost). Criterion 4 (negative control): met, and I ran it rather than reading it — appending a fabricated @trace to crates/tillandsias-plan/src/spec.rs makes the gate exit 1 and name the id; reverting restores exit 0 at baseline=9 current=9. Both exit codes captured directly, not read off a pipeline (last cycle's lesson, applied).

THE TITLE'S COUNT IS STALE TOO: 9 ghosts today, not 14. trace-coverage reports 9, all baselined; validate-traces reports 8 hard plus 6 covered by in-flight changes.

WHAT I DID NOT DO, AND WHY IT IS A JUDGEMENT NOT A BUDGET: criteria 2+3 are the disposition of the 9, and the criteria say "prefer removal" on the argument that a trace nobody wrote a spec for in months is evidence the spec was not needed. That argument fits the low-fanout six and does NOT fit the two largest. spec:meta-orchestration is traced from ~15 litmus tests plus methodology/mo-full-attestation.yaml — the loop this fleet runs on; deleting those traces would remove the only machine-readable evidence that those litmus tests are about anything. spec:enclave-service-catalog has 6+ real sites in headless main.rs. For both, missing-spec reads as a spec never written for something central, not as a concept nobody needed. Bulk-deleting unattended on an argument that does not fit them is not a reduction, so the disposition is now an explicit decision in next_action with the safe six named to go first.

FILED 867-vd4z, which is bigger than the packet that surfaced it. The ghost gate greps six file types and misses *.yaml (979 @trace annotations) and *.md (1347). Gated: ~1876. Ungated: ~2326. MORE THAN HALF THE REPOSITORY'S TRACE ANNOTATIONS SIT OUTSIDE THE CHECK THAT VALIDATES THEM, and the coverage number the gate reports is computed over the minority. At least seven REAL ghosts hide in yaml: browser-isolation-telemetry, control-socket, cross-platform-compilation, enclave-network-isolation, ephemeral-guarantee, forge-launch, service-startup — and openspec/litmus-tests/*.yaml is exactly where litmus tests bind to specs, so these are the bindings the convergence argument leans on. The naive glob fix BREAKS THE BUILD FLEET-WIDE: the same scan finds foo, a, does-not-exist, not-a-real-spec, zzz-definitely-no-such-spec-xyz — deliberate negative-control fixtures — so a fixture-exclusion rule has to come first. Recorded in the packet.

THE DEFECT UNDER THE DEFECT, also in 867-vd4z: three ghost checkers with three different file sets — pre-commit hook (rs sh toml), validate-traces.sh (rs sh toml nix), trace-coverage.sh (those plus ps1 and Containerfile*). They disagree on live data today, 9 vs 8; the missing one is spec:forge-skills-mapping, whose only reference is images/default/Containerfile:195. A Windows contributor adding a ghost trace to a .ps1 gets no feedback from the commit hook at all — it surfaces later, on someone else's build.

FRESHNESS: scripts/resolve-smoke-release.sh, verdict=refreshed, exercised live — daily resolves v0.4.260817.1, stable v0.4.260810.1 (correctly older; stable trails daily), bogus channel refuses with exit 1. All three match the declared grammar. GAP RECORDED IN THE STAMP rather than as a new row: this resolver has no executable fixture, unlike its two siblings (5/5 and 6/6), and litmus:smoke-release-channel-shape pins it by grepping for SOURCE STRINGS. That constrains the text, not the behaviour — a correct refactor turns it red and a behavioural regression that keeps the strings passes. ci-release litmus suite PASS after the stamp.

ADVISORY: compaction not eligible (19 fragments). Deslop not due (order=867 last=834 delta=33 hours=92). triage ungrouped=23 of 166. flow overhead_ratio=3.92 over 11 cycles.
