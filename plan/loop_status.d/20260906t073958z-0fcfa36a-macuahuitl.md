## Cycle — macuahuitl hourly coordination (cron lane)

SIBLINGS MERGED: windows-next (+2), osx-next (+11, over D_max=5). Zero
conflicts. One non-plan file: macneo's litmus-covering-specs.sh separated-stream
fix. UNION LITMUS 290/292, 100% coverage, two failures, both attributed:
credential-channel-check is macbookair's 1091-h3cb verdict change meeting a
linux-next expectation, their fix on land attempt 4; and answer-rate step 15/16,
which is no longer unmeasured — see below.

TWO RELAY REFS LOOKED STRANDED AND WERE NOT. `merge-base --is-ancestor` reported
work/1069-c9w6 (+2) and work/1087-h2z9 (+1) as NOT MERGED; both are
content-landed. 1087's commit landed as d505f66fc under the same subject and
trunk then IMPROVED its fixture (119 -> 151 lines), so merging would have
REGRESSED it; 1069's subjects are both on trunk and its marker appears there
three times. Both merges CONFLICTED, which is the only reason I looked — a clean
merge would have landed a regression while I believed I was rescuing work.
Recorded as `ancestry_is_not_evidence_that_work_is_unlanded`, refined by
lenovinha: a POSITIVE answer is conclusive, a NEGATIVE has three causes. The
other twelve relay refs are genuinely merged.

THE 1001-q3zf AUDIT RULE FIRED FOR THE FIRST TIME, because I ran it properly —
last cycle I reported the stem count and never read the skippable lines, a green
gate that ran no tests inside my own audit. ONE step intersects two hosts:
831-ezea, yoga runs=150 avg=17048, macbookair runs=148 avg=24668, fail_pct=0.
Filed 1103-r7wq with three caveats supplied by the hosts, not by me:
saved_ms_upper is a BOUND; macbookair's build-check runs fail_pct=34 over 225
runs so "zero failures" must not generalise; and their averages come from a
session forcing checks 291:73, apparatus inside the measurement being used to
justify a change. I filed on TWO steps and cut it to one — macbookair re-pasted
before I landed and their top-three had moved within one cycle. Ten of twelve
stems are not pasting at all, mine included.

LAST CYCLE'S UNMEASURED TIMEOUT RESOLVED, and withholding it was right. I
refused to publish answer-rate step 15/16 last cycle because I had run fixtures
in a worktree during the window. This cycle I had not, and it reproduces:
cpu.pressure 5943us vs 5896us, adjudicator saying NOT contended both times. The
cause is the input, measured now: the timing log is 77,578 lines / 12.3 MB and
cycle-metrics.sh takes 12,905ms on it — 65% of the step's whole 20s budget for
ONE invocation, and the step runs it more than once. The budget was written when
the log was small; the log is append-only and nothing prunes it, so this reaches
every host eventually and arrives as a TIMEOUT that reads like a slow machine.
Filed 1105-h8vr, whose next_action refuses a bare budget raise.

AND macuahuitl IS A SECOND HOST FOR 1096-p3tn: /tmp/tillandsias-timing.jsonl
(1,148 lines) sits alongside the canonical 77,578-line log here too. Found
incidentally while measuring the above — neither host went looking, and no check
reports it. Two of two hosts examined.

SWEEP: live=9 unclaimed=0 expired=1 held=0. The expire-candidate was MINE
(1025-a896, held past 24h across two cycles) and I RELEASED it rather than
refreshing: it is blocked on an operator decision about the OAuth pool, and a
live claim against a human decision conflates "in progress" with "waiting",
making the packet invisible to anyone who could advance its unblocked parts.

HEARTBEAT: six peers idle, yolanda requires_action. Loop-status timestamps are
stale for hosts I know are live from this session's own exchanges, so idleness
was established by asking, not by reading mtimes. Nobody starved; every host
that answered had work or finished work.

lenovinha ASKED WHETHER THEY HOLD 1071-adhj AND THEY DID — claimed, finished,
relayed, and invisible to them: completion fragments under the `linux` BUCKET
(1012-hu7d), and check-claims-across-branches reflecting their own claim back as
a sibling's. They have since closed it at 061eb97e0.

ROUTED: both guard defects to lenovinha as ONE pass — their 1093-hzhi (events
entry counted as a packet row: row-set too large) and my 1101-b5rc (staged row
never enumerated: candidate-set too small). Same file, opposite directions.
Their intersection arm is the one I would have missed. I hit 1093-hzhi myself
and worked around it by splitting a fragment, which then tripped the
closure-evidence guard — two guards with incompatible expectations, where the
workaround for one is a violation of the other.

FILED: 1102-e4tk (pirria's — a lock held by another user's LIVE process reads as
dead and is stolen, because `kill -0` returns EPERM for a foreign uid; theft
reproduced end to end). 1104-w9np at p1 (lenovinha's own claim reflected back at
them by check-claims-across-branches as a sibling's, keeping finished work
stranded a day — and it INTENSIFIES with every sibling merge I perform).
CORRECTED 1099-t4xr, premise false: plan_status is not broken, the parameter is
`reference`. What survives is better — four of five reference-taking MCP tools
lack their sibling's hardening, so a wrong parameter name yields a LOOKUP MISS
rather than an error naming it; and plan_status answers from the local fold with
no staleness signal, returning in_progress to a host 7 commits behind while
returning done here at the same moment. macbookair extends it: the CLI reads the
same fold, and they used it as a verification step all session.

OPERATOR QUEUE, plain asks: pirria still cannot push (credential.helper unset in
all three scopes — four packets came by hand relay tonight); yolanda's vmmemWSL
at 7.3/15.5 GiB kills lands and reads as a gate failure; the OAuth all-or-none
PAT decision; and the 03:23:01Z-04:22:15Z security log before any fourth host.
