## Cycle 2026-08-25T09:43Z (linux_immutable yoga — two dispositions, and the honest limit of yesterday's migration)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `loop-cron`. Guards green, `services-ok`, daily gate current (stamped this morning), de-slop not due, lock taken and released, boundary clean both ends.
- **Compaction**: 32 fragments folded. +690/-29 on the base; every deleted line is an LWW field fold paired with its replacement (status values and one `next_action` rewrite), `packet_id` count rises 596 → 600, integrity 591 packets sound. Audited rather than assumed, because the one property worth protecting in a fold is that nothing disappears.
- **795-5itp: two of the four remaining sites DISPOSITIONED**, under criterion 1's second branch. Both blockers re-verified at HEAD rather than inherited from slice 1's list — a disposition that repeats a claim nobody rechecked is how a wrong reason becomes permanent.
- **`tray/mod.rs` — KEEP.** Verified: blocking `std::os::unix::net::UnixStream`, thread per connection, and a subscriber registry of `Arc<Mutex<Vec<Arc<Mutex<UnixStream>>>>>` that `broadcast_control_envelope` locks and writes to synchronously. `Framed` needs tokio `AsyncRead + AsyncWrite`; adopting it means moving the tray onto a runtime, which the packet declines and I agree with. **And the part that changes what is left rather than excusing it**: this site's residual is READABILITY ONLY — the bound is already the shared `MAX_MESSAGE_BYTES` with the same refusal message, both directions since 828-r2ek. The packet's headline argument (nine bounds policies, nine chances to get the maximum wrong) does not apply here.
- **`tests/vsock_listener_e2e.rs` — KEEP, as an exception-list entry, and I am correcting myself.** Last cycle I recorded that this file "should migrate". True while `vsock_server` framed by hand; false the moment it did not. It is now the only end-to-end peer talking to the migrated server over a real vsock socket, so framing by hand here is what makes the interop claim mean anything — migrate it and the e2e asserts the codec against itself. An exception list is worth nothing if its entries carry reasons that have quietly expired.
- **Both reasons are written AT THE CODE**, not only in the ledger, so the next reader meets them where the decision would be reversed.
- **THE WEAKEST POINT OF YESTERDAY'S SLICE 3, said plainly.** That e2e is `#[ignore]`d behind the `vsock_loopback` kernel module. It is not loaded here (`lsmod` empty for vsock, no `/dev/vsock`; the module exists at `/lib/modules/7.1.9-200.fc44.x86_64/.../vsock_loopback.ko.xz`), and `sudo modprobe` wants a password an unattended cycle cannot supply. **So the Framed migration of `vsock_server` is verified over `tokio::io::duplex` and 484 in-crate tests, and has NEVER run over a real vsock socket.** Duplex is an in-memory pipe; it cannot reproduce vsock's read granularity, and read granularity is exactly what the handshake-handoff buffering hazard turns on. One `sudo modprobe vsock_loopback` on any Linux host closes that gap. Until then, treat slice 3's real-transport behaviour as untested rather than tested.
- **Remaining on other hosts' lanes**: `pty_vsock_bridge.rs` (macOS; its :241 drops an oversize message and keeps the session alive, and a Sink error is terminal, so a naive swap kills the PTY session) and `hvsocket.rs` (Windows; no production callers). With those dispositioned or migrated, criterion 1 is dischargeable and the closure litmus becomes writable against a real exception list instead of a wish.
- **Stranded, third cycle running**: 831-ezea and 865-n8vq. `expire-claims` reports `in_progress=2 expired=0 held=0 unknown_age=0` — none expired, so both are inside the 24h TTL and not abandoned. Worth one eyebrow: `in_progress=2` with `held=0` and `expired=0` does not obviously add up, and if those categories are meant to partition the population then one of them is mis-counting. Reported, not chased.
