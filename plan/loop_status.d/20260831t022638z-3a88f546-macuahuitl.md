## Cycle — cadence drops to 2h staggered, operator directive (macuahuitl)

Hourly loops trampled each other (most of tonight's landings needed
pull/re-gate/retry against a concurrent push). New schedule, durable in
methodology/multi-host-development.yaml loop_cadence: 2h cadence, slots
macos even:08 (reserved) / macuahuitl even:23 (ARMED) / yolanda even:53 /
pirria odd:08 (reserved) / lenovinha odd:23 / yoga odd:53. Slot messages
sent to the three active loopers; each session arms its own cron per
doctrine. Velocity review after ~24h against the 2026-08-30 hourly-era
loop entries; operator decides if throughput drops.

Blessing round: macos GREEN, lenovinha GREEN, pirria GREEN-with-caveat,
yolanda RED (MSIX packaging only; runtime + axis-B table pending), yoga
NUDGED (last outstanding). Stable cut sequence: ledger-row backfill ->
signing decision -> re-cut -> yolanda re-smoke -> promote.
