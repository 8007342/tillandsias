## Cycle — macuahuitl 2026-09-04T08:45Z (meta-orchestration, full mode; session cron 9 1-23/4; linux_mutable coordinator)

- **Start Of Cycle green:** preflight ok (rebuilt+expert-refreshed, services-down2 and ca-absent at start), credential ok:gh-keyring-push-verified, branch linux-next, experts healthy, checkout lock acquired (session-cron), boundary snapshot BEFORE any write, opsx ok:clean-tree, claims 0/0/0/0, daily maintenance current, capability row current, scratchpad ok, scheduler `due:not-installed` (session crons carry the cadence).
- **998-3z6g CLOSED on a second regime, here.** The proxy had sat Exited for 4 h binding the reboot-swept /tmp CA path. One command, `tillandsias --ensure-enclave --debug` (new, order 1004-xw3q), 10 s: Vault bootstrapped, the bundle regenerated at ~/.local/state/tillandsias/ca (key 600), the secret refreshed, the proxy RECREATED binding the new path, healthy in 4 s, ca-consistency ok. No hand-repair. The exited inference container still binds the old path and is recreated at the next lane launch, as the migration note says.
- **1004-xw3q CLOSED:** every remedy that named `tillandsias --init` as the orchestration now names `--ensure-enclave` and says --init only builds images. The first draft of the flag mirrored `--status-check`'s dispatch, which sits INSIDE `if init`, so the flag alone fell through into the app's service path and idled until the shutdown handler stopped Vault — three bounded runs with no podman call logged before the dispatch moved to its own returning block. A lesson for the next flag: `--status-check` needs `--init` to dispatch at all.
- **The enclave-health fixture is hermetic** (acting arm stubs the CA-consistency check; scenario 20 keeps the failing stub): 20/20 on a host whose live CA was desynced, so litmus:enclave-service-health-shape is green on the trunk again (red since 8acc30de2). runtime-diagnostics instant PASS 4 FAIL 0.
- **1004-j4p4 implemented (partial):** traces= now derives from trace-coverage.sh --gate (current here); the 765-mza8 citation criterion stays open. 1001-q3zf closed (landed last cycle).
- **Filed:** 1007-3ajn (the local build counter resets every install on a platform branch: build.sh's own guidance is bump, install, revert, so two installs a day carry the same string), 1008-85ry (the first two-host recurrence finding: the same three gate steps top skippable: on macuahuitl and macbookair with fail_pct=0 over 308 and 103 runs; memoise them on declared inputs, prove with each step's negative control run WITH the memo active).
- **Batch triage:** `select-work-batch.sh linux --seed macuahuitl --budget 6` drew architecture-audit-epic (pick=3/3, urgent=1001-q3zf which was closed; then 335/245/251, old unscored audit rows). Took the urgent item and the coordinator's assigned enclave work instead of the three audit rows; recorded as a deviation with its reason.
- **Install:** `./build.sh --install` was killed at the tool's 10-minute cap during the musl launcher build (rc=137; it also took the Vault and builder containers with it). The installed binary is still the release v56.9.2.1; the closure ran with the tree binary at HEAD. The install goes FIRST next cycle, detached, bump reverted after (1007-3ajn).
- **E2E:** local-build gate `skip:live-runtime-present` (recorded, not re-litigated). **Release:** NOT this cycle — no PR in flight, latest v56.9.2.1, but the pre-build instant suite carried two pre-existing reds at cycle start (tracked qcow2 fixture 1006-jfwv on the macOS lane; the enclave-health fixture, fixed here). With this landing one red remains (1006-jfwv). Release decision next cycle if it is cleared.
- **Carried:** 1001-i5ux deployed-hook verification at a running mirror (recipe on the packet); the nix-lane red (litmus:guest-binary-embed-integrity, ci-release) not re-run this cycle. Compaction: fragments folded into the base (941-trcf advisory), see the commit.
- **Fleet this hour:** yolanda slice 2 landed and attested (mount gone; slice 3 Windows half next, host-shell held for a coordinated pass); macbookair 1001-i5ux landed (mirror pre-receive guard; the routine merge whose resolution drops trunk files is the likelier loss path than a force push); lenovinha 1003-v3dc attested (spec index rung 4 in .cache; the ladder has FOUR carriers and the shell agreement guard sees three — a guard that pins N carriers says nothing about one it cannot read); a load-bearing-comment rule from yolanda (a comment a test splits on is source; grep the litmus corpus before deleting it).
- **Operator queue, plain asks, unchanged:** gh login on ESMERALDINHA and macneo; pirria's rustup; 978-juw4 permission; 996-bcjx; the ten quarantined trees.
- **Cycle metrics, verbatim:**
  experts: calls=8 answered=8 unsupported=0 degraded=0 errors=0 answer_rate=100% tools=check,ready source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
  experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
  mcp: servers=1 per_server=cli=8 legacy_untagged=0 health=ok source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
  expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
  flow: cycles=14 avg_completed_per_cycle=0.57 avg_commits_per_cycle=5.86 overhead_ratio=10.25 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
  timing: steps=50203 build_check_ms_avg=74024 build_check_mix=mixed:forced=476,memoised=150 litmus_ms_avg=32372 slowest=check:litmus-pre-build:1778084 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
  repeat: window=since=2026-09-04T08:45:43Z steps=24 top3=litmus-suite=3,litmus:agent-identity-canonical-source-shape=2,litmus:append-event-rejects-unknown-flags-shape=2 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
  recur: window=7d runs=46626 steps=544 top3=build-check:runs=415:total_ms=32893171:avg_ms=79260:fail_pct=26,check:litmus-pre-build:runs=25:total_ms=18608282:avg_ms=744331:fail_pct=44,local-ci-phase-pre-build:runs=25:total_ms=18607423:avg_ms=744296:fail_pct=44 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
  skippable: candidates=72 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=308:avg_ms=15288:fail_pct=0:saved_ms_upper=4693608,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=308:avg_ms=13638:fail_pct=0:saved_ms_upper=4187164,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=308:avg_ms=10827:fail_pct=0:saved_ms_upper=3324025 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
  plan: packets=675 ready=443 plan_bin=./target/release/tillandsias-plan
  repo: commits_this_cycle=- worktree=dirty traces=current
  verdict: attention:worktree-dirty
