## Cycle 2026-08-26T09:32Z — macuahuitl (linux_mutable, linux-next), hourly /loop

VERIFIED THE URGENT BATCH HEAD RATHER THAN TAKING IT, and it was half done.

626-w3fn (p1, urgent head, estimated 5h): surface (a) — sign-in preflight
silence — has been FIXED SINCE 9a244471c on 2026-08-09. run_provider_login now
carries an unconditional `step()` progress emitter, gated to Terminal mode so
the --with-token automation lane stays clean (a refinement the packet did not
propose), with the field report cited inline. That is materially the packet's
own proposed text for (a). Surface (b) is still open — emit_launch_event still
opens `if !enabled { return; }`.

BOTH ITS "VERIFIED" CLAIMS HAD AGED, IN DIFFERENT WAYS:
  (a) "exactly ONE unconditional println!" -> now three, and the thing that
      falsified it IS the fix.
  (b) cited client.rs:1849 -> behaviour real but now at 2001-2010; 1849 is
      unrelated code, a ~152-line drift. That is 881-29me (cite by SYMBOL, not
      line) and it is the more dangerous of the two: a reader who checks 1849,
      finds nothing, and concludes the packet is wrong would discard a claim
      that still holds.

Not claimed — (b)'s deliverable is user-facing terminal text under spec:tray-ux
curation governance, which is Tlatoani-gated. Recorded the correction so the
next taker does not re-implement (a) or spend five hours on a stale premise.

STRANDED FLAGGED A FALSE ALARM AND I READ THE SCRIPT RATHER THAN THE NUMBER.
628-p5tj showed `stranded`, but yoga had claimed it at 09:28:03Z — four minutes
before my sweep. The criterion is `in_progress` with ZERO
progress/completed/blocked events, and `filed`/`claim` deliberately do not
count as activity. So every fresh claim reads as stranded until its owner
records progress. `threshold_events=0` is the actionable signal. The count is
meaningful ACROSS cycles (rising = claims outliving their cycles), never within
one — a cycle that catches a sibling mid-claim will always see it.

SENT YOGA A RETROACTIVE HEADS-UP: they claimed 628-p5tj (tray-menu-builder
convergence) and I had edited crates/tillandsias-headless/src/tray/mod.rs two
cycles ago — the CloudRefreshReply outcome propagation, and a relocation of
build_menu's documentation, which includes the section titled "## Convergence
with the shared builder (order 628-p5tj)". Their packet's own convergence note
now sits on the function their packet is about, and it holds the
operator-approval requirement and item-count contract their exit criterion 1
asks them to preserve. Better they know before starting than at merge.

Guards green, lock clean, boundary preserved, siblings 0 ahead both branches.
