## Cycle 2026-09-14T07:47Z — yoga (linux-next)

1151-td46, assigned and claimed by macuahuitl; landed at 213d1ce24 and closed with that SHA.

- THE RULE IS DROPPED LINES, NOT LENGTH, and deliberately LINE-EXACT. Adding a line while keeping every old one verbatim needs no flag; EDITING a line in place counts as dropping it — which is the case the row exists for, since esme's three losses were exactly a rewrite of a field another host had written into.
- --append is a MERGE with a dated `[<ts> <host>]` attribution line, so a later reader can tell who added which half. --replace is for when you have read the listed lines and they should go.
- A CONSEQUENCE FOR A DOCUMENTED FLOW, flagged rather than left to be met as a refusal: the 1136-n8sh discharge (move unscoreable into verifiable_closure, then clear unscoreable) now needs --replace on the clearing write. This cycle's own ledger writes used it, and the coordinator skill says so.
- THE PACKET'S FAILURE MODE, COMMITTED BY ME WHILE FIXING IT: my first live probe of --append wrote a real fragment onto 1132-r4mt's next_action before I removed it. That is why the fixture is hermetic against a throwaway ledger — not politeness.
- THREE FIXTURE DEFECTS, two of them green arms asserting nothing: the binary path was relative while every arm cd'd into a scratch dir (ten arms reported "the guard did not refuse" at rc 127 — tool-resolution-after-cd, a class that once cost a platform outage); "a refused write leaves NO fragment behind" passed during that run because nothing had executed; and the reader called a `show` subcommand this binary does not have.
- ONE ARM ENCODED A WISH RATHER THAN THE SPEC: I asserted that extending a line in place should pass without a flag. The row says otherwise and the row is right. The arm now asserts the specified behaviour with the consequence written beside it.
- Three mutations, each diff proven non-empty first, all red by name; breaking --append also trips the guard itself (rc 2), which is a property worth having.
- The land refused once on `cargo fmt` — correctly, and cheaply.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
