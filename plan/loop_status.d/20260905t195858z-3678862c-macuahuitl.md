## Cycle 2026-09-05T20:00Z — macuahuitl-fedora hourly coordination (cron lane)

LastExecutionTime 2026-09-05T20:00Z UTC. Scheduler posture: hourly coordinate
:41 and 4h meta :09 armed on this host; no cron changes made, none requested.

SIBLING AUDIT: NOTHING TO MERGE, first fully converged pass today.
  main          ahead=0  behind=379   (release lane, PR only; the meta cycle's duty)
  windows-next  ahead=0  behind=33
  osx-next      ahead=0  behind=36
Seven relay refs on origin; six are at ahead=0. work/1069-c9w6 reads ahead=2,
and it is NOT new work: both commits are already on trunk under rebased SHAs,
confirmed by patch-id (b555b2181 -> 3a79de3eb, 345acb156 -> aee00cdca). A stale
relay ref whose drift count lies is worth knowing about — the count says 2 and
the content says 0.

THE CYCLE'S FINDING, and it is p1. A CLAIM IS TWO WRITES AND HOSTS DO ONE.
`append-event --type claim` records an event keyed by packet_id; `set-field
status in_progress` is a SEPARATE call and is the only thing the fold reads for
status. Found by asking why esmeraldinha's 1072-b7eq read `ready` ninety minutes
after esmeraldinha claimed it to me in a message.

  Corpus, measured: 8 packets carry a `type: claim` event. 4 are completed
  (healthy). FOUR ARE `ready` RIGHT NOW WHILE A HOST HOLDS THEM:
    jq-crlf-on-windows-turns-shell-reads-into-silent-wrong-answers   yolanda 12:59Z
    windows-lane-sends-a-verbatim-argv-and-drops-its-escaping-...    yolanda 12:29Z
    guest-pulls-full-multiarch-qemu-user-static                      esme 17:11Z
    every-host-appends-its-gate-step-to-the-same-region (1072-b7eq)  esme 18:17Z

  And the two instruments disagree in the worst possible direction:
    expire-claims --dry-run --list-live  ->  0 of 4 appear
    tillandsias-plan ready               ->  4 of 4 offered

The coordinator cannot see them held and every host is being handed them. Up to
seven hours, two hosts, both Windows — if the worker skill's Windows path shows
one command where the Linux path shows two, that is the cause rather than the
symptom, and 1079-qb8k's next_action requires measuring before choosing a
remedy. Filed p1. I did NOT write the transitions for the hosts: neither is
credential-blocked, and a coordinator relay would misattribute their claims.

317 CORRECTED ready -> blocked. macbookair released it at 18:05Z with an
accurate reason — the cold-cache slice is done and the boundary is now an
orchestrated forge launch needing operator-authorised proxy and vault
credentials. The reasoning was right and the mechanics left the row misleading:
the blocker was in a `note` summary, which the SELECTOR DOES NOT READ, so 317
sat at `ready` and was offered 1 of 1. This is methodology/distributed-work's
`released` rule arriving as its own first violation, hours after it landed, and
nothing in the tool refuses a release that strands a packet. Now 0 of 1 offered.

MY OWN AUDIT SCRIPT SHIPPED WITH THE DEFECT IT WAS WRITTEN TO FIX, and this pass
caught it. scripts/loop-status-metrics-audit.sh anchored on `skippable:
candidates=`; the very entry DESCRIBING that anchor, in backticks, satisfied it,
so macuahuitl reported as pasting with an empty top3. A guard a comment can
satisfy is satisfied by the history of the thing rather than the thing — inside
the fix for that shape. Now matched on the FULL emitted form
(candidates/floor_ms/min_runs/top3). A line anchor would have been wrong:
measured, `^skippable:` matches 2 of 4 pasting hosts because lenovinha and yoga
indent their block. The script also now distinguishes NOT-PASTING from
last-paste=<ts>, so a cycle that ends with a short closing note is
distinguishable from a host that stopped: yolanda last pasted 20260905t033417z,
macuahuitl 20260905t184703z, and pirria/calmecacpilli never.

CLAIM SWEEP (--dry-run --list-live): live=5 unclaimed=1 expired=0 held=0
unknown_age=0. The unclaimed row is 1025-a896, operator-blocked, self-explaining
since yoga's 1065-4t7t. Note the sweep's own blind spot, now filed: the four
claim-event rows above are NOT in these numbers.

ASSIGNMENT BOARD. yoga -> 1033-iycs. pirria -> 1074-96z9 (relay). macneo ->
1055-e8ie (relay). macbookair -> 1072-qk43. lenovinha -> 1063-nraf continuing
plus 1077-vzwq. yolanda -> jq sweep landing. esme -> 1072-b7eq, plus 1076-kft9
offered if that closes out. calmecacpilli -> the operator's own session,
declined runtime work correctly and is not dispatchable by me.
UNCLAIMED AND READY: 1075-yuxt, 1076-kft9, 1077-vzwq, 1079-qb8k. No idle host to
take them — every session that reads idle holds a live claim, and idleness is
established by asking, not by a session state.

CONVERGENCE: Vc positive. One packet filed (1079-qb8k), one status corrected
(317), one instrument fixed, zero sibling drift. High-Velocity Alignment Event:
INACTIVE.

EXPECTED NEXT LOOP: four set-field transitions from yolanda and esme; esme's
gate-21 outcome and the work/1072-b7eq ref; macneo's cold-provision forensics,
which now need a Linux mount for two preserved images; lenovinha's two cheap
bindings on 1077-vzwq before any new estimate.

CYCLE METRICS, verbatim:

```
experts: calls=8 answered=8 unsupported=0 degraded=0 errors=0 answer_rate=100% tools=check,ready source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=1 per_server=cli=8 legacy_untagged=0 health=ok source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
flow: cycles=17 avg_completed_per_cycle=0.76 avg_commits_per_cycle=5.47 overhead_ratio=7.15 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
timing: steps=70080 build_check_ms_avg=90274 build_check_mix=mixed:forced=583,memoised=198 litmus_ms_avg=41086 slowest=check:litmus-pre-build:1967661 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=3h steps=446 top3=litmus-suite=32,build-preamble=22,build-check=14 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=60189 steps=591 top3=build-check:runs=478:total_ms=46619393:avg_ms=97530:fail_pct=28,litmus-suite:runs=654:total_ms=30556998:avg_ms=46723:fail_pct=60,check:litmus-pre-build:runs=29:total_ms=23778529:avg_ms=819949:fail_pct=45 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=79 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=343:avg_ms=15726:fail_pct=0:saved_ms_upper=5378353,step:running-workspace-tests-cargo-test-workspace-all-targets:runs=61:avg_ms=78161:fail_pct=0:saved_ms_upper=4689683,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=343:avg_ms=12966:fail_pct=0:saved_ms_upper=4434472 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=778 ready=470 plan_bin=./target/release/tillandsias-plan
repo: commits_this_cycle=- worktree=dirty traces=current
verdict: attention:worktree-dirty
```
