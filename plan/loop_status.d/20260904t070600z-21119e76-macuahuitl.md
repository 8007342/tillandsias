## Cycle — macuahuitl 2026-09-04T07:06Z→08:30Z (coordinate-multihost-work, cron :41, first post-restart cycle)

- **Fleet restarted and re-armed.** Seven hosts reported and received directives
  by SendMessage; every host runs `advance-work-from-plan` on a session cron,
  staggered: lenovinha `5 */2`, yoga `25 */2`, yolanda `45 */2`, macbookair
  `15 1-23/2`, macneo `35 1-23/2`, esme `55 */4`, pirria `40 2-22/4` (local).
  macuahuitl: coordinate hourly `:41`, meta `9 1-23/4` carrying the daily
  release. Scheduler line here: `due:not-installed` (session crons are the
  mechanism this restart). The write half of this cycle waited for the
  1001-q3zf gate to release the worktree; the audit ran at 07:06Z read-only.
- **Merged this cycle:** `osx-next` (+10, above D_max: 1000-rqmx client
  guard, 830-xsk2 host relay, 1002-9xmb, attestations) and `windows-next`
  (+4: 997-e4v2 tmpfs slice, both yolanda capability rows, 1001-fmbw) into
  linux-next, zero conflicts, litmus instant pre-build on the union, one gate.
  Landed with it: 1001-q3zf (repeat:/recur:/skippable: cycle-metrics lines,
  operator ask; on this host the top skippable candidate is one gate step run
  304 times at 15 s that never fails), the worker skill's boundary-first rule,
  the macOS build skill's `env -i` launch method, the smoke skill's §4b
  expected-trace note, esme's and macneo's report files, macneo's first
  capability row, and eighteen packets filed from the fleet's findings
  (orders 1004-* and 1005-*; see the ledger).
- **Claims 0/0/0/0, stranded 0.** Heartbeat 3/0/0/6/0 misreads two hosts and
  I established it by asking: lenovinha and yolanda were mid-cycle; esme and
  pirria measure only (dead keyring token; no plan binary). Worker cycles
  without a boundary snapshot read WEDGED here, which the skill edit fixes.
- **Landed by hosts tonight:** 520 closed by measurement (Vulkan lane on
  gfx1152; images older than 32dc4278c are silently CPU-only on AMD, relayed
  to lenovinha and the Windows twins); 1002-7jeg closed (present-but-unusable
  devices are the bring-up lane; five npu-tagged packets reachable again;
  yoga's first diagnosis was wrong and is recorded as disproved); 1003-444f
  filed (gate names every workspace crate's tests, enumeration derived);
  830-xsk2 host relay (the 08-29 accept-proof overstated the state: the
  channel was never read); 1002-9xmb (the Mac's HOME baked into the guest CA
  preamble, succeeding silently as root); 997-e4v2 tmpfs slice (compute_hot_
  budget has a caller; mount slice next); 917-6iwv first graded RAG number
  (31/33; both misses cite a cheatsheet over the normative source); pirria's
  v56.9.2.1 floor smoke PASS end to end, pushed through the plan-only lane.
- **Closed here on pasted evidence:** 980-xcaf (criterion 4 met on a cold
  substrate under the minimal PATH, macneo) and 1000-rqmx (author decision:
  closes on the client half; 1001-i5ux carries literal criterion 1).
  Retargeted to v0.5: 657-zm2n (the 8 GB Mac's row reports no RAM, no
  battery, nothing schedulable) and 917-n3n9 (operator direction: the
  accelerators get used). 804-deux's ~2.47 GB is unverified in magnitude
  (514 MB cold on a never-populated image; wall clock 64 s and 357 s).
- **Fleet defects found tonight, all filed:** the credential guard's store
  arm is green on file presence (esme: green beside a push refused with
  "Invalid username or token"); cycle-preflight declares cargo-absent before
  looking for a runnable binary; the smoke runbook assumes bash (PIPESTATUS
  void under zsh) and has no Windows §3 block; its ledger-row awk is dead two
  ways (`$0` substituted by the skill argument; gawk 5.4 fatal); podman reset
  leaves the CA dir; Windows Ready is not durable after --provision-once and
  --diagnose cannot tell idled-out from failed (macOS twin on 980-ja2m: a
  dead VM differs from a live one in one field, both exit 0; crashloop.state
  says ready on a corpse); the 804-ckst cmdkey verifier is always true;
  loop-status-append reads stdin and blocks forever on a path argument; the
  enclave health check is satisfied by `podman rm`; `tillandsias --init`
  does not orchestrate the enclave and the CA check moved before the runtime
  (998-3z6g residual, this host's, in the 01:09 meta cycle after a fresh
  install); the spec-index delta reuses nothing and starves the floor's gate;
  check-host-tools measures the operator's environment (three instances, the
  macOS tray build has no declared prerequisite set: zig, cargo-zigbuild,
  x86_64 musl target, walked on macneo under a stop rule); the scorable-
  obligation grammar lacks a cargo-test form and a forward-reference form.
- **Spec-index caution (lenovinha):** rung 4 falls into target/ on every
  rootless-podman host and `cargo clean` removes it. **No build-cache sweep
  on any host with a published index until the move to .cache lands.**
- **Operator queue, plain asks:** (1) `gh auth login` on ESMERALDINHA and
  `gh auth refresh -h github.com` on macneo, both interactive, then reverify;
  no PAT exists on either. (2) 978-juw4 permission (unchanged). (3) 996-bcjx
  identity call (unchanged). (4) delete the ten quarantined trees on
  esmeraldinha (unchanged). (5) pirria's Rust toolchain vanished after
  2026-09-03T00:26Z: reinstall rustup so the plan instrument rebuilds;
  whether the floor runs experts at all waits on the reused=0 measurement.
  (6) FYI: the coordinator authorised `brew install zig`, `cargo install
  cargo-zigbuild`, `rustup target add x86_64-unknown-linux-musl` and one
  destructive VM re-provision on macneo, each named by the build script's
  own remedy or the e2e skill, under a stated stop rule.
- **Velocity:** check-convergence-velocity reads unavailable; residual moved
  by three closures (520, 1002-7jeg, 980-xcaf, 1000-rqmx) against twenty
  filings. Nothing is blocked that a host can unblock; two hosts wait on the
  operator's login, one on a toolchain.
- **Litmus:** litmus instant pre-build on the merged union: still running at record time (271 specs executed, log in the coordinator scratchpad); counts in the next entry.
