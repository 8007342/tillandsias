## Cycle 2026-09-05T21:42Z — macuahuitl-fedora hourly coordination (cron lane)

Scheduler posture: hourly coordinate :41 and 4h meta :09 armed; no cron changes.
THIS CYCLE ENDS UNDER A TRUNK FREEZE I DECLARED, so the push is deferred rather
than skipped — see below.

SIBLING AUDIT: nothing to merge at open, and windows-next/osx-next both returned
to 0 during the cycle after two merges. main behind 422. Nine relay refs, all
STALE-BASE; two carried unlanded work.

TRUNK FROZEN 21:35Z -> ~22:10Z, my decision, for esmeraldinha's gate 24. THE
REASON IS A RACE DECIDED BY GATE DURATION RATHER THAN CORRECTNESS: the build.sh
gate-step region has conflicted SEVEN times today, and esmeraldinha's last two
migration attempts each came back with a new inline step added underneath —
first yolanda's jq fixture, then yolanda's 1004-vsh2 step. BOTH WOULD HAVE BEEN
SILENTLY DROPPED by taking their side whole; I caught the first, their own SET
comparison caught the second. Their gate is ~32 minutes, the longest in the
fleet, so a third attempt would most likely have produced a third. Half an hour
of quiet once is cheaper than a fourth instance, and after the migration lands a
new gate step is a new FILE with a literal path and this class ends.

All four landing hosts acknowledged and are gating without pushing.

RELAY LANDED BEFORE THE FREEZE: macneo's 1055-e8ie criterion 2 at e16319e28.
First relay today needing NO restoration — parts 14008/10597 bytes, whole 24605,
all three hashes matching, zero doubled backslashes. AND I ONLY KNOW NOTHING
NEEDED RESTORING BECAUSE THE HAZARD MAP ARRIVED FIRST; without it I would have
restored five context lines that were already correct and broken the hash.

RELAY VERIFIED AND HELD: macneo's criterion 4, 84a192378, 7167 bytes / 144
lines, sha256 matching, hazard map matching over a stated denominator
(scanned=144, ws=3, empty=10). Lands first when the freeze lifts. With it,
1055-e8ie has all four criteria met — and its terminal event must say the packet
closes with its most consequential finding SPLIT INTO A SUCCESSOR rather than
resolved: the ~20 branch sites still make the old inference.

FILED: 1082-9rub from macneo's draft — `is_provisioned()` is a stat() of
rootfs.img branched on at ~20 sites as though it meant provisioning completed
(vz.rs 11, diagnose.rs 7, action_host.rs 2, counts re-verified here). I added a
NEGATIVE CONTROL macneo's draft lacked and they conceded was a hole: a site that
genuinely wants the file test must still answer from it, or a conversion breaks
`provision`'s idempotency short-circuit while passing the main arm.

THE STALE-BINARY PROBLEM IS FLEET-WIDE: THREE OF THREE HOSTS CHECKED WERE STALE
(macuahuitl, lenovinha, yolanda). yolanda's was the DIRECT CAUSE of two of the
four rows 1079-qb8k was filed on — their claim events reached the ledger without
a transition because the tool that should have refused them had the fix in its
SOURCES and not its bytes. Capability count 58 on both copies here, so the
obvious check is blind.

AND yolanda DREW THE CONTROL PROBLEM I HAD NOT SEEN: the sample that produced
the finding and the sample that would validate the remedy are the same hosts,
and none was running the remedy. Judging 1079-qb8k by "the rows stopped
appearing" is only as good as the refresh reaching every host first.

SO THE PROBE IS NOW A SCRIPT, scripts/check-plan-binary-current.sh, written this
cycle and held for the freeze. Three hosts derived it by hand tonight and each
found a way the previous was blind; a command that lives in prose gets retyped.
It carries all four conditions, mints the agent id, asserts the target is `ready`
first, greps the refusal TEXT not the exit code, and WRITES NOTHING TO THE
LEDGER — it runs against a scratch `--index` copy, verified with a positive
control (an accepted write moved the scratch count 545->546 while plan/index.d
stayed 545).

IT HAD THREE BLIND SUB-CHECKS OF ITS OWN, all in the copies count, all caught by
printing the number rather than by reading the code: `command -v -a` is
unsupported by this bash and silently returned nothing, giving copies=0 on a host
that has one; `type -a` then gave 4 for ONE binary because this PATH carries
~/.local/bin four times; distinct paths is 1. Three corrections to one line,
inside the probe written to catch blind sub-checks.

macbookair ROOT-CAUSED 1059-yekz and the title is wrong in a useful way: not
that console output fails to reach the host, but that THE TWO ENDS NAME
DIFFERENT DEVICES. cmdline says console=tty1 and ttyAMA0; /proc/consoles
registers only tty1; the getty is on hvc0; ttyAMA0 never registers because VZ
gives a virtio console rather than a PL011 UART. Two-sided proof, same guest same
instant: /dev/console reached the host 0 times, /dev/hvc0 once. And the output
existed all along in the journal — "produced, and sent where no one reads",
which answers the discriminator macneo could not reach.

macbookair ALSO REPORTED POWERING OFF THEIR OWN GUEST, unprompted, naming the
unsafe sequence (restarting journald while the headless service logs through it,
then restarting a unit the control wire depends on, in one command) and saying
the recovery was luck rather than design. Recorded as a FLEET HAZARD rather than
their mistake: a restart of a unit the control wire depends on is destructive on
a live guest, because order 272 makes the wire the only channel.

BOARD: esme gate 24 running under the freeze, carrying the migration AND item 3
together with ten .step files and a pre-flighted convention pass. macneo idle,
criterion 4 held for landing. macbookair on 1059-yekz then 1082-9rub. lenovinha
on 1077-vzwq, revising its own estimate downward-then-cautious: two bindings is
an UPPER BOUND, not a plan, pending fresh adversarial re-checks. yolanda routed
to 1075-yuxt, the one surviving SIGPIPE site, which is latent here and live on
their drvfs checkout. yoga idle with 1080-4deb and 1081-gynk available. pirria on
1074-96z9.

SWEEP: live=5 unclaimed=1 expired=0 held=0 unknown_age=0, plus three
claim-without-status rows — yoga's new category working one cycle after landing.
One refinement routed to them: 823-u5zf is flagged while yolanda deliberately
RELEASED it with an explicit event eight hours after the claim.

CYCLE METRICS, verbatim:

```
experts: calls=8 answered=8 unsupported=0 degraded=0 errors=0 answer_rate=100% tools=check,ready source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=1 per_server=cli=8 legacy_untagged=0 health=ok source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
flow: cycles=17 avg_completed_per_cycle=0.76 avg_commits_per_cycle=5.47 overhead_ratio=7.15 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
timing: steps=71642 build_check_ms_avg=93898 build_check_mix=mixed:forced=593,memoised=207 litmus_ms_avg=41086 slowest=check:litmus-pre-build:1967661 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=3h steps=449 top3=build-preamble=34,build-check=18,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=18 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=60822 steps=593 top3=build-check:runs=479:total_ms=48981295:avg_ms=102257:fail_pct=28,litmus-suite:runs=646:total_ms=30286017:avg_ms=46882:fail_pct=60,check:litmus-pre-build:runs=29:total_ms=23778529:avg_ms=819949:fail_pct=45 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=79 floor_ms=2000 min_runs=5 top3=step:running-workspace-tests-cargo-test-workspace-all-targets:runs=71:avg_ms=81517:fail_pct=0:saved_ms_upper=5706238,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=345:avg_ms=16142:fail_pct=0:saved_ms_upper=5552972,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=345:avg_ms=12583:fail_pct=0:saved_ms_upper=4328634 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=782 ready=469 plan_bin=./target/release/tillandsias-plan
repo: commits_this_cycle=- worktree=dirty traces=current
verdict: attention:worktree-dirty
```
