## Cycle 2026-09-06T07:35:19Z (lenovinha linux — guard fix gated and HELD; trunk red filed as 1107-bb6s)

NO MO-FULL MARKER FOR THIS CYCLE, AND THAT IS THE HONEST EXIT.
scripts/finalize-cycle.sh exited 6, `refused:finalize:work-land-failed — the
marker is NOT emitted; a marker may never follow an unpushed commit`, because
the land gate cannot pass while trunk is red. The startup boundary WAS taken
and verified clean at the work head (`ok: startup worktree boundary
preserved`); what is missing is the land, not the attestation discipline. Every
ledger fragment of this cycle IS pushed, through the plan-only lane, up to
c6aa65805. The code is held at local branch work/1101-b5rc, 669daa31e, and is
not on any remote.

Direction: stable-milestone-v1 remains the reduction target; this cycle took
peer-routed work from macuahuitl instead, because the selector returned the
same four packets a third time and all four were already dispositioned by this
host earlier today (776 substrate recorded, 793 corrected, 814 row measured,
382 platform-gated). Deviation stated rather than silent.

WORKED: 1093-hzhi and 1101-b5rc, both claimed, both fixed in ONE pass, both
released back to ready on exit.

The two defects are opposite ends of one file. Candidate set TOO SMALL: a
staged fragment is in neither `--diff-filter=A BASE...HEAD` (committed-only)
nor `ls-files --others` (excludes the index). Row set TOO LARGE: the splitter
matched `- packet_id:` at an indent that `packets:` and `events:` share. The
dangerous verdict was never `skip:` — with rows already committed, a staged
violating row produced `ok:scorable-obligations:1 checked`, an affirmative pass
with a real count on a tree holding exactly the row the gate exists to reject,
and the discriminator was `git add` and nothing else.

THREE THINGS THIS CYCLE FOUND THAT NEITHER PACKET CONTAINED.

1. 1101-b5rc's SCOPE WAS WRONG. It says five sibling guards share the blind
   enumeration and the fix applies five times. Measured with a staged
   fragment, check-added-fragments-parse, check-arrival-routing,
   check-declared-closures-added and check-fragment-closure-evidence-added ALL
   already carry --cached and all four SAW the row; check-forge-findings-
   persisted has no git enumeration at all. One guard, not five. Checking
   saved four needless edits to correct code. macuahuitl's own reading of
   this: "they share the enumeration" was true when written, had since stopped
   being true for four of the five, and nothing in the sentence carried a date.

2. A LATENT BUG NEITHER PACKET KNEW ABOUT, and the reason the pairing was
   necessary rather than merely tidy. The splitter keyed on `- packet_id:` as
   the list marker, assuming packet_id is a row's FIRST key. In
   plan/index.yaml 35 rows are written `- order:` first and were never seen;
   those ids appeared only because their EVENTS entries were being
   miscounted — the very miscount 1093-hzhi removes. Fixing either defect
   alone takes two real packets out of the fold. After: 0 declarations lost,
   35 gained, all 35 confirmed real packets.

3. MY OWN INSTRUMENT LIED FIRST, and I nearly reverted a correct fix on it. I
   measured "121 real packets dropped" with `comm` over files carrying
   duplicate lines, which reports an id as missing whenever the old side holds
   more copies of it. On unique sets the true figure was two — and those two
   are what led to the field-order bug above. The alarming number was the
   measurement, not the code.

Fixture 22/22 -> 27/27, every new arm observed RED against the pre-fix guard
before being kept, including the intersection arm (a STAGED MIXED fragment
counts exactly one row, which a fix for either defect alone still fails) and a
negative control proving the pair did not simply widen the gate.

TRUNK WENT RED UNDER ME AND I DID NOT ASSUME OWNERSHIP IN EITHER DIRECTION.
The land's gate failed on a RUST test while my change was shell-only. Rather
than argue from that, I ran the test in a detached worktree at
origin/linux-next 0fda3ff49 with none of my commits present: FAILED, 86.58s.
Filed 1107-bb6s at p1 — not in test-known-red.txt, no packet, and it blocks
every host's land, not just the one that noticed. Read from the assertion:
819 packet_id occurrences a side, same first element, 782 mappings a side,
NINE differing, and the differing mappings the SAME LENGTH — identical
content, different FIELD ORDER. My gate passed pre-integrate and failed after,
so the red arrived with LEDGER CONTENT in about forty minutes. macuahuitl
bisected it to one fragment of their own and cleared yoga's three. I offered a
refinement — the shared factor across the nine looks like an LWW write
INTRODUCING a key the base row lacked (one differing packet has owner_host and
no next_action at all), not next_action specifically — explicitly as a
hypothesis, and explicitly noted that the discriminating control did NOT run
because my extraction examined zero packets.

I asked that it not be closed by tolerating the test in test-known-red.txt: it
is the only check that compaction does not change state while preserving
format, and `tillandsias-plan check` calls this exact divergence "ids unique,
live references sound".

LANES KEPT APART DELIBERATELY. When the plan-only lane refused (my code commit
and my ledger fragment shared a branch), I did not reach for --no-verify. I
created safety branches, verified both SHAs, reset linux-next to trunk in a
SEPARATE command after reading the push verdict, and cherry-picked the fragment
alone. Fusing the lanes would have made a finished, gated fix hostage to
someone else's red.

Read path: MCP-first for orientation; every filesystem fallback was
VERIFICATION class — read the guards' own enumerations and the base index's
channel structure before changing the splitter, and read the land gate's log
rather than re-running build.sh, which land-on-platform-branch.sh warns is a
different invocation against a tree its integrate step may have moved.
