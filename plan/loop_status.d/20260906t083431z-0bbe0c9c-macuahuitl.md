## Cycle — macuahuitl hourly coordination (cron lane)

SIBLINGS MERGED: windows-next (+6), osx-next (+17, over D_max=5). Zero
conflicts, 25 ahead. osx brought macbookair's credential-litmus expectation fix,
which CLOSES the union failure open since 06:00Z — the last of the four that
run's litmus found. Non-plan files: crates/tillandsias-headless/src/main.rs,
methodology/verification.yaml, litmus-credential-channel-check-shape.yaml.

I BROKE TRUNK LAST CYCLE AND FIXED IT, and the fix was a WITHDRAWAL rather than a
repair — so the mechanism is still unknown and the next fragment with the same
structure reintroduces it. lenovinha filed 1107-bb6s at p1 from a clean-worktree
run; macneo attributed it to pristine trunk with a control asserting the test
actually ran. Bisected to one fragment, mine: 732 ok, 736 ok, 740 FAILED,
740-minus-that-one ok. Withdrawn, content refiled shorter, trunk green at
abb7f3998, both blocked hosts released to land their own work.

SO I NOW REGRESSION-CHECK COMPACTION BEFORE EVERY LAND, and did it three times
this pass: on the merged union (749 fragments), and again after each fragment I
added. All green. That check is cheap (47s) next to the cost of blocking four
hosts, and it is the only guard for a defect whose trigger is unidentified.

SWEEP: live=9 unclaimed=0 expired=0 held=0. Nothing stranded — last cycle's
release of my own 1025-a896 held, and lenovinha's 1071-adhj is closed. One
observation: 834-r6vn is claimed by `windows`, a platform BUCKET rather than a
host (1012-hu7d). That is yolanda's claim, unattributable by any query keyed on
either label — the same defect that cost lenovinha the memory of their own
finished work an hour ago.

HEARTBEAT: six peers idle, lenovinha running its finalization. Idleness
established by asking, not by loop-status mtimes, which are stale for hosts I
know are live. Cross-referencing claims against the sweep, EVERY host held work
except macbookair, who had just landed the credential-litmus fix — so 1106-k2df
routed there by capability: they fixed 1091-h3cb's prefix defect, and this is the
same organ one layer down, the one arm everyone believed DID verify.

THE RECURRENCE AUDIT NOW HAS ITS THIRD HOST AND IT IS ME. I have been auditing
whether other hosts paste cycle-metrics while not pasting myself; fixed this
cycle. Two steps now intersect rather than one: running-workspace-tests tops both
macuahuitl (runs=101 avg=95113) and macbookair (runs=80 avg=124225), and 831-ezea
is second here, third there, first on yoga. The intersection WIDENED with a third
host, which is what makes 1103-r7wq worth acting on. But macuahuitl is
forced-heavy too (mixed:forced=636,memoised=228, roughly 3:1), so this is a third
point with the SAME SKEW rather than the unforced baseline the packet asked for.
A host on an ordinary cadence is still owed. Ten of thirteen stems still not
pasting.

A NUMBER I AM RECORDING RATHER THAN ACTING ON, because I have not distinguished
its two readings: this host's recur reports build-check at fail_pct=28 over 480
runs, litmus-suite at 60% over 652, and check:litmus-pre-build at 50% over 28
runs averaging 895,631 ms. macbookair independently reported build-check at 34%.
Either the gate is genuinely unstable or the counter treats an aborted or
superseded run as a failure. Those have opposite remedies and it deserves its own
row.

OPERATOR QUEUE, plain asks. pirria still cannot push — credential.helper unset in
all three scopes; six packets reached the ledger by hand relay tonight, and they
are now holding two verified patches with recorded hashes that only a relay can
land. The OAuth all-or-none PAT decision (1025-a896, released to ready last cycle
so it stops reading as work-in-progress). The 03:23:01Z-04:22:15Z security log
before any fourth host is restored. esmeraldinha and yolanda both cleared
themselves off this queue in the previous pass.
