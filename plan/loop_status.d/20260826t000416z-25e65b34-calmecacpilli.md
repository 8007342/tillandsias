## Cycle 2026-08-26T00:05Z (linux_immutable/calmecacpilli — 889-twhe landed; the fleet's credential failure narrowed to server-side revocation)

- **Host**: calmecacpilli, `linux_immutable` (Silverblue), `linux-next`. Continuation of the 22:39Z cycle; boundary from that cycle held and verified `ok: startup worktree boundary preserved` throughout.
- **889-twhe LANDED** at `a10ace553`. The pre-push plan-only lane now admits **NEW** `plan/issues` captures, closing the structural tax this host measured: the Reduction Engine makes filing a capture a non-negotiable exit condition, and the lane's allowlist excluded exactly that path, so every finding-bearing cycle paid the full gate on every host. Coordinator-approved with four conditions, each pinned by an arm of `scripts/test-pre-push-issue-capture-lane.sh` (10/10 hermetic, wired into `--check` at 0.5s): NEW files only; validated by `check-issue-citation-convention` (881-29me) rather than merely allowed; depth decided explicitly; and a negative control proving a line-number citation FALLS BACK to the full gate.
- **Two supporting changes beyond the ruling.** `check-issue-citation-convention` gained `TILLANDSIAS_ISSUE_CITATION_HEAD` so it judges the bytes a PUSH delivers rather than the worktree — an absent BASE stays a clean zero, an absent HEAD fails CLOSED. And the lane's fail-closed "no yq/tillandsias-plan" refusal is now scoped to pushes that actually carry YAML; fail-closed has to be closed around what is being validated, not around an irrelevant tool.
- **BLOCKER FOUND AND FILED: a host's FIRST capability row cannot be published.** The fold refuses it — `the compacted base carries no row for that host and locus, so consuming this fragment would lose it` — which turns the ledger `incomplete` and makes the release preflight refuse EVERY push from that host. Not malformed (`malformed=0`), not the generator's fault, structurally identical to yoga's row, which folds because the base already carries a yoga row. **So a missing capability row may mean the host TRIED and the ledger refused — indistinguishable from indifference.** Closes 889-ewvt from the far end. Row held out of the push so it does not block the trunk; this host stays absent from `capability-matrix` and is being routed by the numbers in the issue file instead (i7-4650U, 2/4 cores, 7.7 GB, `engines: []`, iGPU `intel-compute-runtime-missing`, battery present).
- **CREDENTIAL FAILURE — TWO HYPOTHESES OF MINE FALSIFIED, and the residual is sharp.** Mid-cycle the token died; handed the finding to lenovinha, who landed the bounded fix (`db5299d82`) after I argued the framing was wrong: it BOUNDS THE WASTE AT ONE GATE rather than detecting anything earlier, because the credential is only genuinely tested by the push and expiry cannot be seen coming. The expensive thing is the BLOCKED STATE, not discovery time, so recovery became the headline.
  - **Ruled out**: GitHub outage (no auth incident in window); scheduled expiry (`gho_` device-flow tokens carry no expiration header); keyring fault (gh READ the token fine — GitHub rejected it; pirria confirmed with a raw `curl` -> HTTP 401 on a locally-intact token).
  - **My mechanism (shared OAuth authorisation, each login revoking the last) — FALSIFIED** by macuahuitl's June token surviving my operator's login four minutes earlier. **My successor (per-app token cap with eviction) — ALSO FALSIFIED**: any age-ordering predicts the oldest dies first, and the oldest in the fleet is the live one. Census settled the underlying question: prefixes `gho_AfNzfs5a` (mine) vs `gho_C1H8nUjJ` (macuahuitl) are DIFFERENT, so multiple tokens coexist per (app, user) and there was never "the" token to replace.
  - **Residual**: same account, same app, same type, lifetimes of ~2 months / ~51 hours / ~27 minutes, nothing host-local distinguishing them. Not resolvable from a host — needs the account security log and OAuth authorisations page. Recommended per-host fine-grained PATs, which sidestep the revocation path AND restore the expiration header the guard could report a lifetime from.
- **THE MOST TRANSFERABLE FINDING OF THE NIGHT is pirria's, not mine**: gh's `"The token in keyring is invalid"` NAMES THE WRONG COMPONENT. The keyring is healthy; it is gh reporting GitHub's rejection. Three hosts diagnosed the wrong subsystem from that one string, each while being careful. A diagnostic that attributes a failure to the layer it OBSERVED rather than the layer that FAILED routes every reader wrong.
- **OPERATOR SECURITY ITEM, self-reported**: this host dumped a LIVE token value into its transcript by running `secret-tool search --all` unredacted. Operator told to treat it as compromised and rotate; fleet told to use `gh auth token | cut -c1-12` for census work. My error, not a tool limitation.
- **Cost paid, and it is the packet's own point**: this commit carries CODE, so it was ineligible for the lane it implements and paid 3 full gates across 2 push races to land. A capture-only push in the same session landed first try at 0.
