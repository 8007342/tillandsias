## Cycle 2026-09-05T12:54:30Z (yolanda — 1064-r8fv, 1055-6yp8 closed, 823-u5zf advanced)

**Landed.** 1055-6yp8 closed on trunk at 15106499d and its close event on
origin/windows-next at e05dda303. 1064-r8fv relayed at 0f6ce95ea. 823-u5zf's
deletion scan at c437e68b5, pushed to origin/windows-next at 9ae10a582.

**Three defects, one shape, and it is the shape of this whole cycle:** a name
that appears both in live code and in prose about that code cannot be
classified by matching the name.

1. **1049-s35z** — jq.exe writes CRLF, so litmus test names carried a trailing
   CR, files were "not found", tests were logged SKIP, and skips are excluded
   from coverage: the runner printed "PASS 100% (1/1 executed)" having skipped
   two thirds of what it was invoked to run.
2. **1055-6yp8** — the litmus step meant to pin the default grepped for a
   DELETED test's name and matched the doc comment explaining the deletion.
   Green over an absence.
3. **823-u5zf** — its closure says "a source scan finds no
   argv_survives_wt_reparse". Evaluated as a grep it reds a CORRECT tree on
   three comments recording the deletion, and the tempting repair deletes the
   only explanation of why the code looks as it does.

**The instrument lied about the instrument.** Measuring 1049-s35z's blast
radius, my first count said "0 of 476 names affected" — MSYS grep treats CRLF as
the line terminator, so the CR is invisible to it. `od -c` shows it. Any audit
of that class that uses grep will report all-clear.

**I understated my own packet and the fixture corrected me.** I filed 1055-6yp8
saying the printed remedy "would move 75 correct entries". It does not fail — it
SUCCEEDS: `git mv <path> skills/<name>` with the destination an existing
directory moves the source INTO it, rc=0, no output, harness entry deleted,
stray buried in the canonical skill. My first fixture missed exactly that,
because its arm asserted `skills/<name>/SKILL.md` survived. It does survive. I
asserted the NEIGHBOUR of the damage instead of the damage.

**1064-r8fv, and the fixture found the half I had not filed.** The landing tool
integrated onto `origin/<branch>` and never merged trunk, so on a platform
branch it could not satisfy the mandated-merge guard however many attempts it
was given. That much I had diagnosed. Arm 3 then found that the retryable
classification grepped a bare `rejected`, which also matches
`! [remote rejected] ... (pre-receive hook declined)` — a server-side refusal
retried to exhaustion, a full gate burned per attempt. That is this file's OWN
header warning one layer down, inside the guard written to prevent it.

**Three bash-dialect and spawn-cost faults, all mine.** `declare -A` is bash 4
and macOS ships 3.2 — the dialect gate caught it; I then left `read -N`, also
bash 4, four lines from the code I had just fixed, and macbookair caught that.
The lesson is not "read -N is bash 4" but that a dialect refusal names ONE
construct and I treated it as the whole finding. Separately, an index-aware
check that called git twice per entry cost 238 spawns and 18s against a 10s
litmus budget — the fix for a false positive arriving as a TIMEOUT — and I then
reintroduced the same cost through the SHAPE of the call, a command
substitution forking per entry.

**Push throughput is this host's real constraint.** Eight refusals across the
cycle: mandated-merge (trunk moves inside a ~23-minute gate), non-fast-forward,
one genuine gate failure of mine, and one plan-only lane refusal that was
CORRECT — my "ledger-only" push still carried code commits. The lane is not "my
last commit is a fragment", it is "the outgoing diff adds only fragments".
Resolved by the relay lane (work/<order>, which no platform pattern matches)
rather than by another fleet-wide freeze — a window is a fleet cost for one
host's throughput problem.

**The lane caught a real defect in my close.** violation:fragment-status-loss —
a 'completed' EVENT with no status transition. Its words: nothing was
discarded, so nothing looks wrong, and the packet just stays claimable forever.
I would have reported a packet closed while leaving it claimable.

**Left outstanding, deliberately.** 823-u5zf's other half — a lane launch
OBSERVED in Windows Terminal — has no observation on any host. The installed
tray on yolanda predates the terminal= record (zero "spawning in-VM PTY" lines
in tray.log) and the distro is Stopped, so it needs a fresh install plus an
interactive launch. The unit test proves the code would log windows-terminal;
it does not prove a window opened, and I did not substitute one for the other.
Also outstanding: re-running test-cycle-preflight-cargo-resolution.sh with its
ARM COUNT once esmeraldinha's fix and macuahuitl's identity arms reach me — a
fixture pass travels across hosts only with its arm count.

Agent: windows-yolanda-opus5-20260905t125430z
