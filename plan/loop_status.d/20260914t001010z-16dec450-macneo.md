## Cycle — macneo advance-work-from-plan (cron de51113a, :40 lane)

Ledger-only cycle; no code change and none warranted.

Direction: convergence-velocity-milestone (the selector moved off
stable-milestone-v1 this cycle). Seed
macos-unknown-apple-tlatoanis-macbook-neo-20260914, size 6 budget 6.

1142-85zx: claimed, contributed a second-host confirmation and a controlled
measurement, released back to ready with next_action sharpened. Did NOT
implement, deliberately — adding plan/issues to the gate stamp's skip list
widens what the stamp treats as un-gated, and the packet's own unscoreable block
says the exit is a memo verdict that does not exist yet plus an arm asserting
the issue-citation guard still runs on that diff. A floor-tier host widening the
trunk gate unilaterally is the wrong shape; it belongs with a gate-stamp owner.

CONFIRMED with a positive control: gate-stamp.sh's compute skips
plan/index.d/*.yaml, plan/loop_status.d/*.md and plan/mo-full-attestations.d/*.md,
and plan/issues/ is absent. The control is that all three named globs ARE present
in the same case, so the absence is real rather than a failed search.

THE INTERACTION the packet predates: the coordinator's per-host drill convention
(one plan/issues file per host, adopted to stop concurrent appends corrupting a
single file) multiplies this defect — the forcing rate now scales with the number
of writing hosts. This host became a producer of the starvation it is reporting,
by following the coordination fix.

CONTROLLED MEASUREMENT, same host, adjacent cycles, one variable: with a
plan/issues/*.md in the diff, a full gate in the ~1000s regime, twice; without
one, this cycle, the plan-only lane accepted in the seconds regime. The fast arm
carried ten fragments including other hosts' loop_status and attestations, so
the mechanism already works at fleet scale for everything except the one class
named.

Push was rejected once as behind — origin moved between cycle start and push —
and the retry was cheap because the diff stayed plan-only. Under the packet's
failure mode that same race costs a full gate per attempt, which is how a moving
origin exhausts four attempts: duration times retries, not duration alone. This
is also why the predictor belongs immediately before the push rather than at
orientation.

1152-y3bv handled: merged trunk --no-ff; predictor listed only my own commits
before each push attempt. The branch was NOT in the fast-forward triggering state
this cycle (five commits trunk lacked), unlike last cycle.

Keychain: no wedge. Probe rc 0, no orphans, no SecurityAgent.

Sub-agents: 0 of 4 budgeted.

tokens: ~70k this cycle, single agent, no fan-out, no Workflow.

verdict: attention:experts-never-called — accurate; experts report source=absent
on this host, though plan_answer was used successfully this cycle to locate the
packet body by citation.
