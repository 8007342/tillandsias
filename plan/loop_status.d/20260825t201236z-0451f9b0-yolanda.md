## Cycle 2026-08-25T20:12Z (windows/yolanda — 731-qth3 closed: a logged-out cloud reply no longer survives sign-in)

- **Host**: yolanda, `windows`, branch `windows-next`, agent `windows-yolanda-claude-20260825t203420z`.
- **Preflight**: `ok:cycle-preflight:rebuilt+expert-absent+wsl-ok+services-no-podman`.
- **Guards**: credential `ok:gh-keyring`; branch `ok:branch-windows-next`; MCP `ok:experts-healthy` + `ok:surface-exposed`.
- **Daily maintenance**: was `due:stale:2026-08-22`, ran and stamped — delegate-sweep `no-registered-runs`, podman/nix `skipped-absent` (neither exists on this host), cargo-gc `deferred` (target 1.2G, under threshold), capability row `ok:capability-row-reported:yolanda`.
- **Merge**: `origin/linux-next` merged into `windows-next` clean, before the push, per `pull_merge_cadence.pre_push_gate`.
- **Batch**: `epic=stable-milestone-v1 role=windows release=v0.5 size=10 budget=10 score=9.706 seed=yolanda pick=3/7 route=rank:7/7:width=7`.

### Work: 731-qth3 (p1 defect) — claimed, implemented, closed

The windows tray reset `cloud_projects_loaded` only on a transition INTO
`LoggedOut`. The push listener primes an unconditional `CloudRefreshRequest`
before any login answer exists, so an empty reply landing while logged out
latched the flag, the `LoggedOut -> LoggedIn` transition did not clear it, and
the submenu's first visible render claimed "(no repos)" to a signed-in user who
has repositories — the exact falsehood the flag was introduced to prevent.

Fixed by resetting on a transition into `LoggedIn` as well; we are past the
equality test, so reaching the branch means the state really changed. The
mutation was factored out of the global `MENU_STATE` into `apply_login_state` /
`apply_cloud_projects_state` so the ordering is unit-testable, following the
`apply_menu_action_state` convention already in that file.

- `cloud_reply_received_while_logged_out_does_not_survive_the_next_sign_in`
  reproduces the PRODUCTION ordering (LoggedOut -> empty reply -> sign-in), not
  the returning-user ordering where the bug is invisible. **Hand-falsified**:
  with the `LoggedOut`-only condition restored it fails with "the first
  post-login render must be the loading row, not (no repos)".
- Negative control `a_repeated_login_state_does_not_discard_the_cloud_list`
  pins that a repeat of the same confirmed state is not a transition.
- `cargo test -p tillandsias-windows-tray` on yolanda: 96 + 9 + 3 passed, 0
  failed. `check-windows-only-sources-verified.sh stamp --from <transcript>` ->
  `ok:windows-sources-verified:4`.
- Commit `cb60d411ad0eba09d8867cc9811ec0c1f9a4dfc4`.
- Cross-linked as a note on the macOS sibling `731-m58f`, whose third exit
  criterion carried the OPEN WINDOWS half. It was one shared bug, not a parity
  gap, and that criterion is now satisfied.

### Freshness audit (order 372)

`methodology/event/012-litmus-podman-runtime-blocker.yaml` — verdict
**refreshed**, stamped. Its closure criteria are met: `scripts/local-ci.sh:1522`
gates the container-backed phase on `podman_runtime_health_probe`, so an infra
blocker is reported separately from a contract failure, and
`scripts/run-litmus-test.sh:86-142` gives litmus its own
`LITMUS_PODMAN_ROOT`/`RUNROOT`/`TMPDIR` plus a wrapper falling back to
`test-support/podman-mock.sh` — the mock/sandboxed-backend branch its
`next_distillation_step` left open. Record stands as correct history.

**NOTED, not filed as a row** (capture routing: this is an observation, not
independently schedulable work): event 012 and event 017 describe the same
rootless-podman blocker one day apart, were distilled into the same probe, and
017 was audited `refreshed` on 2026-08-11 with materially the same reasoning.
Two records for one blocker is duplicated audit cost every time the freshness
rotation reaches them — but merging them is a methodology owner's call, not a
windows drain host's.

### Not taken from the batch, and why

- `635-bhkb` — the remaining work is **macOS-only**. The windows half already
  landed: every version surface in the windows tray reads `WORKSPACE_VERSION`.
  The packet's own 2026-08-13 note says so; its title is stale, not its scope.
- `731-eupn` — a 5h `control-wire` + **both trays** change. The macOS tray
  cannot be compiled or verified on this host, so landing it from here would
  produce a change half of which nothing could check.

### Advisory

- `check-stranded-in-progress`: `summary: population=9 in_progress=9 stranded=9 threshold_events=0`. All linux/any-owned; not this host's to close, and closing requires checking exit criteria against the tree.
- `triage: eligible=106 grouped=99 ungrouped=7 epics=10`.
- `compaction: eligible=false fragments=4 bytes=3496 malformed=0`.
- `ok:deslop-not-due:order=886 last=834 delta=52 hours=139`.

### Metrics

```text
experts: calls=25 answered=25 unsupported=0 degraded=0 errors=0 answer_rate=100%
mcp: servers=2 per_server=forge-plan=23;project-info=50 health=ok
expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
flow: cycles=50 avg_completed_per_cycle=0.66 avg_commits_per_cycle=1.92 overhead_ratio=2.91
timing: steps=170 litmus_ms_avg=62988 slowest=litmus:cycle-metrics-answer-rate-shape:63411
plan: packets=598 ready=368
```
