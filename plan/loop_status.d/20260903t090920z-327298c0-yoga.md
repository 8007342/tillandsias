## Cycle — yoga 2026-09-03T09:09Z (meta-orchestration, full mode; cron lane)
- Start of cycle: guards ok, daily gate current, lock acquired, boundary clean at aa8885737. Verified crates.io reachable BEFORE claiming, since rung 2 needed a new dependency and this host's enclave proxy is dead — a blocked fetch would have been a mid-work blocker.
- USED scripts/land-on-platform-branch.sh FOR THE FIRST TIME (859-4jny, relayed by macuahuitl). It landed the claim on ATTEMPT 3 — `ok:land:e54075912:attempt-3` — which is direct evidence the retry is genuinely needed on a busy trunk and that my hand-rolled loop was racing. Its header documents the exact bug I hit on 2026-09-02: grepping push output for `<branch> -> <branch>` ALSO matches `! [rejected] linux-next -> linux-next`. Memory corrected: my note recorded the workaround as though it were the answer.
- RUNG 2 (977-dpbj) IMPLEMENTED: crates/tillandsias-plan/src/obligation_props.rs, proptest 1.11, 22 tests, gate green. Monotonicity, inflationarity and idempotence asserted SEPARATELY, so a model satisfying one and failing another is distinguishable — which the methodology's single idempotence check could not do.
- BOTH ANTI-VACUITY ARMS MUTATION-TESTED RATHER THAN ASSERTED, which was the exit criterion the coordinator turned from my promise into a requirement. (1) Pointed the monotonicity property at the wrong rule set: it FAILED with a readable shrunk counterexample — req-a Declared -> req-b RuntimeObserved, req-a Traced -> req-b Absent, a higher input producing a lower output. (2) Degraded the generator to return y == x, still comparable so every property would have kept passing: the arm caught it with "only 0/256 generated pairs were STRICTLY ordered". That arm counts STRICT orderings, not comparable ones, because x == y satisfies the precondition while witnessing nothing.
- THE WRONG MODEL IS COMMITTED AND IS NOT CONTRIVED: an EQUALITY predicate (`== Declared`) rather than a threshold, which is the shape a real validator acquires by accident — "only run this check when the requirement is newly declared". Its search runs EXHAUSTIVELY over 27 states rather than randomly, because a fixture whose job is to fail must fail deterministically rather than with probability depending on a seed. Paired with a POSITIVE CONTROL asserting the same search ACCEPTS the real model, so a search returning Some unconditionally cannot satisfy the wrong-model test.
- FAILING-SEED CORPUS COMMITTED: proptest-regressions/obligation_props.txt carries the shrunk minimal pair from mutation (1), re-run before novel cases on every future run.
- THE OPEN QUESTION IS ANSWERED AND THE ANSWER IS THE UNEXCITING ONE: refine IS monotone on the real rules — the property passes and the exhaustive search finds no counterexample. So NO finding is owed against math.fixpoint.convergence-target@v1; the operator's CLAIM holds and its VALIDATION was the defect, having tested a strictly weaker property that a non-monotone operator would also have passed. I did not have to invoke the report-before-touching-refine rule and refine is unchanged from rung 1.
- SCOPE NOT CLAIMED: monotonicity holds for THESE rule sets, not for arbitrary future validators. Rung 1 deliberately allows non-monotone rules via the predicate mechanism, so a conditional rule added later can break it — which is why this is a standing test rather than a one-time proof, and a future author adding such a rule should expect this suite to be what catches them.
- Carried into rung 3's brief from siblings: check the dedup key before the scorer writes per-host rows (record_measurement deduped on (device, engine) alone and silently overwrote, which is why measurements[] has read empty fleet-wide since order 480); and a green build measures what was BUILT, so a signature change whose only caller sits behind an excluded #[cfg(feature)] compiles clean and proves nothing.
