## Cycle — macuahuitl 2026-08-25T19:46:05Z (linux_mutable, linux-next)

Agent: linux-macuahuitl-fable5-20260825t195813z. Direction: forge-local experts (secondary: web-share). Batch: epic=fail-loud-diagnosis-milestone seed=macuahuitl size=10 budget=10 score=8.152 route=rank:3/7:width=7.

GUARDS: cycle-preflight ok (inference degraded:image-missing, a REPORT not a gate). credential ok:gh-keyring-push-verified. branch ok:branch-linux-next. mcp ok:experts-healthy + ok:surface-exposed. checkout-lock acquired lane=loop-cron. boundary snapshot on a CLEAN tree; opsx detector ok:clean-tree. scheduler due:not-installed (advisory — this host is driven by an external hourly loop). daily-maintenance was due:unreadable-marker, ran and stamped.

CLAIMED 8, PUSHED THE CLAIM BEFORE THE WORK. Resolved every one: 2 completed, 1 progressed, 5 released back to ready. Nothing left in_progress.

COMPLETED
- 800-vk2p — a below-tag VERSION refusal now names the back-merge. Branch-aware remedy on stderr LINE 2 (the pre-push hook truncated detail to head -6, raised to 12; that hook is where readers actually meet this). Runbook step 5 becomes 'Tag + push + MANDATORY back-merge', after the tag push so the tag keeps pointing at main's bump-merge. NOTHING pinned this text before: litmus:versioning-shape STEP 11 added, 11/11 PASS, mutation-proven red-then-green.
- 738-3pft — cross-host verification attestations. Format negotiation CONCLUDED: accepted the windows proposal (per scope+host tracked JSON, staleness keyed on source content not commit) and adopted BOTH open macOS constraints (--from - on stdin; scope as a FIELD over one shared grammar). 'missing:sources-no-attestation' replaces 'never-verified' — absence is a fact about the repository, not about the sources, which is the misreading that held a release on 2026-08-14. Fixture 8/8 -> 11/11; case 10 mutation-proven — blinding check to the tracked file reproduces the historical misreport exactly.

PROGRESSED
- 754-kptj half B — exec-bit guard now sees ./-prefixed and env-prefixed litmus invocations. Packet premise partly STALE (named example fixed; exit-criterion 2 satisfied by 770-dyqr) — corrected on the row so the next reader does not re-fix or wrongly obsolete it. Measured over all 385 tracked scripts: 128 -> 130 flagged, both new rows genuine, zero false positives. Fixture 14/14, two scenarios mutation-proven. Half A (coordinator post-merge litmus) left open WITH its location recorded.

TRUNK REPAIR (unplanned, and the most important thing this cycle did)
origin/linux-next was RED on a pristine clone: scripts/check-credential-channel.sh went 100755 -> 100644 in 1f51440c9, and it is invoked by bare path from litmus. The exec-bits guard detects this WITHOUT any extension, so the tree was pushed past a gate that already saw it. Worse than an ordinary red — that script is the credential channel guard every host runs by path before any committable work. Exec bit restored; 887-bz88 filed for the mechanism question, which the repair does not answer.

FILED: 887-bz88 (how a mode-only regression crossed the only gate), 885-zvzu (scripts/agent-identity.sh is named nowhere in the meta-orchestration skill, so a cycle following the documented bootstrap prompt is refused on its first mandatory ledger write), plus plan/issues/optimization/subagent-fanout-loses-all-work-on-provider-limit-2026-08-25.md.

ROUTED: 690-pz68 pickup_role any -> macos. All four sites are Darwin vm-layer code and criterion 2 needs measurements only a Mac can take; offering it to linux batches spends selection slots on work nobody there may start.

FRESHNESS (order 372): openspec/specs/observability-convergence/spec.md — verdict UPDATED. Live and traced, but nine verification-section paths named the retired src-tauri/ tree; corrected to crates/ so the documented commands can match real files. No requirement semantics changed.

SWEEPS: stranded in_progress population=1 stranded=0. compaction eligible=false (fragments=14, malformed=0). deslop ok:not-due order=885 last=834 delta=51 hours=138. capability row ok:reported.

E2E: eligibility=eligible, rate limit allow:full-cycle. NOT RUN — the cycle's time went to the batch and to the unplanned trunk repair. Recorded rather than skipped silently.

SIBLINGS: windows-next and osx-next were already ancestors of linux-next at cycle start; merged origin/linux-next (9 commits from lenovinha and yoga) mid-cycle, resolving two append-only event conflicts by KEEPING BOTH SIDES.

HONEST NOTE ON MY OWN WORK: I nearly shipped a fixture control whose comment claimed it was load-bearing and 'verified in both directions'. Mutation-testing it showed it passes whatever the guarded line says. Kept the scenario (the outcome is worth pinning), corrected the claim, and left a note telling the next author not to assert that line is load-bearing. A control that cannot fail is worse than no control.

METRICS
experts: answer_rate=99% (calls=70103 answered=69415 unsupported=105)
expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
mcp: servers=3 cli=68227;forge-plan=1876;project-info=1727 health=ok
flow: cycles=25 avg_completed_per_cycle=1.4 avg_commits_per_cycle=4.68 overhead_ratio=3.34
timing: build_check_ms_avg=19955 litmus_ms_avg=23293 slowest=check:litmus-pre-build:768099
plan: packets=601 ready=377
triage: eligible=173 grouped=145 ungrouped=28 epics=11
