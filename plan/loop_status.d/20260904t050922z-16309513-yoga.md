## Cycle — yoga 2026-09-04T05:09Z (meta-orchestration, full mode; cron lane) — LAST cycle before the operator's fleet restart
- 998-3z6g IMPLEMENTED: the CA bundle is off /tmp. ca-path.txt declares ${HOME}/.local/state/tillandsias/ca and both readers expand it. 975-rsgm's cause is addressed.
- THE DECISION WAS FORCED BY MEASUREMENT, not chosen. Guest (macbook, real Mac VM): /var/lib works — key present, 600, owner root, survives reboot — BECAUSE the guest runs as root. Host (yoga): uid 1000 rootless, /var/lib not writable at all, $HOME/.local/state is. /var/lib fails on a rootless host for exactly the reason it succeeds in the guest, and /tmp works everywhere and is the bug. HOME-relative was the only candidate either lane permitted, which is why the manifest had to become a TEMPLATE rather than take a new literal.
- TWO EXPANDERS IS A NEW WAY FOR ONE DECLARATION TO MEAN TWO THINGS — the defect 998-qrwu removed — so the agreement is PINNED: rust_and_shell_expand_to_the_same_path runs the REAL scripts/lib-ca-path.sh in a subprocess and compares with ca_path::ca_dir(), rather than reimplementing either side. A second implementation of the comparison would have the same failure mode as a second declaration.
- BOTH EXPANDERS WERE BROKEN AND MY OWN TESTS CAUGHT THEM BEFORE THE GATE. The Rust .replace() had lost its pattern to a bad edit and became replace("", &home), which substitutes at EVERY position — the failure printed a path with HOME interpolated between every character. The shell reader did not expand at all and returned the literal ${HOME}/..., which would have created a directory NAMED ${HOME} — silently, and durably, since persistence is the whole point of the change. Neither would have failed to compile.
- VERIFIED: core ca_path 4/4 including cross-runtime agreement, macos-tray 44/44, workspace compiles, gate green, shell readers answer against the new path, check-ca-bind-migration.sh ok:ca-bind-current:0 (nothing here binds the old path).
- ALSO FIXED because it went stale the moment the value moved: the consistency checker's prose said "/tmp is volatile by design" while naming the new path. Rewritten, /tmp kept as history.
- OPEN AND NAMED, neither doable here: macbook should re-run its guest check against THIS value (it verified /tmp and /var/lib, not this one; in the guest ${HOME} is /root, which no Linux host can exercise), and the closure as written — remove the old dir, restart, observe the proxy reach Running unaided — needs a host with a live enclave. yoga's proxy container is absent entirely and cannot be recreated from this checkout.
- OPERATOR GREEN-FLAGGED A FLEET RESTART mid-cycle. Landed, attested, cron cancelled, reported clean.
