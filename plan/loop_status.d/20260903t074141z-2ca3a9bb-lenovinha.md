## Cycle — lenovinha (linux_immutable, linux-next), 972-6vaj

CLOSED 972-6vaj at cebc0092c. Confirmed the zero-call-site claim myself, then
corrected the packet's diagnosis in both directions.

CORRECTION 1 WIDENS THE GAP. The only PRODUCTION caller of build_run_argv is
tray/mod.rs:2481, and `mod tray` sits behind #[cfg(feature = "tray")] which the
DEFAULT BUILD EXCLUDES. I found this because my signature change compiled
cleanly when it should not have — a default build was silently not compiling
the caller. Rebuilt with --features tray, got the expected E0308, fixed it
there. A change that "compiles" is not evidence when the code is behind a
feature nobody enables.

CORRECTION 2 NARROWS IT. The packet says the four flags are "settable false".
They are not, from outside the crate: fields are private, new() sets all four
true, and NO builder disables any of them. The exposure is regression risk
inside the crate, not a live bypass. I looked for the setters rather than
trusting the sentence.

SHIPPED: build_run_argv returns Result and validates in every profile, and
REFUSES rather than logging-and-continuing — an argv missing a hardening flag
is not a degraded launch to report, it is a container that must not exist, and
a warning beside a successful launch is the fail-safe-looking outcome we keep
paying for. The root maintenance terminal now refuses instead of launching.

cap_add REMOVED rather than allowlisted (the criterion offered both). Zero
production callers; the only use in the tree was one test granting SYS_CHROOT.
An allowlist is a policy to maintain and a place to argue an exception; an
absent field cannot be misused. That test's assertion is INVERTED rather than
deleted, so a future re-add fails a test instead of passing quietly.

THE RELEASE-PROFILE PROOF asserts on the RETURNED VALUE, not on a panic. A test
asserting the old debug_assert fired would pass in debug and prove nothing
about a shipped binary, because in release the assertion did not exist. It
carries its own negative control so a vacuous pass is visible.

THE FINDING NO CRITERION COVERED: the live forge never touches this path. Its
argv is a plain Vec<String> from build_stack_common_args + build_opencode_forge_args
via build_forge_common_args, handed to run_agent_container_attached — it never
constructs a ContainerSpec, so validate_launch_argv could not have covered it
even when called. The forge IS hardened today (verified on the live container:
private userns, expanded CapDrop, no-new-privileges, label=disable) and is
protected by nothing but literals in main.rs. Split to 986-ahnc rather than
ridden in here: that choke point serves every attached agent launch, and a
false-positive refusal there stops every forge on the host — refuse-versus-log
at that site is a decision, not a mechanical extension.

MY OWN MISTAKE, corrected on the ledger: I cited the follow-up as "983-vk2m" in
the closing event before minting it. No such order exists; the real one is
986-ahnc. Exactly what the skill warns about — mint with next-order, never
compute a token. Fragments are immutable so the correction is a new event.

STILL RED AND NOT MINE: delegated_result_fake_podman fails the full parallel
release run and passes alone — the flake filed 2026-09-02, unchanged.

metrics:
  gate: TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check green
  tests: cargo test --release -p tillandsias-podman 175 passed
  built with: --features tray, to reach the one production caller
  filed: 986-ahnc (forge argv validated by nothing)
