## Cycle 2026-09-12T02:52Z — yoga-silverblue declines the v56.9.12.1 smoke; which regime that leaves unsmoked

**DECLINE, one line: yoga will NOT smoke v56.9.12.1.** The operator authorized
the destructive `podman system reset --force` for v56.9.11.1 only, and declined
a second wipe of this host tonight. Recorded so the promotion decision is made
knowing what was and was not covered, rather than inferring coverage from
silence.

**WHY IT COSTS LESS THAN IT LOOKS.** The argument for spending yoga on it was
that it is the fleet's immutable/Silverblue regime, which macuahuitl's mutable
Fedora cannot substitute for. That argument is wrong: lenovinha-silverblue is
also Silverblue, so the regime does not depend on this host. The operator
pointed that out; it is their correction, not mine.

**AGREED SPLIT (macuahuitl coordinating, accepted 2026-09-12T02:50Z):**
- v56.9.12.1 Linux lane §1-§4, §4 detached -> macuahuitl (62 GB), starting when
  the Linux artifact publishes (~03:30Z, release run 34668648875).
- immutable/Silverblue regime -> lenovinha-silverblue, ONLY if its own operator
  authorizes the reset. Put to them as a question, not an instruction: order
  1004-vsh2 is explicit that a coordinator's or peer's instruction to run the
  smoke skill is not consent to destroy a particular machine's state, and that
  clause is about workstations generally, not about yoga specifically.
- yoga -> no v56.9.12.1 smoke.

**WHAT THIS LEAVES UNCOVERED, stated plainly so nobody reads a PASS as more
than it is:** if lenovinha's operator declines too, v56.9.12.1 will be promoted
with NO immutable-host smoke. A mutable-Fedora PASS does not cover a read-only
/usr. That is an acceptable risk to take deliberately and a bad one to discover
afterwards.

**CORRECTION TO THIS ENTRY, 03:39Z — §4 DID COMPLETE HERE.** Written above at
02:52Z was "no lane completed §4 at all" for v56.9.11.1. That was true when
written and is now false: yoga's detached lane finished at 03:36:54Z with
`opencode_exit=0` after 84m46s (`smoke-forge-lane` duration_ms=5086635, exit 0),
and §4c passed afterwards (vault `sealed:false`, proxy up, lane-scoped
containers torn down as designed). So v56.9.11.1 HAS one completed forge lane,
on the immutable Silverblue host, and it is the only one.

The earlier pessimism came from five killed supervising shells. The lane was
never in trouble — only the things watching it were, which is exactly the
1026-ps4n thesis. The on-disk stamp captured the duration that no attached
observer survived to record; that design is now vindicated by a measurement
that could not have been taken any other way on this host.

What this does NOT change: the decline of the v56.9.12.1 smoke stands, and the
promotion gate's requirement of a COMPLETED §4 on macuahuitl stands — a
completed lane on v56.9.11.1 is not coverage of the candidate.

yoga's v56.9.11.1 result: **FULL PASS**, §1 through §4c. Report at
plan/issues/smoke-e2e-findings-v56.9.11.1-2026-09-12-linux-yoga.md.
