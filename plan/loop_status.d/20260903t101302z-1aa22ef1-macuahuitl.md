## Cycle — macuahuitl 2026-09-03T10:08Z (meta-orchestration, full mode)

- **Start of cycle:** linux_mutable, linux-next, worktree clean, level with
  origin at eb2ed7602. Daily maintenance `ok:daily-maintenance-current:2026-09-03`
  — skipped the body correctly rather than by eyeball. Credential channel
  `ok:gh-keyring-push-verified`. Boundary snapshotted and round-trip verified
  before touching anything.
- **Drained 982-sguu, the coordinator half — my own defect, filed six hours ago
  after esme paid a full cycle for it.** Every dispatch now carries the
  credential preflight, named in the coordinate skill's assignment section where
  a coordinator is actually reading rather than somewhere they would have to know
  to look. States that `blocked:*` means stop and report, not work-then-discover.
- **EXIT CRITERION 4 ANSWERED AND IT IS THE REASON THIS RECURS: the caller count
  is ONE.** `check-credential-channel.sh` has exactly one production caller in
  the tree (`scripts/forge-validate.sh:28`). Everything else naming it is a
  fixture, a mode-permission regression test, or prose in the meta-orchestration
  skill. It is a manual step in one skill, so a host that never enters the loop
  never runs it at all. That is structural, not a coordinator oversight.
- **I hit the night's own defect while checking that**, and it is the fifth
  instance: `grep -c check-credential-channel scripts/cycle-preflight.sh`
  returned 1 and I read it as "it runs the guard". It does not. The single
  occurrence is a COMMENT explaining why cycle-preflight deliberately does not
  duplicate the guard's message. A count answered a different question than the
  one I asked of it — same shape as `memory.peak` counting page cache and a `ps`
  filter that could not match the process name. I caught it while implementing
  the packet about instruments deciding answers.
- **The remaining half is esme's and it is the more durable one:** a one-off runs
  the guard itself rather than depending on the dispatch to say so. That does not
  depend on the coordinator remembering, and tonight's repeated lesson is that
  the action which feels like starting must BE the correct action.
- **The new `plan-events` discriminator earned itself immediately.** Rolling call
  over my own filed p1s, it separated untouched rows (982-sguu, 979-xyub,
  991-85bh, 990-u3xy — no events at all) from worked ones in one pass. That is
  what chose this packet: it was the only p1 of mine that was genuinely
  unstarted.
- **Convergence:** 216 of 424 ready rows carry events, measured this hour — so
  half the queue any coordinator reads as unstarted has been worked. That number
  is the calibration for every velocity claim made from the ready count, and it
  was not known before tonight.
- **Litmus:** not run. This cycle changed one skill document and ledger
  fragments; `--check` passed on the tree. Recording the omission with its reason
  rather than implying coverage.
- **Operator queue, four plain asks, unchanged:** renew the esmeraldinha PAT
  (check EXPIRY first); macneo's permission to run a forge build on the operator's
  Mac for the 978-juw4 2 GiB measurement (pegs the machine over an hour);
  yolanda's re-open-a-closed-cycle decision; yolanda's two-hourly cron decision.
- **Nothing is blocked.**
