## Cycle — lenovinha 2026-09-04T09:43Z (advance-work; cron :05) — 805-3egx verified, the Vega places and still loses
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T09:35:06Z` (first handoff carrying this, per the recurrence audit):
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
      flow: cycles=8 avg_completed_per_cycle=0.13 avg_commits_per_cycle=1.75 overhead_ratio=14 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      timing: steps=25945 build_check_ms_avg=136548 build_check_mix=mixed:forced=352,memoised=84 litmus_ms_avg=11414 slowest=litmus:tool-materialization:304387 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      repeat: window=since=2026-09-04T09:35:06Z steps=0 top3=- source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      recur: window=7d runs=23807 steps=422 top3=build-check:runs=312:total_ms=45167415:avg_ms=144767:fail_pct=31,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=214:total_ms=7170680:avg_ms=33507:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=214:total_ms=5387049:avg_ms=25173:fail_pct=0 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=57 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=214:avg_ms=33507:fail_pct=0:saved_ms_upper=7137173,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=214:avg_ms=25173:fail_pct=0:saved_ms_upper=5361876,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=214:avg_ms=19690:fail_pct=0:saved_ms_upper=4194008 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=677 ready=444 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=clean traces=current
      verdict: attention:experts-never-called
- READING THE ABOVE. `verdict: attention:experts-never-called` is HONEST AND MINE: I worked this cycle from direct image/container probes and file reads and called no MCP expert, so the answer_rate has no denominator. `repeat: steps=0` because this cycle logged no build steps into the timing log — no `./build.sh --check` ran, since nothing was edited. `skippable: candidates=57` names terminology-dictionary (avg 33.5s, 214 runs, fail_pct=0, saved_ms_upper=7137173), plan-archiver-ready-set (25.2s, saved_ms_upper=5361876) and plan-ledger-tests (19.7s, saved_ms_upper=4194008). Those saved_ms_upper figures are UPPER BOUNDS over a 7d window and 214 runs, not savings on offer: each assumes the step is skipped every time and never catches anything, and all three currently sit at fail_pct=0, which is the argument for memoising them and equally the reason nobody has.
- 805-3egx VERIFIED, NOT REIMPLEMENTED. 32dc4278c ships vulkan-loader + mesa-vulkan-drivers. Confirmed on the built images rather than from the diff: v0.4.260830.5 has an EMPTY /usr/share/vulkan/icd.d and is the image dev-inference was still running (it reproduces the reported bug exactly); v56.9.2.1 carries radeon_icd.x86_64.json, libvulkan.so.1 and an 18.5 MB libvulkan_radeon.so. No rebuild was needed — the fixed image already existed here and only the container was on the old tag.
- THE VEGA PLACES: `library=Vulkan name=Vulkan0 description="AMD Radeon Graphics (RADV RENOIR)" pci_id=0000:05:00.0 type=iGPU total="8.7 GiB"`, qwen2.5:0.5b `offloaded 25/25 layers`, size_vram == size for both it and nomic-embed-text. A fair GPU arm, not a partial offload.
- AND IT LOSES, ON BOTH SHAPES. decode qwen2.5:0.5b — Vega 80.6/81.3 tok/s vs NVIDIA 346.8/395.3 (4.3-4.9x). embed nomic-embed-text over 40 chunks — Vega 36.2 ms/chunk vs NVIDIA 11.8 (3.1x). The packet's open question, whether the iGPU wins prefill-shaped work as it did on the windows host at 460 vs 587 ms/chunk, resolves NO here — structurally, not as a defect. On yoga and yolanda the integrated GPU IS the GPU, so the comparison is iGPU vs CPU; here it is iGPU vs a discrete RTX 3070 and the crossover never arises. The workload-shape routing rule is not contradicted, it just does not bind on a host with a discrete card.
- SO THE SHIPPED POLICY IS RIGHT AS WRITTEN, now measured rather than assumed: dev-inference-ensure.sh sets OLLAMA_IGPU_ENABLE only for gpu-rocm|gpu-vulkan, and lenovinha is gpu-cuda. Enabling the Vega here would be a 3.1x embedding and 4.3x decode regression. Voted APPROVE on both questions anyway: the driver is INERT on this host (the pre-fix image already placed on NVIDIA, because that lane is CUDA and never touched Vulkan), so it costs lenovinha nothing and unblocks the AMD-only hosts.
- A MEASUREMENT ERROR OF MINE, recorded because it would have been a false red and will cost the next person an hour: my first probe reported `GPU: CPU only` with the device passed, the ICD present and OLLAMA_VULKAN=true — visually identical to the original defect. It was not. Without TILLANDSIAS_INFERENCE_TIER set, `_engine_wanted_backends()` returns empty, the entrypoint installs set=core, and libggml-vulkan.so is simply absent from the tools dir (`engine payload: need 'core' (have 'none')`). With the tier set it becomes `need 'core+vulkan'`, installs 82M, and enumerates. Set the tier or do not file the bug.
- CAPABILITY-ROW CORRECTION THIS PRODUCES, left for the coordinator rather than invented: the row reads `present-unusable: gpu/Cezanne [Radeon Vega Series] (rocm-runtime-missing)`. The reason is TRUE and the conclusion it implies is FALSE — ROCm is absent, and the device is usable via Vulkan and fully places. But "usable" is not the honest value either, since it is measurably the wrong choice here. It wants a state meaning "usable, deliberately not scheduled", which is a schema question, not a value I should mint. Same shape as 917-6iwv's matrix "no engine covers lanes" being read as "the expert cannot run".
- 805-3egx RELEASED TO READY, not closed: it is fleet-gated and closes when each host kind has voted, not on one host's evidence.
- HOST LEFT CLEAN: probe container removed; the vulkan payload landed in the container's own /home/ollama/.ollama, not the mounted model cache, so the shared cache was never modified — verified after. dev-inference Up (healthy), both models size_vram == size on NVIDIA.
