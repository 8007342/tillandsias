## Cycle 2026-09-14T01:35Z — yoga (linux-next)

Attested below. Claimed 1132-r4mt (3d38c9744), landed the work at 87f189b92, released the claim — C2 and C3 are still open, so the row goes back rather than closing.

- THE REFRAME: rather than keep trying to catch an intermittent, make the next one name itself. scripts/archive-plan-packets.sh has EIGHT exit-3 sites and ONE printed a machine-readable token, so rc=3 was structurally unable to say which condition produced it — which is why three cycles have gone into asking WHICH reason string yoga saw in-gate. Six more tokens in the fleet's existing could-not-run:<subject>:<cause> grammar; additive, no prose or existing consumer string moved.
- NEGATIVE CONTROL IS THE POINT: build.sh wages an exit 3 through only in a forge and only on the ruby token. An arm asserts the new tokens cannot satisfy that skip — a stale plan binary or an unreadable fragment must still stop the gate. A naming fix must not drift into leniency.
- TWO DELIBERATE NON-CHANGES, recorded not tidied: _require_ruby emits the SAME token as the --check early exit (one condition, two paths; a second spelling would make the forge skip depend on which path arrived first), and the eighth site keeps `refused:no-plan-binary` because that spelling is the fleet-wide refusal grammar — named as a declared exception inside the fixture so the inconsistency stays visible.
- THE FIXTURE WAS WRONG TWICE BEFORE IT WAS RIGHT, both times looking exactly like a pass: it matched `could-not-run:` anywhere and credited a deleted token to the COMMENT explaining it; then it bounded each site's window at the previous exit-3 and let one site borrow another's token, so deleting a real token STILL passed. Only a small window local to the block bites. Caught by mutation, not by reading — inside the fix for a row about instruments that cannot fail.
- Five mutations, each with the diff proven non-empty first: three token deletions, the forge skip pointed at an archiver:* token, and a token moved to stderr. All red by name.
- LAND REFUSED ONCE, correctly: `Ghost-trace ratchet failed` (584-2qq2) — my fixture's @trace header cited spec:plan-ledger, which does not exist. Replaced with the order refs its siblings use. A gate caught an invented spec name before it became durable.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
