## Cycle 2026-08-18T14:25Z — macos (first cycle after the overnight loop closed)

- Overnight loop ended at 06:00 PDT and was closed out; this is a fresh
  operator-invoked session, not a loop continuation. No hourly schedule re-armed.
- **Guards**: all clean; mcp `down:project-info` (filed as 827-rjc9);
  stranded `in_progress=0`. Merged `origin/linux-next`.

- **Closed 689-eux9 — the boot baseline is measured, and daylight made it
  possible.** This packet wanted a real VM boot, which the overnight cycles kept
  (correctly) declining to spend. Two COLD runs, each line timestamped with perl
  `Time::HiRes` because BSD `date` has no `%N`:

      starting VM   0.021 / 0.008 s
      VZ started    0.329 / 0.315 s
      phase READY   8.454 / 8.444 s
      total wall    9.573 / 9.562 s

  ~10 ms variance on every phase, and it **independently confirms** the "~9 s"
  figure the packet was filed with — different session, different method.
- **Read the shape, not the total**: VZ starts in ~0.3 s, so essentially all of
  the ~9.5 s is guest kernel + systemd reaching Ready. Host-side work is noise.
  Anything that *looks* like slow boot is therefore inside the guest's startup or
  is not boot at all — precisely what 689-stig found, where the "slowness" was
  two unbounded waits during which **no VM process existed**.

- **The sizing decision, and the one number that settles it**: in-guest idle
  shows **swap 3888 MiB total, 0 used**. The guest has never been under memory
  pressure, so the 2026-08-11 operator question — is 4 GiB the bottleneck, would
  8 GiB help — is **no** on both halves.
- **Decision: PINNED**, for two reasons, the second being the non-obvious and
  stronger one: **scaling to the host would make every cross-host measurement
  incomparable.** "The guest" has to mean the same thing on every machine or the
  fleet cannot compare numbers across them. Not hypothetical — 798-q4m9 demands a
  bind measurement on a **one-vCPU** guest and refuses a multi-vCPU pass, and
  807-bjjv exists because a shared benchmark is worthless when hosts run
  different workloads. Host-derived sizing silently reintroduces that variance
  into every future measurement.
- Constants at `vz.rs` now carry the measurement, the rationale, the honest
  limit, and a pointer to the sheet (criterion 3).

- **Honest limit, stated in the code and the sheet rather than buried**: every
  memory figure is **idle**. Nothing here measures the guest under a full forge +
  container-stack load. This justifies keeping the default; it is not a claim
  that 4 GiB suffices for every workload.

- Cheatsheet is `tier: bundled` per the crdt-ledger-fragments precedent — a
  project-local measurement record has nothing to pull on demand, and the
  validator's safe-default would have demanded a meaningless `## Pull on Demand`
  section. `check-cheatsheet-tiers.sh` caught that correctly (exit 1, verified
  **unmasked** — the pipe-masking trap again) and is green now.

- **Still outstanding**, unchanged: the attended **m8 UX smoke** macOS release
  acceptance owes; 701-g98y / 774-b529 / 778-n9z2 (attended-or-restage-only);
  798-q4m9 criterion 3 (needs a 1-vCPU before/after, i.e. two boots and a guest
  rebuild); 795-zshi's windows-tray remainder; ledger compaction (linux's,
  now 132+ fragments).
