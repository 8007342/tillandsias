## Cycle 2026-09-14T08:06Z — yoga (linux-next)

1163-3krg, assigned by macuahuitl and claimed by me on trunk at 39d56ec72 (their own claim push had been refused by a hook under edit in their checkout, so the row was still ready on origin and my flip won by being pushed). Landed at ff91de860.

- THE MECHANISM WAS ALWAYS A READ: the next prefix is one above the highest order in the fold, so next-order has necessarily seen every row filed since the caller last looked. It now says so on stderr; stdout stays the bare token because `order: $(tillandsias-plan next-order)` is the documented composition.
- REPRODUCED ON THE LIVE LEDGER: `next-order --since 1157` lists 1158-y3ad and 1160-nvzs four lines apart — the collision visible in one screen from the tool that caused it.
- A DEFECT I INTRODUCED AND CAUGHT, the row's own shape one level down: my first cut identified the caller with resolve_writer_host(), which falls back to std::env::consts::OS — so "linux" matched EVERY linux host's agent_id segment and the baseline became "the highest order any linux host filed". It read 1182, a row macuahuitl filed; yoga would never have been told about anything lenovinha filed after it. The caller is a WORKSTATION now and the same measurement reads 1150 with 37 rows to read. A test pins the difference, not the fix.
- I ALSO COMMITTED 1063-363b's DEFECT WHILE CLOSING THE ROW: backticks in an INLINE set-field value were command-substituted, bash reported "filed: commande introuvable", and the stored prose lost a word. I had used --summary-file for the event in the same breath and hand-typed the field — the habit protected the long text and not the short one. Corrected from a file.
- WHAT MADE THAT CHEAP: the correction needed --replace, from 1151-td46, which I landed on this host hours earlier; the tool printed old beside new, which is how the missing word was visible. A guard written this morning caught a mistake made this afternoon, in the register it was written for.
- Three mutations, each diff proven non-empty first, all red by name. Crate suite 310 + 46 passed.
- No sub-agents used. tokens: main-context only, 0 sub-agent tokens, 0 agents.
