## Cycle — lenovinha (linux_immutable) 2026-08-23T19:53:04Z — closed 866-pvsx, and corrected the premise I filed it on

Full-mode cycle on lenovinha (linux_immutable, Silverblue, linux-next).
Batch: epic=convergence-velocity-milestone seed=lenovinha size=10 budget=10 route=rank:2/7 score=6.425.

GUARDS: daily-maintenance ok:current; cycle-preflight ok (services-down1 — the proxy, already documented on 767-es4w last cycle, not re-filed); credential ok:gh-keyring; branch ok:branch-linux-next; mcp ok:experts-healthy + ok:surface-exposed; capability row ok; boundary clean.

CLOSED 866-pvsx (completed, with evidence). I filed this packet last cycle claiming the fold "silently ignores unrecognized entry keys" and that the general case was untouched. THAT WAS THE WRONG CAUSE. fragment_coverage_gaps has detected this exact shape all along — an events entry with no `event:` block, no definition fields required. What is actually broken is WHERE THAT ANSWER GOES: the guard runs at exactly two call sites, both inside compaction, and what it does with a gap is refuse to DELETE the fragment. That protects the bytes and tells the author nothing, and it only runs when the fragment count makes compaction eligible. My fragment was never compacted; I ran check, and check never asked. A reporting-channel gap, not a detection gap — 797-5kqe again, and the evidence was 300 lines from the fold I had already read.

THE FIX: fragments::overlay_coverage_gaps(index) runs the same pass at READ time; check emits one `dropped-entry: <path>: <reason>` per gap. Reported, never refused fleet-wide (699-dycj unchanged: build.sh runs check everywhere, so a refusal makes one host's typo every host's red build); --strict-fragments arms the refusal at exit 3. A dropped entry now downgrades `ok:` to `incomplete:` exactly as an unreadable file does, since filed work absent from the ledger is the same partiality — but the two causes stay NAMED separately, because one is repaired by fixing a parse error and the other by rewriting an entry.

THE PART I WOULD KEEP IF I COULD KEEP ONE THING: the diagnosis is now split. Definition fields present gives the 812-d45t message ("move it under packets:", listing the fields); anything else gives the 866-pvsx message naming the offending key and pointing at the set-field subcommand. Reporting my `set_field:` typo as a misplaced definition would have sent me hunting for a lost packet that never existed. The first message a defect gives you decides where you look next.

VERIFIED: four scenarios plus negative control on the reproduction — unrecognized directive named and citing 866-pvsx not 812-d45t; misplaced definition keeping 812-d45t; --strict-fragments exit 3; well-formed event silent with `ok:`. Hermetic overlay_coverage_gap_tests 5/5 (incl. unparseable base yields no gaps rather than panicking). Full crate suite 213+24 green. NO FALSE POSITIVES ON THE LIVE CORPUS: real check still reports ok: 552 packets — that mattered more than the unit tests, since a read-time guard firing on the existing overlay would be switched off within a day.

GATE DISCIPLINE, worth recording: my first gate run printed "Rust code not formatted" and my tail-based check read the PIPELINE's exit 0 as the gate's. It was not — the gate had stopped before writing its stamp. Caught it by grepping for the stamp line rather than trusting the code. Re-ran with the exit code captured directly: GATE_EXIT=0, stamp recorded. This is the same shape as the skill's warning about piping the gate through tail and losing the Failed checks block.

NOTE ON 799-tb7q (no new row): pushing this cycle's claim, the pre-push plan-only lane printed "note: yq absent — YAML parse delegated to tillandsias-plan check". The hook is a bare-yq caller on the HOST, and this Silverblue box has yq only inside the builder toolbox, only since last cycle's init-set change. It degrades gracefully so it is not a defect, but it is a concrete already-degraded caller for the sweep's DEV-HOST group, and it shows the init-set change alone does not reach callers on the host side of the toolbox boundary.

FRESHNESS: scripts/resolve-release-run.sh, verdict=refreshed, no change. Fixture 6/6 green; twin of resolve-open-pr.sh (audited last cycle, same verdict) and the pair still reads as one pattern. Its extra no-tag-given scenario is correct — the tag is an argument here and the branch pair is not.

ADVISORY: compaction not eligible (13 fragments). Deslop not due (order=867 last=834 delta=33 hours=90). triage ungrouped=22 of 166 eligible. flow overhead_ratio=4.08 over 10 cycles.
