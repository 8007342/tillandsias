## Cycle — lenovinha (linux_immutable, linux-next), 973-q4y8 (fault C of 972-cvdg)

CLOSED 973-q4y8 at 5c3103011, and litmus:podman-idiomatic-security-flags now
runs 9/9 END TO END here — the thing I flagged as not done when I closed
972-cvdg an hour ago. macuahuitl ran it on a host with the image, found two
faults beneath mine, fixed those, and deliberately stopped at the design call.

THREE STACKED FAULTS, EACH HIDING THE ONES BELOW, and that is the finding
rather than any one of them. Step 1 called `{{.Security.Rootless}}`, a podman
info path that does not exist, and the runner abandons a test at its first
failing step — so NO security step in that file had ever executed. My
tautology fix could not have revealed the wrong assertions, because nothing
was running to reveal them. Then the image lookup anchored
`^tillandsias-forge:` against podman's `localhost/...`. Only with both fixed
did the assertions run, and they expected a vocabulary podman does not use.

MY OWN 972-cvdg CLAIM WAS WEAKER THAN IT READ. I wrote that those five steps
"reported PASS on every run it ever made". True, and the sharper statement is
that they never ran at all.

THE DECISION, AND THE MEASUREMENT THAT DECIDED IT. I intended to test keep-id
by comparing the container's `id -u` to the host's. That is the obvious test
and it is a FALSE PASS: measured here it returns 1000 both with keep-id and
without, because this host's uid equals the image's USER. I would have shipped
a step that passes on an unhardened container — the exact defect class this
packet family is about, arriving through my own repair for the third time
tonight. Caught only by running both arms instead of reasoning about one.

What discriminates is a bind-mounted host-owned file: 1000 inside with
keep-id, 0 without. Verified both directions on real containers. On that same
unhardened container step 4 still reports NON_ROOT_UID=1000 and passes, which
is the proof that step 4 cannot stand in for keep-id.

SO EACH FLAG GETS THE STRONGEST EVIDENCE AVAILABLE FOR IT, not all three
through one inspect. cap-drop behaviourally (the declaration half DROPPED, not
re-expressed — matching podman's expanded CapDrop adds nothing over steps 5
and 6 and breaks on the next podman). keep-id behaviourally (UsernsMode reads
"private" for any private userns, so no spelling of that assertion can
identify it — re-expressing would have looked stronger and proved less).
no-new-privileges and label=disable by declaration, because nothing here can
observe them and podman records both verbatim; the step name says so.

NOT MINE, STILL RED: litmus:podman-idiomatic-event-driven (killed at budget,
censored) and litmus:podman-idiomatic-storage-isolation-runtime. Different
files, untouched. The suite is at 1/3 and is worth someone's cycle.

metrics:
  gate: TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check green
  e2e: litmus:podman-idiomatic-security-flags 9/9 on real containers
  negative control: KEEP_ID_NOT_MAPPING:host=1000 seen=0 without the flag
  guard: ok:litmus-steps-can-fail:411 file(s) checked
  daily-maintenance: still due:stale:2026-09-02, deferred again; macuahuitl reports the same, so it is drifting fleet-wide
