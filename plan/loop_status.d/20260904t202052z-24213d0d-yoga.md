## Cycle — yoga 2026-09-04T20:40Z (advance-work; recovered from a wedged shell) — 998-qrwu doc fixes, and the quota that cost three fires
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T20:20:52Z`:
      repeat: window=since=2026-09-04T20:20:52Z steps=123 top3=build-preamble=4,build-check=2,build-check-memoized=2
      skippable: candidates=27 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=141:avg_ms=17743:fail_pct=0:saved_ms_upper=2484138,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=130:avg_ms=15860:fail_pct=0:saved_ms_upper=2046031,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=141:avg_ms=14247:fail_pct=0:saved_ms_upper=1994601
      plan: packets=705 ready=456 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=clean traces=current
      verdict: attention:experts-never-called
- READING IT. The plan-ledger-tests candidate is STILL printed under its old key (`-p tillandsias-plan-all-targ`, runs frozen at 130 while its neighbours moved to 141) — that step was replaced by the workspace run in 1003-444f and no longer fires. Its history is a history of an invocation that no longer exists, which is the second cycle I have flagged it; anyone memoising against that key would key on a step that never runs. The other two moved 133 -> 141 normally.
- THREE FIRES MISSED (15:25Z, 17:25Z, 19:25Z) to a wedged shell, not to idleness. I built a cargo target/ inside the session scratchpad on a quota'd tmpfs; it filled the quota, and from then on EVERY Bash call failed with exit 1 and no output — bare `true` included — because the harness could not write its own temp files. Confirmed rather than inferred when a zero-byte Write returned `EDQUOT`. It is not fixable from inside a session: freeing quota needs a shell, and macuahuitl's empty-overwrite idea fails because the tool allocates before it replaces. The operator ran `git diff > ~/997-linux-half.patch && rm -rf <worktree>/target`; /tmp is back to 5% used. NEVER BUILD IN THE SCRATCHPAD — macuahuitl wedged the same way before today's restart, so it is a fleet trap, not a one-off.
- WHAT WAS NOT BROKEN, because the operator asked: git, never. The checkout stayed clean and in sync throughout; the bloat was in /tmp, outside the repository. A wipe-and-force-rebase would have freed nothing, could not have been run without a shell, and would have destroyed the uncommitted slice.
- 998-qrwu DOC FIXES LANDED (f862dd860). Three stale claims, all in text that is only read when something is already wrong. (1) low-end-cpu-inference-floor.md told a reader hitting 000 that the proxy "fails to start once /tmp/tillandsias-ca is wiped on reboot" — PRESENT TENSE, in a live runbook, and false since 998-3z6g. The symptom half is kept, the cause is marked past, and the new location is described as "HOME-relative" IN PROSE: naming the path would have planted a fresh state-root literal and tripped macbookair's new guard (1027-539s) on its first run. (2) check-ca-path-literals.sh's header claimed 36 literals and a pending migration; both landed and the count is 1. It now also states what it does NOT watch — its subject is the PRE-migration literal, never the new CA path and never the root — because two hosts assumed otherwise today. (3) Three remedies named `tillandsias --ensure-enclave`; measured here, the installed v0.4.260830.5 has zero mentions of it in --help, so the remedy fails with an unknown flag on the recovery path it serves. Each now says update rather than substitute --init.
- THE GATE CAUGHT ME EDITING A GENERATED FILE, and it was right. images/default/cheatsheets/ is a DERIVED tree; the authored source is cheatsheets/. The pre-push hook refused with `cheatsheet-image-sync:drift derived-tree-index-stale`, and sync-image-cheatsheets-for-commit.sh printed `skip:no-authored-cheatsheet-change` until I staged the real source, then `ok:cheatsheet-image-current`. Same family as the three stale claims above, found in my own hands rather than someone else's.
- 997-e4v2's Linux half is NOT mine and did not land from here. lenovinha landed it independently at 0b25215b6, reaching the same six tests, the same four ~/src asserts and the same 944-jaef reroot. My patch survives at ~/997-linux-half.patch on this host as a convergence diff, a record item rather than a dependency.
- TOKEN INVESTIGATION, my contribution and its limit: yoga reports `gho_` (OAuth, keyring, live after the operator's refresh), which falsified the PAT-backing hypothesis, and gh 2.97.0, which is consistent with the minting-binary prediction. I said explicitly that a pass is not a proof — version and mint date are confounded on pirria — and proposed the discriminator (a 2.100.0 host with a pre-upgrade token). yolanda and macbookair answered it and refuted the running-binary form. No "do not upgrade gh" guidance issued; it rests with the operator's security log.
