## Cycle 2026-08-26T02:08Z (macos tlatoanis-macbook-air — the macOS lane ran against a published release for the first time: PASS)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 30. Boundary snapshotted first, guards green, daily maintenance current.
- **RAN `/smoke-curl-install-and-test-e2e` AGAINST `v0.4.260826.1` — 14 of 14 assertions PASS.** Steps 0-3; step 4 (`--opencode` forge) is Linux/Podman-only. Evidence at `plan/issues/smoke-e2e-findings-v0.4.260826.1-macos-2026-08-26-macbook.md`, confirmed counted by `promote-stable.sh --dry-run` → `have: macos`.
- **The line this project has never been able to check on this platform**: `tillandsias-tray 0.4.260826.1 (git 341ab0010, built 2026-08-26T02:43:10Z)`. Before 635-bhkb closed a few hours ago every macOS build answered `0.1.0`, so an exact-tag assertion was not expressible and **no previous macOS "pass" ever verified which artifact it tested.** The pre-run capture shows the old binary saying `0.1.0` one last time.
- **The destruction was a real clean room, recorded as such**: both substrate directories EXISTED before removal (12 GiB), and the new `rootfs.img` is provably newer than the post-destruction marker — so "fresh image" is asserted rather than inferred from an exit code.

- **I BOUNDED WHAT THE PASS MEANS, unprompted, while reporting a clean 14/14**: the VM was **provisioned but never BOOTED**. `--provision` writes the image; it does not start the guest. So guest-side behaviour, the control wire, vault init and the guest/tray skew check are all untested — `guest_version` is `null` for exactly that reason. **A macOS PASS here means "the published artifact installs, verifies, and provisions a clean room", NOT "the product works on macOS."** An operator reading `missing=<none>` would otherwise infer three platforms fully exercised. The coordinator is putting that boundary in the promotion record verbatim.

- **THREE FINDINGS FILED — filed, not fixed, per the skill's guardrail** (this skill installs, observes and files; fixes belong to workers claiming the packets):
  1. **`${PIPESTATUS[0]}` is bash-only and macOS has defaulted to zsh since Catalina.** Measured **empty** under zsh 5.9. Every exit-status capture in the runbook — 727-kmks's and the ones I added tonight — degrades in the platform's default shell. I ran the whole smoke under explicit `bash -c`, and said so; otherwise **this PASS would have been partly unverifiable by its own instruments.** Fifth dialect finding of the night, same family as `TIMEFORMAT`/`TIMEFMT`: **the runbook names a construct without naming the interpreter.**
  2. **The installer AUTO-LAUNCHES the tray and begins provisioning** ("Tray started… Provisioning runs in the background"). A live tray (PID 59321) was writing the VM directory immediately after step 1, so **step 2 destroys underneath a live writer — and its `pkill` does not wait for exit.** Added a bounded wait plus SIGKILL escalation with verified exit before `rm -rf`. Not a release defect; a runbook-ordering defect that produces nondeterministic clean-room failures on the one lane that had never run, and would have been diagnosed as flakiness.
  3. **`install-macos.sh` logs `channel: stable` while installing a pinned prerelease.** The pin works — right asset, sha verified — but **the log is the artifact a smoke report cites to prove which channel was exercised**, so a reader auditing this run's evidence would conclude it tested stable.

- **Embedded the raw outputs in the report rather than only citing `target/smoke-e2e/*`**, which is not committed: a citation to an uncommitted directory is proof that does not survive the cycle — the 651-2x5s lesson applied where nobody had applied it.
- **Two runbook defects fixed BEFORE the run, both in the executed path**: the GNU-only `\S` that would have fed every curl a malformed URL, and the `|| true` that would then have swallowed the failure. Together: a green run against nothing. **A PASS tonight means something a PASS three hours ago would not have.**
- **The naming trap nearly cost the whole thing.** My first filename was `smoke-e2e-findings-v0.4.260826.1-2026-08-26.md` — no `macos`. The gate reads platform from FILENAME and first `#` heading only; a flawless report would have contributed nothing and the gate would have said `missing=macos`. Renamed, then **ran the dry-run to confirm rather than trusting the format** — verifying that evidence is counted is a different act from writing good evidence, and only one of them is checkable.

- **HOST STATE LEFT BEHIND, for the next cycle**: `/Applications/Tillandsias.app` = the release `0.4.260826.1`; `/Applications/Tillandsias.app.bak` = the previous local build (`0.1.0`, git 0548ee1f2), created by the installer; `~/Applications/Tillandsias.app.pre-e2e-20260825` = the stale bundle moved aside before the run. Substrate re-provisioned at 1.2 GiB (was 12 GiB), no live tray, 817 GiB free. **None of the three copies deleted** — the two aside-copies are the operator's files and reversibility was the whole point of moving rather than removing them.
- **Release still awaiting the linux and windows legs**; the promote gate correctly reports `missing=linux,windows`.
