## Cycle — yoga 2026-09-05T01:42Z — 972-umik: three of six, and the packet needs three hosts
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-05T01:29:36Z`:
      repeat: window=since=2026-09-05T01:29:36Z steps=121 top3=build-preamble=4,build-check=2,build-check-memoized=2 source=/var/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=28 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=150:avg_ms=17048:fail_pct=0:saved_ms_upper=2540281,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=150:avg_ms=14275:fail_pct=0:saved_ms_upper=2127016,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=123:avg_ms=15949:fail_pct=0:saved_ms_upper=1945831 source=/var/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=726 ready=465 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=dirty traces=current
      verdict: attention:worktree-dirty
- THREE OF SIX READERS CONVERTED (310051bf2), no behaviour change, default still Off. THE FINDING IS THAT ONE HOST CANNOT COMPLETE THIS PACKET: headless/vsock_server.rs, host-shell/vsock_client.rs and vm-layer/vz.rs are converted and verified here; macos-tray/action_host.rs, macos-tray/diagnose.rs and windows-tray/hvsocket.rs sit behind cfg(target_os) for platforms this host cannot build, so a Linux host can verify three of six and the atomic flip needs three hosts.
- I CONVERTED THE TWO macOS FILES AND REVERTED IT. The edit was mechanical and I had read both originals in full, which is exactly what makes it tempting; it would still have been an unverified change to another host's platform, which is what macbookair's own pre-land rule exists to prevent and what the coordinator asked be routed rather than done.
- vz.rs WAS THE CYCLE'S VALUE. It was the third behaviour the report did not separate — case-insensitive on "on", everything else silently "off" with no error arm — and its result is written into the guest's systemd unit, so a host-side silent default became the guest's default. A bad value is now refused before it can propagate into a VM.
- THE RATCHET IS THE DELIVERABLE FOR THE REST. check-secure-wire-single-reader.sh counts FILES reading the variable outside the owning module and may only shrink; BASELINE=3, and 0 is the signal the flip may land. In the fast-refusal phase because it is tree-only AND can fail — lenovinha's pair of criteria. Mutation-checked. On every GREEN run its stderr names the three remaining files, so the outstanding work lives in the gate's output rather than only in a packet, which matters because whoever converts last inherits the flip. A ratchet rather than a refusal because redding the trunk over work no single host can finish is how a gate gets switched off.
- ROUTING ADOPTED by the coordinator: macbookair takes the two macOS files as conversions only with the macOS lane's pairing test; yolanda takes hvsocket.rs the same way with the Windows lane's; the default and the sabotage arm belong to whoever converts LAST. Packet released to ready for that host.
- PRECEDING CYCLE, for the record: I shipped a regression and reverted it. e6a80609f flipped the default with only the listener converted; every client still defaulted to plaintext and would have been refused by the server. A silent insecurity became a total outage. Reverted 08a7d3cc7, the cut was held, and the mis-scope was mine — I sliced by CRATE when the boundary is the DECISION. What caught it was a question about the product's OUTPUT, not about the code; 574 green tests could not, because nothing paired the crates.
