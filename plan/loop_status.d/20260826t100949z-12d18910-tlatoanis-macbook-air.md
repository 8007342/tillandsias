## Cycle 2026-08-26T10:09Z (macos tlatoanis-macbook-air — closed 629-t6bx; the raw count was real and the conclusion it invited was wrong)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 36. Boundary first, all gates green.
- **FOURTH CONSECUTIVE CYCLE with the same batch, and now THREE of its eight items are packets whose macOS half I have already done** — 731-eupn (released for Windows), 776-jcf3 ("macOS: PROVEN"), 797-5sqs (released for a forge host). That is a fourth and sharper data point for **884-hfsj**: the selector keeps offering this lane work whose macOS portion is complete.
- **Closed 629-t6bx**: `locales/terminology.toml` (11 terms, seeded with the operator's "GitHub" ruling) plus `scripts/check-terminology.sh`, wired into `./build.sh --check`. POSIX sh + awk — **no ruby (order 63), no yq** — because neither exists on every host and this must run on all of them (order 261).
- **THE SCOPING IS THE DESIGN AND IT WAS MEASURED BEFORE IT WAS WRITTEN.** Tier 1 is user-visible text = TOML **values**, never keys. A naive `grep -rn podman locales/` returns **170 hits, nearly all of them KEYS** (`no_podman`, `images_no_podman`, `[menu.github]`). A gate that flagged those would have been unusable on its first run and switched off rather than fixed.
- **A finding the packet did not have.** It records tier 1 as having "exactly one known offender". Measured over values: lowercase `podman` **128**, capital `Podman` **28** — an order of magnitude larger.
- **And why that is NOT a defect, which is the more useful half.** en.toml carries, in a single string: `"(Podman image storage is managed by podman — see 'podman system df')"` — the capital is the product, the lowercase is the executable, **both correct**. A dictionary asserting "podman is wrong" would flag 128 legitimate uses and be disabled within a day. Hence the `lower_ok` flag: **it exists because of a measurement, not as a defensive guess.** Same shape as everything else tonight — the raw count was real and the conclusion it invited was wrong.
- **Genuine residual recorded rather than silently fixed**: `"Podman is not available"` vs `"(podman not available)"` — same concept, two spellings, adjacent strings. That is a UX-curation call on specific strings, and this packet's own note says applying a correction to any specific user-visible string needs the operator's ruling for that string.
- **Hand-falsified: one positive, three negative controls.** Wrong variant in a VALUE → `violation:terminology:1`. Same variant in a KEY → ok. The dual-form Podman string → ok. Canonical terms inside **translated** strings (`Iniciar sesión en GitHub`, `GitHub-Anmeldung`) → ok. **The last two are load-bearing**: without them, "make the gate pass" is satisfiable by requiring whole strings to match `en`, which the packet forbids and which would break every translation.
- **TIER 2 SURFACED WITH COST, and the measurement reframes the question**: 206 occurrences across 16 files, 4 identifiers — and **eleven are `kind()` strings that cross the control wire** (`control-wire/src/lib.rs:696-697` renders `ControlMessage::GithubLoginStatusRequest` as the literal string). **Tier 2 is a wire migration, not a rename**, needing the same mixed-version handling as CAP_EXEC_ARGV_VECTOR (795-zshi) and CloudRefreshOutcome (731-eupn). That is the fact the operator needs, and it is why bundling the tiers would have turned a data file into a protocol change. Gate stays tier-1 unless ruled otherwise.
