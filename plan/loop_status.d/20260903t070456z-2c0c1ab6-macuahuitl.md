## Cycle — macuahuitl 2026-09-03T07:05Z (operator prompt; fleet dispatch + macOS provisioning adjudication)

- **Nine packets filed**, seven of them from peer findings I did not have:
  979-xyub (a curl-installed macOS host cannot publish a capability row),
  979-3kfb (the qemu-free raw path is dead), 980-krib (idleness inferred from
  commit timestamps), 981-n5vx (forge memory ceiling declared closed on a
  helper nothing calls), 981-a4t4 (ten of seventeen openspec changes >90d),
  982-sguu (dispatch has no credential preflight), 982-cs43 (every rebase
  stales the gate stamp). Tokens 980-xcaf/ja2m/7rng minted for macneo.
- **I was overruled four times tonight and every one of them was right.**
  macneo: my "the below-crossover case is untested" was false — it is tested
  TWICE and both tests assert the harmful behaviour as correct. Then I approved
  a fix that repairs DEAD CODE: the xz exec is reachable only from the dead
  fetch_recipe_artifact, and convert_qcow2_to_raw spawns qemu-img twice, so
  replacing the resize leaves the convert. yoga: my brief contradicted my own
  ruling at 7f5fbeac1 on backfill scope. yolanda: "idle since 22:35Z" was true
  of commits and false of work. lenovinha: "still ready and unclaimed" was true
  and misleading — the forge had landed it at 8b08a0305 and released the claim.
- **THE SHAPE OF THE NIGHT: a summary standing in for the artifact it
  summarises.** Five instances, four of them mine. A packet context block read
  as current state; a query fold read as the whole ledger (ARCHIVED resolves
  only through status); commit timestamps read as work; a `ready` status read as
  claimable; and my own operator-queue entry saying "two" stale openspec changes
  when it is ten. 980-krib carries the coordination half.
- **THE OTHER SHAPE: a function called only by its own tests.** Three today —
  fetch_recipe_artifact (979-3kfb), compute_memory_ceiling_mb (981-n5vx), and
  in the second case the litmus that "covers" the requirement announces it in
  its own step title, "pure helper". A spec MUST about argv discharged by a test
  about a function, green on every host, flag never emitted.
- **macOS provisioning adjudicated.** Answered the question that decides the
  qemu design: there is NO aarch64.img artifact, all three published SHAs are
  official Fedora artifacts, and recipe-publish CI has never existed
  (.github/workflows holds one file). fetch_recipe_artifact is a survivor of the
  Phase 2b pivot. macneo then measured the image: 10,007 compressed clusters vs
  1,207 normal, so 89% of allocated data is deflate — a normal-clusters-only
  reader would produce an image 89% garbage that probably still boots. They took
  option (b), pure-Rust qcow2, with flate2 already linked and qemu on the host as
  a byte-for-byte oracle. I required the oracle become a committed small fixture,
  because the SHA pin covers the qcow2 download and NOT the conversion.
- **Fleet: 7 idle hosts dispatched, 4 reported back within the hour.** pirria
  (floor) found the forge memory ceiling. esme (floor) diagnosed the 403 to the
  exact hop and stopped itself from filing a wrong cause. lenovinha closed
  969-nhh7 criterion 4 with an independent verification. yoga had already closed
  its cycle an hour before I dispatched it.
- **Merged osx-next twice** (macneo's claim + forensics, then the sizing fix).
  windows-next 0 ahead / 153 behind — yolanda will merge before any push.
- **Operator queue, three items, all plain asks:**
  1. On esmeraldinha, renew the GitHub PAT — check EXPIRY FIRST, the 2026-08-15
     precedent (809-w2xy) was a lapsed token and not a wrong scope, and the two
     are indistinguishable from inside a forge. Confirm repo write to
     8007342/tillandsias, re-seed Vault secret/github/token, verify with
     ls-remote showing authorized/<epoch>. Do NOT import host credentials.
  2. yolanda asks whether to re-open a cleanly closed cycle for scope beyond
     their assigned story, costing a re-acquired lock and several ~520s gate runs
     on their machine and budget.
  3. yolanda asks whether to arm the two-hourly cron: seven days, a full cycle
     per fire. They correctly refused to self-authorise recurring spend, and my
     original "re-arm where appropriate" wrongly implied they could.
- **Nothing is blocked.** Disk recovered (io full 0.07% avg10, 154 GB free), so
  macuahuitl-tillandsias-forge is unpaused for the compaction-linearity work;
  the second `cp -a plan/` and 964-tzmp stay paused.
