## Cycle — lenovinha (linux_immutable) 2026-08-24T13:53:17Z — the "197 exposed steps" was one

Full-mode cycle. All guards green; daily gate current.

CLAIMED AND CLOSED 870-fv7k by doing the measurement it asked for first — and the answer changed its own recommendation. I filed it yesterday saying 197 steps share the false-pass shape. 197 was a STRUCTURAL count (non-quiet grep chained with && and no sentinel): how many steps COULD lie, not how many DO.

THE LIVE EXPOSURE IS ONE. Measured without mutating anything: a step lies right now when its command exits non-zero AND the expectation still matches what the command printed on its way to failing, which is directly observable. Of 2026 steps carrying an expectation, 887 were safe to execute read-only; 856 exit 0; 28 exit non-zero and would correctly be reported FAIL; ONE exits non-zero and would report OK.

THE ONE: litmus:methodology-accountability-shape step 4 grepped a directory list including `src-tauri`, WHICH DOES NOT EXIST here. grep exited 2 — an error, not "no matches" — on every run since. It passed anyway: no -q, so it printed the 69 matches from the directories that do exist, and the expectation was the bare string `methodology-accountability`, present in every one of those lines. The step matched its own output while erroring. Two silent consequences: a directory could vanish from the list unnoticed (one already had), and the trace surface could collapse to one file while the step stayed green. FIXED — nonexistent path removed, assertion is now a FLOOR on file count (44 today, floor 10), sentinel is the only thing that satisfies the expectation, plus a negative control over an empty temp tree that 634-39ik demanded and that this step needed most: what it lacked was never a better grep but any evidence it could fail.

TWO OTHER SCAN HITS WERE MY OWN HARNESS and I checked instead of reporting them. The stdlib portability steps 6 and 8 call helpers the real runner provides by sourcing LITMUS_STDLIB, which my scanner does not do; both pass legitimately through run-litmus-test.sh. Reporting "3 mis-passes" would have been the same mistake I made twice this week — reading a number off a harness I had not verified.

THE FIX RECOMMENDATION IS NOW DIFFERENT FROM WHAT I FILED. With 1 of 886 actually lying, a runner-side exit-code rule is a bad trade — it touches every step and risks the inversion 661-nm73 already recorded, to catch a population of one. A 197-step sweep is also poor value: 197 edits against one live defect, and the shape is a fragility rather than a bug. The proportionate answer is the third option neither framing listed: KEEP THE DETECTOR, FIX INSTANCES AS THEY APPEAR. Committed as scripts/scan-litmus-false-pass.sh — read-only, advisory, gating nothing, portable (derives its root; my first draft hardcoded my own absolute path), with BOTH limits in its header: it sees fewer than half the steps (1141 skipped as not provably read-only — a refusal to run podman resets and installers to find out, not a claim they are clean), and it approximates only the matcher's final fallback, so every hit needs confirming against the real runner.

RESIDUAL, stated rather than buried: those 1141 unscanned steps could hide more. Making them scannable means sandboxing, which is a different packet if anyone wants it.

FRESHNESS: openspec/litmus-tests/litmus-chromium-variants-launch-shape.yaml, verdict=refreshed, EXECUTED (PASS) and also run through the new detector — none of its nine steps mis-pass. Meaningful because the properties it pins are SECURITY ones (safe-by-default window type, remote-debugging flags confined to the debug branch, per-launch mktemp profile with cleanup trap, cap-drop/userns/no-new-privileges), so drift is not cosmetic. Noted that these ARE source pins, legitimate here since there is no runtime to interrogate pre-build, but they are the shape that hid the defect above.

ADVISORY: compaction not eligible. Deslop not due.
