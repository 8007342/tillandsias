## Cycle 2026-09-05T04:30Z — esmeraldinha (windows floor) — 1004-pg9p metrics round

No new release since v56.9.5.1, which this host smoked at 03:46-03:53Z, so the
round is the metrics paste and that is a discharge (1004-pg9p step 5).

repeat: window=since=2026-09-05T04:29:59Z steps=0 top3=-
recur: window=7d runs=380 steps=117 top3=build-check:runs=15:total_ms=12900466:avg_ms=860031:fail_pct=80,step:checking-every-litmus-file-is-bound-retired-or-grandfathered-660:runs=3:total_ms=2011505:avg_ms=670501:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=3:total_ms=412197:avg_ms=137399:fail_pct=0
skippable: candidates=1 floor_ms=2000 min_runs=5 top3=build-preamble:runs=16:avg_ms=11847:fail_pct=0:saved_ms_upper=177707
timing: steps=380 build_check_ms_avg=860031 build_check_mix=mixed:forced=15,memoised=1 slowest=step:checking-every-litmus-file-is-bound-retired-or-grandfathered-660:1141496

REGIME: esmeraldinha, Intel N100 4c/4t, 15.8 GB (7.9 GB visible in the distro),
Win11 26200, tier low-end, gate re-execs into tillandsias-build, drvfs/9p
checkout. `repeat:` is empty because no gate ran inside this cycle window, which
is correct rather than missing.

TWO NUMBERS HERE MUST NOT TRAVEL, and both are artefacts of this host's night
rather than properties of the floor:

1. `build-check fail_pct=80` over 15 runs. Twelve of those failures are mine:
   two gates launched from inside the distro (no CARGO_TARGET_DIR, 1030-i2p8),
   three arms of a parallelism trial that ran concurrently after a driver I had
   wrongly declared dead, and reds on 1022-px54, openssl (1042-svey) and the
   leak-ratio guard (1051-f7e2) which are each now fixed. The product's gate is
   not 80% red on this host; the experiment was. The only figure that describes
   the current gate is gate 10: **25m17s, exit 0, forced, idle, no builder
   re-init and no plan-binary rebuild, including an 18-crate post-merge compile.**

2. `skippable: build-preamble saved_ms_upper=177707`. Still the cold/warm
   bimodal false positive first flagged at 5 runs and now louder at 16: the
   preamble costs ~72 s cold and ~1 s warm, and `saved_ms_upper` is total minus
   one mean, so it re-labels the cold runs as savings. Skipping the preamble
   saves about a second on a warm host and skips hook installation and sidecar
   staging on a cold one. `min_runs=5` is satisfied; the metric is still wrong
   for the same reason, which is the point — more observations do not fix a
   summary statistic applied to a bimodal input.

`660-ryhn` (checking every litmus file is bound/retired/grandfathered) holds as
the slowest single step at 3 observations: avg 11m11s, max 19m01s, 0% failures.
That is the durable floor finding from tonight's timing data.
