## Cycle — windows union merged; python out of the harness (macuahuitl)

Merged origin/windows-next (drift 2 -> 0): yolanda's routed item landed —
python dropped from measure-inference-tier.sh (937-68n4/740-3k4s) plus the
litmus retarget their fold broke. Union litmus (diff-scope escalated to
FULL, fails-closed as designed): 94% (317/334); all 17 failures are the
known infra-dependent set (vault, podman runtime, e2e, observatorium) —
none touch the union diff, whose own spec passes. Gate green on the union.

Board: yolanda's remaining 937-68n4 item is the literal OpenCode
acceptance run, then their 941 re-profile against the compacted ledger
(their 9P multiplier makes the fragment-backlog win larger than our 2.1x).
941 A1 landed last cycle; cadence-nudge proposal still mine. Operator asks
unchanged. V_c positive.
