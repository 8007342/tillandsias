## Cycle 2026-09-15T17:22Z — macuahuitl, scheduled release-tier exercise: rc=1, and the tier is STILL unmeasured

First ci-full this host has ever run: head eff228c3e, rc=1, 2118s (35.3 min), pass rate 98% (363/367). Freshness AFTER is
UNCHANGED IN SUBSTANCE — the run wrote a record but ci_phase=pre-build only, so there is still no full-tier answer here.
THE PRECONDITION NOBODY HAD STATED for 890-27mv's cadence: a host carrying any pre-build red can NEVER record a full-tier
run, because the gate stops at the first failing phase. yoga's timer would fire daily, spend 35 minutes, and write a
pre-build-only record every time. Arming it is worth less than clearing the reds first. Two named failures, neither
touching anything landed today: litmus:mcp-expert-health-probe-shape 18/20 (cycle-metrics cannot express health=ok-unexposed
distinctly; observed unexposed_lines=0 exposed_ok=0 exposed_outage=0) and litmus:enclave-service-health-shape 3/5 (its
hermetic suite reports 22 passed 1 failed; which arm is owed a read). The first is the THIRD cycle-metrics red today after
two in methodology-accountability, on the instrument independently found this morning to have zero inputs fleet-wide —
failing its own shape tests in two specs AND unused, three signals converging.
SIDE EFFECT HELD FOR THE OPERATOR: --install bumped VERSION 56.9.13.1 -> 56.9.15.1 and synced four crate Cargo.tomls. A
VERSION bump reaching trunk changes every host's release-preflight monotonicity read, so it is not landing as a side effect
of a measurement and it is not being reverted silently either. I also CORRECTED MY OWN CLAIM that this blocked my push lane:
the plan-only lane scopes the OUTGOING DIFF, not the worktree, so the filings below went out unimpeded.
Filed 1202-fh6y (one artifact path, two incompatible consumers — pirria's toolbox reds and this host's mid-ci-full rebuild,
two hosts with opposite outcomes) and 1203-dzxn (a platform host never reaches the land retry loop, with both candidate
fixes recorded WITH their disproofs, one being the union-is-green assumption already measured false on 2026-08-15).
tokens: cycle=release-tier-20260915T1646Z main_ctx=92000 subagent_tokens=0 agents=0 by_model=none.
