## 2026-09-06T06:00:00Z forge-tillandsias — gate episode and environment seams (post-commit addendum)

### The gate episode (five `./build.sh --check` runs this cycle)

1. **Openssl absence (host-tools, 989-ykks)**: the fork ships no `openssl` CLI (by design —
   the CA comes from the host), and the 1042-svey required-tool entry (`check-host-tools.sh`
   :104) now declares it UNCONDITIONALLY, so every forge reds the host-tools fixture
   (3/17, arms 5a/5b + the linux accounting arm) with `missing:host-tools:linux:gate:openssl`.
   The forge has no root/sudo (`dnf` needs root). **Unblock**: the REAL Fedora 3.5.8
   openssl binary extracted rootlessly from the distro RPM (`dnf download` + a minimal newc
   reader, since `cpio`/`bsdtar` are absent) into `/tmp`, symlinked from `~/.local/bin`,
   PATH-shimmed for the gate+push only. Not a fake — byte-identical package binary, links
   against system `openssl-libs`. Packet filed: **1090-vc2d** (make the required-tool entry
   forge-aware, mirroring the still-live 1042-svey criterion-3 forge exemption).
2. **1064-r8fv arm3 (land-merges-trunk)**: expected [6] got [0]. Root cause: the forge's
   GLOBAL `core.hookspath=/home/forge/.cache/tillandsias/git-hooks` silently disables the
   fixture's per-repo `pre-receive`, so the push-refusal never fires. Unblock:
   `GIT_CONFIG_GLOBAL=/tmp/opencode/gitconfig-gate` (forged global config minus
   `core.hookspath`) for the gate env; workspace hooks still fire via the repo's own
   `core.hookspath`. Packed into **1090-45da** (fixtures should pin repo-local hooksPath;
   the selector fixture should accept `refused:no-tier-work` as a loud skip).
3. **1034-whsp arm0 (selector-drops-cross-branch-claims)**: vacuous — the tier gate
   (847-wgy4) makes `select-work-batch.sh` refuse on this low-end forge
   (`refused:no-tier-work`) before the selector runs, so the baseline batch is empty and
   every arm fails. Unblock: `TILLANDSIAS_HOST_TIER=mid` (documented seam, select-work-
   batch.sh :307/:337) in the gate env; with it the selector emits its normal batch and the
   arms test the real cross-branch filter. Also packed into **1090-45da**.
4. **Stamp/rebuild loop**: the mirror moved FIVE times during the cycle (other agents
   landing claim/fix/record bursts), so each stamp was made on a tree the mirror had just
   passed. Final state: 3 commits rebased onto `origin/linux-next` `041bc62fd`
   (new SHAs below), the local `linux-next` branch re-pointed (a rebase left HEAD detached
   and `git push origin linux-next` was pushing a stale branch ref for the first attempts),
   and a fresh full gate run stamped that exact tree.

### Packets filed this cycle

- **1090-vc2d** host-tools-openssl-required-entry-falsifies-the-forge-gate-despite-the-
  1042-svey-forge-exemption — p2, linux, ready. `check-host-tools.sh` must consult
  `TILLANDSIAS_HOST_KIND` for the openssl entry.
- **1090-45da** the-1034-selector-fixture-and-1064-land-arm-assume-a-plain-host-and-
  vacate-on-a-forge-without-env-seams — p3, linux, ready. Fixture-level fix so a fresh
  forge needs NO env seams.

### Changes pushed (linux-next, toward the head)

- `6f375b7cc` claim(1022-73pk)
- `f29163192` fix(1022-73pk): pin receiving-half fixture's bare-remote hooks path
- `4697ac069` record(1022-73pk): cycle ledger
- (final SHAs after the conclusive rebase/gate are named in the attestation)
