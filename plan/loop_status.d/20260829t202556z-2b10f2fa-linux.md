## Cycle — macbook stand-down (macuahuitl)

830-xsk2 second half merged (osx-next 30dede1af): lifecycle wiring +
runtime accept-proof DONE — first guest→host vsock crossing in the tree
(opt-in via TILLANDSIAS_HOST_VSOCK_PORT; bind failure never fails boot).

THE FINDING THAT GATES THE REMAINDER: Virtualization.framework RETAINS
guest connection requests and delivers them only when the host pumps its
CFRunLoop. --exec-guest blocks during a command: measured 6/6 guest
attempts ETIMEDOUT at ~2.0s (kernel VSOCK_DEFAULT_CONNECT_TIMEOUT), then
ALL SIX accepted in a burst the instant the command returned. Neither
side's log alone is the truth. Tray mode's continuous NSApplication
runloop should not share the defect — REASONED NOT MEASURED, labelled so
on HostVsockListener's docs. The in-guest hop is therefore NOT STARTED,
deliberately: it cannot be honestly validated through --exec-guest, and
the next claimant needs a continuously-pumping host context — a
constraint on how the slice is TESTED, recorded so it costs nobody a
confusing day. Packet ready, cold-pickup framed.

MACBOOK STANDS DOWN CLEAN: everything touched today closed with evidence
or released with decisions + measurements; nothing under their lease.
Their day: 926-bin4 measure→design→implement→runtime-verify, 739-6r6n,
two dialect regimes, 830-xsk2 both testable halves.

Board: lenovinha on 46 mechanical + 5 -qF redos; yolanda landing
engine-missing then standing down; yoga paused; macuahuitl coordinating.
