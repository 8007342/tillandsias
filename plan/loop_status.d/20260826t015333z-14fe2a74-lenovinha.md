## Cycle 2026-08-26T01:53:33Z — lenovinha (linux_immutable, linux-next, operator /loop)

Two hosts went dark this cycle (pirria credential-dark, calmecacpilli shut down by operator
directive). The fail-loud-diagnosis bundle was rerouted here. Took 894-scxy; 896-f8ti and
895-j8ag remain open and are NOT claimed.

### E2E ran — partially, and the partial is the point

Sixth deferral ended. Ran `/smoke-curl-install-and-test-e2e` on v0.4.260817.1 (daily channel).

**Steps 0-1 PASS.** curl-install on Fedora Silverblue: `install_exit=0`, version assertion PASS
(`Tillandsias v0.4.260817.1` contains the resolved tag), Vault bootstrapped with every AppRole
provisioned, `Init complete.`, no anomalies. **This is the first Linux evidence of any kind for
this release** — it previously had exactly one conforming report (Windows), which my own
platform-aware gate reports as `missing=linux,macos`.

**Steps 2-4 deliberately NOT run.** Not fear of the wipe — it is sanctioned and
`TILLANDSIAS_DESTRUCTIVE_RESET_OK` is unset. v0.4.260826.1 is being cut within minutes;
v0.4.260817.1 was already decided against for promotion; the destructive half costs a full image
rebuild. Spending it on an artifact superseded mid-run and then repeating it on the new tag is
waste, and the new tag is the one that needs three-platform evidence. **Committed follow-up: the
destructive half runs on v0.4.260826.1 once published.** Report filed partial rather than absent.

### 894-scxy — name the layer that FAILED, not the one you OBSERVED

Rerouted from pirria; macuahuitl called it a misroute in the first place and was right — this is
886-qmdz's discrimination one layer over.

gh prints "The token in keyring is invalid". Measured on pirria: secret-service entry present,
login collection UNLOCKED, token retrieved intact at 40 chars — **and GitHub answered 401 on it.**
The keyring was healthy in every component. Three hosts diagnosed the wrong subsystem from that
one string; one built a mechanism on the premise, another relayed a keyring direction fleet-wide.

Discriminator is `gh api user` and nothing else (pirria's instrumentation, handed over verbatim):
200 accepted, 401 rejected server-side. Runs only after the push probe already failed, so the
healthy path pays nothing. **No secret is ever materialised** — `secret-tool search --all` prints
the value inline, which is how two hosts put live `gho_` tokens into their own transcripts. A
fixture arm asserts the guard never calls it.

Five verdicts where there was one, each naming the layer that failed. **pirria's third state** —
plaintext `oauth_token` in `gh/hosts.yml`, where keyring probes are *irrelevant* rather than
negative — is checked FIRST, because a two-state classifier buckets it as "keyring healthy",
which is the same misattribution one layer over. A fifth verdict is mine and was not in the
packet: GitHub accepting the identity while the PUSH fails is neither a keyring fault nor a dead
token, and naming either would be a third misattribution.

Both directions proven by fixture (arms 14-20), hermetic with stubbed `gh` and `busctl`.

**A fixture bug worth recording, being the packet's own defect inside the packet's own test:** my
first arm-14 assertion grepped for `secret-service` to prove the diagnosis does not send the
reader to the keyring — and fired on the sentence "Do NOT go looking at secret-service". A
word-absence test cannot tell an imperative from its negation. **Third time in one night I have
hit incidental matching in my own test code** (888-m75r, 892-aw9p, this).

Class recorded at `cheatsheets/architecture/diagnostic-attribution.md` — bundled and indexed, so
a forge agent finds it. Leads with the sentence the four instances earned: *a signal that names
the wrong layer costs more than no signal*, and the reason — a missing signal leaves a reader
searching; a misattributed one leaves them searching CONFIDENTLY, in the wrong place, and
stopping when they find nothing there. Names 886-qmdz as the control: fixed, fired correctly on
another host days later, stayed fixed. Not promoted to methodology, honouring macuahuitl's ruling.

### Not claimed

896-f8ti and 895-j8ag are open. 895-j8ag has a live reproduction from pirria (UTC 01:43:44Z vs
local 18:43:44 PDT; gate says current, its own marker says 07:44:07Z eighteen hours earlier) plus
the caution that the failure is **timezone-directional** — UTC-negative hosts read stale-as-current
for the last hours of local day, UTC-positive hosts have the mirror problem, so a one-offset
fixture passes on half the fleet. It is also a fleet-TIMING change, same category as 887-bz88's
stamp change, and pirria's advice to land it when the fleet is not mid-release is right.

### Freeze

A fleet-wide `blocked:version-not-monotonic` is expected the moment v0.4.260826.1 lands; remedy is
`git fetch origin && git merge origin/main`. Landed everything before the tag.

### Metrics
```
expert_accuracy: pass=28 graded=28 total=33 rate=100% skipped=5 NOT-EXERCISED-ON-THIS-HOST
flow: cycles=27 avg_completed_per_cycle=0.81 avg_commits_per_cycle=3.04 overhead_ratio=3.73
plan: packets=625 ready=388
```
