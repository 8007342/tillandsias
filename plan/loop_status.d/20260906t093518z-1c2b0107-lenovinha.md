## Cycle 2026-09-06T09:35:18Z (lenovinha linux — 1107-bb6s mechanism found; 1109-veaf and 1110-4v4h filed)

MO-FULL: COMPLETE f018b94b4fd5ebe86843d0ab4eb726035e56cc0b linux-next f018b94b4fd5ebe86843d0ab4eb726035e56cc0b

Direction: the selector returned the same four packets a FOURTH time, all
already dispositioned by this host today, so this cycle took 1107-bb6s — the
p1 this host filed, whose trigger had been withdrawn rather than fixed and
would therefore return silently. Deviation stated, not silent.

1107-bb6s: MECHANISM FOUND, FIX NOT ATTEMPTED, AND THE COMPACTOR IS NOT THE
CULPRIT. Recovered the withdrawn fragment from its removal commit's parent and
reproduced across seven runs:

  trunk                                   ok    86.94s
  trunk + withdrawn fragment              FAIL  78.44s
  trunk + minimal note, same packet       ok
  trunk + block scalar with a blank line   ok
  trunk + summary whose only oddity is an em-dash   ok
  trunk + summary body lines 1-27         ok
  trunk + body lines 28-55                FAIL
  trunk + body lines 28-41                FAIL

Neither length nor non-ASCII: the PASSING half also carried non-ASCII, which
falsified my own em-dash hypothesis.

THE CHAIN. fragment_coverage_gaps checks each fragment against the RE-PARSED
CANDIDATE (843-624y), finds a gap, and compaction REFUSES the fragment, leaving
it on disk — measured, 760 compacted and the trigger declined. The ledger is
never corrupted. But assert_fold_equivalent compares that candidate against a
fold containing EVERY fragment including the refused one, and never reads the
`refused` field compact_text hands it. So any refused fragment reds the test BY
CONSTRUCTION: the component that DETECTED the problem is the one the red points
at.

AND THE GAP MESSAGE MISATTRIBUTES ITS OWN CAUSE. It guards on
`!events.contains(&event_identity(pid, event))` — an event-identity miss, where
identity includes the entire summary text — and prints "NO SUCH PACKET ... check
the id for a typo". The packet EXISTS: 1105-h8vr, declared under `packets:`,
present in the candidate with four events. Its own comment records a coordinator
finding sitting refused for fourteen hours across three cycle reports because of
earlier wording here. The wording was fixed; the CONDITION still covers two
causes and the message still asserts one.

FALSIFIED AND RECORDED so the next reader does not start there: apply_lww
inserts an unfound field at `start + 1` while the fold appends at the end — real,
but splicing at `end..end` with the full trigger restored STILL FAILED.

1109-veaf FILED, AND I CAUSED IT. Trying to inspect a compaction candidate I ran
`tillandsias-plan compact` in a fresh command against the REAL repository, after
the harness had already printed "Session cwd remains
/var/home/lenovinha/claudia/tillandsias". It rewrote plan/index.yaml and deleted
760 fragments — 762 files, one keystroke, no prompt. Recovered fully from HEAD
(md5sum byte-identical, 760 fragments back, "821 packets, ids unique, live
references sound"); nothing committed, nothing pushed, no other host affected.
The operator error is mine and is not the packet: `compact` is the most
destructive subcommand in the tool, spelled like the read-only ones beside it,
and the only one with no gate, while set-field refuses an unknown reference and
append-event refuses a packet it cannot see. Inspecting a candidate currently
REQUIRES destroying the ledger to produce one — which is also what 1107-bb6s's
remaining work needs.

1110-4v4h FILED, AND IT DENIED THIS CYCLE ITS FIRST MARKER. finalize-cycle
exited 4 with "MO-FULL: FAIL local HEAD 01bc637a6 is not durably on
origin/linux-next (observed 26659af85)". THE VERDICT WAS FALSE: both 01bc637a6
and 405cae043 verify as ancestors of origin/linux-next, and 26659af85 is a later
commit from another host descending from mine. converge_remote compares the
remote head to local HEAD with STRING EQUALITY when the question is CONTAINMENT,
so the probe is reliable exactly when the trunk is idle and unreliable exactly
when it is busy — and the fleet heartbeat reads a host that commits without
attesting as WEDGED. The retry above succeeded only because nobody pushed during
that window, which is itself confirmation of the diagnosis. No marker was typed
while it was refusing; the discipline held and the predicate is what is wrong.

FOUR FILINGS, NO CODE, and that is the honest shape of this cycle. Each is a
verdict asserting more than its check supports, which is the same defect this
host has now met in eight forms since 03:35Z: a pipeline's exit code standing in
for a gate's; a background OOM kill indistinguishable from a gate failure;
experts-never-called reported from an absent source; a credential arm affirming
ok from a file it never read; a cross-branch tool telling a host its own
finished work was not its own; a gate calling a pre-existing red NEW; a coverage
gap naming the wrong cause; and an attestation calling a landed cycle unpushed.

Read path: MCP-first for orientation; every filesystem read was VERIFICATION
class — converge_remote, fragment_coverage_gaps, apply_lww, item_span and
block_scalar were each read before any claim was made about them, and the two
hypotheses I could not read my way to were tested and one was falsified.
