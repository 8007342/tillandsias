## Cycle 2026-08-26T02:19:47Z — lenovinha (linux_immutable, linux-next, short capture)

Short cycle, one capture. A fleet-wide warning arrived from yoga about `--ci-full` destroying
uncommitted work; three rounds of mutual correction followed, and the useful output was a
generalisation none of us had at the start.

### What the warning was, and what it actually is

MECHANISM (verified here by inspection, not relayed): `test-gate-stamp-memoization.sh:30` sets
`ROOT` to the real checkout and line 208 runs `$ROOT/build.sh --check --install` against it,
bound as `litmus:release-gates-run-locally`, phase pre-build, which `build.sh:1203` shows
`--ci-full` dispatches. Real, and it reverts uncommitted generated files.

I did NOT reproduce the destruction, by choice: this host has the published v0.4.260817.1 binary
installed from the e2e, and `--install` would overwrite the artifact under test. Four checkable
facts already establish the mechanism. Asked not to be cited as a second reproduction — that
distinction is the accounting error the evening opened with.

### Two corrections in each direction, and the scope that survived

I corrected yoga's blast radius (the recurring cycle runs only `--check`, unaffected). yoga
corrected me back with a lane I missed (`/build-install-and-smoke-test-e2e` runs `--ci-full
--install` by design). macbook then narrowed that again, correctly: the skill branches on
`uname -s`, so macOS uses `build-macos-tray.sh` and Windows `build-windows-tray.ps1` — **routing
to a SKILL is not routing to a COMMAND.** Verified here from the skill's own table (16-18).

FINAL SCOPE — exposed: the local-build e2e's **Linux branch only** (`linux_mutable`), deliberate
`--ci-full` re-gates, direct `local-ci.sh --phase pre-build`. Unexposed: the recurring cycle
everywhere, `--check`, `--test`, macOS, Windows, and this host (linux_immutable is `no` for
local-build at meta-orchestration SKILL.md:1463).

I also corrected a framing that would have cost the next reader time: the destruction is **not
undetected**. The worktree guard hashes dirty-path CONTENT at Start-Of-Cycle and `verify`
compares those hashes, so a reverted tracked file fails Finalization loudly. Accurate: **silent
when it happens, detected after the fact.** Detection is not recovery — but "no moment where the
loss is visible" would send someone hunting for a detector that already exists. The guard's real
limitation stated positively: it **forbids the AGENT** from touching dirty paths and **DETECTS a
third party** doing it. Different powers, not to be blurred.

### The capture (this cycle's actual work)

yoga named the pattern in their own three over-scopes better than I could: **they verified the
mechanism to the line number every time and INFERRED where it runs.** That is the same class the
894-scxy cheatsheet documents, one level out — dispatch, branch selection and host routing are
facts living OUTSIDE the file being read, so a claim about them names what was observed rather
than what determined the outcome.

Added to `cheatsheets/architecture/diagnostic-attribution.md` with both measured instances, the
three questions that would have caught them (which caller invokes this / does the path reach this
line / does the destination branch again), and the cost that makes it matter: **a warning scoped
wider than its defect gets discounted the next time. Over-scoping is not the safe direction** —
it spends credibility the next real warning needs.

### Note on the proposed fix

Sandbox-copying the fixture needs three things, not one: the copy is byte-identical to the
checkout's `build.sh`; a mutation of a **load-bearing** line makes the fixture red; and it still
asserts its memoization property. Miss the second and we close a packet about unfalsifiable
checks with an unfalsifiable check. My arm-14 bug this cycle is the cautionary case — a comment
-byte mutation would pass a naive identity check while proving nothing.

### Standing

Still no `v0.4.260826.1`. The destructive e2e half remains committed to that tag. 896-f8ti and
895-j8ag remain open and unclaimed.
