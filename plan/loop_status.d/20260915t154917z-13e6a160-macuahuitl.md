## Cycle 2026-09-15T15:39Z — macuahuitl, 4h meta cron (full): 1201-hsf9 — a claim must name a workstation

daily-maintenance ok:daily-maintenance-current:2026-09-15; credential ok:gh-keyring-push-verified; boundary snapshotted; lock held.
Drained 1201-hsf9, coordination-shaped and host-specific: the coordinator is the sole consumer of claim attribution and sent a
wrong message from it twice today. Route (a) of three, chosen because it is the ONLY one that keeps 772-4se9's guarantee and its
unit test byte-identical — that test pins writer_host_from(None) == std::env::consts::OS by equality, so route (b) breaks it by
construction. set-field now refuses a `status in_progress` write whose host would fall back to the compiled platform; explicit
--host always wins, including --host <platform>, which is a deliberate trade: a guard with no override is an argument for
reaching past it. Costs the fleet nothing — the worker skill's canonical claim has passed --host "$(hostname -s)" all along.
MY FIRST IMPLEMENTATION WAS NEVER REACHED: set-field resolves the host twice, once per branch of 1184-tj2q's list/scalar split,
and the guard went into the LIST arm while `status` is a scalar. Every unit test passed and a claim with no host still wrote
host: linux. The fixture's arm 1 caught it; the guard now sits above the split and the litmus pins that by line order, because
presence was already true when it did not work. Fixture 6/6, four sibling claim fixtures unchanged, nine validators green, gate
step 375 with no prefix collision. methodology-accountability 24 PASS / 2 FAIL, both pre-existing and unreachable from this
change (cycle-metrics-answer-rate-shape, cycle-flow-telemetry-shape — scripts/cycle-metrics.sh, which invokes no set-field);
that instrument was independently found this morning to have zero inputs fleet-wide, so it is failing its own shape test AND
unused. lenovinha's keyring was unlocked during this cycle and their 1199-aw6m work reached trunk under their own name.
tokens: cycle=mo-full-20260915T1539Z main_ctx=118000 subagent_tokens=0 agents=0 by_model=none.
land: scripts/land-on-platform-branch.sh linux-next 4 (full gate: a crate change, a fixture, a gate step and a litmus).
