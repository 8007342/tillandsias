## Cycle — macuahuitl 2026-09-04T10:05Z (coordinate-multihost-work, cron :41)

- **Merged:** osx-next +1 (macbookair's loop-status entry carrying cycle-metrics verbatim, 83447612f) into linux-next, no conflicts. windows-next level. Trunk quiet since b38376338.
- **Claims 0/0/0/0, stranded 0.** Heartbeat 6/0/0/3/1: yolanda now reads healthy (attested this hour); macneo NEVER ATTESTED (cannot push: two independent faults, gh token invalid and no git push credential in osxkeychain; ask revised on the board); esme and pirria SILENT by author matching, both measure-only and accounted for.
- **Recurrence audit, second pass.** Hosts now pasting the metrics block in their newest entry: macuahuitl (meta cycle), lenovinha, macbookair (after this merge), yoga and yolanda by message and now in their handoffs from the next fire. The three top skippable steps agree on SIX hosts (1008-85ry carries each host's numbers); the fix is input-scoped memoisation, never removal (yoga's pushback recorded). The audit reads `experts-never-called` beside an expert-absent preflight, and a dirty worktree beside a dead credential, as structural (rules on 1001-q3zf).
- **Reads from the hour:** 1011-d578 (yoga's own regression from 1002-7jeg: the widened bring-up lane routes 8 hosts to 7 epics, this host colliding; quick-tier litmus invisible to the instant-tier gate, which widens 1003-444f's class to litmus sizes; yoga takes the fix next, not reverting); 1011-zp59 (the Vega beside the RTX 3070 is usable and deliberately unscheduled, measured 3-5x slower fully resident; lenovinha claims it at :05); 1009-gccx (a fifth of gate runs end red on sub-second-decidable failures; reorder the deciders); 1010-cnjb (a cycle can attest without a loop-status entry; macbookair wrote none for a fortnight while attesting green).
- **Idle hosts: none.** yoga: 1011-d578; lenovinha: 1011-zp59; macbookair: selector's batch at :15; yolanda: 997-e4v2 slice 3 Windows half at :45, host-shell held for a coordinated pass; esme: 1004-pg9p unleased at :55; pirria: stops at cargo-absent until the toolchain ask is settled; macneo: 980-ja2m claim after the login.
- **Release:** not this hour: one instant-tier red on the macOS lane (1006-jfwv) and one quick-tier red on the routing litmus (1011-d578, yoga next); no PR in flight; latest v56.9.2.1. The 01:09 meta cycle (next 05:09 local) records the decision.
- **Operator queue, plain asks:** (1) ESMERALDINHA: `gh auth login`, then reverify and a dry-run push. (2) macneo: `gh auth refresh -h github.com` AND `gh auth setup-git` (or an SSH remote), then reverify and a dry-run push; fetch works only because the repo is public. (3) pirria: reinstall rustup so the plan instrument rebuilds. (4) 978-juw4 permission. (5) 996-bcjx identity call. (6) the ten quarantined trees on esmeraldinha. Nothing is blocked that a host can unblock.
- **Scheduler:** `due:not-installed`; session crons armed (coordinate :41 hourly, meta 9 1-23/4).
