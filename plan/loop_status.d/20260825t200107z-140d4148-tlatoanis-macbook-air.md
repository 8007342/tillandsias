## Cycle 2026-08-25T20:01Z (macos tlatoanis-macbook-air — the macOS lane of 795-zshi, and a remedy that failed its own packet's criterion)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, full mode, iteration 8. Guards green: `ok:gh-keyring`, `ok:branch-osx-next`, `ok:experts-healthy` + surface attested exposed, `ok:capability-row-reported`, deslop not due (order=864 last=834 delta=30, under threshold). Boundary snapshotted at start, clean tree throughout.
- **Batch**: `epic=architecture-audit-epic seed=tlatoanis-macbook-air size=4 budget=10 pick=1/7 route=rank:5/7:width=7`. Claimed the head packet **795-zshi** and pushed the claim before starting, per the claiming rule.
- **Delivered the two items the 2026-08-25 yoga event left precisely scoped** — and it left them scoped because it correctly refused to attempt them from Linux, where the macOS bridge compiles to a cfg-gated stub. This host had the evidence, which is the handoff working as intended: (1) the `--exec-guest` absolute-path-vector versus shell **discriminator**, and (2) **CAP_EXEC_ARGV_VECTOR feature detection** on `HelloAck.server_caps`.
- **THE FINDING WORTH CARRYING FORWARD, because it is a trap dressed as hardening.** My first draft of the discriminator also scanned the ARGUMENTS for shell metacharacters to decide the arm. Its own test caught it routing a hostile project name `a'b` to the SHELL arm — i.e. **the remedy failed this packet's own exit criterion 4**. Inspecting an argument to choose the wire shape puts a host-side parser back in front of user data, which is the exact defect class 795-zshi exists to delete. `spec:vsock-exec-authz` clause 4 says the guest never inspects arguments; that is only true end-to-end if the host honours it too. **The arm is now decided by argv[0] alone**, pinned by a test over `a'b` / `a b` / `a$(id)b` / `a;b` / `a|b`.
- **The capability gate refuses BEFORE `PtyOpen`, and that timing is what the test asserts** — not the error text. A refusal issued after the request is sent has gated nothing: the guest already saw a shape it cannot serve. Refusal rather than fallback, per clause 5, because re-flattening a request the caller expressed as a vector is the rewrite being deleted, and a flattening fallback would leave two code paths for one request — this packet's own warning, turned on its remedy.
- **The `--help` text had become a lie** and was fixed in the same change: it still documented "Words are JOINED and run via `/bin/bash -lc`" as the only behaviour.
- **Released 795-zshi back to `ready`, did NOT close it.** The Windows deletions (`argv_survives_wt_reparse`, `wt_safe_title`) need Windows-lane evidence, and the macOS tray preamble workarounds are not argv flattening at all — they are guest housekeeping (podman rm, CA-key chmod) that must move guest-side first, as the 2026-08-22 slice-3 event already recorded. Both are already scoped in `next_action`.
- **Verified**: 6/6 macos-tray exec-guest tests, 35/35 vm-layer `vsock_exec` tests, `litmus:guest-exec-argv-vector-no-shell` + `litmus:pty-open-argv-allowlisted` 2/2 PASS, `./build.sh --check` green under `TILLANDSIAS_FORCE_CHECK=1`.
- **DAILY MAINTENANCE WAS DUE AT ENTRY (`due:stale:2026-08-20`) AND IS STILL DUE AT EXIT.** Recorded as an honest gap rather than stamped — the marker exists to be falsifiable, and the cycle's budget went to the packet. The next cycle on this host should run the body before draining.
- **The fleet number to watch: `population=11 in_progress=11 stranded=7`.** Seven of eleven claimed packets carry no progress event. That is the 641-e2qa shape at a far higher RATIO than when it was filed, and it is a fleet property no single host can close — surfaced here rather than acted on, because closing a packet requires checking its exit criteria against the tree and guessing is worse than leaving it stranded.
- **E2E**: not run. macOS local-build e2e destroys and re-provisions the Virtualization.framework VM directory; the cycle spent its envelope on the packet instead.
