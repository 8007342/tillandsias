## Cycle 2026-08-25T00:58Z (linux_immutable pirria — a build that ignores the vectors it has, and two guards that stop a healthy host)

- **Host**: pirria, `linux_immutable` (Silverblue, Intel N150, 4 cores, 15.3 GB), branch `linux-next`, lane `loop-cron` (operator `/loop` every 6h, first fire). Routed `tier:low-end` by 847-wgy4 — correctly; the probe reads 4 physical cores.
- **Preflight**: `blocked:preflight:plan:cargo-absent` on the first call, then `ok:cycle-preflight:rebuilt+expert-refreshed+services-down3` with `$HOME/.cargo/bin` exported. See **876-irn7** below — the toolchain was never missing. Enclave `tillandsias-vault`/`-proxy`/`-inference` all reported stopped; noted, not restarted.
- **Scheduler**: `due:not-installed` — no durable timer here. This host cycles only while the operator's session lives, and that is worth stating rather than discovering later (856-s56y).
- **Guards**: branch `ok:branch-linux-next`; MCP `ok:experts-healthy` + surface attested `exposed`; capability row `ok:capability-row-reported:pirria`; de-slop `ok:deslop-not-due:order=875 delta=41 hours=120`; stranded claims `population=2 in_progress=2 stranded=0`; checkout lock `loop-cron:5022`; boundary snapshot on a clean tree; daily maintenance stamped (first ever on this host — it read `due:no-marker`).
- **Batch**: `epic=UNGROUPED role=linux release=v0.6 size=2 budget=10 score=2.023 seed=pirria pick=1/1 route=tier:low-end`; `triage: eligible=2 grouped=0 ungrouped=2 epics=0`. The low-end tier is two packets deep and both were offered; nothing was left behind.
- **Claimed and completed**: 861-n7f5.
- **Delivered**: `scripts/check-engine-cpu-dispatch.sh` — given an engine binary, one falsifiable line naming the CPU features its BUILD dispatches, refusing a build that ignores vector features the host advertises. `scripts/bench-inference-floor.sh` calls it before publishing a lane and substitutes `claimed_engine=refused:<missing>` for the operator's label when it refuses, so a misconfigured build cannot enter the ledger as an engine-lane measurement at all; the HTTP path publishes `dispatch=unchecked:no-engine-binary`, an honest unknown rather than an ok. `tillandsias-plan capability-matrix --cpu-flags [<host>]` is new so the host's half comes from the published accel_probe row, not a second inline probe (859-b2zc). `scripts/test-check-engine-cpu-dispatch.sh` 9/9 hermetic — no engine, no model, no network, no matrix. Gate green, forced, 88s of phases.
- **The arm I care most about is the negative control**: a baseline build on a host with no vector features must PASS. A check that refused it would ban the only build such a host can run, and this is the fleet's floor-host lane — the place where "refuse the slow-looking thing" is most tempting and most wrong.
- **Filed — 876-exg2**: `check-credential-channel.sh` reads a stale-gate pre-push hook refusal as `blocked:gh-cli-only`, and the skill hard-stops the cycle on any `blocked:*`. Hit here on the first cycle after a clean fast-forward, which is exactly where the skill mandates the guard runs (step 2, "immediately after `git fetch`"). One `./build.sh --check` later the same guard said `ok:gh-keyring-push-verified` with nothing about the credential touched, and the remedy it had printed — seed `.git/.gh-credentials` — could not have helped. **yoga hit the identical thing ten minutes earlier** and chose an event on 860-g798 rather than a row; that row is `completed`, so an event on it schedules nothing, which is why this is a row and not a disagreement about severity.
- **Filed — 876-irn7**: `cycle-preflight.sh` answers `blocked:preflight:plan:cargo-absent` on a host carrying cargo 1.98.0, because rustup's PATH edit lives in `~/.bashrc` and every agent tool call is a non-interactive shell. That verdict is the terminal one — the skill's own words are that it is "the one failure the loop cannot reason its way out of" — so an unattended host in this state answers blocked every six hours forever while a human reading the transcript goes looking for a toolchain that is already installed.
- **On 863-iicc (offered, NOT claimed — the ruling it asks for is orchestrator-gated)**: four `./build.sh --check` runs here measured 63s, 78s, 89s wall, dominated by ledger work (fragment-event reconciliation 25.7s, plan archiver 18.3s, plan ledger tests 13.5s) rather than compilation. That is well inside the fleet's 9.1 min median inter-push gap and nowhere near esmeraldinha's modelled 10.5 min. **Criterion 6 should not count pirria as a second reproduction**: low-end tier and slow gate are being treated as one property and they are not — this is the fleet's designated floor by core count and it still gates in 90 seconds. Criterion 7's lever already exists: the pre-push plan-only lane fired on my third push attempt while printing `plan-only lane: not applicable — no ref list on stdin (full gate required)` on the two before it, same command, same one-file outgoing diff, minutes apart. Recorded as a note on the row, not as a fourth row saying "the gate is the lever".
- **Push races, measured rather than modelled**: one lost this cycle. The claim push was rejected because the remote advanced during the 63s gate, and the rebase then invalidated the stamp. That is the shape 863-iicc describes, at 1 re-run rather than esmeraldinha's 2.0.
- **Not done, said plainly**: `target/` is 18G on this box and I stamped `cargo-gc:skipped-under-threshold` before measuring it. That label is optimistic; the next cycle here should either GC or record an honest threshold. Also nothing in `./build.sh` or `scripts/local-ci.sh` runs the new hermetic suite — that is the convention its siblings already follow (`test-check-credential-channel.sh`, `test-cycle-checkout-lock.sh` are invoked by nothing), so it is consistent rather than an oversight, but the fleet now has at least three suites nobody runs and `audit-guard-activation` greps for guard NAMES, which is the same half-measure one level up.
