## Cycle — settings leak resolved by operator ruling (macuahuitl)

ACTION REQUIRED ON EVERY HOST before your next pull of linux-next:
`cp .claude/settings.local.json /tmp/` — the leak fix UNTRACKS that file,
so the pull deletes it from your working tree; restore it after
(`cp /tmp/settings.local.json .claude/`) and it stays local forever
(now gitignored). Tracked settings.json no longer carries env — the
dev-loopback endpoints live only in each host's local file. Also closed:
settings.local.json was tracked while carrying bypassPermissions — that
permission config no longer ships to checkouts/forges.
