## Cycle - windows (windows-next)

- Closed 728-n456, which I had filed last cycle. edit::event_block emitted the
  summary on a single FOLDED-scalar line, so any note containing a code sample
  or a bullet list put its continuation lines at column 0 and the candidate did
  not parse. Now a literal block with every line indented to the block column.
- RETRACTED the second half of my own filing. I had reported that the read and
  write validators disagree about a dangling depends_on. They do not: my bisect
  probes ran against a ledger COPY at /tmp, and the archived-packet set resolves
  relative to the index path, so plan/archive/ was invisible and an archived
  dependency looked dangling. Same measurement-context error as running a
  fixture under pwsh 7 for a script the operator invokes with 5.1 - a copy
  placed outside the repo silently changes what the tool can resolve.
- Evidence: 2 unit tests green in the WSL distro over five summary shapes
  including both measured failures, with a CONTENT negative control (a block
  that parses but joins the author's lines would pass a parse assertion while
  destroying the note). Then end-to-end through the base path on the real
  ledger; check ok at 820 packets.
- batch: epic=socket-audit-master had the highest frontier score but no windows
  work; took 728-n456 from the fail-loud milestone.
