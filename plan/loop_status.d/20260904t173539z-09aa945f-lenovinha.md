## Cycle — lenovinha 2026-09-04T17:48Z (advance-work; cron :05) — 997-e4v2 step 2 Linux half landed with the osx-next merge
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-04T17:35:39Z`:
      experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
      experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
      mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
      expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
      flow: cycles=8 avg_completed_per_cycle=0.13 avg_commits_per_cycle=1.75 overhead_ratio=14 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
      timing: steps=27272 build_check_ms_avg=139059 build_check_mix=mixed:forced=364,memoised=93 litmus_ms_avg=13453 slowest=litmus:tool-materialization:304387 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      repeat: window=since=2026-09-04T17:35:39Z steps=122 top3=build-preamble=2,build-check=1,build-check-memoized=1 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      recur: window=7d runs=25134 steps=429 top3=build-check:runs=324:total_ms=47719906:avg_ms=147283:fail_pct=31,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=225:total_ms=7531749:avg_ms=33474:fail_pct=0,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=225:total_ms=5612983:avg_ms=24946:fail_pct=0 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=59 floor_ms=2000 min_runs=5 top3=step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=225:avg_ms=33474:fail_pct=0:saved_ms_upper=7498275,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=225:avg_ms=24946:fail_pct=0:saved_ms_upper=5588037,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=220:avg_ms=19512:fail_pct=0:saved_ms_upper=4273312 source=/var/home/lenovinha/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=701 ready=456 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=dirty traces=current
      verdict: attention:worktree-dirty
- LANDED 0b25215b6 (verified on trunk before citing). The merge and the slice went together because the slice only compiles on the union; 53fa82ad2 (macbookair's step 2) is now on linux-next. The land script chose MERGE over rebase on its own — "unpushed set has 4 merge commits; MERGING (rebase would replay them)" — so the osx-next merge is preserved rather than replayed.
- MEASURED INDEPENDENTLY. Yoga's patch was unreadable (uncommitted on a dead tmpfs) and macuahuitl withdrew it as my reference; yoga sent its six as INTENT to compare against. I enumerated the five compile sites with the COMPILER rather than reconstructing a patch. Where we could be compared we agreed exactly: same six test names, same shapes, same four `~/src` assertions, same two local-name assertions. That agreement is worth more than inheriting the patch would have been.
- SIX TESTS: PRE-FIX 602 passed / 6 FAILED, POST-FIX 608 passed / 0 failed. Four were 6-rows-to-5; login_three_state's whole failure was the `~/src` presence assert; the sixth is the 944-jaef GetLayout fixture.
- PRESENCE ASSERTS BECAME ABSENCE PINS, not deletions — every `~/src`, `test-project` and `project-beta` assertion now asserts the row is GONE. Deleting them would leave the migration's own effect untested and let a revival pass silently.
- JUDGEMENT CALL 1: state_transition asserted `after.len() == 6` while `before` is 5. Lowering it to 5 would make a test NAMED for a transition assert 5 == 5 — vacuous, and green. It now asserts what actually changes: GitHub Login present before, absent after, Cloud submenu present after.
- JUDGEMENT CALL 2: the 944-jaef fixture is REROOTED onto the Cloud submenu, not deleted — the same conclusion yoga reached independently. Its contract is that GetLayout returns the subtree rooted at the REQUESTED id pruned to the REQUESTED depth; the old handler returned the id-0 full tree and gnome-shell livelocked, costing four desktop sessions. That contract is untouched by this migration; only the submenu it happened to exercise is going. Deleting a pin because an unrelated row moved is how contracts quietly stop being tested.
- ID 21 REMOVED BUT RETIRED, NOT FREED: until step 3 removes the local row from the shared contract on other platforms, 21 still means "local projects" there. Reusing it mid-migration would make two menus disagree about one row — invisible until a client built against one contract talks to a host built against the other.
- SCOPE HELD: scan_project_root, refresh_local_projects, build_local_projects_submenu, LOCAL_BASE, LOADING_LOCAL_ID, ProjectScope::Local all verified present after the change. Only LOADING_LOCAL_ID's two unreachable menu-mapping uses went; its definition and submenu use are intact. Native plumbing is step 3 with the wire.
- PRE-EXISTING DEFECT FOUND, DELIBERATELY NOT FIXED: four absence assertions test `contains("GitHubLogin")` while the label is "🔑 GitHub Login" WITH A SPACE (mod.rs:6596, 6692, 6722, 6800). They can never match, so they always pass — four vacuous guards. THE ASYMMETRY IS THE LESSON: a wrong string in a PRESENCE assert fails loudly and is how I found this; in an ABSENCE assert it is silent forever. Not fixed here because correcting them may turn a vacuous green into a real red, which is a different migration.
- YOGA'S TWO NOT-MINE FAILURES did not surface: 608/608 both in parallel and under --test-threads=1, so the 1021-hf9e interference flake did not appear here.
- EVIDENCE: tray 608/608 (pre-fix 602/6), clippy --features tray --all-targets clean, ./build.sh --check green 284s. 997-e4v2 released to ready — step 3 is open.
