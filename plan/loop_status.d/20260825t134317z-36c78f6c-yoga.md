## Cycle 2026-08-25T13:43Z (linux_immutable yoga — refusing the gate I had asked for, and building the one that works)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `loop-cron`. Guards green, `services-ok`, daily gate current, lock taken and released, boundary clean both ends. 881-29me — filed last cycle — appeared in this cycle's batch and is fully Linux-doable, so the queue routed correctly for once.
- **881-29me CLOSED via its SECOND branch, and I refused the first one on measurement.** When I filed it I offered two routes: (a) a check that a cited span still relates to its claim, or (b) convert the convention to symbols. Having measured the corpus — **1,282 line citations across 487 documents**, into files past 22,000 lines — route (a) is unbuildable as stated, and its cheap approximation is *actively harmful*: verifying "the cited file has at least that many lines" would have **PASSED all six drifted citations that motivated the packet**. Every number was in range; every one pointed at unrelated code. A green gate over a corpus of wrong citations is strictly worse than the honest absence, so I did not build it and said why on the row.
- **What landed instead**: `scripts/check-issue-citation-convention.sh`, a convention ratchet. New citations must name a SYMBOL. A symbol survives every edit that does not rename it, and a rename is an event worth noticing; a line number dies to any insertion above it — 797-8dzt's conclusion for test source slices, applied to prose.
- **Diff-scoped, and that is load-bearing rather than convenient.** With 1,282 pre-existing citations a fleet-wide refusal flips every host red at once and gets disabled the same day — the 699-dycj lesson this repo already paid for. Only newly added lines are judged, against a base ref, the shape of `check-added-fragments-parse.sh`. Existing citations burn down as documents are revised, which is exactly when someone can re-verify them rather than mechanically rewrite them.
- **The escape hatch is narrow and reasoned**: a line ending `<!-- cite-ok: why -->` passes. Quoting a citation that has DRIFTED, as evidence, is precisely how order 245 recorded this finding last cycle — a ratchet with no way to do that would forbid describing its own motivating defect. The reason is mandatory; an unexplained opt-out is how a ratchet becomes decoration.
- **Scope decision recorded**: prose-to-prose citations (`plan/issues/other.md:42`) are not judged. The rot this exists for is prose citing CODE, which moves under it; widening the gate would multiply false positives for no measured defect.
- **Pinned** by `litmus:issue-citation-convention` 6/6, bound: grammar, negative control, positive control (without which the refusal could be satisfied by a gate refusing every added line), the escape hatch, markdown left alone, and the diff-scoped property proven directly.
- **Wired into `./build.sh --check` and hand-falsified THROUGH THE WIRING**, not just the script: one untracked document containing a line citation turns the gate red with the remedy printed and the offending citation named; removing it returns green. An orphaned guard is the 865-n8vq shape and the auditor only greps for invocation, so this was checked by exercise.
- **What it does NOT do, plainly**: it does not repair the 1,282 existing citations and cannot tell whether any one is still correct. It stops the corpus growing worse. The 80 remaining in order 245's document still need re-verification as that document is revised, and NA-05's exit criterion still literally demands "specific **file:line** references" — the wording that generated the rot, and which whoever drives that ratification should change.
- **Standing note, fourth cycle**: `vsock_loopback` is still not loaded, so slice 3's Framed migration remains verified only over `tokio::io::duplex`. Not re-raised beyond this line.
