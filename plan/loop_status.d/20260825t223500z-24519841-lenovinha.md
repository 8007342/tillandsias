## Cycle 2026-08-25T22:35:00Z — lenovinha (linux_immutable, linux-next, coordinator handoff)

Two coordinator-handed packets, both landed. Cadence note: fleet is being phased to avoid the
push contention I measured; my slot would be :27 — raising with my user rather than re-slotting
on a peer's say-so.

### 888-miiy — a host capability gap is now a LOUD RECORDED SKIP (fcfb909a2)

macuahuitl filed it and declined to fix it as the release beneficiary. Root cause is sharper
than "make scenario 6 match scenario 4", and the ledger had already predicted it: spec-rung1's
own author wrote, when adding it, that the set was NOT wired into the default grade set because
"a host without [an index] would get a harness ERROR rather than a skip -- making it default
would turn a missing host artifact into a red build fleet-wide. The gate's own committed-set
test names one file rather than globbing, so this set cannot affect it." Order 786-kjke later
added a globbing step — correct on its own terms — which swept the set in. **The isolation
depended on a property of a DIFFERENT test**, so a legitimate change elsewhere silently removed
it. Nobody violated the warning; the warning was load-bearing and unenforced.

Fix respects the coordinator's non-negotiable: a capability gap must never become a silent green.
`ENGINE_UNAVAILABLE` marks exactly one condition (index ABSENT). A STALE index, missing declared
corpus, unknown engine, missing query_vec all still abort rc=2 — "this host has no index" and
"this host's index is wrong" look alike and mean opposite things. Visibility three ways: per-case
SKIP lines, `skipped=N skipped_engines=` in the summary, and a stderr warning that the run does
not certify those engines. `total` still counts skips so the denominator can't quietly shrink.

cycle-metrics reports rate over GRADED cases: pass/total would read 28/33 = 84% and look like a
16-point accuracy regression when nothing regressed — I'd have replaced one false signal with
another. Appends NOT-EXERCISED-ON-THIS-HOST so a release can see the gap.

Fixture 6→9. Scenario 6 TIGHTENED, not merely unblocked: requires fail=0 AND that every skip is
accounted for. New controls: absent index skips loudly with denominator intact (self-adjusting on
a host that HAS an index); stale index still rc=2; and a genuinely wrong answer still RED
alongside skips — the fail-open control, without which the rest is worthless.

Exit criterion 4 (both branches demonstrated) is HALF MET and recorded as such — this host has no
endpoint. Routed to yoga. Also recorded: litmus:expert-groundtruth-harness is SKIPped by the
runner on this host, so my new step is verified by direct invocation, not under the runner.

### 887-bz88 follow-up — my fix deadlocked the Windows push lane (9dfa14b33)

The exec-bit field I added read the WORKTREE. build.sh dispatches into WSL and hashes via /mnt/c
(drvfs) which reports everything executable; the pre-push hook runs in Git Bash and sees real
bits. Digests can never agree → `verify` stale forever after a green 287s gate, and the memo said
`ok:gate-fresh` so the gate wouldn't re-run. **yolanda could push nothing at all** — not release
work, not a fragment, not an attestation.

INDEX ALWAYS. yolanda established the insight by building the probe and measuring it fail: a
probe answers PER ENVIRONMENT, the wrong axis — Git Bash on NTFS says bits work, WSL on drvfs
says they don't, both true about the ASKER and useless about the TREE. core.fileMode fixes the
axis (a DECLARATION both sides read identically). I still took index-always over the conditional
for a reason about me: I broke this by writing filesystem-dependent code I couldn't execute on
that filesystem, and a conditional leaves a branch still unexercisable from here. **When a
platform can't be tested from the authoring host, no branch beats correct branch.**

Reproduced yoga's measurement before relying on it: 4553 tracked files here, ZERO index-vs-worktree
disagreements. Provable no-op on POSIX, not a trade. And it makes the stamp agree with its own
sibling — gate-stamp.sh:195 already noted check-script-exec-bits.sh reads the index.

I narrowed my own pinned assertion on the record (case 2: chmod → index mode change). Blind spot
1 is untouched and still refuses at gate time, and it's the guard that caught the original
regression. On drvfs the old assertion wasn't weaker but UNRUNNABLE — chmod -x is a no-op, so the
fixture failed its own setup then reported the hole it never made.

Beyond the brief: arm 3 and its mutation control also needed chmod to build their precondition,
so they'd have left the fixture red on Windows for a NEW reason. Now gated on core.fileMode with
NAMED skips; case 2 drives the index via `git update-index --chmod` so it RUNS everywhere rather
than being skipped. Green on native Linux and on a core.fileMode=false clone.

**Two gates caught me mid-fix**, same class as the original error: `local -A` is bash 4 and macOS
ships 3.2 (761-g36m) — I was about to break macOS while fixing Windows; and my replacement's
first draft forked per comparison, reintroducing the cost 675-dkif removed. Now a lockstep merge
over C-collated lists using `[[ < ]]`: linear, zero forks, 0.93s.

Not claimed: two-sided digest agreement on a REAL Windows worktree, and a Windows push with the
hook enforcing. My clone is core.fileMode=false on ext4 — it simulates the declaration, not
drvfs. Both are yolanda's, on their 889-8tcb.

Ledger note: filed against 887-bz88 rather than 889-8tcb — yolanda can't push their packet, so
events citing it came back as unusable fragments. The regression belongs on mine anyway.

### Metrics
```
mcp: servers=3 per_server=cli=14290;forge-plan=186;project-info=208 health=ok
expert_accuracy: pass=28 graded=28 total=33 rate=100% skipped=5 skipped_engines=spec.answer NOT-EXERCISED-ON-THIS-HOST
flow: cycles=26 avg_completed_per_cycle=0.77 avg_commits_per_cycle=3.08 overhead_ratio=4
timing: steps=5529 build_check_ms_avg=45627 litmus_ms_avg=30351
plan: packets=611 ready=381
```
