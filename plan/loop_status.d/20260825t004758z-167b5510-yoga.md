## Cycle 2026-08-25T00:47Z (linux_immutable yoga — the exec wire's closure litmus, written and finally bound)

- **Host**: yoga, `linux_immutable` (Silverblue), branch `linux-next`, lane `loop-cron` (operator `/loop` every 2h).
- **Preflight**: `ok:cycle-preflight:rebuilt+expert-refreshed+services-down1`; dev inference ready. Enclave `tillandsias-proxy` reported `stopped ... health=stale-healthy age=4h5m` — noted, not restarted (a service that exits again immediately is a packet, not a restart).
- **Scheduler**: `due:timer-inactive` — no durable timer armed on this host; it is driven by the operator's `/loop` cron this session. Advisory, stated rather than discovered later (856-s56y).
- **Guards**: branch `ok:branch-linux-next`; MCP `ok:experts-healthy` + surface attested `exposed`; daily maintenance `ok:daily-maintenance-current:2026-08-24`; capability row `ok:capability-row-reported:yoga`; de-slop `ok:deslop-not-due:order=875 delta=41 hours=119`; stranded claims `population=2 in_progress=2 stranded=0`; checkout lock acquired `loop-cron:18380`; boundary snapshot on a clean tree.
- **Credential guard, a FALSE NEGATIVE worth recording**: first run returned `blocked:gh-cli-only` — "gh auth is green but a bounded non-interactive push probe failed". The probe (`git push --dry-run origin HEAD`) had not failed on credentials at all; it was refused by our OWN v5 pre-push hook, because the tree had changed since the last gate stamp. The guard cannot distinguish "no credential channel" from "a local hook declined before the network was touched", and it names the former. Seeding `.git/.gh-credentials` from the gh token cleared it and the push then worked, so the remedy the guard printed was harmless — but on a host where the stamp is stale for an unrelated reason, this guard blocks the cycle on a diagnosis that is not true. Not filed as a row this cycle: it belongs on 860-g798, which owns the push-verified probe.
- **Batch**: `epic=architecture-audit-epic role=linux release=v0.5 size=6 budget=10 score=17.197 seed=yoga pick=2/7 route=rank:6/7:width=7`; `triage: eligible=174 grouped=145 ungrouped=29 epics=11`.
- **Claimed**: 795-zshi (released to `ready`, cycle-scoped) and 837-et6t (**done**).
- **Delivered**: 837-et6t closed. `litmus:guest-exec-argv-vector-no-shell` written (9/9), `scripts/check-exec-argv-vector-workarounds.sh` added with both controls, `vsock-exec-authz` given its first-ever bindings entry, `litmus:pty-open-argv-allowlisted` repaired and ratcheted off `unbound-grandfathered.txt`, and `openspec/specs/vsock-exec-authz/spec.md` reconciled with the landed verbatim-argv arm (795-zshi next_action item 1). Suite: 2/2 tests, 15/15 steps, PASS. Gate green (forced).
- **The finding worth carrying**: the spec guarding the exec wire's authorization boundary was bound to ZERO litmus tests, and its one litmus file — grandfathered under 660-ryhn — had two assertions that had been false since 2026-08-17, when 795-zshi's own first slice moved the code they pinned. Neither could fail, because nothing ran them. A grandfathered assertion is unfalsifiable by construction, and the ratchet list is where they wait. Ten names remain on it.
- **Filed**: 875-v7hv — a litmus step carrying an escaped double quote passes on a host with `yq` and fails on one without it; the runner's grep fallback hands the backslashes to the comparison, which then reports a content mismatch between two identical strings. Measured here on the first run of the newly-bound file.
- **Not attempted, and why**: 795-zshi's code half. The lane that actually crosses the guest allowlist is macOS (`action_host.rs` → `PtySession::open` → `PtyOpen`); the Windows tray hands its argv to `wsl.exe` and never touches the allowlist, so the residual as written over-scopes to "the two tray callers". The macOS bridge's transport carries no `server_caps`, so feature-detection there is plumbing inside a crate that compiles to a cfg-gated stub on Linux — unverifiable from here, and a caps-aware constructor with no caller is the orphaned guard 865-n8vq exists to catch.
