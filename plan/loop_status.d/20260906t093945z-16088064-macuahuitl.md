## Cycle — macuahuitl hourly coordination (cron lane)

SIBLINGS: windows-next fully merged (ahead=0). osx-next +20, over D_max=5,
merged clean — 21 ahead. It brought real code (macos-tray/diagnose.rs,
vm-layer/vz.rs) plus macbookair's 1106-k2df retraction. Compaction
regression-checked before and after: green both times, and it stays a
per-land check while 1107-bb6s has no mechanism.

THE RELEASE IS BLOCKED ON FIVE NAMED FAILURES AND FOUR HAD NO PACKET, which
made the fleet's top-priority work invisible to every host's next-work drain.
Filing them is this cycle's deliverable:

  1109-t8kw p1  three fixtures assert properties of the environment they run in,
                so a correct refusal scores as a failure. committable-branch-
                guard's scratch checkout has no repo-local git identity and the
                guard CORRECTLY refuses; capability-manifest-guard's negative
                control expects a clean tree to warn about nothing and gets
                empty output; and esmeraldinha's tier-gate instance, where a
                low-end host's correct refusal read as an empty batch. Routed to
                esmeraldinha, who already holds one third of the fix.
  1110-c4nf p1  the work selector WRITES to the worktree it is asked to report
                on. Third defect in one component: it reads stale state
                (macneo's pre-selector-merge finding), reports no age on it
                (1099-t4xr), and now mutates the tree. Routed to macneo, who
                holds the other two.
  1105-h8vr     capability-routing-shape has ALSO outgrown its budget — step 1/5
                TIMEOUT at 120s with the same adjudicator verdict, not
                contended, genuinely too slow. Recorded as an event rather than
                filed, because sharing a symptom is not evidence of sharing a
                mechanism — the reasoning that moved 1105 off the timing log
                applies here too, and nobody has measured it.

SWEEP: live=12 unclaimed=0 expired=0 held=0. Nothing stranded, and every host
holds work — macbookair took 1106-k2df back after their premise audit returned a
false negative, macneo claimed 1080-4deb, yoga claimed 1103-i7xq. 834-r6vn is
still filed under the `windows` BUCKET rather than yolanda (1012-hu7d).

HEARTBEAT: three running (esmeraldinha, yoga, lenovinha), four idle
(macbookair, yolanda, pirria, macneo) — but all four idle hosts hold live
claims, so they are between tool calls rather than starved. NO REASSIGNMENT WAS
OWED; what was owed was making the release blockers claimable, which is why this
cycle filed rather than routed. Idleness established by asking, not by
loop-status mtimes.

TWO PEERS SELF-CORRECTED ON FINDINGS I WOULD HAVE TAKEN AT FACE VALUE, and both
corrections were caught by a positive control rather than by review. macbookair
reported that 1106-k2df's probe "does not exist in this tree" after searching
.sh/.rs/.md/.yaml — it exists at crates/tillandsias-headless/src/main.rs:9565 as
a jq argument built from a Rust string literal, with a doc comment stating
verbatim the claim esmeraldinha falsified. They took the claim back and filed a
correction. macneo nearly had me land a skill rule that was already landed; two
of their probes returned 0 with a wrong needle, and their positive control is
what caught it. A search that finds nothing is not proof of absence, now in a
search rather than an instrument.

OPERATOR QUEUE, unchanged and all three carried: pirria still cannot push
(credential.helper unset in all three scopes; every packet from that host tonight
came by hand relay, and they hold two verified patches with recorded hashes). The
OAuth all-or-none PAT decision, 1025-a896. The 03:23:01Z-04:22:15Z security log
before any fourth host is restored. And standing behind them, the structural one:
no dedicated smoke host exists, so the post-release e2e validation has been
unrunnable for three cycles.
