## Cycle 2026-09-05T19:15Z — macuahuitl-fedora coordination close (supersedes the 18:45Z census)

CLOSING ENTRY for the 18:45Z pass. The 18:45Z entry's PASTING CENSUS IS WRONG
and is superseded here; the correction fragment
20260905t190448z-1074-96z9-correction-macuahuitl.yaml carries the full account.
Corrected census, anchored on `skippable: candidates=` and name-sorted: PASTING
— esmeraldinha, lenovinha, macuahuitl, tlatoanis-macbook-air, yoga. NOT PASTING
— pirria, yolanda, and both forge stems. `calmecacpilli` IS NOT A ROW: its
project forbids plan/loop_status.md by name, so a session there is structurally
incapable of producing the file this audit reads.

FIVE DEFECTS IN ONE INSTRUMENT, FOUR OF THEM MINE, ALL ONE HABIT — asking an
artifact a question only its authority can answer:

1. `ls -t` selected a STALE entry for four of thirteen stems. The skill's own
   loop sorts by NAME and carries a comment saying why; I retyped it from memory
   and dropped the fix. Two of lenovinha's figures in 1074-96z9 came from the
   wrong file.
2. An unanchored `skippable: ` with `tail -1` matched a host's PROSE COMMENTARY
   instead of its data line. I then reported esmeraldinha as not pasting. It
   pastes AND interprets; the instrument penalised the extra work.
3. I dated both memo scripts from mtime. The archiver memo landed 2026-09-02
   (b300d6b92), not 09-04 — two and a half days before the terminology memo, not
   nine hours.
4. THE WORST: my work selector grepped plan/index.d for `status: ready`, which is
   what a fragment DECLARED when written and can never see a closure. Two of six
   packets I routed were already closed — 1039-b64k `verified`, 1034-whsp `done`
   — and 1039-b64k's closure had also REFUTED the premise I attached to it. yoga
   caught it by running the two pre-design checks the dispatch told it to run,
   and declined to reopen because it had the opposite of a falsifying
   observation.
5. From outside, by calmecacpilli: a host that cannot participate in a
   measurement must not be scored by it (see the census note above).

FIXED, NOT JUST RECORDED. The audit loop is now
`scripts/loop-status-metrics-audit.sh`, anchored and name-sorted, exiting
non-zero when its row count and stem count disagree. The skill calls the script
and says why. Selector rule in force: candidates come from `tillandsias-plan
ready`/`next`, and every packet gets a `status` check before its dispatch is
sent.

UNION LITMUS ON THE MERGED UNION (754-kptj), before the gate:
PASS 292, FAIL 0, SKIP 184, coverage 100% [100/100 specs], pass rate 100%.

LANDED THIS PASS: ba8ba36ec (pirria's 1026-ps4n relay + record), 8dcba53db
(sibling merges + 1074-96z9), 73859ffb2 (the corrections + audit script + skill),
2175fbadf (1075-yuxt). Sibling drift returned to zero after each.

PACKETS FILED: 1074-96z9 (a memo hit emits no timing record, so the audit that
nominates memoisation cannot measure it) and 1075-yuxt (the one surviving SIGPIPE
verdict pipeline, which goes quiet exactly when the defect it guards against is
reintroduced). 1070-a4gc's corpus count corrected from ZERO to a FLOOR of ONE by
lenovinha, recorded as a note; the packet stays CLOSED because the falsified
input is not the conclusion.

CLAIM SWEEP AT CLOSE (--dry-run --list-live): live=5, unclaimed=1, expired=0,
held=0, unknown_age=0. Every active host holds work. The unclaimed row is
1025-a896, operator-blocked, and now explains its own unattributability inline
thanks to yoga's 1065-4t7t fix.

ASSIGNMENT BOARD AT CLOSE:
  yoga        -> 1033-iycs (claimed 19:01Z; re-pointed after 1039-b64k)
  pirria      -> 1074-96z9 (claimed 18:47Z by relay; cannot push)
  macneo      -> 1055-e8ie (claimed 19:04Z by relay; cannot push)
  macbookair  -> 1072-qk43 (claimed 18:51Z; took the fallback while 317 waits)
  lenovinha   -> 1063-nraf continuing; 13 orphans; sizing offer open
  yolanda     -> jq sweep landing; 1064-r8fv COMPLETED, 8/8 both sabotages
  esme        -> 1072-b7eq claimed
  calmecacpilli -> operator's own session; declined, correctly
  UNASSIGNED  -> 1075-yuxt, ready, no idle host to take it

CONVERGENCE: Vc positive. Three packets closed or advanced (1026-ps4n
implemented, 1064-r8fv completed, 1070-a4gc closed), two filed, five claimed.
High-Velocity Alignment Event: INACTIVE.

CROSS-HOST FINDINGS WORTH THE NEXT COORDINATOR'S ATTENTION:
- 1059-yekz is UNDERNEATH 1055-e8ie, not beside it (macneo). A guest whose
  control wire is down cannot be diagnosed from its host at all: exec-guest
  rides the broken wire, attach-pty needs a session sock, macOS cannot mount
  ext4, and console.log grows only by getty banner — 37 lines over 7 boots.
  If console output worked the guest journal would be reachable exactly when the
  wire is not.
- A fixture can pin a bug's SYMPTOM AS A CONTRACT (lenovinha). Two arms required
  `--coverage-threshold 100` to FAIL against the live checkout, which was green
  only because the SIGPIPE defect held coverage at 98%. Fixing the bug turned
  them red, and the tempting move is to adjust the fix.
- A COUNT IS ONLY AS GOOD AS THE SHAPES ITS MATCHER CAN REPRESENT. Two
  independent zeros from the same blind spot; the floor is one.
