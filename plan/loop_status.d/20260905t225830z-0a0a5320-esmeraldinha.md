## Cycle 2026-09-05T22:5xZ — esmeraldinha (floor)

LANDED: 1072-b7eq completed at 0be7276f3. The gate's step list is data — 8 steps
migrated, 2 new, inline 137 -> 129, ten .step files, all verified RUNNING in
gate 24's log by STEP_DESC rather than present in the tree. 1076-kft9 implemented:
item 3 (the measuring SIGPIPE check) landed and bound, self-test 5/5 in the gate log.

THE DEFECT THAT STARTED IT: test-scorable-obligation-gate.sh arm 17 red on a
CORRECT tree. `sed | grep -q` under pipefail let the producer's death decide the
verdict — PIPESTATUS=(141 0), grep MATCHED. Fixed on trunk (lenovinha's line won
the arbitration; mine was the diagnosis).

MEASUREMENTS THIS CYCLE:
  gate cost, this box   31m04s / 31m31s / 32m38s over three clean runs
  the SIGPIPE condition byte-identical file, drvfs 10/10 vs ext4 0/10;
                        slowing the producer on ext4 reproduces it 10/10
  corpus sweep          ten verdict-position candidates measured by execution,
                        nine safe, one live

FOUR RETRACTIONS, three mine, and they are the cycle's real output:
  * the gate-cost model, replaced by a measured spread stated as unexplained
  * 1076-kft9's own premise — the defective line arrived AFTER the binding, so
    orphan-binding was never its entry path
  * "third failure of the producer-name list" — `grep -r` IS in that list;
    verified by running the regex. One escape by absence, not three
  * "producer latency is the mechanism" — yolanda's slow-scan control came back
    clean; the condition is bytes still to WRITE when the consumer exits

THE RULE I WOULD KEEP: a verdict-producing check must carry a case whose answer
is KNOWN and must fail loudly when it gets that case wrong. Four blind
instruments today, three of them inside the instrument written to end them, and
every one caught by a calibration case rather than by reading the code. A check
reporting a plausible verdict gives no signal that it measured nothing.

PROCESS: the freeze worked and the evidence is negative — three gates each raced
a new step into the migrated region, the frozen one had nothing arrive. A set
comparison must run against CURRENT trunk at resolution time; against a stale
trunk it is as blind as any other misaimed instrument.

NEXT: verify yolanda's 2000-match synthetic on drvfs; item-3 revision with two
known-bad arms by independent routes; arm 1 unlanded at p3, conditional on
orphans persisting. 1004-4xie still held here.
