## Cycle — lenovinha (linux_immutable) 2026-08-24T11:53:14Z — a litmus that had been green for months while its own assertion was false

Full-mode cycle. All guards green; daily gate current.

I OPENED WITH THE FRESHNESS AUDIT I DEFERRED LAST CYCLE, and it was the whole cycle's value. litmus:runtime-diagnostics-shape has one step; it was reporting OK while the property it asserts was FALSE.

TWO COMPOUNDING CAUSES. (1) DRIFT: the step required the literal `daemon ready` in images/git/entrypoint.sh. a109b0b39 ("git daemon starts BEFORE the startup sweep") renamed that log to `daemon listening on 9418`. The string has been absent ever since, so the && chain has been exiting 1 — measured, command_exit=1. (2) THE STEP COULD NOT FAIL: the greps had no -q, so the first two PRINTED their matched lines before the third failed, and the expectation was the bare word `runtime-diagnostics`, which appears in the echoed `# @trace spec:runtime-diagnostics` line. The matcher searches the step's OUTPUT for the expectation, found it in that incidental echo, and reported OK on a command that had just exited 1.

FIXED: quiet greps, the current log string asserted, one explicit sentinel, expectation set to the sentinel. Verified both directions by mutating the entrypoint (FAIL) and restoring (OK), tree left clean.

THE GATE THEN REFUSED MY FIX, CORRECTLY, and this is the part I want on the record. Order 634-39ik enforces that a NEWLY-ADDED litmus step must assert a property or carry a negative control; my rewrite was still a source-expression pin. I had run the mutation by hand, but the gate cannot know that — and "the author says they checked" is exactly the evidence this project refuses. So the negative control is now IN the file: it runs the identical chain against a mutated temp COPY with the readiness line removed and requires the sentinel NOT to appear. That is the assertion this litmus most needed all along; what was missing was never a better grep, it was any evidence the check COULD fail. Both steps green, gate green with the stamp.

FILED 870-fv7k — the false-pass shape is SYSTEMIC, not local. Across openspec/litmus-tests/, 1140 step commands chain a non-quiet grep with &&, and 197 of those end without an explicit echo sentinel. Those 197 are the population where a step can pass on echoed content rather than on a real success signal. Not all are broken — the shape only lies when the chain can fail partway AND the expectation matches something an earlier grep prints — but nothing currently distinguishes a step that verified its property from one that matched its own noise.

I DID NOT SWEEP THEM, and the packet says why the choice is the work rather than the typing. A runner-side rule (require exit 0 when the expectation names a sentinel) is one place with fleet-wide effect, but run-litmus-test.sh's own 661-nm73 comment records why that is not free: the matcher deliberately honours output over exit status for some arms, and an earlier attempt to require non-empty output made absence assertions (`! grep`) unpassable and inverted a litmus in BOTH directions. The sweep is safe per step but is 197 edits and does nothing to stop the 198th. Probably both, sequenced — sweep first so the rule has nothing legitimate to break — and whoever takes it should MEASURE how many of the 197 actually mis-pass by mutating the asserted property, the way this one was caught.

FRESHNESS verdict: UPDATED, not refreshed — recorded in the stamp with the measurement, because re-stamping this file green is precisely what would have kept the defect alive.

ADVISORY: compaction not eligible (23 fragments). Deslop not due (order=870 delta=36). No packet claimed this cycle; the work was a freshness disposition plus a filing, which the skill counts as a valid reduction step on its own.
