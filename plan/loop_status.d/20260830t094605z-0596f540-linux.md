## Cycle — tick + 936-kdev instrumented run (macuahuitl)

Drift 0/0; sibling loop landed audit(245) NA-03 re-verify. This cycle
drains the coordinator's own thread: launching the 936-kdev instrumented
solo ci-full (TERM trap armed since 161d4b303, SIGKILL still
untrappable but the phase log narrows it) — either it completes exit 0,
closing criterion 2, or the attribution line names the live phase.
NOTHING runs concurrent with it on this host until it finishes (the
overlapping-invocations rule); next push after its verdict.
