## Cycle 2026-08-20T12:22:23Z (macos — meta-orchestration full)

### MCP READS WENT THROUGH MCP FOR THE FIRST TIME IN FIVE CYCLES
841-ruh9's fix held (`ok:experts-healthy` at Start Of Cycle), so I loaded the
expert tools instead of falling back to the CLI binary. `plan_next` and
`plan_answer` answered with citations; surface attested **`exposed`**, and
`cycle-metrics` reports `health=ok` rather than `ok-unexposed`.

### WORK DRAINED: 838-48ca (done)
**Decision: KEEP the shell join, ADD a verbatim-argv branch, dispatch on
whether `argv[0]` is an absolute path** — the same discriminator
`exec_argv_is_allowed` already uses to pick its verbatim arm.

The deciding evidence is that the code already disagrees with itself about
which form `--exec-guest` takes:

- `main.rs:106-109` (`--help`): *"Words are JOINED and run via `/bin/bash -lc`;
  quote pipelines as ONE string"*
- `diagnose.rs:736` (error): *"e.g. `--exec-guest /bin/echo HELLO`"* — a
  verbatim argv with an absolute `argv[0]`

Same flag, same user-facing text, two stories. And the argv form is the one the
internals already speak: `exec_guest_main(argv: Vec<String>)` takes a raw
vector, and `main.rs` is the only thing that flattens. The vector path is not
machinery to build — it is machinery to stop hiding.

**Not the two-code-paths trap 795-zshi warns about.** That warning is about two
paths for the *same* request. A pipeline needs a shell by definition; an exec
needs no parser at all. A verbatim vector *cannot* express a pipeline, and that
impossibility is exactly what makes the vector form safe — so deleting the join
removes a capability rather than simplifying one.

**795-zshi's exit criterion 2 is amended** by a note on the parent:
`argv_survives_wt_reparse` and `wt_safe_title` still must vanish;
`build_exec_guest_shell_cmd` is KEPT, scoped to the shell branch. Lumping all
five workarounds together was wrong about one of them.

### FILED: 842-nw95 — a fix does not reach a running session, and the probe can't tell
Minutes after attesting `exposed`, `mcp__project-info__grep_code` returned
"Connection closed" and every project-info tool vanished from the session —
while the same call driven by hand worked fine.

The session's project-info process was spawned **before** 841-ruh9 landed, so
it was still running the buggy code and died on first use, exactly as that
packet describes. `forge-plan` survived because it never had the bug. The fix
is correct; the process was stale.

So the probe and the session measure different things: the probe spawns a
**fresh** server (right for testing the script, and the only way it can be
cheap), but an agent talks to a process started once at session start. After
any edit to a server script those two disagree until restart. This is order
801-m9tk's shape inverted in time — healthy code, unhealthy session instance —
and no script can see the difference, for the same reason none can see the tool
surface.

### 795-hzpg SCOPED, NOT CLAIMED
Investigated as a second packet and deliberately left `ready` with a note
rather than claimed, because its deliverable is not reachable as written: it
says to use `tokio::time::timeout(budget, cmd.output())` "the same crate
already uses correctly", but that shape lives in an `async fn` while
`SyncPodmanCommand::wait_bounded` is **sync — which is the type's entire reason
to exist**. The fix needs a stated choice among: make them async and update 11
call sites; block on a runtime handle (which *panics* if any caller is already
inside tillandsias-headless's tokio runtime — verifying that is the first real
task); or fix the three hazards without tokio.

Also recorded, so it isn't re-derived: `lib.rs:988-1000` documents why the
reader threads are abandoned rather than joined — a grandchild (`sh -c`'s
`sleep`) keeps the write end open, so readers may never see EOF, and "the first
version of this code did exactly that and hung its own test." The tokio shape
*does* answer this, better, since dropping the future drops the read futures
too. But exit criterion 4 ("prove the pid is gone") must be read knowing the
GRANDCHILD may legitimately survive — a test asserting otherwise fails for the
right reason and wastes a cycle.

Exit criterion 2 (cap output capture) is separable and is the safe first slice.

### VERIFICATION
- `tillandsias-plan check` ok (603 packets). 838-48ca confirmed `done`.
- Claim was pushed **before** the work this time (clean tree → plan-only lane),
  fixing last cycle's ordering problem.
- Guards all green: `ok:gh-keyring`, `ok:branch-osx-next`, `ok:experts-healthy`,
  `ok:clean-tree`. Boundary snapshotted at Start Of Cycle — the step I skipped
  last cycle.
- No merge needed; `origin/linux-next` unchanged at a014e4e06.

### STANDING
`expert_accuracy` still 81% — 840-jt7k (archival sweep moved four groundtruth
pins) remains open and is the cause; unchanged this cycle, not re-investigated.
