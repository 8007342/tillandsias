## Cycle 2026-08-26T08:32Z — macuahuitl (linux_mutable, linux-next), hourly /loop

CLOSED 606-um5s. The p1 that had been blocked three times on host capability is
complete: nixpkgs nixos-24.11 (EOL) -> nixos-26.05, crane v0.20.3 -> v0.24.0.

ALL FIVE REMAINING OUTPUTS REALIZED, RC=0, in ~10 MINUTES — and that is a WARM
number that must not be quoted as the cold one. 790-mbk9's "~1,000-crate cold
compile per target" is still the unmeasured case; this host's nix store already
carried the closure. Said so in the packet rather than letting a fast number
stand as a general one.

CLOSURE COMPARISON, with a baseline that cost nothing: the v0.4.260826.1
RELEASE ARTIFACTS were built by CI from the PRE-BUMP flake, so they are a
genuine old side without a second build on an EOL nixpkgs.

  tillandsias-linux-x86_64   15,291,632 -> 15,226,560   -0.43%
  headless-x86_64-musl       14,177,520 -> 14,132,952   -0.31%
  headless-aarch64-musl      13,046,984 -> 13,047,480   +0.004%
  router-sidecar-x86_64       1,607,704 ->  1,595,920   -0.73%

All within +/-0.75%, and ZERO runtime references on every output — which
settles "no user runtime dependency disappears" definitively: there are none on
either side. Caveat stated in the packet: closure size and published FILE size
are different quantities, near-identical for zero-ref static outputs but not
the same measure.

NO BASELINE EXISTS FOR forge-image OR web-image AND ONE CANNOT BE MANUFACTURED
— the old flake could not evaluate .#forge-image at all. Reported the absence
rather than a delta.

FILED 909-xc4k (p3): eachDefaultSystem emits darwin package sets that have
never been buildable (forge-image needs pkgs.glibc for an FHS symlink), and the
DEFAULT `nix flake check` omits exactly those systems — announcing its own
blind spot in a warning printed AFTER its pass verdict. Verified pre-existing
by restoring the old flake and lock. Found by chasing that warning because it
named aarch64-linux, which 606-um5s criterion 2 asks about.

MY OWN ERROR THIS CYCLE, caught before it landed: wrote that packet with an
UNQUOTED heredoc so I could interpolate the order token, which also enabled
backtick command substitution. Two exit_criteria lines lost their backticked
content to executed commands (`nix flake check --all-systems` ran and its error
went into the file). YAML still parsed VALID — the damage was silent. Caught by
reading the rendered criteria rather than trusting the parse, and repaired with
a tool that does not go through a shell. Same shape as everything else: a check
(yq) that passes identically whether or not the content survived.

Guards green, lock clean, boundary preserved, stranded 0, siblings merged
(windows-next 3, osx-next 2, no compiled source).
