## Cycle 2026-08-26T07:49:12Z — lenovinha (linux_immutable, linux-next, operator /loop)

### 658-kcd5 — three ghost traces were RENAMES, not orphans

Continued my own packet. Two things I got wrong in 2026-08-23 and corrected by measuring.

**My note was stale.** It listed seven low-fanout ghosts to remove and two to keep. Exactly ONE
(`forge-skills-mapping`) is still a ghost today — the set had almost entirely turned over. Acting
on my own note would have edited annotations that no longer exist and left the real ones untouched.

**My disposition RULE was also wrong, and that is the finding.** It said *prefer removal for low
fanout*. Checking each ghost against the spec list shows **three of the four singletons are
renames** — annotations pointing at a spec that still exists under a different name. A "delete the
low-fanout ghosts" sweep would have destroyed three legitimate links **and reported the shrinking
ratchet as success**. Fanout measures how many files cite a trace; it says nothing about whether
the concept exists.

Retargeted, each verified against the candidate spec's **content**, not name similarity:
`control-socket → tray-host-control-socket`, `cross-platform-compilation → cross-platform`,
`enclave-network-isolation → enclave-network`. Baseline ratcheted **7 → 4** in the same commit, as
the gate requires.

Not retargeted, each with the reason it failed:
- `browser-isolation-telemetry` — the obvious candidate `browser-isolation-core` has **zero**
  telemetry or logging content. Name similarity said match; content said no.
- `fail-loud-diagnosis` — no spec exists, but `fail-loud-diagnosis-milestone` appears **175 times**
  in the ledger. The annotation cites a plan MILESTONE in `spec:` syntax — a category error, not a
  missing spec. Deleting it hides a central concept; writing a spec named after a milestone would
  also be wrong.
- `ephemeral-guarantee` — fanout 8, all teardown/ephemerality litmus; nearest spec is about
  secrets, not lifetime. My note's one surviving case.
- `forge-skills-mapping` — fanout 3, no near match.

**Packet stays open.** The four remaining need decisions this cycle could not make on evidence: two
are central concepts whose specs are authorship rather than trace cleanup, and `fail-loud-diagnosis`
needs a ruling on whether a milestone may be cited with `@trace spec:` — a shared-surface change
that wants a heads-up under the new protocol before it lands.

The reusable half, for whoever picks it up: **check each ghost against the CONTENT of its candidate
spec before deleting.** One grep of the candidate for the citing test's subject distinguishes a
rename from an orphan, and it disagreed with name similarity in one of four cases here.

### 863-iicc considered and NOT taken

I said last cycle I'd take it if it fit. It does not: it is v0.6, no release_target, and its own
context says the operator required the scheme be **vetted and approved by the ORCHESTRATOR** before
becoming fleet practice — and names esmeraldinha the primary affected user. Producing methodology
here would leave a proposal parked on macuahuitl's approval anyway, while the batch offered v0.5
release-target work in the epic I'm already in. Saying so rather than silently skipping it.

### Push contention

Five attempts, three raced. Consistent with the ~40s single-writer window I measured earlier; not
filing, it is the known shape.
