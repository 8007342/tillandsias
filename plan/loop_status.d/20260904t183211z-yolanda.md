## Cycle 2026-09-04T18:15Z-18:35Z — yolanda (windows), advance-work-from-plan

Boundary first; `linux-next` merged before the selector. Step 3's wire work could
not run (macbookair's consumers land in the 19:45Z window, `tray/mod.rs` still
carried 14 references), so: **filed 1029-5wvd**, **flagged a wire-version
collision**, and **closed 805-ek9e** on a selector pick.

- **1029-5wvd filed (p1).** macbookair measured what my crate reading only
  suspected: a pure reorder of two adjacent `ControlMessage` variants moves
  `EnumerateLocalProjects` 9 → 10 and **`cargo test` passes 64/64 while
  `./build.sh --check` exits 0**. The whole gate is green on a renumbered wire.
  Filed explicitly as *not* closeable by tonight's commit — the bump protects
  peers, my five-variant pin covers one commit's risk, and neither closes
  "nothing in the tree notices a renumbering".
- **A second breaking wire change was queued and nobody had connected them.**
  803-49re's residual is 890-y72v, whose fix changes
  `DeliverCredentialsReply`'s payload shape — a breaking wire change needing its
  own bump. Raised it while I still had ninety minutes to batch it. Ruling:
  land 3 tonight, 890-y72v carries 4, with the coupling recorded on that packet
  so version 4 is a known consequence rather than a Monday discovery. Right
  call — nobody could specify that discriminator with authority before 19:45Z,
  and I could not test it here.

**805-ek9e closed as obsolete, with evidence for both halves.** Its mechanism
(an injected guest wrapper) was never built and its own ledger already said not
to build it; its intent — lanes in Windows Terminal with paste and clickable
links — is met by 823-u5zf + 836-smm2, with `argv_survives_wt_reparse` deleted
in `c3f6591a1`.

**I sabotage-tested the pin rather than citing it green**, which is the standard
I spent the day holding others to. Injecting the exact flattened shape the
packet reports (`["bash","-lc","cd … && exec bash -l"]`) into the real
`launch_spec` turns the test RED — "falls back to conhost and loses paste" —
green on restore. **My first attempt at that control did not compile**, so it
proved nothing and is not offered as evidence; only the second run is. The
pin's own comment records that an earlier revision asserted over a hand-written
vector while nothing called `launch_spec` — the same proxy-read-as-the-thing
failure, already caught here by someone else.

That is the disposition question answered honestly: a dead mechanism with a
*live* intent is a REWRITE, never a close. This is a close only because the
obligation is **discharged**, not dropped — and the way to know the difference
was to check the symptom is gone *and* that something would notice its return.

### cycle-metrics (`--cycle-start 2026-09-04T18:15:08Z`), verbatim

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent
timing: steps=9130 build_check_ms_avg=596625 build_check_mix=mixed:forced=129,memoised=17 litmus_ms_avg=3478 slowest=step:checking-that-added-fragments-declared-closures-resolve-885-92iu:490764 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-04T18:15:08Z steps=0 top3=- source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=8371 steps=124 top3=build-check:runs=113:total_ms=71746491:avg_ms=634924:fail_pct=19,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=92:total_ms=10189269:avg_ms=110752:fail_pct=0,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=92:total_ms=6291491:avg_ms=68385:fail_pct=0 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=50 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=92:avg_ms=110752:fail_pct=0:saved_ms_upper=10078517,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=92:avg_ms=68385:fail_pct=0:saved_ms_upper=6223106,step:writing-the-gate-stamp:runs=92:avg_ms=58000:fail_pct=0:saved_ms_upper=5278064 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=703 ready=456 plan_bin=./target/release/tillandsias-plan.exe
repo: commits_this_cycle=- worktree=dirty traces=current
verdict: attention:worktree-dirty
```
