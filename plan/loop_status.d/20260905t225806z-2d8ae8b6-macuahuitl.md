## Cycle 2026-09-05T23:05Z — macuahuitl-fedora hourly coordination (cron lane)

Scheduler posture: hourly coordinate :41 and 4h meta :09 armed; no cron changes
made, none requested.

SIBLING AUDIT: NOTHING TO MERGE. windows-next behind 11, osx-next behind 46,
main behind 456, all ahead=0. Trunk 8289bf1a4, worktree clean, 0/0.

THE CYCLE'S FINDING: FOUR PACKETS HAVE THEIR ARTIFACTS ON TRUNK AND STILL READ
`in_progress`. Verified by checking the artifact rather than inferring from the
land:

  1049-s35z  scripts/test-jq-multiline-capture.sh          present   yolanda
  1072-b7eq  scripts/gate-steps.d/060-1072-b7eq.step       present   esmeraldinha
  1059-yekz  scripts/test-forge-project-guard-hooks.sh     present   macbookair
  1076-kft9  scripts/test-sigpipe-verdict-measured.sh      present   esmeraldinha

Work landed, status never written — 1080-4deb's family from the end that cost
macneo most of a cycle when 1055-e8ie read `ready` with two of its commits
already on trunk and I routed a host onto finished work. Four more instances in
one evening. Chased to the owners; NOT closed from here, because a status
transition from a third host rewrites authorship (1079-qb8k).

YOGA'S 1082-k3wp IS LIVE AND THE SWEEP IS DOWN FROM THREE CLAIM-WITHOUT-STATUS
ROWS TO ONE. The remaining row now names BOTH remedies instead of assuming the
host forgot:

  claim-without-status  823-u5zf  yolanda  2026-09-05T12:29:30Z
    (owes either `set-field <id> status in_progress` or, if it decided NOT to
     hold the packet, `append-event <id> release "<why>"` — a typed release,
     because prose saying so is not a signal any reader can act on)

That is the refinement working one cycle after landing, on the row that
motivated it.

FILED LAST CYCLE AND NOW ON TRUNK: 1084-x8ya, macneo's successor to 1055-e8ie —
a guest whose provisioning COMPLETED and never reaches Ready. Claimed for them
by relay. Both of their scope caveats are in next_action rather than left to
surface at criterion 1: they can reproduce on demand and CANNOT close it without
a Linux mount, and it may not be a defect in the sense the title implies. Its
`unscoreable:` is stated rather than a pin invented — no gate has a guest, and
the Linux host that could mount the image cannot reproduce the failure.

A FRAMING I WITHDREW THIS CYCLE. I offered lenovinha "second occurrence" on
their archiver-memo red. They declined it correctly: that arm passed 6/6 in
their green gate and has not recurred; the two reds since were DIFFERENT steps,
both correct refusals — the scorable-obligation decider in under a second on two
packets whose closures opened with prose, and the ghost-trace ratchet on the
very last step. I was pattern-matching on "that host's gate is red again"
instead of reading which step, which is the thing I have spent the evening
asking other hosts not to do.

METHODOLOGY GAINED lenovinha's fourfold rule, appended to
quoted_history_lives_in_comments_guards_scan_declarations:

  A FILE THAT CONTAINS THE STRING IT REASONS ABOUT IS A FALSE POSITIVE FOR EVERY
  SCANNER KEYED ON THAT STRING, AND PROSE IS NOT EXEMPT.

The count is the argument: one file, one day, four times — the marker written
literally, the COMMENT explaining that hazard spelling it out, a third in an
assertion grepping for the literal, and a FOURTH in the first draft of the
comment explaining the third. The fourth caught only by grepping their own fix
before re-gating rather than trusting a lesson they were mid-way through writing.

RELAY INBOUND: pirria's 1074-96z9, metadata and per-part hashes published before
content as the protocol requires — 25891 bytes, 399 lines, sha256 f9b537d9…,
two parts. Four arms each run twice, against the shipped file and against a
mutant with the fix's lines deleted, 12/12. And END TO END ON TWO REAL GATES
rather than only the fixture: one archiver-check-miss and two
archiver-check-hit are in that host's timing log, so the hit rate is now
countable — the cheaper half of what 1001-q3zf's digest was wanted for.

Their dialect-gate catch is worth the record: their first fixture built
timestamps with `date -u -d @<epoch>`, and 761-g36m refused it because BSD date
SUCCEEDS with garbage output, so their `|| date -u` fallback was useless against
a command that succeeds wrongly. Same class as their host-dependent identity arm
and as the packet they were fixing — correct in the regime that can see it,
silently wrong in the one that cannot.

calmecacpilli's session is GONE from the peer list. It is the operator's own and
not mine to restart; the website-work summary the operator asked for is still
owed and is now blocked on that session existing.

BOARD: yoga idle holding 1081-gynk and 1082-k3wp, both landed, closures owed.
lenovinha working, eight commits, gate four in flight. esme idle holding three
rows, two of them open-on-landed. macbookair idle, 1059-yekz open-on-landed,
1082-9rub offered and not yet claimed. yolanda working. pirria working, relay
inbound. macneo working on 1084-x8ya.

SWEEP: live=10 unclaimed=1 expired=0 held=0 unknown_age=0.

CYCLE METRICS, verbatim:

```
experts: calls=8 answered=8 unsupported=0 degraded=0 errors=0 answer_rate=100% tools=check,ready source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=1 per_server=cli=8 legacy_untagged=0 health=ok source=/home/tlatoani/claudia/tillandsias/.cache/metrics/forge-expert-usage.jsonl
expert_accuracy: pass=33 graded=33 total=33 rate=100% source=groundtruth-all-sets
flow: cycles=17 avg_completed_per_cycle=0.76 avg_commits_per_cycle=5.47 overhead_ratio=7.15 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-cycle-flow.jsonl
timing: steps=72274 build_check_ms_avg=96934 build_check_mix=mixed:forced=597,memoised=209 litmus_ms_avg=41086 slowest=check:litmus-pre-build:1967661 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=3h steps=158 top3=build-preamble=25,build-check=14,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=14 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=60388 steps=595 top3=build-check:runs=476:total_ms=50735664:avg_ms=106587:fail_pct=27,litmus-suite:runs=629:total_ms=30108152:avg_ms=47866:fail_pct=60,check:litmus-pre-build:runs=28:total_ms=23045017:avg_ms=823036:fail_pct=43 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=79 floor_ms=2000 min_runs=5 top3=step:running-workspace-tests-cargo-test-workspace-all-targets:runs=75:avg_ms=89175:fail_pct=0:saved_ms_upper=6598991,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=345:avg_ms=16496:fail_pct=0:saved_ms_upper=5674796,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=345:avg_ms=12427:fail_pct=0:saved_ms_upper=4275064 source=/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=785 ready=465 plan_bin=./target/release/tillandsias-plan
repo: commits_this_cycle=- worktree=dirty traces=current
verdict: attention:worktree-dirty
```
