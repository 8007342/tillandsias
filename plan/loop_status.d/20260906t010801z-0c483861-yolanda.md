## Cycle 2026-09-06T01:08:01Z (yolanda — 1004-5f7p closed; and my plan binary was writing)

**Landed** 5ea174d10. `--diagnose` can now tell "came up, then correctly idled
out" from "never came up" — two states that were previously
`distro_running:false` with exit 2 and byte-identical apart from a log tail.

**The packet is not the shape it was clustered with, and macbookair guessed that
before I started.** 1082-9rub was a MISSING transition: an artifact existed and
provisioning never completed. This is a transition that COMPLETED and then
stopped being true — the guest provisions perfectly and WSL2 idles it down about
forty seconds later, exactly as designed. Nothing in provisioning is broken; the
defect was in the VOCABULARY. I told them the template did not transfer whole
rather than bending the packet to fit it, which is what they had asked for.

**Their caveat inverted, and that is the useful part.** They warned that a
completion record is a record of a PAST completion, so a non-durable ready state
leaves exactly that behind and I would rebuild their defect one level up. True in
their shape — here the staleness IS the signal. The question is not "is the guest
ready now", which `distro_running` already answers, but "did this installation
ever reach Ready". The property they had to defend against is the one I needed.

**Fails closed** — absent, unreadable, or belonging to a different installation
all read `never-observed-ready`. Both inversions sabotaged and both caught.

**MY PLAN BINARY WAS RELEASING OTHER HOSTS' CLAIMS, and only running it found
that.** The fleet check came back **5 passed, 11 failed** here — five of eight
hosts exposed. I checked trap 1 explicitly by counting SKIP lines rather than
trusting the exit code, so it was a real reading rather than an unresolved probe.

**And the remediation I was given was wrong in the same way the diagnosis had
been.** I was told esme's gate would bring windows-next current and my exposure
would close without me doing anything. That is true of the TREE and false of the
BINARY: my tree already carried the fixture — I ran it from there — while the
binary read 5/16, and **nothing in an ordinary cycle rebuilds
`target/release/tillandsias-plan.exe`**. After the rebuild: **16 passed, 0
failed**, zero SKIP.

**A recency heuristic is a version string wearing different clothes.** My binary
was rebuilt four hours earlier, which FEELS current, and every source-side signal
agreed. Only running the thing and counting arms separated them. That is the
fourth instance of my own rule tonight and the first where the misleading signal
was a feeling rather than an output.

**I also tried to make a touchpoint out of a grep, and the gate refused.** Adding
`ready_history` requires the five-touchpoint lockstep the exact-count test
demands, and one touchpoint is a litmus step that greps `tray-diagnose.ps1` for
each field name. I extended it; 634-39ik refused it. Removed rather than
defended — **a pin that cannot fail is not a touchpoint, it is a decoration on
one.**

**Not decided, deliberately:** whether `--provision-once` should leave the guest
resident is a product decision about what the command promises. Exit codes are
unchanged, because turning the idle case into exit 0 would mask a real outage for
any caller that needs the guest up.

Agent: windows-yolanda-opus5-20260906t010801z
