## Cycle 2026-08-26T13:45Z (windows/yolanda — drained 663-64xi to `implemented`; obsoleted a duplicate I filed)

- Guards ok, lock free, daily maintenance current, deslop not due.

### 663-64xi — implemented, field verification explicitly outstanding

Reproduced before writing anything: `InitialTooltip = "Tillandsias 0.4.260728.1"`
against an installed `0.4.260826.1` binary — a month's drift, and the same value
the operator reported sixteen days earlier. Defect 1 (accumulation) did NOT
reproduce here: one entry only, because the installer's prune works and I watched
it fire during the release e2e. It is portable-flow-specific, as the packet says.

**Nearly the wrong diagnosis:** my first read queried the property `ToolTip` and
got empty, which would have read as "no tooltip is written at all". The value is
`InitialTooltip`. Caught by enumerating `GetValueNames()` rather than querying a
name I had guessed — third time in two days that has caught a constructed name.

The tray now reconciles at startup: refresh its own entry, prune tillandsias
entries whose exe is gone, **leave** entries whose exe still exists elsewhere.
The decision is a pure ungated module so all three criteria run on every host —
which matters most for the negative control, the arm that makes this a
reconciliation rather than a purge. "Prune what is not me" would pass every
positive test and quietly break a dual portable+installed setup.

**`implemented`, not `completed`:** the policy is verified, the Win32 walk is not.
Exercising it needs a tray built from this commit, and the installed tray is the
previous release. Claiming `completed` off a locally-green unit suite would be the
platform-green-counted-as-another error this fleet corrected on 2026-08-25.
`next_action` names the four field steps, including the enumerate-not-query
warning and which half is the negative control.

### 895-bkrn obsoleted — a duplicate I filed without checking

`667-kfyn` has asked for the same outcome since **2026-08-10**, owns
`scripts/with-wsl2-builder.sh`, and has a better design: a distro-native mirror
synced per invocation, a mechanical fidelity check, an escape hatch, and a
before/after measurement as an exit criterion. I filed 895-bkrn on 08-26 without
checking for an existing owner — the exact rule I have spent two days citing at
other hosts.

**And 667-kfyn's design dissolves the blocker I invented.** I had marked 895-bkrn
"blocked on a decision" because relocating the checkout would re-plumb every
Git-Bash-side path resolving `./target/release/tillandsias-plan.exe`. That is true
of MOVING and false of MIRRORING — 667-kfyn leaves the /mnt/c checkout where Git
Bash expects it. The operator decision I said was required was an artifact of my
framing.

All measurements moved to 667-kfyn, including one that corrects its rationale **in
its favour**: its title is about "cargo source I/O", but cargo is only 10.6% of a
350s gate while plan-ledger reads and tree-wide hashing are 61%. A whole-checkout
mirror serves six times more than it claims.

### 750-zrt4 — a second instance, different mechanism

The selector returned a byte-identical batch on three consecutive cycles, with
`urgent=` a macOS p1 this host cannot claim and two ancient unscored rows — zero
windows work — while `plan_query pickup_role=windows status=ready` returns 15+
claimable packets. Recorded as an event rather than a new row, and I did **not**
diagnose it: two candidate causes (role-blind urgent override; ordinal routing
landing a low-rank host on a 2-packet epic), and picking one without reading the
selector would be the confident-wrong-cause shape.
