## Cycle — yoga 2026-09-05T01:2xZ — 972-umik: I shipped a regression and reverted it
- CYCLE METRICS, verbatim, `scripts/cycle-metrics.sh --cycle-start 2026-09-05T00:47:02Z`:
      repeat: window=since=2026-09-05T00:47:02Z steps=121 top3=build-preamble=14,build-check=13,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=6 source=/var/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      skippable: candidates=28 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=149:avg_ms=17006:fail_pct=0:saved_ms_upper=2516888,step:checking-user-visible-terminology-against-the-dictionary-629-t6b:runs=149:avg_ms=14269:fail_pct=0:saved_ms_upper=2111929,step:running-plan-ledger-tests-cargo-test-p-tillandsias-plan-all-targ:runs=124:avg_ms=15924:fail_pct=0:saved_ms_upper=1958692 source=/var/home/tlatoani/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
      plan: packets=726 ready=466 plan_bin=./target/release/tillandsias-plan
      repo: commits_this_cycle=- worktree=clean traces=current
      verdict: attention:experts-never-called
- THE HEADLINE IS A REGRESSION I SHIPPED AND REVERTED. e6a80609f flipped the control wire's default to secure with only the LISTENER converted. All four clients still carry their own parsers defaulting to plaintext — host-shell/vsock_client.rs, macos-tray/action_host.rs, macos-tray/diagnose.rs, and windows-tray/hvsocket.rs with its byte comparison — and nothing in scripts/ or packaging/ sets the variable. Every client would have opened a plaintext connection to a server that had just started refusing plaintext. A silent insecurity became a TOTAL OUTAGE, which is strictly worse than the defect. Reverted at 08a7d3cc7; macuahuitl held the daily cut and the trunk carries zero behaviour change.
- WHY MY TESTS SAID IT WAS FINE. 574 passed because the headless tests drive handle_connection_with_mode explicitly. The failure is CROSS-CRATE — a server in one crate against clients in four others — and no unit suite pairs them. I had a green suite and a quoted Unauthorized error that LOOKED like proof the fix worked; it was the sound of every client failing.
- THE MIS-SCOPE, which is the lesson. I sliced the packet by CRATE when its boundary is the DECISION. "The default is secure" is one fact about a HANDSHAKE: flipping one side is not a smaller version of flipping both, it is a different and worse change. A partial slice is only valid when the partial state is COHERENT, and I never checked that this one was.
- WHAT CAUGHT IT: macuahuitl asked for the pre-fix and post-fix OUTPUTS of a listener started with no env. Going to produce the post-fix output is what made me ask what the clients would do against it. Nothing in my own process would have surfaced it. The question that found it was about the PRODUCT, not the code.
- BANKED, and it is real: the shared reader in tillandsias-control-wire, the listener's conversion to it, the divergence table, and TWO measured corrections to the packet. (1) The home is control-wire, NOT secure-channel: four of five reader crates already depend on secure-channel but vm-layer does NOT, while ALL FIVE already depend on control-wire — so control-wire is a pure move and secure-channel would have added the very edge the "move, not a new edge" argument existed to avoid. (2) There are THREE behaviours, not two: vz.rs is case-insensitive on 'on' and silently maps everything else to off, and its value PROPAGATES INTO THE GUEST through the systemd unit it writes.
- THE REVERT ALMOST REPEATED THE ORDER'S OWN DEFECT. Reverting the code alone would have left three comments asserting the OPPOSITE of the behaviour — the module header, the enum variant doc, and a refusal message telling operators "absent means ON" — in the file whose whole subject is a decision that meant different things in different places. Corrected in the same commit. No test would have found them; tests do not read comments.
- A SHELL ACCIDENT LEFT TWO TRACES, both now cleaned. I put backticks around a code fragment inside a double-quoted append-event argument, so the shell EXECUTED it: the event text lost the fragment (corrected by a follow-up event) and the redirection inside it created an untracked file named `off` in the checkout, which is what failed the boundary verify at finalize. Same family as 775-b4qz; append-event text is shell-quoted and backticks in it are executable.
- ALSO THIS CYCLE: 1028-3eiz completed at 04ade3b56, three asserts not four. 972-umik returns to in_progress under my name with the slice boundary rewritten as the decision — six readers, a cross-crate pairing test, and a sabotage arm that leaves one reader on plaintext and expects red. That sabotage arm is precisely what my 574 green tests could not be.
