## Cycle — yoga 2026-09-03T01:09Z (meta-orchestration, full mode; cron lane)
- Start of cycle: preflight ok (tillandsias-proxy down for a FOURTH cycle — operator); new UTC day so the daily gate was due, body run and stamped; guards ok; lock acquired; boundary clean at a277c5776.
- CAPABILITY ROW HAD DRIFTED — `row-only=gpu/container/ollama, probe-only=-`, the urgent direction, since routing that consumes the matrix would send GPU work to a triple this host no longer advertises. Republished. THE TRAP UNDER IT: the probe resolves `tillandsias` through PATH, and this host's INSTALLED binary is dated 08-30 — four days behind HEAD, schema_version 2, no render_nodes, predating both yolanda's proof producers and 967-6ax6's container-name fix. A row generated from it would have written a stale claim into the routing matrix. Rebuilt tillandsias-headless and probed with target/release first on PATH: schema 3, render_nodes populated. Same class as everything this session — an instrument's AGE silently entering the answer, with the row looking like a statement about hardware.
- 793-zumy VERIFICATION DELIVERED (owed to yolanda since her merge): with current code this host reports accel_proof=reachable, and render_nodes is exactly one row — {node renderD128, vendor_id 4098 (0x1002), device_id 4372 (0x1114), driver amdgpu, vantage container, proof reachable}, legacy_tier still gpu-rocm and still the first legacy_tier-spelling match. All four of her predictions confirmed, including the one she said was the only part actually hers to be wrong about.
- COMPACTION: eligible at 146 fragments, folded, 538 -> 563 packets, +2845/-41. Read the removed LINES rather than a histogram: every one belongs to a field some fragment reassigned — superseded capability-row list items (`- ts:`), a `lanes` entry (`- container`), `status`, and multi-line `next_action` block scalars. The documented structured-value case, not structure loss.
- 964-zedm IMPLEMENTED. spec-index-ensure.sh staged via the ambient TMPDIR; in a forge /tmp is a 256 MB tmpfs while the index root is on a 1.2 TB overlay, so a cold 22,645-chunk build died on ENOSPC with 1.2 TB free. Now stages beside $INDEX_ROOT. THE REASONING THAT PICKS THE DESTINATION, since "somewhere bigger" is not a rule: the staged payload and the published index grow together (~92 MB staged for a 22.7k-chunk corpus here), so the filesystem that can hold the RESULT can hold the WORKING SET — capacity becomes a property of the code rather than of the ambient environment.
- DELIBERATELY NOT native_scratch_dir(), and this is the reusable half: that helper looks like the obvious reuse and would have REPRODUCED the bug. It answers "is this worktree on a SLOW filesystem, and where should scratch live for speed", and by design returns the caller's fallback on fast native Linux — exactly the forge case, so it would have handed back /tmp. Speed and capacity are different properties. Reuse is right when the QUESTION matches, not when the shape does.
- Criterion 3 done too: the verdict printed `blocked:spec-index:payload-failed` and nothing else while jq's "No space left on device" went to stderr with no path, so the reader learned a payload failed rather than that a tmpfs they never chose was full. It now prints both directories with df free space and names TILLANDSIAS_SPEC_INDEX_TMPDIR. Same lesson as 965-sxec's refusal last cycle.
- VERIFIED: scripts/test-spec-index-staging-capacity.sh 6/6, wired into --check (invisible on any host with a roomy /tmp, which is every host that runs the gate). Includes a REGRESSION arm asserting the ambient-TMPDIR form is not reachable before the index-root form, and a NEGATIVE CONTROL that the unwritable-index-root fallback still exists — a "fix" that just deleted the old path would otherwise pass. DECISIVE LIVE CHECK: with TMPDIR pointed at a NONEXISTENT directory the build completed (ok:spec-index:built:22867) — precisely the condition that used to yield blocked:spec-index:no-tmpdir. Staging no longer consults TMPDIR at all, which is stronger than "TMPDIR is big enough here".
- SCOPE NOT CLAIMED: criterion 2 wants a host whose /tmp is a real 256 MB tmpfs and creating one needs root, so I proved the mechanism rather than the closure. Named in next_action for a forge. Pairs with 965-sxec: both were ways the forge lane silently failed a step that passes everywhere else, and both needed a forge to notice and a non-forge to fix.
- Scratchpad headroom WARN (4246M of 6052M, under the ~4500M a cold Rust target needs) — advisory, and I build in the checkout, so no action. Scheduler still `due:timer-inactive`: the armed cron is a SESSION cron, which that check cannot see; a durable timer is 856-s56y and remains unclaimed.
