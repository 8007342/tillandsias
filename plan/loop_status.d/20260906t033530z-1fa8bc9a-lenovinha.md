## Cycle 2026-09-06T03:35:30Z (lenovinha linux — advance-work: 793-qc6q locus-aware accel routing landed; 776-jcf3 substrate measured)

MO-FULL: COMPLETE 79abf156a0471521a7b69718f2330c138f7213a5 linux-next 79abf156a0471521a7b69718f2330c138f7213a5

Direction: forge local experts is the standing theme; this cycle reduced against
stable-milestone-v1, which the batch selector ranked 2/8 by residual (seed
silverblue-ryzen7-amd+nvidia-lenovinha-20260906, budget 4, triage
eligible=241 grouped=156 ungrouped=85 epics=11).

WORKED: 793-qc6q, one partial slice, landed 68552584e, claim released to ready.
The packet's next_action named the blocker as "the routing still says CPU" and
the fix axis as locus-awareness; that is now closed and two criteria remain,
neither of them mine to close.

The defect restated from the code rather than the packet: phase_device_usable
hard-coded `lanes contains "container"`, so route_phase could only answer for
one lane. On macOS that reading is ACCURATE and the answer is still wrong —
Metal does not cross into a forge container, the document says so correctly, and
routing believed it, sending both phases to the CPU on a host whose GPU wins
decode 1.27-1.64x and prefill 3.2-3.8x. The device state was never the bug. The
question was under-specified. RoutingLocus{Container,HostNative} is now a
required parameter with deliberately no default.

THE HALF THE PACKET DID NOT NAME, and the one I would have missed by
implementing only what was written: the ENGINE is lane-bound too. Only
supported_device_classes was consulted, so the fleet's container-only ollama
would have qualified a device for the host-native lane — the same defect one
field further in. And decode_crossover_b paired a CPU row with a GPU row
regardless of MeasurementRecord.locus, which is a second soundness bug: a
crossover is a subtraction between two curves, and that field exists precisely
because this fleet measured one suite at two loci and the 5-10% hop INVERTED a
conclusion. That inversion was reachable inside a routing threshold where
nothing downstream could see it.

EVIDENCE, feature set beside every count: 679 passed / 0 failed / 4 ignored
under --features tray,listen-vsock; accel_probe 86 passed (81 before); litmus
accel-capability-probe 2/2 PASS and inference-policy-router 1/1 executed PASS,
re-run green on the landed tree. NEGATIVE CONTROLS RUN, not reasoned about:
reverting the device-lane hard-code and the crossover locus key turned 3 of the
5 new tests RED (83 passed); reverting ONLY the engine-lane check turned the
4th RED (85 passed).

NOT CLAIMED, measured and recorded: 776-jcf3, the batch's p1 top pick. Its
credential blocker is GONE (real push 03:38Z, 6f8f63dbb) but this host has no
tillandsias-git image, no mirror, no proxy, no forge, and --ensure-enclave
scopes to network/Vault/proxy by its own help text. Criterion 1 on linux needs a
lane launch, which is the build-install-smoke-e2e shape and not an advance-work
slice. A different reason from the 2026-08-26 note, which said the unattended
lane lacked credentials; both now sit on the packet.

THREE THINGS THIS CYCLE LEARNED, all the same shape — a verdict emitted from a
path that could not observe what it names:

1. `./build.sh --check 2>&1 | tail -30` printed "[exited with code 0]" while
   clippy was failing inside it, because a pipeline exits with tail's status.
   `error: could not compile` was in the text I had just printed. I nearly
   debugged past my own red gate. Redirect and capture $? directly.

2. The gate was OOM-killed TWICE as a BACKGROUND TASK by the harness watching
   FREE memory (272Mi) rather than AVAILABLE (9.2Gi). No kernel OOM lines in
   dmesg, and this host's own 1047-h88p cap already pinned CARGO_BUILD_JOBS=1,
   so job count was not the cause and lowering it further would have been
   folklore. Running the same gate in the FOREGROUND completed first try,
   GATE_EXIT=0, 466s. A background-task memory kill and a gate failure are
   indistinguishable from outside and the fix for one does nothing for the
   other.

3. cycle-metrics returned `attention:experts-never-called` while reporting
   `source=absent`. I called plan_answer and plan_status through MCP this
   cycle. The metric is measuring its own blindness, not the behaviour it
   names. Recorded rather than accepted, because a verdict from an absent
   source reads identically to a verdict from a present one.
   experts_substitution: unknown, per the standing rule.

Peer traffic, macuahuitl: confirmed from source that check-credential-channel.sh
returns on three PRESENCE arms (store file, GH_TOKEN, GITHUB_TOKEN) before ever
reaching the one verifying arm at :337, so 860-g798's remedy is unreachable on
any host that has seeded the store. Sharper than "skips verification": arm 1 is
`[ -s "$cred_file" ]` and never reads the contents, so it AFFIRMS ok: in front
of a possibly-revoked token. lenovinha prints reaches-arm-4, so this host's
readings were the verifying arm actually running. Left the packet with them
rather than double-file it, which is the 814-iyu7 lesson applied.

Read path: MCP-first throughout (plan_answer for Direction and packet spans,
plan_status for statuses); fell back to the filesystem twice, both VERIFICATION
class — read the cited plan/index.yaml spans before claiming, and read
check-credential-channel.sh's arm ordering before repeating a peer's finding as
fact.

DEFERRED, stated rather than skipped silently: the full litmus suite came back
373/388 with 15 failures on the pre-land tree, and my `tail -20` truncated the
names. The two specs BOUND to the surface I touched pass, twice, so the landing
requirement is met — but I have not attributed those 15 and am not claiming they
are pre-existing. A full re-run with complete capture was still in flight at
cycle end; whoever reads this next should not treat the 15 as explained.
