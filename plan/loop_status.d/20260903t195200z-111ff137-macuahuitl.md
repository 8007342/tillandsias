## Cycle — macuahuitl 2026-09-03T19:50Z (coordinate-multihost-work, cron 23-odd)

- **Merges.** `osx-next` 5 ahead, merged — macbook's ratchet-wiring cause
  correction, an attestation, two cycle-status fragments. `windows-next` 0 ahead
  / 2 behind, **levelled again** with ancestry re-verified at push time; third
  time today, and it keeps drifting because yolanda is still blocked on the
  operator.
- **CLAIMS ARE NEARLY CLEAR: live=0, unclaimed=1, expired=0.** 978-juw4 went to
  `blocked` last cycle; 795-5itp and the rest have closed out. The one remaining
  was 980-xcaf, macneo's, unclaimed-in-progress for 10.7h with its author silent
  and a status ask unanswered.
- **980-xcaf RELEASED TO `ready`, NOT CLOSED**, and the distinction is the whole
  point. An unclaimed `in_progress` row is unowned AND unroutable at once —
  excluded from the ready queue, worked by nobody. Last cycle I left it and
  recorded that as a deliberate choice; at ten hours the cost of invisibility
  exceeds the cost of moving it, and moving it asserts nothing about whether the
  work is done. I did NOT close it: macneo reported it verified end to end, and
  confirming that is theirs.
- **The carry-forward is from macneo's own reports, not my summary of them** —
  the end-to-end result under the minimal GUI PATH, the qcow2 cluster census
  (10,007 compressed against 1,207 normal, so 89% of allocated data is deflate),
  and the **`open -a` masking trap**, because the obvious verification propagates
  the calling shell's PATH and yields a green that means nothing. Whoever takes
  it does not spend a cycle earning a false green.
- **Roll call.** macbook 19:35Z and yoga 19:21Z active within the half hour.
  lenovinha 16:43Z — asked, and offered two genuinely untouched p3 rows chosen
  with the `worked:` marker rather than guessed. macneo silent 10.7h; already in
  the operator queue as a question about the host rather than another ping.
  pirria, both forges and esme all finished or blocked by their own statement.
  **esme-windows has no ledger footprint at all** — correct, not idle: their lane
  is orchestrator-gated for landing, so their four findings today reached the
  trunk through me.
- **Operator directive landed this cycle: context-cost metrics** (997-pdgf).
  Instrumentation only, no remedy adopted, and the methodology now carries a
  refinement obligation for the metrics themselves. `/compact` and `/clear` are
  recorded as candidates precisely so adopting one is a visible decision rather
  than a drift.
- **yoga's enforcement rung caught my own packet** hours after landing —
  first time on a stranger's work rather than a fixture. I took the `unscoreable`
  arm for both rows and named the test that lifts the exemption on one, rather
  than naming a litmus that does not exist.
- **Litmus:** not run. The merge brought ledger fragments and an attestation;
  `--check` passed on the merged tree.
- **Operator queue, five plain asks:** renew the esmeraldinha PAT — and note
  759-vceg now explains why the login exited zero without fixing it, so the ask
  is now *confirm Contents:write and re-run from a checkout with an origin*;
  permission for macneo's forge-build measurement; **is macneo-macos still up**;
  the root-vs-forge privilege call on 996-bcjx; and the ten quarantined trees.
  yolanda's two decisions remain open beneath those.
- **Nothing is blocked** that is not named above.
