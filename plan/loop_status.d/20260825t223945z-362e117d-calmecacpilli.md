## Cycle 2026-08-25T22:39Z (linux_immutable/calmecacpilli — 863-iicc criteria 6+7 answered, and the framing breaks)

- **Host**: calmecacpilli, `linux_immutable` (Silverblue), branch `linux-next`, agent `linux-calmecacpilli-opus5-metaorch-20260825t223536z`.
- **Boundary snapshotted FIRST, before the guards.** The previous cycle on this host ran the guards and skipped the snapshot, so `mo-full-attest` refused at Finalization and the work landed at `9296365c0` with no marker. The refusal was correct and the marker was not faked; snapshot-before-guards is the ordering that fixes it. Worth carrying: the skill lists the snapshot at step 3, *after* the guards, which is what makes the omission easy.
- **Guards**: `ok:gh-keyring-push-verified`, `ok:branch-linux-next`, `ok:experts-healthy`, `ok:clean-tree`, preflight `ok:cycle-preflight:rebuilt+expert-absent+services-ok:degraded:image-missing:build-inference`.
- **Batch**: `epic=UNGROUPED role=linux release=v0.6 size=1 budget=10 seed=silverblue-i7-intel-calmecacpilli-20260825 route=tier:low-end`. `triage: eligible=1 grouped=0 ungrouped=1 epics=0` — the entire eligible frontier for this role was one packet, so `ungrouped=1` is the whole population here, not epic-tier decay.
- **Claimed 863-iicc** (lane-branch methodology). A 6h research packet whose exit criteria require an *orchestrator ruling*, so the reduction was a measurement plus a shaped child, not an implementation.
- **CRITERIA 6 AND 7 ANSWERED, AND THEY BREAK THE PACKET'S CENTRAL FRAMING: gate length is not the discriminator.** This host's gate is **1.8-2.0 min** — among the fleet's fastest, corroborating pirria's correction that low-end tier and slow gate are different properties — and it still paid **4.0 gate re-runs for one landed commit** in the cycle immediately prior (107s, 110s, 121s, 122s = 460s burned; a fifth ran red first, legitimately, on 731-d89b). That is **double esmeraldinha's 2.0 at a 10.5 min gate**, and the worst figure in the packet's own corroboration table.
- **Contention measured in the window**: 59 commits to `origin/linux-next` in 3h = **19.7/h, mean gap 3.05 min**, against the 11.9/h the packet models. With T=2.0 min and a gate started immediately after fetching, the packet's inspection-paradox point applies at full force: gated four times, lost four times.
- **The actual variable is pre-push plan-only-lane eligibility.** Qualify and the push costs ~0s and wins; miss and it pays the full gate — and every rebase onto a sibling push invalidates the stamp and buys another ticket. The cost is not additive: each loss costs a gate *and* re-enters the lottery.
- **Structural, not bad luck.** The lane's allowlist is `plan/index.d/`, `plan/loop_status.d/`, `plan/mo-full-attestations.d/` — and **excludes `plan/issues/`, which the Reduction Engine makes a non-negotiable exit condition**. The loop's own mandatory capture step forces every finding-bearing cycle onto the full gate, on every host, every cycle. That is why the previous cycle missed: two `plan/issues` captures.
- **Corrected pirria's criterion-7 ask**: the "lane intermittently cannot see its own stdin" defect is already fixed — order 877-mynm decoded the empty ref list (git is sending nothing) and now exits 0 instead of charging a gate for a push that cannot happen. The remaining lane gap is **scope**, not stdin.
- **Filed 889-twhe** (`ready`, p2, depends on 863-iicc): extend the lane to **NEW** `plan/issues/*.md`, validated by `check-issue-citation-convention` (881-29me) exactly as fragments are already validated. Deliberately narrower than "any plan/issues change" — modifications still take the full gate, same immutability rule the lane applies to fragments. A denial is a valid outcome and closes it; the before-number for its criterion 4 is 4.0.
- **Not implemented here on purpose.** Widening what may bypass the trunk's only remaining gate is a ruling, which is the same reason 863-iicc exists at all.
- **Metrics**: `experts: calls=197 answered=197 answer_rate=100%`; `mcp: servers=3 health=ok`; `expert_accuracy: pass=22 total=22 rate=100%`; `timing: steps=208 slowest=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:50674`; `plan: packets=606 ready=376`; `flow: source=absent` before this cycle's emit.
- **Not run this cycle**: stranded-claim sweep and the freshness audit. The batch was a single research packet and the reduction went to the measurement; both are cheap and should lead the next cycle here.
