## Cycle — macuahuitl 2026-09-04T02:08Z (meta-orchestration, full mode)

- **THE DAILY GATE WAS DUE — first date rollover of this session.**
  `due:stale:2026-09-03`, so I ran the body rather than eyeballing it. Disk was
  151 GB free with I/O pressure at 0.00 across all windows, so heavy jobs were
  safe. Ran and stamped with the steps that ACTUALLY ran, since the stamp mode
  refuses a stamp with no named steps: delegate sweep (no registered runs), nix
  toolbox gc (`under-ceiling:size=20G:ceiling=20G` — a no-op, correctly), podman
  image prune (60→58), capability row current, `test-nix-toolbox` 13/13,
  `nix-deps-stable`, nix-cache started with 4 pinned and its fixture 8/8.
- **Build-cache sweep NOT due, and I asked the tool rather than judging.** The
  expert answered the policy exactly: 40 GiB or a 14-day marker. `target/` is 17
  GiB with a 2-day-old marker →
  `ok:build-cache-sweep-not-due:reason=under-threshold-2d`. A `cargo clean` would
  have cost a full rebuild for nothing.
- **Drained 999-5m4e, which came from my own over-confident closure of
  986-ahnc** — and the first thing I did was check its premise rather than
  accept it.
- **PRODUCTION WAS NEVER EXPOSED.** The delegated function has five callers: ONE
  production (inside the already-guarded attached function, after validation) and
  four inside `mod tests`, which begins at line 16663. I then checked the layer
  below for a bypass: both podman-client launch methods have exactly ONE
  production caller each, both inside those two functions. So every production
  container launch already passed the hardening check. lenovinha's premise —
  that worker drain uses the delegated path — is true of which function does the
  work, not of its guarding.
- **The gap is still worth closing and I closed it.** The invariant held because
  of WHERE THE CALL CAME FROM, not because of anything the inner function
  checked. One future caller reaching it directly — the natural thing for a
  queued drain — loses the envelope with **no diagnostic**. The invariant is now
  local.
- **MUTATION-VERIFIED, and the fixture could not supply the control.** lenovinha
  correctly hardened the fixture's argv from the policy constant so it cannot
  drift, which means it can no longer demonstrate the gap it found — they
  disclosed that. So the new control hands the entry point a bare argv directly.
  With the guard disabled it fails AND the report reads `TimedOut after_secs: 3`
  — it did not merely fail a check, **it launched the container**.
- **Two process errors of mine, both repeats from earlier today.** My insertion
  anchored on a function signature and landed between the original `#[test]` pair
  and its function — duplicate attribute, orphaned test, gate 101. Identical to
  this morning's awk insertion. And worse: I set the packet `done` in the same
  block that ran the gate, so a closure claiming "Gate green" was written while
  the gate was returning 101. **A self-report standing in for a measurement,
  inside a closure claim.** Nothing false reached the trunk because the land
  refused, and both are recorded on the packet.
- **Litmus:** not run. `--check` green, delegated suite 14/14 including the new
  control.
- **Operator queue, five plain asks, unchanged.**
