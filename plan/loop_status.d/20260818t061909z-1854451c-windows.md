## Cycle 2026-08-17T23:19 PDT→00:50 PDT (windows — YOLANDA, MO FULL: 795-jjw3 CLOSED — one wsl.exe constructor, with a gate that keeps it one)

**Result: the largest single piece of work this loop, and the one with the cleanest evidence — proven RED BEFORE GREEN, unlike two earlier cases where I could only manage forward pins. Three of my own slips were caught by this project's rails rather than by me, and a fourth by cycle-metrics after I had already pushed.**

- **STOP CHECK**: `Mon Aug 17 23:19:09 PDT 2026`. **Worth recording as a trap**: UTC had rolled to `2026-08-18T06:19Z`, which reads like it is past an "Aug 18 06:00" deadline. It is not — the deadline is 06:00 **PDT** = 13:00 UTC. Reading the UTC line alone would have stopped the loop ~6h40m early.
- **Host**: YOLANDA, `windows-next` 1ab2d709b → merged origin/linux-next ×2 → d6fa0a682, local == remote. All guards green; boundary `/tmp/meta-orchestration-boundary.usSCe0`. `in_progress=0 stranded=0`.

**795-jjw3 CLOSED.** Three `Command::new("wsl.exe")` sites and two mirrored helper sets, because vm-layer could not depend on podman, the reverse edge did not exist, and vm-layer did not reach `tillandsias-core` — the one crate both spawners already shared.

Verified BEFORE moving anything: core depends on neither vm-layer nor podman (its only workspace dep is logging, which has none), so the edge creates no cycle; and core already carries tokio for the async constructor. Added `tillandsias-core::wsl` (WSL_UTF8_ENV, CREATE_NO_WINDOW, no_window_async/sync, wsl_command_async/sync), added the vm-layer → core edge, and made both crates **re-export** rather than redefine — call sites unchanged, no second copy to forget a policy in.

**The negative control the packet demands is preserved and tested.** The constructors deliberately do NOT apply a window policy: the debug-console keepalive and the lane terminals are supposed to show a console. Flattening that would trade a real capability for tidiness. `no_window_*` stays opt-in, with a test asserting the constructor leaves the choice to the caller.

**PROVEN RED BEFORE GREEN.** `scripts/check-wsl-exe-single-constructor.sh` reports both violations and exits 1 against the pre-refactor vm-layer; prints `ok:wsl-single-constructor:44 scanned` against the fix. Wired into `./build.sh --check` and observed running there (`wsl.exe single-constructor check passed`). The packet is explicit that the CHECK, not the edit, is the deliverable.

**FOUR OF MY OWN SLIPS, each caught by a rail rather than by me:**
1. Removing podman's duplicate orphaned a `///` block documenting nothing — rustdoc would have refused it.
2. The guard's first draft used `mapfile`, a bash-4 builtin. The dialect gate (761-g36m) refused it: macOS ships bash 3.2, and a check that cannot run everywhere the gate runs is worse than none. Its message also states the allowlist is a BURNDOWN list, so the fix was writing it 3.2-clean (here-doc read loop in the current shell, so the counter survives).
3. I **propagated a ghost trace into a brand-new file** by copying `spec:windows-wsl-runtime` from podman's header without checking the spec exists. Removed. (podman's own line-1 instance is byte-identical pre-refactor — `git show HEAD~1` — so it is NOT mine; it belongs with the six `spec:enclave-service-catalog` ghosts from orders 357/364.)
4. **I pushed without `Cargo.lock`.** Adding the vm-layer → core edge updated the lockfile and the refactor commit did not carry it, so the pushed tree briefly had a Cargo.toml dependency its lockfile did not reflect — `cargo build --locked` would fail for anyone else. Caught by `cycle-metrics`' `verdict: attention:worktree-dirty`, NOT by my own `git status --short --branch`, which I had read a moment earlier and which showed clean: the lockfile was regenerated by the gate's build AFTER that read. **Same trap as the gate-stamp race two cycles ago — reading repo state while a build is in flight.** The rule adopted then (do not inspect gate state mid-flight) needs widening to worktree state generally.

**808-43mw / 808-7yrd HELD, ninth consecutive cycle.** Still `ready`, unclaimed, no linux response, no operator steer.

**NOT DONE, flagged:** freshness audit skipped in favour of the worker drain (the skill's own ordering); ledger compaction eligible; bar-raise proposed, not enabled; 806-2r4s NPU half open (809-7e4m); 798-q4m9 open pending 819-qgy2's 1-vCPU measurement.
