## Cycle 2026-09-11T19:17Z addendum — macuahuitl-fedora, release v56.9.11.1 and the gate that had to be fixed to cut it

**RELEASE CUT: Linux and macOS published, Windows job failed (run 34649585604).** PR #109 (parked DO-NOT-MERGE since 2026-09-06) was
retitled and merged to main (7a8f57b58), VERSION bumped to 56.9.11.1 through
release/version-bump (main bump-merge f1f7c01bc), tag v56.9.11.1 pinned there,
back-merged into linux-next, release.yml dispatched as run 34649585604.
Evidence for the cut: ./build.sh --ci-full on 9125c71df GREEN — litmus 356/356,
checks 32/32 (21:01Z-21:22Z). No macOS/Windows host smoke: both down since
2026-09-06; their artefacts are CI-built only. Post-release e2e smoke not run
on this host (no dedicated smoke host; the destructive-reset consent rule).

**THE FIRST FULL GATE WAS RED (41497b5ea, 20:19Z-20:42Z): 352/356 litmus,
3 failed checks.** Traced, each to a different cause:
- markdown-distillation: CODEX.md (440cde994's fourth name for AGENTS.md) —
  allowlisted beside GEMINI.md (865-q4wp class).
- guard-activation: check-deslop-sweep-health.sh (829-dkuc, 2026-09-06) was
  invoked by nothing — wired into local-ci as a report; declared in
  gate-divergence-declared.txt as excluded (1087-h2z9's gate refused the
  undeclared difference on the next land, which is that gate working).
- litmus:capability-routing-shape: PR 109 blocker #2, timing out at 120s since
  2026-09-06 — cause is 1034-whsp's cross-branch fold on every selector run
  (185s idle vs 28-47s before); the fixture stubs the fold through the
  selector's own seam (47s, 8/8); the live-path cost is recorded on 1034-whsp.
- concurrent-forge-shared-stack-safety fixture 2b and expert-groundtruth-harness
  step 5/31: red in-gate, green standalone within the hour — recorded on
  1120-s3e5 as two more instances of the in-gate-only shape (three fixtures in
  one gate run). Not fixed; the second gate passed them.
The second full gate on the fixed head was green first try.

**LANDS THIS CYCLE: 8 attempts for 3 lands.** Refusals, in order: the 877-mynm
mutation control (in-gate only, 1120-s3e5), long-running view missing the
compaction-exposed 1087-h2z9 row, a new packet without a scorable obligation
(977-448j; set-field cannot satisfy it — the fragment's own bytes must), the
gate-divergence declaration. Each is a ledger-shape gate invisible until the
gate runs; running the three checkers by name before landing would have saved
four gates (~16 minutes). Recorded in this host's memory, not only here.

**SIDE EFFECT TO KNOW ABOUT.** --ci-full ends by installing the tree's launcher:
this host's tillandsias --version went from the operator's local v56.9.11.3 to
the tree's v56.9.5.1 label (HEAD code, older label). Filed as 1122-6sqz; the
operator's own local install flow should re-run.

**OPERATOR ASKS THIS CYCLE, BOTH FILED.** Token counters + sub-agent rules:
1119-6wn6 (rules landed in both skills; counter open). The agent-commit
trailer as the machine-readable commit-to-packet link: 1121-g47q (measured:
115 of 727 commits in 7 days cite no order; 440cde994 was one).

**tokens (addendum):** subagent_tokens=0 agents=0 after the operator's directive;
main-context spend for the whole cycle ≈ 500k (15.00M -> 14.50M remaining).

Windows job failure root cause and fix: 1122-xi2f (the 1059-ry6t placeholder check demanded both arches while order-282 staging resets the non-host arch to 0 bytes; the check now covers the embedded arch only, applied blind from Linux, a Windows host must confirm).
