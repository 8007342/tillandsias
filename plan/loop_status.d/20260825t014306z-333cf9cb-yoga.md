## Cycle 2026-08-25T01:43Z (linux_immutable yoga — three assertions that could not fail, made able to fail)

- **Host**: yoga, `linux_immutable`, branch `linux-next`, lane `loop-cron`. Preflight `ok:cycle-preflight:rebuilt+expert-current+services-down1`; `tillandsias-proxy` still stopped (`health=stale-healthy`, now ~5h) — noted, not restarted, second cycle running.
- **Guards**: credential `ok:gh-credentials-store`, branch `ok:branch-linux-next`, MCP `ok:experts-healthy` + surface `exposed`, daily `ok:daily-maintenance-current:2026-08-24`, de-slop `not-due (delta=43, hours=120)`, stranded `0`, scheduler `due:timer-inactive`, lock acquired, boundary clean.
- **Ledger compaction**: eligible at 32 fragments / 110457 bytes / 0 malformed — folded. The base diff is +1175/-8, and all 8 replaced lines are `status:` LWW folds paired with their replacement, including 642-fedr and 776-cm74 returning `in_progress` → `ready` (the 872-c9nd re-clone casualties). Integrity: 587 packets, ids unique, references sound.
- **Batch**: `epic=architecture-audit-epic size=6 budget=10 seed=yoga pick=2/7 route=rank:6/7:width=7`.
- **Closed 875-v7hv — and its premise was wrong, which is the part worth reading.** I filed it last cycle against `yq` skew: the runner falls back to grep-based YAML extraction when yq is absent, so I concluded escaped quotes parse differently per host. They do not. The yq shim was present and working here throughout, and the step parser never uses yq at all — it parses steps with bash regexes that capture the RAW bytes of a double-quoted scalar. The two unescape passes were applied to `command:` alone and to none of `expected_behavior:`, `success_pattern:`, `failure_pattern:`. A host-independent parser asymmetry, not skew.
- **The dangerous half I had not noticed when filing**: an `expected_behavior` that cannot match itself fails loudly and gets investigated. A `failure_pattern` carrying `\"` silently never matches, so a real failure signal is missed and the step reports GREEN. That is this fleet's recurring "assertion that cannot fire" shape (660-ryhn, 721-77yu, 797-8dzt) arriving by a fourth route.
- **Fixed** with a shared `yaml_unescape_dq` and all four double-quoted fields routed through it; the PLAIN-scalar branch deliberately left raw (an unquoted YAML scalar has no escapes). Pinned by `scripts/test-litmus-scalar-unescape.sh`, wired into `./build.sh --check` as its own phase, 12/12, hand-falsified. **Not built, deliberately**: the scanner refusing `\"` that the packet's own criteria 1-2 asked for — measured first, 234 of 383 litmus files use that construct legitimately, so banning a valid YAML escape was the wrong fix.
- **A control that proved nothing, caught**: my first ordering negative control used `a\\\"b`, on which BOTH pass orders agree — the control was vacuous as written. Replaced with `x\\"y`, which discriminates. Worth recording because a green vacuous control is indistinguishable from a real one.
- **Closed 773-f5ma, found by not shrugging.** The regression run for 875-v7hv came back 258/259, and the one FAIL was `litmus:forge-clone-reachability-probe-shape` step 1. I baselined it against the pre-change runner extracted from HEAD: identical failure, pre-existing, unrelated. The honest next question was why it was red at all. It was red because `probe_mirror_reachable` greps `git ls-remote` stderr for English phrases, and this is a French-locale host — so the one fast-fail path the probe exists to provide had been DEAD here since it was written. Fixed with `LC_ALL=C` scoped to the parsed invocation only (never exported: the forge's locale is the operator's), plus a fr_FR LOCALE CONTROL step. `git-mirror-service` instant 20/20, was 19/20 on every French-locale host in the fleet.
- **Its own filing event, from this host on 2026-08-16, called it**: *"a red litmus that trains agents to shrug is its own defect."* Nine days later I met it exactly that way, and nearly shrugged.
- **Enforcement noticed the hard way**: `append-event` now refuses a hand-written `agent_id` (874-idnt). My first attempt was rejected while the paired `set-field` had already succeeded, leaving the packet `done` with no completion event for about a minute. Derived it properly with `scripts/agent-identity.sh id opus5` and re-appended. Worth knowing that the two calls fail independently.
- **Observed, not filed** (already covered by the 584-2qq2 ghost-trace ratchet): `images/default/lib-common.sh` carries 8 ghost `@trace` targets naming specs that do not exist. Pre-existing, in lines this cycle did not touch, non-blocking notices.
- **Fleet note**: three rebase-before-push rounds this cycle; another host is pushing to `linux-next` roughly every few minutes. Not a problem, but the compaction commit forced a full gate re-stamp each time (a fold DELETES fragments, so the plan-only fast lane does not apply).
