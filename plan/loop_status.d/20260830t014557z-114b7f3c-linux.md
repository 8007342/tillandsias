## Cycle — hourly tick (macuahuitl)

939-jxgz CLOSED BY THE TWINS: bound verified on BOTH hosts, np=2 arm
PASSES (post-fix the pre-fix 13/67/88/124s spread does not reappear —
the transport deadline reaches every phase; yoga's refutation-risk
prediction survived its own test). Full arc: filed from ladder data →
mechanism pre-registered and falsified-tested → transport-deadline fix →
mutation control (neutralized deadline forced a 120s kill) → both-twins
re-verification, all inside one evening. Headline numbers: 60000-cell
spread 91.7s → 714ms; default-budget median 4.5x faster (the unbudgeted
decomposition phase was taxing EVERY request, not just the tail).

Tier recommendation now stable and recorded: Ryzen-AI-300 class ships
0.5b + retrieval-only floor as normal mode; 7B never completes synthesis
on CPU; raising the budget buys a guaranteed wait (deterministic at the
bound); leave the default. GPU paths are the escape: lenovinha's CUDA
lane works (296 tok/s); ROCm needs runtime-in-image (packet filed);
Windows needs a Vulkan ICD (operator-gated .wslconfig provisioning).

Board: yoga + yolanda back on 937-68n4's endpoint half (OpenAI facade —
the remaining deliverable); lenovinha 793-zumy enumeration redesign;
macbook idle (Apple-credential-gated); macuahuitl coordination, 936-kdev
open (SIGTERM attribution trap landed, sender still unidentified).
Trunk integrated both directions; velocity strongly positive.
