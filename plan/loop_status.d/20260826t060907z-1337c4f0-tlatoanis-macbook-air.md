## Cycle 2026-08-26T06:09Z (macos tlatoanis-macbook-air — closed 782-zy95, and my own verification lied to me)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 34. Boundary snapshotted first; guards green; **daily maintenance was due for the new UTC day and I ran the body** (delegate sweep, podman skipped — no machine on this host, nix absent, feature-lane compile added to the stamp).
- **BOTH p1 HEADS OF MY BATCH ARE UNADVANCEABLE FROM THIS LANE** — 731-eupn's remainder needs Windows evidence (I released it there myself), and 776-jcf3 records *"macOS: PROVEN"* with linux and windows outstanding. That is a **second concrete instance of 884-hfsj**: `pickup_role` records who may CLAIM, not what the REMAINING work needs, so a macOS host is offered two p1s whose macOS halves are already done.
- **Closed 782-zy95**: `c38e91f83`, whose message says only *".gitignore: add mock-release/"*, rewrote the file +62/−76 alphabetically sorted — hoisting every explanatory comment into a block at the top and leaving ~30 rules reading as arbitrary. **The rationales were detached, not lost**, which is the mechanism behind the operator's report that ignore rules "flip-flop". Recovered the pre-sort structure from `c38e91f83^` and gave the **nine post-sort patterns** a rationale each, cited to the commit that introduced it.

- **THE NEAR-MISS IS THE MORE USEFUL HALF, AND IT WAS MY OWN VERIFICATION THAT LIED.** My first reconstruction attached two rationales as **trailing** comments (`/target-musl/    # sidecar target dir`). **Gitignore has no inline comments** — a `#` anywhere but line-start is literal — so both patterns silently stopped matching.
- **My pattern-set diff normalised through `sed 's/[[:space:]]*#.*//'` — stripping the exact characters that broke the patterns — and reported `IDENTICAL — no rule added, removed or altered`.** A check whose normalisation removes the defect it is checking for. Caught only by running `git check-ignore -v target-musl/foo` and getting nothing where the old file returned `.gitignore:69:/target-musl/`.
- **Verification now has two legs because the first one is the one that lied**: a raw pattern-set diff with no stripping, and a **behavioural** `git check-ignore` sweep over 20 representative paths — including the `!cheatsheet-sources/.gitkeep-tombstone` **negation**, the one entry whose correctness depends on ordering relative to its parent rule. Byte-identical verdicts old vs new.
- **Generalisable, and it is this packet's own defect class turned on my repair**: an unattributable ignore rule reads as documented and documents nothing; a normalising diff reads as a check and checked nothing. **Both survive inspection because the thing that would falsify them was removed before the inspection ran.**

- **Filed two build-cache findings, both from stamping the marker honestly rather than running a sweep the size threshold did not call for:**
  - **905-97xh** (p3): the marker records `date=2026-08-25 utc=2026-08-26T06:09:41Z` — **one line, two days**. `check-daily-maintenance.sh` stamps UTC; `check-build-cache-sweep.sh` stamps *and compares* local. Self-consistent today, so not a correctness bug, but it is the one holdout from this project's UTC discipline and a DST/timezone move puts a one-day step in the staleness math.
  - **906-qi89** (p3): "due" at 18 GiB against a 40 GiB threshold **because the marker had never been initialised**, prescribing a `cargo clean` the policy itself says must be rare. `over-size`, `stale-Nd` and `no-marker` are three different facts; only two are evidence of anything. Stamped with an honest action string recording that no reclaim ran and why — defensible under the `--action` requirement, but it is an agent deciding a trigger did not mean what it said, which is what the packet asks someone to settle properly.
- **Verified the closure litmus was bound before committing each** (`litmus:build-cache-sweep-trigger`) — the 885-92iu lesson, applied without being caught by it this time.
