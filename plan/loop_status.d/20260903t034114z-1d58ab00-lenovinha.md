## Cycle — lenovinha (linux_immutable, linux-next), 976-suab

CLOSED 976-suab at 8eb159983. 653 requirements across 177 spec files: 0
identified before, 653 identified and unique after, in a diff of 653 added
lines and 0 removed where every added line is exactly a req-id comment.

THE PACKET WAS ESTIMATED AT 8 HOURS AND FIT IN ONE CYCLE, which is worth
recording so the next estimate is better rather than treated as a win: the
work is a generator, a validator, a fixture, one mechanical pass, and two
documentation edits. What made it tractable was that the operator had already
ruled the hard part — refinement keeps the id, changed obligation gets a
tombstone and a new one — so no design question was open.

BOTH FLAGGED TRAPS WERE REAL AND ARE PINNED BY TESTS.
Idempotence: proven on the real corpus (second run 0 new, 653 already had one,
0 files rewritten, byte-identical git diff) and hermetically, including that a
hand-written id survives and that appending ONE requirement stamps only that
one — the real-world path, since nobody re-stamps a corpus.
Tombstones: concrete, not theoretical. 30 of 177 files are tombstoned, 29 have
had their bodies stripped, and opencode-web-session STILL HOLDS 9
REQUIREMENTS. Exempting tombstones would have left exactly those 9
unidentifiable and the non-regression denominator would shrink as things
retire. Generator and validator both cover them; the fixture contains a
tombstoned spec with a live requirement so an optimisation that skips them
fails.

I FELL INTO A THIRD TRAP OF THE SAME FAMILY, in my own tooling, for the third
time in a day. The generator derived its root from BASH_SOURCE and cd'd there
unconditionally, so my "test" against a copied corpus stamped the LIVE specs
while reporting success against what I believed was a sandbox — and my
idempotence check then diffed the untouched copy against itself and printed
"IDEMPOTENT: byte-identical", which was vacuously true. A green measuring the
wrong thing. The outcome was harmless because stamping was the intent anyway;
the VERIFICATION was not, and I nearly recorded that vacuous control as proof.
I caught it only because a sample grep afterwards showed no id in the file I
thought I had just stamped. TILLANDSIAS_SPEC_ROOT exists because of it.

WHAT IS DELIBERATELY NOT ENFORCED, and is documented as judgement in
.claude/skills/openspec-sync-specs/SKILL.md where an author handles MODIFIED
and RENAMED requirements: whether an edit refines or replaces is about
MEANING. A changed obligation that kept its old id passes the gate exactly as
a correct refinement does, and the docs say the green tells you the field is
populated, never that you judged it right.

THE 0.10 WEIGHT is payable now and proximity.yaml says why at the line — the
precondition is ENFORCED, not asserted, since the validator runs in the gate.
I did not invent a scorer: nothing computes evidence_credit today, and paying
a weight and computing it are different acts.

metrics:
  gate: TILLANDSIAS_FORCE_CHECK=1 ./build.sh --check green, both new checks wired
  fixture: scripts/test-requirement-ids.sh 6/6
  corpus: 653 requirements, 653 unique ids, 0 duplicates, 0 missing
  diff: 137 files changed, 1056 insertions, 0 deletions
