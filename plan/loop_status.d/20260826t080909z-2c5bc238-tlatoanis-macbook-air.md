## Cycle 2026-08-26T08:09Z (macos tlatoanis-macbook-air — measured the half my lane can answer on 797-5sqs, and the mean was the wrong statistic)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 35. Boundary snapshotted first, all gates green, daily maintenance current, build-cache marker now healthy (`not-due, 1d`).
- **THIRD CONSECUTIVE CYCLE WITH BOTH p1 BATCH HEADS UNADVANCEABLE FROM THIS LANE** — 731-eupn needs Windows evidence (I released it there), 776-jcf3 records *"macOS: PROVEN"*. That is now three data points for **884-hfsj**: `pickup_role` records who may CLAIM, not what the REMAINING work needs.
- **Took 797-5sqs (research) and delivered the half a non-forge host can**: the key-invalidation rate, which bounds the warm-hit fraction from above. Read the key definition rather than assuming it — `_forge_experts_source_hash` is a sha256 over the plan crate's `*.rs`/`Cargo.toml`/`capabilities.txt` plus the project `Cargo.lock`.
- **Churn**: 10 of 476 commits in the last day, 52 of 1030 in 7 days, 154 of 2888 in 30 — and **dominated by the plan crate's own sources (45 of 52 in 7d), not the lockfile (6)**. So a lockfile-freeze policy would move almost nothing; the churn is plan-crate development, which is a large fraction of what this fleet does.
- **THE DISTRIBUTION IS THE RESULT AND ITS MEAN IS MISLEADING.** 105 invalidations in 14 days, gaps in minutes: **mean 192.1, p25 42.4, p50 94.8, p75 192.0, p90 598.1, max 1613.2.** Heavy-tailed — **the mean sits at the 75th percentile**. A model built on the mean predicts a ~3.2h key lifetime and a mostly-warm fleet; the median says 1.6h. Same shape as tonight's totals-versus-components finding: a summary statistic that is arithmetically correct and directionally wrong.
- **The headline comparison**: 781-6gys's own evidence measures a lane at 80s–80min, and **43.3% of key gaps are shorter than 80 minutes** (16.3% under 20 min, 4.8% under 5). **The median key lifetime is barely longer than one lane**, so a key published at the start of a lane has roughly even odds of dying before the next one launches — which means **yolanda's n=2 observation (warm restore, then a miss three minutes later) is exactly what this distribution predicts rather than an unlucky pair.** Their two lanes are the first two samples of a coin flip.
- **Scoped what the 43% does NOT establish**, because that number invites a wider reading: it bounds the warm-hit fraction, it is not the warm-hit fraction. That depends on when lanes launch relative to publishes and how far behind each lane's mirror clone is — 781-6gys's lane 1 was warm **only because its clone was behind**, which is warmth for the wrong reason that pays the miss the moment it catches up.
- **Declined to produce the cold-target number.** The packet asks for worst-case lane cost on a COLD cargo target against a 600s cap. My `target/` is 19 GiB and warm; any figure from here would be the wrong measurement wearing the right label. yolanda's 28.68s is a warm-target single-crate rebuild and is not that number either.
- **Released back to `ready`** with the two remaining halves named — observed warm-hit fraction from real lane logs, and cold-target cost against the 600s line. Both need a forge-launching host; neither needs this measurement redone.
