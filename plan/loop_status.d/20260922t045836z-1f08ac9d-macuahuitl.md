## Cycle macuahuitl-fedora 2026-09-21T22:30Z — coordinator handoff since 20:00Z (dd255e4c1): the unified spec, the design audit falsified in part, a p0 sidecar fix, the keyring crash root-caused on two hosts, and the landings handed to yoga while macuahuitl cannot push

**Lead — origin now:** linux-next 3e800f7cf at writing (plan-only pushes since land73 db6865b22); windows-next levelled to db6865b22. macuahuitl CANNOT PUSH: gnome-keyring-daemon crashed at 21:48:03Z (SIGABRT, `gkd_secret_service_get_pkcs11_session: assertion 'client' failed` → `invoke_get_property_in_idle_cb` assertion), the login collection came back locked, land74 refused auth-failed with its stamp kept, and the operator is away for hours. Reads and verification by ancestry continue from here.

**Operator decisions this segment:** the local checkouts are removed (confirmed directly) and the unified spec was routed to the coordinator: openspec/changes/cloud-only-project-lifecycle is on trunk at 236384daf (no host checkout reaches a forge; the tray lists only the remote repositories idempotently, paged; the mirror is the only source and sink; the uncommitted-edit affordance becomes a ref the mirror carries and the forge seeds from; the mirror's exported heads fast-forward from upstream on demand and on a cadence with a readable sync state; the relay logs the pusher). Two readings in its proposal await the operator's veto: retiring the opt-in TILLANDSIAS_FORGE_HOST_MOUNT live-edit path, and retiring the mirror-to-host working-copy sync and the ~/src scanner. The pirria token seed waits on the operator (they are away).

**Landed (proven by ancestry):** land70 236384daf (the spec); land71 dfa35c605 (PR #134 lenovinha's p0 sidecar fix — one agent identity leaked per renewal, the lane died ~2 h after boot — and the design's A1 STRUCK); land73 db6865b22 (pirria's stable-channel one-shot smoke PASS — the promotion fully proven — the two real-host arms of 1312-i6da, the runbook's image-recreation check scoped to localhost/, the tray-ux overflow-label sentence). Plan lane from here: the 1340-tzsx audit event (67a55f09b) and its A1 correction (3a5237aa4), esme's 1338-x5rq, pirria's and lenovinha's fragments, yoga's acceptance record. land72 was killed before landing (PR #136's first head carried a compile error behind the tray feature; fixed and verified by pirria); land74 (PR #136's fixed tip) refused on auth.

**The design audit (1340-tzsx):** accepted with seven amendments; A1 (the forge's known_hosts line against a port-2222 lookup) was FALSIFIED on a live forge — `ssh-keygen -F` on two hosts was one instrument twice; the client verifies the bare line fine — struck in both sections of the document and corrected on the row; the same transcript found the real forge-client defect (1342-r4pv). Lenovinha closed 1340-tzsx and 1313-prin; the T11 flip waits on a forge acceptance and on 1342-r4pv's arm 2 (cliff reading 23:14:43Z at the real 1200 s cadence).

**Keyring crash (1347-r9g8, p0, lenovinha):** the same assertion and frames on macuahuitl (21:48:03Z) and lenovinha (21:42:03Z), both mid-gate; a Secret Service call whose client vanished mid-read. Discriminator across four hosts: the two that crashed ran gh calls under a timeout wrapper during gates (my PR watch here; the credential guard's caps on lenovinha); pirria (one daemon pid for three days, zero coredumps) and yoga never wrapped one. Arm A: no fleet tool kills a gh call mid-flight against a serving keyring (a probe redesign — 988-7kxf forbids simply deleting the caps); arm B: the mechanism, reproducible only from an unlocked host with its owner's per-run consent; locked-collection runs refuse as evidence. My PR watch now polls the public API unauthenticated.

**Fallback topology (in force until macuahuitl is unlocked):** yoga lands — the queue for #136 (pirria's tray fixes at its tip) and #137 (yoga's correction of the stale exit-0 warning), then six PR-less refs after reviewing each (the forge's 9e0b57418 status-loss scanner, d051a120c and f64b1a442 gate-binding assertions, 02f139603 host resolver; lenovinha's 1346-cdwt at 4466ba733, frozen by lenovinha until yoga says the batch is done); yoga's git pushes run off a file store, so a keyring crash there costs only gh; pirria holds the PR-queue fallback (their gh works); lenovinha the PR-less fallback (their gh is 401). Landing tools wrap no gh call in a timeout (checked).

**Rows moved:** 1338-i23k RETRACTED by yoga (the exit-0 reading was tail's status through the harness; rc=1 on three hosts) — closes obsoleted from yoga's host after #137 lands; 1333-jpq5's headline 24 corrected to 2 by pirria (a head -1 on multi-binding lookups) and re-scoped by amendment; 1312-i6da's closure split by instrument (hermetic fixture + a real-host smoke record; lenovinha restates, pirria's record is the smoke half); 1339-had5 worse than filed (two release-gate steps could not fail at all: structured assertions adjudicate alone, expected_behavior is documentation) — fixed per step by the forge; 1334-d8bh refuted by yolanda's own paired run; 1339-r9xv filed p1 on operator direction (install-windows.ps1 never writes .wslconfig; a 16-core host performs worse than a 4-core out of the box); 765-xpct's owed number delivered (4.8–5.0 s per code-only cycle, after the first build measured zero).

**What is LEFT — for whoever picks this up:**
1. When macuahuitl is unlocked: if #136 landed via yoga, `git reset --hard origin/linux-next` here (the local land74 relay commit is then redundant); otherwise relaunch land74 (its stamp is valid). Resume relays; level windows-next; push this handoff.
2. Verify yoga's landings by ancestry and tell the authors the merge shas ON GITHUB (a forge's mirror lags).
3. The cloud-only change's tasks: note A3 in ordinary use (lenovinha's plan push to linux-next through the lane REJECTed on a stale old-id with the mirror four behind) as the second argument for T1 before any flip.
4. Operator asks, plain: unlock the keyring on macuahuitl (the login prompt); seed pirria's GitHub token (`tillandsias --github-login --with-token`); decide cosign (1324-ujvb); veto or accept the two readings in the cloud-only proposal. Nothing else is blocked on the operator.
5. Unrouted ready rows from tonight: 1326-w9t9, 1327-r4bx, 1329-er33, 1330-bb87, 1330-j5is, 1335-2nzf, 1336-k4ih, 1336-p84n, 1337-sj62, 1337-7jr5, 1338-tkfh (page half landed), 1338-x5rq, 1339-r9xv, 1343-ujjc, 1344-q6uc, 1345-8hyg (p1, the night's main output), 591-x7ws (after T1/T4).

**Sub-agent spend (attested):** none spawned this segment; session cumulative subagent_tokens=537835 agents=7 (sonnet).

**Addendum 22:40Z — the landing queue drains ONE candidate per run (1316-bnzt regression, yoga):** `scripts/land-queue.sh` read its candidate list from stdin and `./build.sh --check` consumes stdin, so `--limit N` landed one PR and printed `ok:`; every queue run from macuahuitl tonight was `--limit 1`, where this is invisible. The fixture's thirteen arms were green because every stub gate was `exit 0`, which consumes nothing. Fix on work/1316-bnzt-stdin (the list read on fd 3, the gate fed /dev/null, arm 9 with a hungry stub on the list's fd) — yoga lands it by their own tool once arm 9 reds under mutation, then re-runs the drain. Until it lands, run the queue with `--limit 1` and re-run per PR, or use the relay lane.

**Addendum 23:20Z:** 1342-r4pv is CLOSED at the true cadence (lenovinha, real forge, 23:16Z: seven renewals past MaxAuthTries=6, two agent identities at n=3 and n=8, the forced command's authorisation refusal where the pre-fix code died at authentication; the pre-fix code would hold nine identities and have been dead ~5 h) — it no longer blocks the T11 flip, which still waits on a forge acceptance with A2–A4. 1347-r9g8: pirria is the separating observation (keyring-routed git like lenovinha's, no wrapped gh, no crash in 3d04h, zero keyring coredumps against 46 total as the positive control) — keyring-routed git does not track the crash; wrapped-gh-in-gates does on all four hosts, still confounded with gate weight; lenovinha retracted the competing hypothesis. Arm A (the credential guard's probe redesign — Secret Service read first, never start gh against a non-serving collection, gh bounded only as a fallback, one emitter per verdict; the guard today answers two verdicts to one state through five emitters) is pirria's, lenovinha reviewing. For yoga's PR-less batch lenovinha's ref is now work/1342-r4pv-arms at 4aece51a9 (a superset of 1346-cdwt at 4466ba733). Rows moved since 22:30Z: 1342-r4pv closed; 1347-r9g8 amended (consent rule, discriminator, retraction); 1333-jpq5 amended (24→2).

**Addendum 00:10Z:** the queue fix is at work/1316-bnzt-stdin 26dcd4960 (the list read on fd 3, the gate fed /dev/null, arm 9 bounded at 120 s and proven both ways on the third attempt), not yet landed; yoga is draining #136/#137 with the fixed copy meanwhile and opens a PR for the fix. PR-less batch split: lenovinha reviews and lands the forge's four over their lane (pre-fix control for the status-loss fixture; assertions read; the resolver's arms run); yoga keeps lenovinha's own ref because an author must not land their own work unreviewed.

**Addendum 00:45Z:** yoga's fixed queue landed #137 at 66dfa91bf (the first queue landing from a peer host; verified here by ancestry); yoga levelled windows-next to 66dfa91bf and closed 1338-i23k obsoleted at 92a41181d citing three hosts, 4878066bd dropped. #136 was EVICTED on a gate failure the tree does not reproduce (base alone, base+candidate and trunk all 6/6 by hand) — the queue evicts without a base control, a two-outcome compare over a three-outcome world; the best reading is a transient fold state during a four-fragment burst; the eviction comment is retracted on the PR itself; #136 is re-queued unchanged and #138 (the queue's stdin fix) is ready; the next drain takes both, then the forge batch with lenovinha's four verdicts. A second 1316-bnzt regression row (re-run the gate on the base before evicting) is yoga's to file.

**Addendum 02:10Z:** yoga overrode the review split to ONE WRITER (lenovinha reviews only; yoga pushes) — correct, the race 1316-bnzt exists to delete. Lenovinha's four verdicts all HOLD (status-loss: pre-fix control run on their host; splitter: clean; bindings: the expected count is a prefix-only assertion, noted; host resolver: a relative-invocation regression that fails closed — yoga adds the one-line fix and a red-then-green arm in the same batch). 1348-gkvi p1: lenovinha's lane exited after ~3 h and removed the mirror, sidecar and forge against the skill's own sentence; restored. The UTC day rolled to 2026-09-22: daily maintenance and the cut are the coordinator's once macuahuitl is unlocked.

**Addendum 02:15Z:** the second drain evicted #136 again, this time attributably: base alone passes, base+candidate fails with blocked:plan-ledger-incomplete because pirria's separating-observation fragment forward-references the 1347-r9g8 packet that lives on lenovinha's unlanded ref (documented in the fragment's own header) — a fourth eviction state, correct-but-ahead-of-its-dependency, which the ledger check names and the gate's exit status cannot; on eviction the queue should attach the ledger verdict. #138 was skipped by the work-ref grammar (a suffixed name) and yoga re-points it to work/1316-bnzt. Landing order: lenovinha's superset first, then the forge's four with the resolver fix and arm, then #138 and #136.

**Addendum 02:40Z:** yoga reviewed and landed lenovinha's outstanding work at c3c65a7cb (1346-cdwt's hermetic-fixture split, the 1342-r4pv closure at the true cadence, the 1347-r9g8 row and its amendments including the unscoreable declaration) — verified here by ancestry. That puts the 1347-r9g8 packet on trunk and unblocks #136. Remaining in yoga's order: the forge's four with the resolver fix and its arm, then #138 (re-pointed to work/1316-bnzt) and #136.

**Addendum 03:43Z:** #136 landed through yoga's fixed queue at 7715cf239 (pirria's tray fixes at their flat-by-default tip; 591-33s6, 997-e4v2's scaffolding slice and 1334-gv33 close on it). Remaining in the sequence: #139 (the queue's stdin fix) and the forge's four with the resolver fix. The fleet is quiesced on plan-lane pushes to linux-next until yoga signals; the coordinator's staged pushes (land76 with the operator's cloud-only decisions, the tray-ux flat delta and the join-the-fleet asymmetry line; 1349-cjvu; the 1324-ujvb and 591-x7ws decision events; this handoff) follow the lift.

2026-09-22T04:39Z addendum: yoga's sequence landed at 95f48988d (eight landings verified by ancestry, row-boundary fixture 5/5 on trunk); windows-next levelled to 95f48988d; quiesce lifted by name. Plan lane 2ff7a0e27 carried 1349-tdpg (census, unscoreable stated) and the 1324-ujvb/591-x7ws decisions. land77 in flight: cloud-only docs + PR #140 + PR #141 (queue skips both as not-a-work-ref; refs must be work/<order>). esme filed the ancestry-vs-re-emit rule on 1336-k4ih. Next: this host's day gate, pre-build litmus on trunk (count ALL FAIL lines), the 2026-09-22 cut with a freeze declared by name.

2026-09-22T04:43Z assignment round: every answering host was idle after the lift (yoga, esme, yolanda, macneo, macbookair; lenovinha asked about T1, no answer yet). Routed: yoga T4 (self-filed 1350-8hmy, in_progress, 1252-znbn released to ready with the decision-first next_action); esme 1186-w3ph; yolanda 1339-r9xv (work/1339-r9xv, PR into linux-next); macneo: 1084-x8ya and 1132-r4mt both superseded by their own newest events (a release, two landed halves) while next_action still read as current, so macneo files that selector row, then verifies 1302-7j8p on the macOS regime, then 920-pxg6; macbookair 1254-47xd (PR #142 draft, five of six criteria, criterion 2 blocked by a non-NVIDIA GPU whose container reaches the render node: second regime routed to yoga after the land); pirria 1324-ujvb after arm A (PRs #140/#141 relayed in land77; the queue skipped both as not-a-work-ref and comments the reason in a real run). WSL2 memory: 1334-d8bh attribution refuted by yolanda on two hosts by two methods, measurements stand, one arm not two by esme's operator; two unknowns named. osx-next levelled by macbookair's merge 1fb702be2 (six plan-only commits ahead were content-identical); relay it into trunk with the next gated land. macneo runs the 1327-r4zb app-watch (828+ stamps): no reimage or reboot of macneo. Stale-ready candidates this pass: 1302-7j8p (2 citing commits), 1321-2ixp (1); handed to nobody yet, verify by execution. Loop-status entries lag messages fleet-wide (newest per host: yoga 09-19, pirria 09-18, lenovinha 09-14, yolanda 09-06): hosts coordinate by message and work refs now; the metrics audit reads that as silence.

2026-09-22T04:58Z land77 landed c1665ac62 (attempt 2, stamp adopted across plan-only trunk moves): PR #140 and #141 merged and verified by ancestry; cloud-only decision docs on trunk by content. PR #142 (macbookair, 1254-47xd) ready for the queue. lenovinha's parked 1345-8hyg correction relayed by content in this push. Still open for the operator: unlock lenovinha's login keyring; seed pirria's GitHub token; PR #132 runtime check on a clean Windows host.
