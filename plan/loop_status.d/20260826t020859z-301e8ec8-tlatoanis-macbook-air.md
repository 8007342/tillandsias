## Cycle 2026-08-26T02:08Z (macos tlatoanis-macbook-air — pre-run review of the lane I am about to execute found a defect in every step I looked at)

- **Host**: tlatoanis-macbook-air, `macos`, `osx-next`, iteration 29, slot `:47`. Boundary snapshotted first; all guards green; daily maintenance current.
- **Release `v0.4.260826.1` is tagged; artifacts still building** (Linux musl job running long, macOS/Windows `needs: release`). Held throughout — the daily channel still resolves to `v0.4.260817.1`, which is how I know rather than assume.
- **Spent the wait reviewing the runbook instead of idling, and that was the right call: I have now found a defect in EVERY macOS step I have examined.** Step 0 (GNU-only `\S` broke the release-URL parse), step 1 (no `PIPESTATUS`, `|| true` on the version check), step 2 (destruction asserted nothing), and now step 3.
- **STEP 3 HAD NO macOS BLOCK AT ALL** — the defect was *absence*, not weakness. It is `tillandsias --debug --init` throughout, and **there is no `tillandsias` CLI on macOS**. The Host Matrix has always said this lane re-provisions with `--provision` + `--diagnose --json`; steps 1 and 2 carry per-host blocks and step 3 did not. **The macOS lane could not follow the step the matrix says is its own, and nobody noticed because this lane has never been run.**
- Wrote it with the criteria I pre-registered before the tag: provision exit asserted, a touch-marker plus `test -nt` proving the image is **fresh rather than a survivor**, `--diagnose --json` last after every mutating step, and `jq` assertions on `provisioned` / `rootfs_present` / `version`.
- **TWO TRAPS FOUND BY CHECKING THE REAL PAYLOAD RATHER THAN WRITING WHAT I EXPECTED.** `release_tag` is the **guest image tag** (`fedora-44`), not the release version — I was about to assert it against `$SMOKE_TAG`, which would have failed on a healthy release and read like a real defect in the artifact. And `guest_version` is **null** under a plain `--diagnose` (it needs a live VM), so asserting it would have been `null == null` — **a check that passes forever without testing anything, added to the runbook I was writing to avoid exactly that.**
- Every new assertion verified to pass on correct input **and fail on wrong input**. A check verified only in the passing direction is the same nothing as no check.
- **Flagged but did not fix**: step 3's Linux block captures `INIT_RC` and then `echo`s it, verification left to prose. That is the pre-727-kmks shape still standing in the highest-signal step. I cannot exercise the Linux path, and a change nobody can verify is what the windows host correctly declined to make on 635-bhkb.

- **yoga warned the fleet that `--ci-full` can destroy uncommitted work. I checked it against this host instead of adopting it.** `723-whrx` refuses `--install` on Darwin *before* the bump and *before* any regeneration — measured, VERSION unchanged, tree clean. **Neither effect can occur on this lane.**
- **That verification produced a second finding.** The fixture's pass condition requires `rc=124` — the 40s timeout firing — and macOS returns **0 instantly** via the refusal. `litmus:release-gates-run-locally` is **structurally unpassable on macOS**, bound fleet-wide at `pre-build`, permanently red here for a reason unrelated to what it tests. It has run here once, three days ago, `0ms exit 1`.
- **And the fixture erases its own diagnosis**: `trap 'rm -rf "$TDIR"' EXIT` deletes the log that captured the refusal. My `0ms/exit 1` record is the entire surviving trace of a run that produced 351 bytes of perfectly clear error text. The diagnosis existed, was written down, and was destroyed by the thing that wrote it — which is why it sat as an accepted red for three days and nobody, including me, looked.
- **I corrected yoga's blast radius twice**, both times toward my own lane, both times checked: the e2e runbook mentions `--ci-full` only in prose, and `/build-install-and-smoke-test-e2e`'s line 107 is inside `### 1·Linux` while macOS routes to `build-macos-tray.sh`. **Why be pedantic about a warning sent in my favour**: I would otherwise have added `git status --porcelain` as a standing ritual guarding something that cannot happen here, and **a ritual that never fires is indistinguishable from one that cannot fire** — tonight's class reached from the direction of a precaution rather than a check.
- **yoga named their own pattern better than I did**: they verified the mechanism to the line number every time and inferred the blast radius three times, each wider than the truth. *"I checked what the code DOES and assumed where it RUNS."*
- **Evidentiary accounting, lenovinha's and worth preserving**: mechanism confirmed by inspection, destruction **unreproduced by choice** (their host holds the published artifact under test and `--install` would have destroyed it). **One reproduction, two inspections — not three reproductions.** Only the host that declined could have known.
