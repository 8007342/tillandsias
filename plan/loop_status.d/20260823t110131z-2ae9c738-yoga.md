## Cycle 2026-08-23T11:01:31Z (linux-immutable yoga — meta-orchestration full)

**Story (epic stable-milestone-v1, seed=yoga, route=rank:6/7, batch 10,
claimed 3, completed 3, commits 90fc4af66 + 46ba57418 + 3d3da7b7e):**
- 775-b4qz CLOSED — set-field's fragment body extracted to
  fragments::set_field_fragment_body (unit tests pin the production bytes:
  ": ", "#", leading "- ", quotes, multi-line, bare timestamp — all
  round-trip byte-identical through the fold); after every write set-field
  re-parses with the fold's parser and verifies the LWW value reads back,
  renaming a failing fragment to .yaml.rejected and exiting non-zero.
  Closes the exit-0-on-corruption class. Timestamp-shaped values quoted for
  the mirror Psych gate. 232 tests green; the three closure writes this
  cycle went through the new verified path.
- 627-c9c2 CLOSED — methodology/distributed-work.yaml gains
  concurrency_block (fragment-only writes; base admits compaction) and
  mirror_psych_gate (Psych 5 vs bare timestamps; base-touching pushes fail
  the mirror wholesale; compaction only on direct-push hosts);
  plan/index.d/README.md carries both rules.
- 660-z774 CLOSED — litmus:cycle-batch-triage-shape step 23 POOL FIDELITY:
  fixture ledger proves dep-blocked, ghost-dep and foreign-live-leased p0
  packets never fill batch slots, while the open blocker and an
  expired-lease packet stay claimable. Two-sided, PASS.

**Freshness audit (order 372): litmus:no-unprotected-plan-ledger-readers →
verdict UPDATED.** Audit found the suite red; four defects repaired, each
verified two-sided (46ba57418):
- check-plan-ledger-readers.sh comment exemption never matched grep -n's
  NN:-prefixed lines (latent since birth; first tripped by a ruby recipe
  COMMENT in check-arrival-routing.sh).
- with-tillandsias-builder.sh: `toolbox run` drops the caller's env, so on
  immutable hosts EVERY TILLANDSIAS_* flag silently died at the re-exec —
  FORCE_CHECK could not bypass the memo (memoization fixture case 12 caught
  it live), SKIP_VERSION_BUMP could not stop the bump. Namespace now
  forwarded; musl-gcc provisioned (ring's cc-rs needs it for the
  musl-static launcher; fresh toolboxes failed --install); init probe
  widened so existing toolboxes re-init.
- plan-binary-probe.sh: no-op cargo build never touches the binary, so the
  mtime vintage test refused a fresh instrument after git checkout
  refreshed byte-identical mtimes; a zero-exit build now records cargo's
  fingerprint verdict on the artifact it governs.
- test-set-field-yaml-shapes.sh: ruby absent host-side read as
  all-cases-invalid; now refuses loudly on its own token.
gate-stamp-memoization fixture ok:14/14; methodology-accountability suite
PASS.

**Cross-host trunk repair (3d3da7b7e):** lenovinha's 829-dkuc retraction
batch obsoleted five ARCHIVED split-parents; the fragments rode the
plan-only push lane and the next full gate went red fleet-wide
(fragment-status-loss: "NO SUCH PACKET in the fold" for closures that were
not lost — query's live map vs the archive-answering resolver).
check-fragment-status-loss.sh now re-asks the resolver before failing:
declared == resolved ⇒ archived-row supersession advisory; unknown pid
still fails hard (negative control verified). The cold/warm query
nondeterminism is filed:
plan/issues/research-query-fold-surface-flipped-on-unchanged-tree-2026-08-23.md.

**Environment repair:** post-reboot enclave: vault (SIGKILL at shutdown,
4d8h dead) restarted clean; proxy unstartable (tmpfs CA bind) — second-host
repro appended to
plan/issues/vm-proxy-ca-tmpfs-restart-fragility-2026-07-06.md (promotion
candidate, noted not promoted). orchestrate-enclave.sh argv defects filed
as 861-hisq. Ledger compaction run (25 fragments folded, capability-row
fragments correctly left unfolded).

**E2E:** curl-install e2e not due — latest-marked release v0.4.260810.1
already carries a Linux curl-smoke PASS (644-7w89); destructive reset
withheld from an actively serving substrate on that ground.

**Watch items:** archive-answerability harness hiccuped once (cargo emitted
no test-result line, rc=0; green on retry — intermittent with a schedule);
stranded sweep names 831-ezea (macuahuitl's live arrival-routing claim,
active today — not abandoned); litmus/fixture invocations of build.sh run
without SKIP_VERSION_BUMP and re-fire the six-file bump on a new day (twice
this cycle; reverted both times).

**Metrics (verbatim):**
experts: calls=3254 answered=3250 unsupported=4 degraded=0 errors=0 answer_rate=99% tools=answer,append-event,arrival-routing-check,blocking-counts,capability-matrix,check,closure-evidence-check,compact,dependencies-of,expert_capability,expire-claims,fragments,next,next-order,parked-blocks,plan_answer,plan_check,plan_query,query,ready,select-rows,set-field,status source=/tmp/forge-expert-usage.jsonl
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=3 per_server=cli=3230;forge-plan=24;project-info=23 legacy_untagged=0 health=ok source=/tmp/forge-expert-usage.jsonl
expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
flow: cycles=4 avg_completed_per_cycle=0.5 avg_commits_per_cycle=2.25 overhead_ratio=4.5 source=/tmp/tillandsias-cycle-flow.jsonl
timing: steps=1067 build_check_ms_avg=48397 litmus_ms_avg=85842 slowest=step:building-portable-launcher-musl-static-with-tray-support-for-ins:319404 source=/tmp/tillandsias-timing.jsonl
plan: packets=536 ready=381 plan_bin=./target/release/tillandsias-plan
repo: commits_this_cycle=29 worktree=clean traces=unknown
verdict: ok:nothing-flagged
