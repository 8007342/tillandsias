## Cycle 2026-08-18T19:23 PDT→20:00 PDT (windows — YOLANDA, MO FULL: overnight loop cycle 2, 828-k3mq second deliverable + 830-t4qp filed)

**Result: `no-credential` stopped meaning two things with opposite remedies, proven by a falsifiable pair rather than asserted.**

- **Host**: YOLANDA, `windows-next` 8dcf50498 → f8ae44fbf (claim) → this cycle. Preflight `ok:cycle-preflight:rebuilt+expert-absent+wsl-ok+services-no-podman:skip:unsupported-host`. Guards: credential `ok:gh-keyring`, branch `ok:branch-windows-next`, expert-base `ok:expert-base-ready`, opsx `ok:clean-tree`. Boundary `/tmp/meta-orchestration-boundary.mfxPA6`. Sibling heads: main=0bba6525f, linux-next=6de002620 (already merged), windows-next=8dcf50498, osx-next=40004a25a.
- **CLAIMED FIRST THIS TIME.** Cycle 1 predated linux's claiming protocol arriving in-tree; this cycle pushed `claim(828-k3mq): yolanda` (f8ae44fbf) BEFORE any work and released back to `ready` at exit. Note for the fleet: the runbook's `--host "$(hostname -s)"` is not portable — MSYS git-bash rejects `hostname -s` ("unknown option -- s"), which is order 743-mgf3's finding resurfacing in new text. Used the normalized node name (`yolanda`) instead.

**828-k3mq SECOND DELIVERABLE.** The probe tested three conditions in one `if` — vault-cli absent, client-token sink unreadable, GitHub secret unreadable — and published `no-credential` for all three. The first two mean the mirror cannot authenticate to Vault at all, so the GitHub token's state is UNKNOWN and GitHub Login is the wrong remedy; the third is exactly what GitHub Login repairs. New `agent-unauthenticated` state, ordered before the GitHub check, with a real `lookup-self` as the middle rung so an EXPIRED-but-present token is caught — which was the actual 2026-08-18 shape. The guard gained the matching arm with the opposite instruction spelled out.

**THE KNOWLEDGE EXISTED ONE LAYER DOWN AND NEVER REACHED THE REF.** `relay-refs.sh` has discriminated on `lookup-self` since order 424 and says "do NOT run GitHub Login" in its log; `litmus:git-mirror-relay-token-expiry` pins that behaviour and passes. The published verdict the forge actually consumes did not carry the distinction. The two layers now agree.

**Verified by a PAIR, both green** — step 4/10 (no vault client → `agent-unauthenticated`) and step 5/10 (working vault-cli that authenticates but holds no github token → `no-credential`). The second is the control: without it, collapsing everything back into `agent-unauthenticated` would pass the first. The pre-existing fixture that asserted `no-credential` for the no-vault-cli case was UPDATED rather than deleted, because that case's meaning genuinely changed.

**830-t4qp FILED** — the git-mirror-service pre-build suite is 14/20 on this host and all six failures are pre-existing windows host-environment failures: three fixtures commit without exporting `GIT_AUTHOR_*`/`GIT_COMMITTER_*` (a pattern `test-git-mirror-relay-verified-ack.sh` already gets right), one CRLF case, one blocked by the native-windows `E0425`, and one guard fixture under a restricted PATH. The last one touches the file this cycle edited, so it was **attributed rather than assumed**: the same fixture run against `git show HEAD:` and against the edited file both returned `rc=1 out=missing:no-credential-channel`, identical. Not a regression.

**NOT DONE, carried:**
- **828-k3mq released to `ready`, deliberately not completed.** Both deliverables are in the tree, but the packet's stated closure asks for a drain-level integration test (mint → leave container running → run the drain → assert the accessor still authenticates). What exists is narrower: unit tests over the classification and litmus controls over the verdict split. Claiming completion on a closure that was not met is the one thing the evidence discipline exists to prevent.
- Structural residual, unchanged: a 48h SecretID affords exactly ONE re-auth against a 24h token max_ttl, so a mirror reused past 48h still dies.
- Queue `829-p7wq` entries 3-7 untouched: 805-yzhw (the operator's OpenCode hang), 823-u5zf, 827-h4tz, ledger compaction, overflow.
