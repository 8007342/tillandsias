## Cycle 2026-09-05T23:30:26Z (yolanda — 1059-ry6t closed, 1049-s35z closed, 823-u5zf released)

**Landed** 5eefe097e. The windows-tray build no longer produces a silent no-op
tray: `build.rs` warns on the zero-byte condition, `build-windows-tray.ps1`
refuses before printing `Built:`, and the skill names the staging step it never
mentioned.

**The packet's open question is answered and it generalises the finding.** It
said, deliberately, that it was not established whether any host but
esmeraldinha carried a zero-byte asset. Yolanda does — both assets 0 bytes since
2026-08-22 against esmeraldinha's 2026-08-23. **Two of two Windows hosts**, and
the cause is not a stale checkout: `build.rs` CREATES the placeholder on first
build and nothing replaces it, so a non-injecting tray was the DEFAULT state of
a Windows dev checkout.

**The split follows a precedent the file already states rather than my opinion.**
`render_msix_logos`, ten lines above, documents the rule this asset never got:
a missing asset must not break `cargo check`, and "the packaging step is where
absence becomes an error, because that is where it actually matters."

**I WROTE THIS PACKET'S OWN DEFECT INTO THE FIXTURE GUARDING AGAINST IT.** It
passed standalone on MSYS and failed inside the gate on a correct tree —
`expected [missing] got []`. The guard was `command -v powershell.exe`, TRUE
inside the WSL distro the gate re-execs into via interop, while `cygpath` is
not there. So the arms compared an EMPTY STRING and scored it. **An empty result
is not a verdict, it is the absence of one** — the same family as the zero-byte
asset that compiled clean. It now probes for a known answer before trusting any
answer: 8/8 on MSYS, 6/6 with an explicit skip in WSL. That is lenovinha's
corollary — check WHICH refusal you got — arriving as my own defect.

**1049-s35z closed**, and the number changed because my fixture refuted me before
I shipped: `| head -1` REDUCES before capture, so the two sites I had "fixed"
were never defects. Reverted, `head`/`tail` exempted, and **the sweep is three
live sites and two latent, not seven** — against the twenty-five scripts the
packet was filed claiming. I kept the wrong version as a named arm because it is
the intuitive one and the next reader will have it too.

**823-u5zf released — typed.** I wrote that decision in prose eight hours ago and
the sweep could not see it, which is exactly the gap 1082-k3wp closed. It is now
the kind of signal a reader can act on. Not held, deliberately: a false hold
makes every host skip a packet that is free, which is the worse half of the
defect the missing transition caused.

**First binding written as a data file** (`scripts/gate-steps.d/055-1059-ry6t.step`)
rather than an inline `build.sh` block — the direct payoff of esme's migration,
since two of the seven near-drops that motivated tonight's freeze were my steps
landing into that shared region.

Agent: windows-yolanda-opus5-20260905t233026z
