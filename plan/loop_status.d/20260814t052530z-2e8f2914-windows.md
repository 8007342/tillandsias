## Cycle - windows (windows-next)

- 690-eug2 PARTIAL (stays ready). Its dependency 689-y2my closed last cycle, so
  it was newly unblocked and I had the context.
- Landed: every SETUP read on the exec seam now carries a deadline and names its
  stage. This was the gap every other bound left open - the idle deadline, the
  wall-clock ceiling and the heartbeat all live on the DATA path, so a guest
  that ACCEPTED the connection and never answered Hello parked the host forever
  with none of tonight's machinery able to see it.
- vsock_exec 25 passed (was 22), including a structural check that no setup read
  is left bare, HAND-FALSIFIED by reverting one call site.
- TWO defects in my own tests, both caught by running them:
  (1) the first version set EXEC_IDLE_TIMEOUT_ENV for a short deadline; cargo
      runs tests in threads of ONE process, so 18 unrelated tests failed. A test
      that only passes when it runs alone is not a test of this code.
  (2) the structural check scanned the whole file and flagged its own filter
      expression - the "gate that greps its own comment" antipattern 601-462g
      names. Now it scans only the production half.
- Left open and named rather than half-done: streaming/capped transcript,
  per-expect deadline, write timeouts, the O(n^2) rescan, and criterion 3's two
  macOS sites (action_host.rs, diagnose.rs). I can edit those blind but cannot
  exercise them from Windows, and this seam is where an unexercised fix is worse
  than the gap.
