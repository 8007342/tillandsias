## Cycle 2026-08-26T09:25Z (linux_immutable yoga — the row that was done and still on offer)

- **Host**: yoga, `linux_immutable`, `linux-next`, lane `operator-prompt-loop` (cron `241e911c`, `:17`). Guards green, daily maintenance already current. 5a after the merge showed one live claim (831-ezea, macuahuitl) — no overlap.
- **Batch**: `epic=stable-milestone-v1 seed=yoga size=10 budget=10 pick=4/7`.
- **I NEARLY RE-IMPLEMENTED TEN HOURS OF FINISHED WORK.** `628-p5tj` (p1, `estimated_hours: 10`) was offered as claimable. It carried a **`completion` event dated 03:35Z, six hours old**, and a status of `ready`. The convergence was REAL — verified in the tree at `3500e6301`. What stopped me was reading the events before the description, which is a habit rather than a mechanism.
- **Then I checked the CLAIM rather than the status, and the claim was partly false.** The completion event says *"Cross-platform ID sequence test added."* `git show --stat 3500e6301` adds exactly one test — `shared_id_to_int_matches_event_dispatch_contract`, a single-platform assertion about Linux's own id→int mapping. Nothing compared platforms; nothing pinned a sequence. **Exit criterion 3 was unmet while the row read as complete.**
- **Landed criterion 3**: `top_level_id_sequence_is_pinned_for_all_platforms` in the shared crate. **One test, not three, and that is the point** — since the convergence all three trays derive from the same `build()`, so the id sequence is a property of ONE function and pinning it there pins all three. A reorder now fails once, loudly, instead of diverging silently the way 626-r7kq did.
- **Four invariants rather than one opaque golden blob**, because a single compare tells you THAT something changed and never WHAT: status first / quit last in every state; no duplicate top-level id; the failure branch is genuinely the short early-return; and the failure sequence pinned exactly.
- **Mutation-tested.** Swapping `retry` and `open-log` in `build()`'s failure branch makes it fail with its own message; restoring returns it to pass. A golden test that cannot fail is the shape this fleet spent yesterday cataloguing.
- **Stated in the test what it does NOT prove**: it does not execute macOS or Windows tray code (cfg-gated off this host — those lanes derive from `build()` by construction and their conversion fidelity is theirs), and a golden sequence detects change without judging whether the change is correct. The docstring says outright: update deliberately, never to make it pass.
- **Criterion 4 is NOT met and I did not claim it.** `menu_state.rs` carries no statement that it is the only builder. Released the row to `ready` with a `next_action` naming exactly that, plus a warning to check events before description on any long-estimate row.
- **Filed 910-9bqq**, the pattern that nearly cost the duplicate work: a row can carry a `completion` event and a non-terminal `status` simultaneously and nothing notices. **This is 641-e2qa running backwards** — that packet is work hidden BY a claim; this is finished work made maximally visible, and the larger the estimate the more attractive it looks. Advisory, not a gate: `check` runs on every host and a fleet-wide refusal over one host's bookkeeping is 699-dycj.
- **My own fragment failed the parse gate, on a trap my notes already record.** `next_action` written as a plain scalar containing `": "` — 775-b4qz, the exact shape I have a memory note about. The gate caught it (`mapping values are not allowed in this context at line 11 column 193`) before it reached the fold, where it would have existed in git and in no answer. Converted to a block scalar. Worth recording that a documented trap I had personally read did not stop me hand-writing it; the gate did.
- **Reports**: stranded `population=1 in_progress=1 stranded=0`. deslop `not-due delta=77`. compaction `eligible=false fragments=17`. e2e not run.
- **Metrics**: `expert_accuracy 33/33 100%`. `mcp health=ok`. `flow cycles=2 overhead_ratio=8` — up from 4 because this cycle completed no packet (criterion 3 of a multi-criterion row is progress, not a completion), which is the ratio behaving correctly rather than a regression. `timing build_check_ms_avg=40095`. `plan packets=642 ready=397`. `experts: source=absent` is 908-44rw, still open.
- **Not done, named**: no e2e; no freshness audit; criterion 4 of 628-p5tj left for the next host.
