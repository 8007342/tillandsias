## Cycle 2026-09-04T12:15Z-12:50Z — yolanda (windows), advance-work-from-plan

Boundary first on a clean tree; `linux-next` merged before the selector.
**803-49re fixed** — one of the two release blockers. windows-tray bins go
**107 passed / 1 failed → 108 passed / 0 failed**.

**Pre-fix result, stated because the criterion rule requires it:**
`cargo test -p tillandsias-windows-tray --bins` FAILS by construction before
this change. Recorded before the edit, not reconstructed after.

- **Nothing in the product was wrong.** The purge works. Assertion 3 grepped
  `install-windows.ps1` for two credential literals that `b22d09fd8` — Part A of
  this same packet — had deliberately moved into
  `scripts/clear-vault-host-credentials.ps1` so the two purge paths could not
  diverge. A **correct refactor turned its own pin red.**
- **Two stale assertions, not one.** Assertion 5 also required the installer to
  name `tillandsias-vm-uuid`; it has named it zero times since `b22d09fd8`.
  Nobody saw it because assertion 3 failed first and stopped the test. One
  unwatched crate hid two stale pins, and **a suite that halts at the first
  failure hides its own tail** — "fix the red" would have produced a second red
  immediately, on a crate nothing runs.
- **The pin now follows the code, not a file:** the helper must own both
  credential names, and *both* purge paths must dot-source it and call
  `Clear-TillandsiasVaultHostCredentials`. Both, because "there are TWO purge
  paths" is what made this a p1.

**Negative controls, both armed, both on the right assertion.** Dropping the
CALL from the second purge path → red at `main.rs:568`. Renaming a credential
inside the helper → red at `main.rs:547`. Both files restored and verified.

**What the pin does NOT prove, now in its own doc comment.** It proves the
wiring, not that a purge clears anything — every assertion is over source text.
This is the pair macbookair measured on 1006-jfwv the same hour: a qcow2
generator validated only against our own reader gave five green tests on a file
`qemu-img` refuses to open. **Agreement between two artifacts is not evidence
about reality, and it is harder to catch than an empty window** — a stale anchor
leaves a window you can open and find empty; mutual consistency leaves nothing
anomalous to look at.

**The disproved hypothesis, filed deliberately** (yoga's suggestion). I first
reported this red as "pre-existing, not mine, arrived via a linux-next merge",
on the evidence that it failed on a pristine tree after stashing. That evidence
cannot support that claim — it proves only the failure is not in my *working
tree*. The right instrument is `git log` on the introducing commit, which named
`b22d09fd8`, mine. macbookair and yoga each caught the same error in their own
claims the same day. Three hosts, one day, one shape: **a check consistent with
the hypothesis that cannot discriminate against it.**

**Measured for the 1004-vsh2 fallback on a real cmdkey:** `cmdkey /list:<target>`
echoes the target in its header even when the body is `* NONE *`, so
`-match [regex]::Escape($cred)` is true for every target and §2 throws on every
run. Esme's report confirmed, mechanism identified. **Caveat recorded so nobody
lands the obvious fix:** testing for absence of `* NONE *` is locale-dependent,
as is the header — an English-text predicate trades a bug that fails every run
for one that fails only on some operators' machines.

**Also corrected this cycle:** I had been quoting my cron (`45 */2`, local PDT)
as though it were UTC, and gave macbookair a fire time that existed in neither
frame. Fires are 11:45Z / 13:45Z / 15:45Z. And last cycle's metrics went to the
coordinator in a message rather than into `loop_status.d`, which the audit reads
— work done, evidence in the wrong place, indistinguishable from work not done.
Hence the block below.

### cycle-metrics (`--cycle-start 2026-09-04T12:15:07Z`), verbatim

```
experts: calls=0 answered=0 unsupported=0 degraded=0 errors=0 answer_rate=- tools=- source=absent
experts_substitution: unknown (needs the agent harness tool log; not derivable in-repo)
mcp: servers=0 plan-expert-calls=0 other-servers=uninstrumented-see-682-m8ek health=ok source=absent
expert_accuracy: pass=31 graded=33 total=33 rate=93% source=groundtruth-all-sets
flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent
timing: steps=8516 build_check_ms_avg=586139 build_check_mix=mixed:forced=124,memoised=11 litmus_ms_avg=3478 slowest=step:checking-that-added-fragments-declared-closures-resolve-885-92iu:490764 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
repeat: window=since=2026-09-04T12:15:07Z steps=117 top3=build-check=2,build-preamble=2,step:checking-a-check-that-could-not-run-never-claims-what-it-would-h=2 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
recur: window=7d runs=7757 steps=120 top3=build-check:runs=108:total_ms=67463148:avg_ms=624658:fail_pct=19,step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=87:total_ms=9860740:avg_ms=113341:fail_pct=0,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=87:total_ms=5957982:avg_ms=68482:fail_pct=0 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
skippable: candidates=49 floor_ms=2000 min_runs=5 top3=step:checking-the-plan-archiver-preserves-the-ready-set-831-ezea:runs=87:avg_ms=113341:fail_pct=0:saved_ms_upper=9747399,step:ratcheting-ghost-traces-for-the-gate-stamp-584-2qq2:runs=87:avg_ms=68482:fail_pct=0:saved_ms_upper=5889500,step:writing-the-gate-stamp:runs=87:avg_ms=57753:fail_pct=0:saved_ms_upper=4966839 source=/c/Users/bullo/claudia/tillandsias/.cache/metrics/tillandsias-timing.jsonl
plan: packets=686 ready=450 plan_bin=./target/release/tillandsias-plan.exe
repo: commits_this_cycle=- worktree=dirty traces=current
verdict: attention:worktree-dirty
```

`worktree-dirty` is this entry being written; `commits_this_cycle=-` because the
fix commit lands with it.
