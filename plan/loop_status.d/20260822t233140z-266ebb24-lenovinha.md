## Cycle 2026-08-22T23:31:40Z (linux-immutable lenovinha — meta-orchestration full; first fleet-rejoin cycle)

### SECOND LINUX HOST JOINED; THE CROSS-HOST CLAIM PROOF IS DONE
This is the rejoin the fleet section gates on ("second Linux, then macOS, then
Windows"). The one thing a single host could not prove — a row claimed THERE
being skipped HERE — is now measured: **831-ezea (claimed on macuahuitl, live
lease) is absent from `next linux` on this host** (213 eligible offered, zero
hits for the claimed row). Direction two exercised by protocol: this cycle's
three claims were pushed BEFORE work (49b1c66a2) and released on exit —
837-m47d to `completed`, 828-r2ek and 795-hzpg back to `ready` with sharpened
`next_action`. Selector separation used as instructed:
`--seed silverblue-ryzen7-amd+nvidia-lenovinha` (derive-host-identity) →
`epic=architecture-audit-epic size=8 budget=10 pick=1/3 score=23.101`.

### START-OF-DAY GATE: STAMPED, ONE POST-REBOOT FINDING
`due:no-marker` → body ran → `ok:daily-maintenance-stamped:2026-08-22`.
Post-restart checklist: MCP experts fresh (`source_commit == HEAD`), NTP
synced, images whole at installed v0.4.260817.1, hooks reinstalled, sidecar
staged-on-build. Vault restarted healthy after the reboot; the PROXY cannot be
`podman start`ed at all — its container definition bind-mounts
`/tmp/tillandsias-ca/intermediate.crt`, wiped by design at reboot, so the
preflight's own printed remedy is unsatisfiable for it. Routed as an event on
795-e5c7 (the CA-dir-by-construction row), not a new row.

### WORK DRAINED (architecture-audit-epic, 3 of 8 batch items advanced)
- **837-m47d COMPLETED — by verification, not re-implementation.** The
  deliverable already landed as 823-u5zf: `launch_spec` emits the verbatim
  vector, byte-pinned by tests. Evidence at e861f921b: host-shell `pty::` 28
  passed; `exec_allowlist` 7 passed (confirmed EXECUTED — the first filter
  invocation matched zero tests, the 831-wmn4 trap live). Four stale comments
  still describing the retired single-quoted interpolation were corrected in
  the same commit. Residual routed to parent 795-zshi: the tray path does not
  feature-detect CAP_EXEC_ARGV_VECTOR (an undischarged MUST from the epic's
  own sequencing note). 837-svzd got a verify-first note — its workarounds may
  already be deleted.
- **828-r2ek linux slice.** The packet's own ledger dispositions
  transport.rs/provisioning.rs writes as deliberately-unbounded TEST FAKES
  (the negative half of interop evidence); that disposition is now pinned
  IN SOURCE so a future bounds sweep cannot undo it. Only production
  remainder: the two `hvsocket.rs` writes, windows-lane by precedent —
  `next_action` carries the concrete helper-extraction plan.
- **795-hzpg: design decided + slice A landed.** The two prior scoping notes
  demanded a choice; the evidence made it: option 2 (block_on) PANICS today
  (status_bounded provably executes on tokio worker threads at four sites),
  option 1 cannot cover `impl Drop` and would demolish 714-4r6w's compile-time
  sync-budget type. Chosen: sync-native bounds. Slice A shipped at e861f921b:
  `read_capped()` + `MAX_CAPTURED_OUTPUT_BYTES` (8 MiB/stream), truncation
  reported never silent, guard grows `violation:unbounded-capture` with
  fixture case 5 (5/5), three unit tests pin boundary/drain/criterion-3.
  Slice B (parked deadline wait) is the carried `next_action`.

Untouched from the batch, deliberately: 795-zshi (parent, cross-platform
closure), 795-5itp (large refactor, slice 1 landed elsewhere), 335/245/251
(research/multi-cycle heavies — 245 is flagged for operator-guided sessions).

### LEDGER FOLD
`compact` folded 39 fragments (+1407/−6 lines; the 6 are field-update
replacements) and correctly REFUSED the two capability-block fragments —
the 843-624y guard observed working on its first live run from this host.
Integrity `ok: 513 packets` before and after.

### E2E DISPOSITION
Local-build lane: not applicable on immutable Linux (the skill's own
Silverblue note — do not believe `e2e-preflight`'s `eligible`). Curl-install
lane: DEFERRED — no release newer than last tested evidence (v0.4.260817.1
has 2026-08-17 PASS findings; GitHub's latest-marked release v0.4.260810.1 is
older), matching the 2026-07-10 windows precedent for the same condition.

### FRESHNESS AUDIT (standing class, seed 20260822)
`scripts/check-carry-forward.sh` → **refreshed** (stamped). One observation
routed as an event on 831-ezea: its promotion-bar "Reproduce:" comment steers
the next auditor into python3, which committed automation forbids.

### METRICS (verbatim)
- experts: calls=123 answered=122 unsupported=1 degraded=0 errors=0 answer_rate=99% source=/tmp/forge-expert-usage.jsonl
- mcp: servers=3 per_server=cli=120;forge-plan=3;project-info=1 legacy_untagged=0 health=ok source=/tmp/forge-expert-usage.jsonl
- expert_accuracy: pass=22 total=22 rate=100% source=groundtruth-rung1
- flow: cycles=0 avg_completed_per_cycle=- avg_commits_per_cycle=- overhead_ratio=- source=absent (first record emitted this cycle: cycle=lenovinha-20260822T235624Z claimed=3 completed=1 commits=5)
- timing: steps=111 build_check_ms_avg=65808 litmus_ms_avg=- slowest=step:running-clippy-strict-includes-the-workspace-type-check:35295 source=/tmp/tillandsias-timing.jsonl
- stranded-claim sweep: population=4 in_progress=4 stranded=3 threshold_events=0 (the 3 were this cycle's own fresh claims, since released/closed)
- deslop: ok:deslop-not-due:order=848 last=834 delta=14 hours=70 reason=under-threshold
- surface: ok:surface-exposed; guards: ok:gh-keyring, ok:branch-linux-next, ok:clean-tree
- triage: eligible=181 grouped=162 ungrouped=19 epics=11 urgency_unscored=20 caps_filtered=5
