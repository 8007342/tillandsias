## Cycle 2026-08-18T18:23 PDT→18:40 PDT (windows — YOLANDA, MO FULL: overnight loop cycle 1, 828-k3mq slice A landed)

**Result: the p0 that caused tonight's forge-write outage now has its fix in the tree, with a hermetic three-case gate on the decision it turns on.**

- **Host**: YOLANDA, `windows-next` 496ade239 → this cycle. Preflight `ok:cycle-preflight:rebuilt+expert-absent+wsl-ok+services-no-podman:skip:unsupported-host`. Guards: credential `ok:gh-keyring`, branch `ok:branch-windows-next`, expert-base `ok:expert-base-ready`, opsx `ok:clean-tree`. Boundary `/tmp/meta-orchestration-boundary.7NzzY2`. Sibling heads at start: main=0bba6525f, linux-next=459d7cb03→6de002620, windows-next=496ade239, osx-next=40004a25a.
- **First cycle of the operator-directed overnight loop** (hourly at :19, queue `829-p7wq`). Queue entry 1 claimed and advanced.

**828-k3mq SLICE A.** The credential drain now carries the same refcount the container teardown always had. `AppRoleAutoAuthRegistration` gained an owning container; `revoke_pending_container_tokens` keeps material whose container is still Running, keeps it LOUDLY when the owner's state is Unknown, and destroys only when the owner is Gone. The comment at `main.rs:120-128` — which asserted that an interactive lane's mirror "is stopped before the lane returns", and was the load-bearing justification for the unconditional destroy — now states the teardown is refcounted and names the 24h failure the gap produced.

**THE TRI-STATE IS THE WHOLE FIX, and reusing `container_running` would have rebuilt the bug.** That helper collapses every failure to `false`. Correct for "should I start this?"; here it means a transient `podman inspect` failure reads as "the container is gone" and the drain destroys a live mirror's credential — the same outage through a different door. `OwningContainerState` separates Gone (inspect answered, or said "no such container") from Unknown (inspect could not answer), and Unknown is treated as Running. Same leak-not-destroy rule `cleanup_shared_stack_if_no_running_forge` already applies to a failed listing.

**Gated, not asserted.** The decision was split into a pure `classify_owning_container_output` so it can be exercised without a podman daemon — a rule testable only against a live daemon is a rule nothing gates, which is how this class keeps shipping. Three tests green in the Linux builder, the middle one a deliberate control (without it "keep everything" passes while the fix becomes a blanket credential leak). `./build.sh --check` green, 264s of gate phases.

**NOT DONE, carried:**
- **828-k3mq remains open** — the `no-credential` verdict still cannot distinguish a dead Vault Agent token from an absent GitHub token, and the two have opposite operator remedies. Live 24h re-verification is a next-day observation, not cycle-scoped.
- **Residual by design, named so it is not rediscovered**: this bounds the outage at the SecretID's 48h TTL, not forever. `APPROLE_AGENT_SECRET_ID_TTL` (48h) affords exactly ONE re-auth against a 24h token max_ttl, so a mirror reused past 48h still dies. A durable answer needs re-mint-on-demand, or a mirror lifetime bounded to its credential.
- Queue `829-p7wq` entries 2-7 untouched: 828 slice B, 805-yzhw, 823-u5zf, 827-h4tz, ledger compaction, overflow.
- **Adjacent finding, unfiled as a packet**: `tillandsias-headless` does not compile natively on Windows — `enforce_ca_key_mode` is `#[cfg(unix)]` but called unconditionally at `main.rs:3219`. Invisible to `./build.sh --check`, which re-execs into the `tillandsias-build` WSL2 distro and compiles for Linux. Harmless in production (headless runs only in the guest) but it rules out native `cargo check` as a verification lane for that crate, which matters for 827-h4tz's remedy.
