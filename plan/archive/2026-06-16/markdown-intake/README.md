# Markdown Intake Archive - 2026-06-16

This directory preserves ad hoc Markdown moved out of noncanonical locations
during the repeat/orchestration cleanup.

Use `methodology/markdown-distillation.yaml` to find each original path and its
canonical destination. New durable claims should go to methodology YAML,
OpenSpec specs, plan issues/steps, or cheatsheets instead of restoring these
files to their old locations.
