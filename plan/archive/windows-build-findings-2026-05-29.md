# Windows tray build findings — 2026-05-29

Generated by `/build-windows-tray`. One section per build run. Sibling hosts
should grep `SECTION_KIND` to see whether the Windows build is green on a
given day; section bodies surface anything that affects cross-host work
(rare unattended regressions, install-script edge cases, smoke surprises).

trace: skills/build-windows-tray/SKILL.md (the skill that wrote this)
       plan/steps/windows-next-thin-tray.md (the windows tray v0.0.1 ledger)
       plan/issues/macos-build-findings-2026-05-29.md (sibling host's parallel)

---

### 20260530T040534Z — ok

- agent_id: windows-bullo-claude-opus-20260530T040534Z
- head_sha: 90355598 (post-FF of origin/linux-next; 53 commits absorbed,
  mostly internal litmus-coverage bumps + macos build-findings cron entries +
  one new macOS UX-gaps doc — none touched shared-contract crates)
- version: 0.2.260528.1 (workspace VERSION; same as macOS sibling header)
- build_run_id: 20260530T040534Z

**Build**:
- duration: 52.3 s wall-clock (cold cargo state in this session; cargo
  re-touched 5 crates — control-wire, vm-layer, scanner, host-shell,
  windows-tray — even though no source changed since this morning's 11:17 PDT
  fire, because the merge updated workspace metadata. Warm rebuild on the
  same FF base would be sub-second.)
- exe: target/release/tillandsias-tray.exe (6,326,272 bytes — bit-identical
  size to this morning's 11:30 install, confirming the merge-of-no-source
  produced no binary delta)

**Autonomous smoke** (post-install diagnose):
- `--diagnose --json` exit: 2 (degraded — wire unreachable because VM is not
  running; this is the expected steady state when the tray is not actively
  driving WSL. Matches the macOS sibling's exit 2 for the analogous
  `provisioned=false` reason. Exit 0 requires distro registered AND wire
  reachable AND phase=Ready; exit 1 is hard failure. Per
  `litmus:exit-code-provisioned-zero-degraded-two-symmetric`, this tri-state
  is pinned across both trays.)
- `--diagnose --json` keys present:
  [version, log_path, log_exists, wt_present, distro, distro_registered,
   release_tag, manifest_pin_x86_64_tar, wire, recent_log_tail] — all 10
  schema keys per `litmus:windows-tray-diagnose-cli-surface`. Schema
  unchanged across the merge (no contract bump on the shared modules).
- distro registered: true
- control wire reachable: false (phase=null, podman_ready=null,
  last_event=null — the `wire` sub-object collapses to nulls when wire
  unreachable, by design)
- release_tag: v0.2.260526.1 (matches `RECIPE_RELEASE_TAG` const, pinned by
  `litmus:recipe-release-tag-symmetric` cross-tray)
- manifest_pin_x86_64_tar: a28cabe7c9df (Fedora rootfs digest prefix; binary
  has the pinned manifest baked in via build.rs)
- wt_present: true (Windows Terminal detected — the menu Open Shell path
  will route through `wt.exe` per `litmus:windows-tray-architectural-invariants`
  invariant #6)
- log_exists: true

**Install**:
- target: %LOCALAPPDATA%\Programs\Tillandsias\tillandsias-tray.exe
- backup made: yes (prior install was 6,326,272 B from 2026-05-29 11:30:37
  local — i.e. this morning's daily build; replaced with 21:03:59 local
  rebuild from the post-merge tree)
- post-install diagnose schema match: yes — installed copy produces the same
  10-key JSON as the in-tree binary, confirming the install did not corrupt
  the bundle

**Findings** (free-form):
- Nothing regressed. This is the **first /build-windows-tray invocation
  using the new structured findings-file format** (skill was refactored
  earlier this tick to mirror macOS's `plan/issues/...-findings-YYYY-MM-DD.md`
  ledger pattern); this entry validates the format end-to-end before the
  scheduled cron fires tomorrow 11:17 AM PDT.
- Notable cross-tray observation: the `--diagnose --json` `version` field
  on **both** trays reports the crate's `Cargo.toml` `version = "0.1.0"`
  (sourced via `env!("CARGO_PKG_VERSION")` in `diagnose.rs`), NOT the
  workspace VERSION (`0.2.260528.1`). The ledger headers on both sides
  quote the workspace VERSION (read from the repo's `VERSION` file by the
  build skill) — so the ledger and the binary disagree on what "version"
  means. This is symmetric, not a Windows-specific bug, but a future
  cleanup item: either bump both crates' `Cargo.toml` `version =` on
  release, or have `diagnose.rs` read the workspace VERSION at build time.
- The merge-of-no-source still re-touched 5 crates' object files (~50s
  cargo build vs. expected sub-second). Likely cause: the merge updated
  workspace-level metadata (e.g. binding-coverage YAML) that cargo treats
  as a dependency input on first build after the merge. Not a regression;
  observed for the record.

**Cross-host visibility note**: The `--diagnose --json` `version` field
divergence (crate `0.1.0` vs workspace `0.2.260528.1`) is symmetric across
both trays. A future coordinated cleanup could (a) bump crate Cargo.toml
versions on release, or (b) inject the workspace VERSION into the diagnose
JSON via `build.rs`. Not blocking — flagged so the macOS host and the linux
integration loop are aware before any release-discipline tightening.

**Next iteration ask**: N/A (SECTION_KIND=ok). Next scheduled
`/build-windows-tray` fires Sat 2026-05-30 11:17 AM PDT per CronCreate
`8c5d0a16`.

---

### 20260530T053000Z — ok (cross-tray version-field cleanup landed, Windows side)

- agent_id: windows-bullo-claude-opus-20260530T053000Z
- head_sha: post-merge (74854a1d Merge linux-next: forge-hot-cold-split RAM
  gate + `--memory` ceiling helpers, +4 commits / +515 LOC into
  tillandsias-core, no shared-contract touches)
- version: 0.2.260528.1 (workspace VERSION)
- build_run_id: 20260530T053000Z
- prior section (this file): 20260530T040534Z — observed the divergence;
  this section acts on it

**Change made**: actually fix the cross-tray `--diagnose --json` `version`
field divergence noted earlier today (the "future cleanup item"), Windows
side. Pattern: read workspace VERSION in a `build.rs` and inject as a
`WORKSPACE_VERSION` env var that the diagnose surface uses, replacing the
crate-static `env!("CARGO_PKG_VERSION")`. Symmetric coordination ask filed
for macOS host to mirror (`plan/issues/tray-convergence-coordination.md`,
appended section ASK macOS host).

Files touched (Windows-owned only — did NOT touch macos-tray):
- `crates/tillandsias-windows-tray/build.rs` — added WORKSPACE_VERSION
  emission block at top of `main()`, BEFORE the windows-target gate, so
  cross-checks from non-Windows targets also have the env var available.
  Reads `../../VERSION` (workspace root, relative to crate manifest dir),
  trims, falls back to `CARGO_PKG_VERSION` if read fails (defensive — keeps
  cargo check green even if the workspace VERSION file is ever moved or
  the build is run from a stripped-down source tree).
- `crates/tillandsias-windows-tray/src/notify_icon.rs:1065` —
  `env!("CARGO_PKG_VERSION")` → `env!("WORKSPACE_VERSION")` in
  `DiagnoseReport.version`, with a 3-line comment pointing readers at
  build.rs.

**Build**:
- duration: 40.6 s wall-clock (cold cache after the FF merge re-touched
  workspace metadata)
- exe: target/release/tillandsias-tray.exe (6,326,272 bytes — bit-identical
  size to this morning's build; the version-string swap doesn't affect
  total exe size at the byte level because both strings are short
  `&'static str` literals)

**Smoke** (post-install diagnose on real hardware):
- `--diagnose --json` exit: 2 (degraded — wire unreachable, expected
  steady state; matches `litmus:exit-code-provisioned-zero-degraded-two-symmetric`)
- `--diagnose --json` `version` field: `0.2.260528.1` (was `0.1.0` before
  this commit) — matches the workspace VERSION file + ledger headers
- `--diagnose --json` `release_tag` field: `v0.2.260526.1` (unchanged —
  this is the rootfs RECIPE_RELEASE_TAG const, not the binary version)
- `--diagnose --json` `manifest_pin_x86_64_tar` field: `a28cabe7c9df`
  (unchanged)
- all 10 schema keys still present and at expected types
  (`litmus:windows-tray-diagnose-cli-surface`)

**Litmus**:
- `windows-native-tray` suite: 7/7 PASS (all 7 bound litmus tests,
  including `windows-tray-diagnose-cli-surface` — the schema pin doesn't
  pin the *content* of the version field, only its presence + `&'static
  str` type, so the swap is binding-clean)
- `cargo fmt -p tillandsias-windows-tray -- --check`: clean
- `cargo clippy -p tillandsias-windows-tray --release --no-deps -D warnings`:
  clean

**Findings** (free-form):
- The Windows-side bug observed in section 20260530T040534Z is now resolved
  on the Windows side. The fix is contained to the windows-tray crate and
  does not touch any shared crate, so it cannot regress macOS or Linux.
- macOS-side mirror is the natural next step (asked in
  `plan/issues/tray-convergence-coordination.md`) — once the macOS host
  picks it up at its next `/build-macos-tray` fire, both `--diagnose --json`
  surfaces will report the workspace VERSION instead of the crate-static
  `0.1.0`, and the cross-tray symmetry on this field will hold for real.
- Until macOS mirrors: there is a transient asymmetry — Windows reports
  workspace VERSION, macOS still reports crate-static `0.1.0`. This is a
  one-way fix (Windows is more correct), not a contract regression. No
  litmus test pins the content of `version` (only its presence + type),
  so the symmetric-pin litmus tests remain green on both sides.

**Cross-host visibility note**: see the ASK macOS host section appended
to `plan/issues/tray-convergence-coordination.md` for the exact mirror
shape (new `build.rs` in `crates/tillandsias-macos-tray/`, swap in
`crates/tillandsias-macos-tray/src/diagnose.rs:120`). Linux host: no
action needed; the workspace VERSION is already what `release.yml`
quotes for artifact filenames + nothing on Linux reads `CARGO_PKG_VERSION`
for user-visible surfaces.

**Next iteration ask**: macOS host mirrors the fix at next
`/build-macos-tray`. Tomorrow's `/build-windows-tray` cron fire
(`8c5d0a16`, 11:17 AM PDT) will produce a new section in this file
(or in tomorrow's date file) re-validating the fix is sticky.

