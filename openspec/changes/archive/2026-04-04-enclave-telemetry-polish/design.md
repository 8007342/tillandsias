## Context

All enclave containers are implemented. This phase ensures telemetry events are properly emitted and documentation is complete.

## Goals / Non-Goals

**Goals:**
- Verify all `--log-proxy`, `--log-enclave`, `--log-git` events are emitted at the right points
- Update CLAUDE.md with enclave architecture
- Final @trace annotation sweep
- Archive all 5 OpenSpec changes

**Non-Goals:**
- Anomaly detection (future)
- Telemetry dashboard (future — Landscape view)
