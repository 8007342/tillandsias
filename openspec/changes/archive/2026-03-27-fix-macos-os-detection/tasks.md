# Tasks

- [x] 1. Add macOS detection branch to `detect_host_os()` in `crates/tillandsias-core/src/config.rs`
- [x] 2. Run `cargo test --workspace` — all tests pass
- [x] 3. Verify no regressions in Linux `/etc/os-release` path
