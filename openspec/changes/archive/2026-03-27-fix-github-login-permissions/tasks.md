# Tasks

- [x] 1. Change git config mount from `:ro` to `:rw` in `src-tauri/src/github.rs` (2 locations: clone args and `build_gh_run_args`)
- [x] 2. Change git config mount from `:ro` to `:rw` in `src-tauri/src/runner.rs`
- [x] 3. Change git config mount from `:ro` to `:rw` in `src-tauri/src/handlers.rs` (3 locations: build_run_args, single-project shell, multi-project shell)
- [x] 4. Change git config mount from `:ro` to `:rw` in `gh-auth-login.sh` (auth login command)
- [x] 5. Run tests — all pass
