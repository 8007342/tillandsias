## Tasks

- [ ] 1. Change `NIX_IMAGE="docker.io/nixos/nix:latest"` to `NIX_IMAGE="docker.io/nixos/nix:2.34.4"` in `scripts/build-image.sh`
- [ ] 2. Add comment: `# Pinned for reproducibility. Update: podman pull docker.io/nixos/nix:<new-version>`
