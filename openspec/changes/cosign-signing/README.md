# cosign-signing

Sigstore Cosign keyless signing integration into the release pipeline — binary integrity verification via GitHub OIDC, transparency log recording, and public verification commands — Phase 2 of the release strategy
