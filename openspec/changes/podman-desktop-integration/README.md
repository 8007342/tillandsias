# podman-desktop-integration

Add podman cheatsheets (general, desktop for Windows/macOS). Handle podman machine init and machine start for macOS/Windows. Investigate silent non-interactive install of Podman Desktop in user space without tray icon during curl app install. Cheatsheets needed for machine lifecycle management.
