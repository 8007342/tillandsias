## Tasks

- [x] 1. Add `gh auth setup-git` to entrypoint.sh (Phase 1)
- [x] 2. Configure opencode.json deny rules for ~/.config/gh and ~/.config/tillandsias-git (Phase 2)
- [ ] 3. Manual: rebuild forge image, verify `git push --dry-run` works without prompt
- [ ] 4. Manual: verify OpenCode cannot `cat ~/.config/gh/hosts.yml`
