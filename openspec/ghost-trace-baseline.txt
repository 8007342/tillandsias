# ghost-trace baseline (openspec/ghost-trace-baseline.txt) — the RATCHET's floor.
# History: 2026-08-16 seven union-gate ghosts (767-yrnd) fixed and pruned; 2026-08-23 the
# yaml rung (867-vd4z, lenovinha) grew it once by six; 2026-08-25 eight pruned (740-k8ef);
# 2026-09-02 the markdown rung grew it once by fourteen, and the 658-kcd5 cleanup the same
# day re-pointed or dropped every cheatsheet/doc/litmus trace to the spec that owns it
# (agent-source-of-truth -> knowledge-source-of-truth x15, chromium-browser-isolation ->
# browser-isolation-core x5, container-health -> podman-idiomatic-patterns x4, ... and
# docs examples that spelled real annotations with made-up ids now cite real specs).
# The baseline may only shrink from here; what remains is listed with its reason.
# ephemeral-guarantee: three litmus tests (forge-experts-ephemerality-shape,
#   forge-experts-teardown-ephemeral, init-log-cleanup) declare it as their OWN spec and
#   bind under it; the spec FILE is owed — writing it (or re-homing the three) is a spec
#   decision, not a cleanup. Every other trace to it now names the test's real spec.
ephemeral-guarantee
