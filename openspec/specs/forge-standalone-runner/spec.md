<!-- Tombstoned: superseded by spec:forge-standalone -->
# forge-standalone-runner Specification

## Status

obsolete

## Purpose

This document is retained for history only. The canonical standalone forge
launcher contract now lives in `openspec/specs/forge-standalone/spec.md`.

## Redirect

- Use `spec:forge-standalone` for the active launch contract.
- Use `run-forge-standalone.sh` for the current implementation.

