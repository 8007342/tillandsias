<!-- @tombstone superseded:browser-isolation-tray-integration+tray-app -->
<!-- @trace spec:browser-daemon-tracking -->
# browser-daemon-tracking Specification

## Status

obsolete

## Tombstone

`browser-daemon-tracking` was the old browser-container lifecycle bucket.
The live browser session and cleanup contract now live in
`browser-isolation-tray-integration` and tray lifecycle management in
`tray-app`.

Historical trace links are retained for archive consumers only.

## Superseded By

- `openspec/specs/browser-isolation-tray-integration/spec.md`
- `openspec/specs/tray-app/spec.md`
