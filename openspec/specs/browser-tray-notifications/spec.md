<!-- @tombstone superseded:tray-progress-and-icon-states+tray-app+browser-isolation-tray-integration -->
<!-- @trace spec:browser-tray-notifications -->
# browser-tray-notifications Specification

## Status

obsolete

## Tombstone

`browser-tray-notifications` was superseded by the tray progress chip, tray
app menu state, and the secure browser launch flow. Browser launch feedback
now lives in `tray-progress-and-icon-states` and `tray-app`, while the live
browser/session launch path is owned by `browser-isolation-tray-integration`.

Historical traces are retained for provenance only.

## Superseded By

- `openspec/specs/tray-progress-and-icon-states/spec.md`
- `openspec/specs/tray-app/spec.md`
- `openspec/specs/browser-isolation-tray-integration/spec.md`
