<!-- @tombstone superseded:default-image+build-script-architecture -->
<!-- @trace spec:windows-cross-build -->
# windows-cross-build Specification

## Status

status: obsolete

## Tombstone

`windows-cross-build` described a cross-platform wrapper and `cargo-xwin`
workflow that is not implemented in the current repository. The live build
contracts now live in `default-image`, `nix-builder`, `dev-build`, and the
current Linux-first tooling.

Historical trace links are retained for archive consumers only.

## Superseded By

- `openspec/specs/default-image/spec.md`
- `openspec/specs/build-script-architecture/spec.md`
