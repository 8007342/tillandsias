<!-- @tombstone superseded:default-image+forge-hot-cold-split+tools-overlay-fast-reuse -->
<!-- @trace spec:layered-tools-overlay -->
# layered-tools-overlay Specification

## Status

obsolete

## Purpose

Historical overlay capability retained for traceability only. The live
tool lifecycle is now owned by `default-image`, `forge-hot-cold-split`,
and `tools-overlay-fast-reuse`.

## Notes

- The operative requirements have been removed.
- Keep the trace links readable, but do not add new requirements here.
