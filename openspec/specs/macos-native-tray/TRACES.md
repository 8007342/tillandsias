# macos-native-tray traces

<!-- Placeholder. Traces will be populated by `./scripts/generate-traces.sh` after the implementation (Phase 5) lands annotated source code referencing `@trace spec:macos-native-tray`. -->
