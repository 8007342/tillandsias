<!-- @tombstone installer on hold: curl-installer-tray-only -->
obsolete

Historical installer progress UX. The installer is currently on hold; the remaining live install path is tray-specific and will be revisited only if/when the headless distribution shape stabilizes.
