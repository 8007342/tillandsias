# windows-native-tray traces

<!-- Placeholder. Traces will be populated by `./scripts/generate-traces.sh` after the implementation (Phase 4) lands annotated source code referencing `@trace spec:windows-native-tray`. -->
