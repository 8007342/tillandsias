<!-- @tombstone superseded:default-image+nix-builder+dev-build -->
<!-- @trace spec:build-script-architecture -->
# build-script-architecture Specification

## Status

status: obsolete

## Tombstone

`build-script-architecture` described the old portable/AppImage split between
local and clean builds. The live build architecture now lives in
`default-image`, `nix-builder`, `dev-build`, and the current `build.sh` /
`scripts/build-image.sh` flow.

Historical trace links are retained for archive consumers only.

## Superseded By

- `openspec/specs/default-image/spec.md`
- `openspec/specs/nix-builder/spec.md`
- `openspec/specs/dev-build/spec.md`
