<!-- @tombstone superseded:host-browser-mcp -->
<!-- @trace spec:browser-debounce -->
# browser-debounce Specification

## Status

obsolete

## Tombstone

`browser-debounce` was an old duplicate of the browser launch throttle.
The live per-project safe-window gating now lives in `host-browser-mcp`.

Historical trace links are retained for archive consumers only.

## Superseded By

- `openspec/specs/host-browser-mcp/spec.md`
