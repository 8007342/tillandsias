<!-- @tombstone superseded:init-command+dev-build -->
<!-- @trace spec:init-command -->
# build-lock Specification

## Status

obsolete

## Purpose

Historical build-serialization helper. The live build coordination behavior is
now owned by `init-command` and the higher-level build orchestration specs.

## Superseded By

- `openspec/specs/init-command/spec.md`
- `openspec/specs/dev-build/spec.md`

## Notes

- This tombstone remains for archive readability.
- The current build-lock behavior is verified through the init/build chain
  rather than this standalone umbrella.
