# Generic Git Project Fixture

A minimal Node.js/Make project for generic project expert discovery testing.

## Build and Test
Run `npm test` or `make test` to execute tests.
Run `npm run build` or `make build` to compile the project.
