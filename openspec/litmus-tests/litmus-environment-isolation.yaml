name: litmus:environment-isolation
spec: environment-runtime
description: Verify forge sees ONLY minimal environment variables, no host env leakage
severity: high

preconditions:
  - tillandsias tray running
  - forge container available
  - host environment has multiple env vars set

critical_path:
  - step: "baseline: count host environment variables"
    command: "env | wc -l"
    timeout_ms: 2000
    expected_behavior: "host has 20+ env vars typically"

  - step: "execute forge container and count env vars"
    command: 'FORGE_IMAGE="${TILLANDSIAS_FORGE_IMAGE:-$(podman images --format '"'"'{{.Repository}}:{{.Tag}}'"'"' | grep '"'"'^tillandsias-forge:'"'"' | head -1)}"; test -n "$FORGE_IMAGE" && podman run --rm "$FORGE_IMAGE" env | wc -l'
    timeout_ms: 5000
    expected_behavior: "forge has 3-10 env vars only"

  - step: "list forge environment variables"
    command: 'FORGE_IMAGE="${TILLANDSIAS_FORGE_IMAGE:-$(podman images --format '"'"'{{.Repository}}:{{.Tag}}'"'"' | grep '"'"'^tillandsias-forge:'"'"' | head -1)}"; test -n "$FORGE_IMAGE" && podman run --rm "$FORGE_IMAGE" env | sort'
    timeout_ms: 5000
    expected_behavior: "minimal env vars"

  - step: "verify no host-specific vars in forge"
    command: 'FORGE_IMAGE="${TILLANDSIAS_FORGE_IMAGE:-$(podman images --format '"'"'{{.Repository}}:{{.Tag}}'"'"' | grep '"'"'^tillandsias-forge:'"'"' | head -1)}"; test -n "$FORGE_IMAGE" && podman run --rm "$FORGE_IMAGE" env | grep -E '"'"'DOCKER_HOST|SESSION_MANAGER|DBUS_SESSION_BUS_ADDRESS|XDG_'"'"' | wc -l'
    timeout_ms: 3000
    expected_behavior: "0 matches (no host session/display vars)"

  - step: "verify PATH is correctly set"
    command: 'FORGE_IMAGE="${TILLANDSIAS_FORGE_IMAGE:-$(podman images --format '"'"'{{.Repository}}:{{.Tag}}'"'"' | grep '"'"'^tillandsias-forge:'"'"' | head -1)}"; test -n "$FORGE_IMAGE" && podman run --rm "$FORGE_IMAGE" which cargo'
    timeout_ms: 5000
    expected_behavior: "/opt/cargo/bin/cargo or /usr/local/cargo/bin/cargo"

gating_points:
observability:
  traces:
    - "@trace spec:environment-runtime"
    - "@trace spec:forge-launch"
  expected_log: "Environment isolation verified: minimal env vars"
  log_fields:
    spec: "environment-runtime"
    test: "environment-isolation"
    forge_env_count: "3-10"

rollback:
  on_failure: "re-check forge image environment configuration"
  command: "podman inspect tillandsias-forge 2>/dev/null | grep -A 20 'Env' | head -20"
