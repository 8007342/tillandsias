trace: spec:methodology-accountability

event_id: methodology.event.011-closed-trend-minimalism@v1
status: distilled
observed_at: "2026-05-06T00:00:00Z"
reporter: "codex"
summary: >
  The trend dashboard still carried explanatory prose and residual bars that
  did not add information to the human scan path. The chart needed to collapse
  to a single readable title plus stacked closed-trend windows.

observed_signal:
  description: >
    The dashboard Markdown included per-window prose, residual bars, and a
    repeated section heading that made the visible chart harder to scan as the
    record count grew.
  signal_type: "dashboard"

expected_model:
  description: >
    The human view should foreground the closed-trend progression only. Any
    complementary data such as residual trend belongs in machine-readable JSON
    or downstream tooling, not in the scan-first Markdown surface.
  source_refs:
    - scripts/update-convergence-dashboard.sh
    - methodology/litmus-centicolon-wiring.yaml
    - methodology/proximity.yaml

violated_or_missing_claims:
  methodology_claim_ids:
    - dashboard_trends_must_prioritize_scan_readability
    - residual_data_should_not_clutter_human_trend_view
    - chart_titles_should_be_global_not_repeated_per_window
  spec_requirement_ids:
    - methodology-accountability.release_dashboard
  litmus_ids: []
  cheatsheet_refs:
    - docs/cheatsheets/centicolon-dashboard.md

affected_artifacts:
  specs:
    - openspec/specs/methodology-accountability/spec.md
  methodology_files:
    - methodology/event/index.yaml
    - methodology/proximity.yaml
  code_paths:
    - scripts/update-convergence-dashboard.sh
  scripts:
    - scripts/update-convergence-dashboard.sh
  dashboards:
    - docs/convergence/centicolon-dashboard.md
    - docs/convergence/centicolon-dashboard.json

evidence_refs:
  commits: []
  files:
    - scripts/update-convergence-dashboard.sh
    - docs/convergence/centicolon-dashboard.md
    - docs/convergence/centicolon-dashboard.json
  logs: []
  ci_runs: []
  external_sources: []

uncertainty_delta: low
provisional_classification: dashboard_presentation_gap

next_distillation_step:
  owner: "codex"
  action: >
    Keep the dashboard title global and the closed-trend chart compact, then
    verify the generated Markdown stays screen-fit as the signature log grows.
  target_artifact: "docs/convergence"

closure_criteria:
  - chart_has_one_global_title
  - window_blocks_stack_without_explanatory_prose
  - residual_trend_remains_machine_only
