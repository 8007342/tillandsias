# /etc/tillandsias/swap.conf — root-owned; read by the helper and the units.
# @trace order:1376-8zdz (design §9.6)
SWAP_DIR=/var/swap
SWAP_PRIORITY=10
SWAP_TIERS_GB=8:16:24
SWAP_RESERVE_GB=20
# SWAP_SIZE_GB=8   # uncomment to override the tier policy
