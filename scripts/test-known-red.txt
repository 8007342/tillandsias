# Tests allowed to be red on a clean tree (scripts/check-test-baseline.sh).
# Each entry needs a filed issue; an unexplained name here is how a baseline
# stops meaning anything.
#
# One key per line, `<test-binary-stem>::<test name as cargo printed it>` —
# the stem is the `Running … (target/debug/deps/<stem>-<hash>)` line above the
# results, hash stripped. A bare test name is ambiguous across binaries and
# would excuse a failure it was never meant to cover.
#
# THE LIST MAY ONLY SHRINK. The gate fails when a listed test PASSES, so an
# entry cannot outlive the defect it excuses. Deleting a line is the normal
# way this file changes; adding one is a deliberate act that needs the issue
# above it.
#
# MEASURED, and this is a per-host number: on Linux at f9ee195a9 (2026-08-21)
# `cargo test --workspace --no-fail-fast` is 1894 passed, 1 failed, 10 ignored
# — the one failure is the entry below. macOS's equivalent set was EIGHT
# (2026-08-20), and none of those seven others are listed here, so a macOS host
# adopting this gate will see them as new red until it measures and files its
# own.
#
# REMOVED 2026-09-04 — FIXED, not tolerated. Order 1022-px54.
#
# The entry below was here since 2026-08-22 as 845-7a88, described as "fails on
# any host without a working Squid launch in the test environment". That
# description was wrong about the cause, which is why it sat for two weeks: the
# test never launched Squid at all. It read images/proxy/entrypoint.sh as TEXT
# and asserted the literal `exec squid -N`, and commit 7292b097b (767-es4w)
# had changed that line to `exec /usr/local/bin/squid-supervisor squid squid -N`
# so a mid-service crash would be counted and restarted. The assertion drifted;
# no environment was ever required.
#
# It is now fixed at the assertion (pinning the supervised launch and the
# parse-before-serve ordering) rather than tolerated here. Removing the entry is
# the other half of the fix: this file's own warning says a stale entry "must not
# become the reason the file is ignored", and the gate enforces that — it refused
# with `stale=1` the moment the test went green, because a known-red that has
# started passing makes the NEXT break of that same test invisible.
